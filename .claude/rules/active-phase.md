# Active Phase Context

Phase: 28 — Corpus COMPLETENESS + grounded-coverage interview + streaming render + branding/compliance (M7). COMPLETE — all T1–T10 [x]; delivery accepted; committed 24e4a08. NO active phase (run /dev-plan for Phase 29).

Why: the user found the CORE defect Phase-27's assessment MISSED — verbatim red-flag EXTRACTION was grossly INCOMPLETE (the grounding gate checked each flag was REAL, never that we got them ALL; opioid doc shipped 15 of ~80). Plus: STOP fabricating coverage/data — ground it in a y/n/PARTIAL interview.

What shipped: complete re-extraction 634→903, dedup →**875 indicators** (every flag re-grounds through the BYTE-FROZEN gate); coverage GROUNDED in a 28-cap+20-data-source user interview (NOT fabricated; new `capability`/`data_source` per-indicator fields; 258 covered/191 partial/454 gap; 220 BUILD_NOW·147 SOURCE_DATA); 28 templated build_logic for the 220 BUILD_NOW; a FULL-MOTION STREAMING "agent reading" render (each phrase highlights as the read edge reaches it); FINTRAC attribution RELOCATED to a per-doc page footer (non-negotiable HELD, not removed); "AML Corpus Explorer" branding; de-piped markitdown tables. Harness 148→**165**; --check all 4/4 ZERO DRIFT; --selftest PASS; all 42 --check-derived clean; dist/corpus ~2.40MB.

Constraints held: the grounding core derive_signals.py (normalize/rf_region/check_record) BYTE-UNCHANGED; the showcase + every source md + every corpus-status.json + data/typology-map.json FROZEN byte-clean. NO non-negotiable change. NO fabricated numbers.

Phase-29 candidates (evidence-backed): an overlap/dedup guard at the extraction gate (faithfulness ≠ uniqueness — the sweep produced 28 dupes); harness fidelity for animated render paths (the staged-render blind spot); a capability-lens UI (the new tags are unused); revisit the "Illustrative" badge wording (coverage is now grounded).

Gates:
- [x] Direction confirmed by user (complete extraction + interview-grounded coverage + UX/branding/compliance; prove-then-full-sweep; 2026-06-07)
- [x] Delivery accepted (T1–T10 done; reviewed in-session; committed 24e4a08; 2026-06-07)
