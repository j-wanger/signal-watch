# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-06 by /dev-debrief (Phase 23 DELIVERED — FINTRAC depth: source #4 grown 3→10 [9 OAs + 1 Operational Brief] via the inverted "Indicators of <X>" anchor; all 5 tasks [x]; exit criteria GREEN; reviewer 9/10 ACCEPT — DELIVERED + accepted, committed b0fcda4)

## Recommended Next Action

**Phase 23 is DELIVERED + accepted + committed (b0fcda4, pushed to main); the demo is at Definition of Done
with a 4-source, 2-jurisdiction corpus (FinCEN advisories + alerts + OFAC + FINTRAC) — 42 derived across 46
publications.** Both gates are flipped (direction + delivery accepted; reviewer 9/10 ACCEPT, no CRITICAL/HIGH).
Run `/dev-plan` ONLY for a net-new stakeholder ask — there is no carried scope debt that the user has chosen
to defer to. Before presenting, walk `tests/smoke-checklist.md` (human-eye check, esp. a dense 57-row
build-recs screen for human-trafficking).

What landed this phase: FINTRAC source #4 grew 3→10 strategic-intel products (9 Operational Alerts + 1
real-estate Operational Brief) — "Canadian depth" for the demo's audience (a Canadian bank). The genuinely
new gate work was an INVERTED "Indicators of <X>" anchor (`_RF_HEADER_FINTRAC_INV`, two narrow branches +
boilerplate exclusion via a connector gate) — NOT the planned literal "TABLE OF INDICATORS" (a pre-impl
misread; the Brief heading was markitdown-fragmented). Regression-gated to 0/39 rf_region shift; the
grounding core (`normalize`/`check_record`) stayed byte-untouched; `build.py`/`corpus.html` UNCHANGED
(the 4-type menu/chips/count are data-driven). Honest yield: 225 indicators / 50 BUILD_NOW; full lists
kept (no per-doc caps, user-directed), only human-trafficking's 2016 Appendix dropped (a double-count of a
separate 2016 OA). NO non-negotiable change (depth within the Phase-22 Crown-copyright basis).

Standing watch-items for any next phase (soft, not committed): the corpus is now 46 publications in a flat
SELECT menu (a search/filter/group-by-source UI may help as it grows); `derive_signals.py` carries forward
+ inverted FINTRAC + OFAC + FinCEN anchor sets, so a 5th heading form should trigger the DEFERRED
source-scoped rf_region refactor; and the indicator-count range is now 16-57 — a human-eye smoke check
(`tests/smoke-checklist.md`) on a 57-row build-recs screen is worth doing before presenting. The always-on
"Illustrative data & outputs" badge + the source-aware Crown-copyright attribution stay; NEVER reproduce a
FINTRAC doc without its required attribution; NEVER fabricate a BUILD_NOW.

## Active Phase

**[[phase-23-fintrac-depth|Phase 23: FINTRAC depth — grow source #4 with the remaining anchorable strategic-intel products (Operational Alerts + Operational Briefs)]]** (status: complete — delivered + accepted, committed b0fcda4 2026-06-06; all 5 tasks [x], exit criteria GREEN, reviewer 9/10 ACCEPT)

Entry criteria: MET (Phase 22 complete + accepted + committed [6765d26 + 7f6bc22]; the corpus ships 35 derived
across 39 publications via the CORPUS_SOURCES registry across 4 sources [FinCEN advisories + alerts + OFAC +
FINTRAC]; FINTRAC is registered as source #4 with 3 derived OAs; the quote-grounding gate `normalize`/`check_record`
is source-agnostic + byte-frozen; the FINTRAC "indicators" anchor add [Phase 22] + the OFAC widening [Phase 21]
proved the narrow-global-anchor widening pattern holds with 0 regression; the FINTRAC Crown-copyright non-commercial
reproduction basis is already established in CLAUDE.md + HANDOFF — NO non-negotiable change this phase; user chose
FINTRAC depth at the goal gate [audience = a Canadian bank] + OAs + Briefs WITH the regression-gated widening at the
scope gate).
Exit criteria: ≥5 new FINTRAC strategic-intel products (OAs + the real-estate Operational Brief) acquired
(hand-curated index.json) → converted → derived via the inverted loop, each `--check-derived` clean, FINTRAC source
3 → ~8-11 · the rf_region anchors widened for the new FINTRAC heading forms (narrow, specific, 0-collision — e.g.
"TABLE OF INDICATORS" for the briefs) with ZERO FinCEN/OFAC/existing-FINTRAC regression (all 35 prior records
`--check-derived` clean, `--selftest` passes incl. new bidirectional FINTRAC fixtures, every prior md's rf_region
byte-unchanged) · `--check all` zero drift, the 3 other sources + the showcase byte-frozen · the harness counts
updated (FINTRAC bucket 3→N, total/derived) + a FINTRAC Brief walks the arc · README + CLAUDE counts updated; NO
non-negotiable change.

Progress: ~0% (planned; direction approved, implementation not started)

## Active Phase Contract

Phase: 23 - FINTRAC depth — grow source #4 with the remaining anchorable strategic-intel products (Operational Alerts + Operational Briefs)
Tasks: 5 (see tasks.md) — T1 (M) acquire + convert the candidate FINTRAC batch (hand-curated index.json of OA/Brief
`<slug>.pdf` URLs; reuse `acquire_fincen.py --source data/fintrac`) + per-doc rf_region triage + capture the
FinCEN+OFAC+existing-FINTRAC BASELINE · T2 (L) the GATE change — a NARROW, SPECIFIC, 0-collision anchor per new
FINTRAC heading form ("TABLE OF INDICATORS" for the briefs; an inverted "Indicators of ML/TF" form if found),
regression-gated: all 35 existing records clean + every prior md's rf_region byte-unchanged (NO-OP if T1 found no
new form) · T3 (M) derive the anchoring batch via the inverted loop (`--check-derived` clean; FINTRAC Crown-copyright
non-commercial provenance, NOT "public domain"; honest BUILD_NOW only where FI-observable; never fabricate) · T4 (S)
regen + rebuild + drift guard + extend the harness counts + a FINTRAC Brief walking the arc · T5 (S) docs (counts +
the Operational-Brief inclusion; NO non-negotiable change).
Transition: continue (lite). UNFREEZE: `scripts/acquire_fincen.py`, `data/fintrac/**` (grow source #4 — NEW docs +
NEW derived + corpus-status.json; the 3 EXISTING FINTRAC records stay byte-untouched), `scripts/derive_signals.py`
(anchors + fixtures only — grounding core untouched), `dist/corpus/index.html`, `tests/**`, `CLAUDE.md`, `README.md`.
NO structural build.py/corpus.html edit (menu/chips/count are data-driven — adding docs flows through the merge).
FROZEN byte-untouched: `index.html`, `config/**`, the 3 typology dists, `scripts/build.py` + `corpus.html` (no
structural edit), AND the THREE OTHER sources `data/fincen/**` + `data/fincen-alerts/**` + `data/ofac/**` (mds +
derived + corpus-status.json), AND the 3 EXISTING `data/fintrac/` records (grow, don't modify).
Abort: if a new heading form shifts ANY FinCEN/OFAC/existing-FINTRAC rf_region or fails ANY of the 35 existing
records (complexity contaminates the core — subtraction test fails), SKIP that doc (honest non-derivable) — do NOT
broaden a shifting anchor. NEVER reproduce a FINTRAC doc without its required Crown-copyright attribution; NEVER
fabricate a BUILD_NOW. If blocked >3 attempts, ask user: skip or abort.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| Phase 23 = FINTRAC depth — grow source #4 from 3 derived OAs to the full anchorable set of FINTRAC strategic-intel products (Operational Alerts AND Operational Briefs, incl. the real-estate brief), deriving everything that anchors + grounds (~5-8 new docs, source 3 → ~8-11, becoming the second-deepest source). NO new source, NO non-negotiable change (the FINTRAC Crown-copyright basis is established Phase 22), NO architecture / build.py / corpus.html STRUCTURAL change (menu/chips/count are data-driven). User chose "FINTRAC depth" over cross-corpus synthesis / presentation at the goal gate (audience = a Canadian bank — weight Canadian-relevant scale), and "OAs + Briefs WITH the regression-gated widening" over OAs-only-no-gate-touch / a source-scoped refactor at the scope gate. EXCLUDE FINTRAC's sector GUIDANCE indicator pages (compliance guidance, not strategic-intel — would blur the corpus identity) | high | 2026-06-06 |
| Gate approach = NARROW-GLOBAL-ANCHOR widening (the proven Phase-21/22 pattern), regression-gated to ZERO FinCEN/OFAC/existing-FINTRAC rf_region shift, NOT a source-scoped rf_region refactor (DEFERRED — a future option if heading forms proliferate). New FINTRAC heading forms get narrow, specific, 0-collision anchors ("TABLE OF INDICATORS" CONFIRMED this session for the real-estate brief [bare, ~40 indicators — the narrow ML/TF-qualified FINTRAC anchor will NOT match it]; an inverted "Indicators of ML/TF" form if found). The grounding core `normalize`/`check_record` stays byte-untouched. ABORT: skip-the-doc (honest non-derivable) if a form shifts any existing region; NEVER broaden a shifting anchor (subtraction test — complexity must not contaminate the core) | high | 2026-06-06 |
| Acquisition = HAND-CURATED (no FINTRAC crawler; crawl_fincen.py stays FinCEN-only). FINTRAC serves a PDF at `<page-slug>.pdf` → the existing acquire_fincen.py `_to_pdf_url` direct-download branch handles it with NO tweak (Phase 22 confirmed); pdf_to_md.py provenance is already source-aware (FINTRAC = Crown-copyright, never "public domain"). Candidate batch: human-trafficking/Project Protect (oai-hts-2021), OCSE/Project Shadow (exploitation), romance fraud/Project Chameleon, illegal wildlife trade, casino/Project Athena, the real-estate Operational Brief (real); the final set is finalized at acquisition by what anchors + grounds. Honest BUILD_NOW where FI-observable, NEVER fabricate | high | 2026-06-06 |
(Phase 22 decisions — FINTRAC as corpus source #4, the demo's FIRST cross-jurisdiction source (US Treasury FinCEN + OFAC → +Canada FINTRAC), via the VERBATIM-corpus path; a NARROW ML/TF-qualified "indicators" anchor add (`_RF_HEADER_FINTRAC`/`_RF_INTRO_FINTRAC`, 0-collision across all 36 mds, 0 FinCEN/OFAC shift); the verbatim non-negotiable extended to add FINTRAC as a Crown-copyright NON-COMMERCIAL reproduction LICENCE (NOT public domain — distinct from US-federal 17 USC §105; FINTRAC attribution required; not commercial redistribution; other non-US/non-FINTRAC still paraphrase) — user signed off "proceed"; hand-curated `<page>.pdf` acquisition (`_to_pdf_url` needed no tweak) + source-aware provenance + a 4-type SOURCE-AWARE menu. See the `[[2026-06-06-phase-22-fintrac-corpus-source|Ph22]]` journal.)
(Phase 21 decisions — OFAC as corpus source #3 WITH a regression-gated rf_region anchor widening ("Risk Indicators"/"Deceptive Practices"/"Risk Factors" + the risk-indicator intro; 0 FinCEN rf_region shift, all 29 records clean); the verbatim public-domain non-negotiable extended FinCEN-only → US-federal (17 USC §105 — FinCEN + OFAC + US federal/Treasury; FINTRAC still excluded then); hand-curated OFAC acquisition (JS-SPA, no crawl) via --source + a `_to_pdf_url` tweak + the parameterized issuer; honest 0-BUILD_NOW allowed (sanctions/vessel often non-FI-observable). See the `[[2026-06-06-phase-21-ofac-corpus-source|Ph21]]` journal.)
(Earlier Phase 20 decisions — scale via OTHER FinCEN publication TYPES (Alerts) inside the verbatim regime (NO non-negotiable change); the thin CORPUS_SOURCES registry decoupling source-id from storage dir (data/fincen/ byte-frozen, ready for {advisories, alerts, …ofac}); the gate reused UNCHANGED. See the `[[2026-06-06-phase-20-multi-source-spine|Ph20]]` journal.)
(Earlier Phase 18–19 decisions — see `[[2026-06-06-phase-19-durability-closeout|Ph19]]`, `[[2026-06-06-phase-18-corpus-explorer-arc|Ph18]]` journals. Load-bearing carry-overs: Ph19 DURABILITY CLOSEOUT — a committed ZERO-DEP Node `vm`+DOM-shim harness (`tests/corpus-explorer.test.mjs`, 28 assertions, NEVER jsdom — file:// offline ethos) that loads the COMMITTED `dist/corpus/index.html` so it doubles as a build-output smoke test (EXTEND this in Ph20 T5, don't replace); `_rf_triage` is PINNED + DISCLOSED NOT rewritten (an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today). Ph18 THE ARC — the corpus explorer's 5-screen arc (Select → Coverage → Build recs/GATE → Signal → Close the loop), div-toggle human gate folded into Build-recs (NOT `<input>` — keyboard-safe), COVERAGE close-the-loop payoff via `coverageIndex()` NOT precision combination-lift (precision-lift EXPLICITLY REJECTED — records carry no precision/lift numbers; porting it fabricates ~12 per-advisory stats, violating "never present synthetic numbers as real"). The Ph20 alert records reuse this arc unchanged per record.)
(Earlier Phase 16–17 decisions — see `[[2026-06-06-phase-17-complete-corpus-derivation|Ph17]]`, `[[2026-06-06-phase-16-invert-extraction|Ph16]]` journals. Load-bearing carry-overs: Ph17 the REAL subtraction — `extract_red_flags` + the whole `--scaffold`/`--draft`/`--scaffold-derived` authoring stack + `anthropic`/`os` DELETED (1202→600, stdlib-only); the inverted loop is the SOLE derivation path; `_rf_triage` counter reuses the `rf_region` span; corpus 12/14 live (only the 2 FATF non-derivable). Ph16 INVERTED the extraction boundary — the LLM EXTRACTS, the deterministic layer GATES (normalize(flag) ⊂ normalize(md) = the traceability authority, replacing src_line∈extractor) + a coarse `rf_region()` section-cite relevance guard + the cover×data matrix + BUILD_NOW⇒full-build_logic; corpus-status SHAPE preserved; `derivable: false` = the 2 FATF advisories.)
(Earlier Phase 12–15 decisions — see `[[2026-06-05-phase-15-harden-extraction-faithfulness|Ph15]]`, `[[2026-06-05-phase-14-scale-corpus-derivation|Ph14]]`, `[[2026-06-05-phase-13-corpus-explorer|Ph13]]`, `[[2026-06-05-phase-12-fincen-corpus-derivation|Ph12]]` journals. Load-bearing carry-overs: Ph15 harden-FAITHFULNESS (footnote-resume; glued-no-separator stays FLAGGED, no safe deterministic split; derived records store RAW text, `corpus.html` esc() the sole escaper); Ph14 PURE-AUTHORING scaling (build.py makes an advisory live by the presence of `derived/<id>.json`); Ph13 standalone STAGED 4-screen corpus explorer (`dist/corpus/` via `corpus.html`, all-14-honest status, six-act showcase byte-frozen); Ph12 SINGULAR corpus-backed demo (user picks 1 of 14; per-advisory `derived/*.json` the analytical artifact, the 3 typologies stay the showcase), build.py DECOUPLED from authoring (reads committed corpus-status.json + derived/*.json, never imports derive_signals.py), LLM backend = THIS session not `--draft`, validate at the build boundary.)

(Earlier Phase 7–11 decisions: see `[[phase-11-automated-derivation]]`, `[[phase-10-fincen-corpus-crawler]]`, `[[phase-08-doc-true-up]]` + journal. Load-bearing carry-overs: Phase 11 AUTOMATE → variant B (LLM-drafted signal NOW), boundary-preserving (LLM proposes; build.py + schema + 2 human gates dispose); authoring-time vs ship-artifact split (build-time tools; ship file stays single-file/offline/no-fetch, HANDOFF §4/§4.5); validate at the build boundary (deterministic validator, fail loud); FinCEN-only verbatim public-domain exception (17 USC §105, NOT FINTRAC); lite ceremony.)

## Blockers and Open Questions

- [RESOLVED 2026-06-06 · planning] Phase-23 direction → **FINTRAC depth — grow corpus source #4 from 3 derived Operational Alerts to the full anchorable set of FINTRAC strategic-intelligence products carrying enumerated ML/TF indicators (Operational Alerts AND Operational Briefs, incl. the real-estate brief)**, deriving everything that anchors + grounds (~5-8 new docs → source 3 → ~8-11, the second-deepest source). User chose **FINTRAC depth** over cross-corpus synthesis / presentation at the goal gate (audience = a Canadian bank — weight Canadian-relevant scale per the carried memory), and **OAs + Briefs WITH the regression-gated narrow-anchor widening** over OAs-only-no-gate-touch / a source-scoped rf_region refactor at the scope gate. NO new source, NO non-negotiable change (the FINTRAC Crown-copyright non-commercial reproduction basis was established in Phase 22), NO architecture / build.py / corpus.html STRUCTURAL change — the SELECT menu, doc_type chips, and count line are already data-driven, so new docs flow through the merge automatically (only the harness's hardcoded counts + the docs' counts update). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core `normalize`/`check_record` stays BYTE-UNTOUCHED — only the rf_region relevance anchors widen. THE ONE REAL RISK — heterogeneous heading forms: the 3 derived OAs head their lists "Money laundering indicators" (ML/TF-qualified → anchors under the existing narrow `_RF_HEADER_FINTRAC`), but the real-estate Operational BRIEF was CONFIRMED this session to head its ~40-indicator list with bare "TABLE OF INDICATORS", which the deliberately-narrow ML/TF-qualified FINTRAC anchor will NOT match → T2 carries a REGRESSION-GATED anchor widening per new heading form discovered (e.g. "TABLE OF INDICATORS"; possibly an inverted "Indicators of ML/TF" form), each NARROW + SPECIFIC + verified 0-collision across all 36 FinCEN+OFAC mds, every existing rf_region BYTE-UNCHANGED, all 35 existing records `--check-derived` clean. Acquisition is HAND-CURATED (FINTRAC serves a PDF at `<page-slug>.pdf` → the existing `_to_pdf_url` direct-download branch handles it with NO tweak, Phase-22 confirmed; pdf_to_md.py provenance already source-aware); EXCLUDE FINTRAC's sector GUIDANCE indicator pages (compliance guidance, not strategic-intel — would blur the corpus identity). KNOWLEDGE GAPS: none unfilled (the FINTRAC catalog, the real-estate brief's "TABLE OF INDICATORS" heading form, and acquisition feasibility all verified this session). ABORT: if a new form shifts ANY FinCEN/OFAC/existing-FINTRAC rf_region or fails ANY of the 35 existing records, SKIP that doc (honest non-derivable) — NEVER broaden a shifting anchor (subtraction test). NEVER reproduce a FINTRAC doc without its required Crown-copyright attribution; NEVER fabricate a BUILD_NOW; the always-on badge + the source-aware verbatim attribution stay; data/fincen/ + data/fincen-alerts/ + data/ofac/ + the showcase + the 3 EXISTING FINTRAC records stay byte-frozen (grow, don't modify)
- [RESOLVED 2026-06-06 · planning] Phase-22 direction → **add FINTRAC (Canada's FIU) as corpus source #4 — the demo's FIRST cross-jurisdiction source (US Treasury FinCEN + OFAC → +Canada FINTRAC), via the VERBATIM-corpus path** (user chose FINTRAC over the 3 documented next-source directions — "this demo is for a Canadian bank after all" — and verbatim-corpus over paraphrase-corpus / paraphrase-showcase). What's genuinely NEW vs the OFAC source-#3 add: (1) a DIFFERENT compliance basis — Crown-copyright NON-COMMERCIAL reproduction LICENCE (per FINTRAC's Terms & Conditions), explicitly NOT public domain (the US-federal 17 USC §105 basis is no-copyright-at-all); requires FINTRAC's specific attribution (© His Majesty the King in Right of Canada + complete title + author + "a copy of the version available at <URL>"), is NOT commercial redistribution, and all other non-US/non-FINTRAC sources still paraphrase (user signed off "proceed"); (2) a gate vocab widening for FINTRAC's "indicators" heading (vs FinCEN "red flags" / OFAC "risk indicators"), deliberately NARROW (ML/TF-specific `_RF_HEADER_FINTRAC`/`_RF_INTRO_FINTRAC`) to avoid shifting fin-2020-a008's "Financial/Behavioral Indicators" region — verified 0 collisions across all 36 existing mds; (3) SOURCE-AWARE attribution rendering (FINTRAC must NEVER show the US-federal "public domain" string). Acquisition is HAND-CURATED (FINTRAC OAs are HTML pages WITH a PDF version → data/fintrac/index.json lists the OA PDF-version URLs, reusing acquire/pdf_to_md via --source + the OFAC `_to_pdf_url` handling; crawl_fincen.py stays FinCEN-only). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted loop + the gate; the grounding core `normalize`/`check_record` stays byte-untouched. KNOWLEDGE GAPS: none unfilled (compliance basis, OA structure/~30–35 enumerated indicators, and acquisition feasibility all verified this session via the FINTRAC Terms & Conditions + a real OA page). ABORT: if the widening shifts ANY FinCEN/OFAC rf_region or fails ANY of the 32 existing records, REVERT + narrow the anchor; T2 carries a convert-one-first rf_region + 0-baseline-shift CHECKPOINT. NEVER reproduce a FINTRAC doc without its required attribution; NEVER fabricate a BUILD_NOW; the always-on badge + the verbatim source attribution stay; data/fincen/ + data/fincen-alerts/ + data/ofac/ + the showcase stay byte-frozen (prove source #4 via the MERGE)
- [RESOLVED 2026-06-06 · planning] Phase-21 direction → **add OFAC as corpus source #3 WITH a regression-gated rf_region anchor widening** (user chose "widen the gate" over a small-clean-source / holding OFAC). Q1 (the load-bearing risk — do OFAC docs ground under the EXISTING anchors?): the red-flag-TEMPLATE OFAC docs already ground UNCHANGED (Sham Transactions rf_region (82,236), triage clean·26), but most OFAC docs use "Risk Indicators" (VC guidance), "Deceptive Practices" (maritime), or defer to a co-issued FinCEN advisory (ransomware → no list). rf_region is hard-coupled to the literal "red flag(s)" phrase (4 regexes) and `check_record` treats `rf_region==None` as a HARD violation → **widen the anchor set** to also match "Risk Indicators"/"Deceptive Practices"/"Risk Factors" headings (+ the risk-indicator intro), GATED by ZERO FinCEN regression (verified low risk: "risk indicators" 0 FinCEN mds, "deceptive practic" 0, "risk factors" 1; 0 derived records mention them → the new anchors are ~inert for FinCEN, rf_regions stay byte-identical). ABORT: if the widening shifts any FinCEN region or fails any of the 29 records, REVERT + fall back to the small-clean-source (red-flag-template OFAC only). Q2 (acquisition): OFAC's site is a JS SPA with no static listing → **hand-curated** `data/ofac/index.json` from `/media/<id>/download` PDFs; `acquire_fincen.py`/`pdf_to_md.py` reused via `--source data/ofac` + a `_to_pdf_url` direct-download tweak (absolute `/media/.../download` or `/system/files/` URL = zero-hop); crawl_fincen.py stays FinCEN-only; the `corpus_status_records` issuer parameterized (FinCEN / OFAC). Q3 (proof-batch size): ≥3 OFAC docs (the registry mechanics are already proven; OFAC is proving the agency-extension + the gate widening + the non-negotiable change). Q4 (compliance): extend the verbatim non-negotiable FinCEN-only → US-federal (17 USC §105 — FinCEN + OFAC + US-federal/Treasury) in CLAUDE.md + HANDOFF, STILL excluding FINTRAC (Canadian Crown copyright) + any non-US/non-government source; user signed off. OFAC content is sanctions/vessel-oriented → records may be honestly 0-BUILD_NOW (non-FI-observable); NEVER fabricate a BUILD_NOW; the always-on badge + verbatim public-domain attribution stay; the two existing FinCEN sources + the showcase stay byte-frozen (prove source #3 via the MERGE)
- [RESOLVED 2026-06-06 · planning] Phase-20 direction → **scale the corpus beyond FinCEN *advisories* to other FinCEN *publication types* (Alerts first) via a THIN multi-source SOURCES registry — staying entirely inside the verbatim public-domain regime so NO non-negotiable change** (still FinCEN, still 17 USC §105, still verbatim; the quote-grounding gate is reused UNCHANGED). User reframed at the gate ("try FinCEN articles first, other than advisories") — chose OTHER FinCEN publication types over other agencies (OFAC — documented next-source candidate) / a new showcase typology. Architecture: a thin SOURCES registry decouples source-id from storage dir; `build.py`'s `render_corpus` iterates sources, reads each source's `corpus-status.json` + `derived/*.json`, merges by id into one `__CORPUS__`; `derive_signals.py --corpus-status` takes an optional source path. ZERO churn on what works: `data/fincen/` stays the `fincen-advisories` source BYTE-FROZEN (prove multi-source via the MERGE, not a migration — the subtraction test rejects renaming 14 mds + 12 derived + corpus-status); alerts land in a NEW `data/fincen-alerts/`. Proof = ~5 alerts grounded via the inverted loop (LLM extracts, `--check-derived` disposes); the remaining alerts are cheap follow-on. Open sub-question, resolved at impl by the T2 CHECKPOINT: do alert PDFs convert to a groundable red-flag region? — convert ONE alert and confirm `rf_region(md)` is not None before committing the batch; degrade path = pivot to FinCEN Financial Trend Analyses (indicator-heavy) or derive fewer-but-deeper. If the registry can't merge without growing as complex as a per-source migration (subtraction MOVES not shrinks), narrow to advisories-only + a registry stub. NEVER fabricate; the always-on badge + the verbatim public-domain attribution stay
- [RESOLVED 2026-06-06 · planning] Phase-18 direction → **give the corpus explorer (`dist/corpus/`, from `corpus.html`) a dramatic 5-screen arc — Select → Coverage → Build recs/GATE → Signal → Close the loop — mirroring the showcase's Act 3 (human gate) + Act 6 (loop closes), grounded ENTIRELY in existing data, NO fabricated numbers**. User chose the corpus-explorer arc over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate. The payoff is COVERAGE close-the-loop (the existing `coverageIndex()` recompute with picked gaps flipped to covered), NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (the corpus records carry no precision/lift numbers; porting it would fabricate ~12 per-advisory illustrative stats, violating the "never present synthetic numbers as real" non-negotiable). Scope contained to `corpus.html` (+ dist/corpus rebuild + docs): the arc reuses existing fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card) → NO schema/data/build.py change; the gate uses div-toggles (NOT `<input>`) to avoid keyboard-nav collision + preserve determinism; the gate folds into the existing Build-recs screen (subtraction test). Open sub-question, resolved at impl: can interactive selection be made keyboard-safe + deterministic without growing complex? Abort/degrade path = close-the-loop-only (no gate; coverage close over ALL BUILD_NOW — the payoff still lands); if the coverage math can't be honest, keep the close screen informational; NEVER fabricate precision/lift
- [RESOLVED 2026-06-06 · planning] Phase-17 direction → **complete the corpus derivation (7/14 → 12/14) + the REAL subtraction (delete `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack), unblocked by a cheap `rf_region`-bounded triage counter**. User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table (it would leave un-regenerable stale triage). The debrief had the dependency backwards — `extract_red_flags` is off the correctness path but is the SOLE producer of the extraction/flag_count triage the byte-frozen `corpus.html` renders; the ~12-line counter (reuses the existing `rf_region` span) replaces that role, keeps `--corpus-status` regenerable, and is materially smaller. Subtraction-FIRST ordering: delete + counter + reshape (T1), prove the 7/14 still builds clean on the stripped spine, then scale (T2–T4). Open sub-question, resolved at impl: can the `rf_region`-bounded counter preserve corpus-status.json's SHAPE / pass build.py validation without growing as complex as `extract_red_flags`? Abort path = STOP the deletion, keep `extract_red_flags`, narrow to scale-only (subtraction test must SHRINK complexity, not move it). Degrade-path for a LOW/glued advisory that won't ground faithfully = scale-what-passes (the deletion still lands — counter-gated, not scale-gated)
- [RESOLVED 2026-06-06 · planning] Phase-16 direction → **INVERT the extraction architecture** (the LLM extracts candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE — normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority — + a cheap section-cite relevance guard) **+ scale as proof to 7/14** (COVID-EIP fin-2021-a002 + ≥1 previously-LOW/glued advisory). User chose this over pure breadth-scaling / a structure-preserving converter (DISSOLVED). Decisions A (groundedness + section-cite, not pure-groundedness, not a full parser), B (demote `extract_red_flags`, preserve corpus-status SHAPE, derivable false only for the 2 FATF advisories), C (2 records as proof over count) confirmed. Open sub-question, resolved at impl: can `normalize()` ground the escrow STRESS case (header-glued + hyphen-truncated raw L499) without becoming as complex as the extractor it replaces? Abort path = keep quote-grounding for the clean records, leave `extract_red_flags` as-is for the rest, narrow breadth to COVID-EIP only (subtraction test must SHRINK complexity, not move it)
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus | 2026-06-06 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>` | 2026-06-06 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (621KB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips) | 2026-06-06 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived, BYTE-FROZEN) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored | 2026-06-06 |
| tests/corpus-explorer.test.mjs | Zero-dep Node DOM-shim arc harness — Phase 23 extended to 74 assertions (4-type menu + an alert, an OFAC advisory, a FINTRAC OA, AND a FINTRAC Operational Brief each walking the 5-screen arc; Crown-copyright + Brief-subtype asserts) | 2026-06-06 |

## Session Journal (last 5)

- [2026-06-06] [[2026-06-06-phase-23-fintrac-depth|Phase 23 FINTRAC depth — Operational Alerts + Operational Briefs (3→10) (M7)]] (lite, 5 tasks, READY FOR COMPLETION — all 5 [x], exit criteria GREEN; reviewer 9/10 ACCEPT, no CRITICAL/HIGH) — grew FINTRAC source #4 from 3 derived Operational Alerts to **10** strategic-intel products (9 OAs + 1 real-estate Operational BRIEF) — "Canadian depth" for the demo's audience (a Canadian bank). User chose FINTRAC depth at the goal gate over cross-corpus synthesis / presentation, and OAs+Briefs WITH the regression-gated widening over OAs-only / a source-scoped refactor (DEFERRED). Reused the Phase-20 registry + inverted loop + quote-grounding gate; grounding core `normalize`/`check_record` byte-UNTOUCHED; `build.py`/`corpus.html` UNCHANGED (4-type menu/chips/count are data-driven). **T1** acquired+converted 7 NEW docs via `--source data/fintrac` (`acquire_fincen.py`/`pdf_to_md.py` needed NO edit — confirmed the Phase-22 finding); index.json 3→10; triage = 4 anchor as-is, 3 inverted-form; captured the 39-md BASELINE. **T2 (L, the gate change)** the PLANNED literal "TABLE OF INDICATORS" anchor was a MISREAD — the real-estate Brief heading is markitdown-FRAGMENTED ("4. INDICATORS OF MONEY" / "LAUNDERING IN REAL ESTATE"); the real surface is the INVERTED form. Added `_RF_HEADER_FINTRAC_INV` (two narrow branches: (a) "of <ML/TF>" with a CONNECTOR-gated trailing clause + `:?$` that EXCLUDES the boilerplate "Indicators of <ML/TF> can be thought of as red flags …" opening the 3 existing OAs; (b) "relating to | associated with <topic>"). REGRESSION CLEAN: **0 of 39** rf_regions shifted, all 35 prior records clean, `--selftest` +4 bidirectional fixtures. **T3** 7 derivations via the inverted loop (one subagent per doc, self-gated then re-checked): **225 ind / 50 BUILD_NOW** (human-trafficking 57/18, OCSE 44/4 [SOURCE_DATA-heavy], romance-fraud 28/5, wildlife 16/2, professional-ml 26/7, cannabis 21/8 [BUILD_NOW-rich], real-estate 33/6). FAITHFULNESS FIX: dropped human-trafficking's 2016 Appendix (39 ind — md L554 reproduces a SEPARATE 2016 OA → cross-doc double-count), kept the 57 genuine 2021 primary; KEPT full lists, NO per-doc caps (user "keep it"). FINTRAC OAs are far denser than FinCEN advisories (house norm ≤24; new range 16-57). **T4** regen corpus-status (10 clean) + rebuilt `dist/corpus` (432KB→621KB; 46 publications, 42 derived); harness 61→74 incl. a dedicated real-estate Brief full-arc walk. **T5** CLAUDE.md + README.md counts 39→46 / 35→42 / 3→10 + a Phase-23 bullet/paragraph; NO non-negotiable change (depth within the Phase-22 basis). All gates GREEN: `--check all` 4-artifact ZERO DRIFT, `--selftest` PASS, all 42 derived clean, harness 74/74, 0/39 rf_region shift, frozen set byte-clean. Corpus now **42 derived across 46 publications, 4 sources, 2 jurisdictions**. Review gate: unified reviewer 9/10 ACCEPT, two LOW (real-estate rf_region opens at the cover title → over-inclusive but 0/33 flags ground there, safe-by-design; the inverted anchor's branches are line-prefix-broad, 0 regressions) — both grounding-backstopped, NO ship fix. ESCAPE HATCHES: 2 DISCOVERY (inverted heading form vs the planned literal; the 2016-appendix faithfulness drop) + 1 USER OVERRIDE (keep full indicator volume, no caps). Committed b0fcda4; delivery gate accepted. **Retro TRIGGERED** (20 completed phases; 20 % 5 == 0): 0 recurring blockers, 0 reversals (the anchor was a corrected misread, not a reversal); 1 high-signal — the user steers toward SCALE/depth + HONESTY over showcase-polish (now well-modelled in memory); rising cost = anchor accretion → a 5th heading form triggers the deferred source-scoped refactor. SOFT OBS: 46-publication flat SELECT menu (search/filter UI Phase N+1) · indicator range 16-57 (smoke-check a 57-row build-recs screen before presenting) · FINTRAC's anchorable set may extend further but 10 is substantial depth — diminishing returns; /dev-plan only for a net-new ask.
- [2026-06-06] [[2026-06-06-phase-22-fintrac-corpus-source|Phase 22 FINTRAC as corpus source #4 (first cross-jurisdiction source; gate widened for FINTRAC "indicators" vocab; Crown-copyright non-commercial reproduction) (M7)]] (lite, 6 tasks, DELIVERED + accepted + committed 6765d26 + 7f6bc22; reviewer 9/10 ACCEPT, no CRITICAL/HIGH) — the demo's FIRST move beyond US-federal: FINTRAC (Canada's FIU) as the 4th corpus source, the first cross-jurisdiction source (US Treasury FinCEN+OFAC → +Canada FINTRAC). User chose the VERBATIM-corpus path over paraphrase-corpus/paraphrase-showcase ("this demo is for a Canadian bank after all"). **T1 (L)** widened the rf_region anchors for FINTRAC vocab — `_RF_HEADER_FINTRAC` (standalone ML/TF-QUALIFIED "indicators" line) + `_RF_INTRO_FINTRAC`; deliberately NARROW (fin-2020-a008 uses "Financial/Behavioral Indicators"); 0× across all 36 mds → 0 rf_region shift, all 32 records clean. The `corpus_status_records` issuer + a per-source `licence` parameterized. **T2** hand-curated acquisition (`_to_pdf_url` needed NO tweak); DISCOVERY: `pdf_to_md.py` provenance made source-aware. **T3** 3 OAs via the inverted loop (42 ind / 11 BUILD_NOW; TF honestly SOURCE_DATA-heavy; synthetic-opioids = the Canadian fentanyl counterpart). **T4–T6** build.py registry source #4 (doc_type "FINTRAC"); corpus.html 4-type SOURCE-AWARE menu (3 blanket "public domain" claims corrected); harness 49→61; the verbatim non-negotiable extended "ONE exception" → "TWO verbatim bases" (US-federal public domain + FINTRAC Crown-copyright non-commercial licence) identically in CLAUDE.md + HANDOFF.md. Phase 23 (this session) then grew FINTRAC 3→10.
(Earlier entries — Phase 21 OFAC-corpus-source ([[2026-06-06-phase-21-ofac-corpus-source|journal]], impl 68ee1dc + gate f61f92c) — OFAC (US Treasury) as the 3rd corpus source, the demo's first move beyond a single agency, WITH a regression-gated rf_region widening (`_RF_HEADER_OFAC`/`_RF_INTRO_OFAC`; 0 FinCEN shift); 3 derivations (19 ind / 4 BUILD_NOW; maritime SOURCE_DATA-heavy); verbatim non-negotiable extended FinCEN-only → US-federal (17 USC §105). Phase 19 durability-closeout ([[2026-06-06-phase-19-durability-closeout|journal]], impl ab0739a + gate 32185dc) — committed the ZERO-DEP Node `vm`+DOM-shim corpus-explorer harness (28 assertions, NEVER jsdom; loads the COMMITTED dist → doubles as a build-output smoke test) + PINNED `_rf_triage` (a glued `--selftest` fixture + disclosure, NO output change — an accurate counter reintroduces the Ph17-deleted parser). Phase 18 corpus-explorer-arc ([[2026-06-06-phase-18-corpus-explorer-arc|journal]]) — gave the corpus explorer the showcase's two missing beats: a 5-screen ARC (Select → Coverage → Build recs/GATE → Signal → Close the loop), the BUILD_NOW rows as div-toggle human gate (NOT `<input>` — keyboard-safe), the COVERAGE close-the-loop payoff via `coverageIndex()` NOT precision combination-lift (precision-lift EXPLICITLY REJECTED — fabricates ~12 per-advisory stats); corpus.html-only scope, dist/corpus rebuilt, `--check all` zero drift. Phase 17 complete-corpus-derivation ([[2026-06-06-phase-17-complete-corpus-derivation|journal]]) — the REAL subtraction: deleted `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` stack + `anthropic`/`os` (1202→600, stdlib-only); the ~14-line `_rf_triage` counter replaced the extractor's triage role; scaled 7→12/14 via the inverted loop (only the 2 FATF non-derivable); `--check all` zero drift; RETRO Dim 3 = the recurring user override toward scale/architecture over the showcase-debt true-up. Phase 16 invert-extraction (LLM extracts → deterministic groundedness gate disposes; one `normalize()` rule absorbed the closed FinCEN-md artifact set; ransomware previously-unreachable glued advisory derived; 5/14→7/14; SOFT OBS that the file grew → Phase-17 deletion), Phase 15 harden-extraction-faithfulness (footnote-resume + esc() sweep), Phase 14 scale corpus derivation (2/14→5/14, pure authoring), Phase 13 corpus explorer THE PAYOFF (`dist/corpus/` staged 4-screen, the artifact Phase 18 gave an arc), Phase 12 corpus-derivation foundation, Phase 11 automated derivation, Phase 9 build-drift guard, Phase 8 doc true-up, M6 pipeline walking skeleton, M5 ship, M3/M2/M1 — see `index.md` journal list + their journal articles. Trimmed to last 2 full entries per the _CURRENT_STATE size budget overflow rule.)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN/E-23. Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
