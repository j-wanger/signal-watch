# Active Phase Context

Phase: 26 - Elevate the corpus demo to showcase quality — register re-translation + progressive render + build/lift wow beats + grouping/sort + landing (M7) — SHIPPED 2026-06-07; all 6 tasks [x]; exit criteria met; reviewer 10/10 ACCEPT. Delivery gate pending acceptance.

Objective: elevate the corpus demo to the six-act showcase's bar (the five gaps the user named on the BUILT Phase-25 artifact): prose-register red flags, a static article screen, a "doesn't wow" Signal screen, ungrouped docs/red flags, no landing page.

Delivered: all 42 docs' `red_flag` re-translated to the fentanyl/elder AML-indicator register via an 84-agent workflow + a byte-surgical applier (verbatim `flag` + grounding byte-untouched, only `red_flag` VALUES; 634/634 distinct 12–240-char) · progressive "agent reading" article render · Select source-grouped + newest-first · Coverage red-flag section sub-grouping · a build-log (REAL `build_logic`) + a NEW Combination-lift screen (a GENERIC illustrative template behind a loud "illustrative · pending calibration — NOT measured on this document" tag) · a story-driven landing page. Per-doc arc 5→6 detail screens; harness 108→139; dist/corpus 2.19MB→2.17MB.

Key constraints (held): the showcase (index.html + config/typologies/** + the 3 dists) + every source md + every corpus-status.json + data/typology-map.json + the grounding core derive_signals.py + the authoring scripts byte-FROZEN. The combination-lift template is GENERIC + loud-illustrative-badged, never per-doc fabricated, never presented as real. NO non-negotiable change.

Gates run: `--check all` 4/4 ZERO DRIFT · `--selftest` PASS · harness 139/139 · 42/42 records `--check-derived` clean · frozen set byte-clean · reviewer 10/10 ACCEPT.

Standing follow-ups: a story-driven LANDING for the six-act SHOWCASE (index.html — roadmapped, needs unfreezing) · the user supplies real backtested combination-lift figures to replace the illustrative template (no date set).

Abort rule (was): register-unhittable → report before T2 (cleared at T1); a wow beat needing a fabricated per-doc finding → CUT to the generic template (held). Blocked >3 attempts → ask the user: skip or abort.

Gates:
- [x] Direction confirmed by user (all-in elevation, workflow-driven, illustrative-badged wow numbers; the register rubric + the honesty safeguard signed off; 2026-06-07)
- [x] Delivery accepted (post-implementation report 2026-06-07)
