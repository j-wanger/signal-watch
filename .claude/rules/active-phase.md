# Active Phase Context

Phase: 23 - FINTRAC depth — grow corpus source #4 from 3 Operational Alerts to 10 FINTRAC strategic-intel products (9 OAs + 1 real-estate Operational Brief) — M7 — DELIVERED 2026-06-06; all 5 tasks [x]; exit criteria GREEN (--check all 4-artifact zero drift, --selftest PASS incl. 4 new inverted-form fixtures, all 42 derived --check-derived clean, harness 74/74, 0/39 rf_region shift, frozen set byte-clean); reviewer 9/10 ACCEPT, no CRITICAL/HIGH, two LOW (grounding-backstopped, no ship fix). READY FOR COMPLETION pending commit. Corpus now 42 derived across 46 publications, 4 sources, 2 jurisdictions; the demo is at Definition of Done — run /dev-plan only for a net-new stakeholder ask.
Objective (all DELIVERED): T1 (M) acquire + markitdown-convert 7 NEW FINTRAC docs via --source data/fintrac (acquire_fincen.py/pdf_to_md.py needed NO edit) + per-doc rf_region triage + 39-md baseline · T2 (L) the gate change — an INVERTED "Indicators of <X>" anchor `_RF_HEADER_FINTRAC_INV` (two narrow branches + boilerplate exclusion; NOT the planned literal "TABLE OF INDICATORS" — a misread), 0/39 regression, grounding core byte-untouched, --selftest +4 fixtures · T3 (M) 7 derivations via the inverted loop (225 ind / 50 BUILD_NOW; human-trafficking 2016-appendix dropped as a cross-doc double-count) · T4 (S) regen corpus-status + rebuild dist/corpus 621KB + harness 61→74 (real-estate Brief full-arc walk); build.py/corpus.html UNCHANGED (data-driven) · T5 (S) CLAUDE.md + README.md counts 39→46 / 35→42 / 3→10 + Phase-23 bullet; no non-negotiable change.

Scope (the UNFREEZE, all consumed): `scripts/derive_signals.py` (the inverted anchor + 4 fixtures), `data/fintrac/**` (source #4 grown 3→10), `dist/corpus/index.html`, `tests/**`, `CLAUDE.md`, `README.md`. `scripts/acquire_fincen.py` + `scripts/pdf_to_md.py` were in scope but needed no edit (Phase-22 finding confirmed). FROZEN byte-untouched (verified): `index.html`, `config/**`, the 3 typology dists, `scripts/build.py` + `corpus.html` (data-driven — no structural edit), `data/fincen/**` + `data/fincen-alerts/**` + `data/ofac/**`, and the 3 EXISTING `data/fintrac/` records.

Key constraints (all HELD):
- GATE REGRESSION: the inverted anchor kept all 35 prior records `--check-derived` clean + `--selftest` passing AND every FinCEN+OFAC+existing-FINTRAC md's rf_region BYTE-UNCHANGED (0 of 39 shifted; reviewer independently reproduced via a HEAD-vs-working-tree diff). The grounding core normalize/check_record byte-untouched — only the rf_region relevance anchors widened.
- COMPLIANCE: NO non-negotiable change — depth within the Phase-22 FINTRAC Crown-copyright NON-COMMERCIAL basis; provenance stays source-aware (FINTRAC never shows "public domain"); the always-on badge stays.
- HONESTY: kept the full indicator lists (no per-doc caps, user-directed); dropped ONLY human-trafficking's 2016 Appendix as a faithfulness fix (cross-doc double-count); NEVER fabricated a BUILD_NOW (SOURCE_DATA-heavy docs honest).

Exit criteria (all MET): see the DELIVERED line above.
Abort (NOT needed): if a new heading form had shifted ANY existing rf_region, SKIP that doc + narrow the anchor. Blocked >3 attempts → ask the user: skip or abort.

Gates:
- [x] Direction confirmed by user (FINTRAC depth at the goal gate — audience = a Canadian bank; OAs + Briefs WITH the regression-gated narrow-anchor widening at the scope gate, over OAs-only / the source-scoped refactor; 2026-06-06)
- [x] Delivery accepted (post-implementation report 2026-06-06; reviewer 9/10 ACCEPT, two LOW grounding-backstopped; impl commit b0fcda4)
