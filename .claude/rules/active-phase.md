# Active Phase Context

Phase: 46 — Corpus live derivation mode: local-model agentic derivation through the frozen gate (M7×M8 convergence). Lite. 4/4 tasks [x] same-session (overnight 2026-06-10→11) — READY FOR COMPLETION; delivery gate pending. PRESENTATION TODAY 2026-06-11 (dist/corpus, bank stakeholders) — it OUTRANKS the delivery commit; the phase is presentation-safe (dist/corpus BYTE-IDENTICAL throughout).

Objective (DELIVERED): T1 harness probe — direct + ONE violation-guided retry CHOSEN over opencode at the USER CHECKPOINT (identical 17/17 indicators on held-out FINTRAC-2024-OA001, both gate-green first shot; direct 82.6s vs opencode 255.1s = 3.1×; the iterate-on-gate-failure loop NEVER ENGAGED; report .dev-wiki/tmp/ph46/ph46_probe.md). T2 scripts/serve_corpus.py (NEW stdlib companion: staged NDJSON /derive, the imported FROZEN check_record disposes, retry then grounded-or-dropped; live E2E 17 kept / 0 dropped, independent re-gate 0 violations). T3 corpus.html /*LIVE_*/ region (paste-only entry, UNREVIEWED session docs through the existing 6-screen arc, Phase-44-pattern processing takeover) + render_corpus strip. T4 docs/corpus-live.md + smoke notes + CLAUDE.md; full regate.

Scope (held): scripts/serve_corpus.py (NEW) · corpus.html /*LIVE_*/ region only · scripts/build.py (strip, one line) · tests/corpus-explorer.test.mjs (273→303) · docs/corpus-live.md · tests/smoke-checklist.md · CLAUDE.md · .dev-wiki/tmp/** (probe, LOCAL-ONLY).

Key constraints HELD: grounding core + derived data + overlays FROZEN (no anchor extension needed — the held-out doc parses clean); dist/corpus BYTE-IDENTICAL (--check all 5/5); live output DISPLAY/PROPOSE-only, held-out doc NOT committed; showcase + news artifacts byte-identical; NO non-negotiable change.

Exit criteria: ALL MET — probe report + checkpoint; serve_corpus --selftest + gate-green E2E NDJSON; corpus 303 incl. live-strip assertion; docs/smoke/CLAUDE.md in place; full regate green (9 suites). Assumption revisit: A1–A4 ALL held (A3 without the swap clause firing).

Next action: PRESENT TODAY first (pre-present: --check all → node suites → smoke-checklist walk). Then DELIVERY GATE — present the post-implementation report; on acceptance commit to main (phase-work commit, then the gate-log flip in its OWN post-commit-verify commit) and push. The news-side opencode lift eval is SEPARATE (staged in .dev-wiki/tmp/ph46/) — user's call at the next /dev-plan.

Abort rule (stood down — phase complete): blocked >3 attempts → ask skip or abort.

Gates:
- [x] Direction confirmed by user (assumption gate closed 2026-06-10: all_accept TRUE, A1–A4; T1 harness checkpoint taken 2026-06-11)
- [x] Delivery accepted (post-implementation report 2026-06-11; commit verified)
