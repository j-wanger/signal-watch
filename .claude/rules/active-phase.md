# Active Phase Context

Phase: 50 — AML program build: the demo became the program. A major user REFRAME at the dev-plan gate (2026-06-12): build the blueprint's §3 design-stage workloads as REAL systems, **one real-system-class repo per pillar**. signal-watch is now the program-ARCHITECTURE home (blueprint + this lifecycle record only); the build lives in sibling repos.

Pillar 1 = the **data substrate** → **`/Users/jwang/aml-substrate`** (Python; ultra-realistic synthetic Canadian retail-banking data where ML typologies EMERGE from modeled behavior). Its DESIGN.md holds the architecture, schema + distribution spec, gate, and cited sources. **aml-substrate Phases 1–11 DELIVERED 2026-06-13→16** (HEAD 0daa3cc; 360 tests green; gen/ freeze byte-stable). Highlights: P1 Foundation (population/KYC graph + 6-channel background engine + EMT) · P2 Emergence (criminal/mule/shell designation + fresh-Python laundering engine + transitive labeling + the A1 permutation-null separability gate; 1:21,657 imbalance; structuring breaks Benford) · P3 monitoring/grounding-chain · P4 1M-scale (19.38GB) · P5 entity-resolution (pw-F1 0.876) · P6 the ER⊥laundering NEGATIVE finding · **P7 Observable network linkage** (closed P6: `counterparty_account_id` populated + mirrored credit leg + case-id leak fixed + a positive freeze guard `scripts/check-gen-freeze.sh`; mule network now observable, recall 1.0) · P8 network reconstruction · P9 realism-validated + baseline-locked (`measure-baseline.json`, `--validate --check/--freeze`) · P10/P11/P12/P13 four subtlety/composition phases → the measured **TRIPLE-NULL** (HEAD bafc67d, 397 tests, gen byte-frozen #5): composition is NEVER required to detect laundering — amount-ROC (multivariate −0.34 vs total_amount 0.95), network-ROC (composed 0.916 vs best single 0.886, below the sealed 0.05 margin), network-precision (AP 0.043 vs 0.033, below the sealed 0.083 floor). Root cause STRUCTURAL: one ring ≈ one mule doing every typology → typologies co-occur REDUNDANTLY (any single detector catches the case). The §6 levers (Hawkes/dormancy temporal engine, legit-overlap cohort) were measured/predicted null AND counterproductive (a dormancy-burst drives velocity to a near-perfect single-feature tell). **Composition DETECTION-LIFT is RETIRED (user gate 2026-06-16)** — the triple-null is a PUBLISHED honesty asset, not a gap; composition's value pivots to evidence-assembly + corpus-redundancy + volume-inversion (none needs detection-lift). **Next: a cheap measure-only STRUCTURE-DETECTOR reachability probe** — the one config the triple-null never tested (a purpose-built network-structure signal over the P7/P8 graph; NO gen-unfreeze; pre-registered two-baseline, held-out) → null = retire unconditionally / clear = build the structure signal (still NOT P14). **P14 case-construction DEFERRED INDEFINITELY** (a self-engineering tautology + emergence-doctrine violation). The Hawkes/dormancy redesign survives only as a §6 temporal-FIDELITY lever (Leg B 0/3), never a composition lever. (Superseded brief `aml-substrate/docs/phase-12-multivariate-subtlety-PLAN-BRIEF.md` — P12/P13 consumed + nulled it.)

Pillar 2 = the **case-investigation → composition → SAR/STR-narrative** workload (blueprint §3 rows 4–6; aml-substrate's DESIGN names it "pillar #6 — the agentic capstone"). **Greenlit 2026-06-16 to run in PARALLEL** with Pillar 1 (the program is now parallel-pillar, not serial — the judgment/composition/narrative end doesn't wait on transaction-detection realism). First increment = the FULL chain (composition + judgment + SAR/STR narrative + completeness checklist + Class-A sign-off seam), built DEPTH-FIRST (one case → signed SAR, then a stratified set), with REAL deterministic verifiers (citation + completeness + grounding-replay). Pillar 2 repo = **`/Users/jwang/aml-casework`** (created + scaffolded 2026-06-16, genesis commit 08b06df; its own dev-wiki + dev-init pending in a rooted session). Bootstrapped on SYNTHETIC evidence bundles following the contract (user's call — unblocks Pillar 2 without waiting on Pillar 1's persist step): the executable contract + `validate_bundle` (first deterministic verifier), the depth-first thin-slice fixture (real corpus grounding, synthetic banking data — C4 structuring + C15 shell, two non-redundant axes), tests GREEN. **Pillar 2 has since reached Phase 5** (HEAD 0316580, 122 tests + 4 deselected integration, mypy --strict clean — verified at signal-watch HEAD ce21630 on 2026-06-17): SIX deterministic Class-G verifiers (contract.validate_bundle · grounding_replay · completeness · citation · corpus_grounding · narrative_grounding) + the Class-A signoff seam + 5 stratified fixtures + the depth-first SIGNED thin slice. **The Phase-2 KEY GAP is CLOSED** — P3 wired `corpus_grounding.py` to read the FROZEN signal-watch corpus (vendored pin signal-watch@a75a136, records IND-11/IND-04) as data: grounding is now ENFORCED-real, not authored-real. **The neural narrator is BUILT** — P4 `drafter_claude.ClaudeDrafter` (claude-opus-4-8, no longer deferred; the gate caught a real $1.84M-vs-$18,400 units slip) + a 6th prose-grounding verifier; P5 widened generation across the 3 strata with ZERO core-generator changes (proof-plus-adapter, ~10-line prompt diff). Next-work is Pillar-1-blocked (reporting_entity field + inculpatory-capability registration await the substrate persist seam) → Phase 6 unframed. **The load-bearing un-executed join: aml-substrate persists NO evidence (`cli.py` prints + discards; no `evidence/` dir, no alert_id/dossier_id minting), so casework's chain still runs on hand-authored synthetic bundles — the substrate→casework data flow (contract §5a) has never run (VERIFIED 2026-06-17).** signal-watch owns the cross-pillar **integration contract** `docs/pillar-integration-contract.md` — **§3 re-grounded 2026-06-16** to the triple-null: composition = redundancy-aware evidence-assembly + volume-inversion, detection-lift retired; the network-structure signal is the one un-refuted nuance, deferred pending the structure-detector probe. **Header re-stamped to aml-substrate@bafc67d 2026-06-17** (§1/§2 evidence dataclasses verified UNCHANGED P11→P13 — only `validate/*` moved; satisfies the re-ground-before-consume process rule).

**signal-watch-local: Phase 56 — Chain workbench (the HTML analyst UI for the substrate→casework→verify chain)** (PLANNED + active 2026-06-17). An HTML frontend living in signal-watch, reframed (the user's "detection doesn't need runtime gen") to an ANALYST CASE-WORKBENCH: detection pre-baked/upstream (evidence bundles vendored like the corpus pin); per case, the casework consume (6 verifiers + a LIVE neural SAR draft, ClaudeDrafter) + signal-watch's e2e_chain_check cross-pillar verify run LIVE, stage-streamed → CONNECTED + the flag→corpus audit walk. Dev-time companion `scripts/serve_chain.py` (stdlib; GET /cases, POST /run; ANTHROPIC_API_KEY server-side + stub fallback) + `chain.html` (dossier theme) — NEVER a ship artifact (offline dists byte-frozen; chain.html NOT a build target). Subprocess + file handoff only — NO sibling import. The one sibling prerequisite: a thin casework consume CLI (T2 brief → a casework session); the signal-watch SPINE (library + companion selftest-stubbed + UI + docs) is deliverable now, the LIVE run gated on the CLI (two-beat, like Phase 55). Tasks T1–T5; ledger Phase-56 (all_accept:false; A4 casework-CLI the T0 weakest; A3 defer-neural REJECTED→live-in-v1; A0/A2/A1'/A5 accept). Grounded against aml-substrate@df23bba + aml-casework@85602c1. Plan [[phases/phase-56-chain-workbench]].

**Prior — signal-watch-local (COMPLETE):**
**signal-watch-local: Phase 55 — Cross-pillar end-to-end bridge (the 3-pillar full demo connected)** (COMPLETE 2026-06-17 — both beats met; the 3-pillar full demo connects end to end, adversarially verified; user-confirmed complete. No active signal-watch-local phase now — next work is SIBLING-rooted [casework: wire the neural ClaudeDrafter into a demonstrable path · widen the 5/17 corpus pin · restore C3 distinct-counterparty replay] or a new `/dev-plan`). Connect the 3-pillar demo: the actual wiring executes in SIBLING sessions (their dev-* hooks bind there), the AGGREGATION + FINAL DELIVERY sit here. signal-watch delivers the SPINE this session — ratify the Pillar-1→Pillar-2 serialization contract (§2/§5a) + a re-runnable cross-repo verification harness `scripts/e2e_chain_check.py` (PROVE the join, read committed sibling outputs, NO sibling import) + the launcher front door `dist/index.html` (8th build target) + the two sibling PLAN-BRIEFs + the walkthrough; the `--real` chain verification + the final DELIVERY GATE is GATED on the siblings landing bridge #1 (aml-substrate persist+mint-ids) + #2 (aml-casework consume-real-bundle). Vertical slice = a C4-structuring case (code-verified reachable on the existing pin: substrate StructuringDetector C4 → fin-2026-alert001:IND-11 → casework grounding_replay C4). Shape = "verifier harness + walkthrough" + the launcher re-scope (A3). Grounded against aml-substrate@bafc67d + aml-casework@0316580. Plan [[phases/phase-55-cross-pillar-e2e-bridge]]; ledger Phase-55 (all_accept:false; A0 §5a-schema-ratifiable-now the T0 weakest, A2 C4-slice DEFENDED by code, A3 reject→launcher). The ONE ship-surface change is the launcher (dist/index.html + data/pillar-status.json); the 5 EXISTING artifacts stay byte-frozen, `--check all`→8/8; console/triage NOT re-opened. **SPINE DELIVERED + committed 2026-06-17 (b37d7a0 + e95df82):** T1–T6 done; `--check all` 8/8 zero drift; `e2e_chain_check --selftest` + `launcher.test.mjs` (23) green; the review gate confirmed the §2 id-mint rule BYTE-IDENTICAL across contract/harness/briefs + found 4 fixes (HIGH `--real` refuses synthetic fixtures · MED signoff required · 2 LOW) → e95df82. Beat 2 (the `--real` chain green) is GATED on the sibling sessions; the phase stays ACTIVE. **Bridge #1 VERIFIED 2026-06-17** (substrate-side, vs aml-substrate@df23bba `CASE-P-0010361` — **A0 did NOT bite**: schema + §2 id-mint + corpus grounding all matched on first real contact; the harness gained a `--real --substrate` substrate-side mode; pillar-status bridge_1=done; launcher rebuilt, --check all 8/8). NEXT: bridge #2 (casework consume) — the emitted case is MULTI-TYPOLOGY (C4/C3/C2/C5/C15, one mule) so casework must register C2/C3/C5 grounding_replay (brief updated). **BRIDGE #2 DONE → FULL CHAIN CONNECTED 2026-06-17** (casework@85602c1 Phase 6 registered C2/C3/C4/C5/C15 + ingest.py emitted CASE-P-0010361-signed.json; `e2e_chain_check --real` CONNECTED; pillar-status all-green; launcher rebuilt; --check all 8/8). **ADVERSARIALLY VERIFIED** (4-lens workflow): independent join re-derivation (5/5 ids exact, flags byte-grounded in SOURCE regulator md), casework integrity (record_signoff runs all 6 verifiers, build_signed_sar == committed byte-for-byte, non-no-op by perturbation), narrative faithful — all CONFIRM. Casework-side honesty notes (by design): committed SAR narrative is a deterministic FixedDraftStub draft not the neural ClaudeDrafter (gate-is-oracle; ClaudeDrafter @integration) · corpus_grounding enforces a vendored 5-of-17 subset (in_sync) · C3 replay count-based (bundle counterparty_ref=null). PROCESS NOTE: a verifier agent overstepped "do not modify" + committed e5a3138 (incomplete all-green flip — stale launcher + HEADs); undone (reset --soft) + redone clean with corrected grounding HEADs (df23bba/85602c1).

**Prior — the measure-first workstream (signal-watch-local, all NON-ship over the FROZEN corpus; ship corpus byte-frozen; DELIVERED + committed):**
- **Phase 54 — C/D tag control (DELIVERED 2026-06-16):** the blueprint §4–§5 measured-not-gated control class made CONCRETE + EXECUTABLE for the C/D tag — closing the named-OPEN Phase-47 T2/T3 control-story blocker. Direction = the user's REFRAME ("prototype the tag control") + a rigor ESCALATION ("execute the independent effectiveness challenge ONCE"). The independent challenge EXECUTED: a context-matched, genuinely cross-family local **Qwen3.6-35B** rater (probe-gated; competence user-adjudicated — 8/8 in-vocab, hit the named C8/C14 neighbour, flagged a plausible committed mis-tag) over the same committed n=96 → C **0.604** (κ 0.583) / D **0.646** (κ 0.599); cross-rater C 0.677 / D 0.646; **Krippendorff's α** over {committed, Opus-blind, Qwen-cm} C **0.634** / D **0.618**. FINDING: context-matched cross-family agreement is statistically indistinguishable from same-family self-consistency (0.677; Wilson CIs overlap) → reliability GENUINE (survives a family change, chance-corrected, ~0.6/axis, never validated-correct), context-matching resolves the Phase-53 confound (an INFERENCE from the aggregate, not a Qwen forced-pairwise re-run). The CONTROL made real: `docs/cd-tag-control.md` (NON-ship SR-11-7 Pillar-2 + OSFI E-23; scope/risk-tier/baseline/cadence/3 trip-wire classes/independent-challenge/three-lines-of-defense/re-baseline/audit-walk) + `cd_correctness.py --control-check` (PASS 7/7 on the frozen corpus) / `--control-freeze` — BREACH demonstrated on injected drift (selftest + live perturb-and-restore). The model dependency isolated in the dev-time companion `scripts/cd_rate_independent.py` (build.py + cd_correctness.py import NEITHER — the replay core stays pure stdlib); NO blueprint edit. A0 (T0 weakest) HELD with a positive surprise (Opus fallback not needed); A1–A3 HELD. Artifacts: `scripts/cd_rate_independent.py` (NEW) · `scripts/cd_correctness.py` (extended) · `data/cd-correctness/{independent-sample,cd-control-baseline}.json` (NEW) · `docs/cd-tag-control.md` (NEW) · `docs/cd-correctness-report.md`. Plan [[phases/phase-54-cd-tag-control]]; ledger Phase-54.
- **Phase 53 — C/D disagreement decomposition (DELIVERED 2026-06-16):** finished Phase 52 — ran the decomposition its report named as missing + added Cohen's κ. Enlarged random (n=96) self-consistency C/D **0.677**, **κ≈0.65≈raw** (genuine, not chance). Decomposition of the 62 mismatches: both-defensible **1.6%**, neither **0%** → the "scatter" guess **REFUTED** (disagreements SHARP). Direction CONFOUNDED by context asymmetry (flag-only re-rate vs full-doc committed): committed-better **37%** = clean LOWER bound; blind-better **61%** = UPPER bound (UNMEASURED error+confound mix, NOT a committed-error rate). A0 surprise: predicted both-defensible-inflation didn't fire; the confound is context-asymmetry (ledger A0 revisit = bit). Follow-up reframed → a CONTEXT-MATCHED rater. `scripts/cd_correctness.py` · `data/cd-correctness/{random-sample-large,decomposition-sample}.json` · `docs/cd-correctness-report.md`.
- **Phase 52 — C/D-tag reliability (DELIVERED 2026-06-16):** the unguarded dimension (the grounding gate never checks the per-indicator C/D codes) measured stratified + blind: random same-model re-rate reproduces C/D ~62.5%/axis (self-consistency, NOT validated correctness) · blind pairwise upholds the Phase-34 corrections 70.8% (~3:1, adjudicable, 0 neither). Artifacts: `scripts/cd_correctness.py` · `data/cd-correctness/*` · `docs/cd-correctness-report.md`.
- **Phase 51 — corpus redundancy (DELIVERED 2026-06-16):** cross-regulator co-occurrence ≤ 0.325; genuine equivalence est ~1.4% strict / ~11.5% band (single-rater illustrative). `scripts/corpus_redundancy.py` · `docs/corpus-redundancy-report.md`.

Remaining measure-first candidates route to SIBLING repos (a signal-watch-rooted session can't drive their lifecycle): the substrate→casework **persist seam** (contract §5a — aml-substrate emits no evidence yet, so casework still runs on hand-authored synthetic bundles; this is the load-bearing un-executed join, ~1 day to wire + mint ids over the existing `compose()` / `io.serialize`) → an aml-substrate-rooted session; the structure-detector reachability probe (aml-substrate). (The casework audit-walk-to-source gap is now CLOSED — done in casework P3.) Next signal-watch-local candidates (all deferred-with-owner now that Phase 54 DELIVERED the control + executed the independent challenge once): a **HUMAN domain-expert independent rater** (the gold standard beyond cross-family model consensus — the control SCHEDULES it; Phase 54 executed the model leg only); the control extended to the whole measured-not-gated CLASS (the reusable pattern, e.g. the news red_flag translation — C/D as the proven instance); a **Qwen forced-pairwise decomposition** (converts the Phase-53 confound bound into a measured share); the deterministic section-heading→capability residual (the Phase-37 thread).

Doctrine (aml-substrate): data-first/emergent (typologies emerge, NEVER injected/stamped); deterministically SCRIPTED generation, no runtime LLM (small statistical models OK; LLM only authors scripts); grounded in researched REAL schemas + distributions; everything SYNTHETIC, no real customer data, ever.

Note: aml-substrate has its OWN `.dev-wiki/` (P1–P13 delivered, the triple-null; composition detection-lift retired; next = the structure-detector probe) and aml-casework has its own (P5 delivered — six Class-G verifiers + the real neural narrator). Run the dev-* skills from a session ROOTED in each (their hooks fire only there; a signal-watch-rooted session can't drive their lifecycle). DESIGN.md + docs/phase-*.md remain durable. **This file drifts between cross-pillar reviews and the drift turned MATERIAL once (the contract shipped a premise the substrate had refuted) — trust each sibling repo's CURRENT HEAD for its live state. PROCESS RULE (2026-06-16): any cross-pillar artifact [integration contract, sales pitch, governance model, _CURRENT_STATE pillar facts] MUST be re-grounded against the sibling repo's current measured verdict before it is committed or consumed; record the grounding HEAD inline.**

FROZEN (signal-watch — no further demo work this track unless re-opened): the 5 ship artifacts + dists byte-identical (index.html + config + 3 typology dists; corpus.html + dist/corpus; news.html + dist/news; console.html + dist/console; triage.html + dist/triage); derive_signals.py; news pipeline; all committed derived data + the 3 overlays; docs/program-blueprint.md + blueprint-report.html.

Abort rule: existing signal-watch dists drift → STOP and surface (never re-baseline).

Gates (Phase 50, program-kickoff direction gate — closed 2026-06-12, all_accept: false):
- [x] Direction confirmed by user (A1 foundation-first/emergence-ready accept · A2 build-on-research [user override of review-first] · A3 1M-scale reject→small-first · A4 hybrid eyes-open→deferred to phase 2; ledger Phase-50 block, revisit-status recorded at Phase-1 delivery)
- [x] aml-substrate Phase 1 (Foundation) delivered 2026-06-13 (54 tests; A1 proven, A3 validated, A2 held, A4 deferred)
- [x] aml-substrate Phase 2 (Emergence) delivered 2026-06-13 (83 tests; A1 measured-not-assumed via the separability gate, A2 bounded, A3 fresh works, A4 1:21k validated at scale, A5 no migration)
- [x] aml-substrate Phases 3–6 delivered 2026-06-13→14 (P3 monitoring 154 tests · P4 1M-scale 19.38GB · P5 ER pw-F1 0.876 · P6 the ER⊥laundering negative finding; 239 tests total) — per-phase gates tracked in that repo
- [x] aml-substrate Phases 7–11 delivered 2026-06-15→16 (P7 observable network linkage [closed the P6 finding] · P8 reconstruction · P9 realism-validated + baseline-locked · P10/P11 subtlety; 360 tests total, HEAD 0daa3cc) — per-phase gates tracked in that repo
- [x] aml-substrate Phases 12–13 delivered 2026-06-16 → the measured TRIPLE-NULL (composition never required, all 3 axes; 397 tests, HEAD bafc67d) — the §6 levers nulled + counterproductive; per-phase gates in that repo
- [x] Pillar 2 greenlit PARALLEL + repo created/scaffolded 2026-06-16 → /Users/jwang/aml-casework; has since reached **Phase 5** (HEAD 0316580, 122 tests, mypy --strict clean — verified 2026-06-17): 6 Class-G verifiers + corpus-grounding ENFORCED-real + the real neural narrator built; load-bearing gap = the substrate→casework persist join (contract §5a) never executed

Direction gate (2026-06-16, two-workflow review through the new lenses — composition retired, measure-first):
- [x] Composition DETECTION-LIFT retired by user — pivot to evidence-assembly + corpus-redundancy + volume-inversion; triple-null kept as a published pitch asset; P14 deferred indefinitely; settle via one cheap structure-detector probe
- [x] Audience = measure-first (no imminent sell conversation) — close a frontier with a MEASURED result before more pitch/architecture polish
- [x] Re-grounded the refuted artifacts (contract §3 + pitch composition claim) + added the re-ground-before-commit process rule [done this session, architecture home]
- [x] casework audit-walk-to-source verifier (wire citation verifier to the frozen corpus) — DONE in casework P3 (corpus_grounding.py reads the vendored frozen-corpus pin signal-watch@a75a136; verified 2026-06-17)
- [x] corpus redundancy measurement PLANNED as **Phase 51** (2026-06-16) — direction gated to the HYBRID non-ship artifact (honesty-axis decision: in-constraint deterministic upper bound + sampled consensus-class semantic estimate; ship corpus untouched); tasks T1–T4 + the ledger Phase-51 block written; the measurement workstream's other number (C/D-correctness over the 213 console divergences) stays a parallel measure-first candidate
- [x] Phase 51 DELIVERED 2026-06-16 (T1–T4): cross-regulator co-occurrence ≤ 0.325 (deterministic) · genuine equivalence est ~1.4% strict / ~11.5% band (single-rater illustrative, consensus deferred) — the §13 fm-1 frontier measured honestly; the corpus's no-dedup stance empirically vindicated; ship corpus byte-frozen (--check all 7/7 zero drift). Artifacts: scripts/corpus_redundancy.py · data/corpus-redundancy/equiv-sample.json · docs/corpus-redundancy-report.md
- [ ] the structure-detector reachability probe (measure-only) → an aml-substrate-rooted session
- [x] Phase 52 DELIVERED + accepted 2026-06-16 (T1–T4; both gates accepted, all assumptions HELD): the unguarded C/D dimension measured honestly — blind same-model re-rate reproduces C/D ~62.5%/axis (self-consistency, NOT validated correctness; independence deferred-with-owner) · blind pairwise upholds the Phase-34 corrections 17/24 on the hard subset (adjudicable, 0 neither). The random and divergence numbers are different samples/tasks, **NOT subtractable** — defensible adjacent-code scatter is plausibly one driver of the random mismatch, share unmeasured (n=24, the two rates statistically indistinguishable; the original "gap = scatter, not error" finding was tightened post-delivery). NON-ship; ship corpus byte-frozen (--check all 7/7 zero drift). Artifacts: scripts/cd_correctness.py · data/cd-correctness/{random,divergence}-sample.json · docs/cd-correctness-report.md.
- [x] Phase 53 DELIVERED + accepted 2026-06-16 (T1–T4; both gates accepted; A0 BIT [mechanism differed — bound held, but the predicted both-defensible-inflation didn't fire; real confound = context asymmetry], A1–A3 HELD): DECOMPOSED the C/D disagreement (finished Phase 52's named-missing measurement). Enlarged random (n=96) self-consistency C/D 0.677, κ≈0.65≈raw (genuine, not chance). Decomposition (62 mismatches): both-defensible 1/62, neither 0/62 → "scatter" REFUTED; committed-better 23/62 (37%, clean LOWER bound) · blind-better 38/62 (61%, UPPER bound, context-confounded, NOT a committed-error rate). NON-ship; --check all 7/7 zero drift. Follow-up reframed → a CONTEXT-MATCHED independent rater. Artifacts: scripts/cd_correctness.py · data/cd-correctness/{random-sample-large,decomposition-sample}.json · docs/cd-correctness-report.md.
- [x] Phase 54 PLANNED + active 2026-06-16 — direction gate all_accept:true (A0 local-model availability+competence [T0 weakest, probe-gated + named Opus-context-matched fallback; Qwen confirmed UP] · A1 context-matched=committed-inputs · A2 replay-core-stays-pure/model-isolated · A3 non-ship + no-blueprint-edit). The user reframed off a 4th measurement to **prototype the tag CONTROL** (the blueprint §4–§5 measured-not-gated class made concrete+executable for C/D) + chose to EXECUTE the independent challenge once (context-matched cross-family local-Qwen → clean reliability number + Krippendorff-α). Resolves the Phase-47 T2/T3 control-story blocker. Tasks T1–T5 + the ledger Phase-54 block written.
- [x] Phase 54 IMPLEMENTED 2026-06-16 (T1–T5 all [x], exit criteria met, READY FOR COMPLETION): the independent challenge EXECUTED on the local cross-family Qwen3.6-35B (context-matched, probe-gated, competence user-adjudicated) → C 0.604 (κ 0.583) / D 0.646 (κ 0.599), cross-rater C 0.677 / D 0.646, Krippendorff-α C 0.634 / D 0.618 — context-matched cross-family agreement statistically indistinguishable from same-family self-consistency (0.677; Wilson CIs overlap) → reliability GENUINE, the Phase-53 confound resolved. The CONTROL made real: docs/cd-tag-control.md (SR-11-7 Pillar-2 + OSFI E-23) + cd_correctness.py --control-check (PASS 7/7) / --control-freeze (BREACH on injected drift). NON-ship; --check all 7/7 zero drift; build.py imports neither companion; NO blueprint edit. A0 HELD with a positive surprise (Opus fallback not needed); A1–A3 HELD. Closes the Phase-47 T2/T3 control-story blocker.
- [x] Phase 54 DELIVERY accepted 2026-06-16 (user accepted the delivery report; committed + pushed to main — gate-state follows git-state)

Direction gate (2026-06-17, /dev-plan — connect the 3-pillar full demo; aggregation + final delivery sit in signal-watch, wiring routes to sibling sessions):
- [x] Phase 55 PLANNED + active 2026-06-17 — direction gate all_accept:false (A0 §5a-schema-ratifiable-now [T0 weakest] ACCEPT · A1 verify-by-reading-committed-outputs / no-sibling-import ACCEPT · A2 C4-slice-on-existing-pin don't-know→DEFENDED-by-code [three-way C4↔IND-11↔C4 alignment]→ACCEPT · A3 pure-non-ship REJECT→re-scoped to the launcher+pillar-status [one ship-surface file]→ACCEPT). Shape = verifier harness + walkthrough. The SPINE (T1–T6: ratify contract · e2e-acceptance · e2e_chain_check fixture-proven · launcher 8th target · 2 sibling briefs · walkthrough) is deliverable this session. Tasks T1–T6 + the ledger Phase-55 block written.
Direction gate (2026-06-17, /dev-plan — the chain workbench HTML UI):
- [x] Phase 56 PLANNED + active 2026-06-17 — direction gate all_accept:false (A0 detection-pre-baked/live-verify-is-proof ACCEPT · A2 vendored-bundle-library ACCEPT · A4 casework-consume-CLI-is-cheap [T0 weakest] ACCEPT · A1' companion-subprocesses-CLIs-no-import ACCEPT · A5 server-side-key+stub-fallback ACCEPT; A3 defer-live-neural REJECTED→pulled into v1). Reframe: orchestrator→analyst-workbench (detection upstream). SPINE T1–T5 (vendored library · casework-CLI brief · serve_chain.py selftest-stubbed · chain.html · docs) deliverable this/next session; the LIVE neural run is beat 2, gated on the casework consume CLI.
- [ ] Phase 56 DELIVERY — the SPINE (T1–T5) accepted AND the LIVE run green (real neural SAR drafted in the browser → e2e_chain_check --real CONNECTED) once the casework consume CLI lands. TWO-BEAT.

- [x] Phase 55 DELIVERY (TWO-BEAT) — **BOTH beats met 2026-06-17.** Beat 1 spine (b37d7a0 + e95df82; T1–T6). Beat 2: bridge #1 (aml-substrate@df23bba) + bridge #2 (aml-casework@85602c1) landed → `e2e_chain_check --real` **CONNECTED** (10867ff), **adversarially verified** (4-lens workflow; A0 did NOT bite — real emission matched byte-for-byte). pillar-status all-green; --check all 8/8; pushed. Phase functionally COMPLETE (active→completed pending the user's explicit call — never auto-transitioned).

Per-pillar/per-phase gates for the sibling repos are tracked there (their `.dev-wiki` + DESIGN.md §8 + docs/).
