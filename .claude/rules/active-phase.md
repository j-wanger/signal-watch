# Active Phase Context

Phase: 17 - Complete corpus derivation + delete extract_red_flags (the real subtraction) — M7 — DELIVERED, reviewer ACCEPT 9/10, READY FOR COMPLETION (all 6 tasks [x], exit criteria MET; awaiting impl + gate commit, 2026-06-06). No next phase planned — run /dev-plan for Phase 18.
Objective: finish the Phase-16 inversion with the REAL line-count subtraction + scale to completion. (A) Delete `extract_red_flags` + the now-dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack the inverted loop replaced; add a ~14-line `_rf_triage` counter reusing the `rf_region` span. (B) Scale the 5 remaining derivable advisories via the inverted loop → corpus 7/14 → 12/14 live.

Delivered (verified in tree): T1 THE SUBTRACTION won — deleted `extract_red_flags` + the whole scaffold/draft stack (scaffold_config, write_scaffold, draft_judgment, write_draft, _few_shot, _apply_judgment, _derived_skeleton) + the `anthropic` dep + the `os` import; `derive_signals.py` 1202→600 (−602, −50%, beat the ~700 estimate; def-grep 0; stdlib-only). `_rf_triage(md, region)` reuses `rf_region` (real shrink, not a reintroduced parser); `--selftest` gate-only PASS. T2–T4 scaled 5 records via the inverted loop — fin-2026-a001 (glued health-care, 24 ind), fin-2021-a001 (16), fin-2024-a001 (9), fin-2025-a001 (11), EFE fin-2022-a002 (12 FINANCIAL only); the 2 "LOW" validated clean, no degrade; all 12 derived `--check-derived` clean + grounded → 12/14 live (only the 2 FATF advisories non-derivable). T5 dist/corpus rebuilt 109,948→180,119 B, `--check all` 4-artifact ZERO DRIFT. T6 README + CLAUDE document the deletion + the counter + 12/14. DISCOVERY: `corpus_status_records` uses `_rf_triage` UNIFORMLY for all 14 (simpler than the planned per-live read). 1 reviewer MEDIUM (latent triage-overcount footgun for a future not-yet-derived glued advisory — harmless in ship, disclosed; NOT fixed inline). index.html/corpus.html/build.py/config/** + 3 typology dists BYTE-FROZEN.

Deferred (Phase-18 candidates): tighten the coarse `_rf_triage` counter (the reviewer MEDIUM) · drop the stale `anthropic` pin from requirements-authoring.txt (derive_signals.py no longer imports it) · centralize the two triage thresholds · FATF non-derivable labeling polish · the recurring showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.

Gates:
- [x] Direction confirmed by user (scale-to-complete + delete; counter bridge; may-grow-later → no lean freeze — 2026-06-06)
- [ ] Delivery accepted
