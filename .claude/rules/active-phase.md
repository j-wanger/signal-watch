# Active Phase Context

**Phase 73 — *Rich investigation case in the LIVE workbench: the matched FILE/DISMISS pair, rail-aware counterparty network, entity resolution, and the affirmative-clear verdict*** (signal-watch-local, STANDARD — user override) — DELIVERED 2026-06-23; READY FOR COMPLETION (T1–T5 all [x]). **No active signal-watch-local phase now — run `/dev-plan` for the next.**

## Status
**COMPLETE — T1–T5 ALL [x] (STANDARD).** The orchestrator flips the delivery gate after the commit verifies. AUTHORED the north-star rich investigation case — the matched FILE/DISMISS pair **CASE-A Northgate** (files) / **CASE-B Lakeshore** (clears) — firing the SAME grounded signals yet resolving OPPOSITELY on an authored network + source-of-funds layer (THE THESIS HELD). The direction INVERTED the pillar dependency (signal-watch authors the rich case first — the artifact is the spec; substrate/casework PARKED). The two verdicts are LIVE-ENGINE OUTPUT (`serve_workbench` casefile path derives the inputs from the evidence; the authored `expected_*` a regression oracle via a fixture-drift bridge). **THE A1 GUARD HELD** — the affirmative-`cleared` verdict (mechanism + 0 legs + affirmative mitigation established) is a SEPARATE clear path; the file/determination bar is BYTE-UNCHANGED (proven by `evidence_requirements.py --selftest`). USER OVERRIDE mid-implementation: the two cases UNLINKED (each self-contained; the excluded near-match reframed against a self-contained synthetic watchlist entry) — standing rule: cross-reference only on a REAL link. STANDARD review gate → FIX-THEN-SHIP (0 must-fix; 2 should-fix both fixed: the bare-entity-id STR-provenance leak → display names; the fixture↔live-computation drift → a json-equals bridge). NEW companion source `data/casefile/`; companion-only, build.py imports none of it; `--check all` 8/8 ZERO drift; `node tests/workbench.test.mjs` 127→151; `serve_workbench --selftest` 355→357 (the pair LEADS); `uv run pytest` 20.

## Phase 74+ candidates
The cross-pillar contract doc `docs/rich-case-target-contract.md` (the DEFERRED handoff — what substrate must EMIT + casework SIGN/REFUSE/CLEAR, incl. CW-4 the live cleared-by-mitigation verdict + the txn-less party-leaf sign) · money-flow graph legibility (cap/group the dense inbound into a super-node) · breadth (more showcase cases / a second matched pair) · the casework txn-less-contract follow-on (651 txn-less C14 party-leaf cases, sibling — carried from Phase 72) · roll the determination/affirmative-clear model across the triage + gate consoles (disposition-only — carried from Phase 69/70) · C1/C7/TF stay sibling-rooted.

## Gates
- [x] spec — generated this phase (STANDARD ceremony; `specs/phase-73-rich-investigation-case-live-workbench.md`)
- [x] Direction confirmed by user (assumption positions 2026-06-23; A0–A6 accept, A1 accept-with-guard; all_accept:true, warned + restated)
- [x] Delivery accepted (post-implementation report 2026-06-23; user "Accept /dev-debrief"; committed f804722)

Plan [[phases/phase-73-rich-investigation-case-live-workbench]]; ledger Phase-73.
