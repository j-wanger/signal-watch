# Active Phase Context

Phase: 38 — Consolidate the live news subsystem (verify agent backend + watchlist management). IN PROGRESS, direction approved 2026-06-08. 7 lite tasks T1–T7 (no L; T2 entity-precision filter + T4 Qwen-vs-Opus stress test both added per user adjudication). T1–T4 ✓ DONE (backend half + stress test). KEY PIVOT (T4 stress test on 3 NEW articles): the T2 denylist OVERFIT (38 noise leaked on new articles) → trimmed screen_entities to structural rules + DROPPED the denylist; the real lever is a SUBJECTS-ONLY rule in serve_news.SYSTEM_PROMPT (context shaping; Qwen noise 38→~3, generalizes). Usability verdict: a well-prompted LOCAL Qwen is usable (35 vs Opus 29 entities — the gap was instruction clarity, not capability); NO API backend needed. SECOND PASS (user idea, WIRED IN on-by-default): keep-biased per-entity verify in serve_news.extract (LIVE-only, layered ON TOP of the deterministic build_record; fail-OPEN=KEEP; --no-verify-entities to disable) — drops residual institutional noise with ZERO subject loss. T5 ✓ (store prune + /watchlist/prune route). REMAINING: T6 news.html watchlist view UI · T7 regate+docs+fixture-promotion+CLAUDE.md. Lite ceremony.

Objective: CONSOLIDATE the Phase-35/36 LIVE news subsystem (companion-served, dev/authoring-time only) — chosen over extending source scale. HALF A (centerpiece): verify + lock the agent backend against the now-LIVE Qwen (127.0.0.1:8080) — capture raw model output as committed fixtures + offline replay tests (parse→build→ground, dep-free) + an opt-in --live smoke; evaluate two-layer (grounding=faithfulness + a Claude-reference comparison as INTER-RATER AGREEMENT). HALF B: a watchlist-management VIEW + prune (store method + serve_news route + news.html live panel) — completes the Phase-36 loop.

Scope (UNFREEZE): tests/fixtures/news-live/** (.qwen.json + .golden.json per article) · tests/news_live_test.py · scripts/news_ground.py (T2 — `screen_entities`, live-path only) · scripts/news_store.py · scripts/serve_news.py · news.html (live region only) · dist/news/index.html (rebuilt, byte-identical via the strip) · tests/news-stream.test.mjs · .dev-wiki/tmp/** · docs/news-live.md · tests/smoke-checklist.md · CLAUDE.md (T6 in-place `## Current state`) · this file.

Key constraints:
- OFFLINE-STRIP non-negotiable: ALL new live + watchlist-view code lives inside /*LIVE_START*/…/*LIVE_END*/ → render_news strips it → dist/news BYTE-IDENTICAL, zero network/store code offline (screens the synthetic book only).
- build.py NEVER imports the live/store layer (serve_news/news_store stay companion-only); it DOES import news_ground (the SHARED stdlib grounding primitives, by design).
- AGREEMENT-HONEST (Phase 34/37): the Claude reference is a SECOND rater, NOT ground truth — Qwen-vs-Claude reported as INTER-RATER AGREEMENT + divergences for the USER to adjudicate; NO accuracy/precision/recall number; the always-on badge stays. Grounding gate ≠ completeness gate ≠ correctness gate. Do NOT loosen the gate to make a fixture pass.
- T2 ENTITY-PRECISION FILTER is FAITHFUL + LIVE-PATH-ONLY: `screen_entities` drops noise/alias-dups, NEVER invents; it preserves every consensus subject; called only from serve_news.build_record, NOT build.py → offline dist/news + committed derived records byte-frozen.

Exit criteria: backend verified against the real Qwen with committed raw-output fixtures + offline replay tests pinning the contract incl. the ungrounded-elision drop; a Qwen-vs-Claude agreement+divergence finding (consensus, no accuracy number); an opt-in --live smoke OFF the default run; a watchlist VIEW + prune in the live region; dist/news byte-identical; --check all zero drift; node news + corpus + news_live_test + news_ground/serve_news/news_store --selftest green; NO non-negotiable change.

Abort rule: live backend regresses against the real Qwen → STOP + report (the gate is the backstop; do NOT loosen it). A captured fixture can't drive a deterministic offline replay → re-confirm the recorded-fixture approach. dist/news can't stay byte-identical after the strip → surface it. Blocked >3 attempts on a task → mark [blocked: …] + ask the user: skip or abort.

Gates:
- [x] Direction confirmed by user (consolidate live subsystem: verify agent backend + watchlist mgmt; Claude-reference comparison folded into T1 as inter-rater agreement; approved 2026-06-08)
- [ ] Delivery accepted (post-implementation report)
