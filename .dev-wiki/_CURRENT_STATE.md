# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-22 — **Phase 67 DELIVERED + accepted 2026-06-22 — the LIVE investigator workbench is now shippable from a bare clone (vendored aml-casework + make setup).** Prior: signal-watch-local **Phase 66 — Workbench richness: richer slice + OSINT corpus depth + the substrate BO-graph emission handoff brief** DELIVERED + accepted 2026-06-22, LITE — a 3-part phase: a richer 294-case vendored slice via a DETERMINISTIC substrate re-emit [all 23 combos, 99/294 coverage, 189/66/39 funnel] + a deeper OSINT corpus whose ownership records MIRROR the substrate RelationshipEdge schema [record-SOURCED ownership — the model can't fabricate the pct/direction; two ownership→sanctions chains] + the aml-substrate beneficial-owner/ownership-graph EMISSION handoff brief [sibling-executed, the Phase-55–58 pattern]; live execute-once + an adversarial pass caught & fixed 3 honesty flaws; THE GOVERNOR: demo-VISIBLE richness only, ZERO catch-rate/detection-difficulty/lift — the substrate is single-signal-separable [composition subsumed by network linkage, P16]; companion-only; --check all 8/8 ZERO dist drift. Prior: **Phase 65 — Agentic tool-calling: the investigator evidence-gathering loop (companion)** DELIVERED + accepted 2026-06-21, LITE, committed 761a446 — built signal-watch's FIRST multi-step tool-calling agent loop [a GATHER beat between SIGNALS and DECIDE: the agent proposes a tool → a deterministic tool over a COMMITTED SYNTHETIC corpus → grounded-or-stripped via the reused news_ground gate → a liveGraphLayout network view; companion-only; EXECUTED LIVE — the registry→sanctions chain fired vs a local Qwen3.6-35B, 2 grounded/0 dropped/0 fabricated; two adversarial passes, all findings fixed]. Prior: **Phase 64 — Precedent-confidence gating engine + the elicitation loop (live)** DELIVERED + accepted 2026-06-21 [committed 0ee4489]: the static precedent-confidence bucketer became a LIVE gating CONTROL [an explicit gating_policy + a pure route()] + the session-only elicitation loop [route on §12 firing frequency / dispositions stay §14 illustrative]; companion-only; --check all 8/8 ZERO dist drift. Prior: **Phase 63 — Investigator Case Workbench (capstone sales-pitch demo)** DELIVERED + accepted 2026-06-21 [committed a3fde1e]: a COMPANION-SERVED investigator case workbench [`workbench.html` + `scripts/serve_workbench.py`, companion-only, NOT a 9th build/dist target — lite holds] over a REAL [synthetic] aml-substrate alert POPULATION [200-case deterministic slice vendored into `data/workbench/`, pinned aml-substrate@f90bd39, `meta.synthetic:true`; TOOL-USE / file-contract — build.py imports NO sibling]. RE-SCOPED post-gate to option B [USER OVERRIDE — A0 flipped → substrate-grounded real population VERIFIED feasible @f90bd39, NO precursor phase]. Three beats: clutter [real PartyView KYC + cross-account aggregation + txn detail + REAL counterparty edges, synthetic display identity over the real KYC] → signals-on clarity [grounding/corroboration; ZERO catch-rate number — the detection-lift triple-null governs] → live finale [the existing serve_chain → casework consume; default Claude, stub fallback, NDJSON stage reveal]. **The T4 cross-pillar finding → the FAIL-CLOSED defensibility climax:** coverage is MEASURED [curate runs casework's stub over all 200 bundles → **57/200 sign end-to-end**]; composed cases fail on a real substrate↔casework **C3/C15 replay divergence** [the gate correctly REFUSING → escalate-to-human, never loosening — the verifier was never loosened, abort rule held]. Precedent-confidence anchored to REAL firing frequency [gate funnel **129 auto-clear / 52 review / 19 human-gate**]; DISPOSITION direction stays ILLUSTRATIVE [the Phase-62 split]. 4 exemplars tagged within the population [mule CASE-P-0002174 / fp_trap CASE-P-0018660 / thin CASE-P-0008468 / ambiguous CASE-O-000008]. PLUS a thin PYTEST WRAPPER [USER OVERRIDE, mid-phase — `pyproject.toml` package=false + `tests/test_selftests.py` + `uv.lock` → `uv run pytest` runs the whole suite, 17 cases; NOT full py-init — the dep-free paths stay the source of truth]. `uv run pytest` 17 passed; `build.py --check all` 8/8 ZERO dist drift; `tests/workbench.test.mjs` 61 assertions green; no runtime deps. Follow-ons deferred: C3/C15 contract alignment [sibling-repo] · agentic tool-calling · the precedent-confidence GATING engine · the substrate ownership-graph emission. Ledger Phase-63 [all_accept LATER FLIPPED to false via the A0 re-scope; A1'/A2'/A3' HELD; the T4 finding bit productively]. Prior: signal-watch-local **Phase 62 — Grounded probe-history consume (§12) + §14-frozen boundary + P22 pin re-ground** COMPLETE + accepted 2026-06-21: consumed aml-substrate's P22 grounded probe-history projector into the §12 non-ship measurement layer [`probe_history_stats.py`]; grounded `alert-history.json` = 4,966 real label-blind firings, substrate@ae98924 pinned; **the §12/§14 SOURCE BOUNDARY** [right for §12 MEASUREMENT, wrong for §14 — the substrate emits no adjudicable fact pattern] → the planned §14 dist/triage unfreeze STOOD DOWN at the T4 checkpoint, dist/triage BYTE-FROZEN. Prior: signal-watch-local **Phase 61 — Blueprint review against what's been implemented** COMPLETE + accepted 2026-06-20: a 96-agent cross-repo audit → the NEW committed `docs/blueprint-implementation-review.md` [three-tier status vocab: demo-built / pillar-build-synthetic / design-stage; the §3 binary was OUTGROWN — 4 "(design-stage)" rows now carry committed SIBLING code at pillar-build-synthetic scale → the blueprint was UNDER-claiming; 8 blueprint-stale + 6 impl-diverged enumerated] + a verified-gated deep blueprint true-up [§3 markers + §7/§8/§11/§13 prose, HEADs pinned, Status: DESIGN + §15 charter PRESERVED] + a batched verify-first cross-pillar re-ground to substrate@34400e2/P20 + casework@c6d8401/P12 [genuinely ZERO tier movement — reachable-now held 171]; --check all 8/8, only dist/index.html moved [the sanctioned launcher cascade, Phase-60 Option-A]; ledger Phase-61 all_accept:true. Prior: signal-watch-local **Phase 60 — Consume the landed sibling halves: the C7 reachable-now rise + the real e2e chain** COMPLETE 2026-06-19 — reachable-now **93→171 (+78, all C7, the first real rise**; C8/C14/C26 blocked on emergent behavior, NOT wiring); the real C8 chain CONNECTS (substrate C8 v0.2 bundle → casework signed SAR, zero blocking violations); launcher re-grounded (Option-A, 7/8 dists byte-identical); committed + pushed to main. Prior: signal-watch-local **Phase 57 — Live neural SAR/STR draft + pluggable drafter backends (claude · OpenAI /v1 · opencode)** COMPLETE 2026-06-18 (beat 1 delivered + accepted + committed f49ec1b; user marked completed; beat 2 [live openai/opencode] gated on the casework adapters; NO active signal-watch-local phase now). Made the Phase-56 workbench's "LIVE neural SAR/STR draft" real end-to-end + opened the drafter to multiple backends: claude [OAuth, exists] · an OpenAI-standard /v1 endpoint [local models direct] · opencode [drive drafting THROUGH its agent loop]. The drafter is casework's pluggable Drafter Protocol; the gate stays the oracle. Two-beat, two-repo (P55/P56 rhythm): beat 1 = the signal-watch spine [serve_chain N-backend pass-through + chain.html picker + live-draft+verdict + the live-claude run], drivable NOW; beat 2 = the openai+opencode adapters [briefs here, live runs gated on casework sessions]. Sibling state code-verified: aml-casework@2381d71 [two past the held pin f0542b7] — Phase 7 consume CLI + Phase 8 OAuth-wired ClaudeDrafter; the drafter is a pluggable Protocol, no OpenAI/opencode adapter exists. Gate all_accept:true [A2 REFRAMED at Q1 to opencode-agent-loop, displacing the weakest assumption → A0 opencode-feasibility = T0 weakest, mitigated by the backend-agnostic spine + the /v1 fallback]. Grounded against aml-substrate@df23bba + aml-casework@2381d71. Plan [[phases/phase-57-neural-drafter-backends]]; ledger Phase-57. Prior: signal-watch-local **Phase 56 — Chain workbench** COMPLETE — both beats met (delivery accepted 2026-06-17); functionally complete, active→completed pending the user's explicit call. The analyst case-workbench for the substrate→casework→verify chain: detection pre-baked/upstream (substrate evidence bundles vendored like the corpus pin), per case the casework consume + the cross-pillar verify run LIVE, stage-streamed → CONNECTED + a flag→corpus audit walk. T1–T5 [x]: data/chain-cases/CASE-P-0010361 (vendored byte-identical from aml-substrate@df23bba) + validate_chain_cases.py; the casework consume-CLI brief; scripts/serve_chain.py (stdlib companion, --selftest STUBBED→CONNECTED offline, pillar-status snapshot/restore, no sibling import); chain.html (dossier workbench, tests/chain.test.mjs 31/31); docs/chain-workbench.md. --check all 8/8 zero drift; chain.html NOT a build target. **TWO-BEAT — BOTH MET: beat 1 (spine) committed 16920d4; beat 2 — the casework consume CLI landed (aml-casework@f0542b7) → serve_chain drives it → e2e_chain_check --real CONNECTED, verified from signal-watch (pin re-grounded to f0542b7, launcher rebuilt, --check all 8/8).** Plan [[phases/phase-56-chain-workbench]]; ledger Phase-56 [all_accept:false; A4 casework-CLI HELD at beat 2 (brief-A0 resolved via the bundle-derived drafter_stub); A0/A2/A1'/A5 held]. Prior: **Phase 55 — Cross-pillar e2e bridge** COMPLETE [both beats; the 3-pillar full demo connects end to end, adversarially verified]. Prior: signal-watch-local **Phase 53 — C/D disagreement decomposition** DELIVERED + accepted [the measure-first workstream's 3rd number, FINISHES Phase 52; both gates accepted; --check all 7/7 zero drift]. MEASURED: enlarged random self-consistency C/D 0.677, κ≈0.65≈raw (genuine, not chance); decomposition of the 62 mismatches REFUTES the "scatter" guess (both-defensible 1.6%, neither 0%); direction confounded by context-asymmetry → committed-better 37% = clean LOWER bound, blind-better 61% = UPPER bound (NOT a committed-error rate). A0 mechanism correction recorded (predicted both-defensible inflation didn't fire; the confound is context-asymmetry). Follow-up reframed: an independent rater must be context-MATCHED]. Prior: **Phase 52 — C/D-tag reliability** DELIVERED + accepted [the 2nd number; C/D self-consistency ~62.5%/axis, Phase-34 corrections upheld 70.8% blind ~3:1; all assumptions HELD]. Program context: Phase 50 — AML program build. signal-watch = program-ARCHITECTURE home [blueprint + this lifecycle record]; the real build spun out one repo per pillar. Pillar 1 = data substrate → /Users/jwang/aml-substrate [Python]: **Phases 1–11 DELIVERED** 2026-06-13→16, 360 tests green, gen/ freeze byte-stable; **Phase 12 (multivariate subtlety — the §6 realism lever) direction set 2026-06-16**, planning handed to an aml-substrate-rooted session. Last touch: the 2nd cross-pillar program review [quick debrief, no signal-watch code change]. NOTE: this repo CANNOT drive aml-substrate's lifecycle [its dev-* hooks bind there]; the per-phase facts below will drift between reviews — trust aml-substrate's own repo for its live state.)

## Recommended Next Action

**Phase 67 DELIVERED + accepted 2026-06-22 (signal-watch-local, LITE).** The LIVE investigator workbench is now SHIPPABLE from a bare `git clone` of signal-watch — the full arc incl. the DECIDE signed-SAR finale runs with NO sibling repo, via `make setup`. Mechanism: VENDORED aml-casework into `vendor/aml-casework/` (its `src/aml_casework` + pyproject + uv.lock + README + its PINNED signal-watch corpus snapshot under `fixtures/corpus/`) via `scripts/vendor_casework.sh` + a `VENDORED_AT` pin (casework@81df91c); `make setup` builds the vendored venv (`uv sync`). `serve_chain.CASEWORK_DIR` now resolves `$AML_CASEWORK_DIR` > the vendored copy > `../aml-casework` sibling > a named GATED stage. **THE A0 GUARD EARNED ITS KEEP:** proving it in an isolated no-siblings clone surfaced a hidden coupling (casework's `corpus_grounding` verifier needs signal-watch's corpus snapshot) → vendored `fixtures/corpus/` too, so the SAR signs OFFLINE with no env. Boundary HELD: vendoring is a DISTRIBUTION choice, NOT import-coupling — the companion subprocesses the vendored CLI over the file-handoff; build.py NEVER imports casework; the 5 offline ship artifacts + 8 dists stay BYTE-FROZEN (`--check all` 8/8). The live tier needs Python ≥3.11 + uv (offline artifacts stay zero-dep). A composed mule still fail-closes at the verifier (the designed defensibility climax, unchanged). VERIFICATION: an isolation e2e (clone with no siblings → `make setup` → signed SAR offline, no model) + `uv run pytest` 18/18 + `node tests/workbench.test.mjs` 103/0 + `--check all` 8/8 ZERO dist drift + build.py imports no casework. Grounded against signal-watch HEAD (Phase-66 0fa3830 + the doc-hygiene 7340012) and aml-casework@81df91c. Plan [[phases/phase-67-shippable-live-workbench]]; ledger Phase-67. **No active signal-watch-local phase now. Run `/dev-plan` for the next.**

**Prior — Phase 66 DELIVERED + accepted 2026-06-22 (signal-watch-local, LITE).** Workbench richness — two signal-watch-LOCAL richness wins + the aml-substrate BO-graph emission handoff brief, bundled into one phase for cross-repo coherence. **T1:** re-vendored a wider **294-case slice** via a DETERMINISTIC substrate re-emit (src/ byte-identical to the f90bd39 pin → SAME population, pin holds) + raised curate caps + a combo-coverage pass → **all 23 population combos surfaced** (was 14), coverage re-measured **99/294**, gate funnel **189/66/39**; visible VOLUME + full combo spread, NOT detection difficulty. **T2 (PRIMARY):** deepened `data/osint/corpus.json` (now 14 subjects) with registry `relationships:[{src,dst,label,ownership_pct}]` MIRRORING the substrate `RelationshipEdge` schema; ownership is **record-SOURCED** (the gate reads label/pct/DIRECTION from the record, not the model — a live run caught the model fabricating a pct + flipping direction); direction-aware ownership edges, jurisdiction chips, ownership rendered "N pct" (never "N%"); two ownership→sanctions chains. **T3:** the aml-substrate BO-graph EMISSION handoff brief (`docs/substrate-bo-graph-emission-PLAN-BRIEF.md` — `PartyGraphView` + `related_parties[]` + a non-tautological C14 BO-disclosure detector + the 1:1 consume-side mapping to the T2 OSINT shape; the Phase-55–58 sibling-brief pattern, executed in the sibling, NOT built here). **T4:** EXECUTED the loop LIVE once; live + an adversarial pass (3 skeptics + re-verification) caught & FIXED **3 honesty flaws** (model-fabricated ownership pct → record-sourced; the abbreviation-period sentence-bridge guard; the model-authored synthesis swept for banned "%" tokens + a test). **THE GOVERNOR (load-bearing, A0):** every richness claim is demo-VISIBLE (more cases / deeper profiles / richer network), ZERO catch-rate/detection-difficulty/lift number — the substrate is single-signal-separable (composition architecturally subsumed by network linkage, P16), DOCUMENTED not hidden; the GATHER findings stay consistency-not-correctness. Companion-only (workbench/osint NOT ship targets; the honesty seam + always-on badge UNCHANGED); `uv run pytest` 18/18 · `node tests/workbench.test.mjs` 103/0 · all `--selftest` green · `build.py --check all` **8/8 ZERO dist drift** · build.py imports no sibling/companion. Plan [[phases/phase-66-workbench-richness]]; ledger Phase-66. Grounded against the committed Phase-65 workbench (signal-watch HEAD 761a446; aml-substrate@9d2e65c / aml-casework@81df91c).

**Prior — Phase 65 DELIVERED + accepted 2026-06-21 (signal-watch-local, LITE).** Agentic tool-calling — the investigator evidence-gathering loop. Signal-watch's FIRST multi-step tool-calling agent loop (`scripts/osint_tools.py`), COMPANION-ONLY (a `serve_workbench.py` + `workbench.html` GATHER beat between SIGNALS and DECIDE; NOT a 9th ship target). On a selected case the agent proposes a tool → calls a deterministic tool over a COMMITTED SYNTHETIC OSINT corpus (`data/osint/corpus.json` — registry/adverse-media/sanctions) → each finding is GROUNDED-OR-STRIPPED by the REUSED `news_ground` gate (the quote must be a real substring of the cited synthetic record; the synthesis distinct; the entity/link a name the record DECLARES — exact, not a free substring) → grounded tool-evidence extends the grounding chain + feeds a `liveGraphLayout` network. **THE HONESTY SEAM (held): consistency, not correctness** — the corpus is synthetic, the chain is authored-not-discovered, the grounded quote shows beside a SUBORDINATE illustrative synthesis, rejections render WITH reason, ZERO catch-rate/detection-lift number. **EXECUTED LIVE** over the mule (CASE-P-0002174 "Zane Zhao") against a local Qwen3.6-35B: the registry→sanctions chain fired (registry → discovered Crescent Dunes Trading FZE → the OFAC hit), **2 grounded / 0 dropped / 0 fabricated**, reproducible. TWO adversarial passes (pre-build design + post-build code review, every finding re-verified): security/§4.5/XSS CLEAN; 3 honesty-gate findings (fragment-entity bypass + 2 latent: tab/CR span-bridge, validator sweep gap) all FIXED + locked with selftests; fixing the medium surfaced a real link-granularity bug (an ungrounded optional link was killing valid findings → now drops to None, the finding survives, which is why the live chain fires reliably). `uv run pytest` 18/18; `node tests/workbench.test.mjs` 100/0; `osint_tools`/`serve_workbench --selftest` green; `build.py --check all` 8/8 ZERO dist drift; build.py imports no sibling/companion; 8 ship dists byte-frozen (companion-only). Plan [[phases/phase-65-agentic-tool-calling]]; ledger Phase-65. **No active signal-watch-local phase now — the remaining LFCM frontiers (the C3/C15 cross-pillar contract alignment; the substrate ownership/beneficial-owner graph emission) are SIBLING-rooted (aml-substrate/aml-casework), driven in their own repos, not from here. Run `/dev-plan` when the next signal-watch-local phase is chosen.**

**Prior — Phase 64 DELIVERED + accepted 2026-06-21 (committed 0ee4489; signal-watch-local, LITE).** Precedent-confidence gating engine + the elicitation loop (live): the Phase-63 workbench's STATIC precedent-confidence bucketer became a LIVE parameterized gating CONTROL — an explicit `gating_policy` + a pure `route(confidence, sample_size, policy) → {auto-decide|review|human-gate}` (reproduces the committed 129/52/19 funnel from policy defaults + the monotonicity invariant); the decision APPLIED (auto-clear → illustrative disposition; human-gate → escalate) + the session-only ELICITATION LOOP (/adjudicate → grow the combo's session precedent → recompute confidence → re-route; persists nothing — blueprint §14's continuous adjudication loop made live) + the gating panel UI; EXECUTED ONCE live over the 200-case slice. **The honesty seam HELD:** the loop shifts ROUTING via REAL §12-grounded sample-growth; the recorded/auto-applied DISPOSITION stays §14 ILLUSTRATIVE (NO §14 re-grounding from probe-history — the Phase-62 boundary). Companion-only (a serve_workbench extension, NOT a 9th ship target); `serve_workbench.py --selftest` + `node tests/workbench.test.mjs` + `uv run pytest` green; `build.py --check all` 8/8 ZERO dist drift. **Phase 65 is the direct follow-on: agentic tool-calling — the one remaining signal-watch-local Phase-63 follow-on.** Plan [[phases/phase-64-precedent-confidence-gating-engine]]; ledger Phase-64.

---

**Phase 62 DELIVERED + accepted 2026-06-21 (signal-watch-local, LITE).** Grounded probe-history consume (§12) + §14-frozen boundary + P22 pin re-ground. Consumed aml-substrate's P22 grounded probe-history projector into the §12 non-ship measurement layer (`probe_history_stats.py`); grounded `alert-history.json` = 4,966 real label-blind firings, substrate@ae98924 pinned. **The §12/§14 SOURCE BOUNDARY** (right for §12 MEASUREMENT, wrong for §14 — the substrate emits no adjudicable fact pattern) → the planned §14 dist/triage unfreeze STOOD DOWN at the T4 checkpoint, dist/triage BYTE-FROZEN. Honesty split: grounded detection / illustrative dispositions. Cross-pillar pins P20(34400e2)→P22(ae98924), ZERO tier movement (reachable-now 171); --check all 8/8. Plan [[phases/phase-62-grounded-probe-history-consume]]; ledger Phase-62. **This is the load-bearing boundary Phase 64 holds — route on §12, dispositions stay §14 illustrative, NO §14 re-grounding from probe-history.**

---

**Earlier phases (Phase 61 and back) — full narrative in git log + the `.dev-wiki/` journal + HANDOFF.md §8.** Phase 61 blueprint review + deep true-up + batched cross-pillar re-ground (ZERO tier movement, reachable-now 171). Phase 60 the first real reachable-now rise 93→171 (+78, all C7) + the real C8 e2e chain CONNECTS. Phase 57 live neural drafter backends (beat 1). Phases 55/56 the 3-pillar e2e bridge + chain workbench. Phases 51–54 the measure-first workstream (corpus redundancy / C/D reliability / disagreement decomposition / the C/D tag control). Phase 50 the AML program build kickoff (one repo per pillar; aml-substrate Pillar 1). Phases 47–49 the program blueprint + gate console + triage console (M9). NOTE: this repo CANNOT drive aml-substrate's lifecycle (its dev-* hooks bind there); trust aml-substrate's own repo for its live state.

## Active Phase

**[[phases/phase-67-shippable-live-workbench|Phase 67 — Make the LIVE investigator workbench shippable from a bare clone (vendor aml-casework + make setup)]]** (status: COMPLETE — DELIVERED + accepted 2026-06-22, LITE)

T1–T4 all [x]. VENDORED aml-casework (+ its pinned signal-watch corpus snapshot) into `vendor/aml-casework/` via `scripts/vendor_casework.sh` + a `VENDORED_AT` pin (casework@81df91c); `make setup` builds the vendored venv; `serve_chain.CASEWORK_DIR` resolves `$AML_CASEWORK_DIR` > vendored > `../aml-casework` sibling > GATED → a bare `git clone && make setup && python3 scripts/serve_workbench.py` runs the FULL live workbench, incl. the DECIDE signed-SAR finale, OFFLINE with the stub drafter and NO sibling repo. The A0 guard proved it in a no-siblings isolation clone (which surfaced + fixed the corpus-snapshot coupling); vendoring is distribution NOT import-coupling — build.py never imports casework; the 5 offline ship artifacts + 8 dists byte-frozen. **Verification:** isolation e2e (signed SAR offline, no model) + `uv run pytest` 18/18 + `node tests/workbench.test.mjs` 103/0 + `--check all` 8/8 ZERO dist drift + build.py imports no casework.

## Active Phase Contract

Phase: 67 — Make the LIVE investigator workbench shippable from a bare clone (vendor aml-casework + make setup). signal-watch-local **COMPLETE — DELIVERED + accepted 2026-06-22**, LITE, 4 tasks [x]. Objective MET: a recipient who clones signal-watch runs the FULL live workbench — incl. the DECIDE signed-SAR finale — with NO sibling repo, via `make setup`. VENDORED aml-casework (`src/aml_casework` + pyproject + uv.lock + README + its PINNED signal-watch corpus snapshot under `fixtures/corpus/`) into `vendor/aml-casework/` via `scripts/vendor_casework.sh` + a `VENDORED_AT` pin (casework@81df91c); `make setup` builds the vendored venv; `serve_chain.CASEWORK_DIR` resolves `$AML_CASEWORK_DIR` > vendored > `../aml-casework` sibling > GATED. **A0 EARNED ITS KEEP:** proving `make setup` + a signed SAR in an ISOLATED no-siblings clone surfaced a hidden coupling (casework's `corpus_grounding` verifier needs signal-watch's corpus snapshot) → vendored `fixtures/corpus/` too, so the SAR signs OFFLINE with no env. Live tier = "real pipeline + model-you-provide" (the deterministic stub-drafter SAR pipeline runs OFFLINE; the neural SAR/GATHER need a model set SERVER-SIDE, NOT bundled). A composed mule still fail-closes at the verifier (the designed defensibility climax, unchanged).
FROZEN (signal-watch, held this phase): the 8 ship dists byte-identical (index.html + config + 3 typology dists; corpus.html; news; console; triage; launcher dist/index.html) — Phase 67 added only `vendor/aml-casework/` (a companion DISTRIBUTION copy; the venv gitignored, the src committed) + `make setup` + the `serve_chain` resolution + docs (companion/distribution surface; NO dist touches; `--check all` 8/8 ZERO dist drift). The subprocess boundary held — `build.py` NEVER imports aml_substrate/aml_casework; the 8 dists are byte-frozen.
Program context — Phase 50 — AML program build: the data-substrate pillar (spun out). The real build lives in /Users/jwang/aml-substrate (its own git repo, Python, real-system-class) + /Users/jwang/aml-casework; signal-watch is the program-ARCHITECTURE home (docs/program-blueprint.md) + this lifecycle record + the demo artifacts. aml-substrate/aml-casework facts drift between reviews — code-verify the sibling's live state, never reason from loaded facts.
Abort: if `uv sync` won't build the vendored casework venv standalone (hidden coupling) → STOP-and-surface, escalate to a pre-built wheel (or Docker) per A0; any vendored import sneaking into build.py / any dist drift / the subprocess boundary broken / real data in the vendored source / a new ship target / a validator loosened to force a fit → STOP and surface (never re-baseline).

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| [[phases/phase-67-shippable-live-workbench]] Phase 67 DELIVERED + accepted 2026-06-22 = the LIVE workbench is shippable from a bare clone — VENDORED aml-casework (+ its pinned corpus snapshot) into vendor/aml-casework/ + make setup; serve_chain resolves vendored>sibling>GATED; the A0 guard proved it in a no-siblings isolation clone (signed SAR offline) + surfaced/fixed the corpus coupling; vendoring is distribution not import-coupling (build.py never imports casework; 8 dists byte-frozen). | high | 2026-06-22 |
| [[phases/phase-67-shippable-live-workbench]] Phase 67 PLANNED 2026-06-22 = make the LIVE workbench shippable from a bare clone by VENDORING aml-casework (copy + make setup + VENDORED_AT pin), companion still subprocesses it (distribution not import-coupling); live tier = real pipeline + model-you-provide; A0 verify-first (prove the venv builds in an isolated clone) else escalate to a wheel. | high | 2026-06-22 |
| [[phases/phase-66-workbench-richness]] Phase 66 DELIVERED + accepted 2026-06-22 = the two signal-watch-LOCAL richness wins (294-case slice, all 23 combos, 99/294 coverage, 189/66/39 funnel · a deeper OSINT corpus whose ownership records MIRROR the substrate RelationshipEdge, record-SOURCED ownership [the model can't fabricate the pct/direction], two ownership→sanctions chains) + the aml-substrate BO-graph emission handoff brief. Live execute-once + an adversarial pass caught & fixed 3 honesty flaws. THE GOVERNOR: demo-visible richness, ZERO detection-difficulty (single-signal-separable). Companion-only; --check all 8/8. | high | 2026-06-22 |
| [[phases/phase-66-workbench-richness]] Phase 66 DIRECTION (gate all_accept:true; A1 coherence + A2 slice positioned) = the two signal-watch-LOCAL richness wins (richer ~320 vendored slice via a deterministic substrate re-emit + a deeper GATHER OSINT corpus whose ownership records MIRROR the substrate RelationshipEdge schema) + the aml-substrate beneficial-owner/ownership-graph EMISSION handoff brief, bundled into one phase for cross-repo coherence. THE GOVERNOR: demo-VISIBLE richness only, ZERO catch-rate/detection-difficulty/lift (the substrate is single-signal-separable — composition subsumed by network linkage, P16). Companion-only / LITE. | high | 2026-06-21 |
| [[phases/phase-65-agentic-tool-calling]] Phase 65 DIRECTION (gate all_accept:true; A0/A2/A4 positioned explicitly, A1/A3 by precedent) = AGENTIC TOOL-CALLING (the one remaining signal-watch-LOCAL Phase-63 follow-on) — chosen at the Step-9 gate over (B) the sibling-rooted C3/C15 alignment [highest-value but un-drivable from this repo — it lives in aml-substrate/aml-casework] and (C) a consolidation/pitch-polish phase. Build signal-watch's FIRST multi-step tool-calling agent loop (verified: none exists — every companion is single-shot `call_llm`): add a GATHER beat to the workbench arc (between SIGNALS and DECIDE) — the agent proposes a tool → calls a deterministic tool over a COMMITTED SYNTHETIC evidence universe (`data/osint/**`, authored this phase) → each finding GROUNDED-OR-STRIPPED by the REUSED `news_ground` gate (claim ⊂ the tool's returned text; ungrounded DROP) → grounded tool-evidence extends the case grounding chain + feeds a `liveGraphLayout` network view; backend by NAME (Phase-57 §4.5) + capped iterations (Phase-47 D3) + deterministic-STUB fallback (demo runs model-free); EXECUTE the loop ONCE live over a marquee exemplar. Companion-only / LITE / persists nothing (reuse `liveGraphLayout`, NOT the DuckDB `news_store`). Feasibility code-verified firsthand this session (Explore scout). | high | 2026-06-21 |
| Phase 65 HONESTY SEAM (load-bearing, A0) = tool-evidence is GROUNDED-OR-STRIPPED against the tool's OWN returned text via the reused `news_ground` gate — the agent proposes, the deterministic gate disposes, ungrounded findings DROP. The gate verifies CONSISTENCY (claim ⊂ returned text), NOT correctness — the known limit of the grounding contract; BOUNDED here because the corpus is COMMITTED synthetic (not model-generated) + SURFACED honestly (badge on; ZERO catch-rate/detection-lift number). It shows tool-evidence EXTENDING the grounding chain like every other modality ("grounding is universal, the substrate varies"), NEVER that the synthetic findings are true; final correctness stays with the human / cross-pillar verifier. Fallback (A0): network-ER over the existing committed counterparty edges only. | high | 2026-06-21 |
| [[phases/phase-64-precedent-confidence-gating-engine]] Phase 64 DIRECTION (gate all_accept:true) = the precedent-confidence GATING engine (Phase-63 follow-on #3, "the LFCM elicitation-loop path" + the measuring→controlling pivot) — chosen at the Step-9 gate over agentic tool-calling (local) + the sibling-rooted C3/C15 alignment. Lift the static `_confidence` bucketer (500/50 + cleared-% 88/62/28) into a visible `gating_policy` + a pure `route()`; APPLY the decision; add the session-only ELICITATION LOOP; EXECUTE ONCE over the 200-case slice. Companion-only / LITE / persists nothing. | high | 2026-06-21 |
| Phase 64 HONESTY SEAM (load-bearing, A3) = the §12-routing / §14-illustrative-disposition invariant: the loop shifts ROUTING via REAL sample-growth (§12-grounded firing frequency); the recorded/auto-applied DISPOSITION stays labeled ILLUSTRATIVE; NO §14 re-grounding from probe-history (the Phase-62 stand-down holds). It shows WHERE human judgment gets spent, never that the auto-dispositions are correct. | high | 2026-06-21 |
| Phase 64 SHAPE (A1/A2) = companion-only (the loop is live-stateful → a serve_workbench extension, NOT a 9th ship target; Phase-49 new-ship→standard does NOT fire) + recompute from cases.json (n_precedent stored → records byte-frozen, no substrate re-emit, no sibling import); --check all stays 8/8. | high | 2026-06-21 |
| [[phases/phase-63-investigator-case-workbench]] Phase 63 DIRECTION (gate all_accept:true, RE-SCOPED post-gate to option B) = a COMPANION-SERVED INVESTIGATOR CASE WORKBENCH — the capstone sales-pitch demo — extending the existing `serve_chain.py` companion (a SERVED page, NOT a build/dist target; the chain.html/Phase-56 precedent) over a **REAL (synthetic) substrate alert POPULATION**. THE RE-SCOPE: the gate originally closed all-accept on a CURATED-4 / local-synthetic / self-contained set; the user then chose option (B) — feed the real substrate population. VERIFIED feasible this session: aml-substrate@f90bd39 ALREADY emits the rich investigator clutter (contract v0.2 PartyView: risk_rating/cdd_level/pep_tier/sanctions+adverse-media flags/occupation/source_of_funds+wealth/nationality/residency/NAICS/expected_monthly_volume+count, label-leak-proof) at POPULATION scale (`--emergence --monitor --emit-evidence --emit-screening`, ~500–2,000 deterministic synthetic bundles per 50k-client run) → NO substrate precursor phase. Two build parameters chosen: a BOUNDED slice (~100–300 cases), and REAL txn counterparty edges only (the formal ownership graph deferred to a substrate follow-on). The emit is TOOL-USE (file-contract; build.py never imports the siblings — the Phase-62 pattern); the slice is vendored into `data/workbench/` pinned @f90bd39 (`meta.synthetic:true`). Three beats: Beat 1 (clutter — a QUEUE of the real population + a per-case dense investigator page: real PartyView KYC + cross-account aggregation over subject.account_ids + txn detail + REAL counterparty edges; offline/model-free; a synthetic display identity over the real KYC since the substrate omits name/DOB by design) → Beat 2 (signals on — a presenter toggle overlays the GROUNDED signals + composes the risk picture, honest framing ONLY [grounding/clarity/corroboration; ZERO catch-rate/precision number — the detection-lift triple-null governs; the always-on badge stays]) → Beat 3 (live finale — the "decide" action wires the EXISTING serve_chain → casework consume [run as a TOOL] on a SELECTED real case, default Claude, configurable [openai/opencode/stub], stub fallback, NDJSON stage reveal + an HONEST REAL coverage statistic ["N of M cases ground end-to-end now; the rest is the visible frontier"]). The 4 named exemplars (textbook mule / false-positive trap / thin single-signal / ambiguous-medium) tagged WITHIN the real population; each case shows an illustrative precedent-confidence badge (real firing frequency; DISPOSITIONS label-blind illustrative — the Phase-62 split). **6 revised decisions:** (1) (B) substrate-grounded real population over the curated-4 hybrid [A0 flipped → VERIFIED feasible @f90bd39, no precursor]; (2) bounded vendored slice ~100–300 cases [A2'], the 4 shapes exemplars tagged within the population; (3) real txn counterparty edges only [A1'], ownership-graph emission deferred; (4) 3 emission gaps handled in signal-watch [synthetic display identity / cross-account aggregation / txn-edge network]; (5) coverage statistic REAL/measured, precedent-confidence anchored to real firing frequency, dispositions illustrative [A3', the Phase-62 split]; (6) companion-only / LITE holds, live finale = wiring + checkpoint, honest value framing = grounding/defensibility NOT catch-rate. DEFERRED to follow-ons: the agentic tool-calling (OSINT / counterparty / network-ER), the precedent-confidence GATING engine, and the substrate ownership-graph emission. ARCHITECTURE INVARIANTS: companion-served / off by default / scripted fallback; NO keys in the frontend (creds server-side, browser sends a NAME only — Phase-57 §4.5); synthetic data only; build.py NEVER imports aml_substrate/aml_casework; `--check all` 8/8 ZERO dist drift. Gate all_accept:true, RE-SCOPED to option B (A0 flipped → substrate-grounded real population VERIFIED @f90bd39 · A1' real txn edges only · A2' bounded vendored slice ~100–300 · A3' coverage REAL / dispositions illustrative). Ledger: Phase-63 block. Grounded against aml-substrate@f90bd39 + aml-casework@c6d8401 + corpus@472b44e. | high | 2026-06-21 |
| [[phases/phase-62-grounded-probe-history-consume]] Phase 62 DIRECTION (taken at the gate) = consume aml-substrate's **P22 "grounded probe-history"** into the §12 non-ship measurement layer (`probe_history_stats.py`) and (as PLANNED) the §14 triage console via a sanctioned USER-OVERRIDE unfreeze of `dist/triage`. **DELIVERED OUTCOME (2026-06-21): the §14 unfreeze was STOOD DOWN at the T4 pause-checkpoint** — reading `curate_triage_scenarios.py` firsthand surfaced the §12/§14 SOURCE BOUNDARY (the substrate's label-blind probe-history is RIGHT for §12 [firing/disposition MEASUREMENT] but WRONG for §14 [the triage console needs adjudicable FACT PATTERNS the substrate doesn't emit; curate couples via only 5 metadata fields on 7/20 scenarios, the TM-104 pair is C20 — no detector]); the user accepted the boundary → **dist/triage stays BYTE-FROZEN, NO re-curation**, the only dist change is the launcher pin cascade (dist/index.html, 1 line). Substrate P22 projects `monitor/compose.Dossier` into a grounded `alert-history.json` (conformance-validated against a VENDORED copy of signal-watch's OWN `probe_history_stats.py` @58925a8); it REPLACED the SYNTHETIC Phase-48 fixture as the §12 curate source (4,966 real firings, substrate@ae98924). The **C-code→TM-### namespace map is SHIP-LOAD-BEARING** (the silent_rules metric). build.py NEVER imports the substrate. **Honesty split (A3 accepted at delivery):** alert/entity/cited-evidence GROUNDED, dispositions label-blind ILLUSTRATIVE (every disposition-derived metric tagged "[over illustrative dispositions]"). Gate all_accept:false (A0 verify-first the P22 committed HEAD [T0 weakest] HELD with real work — the loaded P21 facts were stale · A1 end-to-end unfreeze REJECT→REVISED-ACCEPT w/ the T4 checkpoint — CHECKPOINT FIRED → §14 stood down · A2 map ship-load-bearing+selftested HELD · A3 don't-know→DELIVERY-accepted). Ledger: Phase-62 block. Grounded against aml-substrate@ae98924 + aml-casework@c6d8401 + corpus@472b44e. | high | 2026-06-21 |
| [[phases/phase-61-blueprint-implementation-review]] Phase 61 DIRECTION = AUDIT the blueprint §1–§15 against the as-built program (3 repos) + a deep VERIFIED true-up. The blueprint is a Phase-47/48 DESIGN snapshot whose §3 built-vs-design-stage partition is materially STALE (the program split into pillars P50 + built aml-substrate P1–20 @34400e2 + aml-casework P1–12 @c6d8401 + signal-watch's coverage map / e2e chain / C/D control; the four §3 "(design-stage)" rows now carry committed SIBLING implementation). The user chose C — report + deep blueprint revision (audit doc docs/blueprint-implementation-review.md + §3 markers + §8/§11/§13 prose), BATCHED the cross-pillar re-ground (coverage map @P18 + e2e pins → substrate@P20/casework@P12, verify-first NO-OP-expected, reachable-now stays 171), and re-sync blueprint-report.html. SPINE (A0, T0 weakest): verify-first gates every blueprint edit + a THIRD honest status (pillar-build/synthetic-scale) where binary built/design-stage overclaims a synthetic pillar build. Re-opens the FROZEN-set blueprint (USER OVERRIDE). LITE, NON-ship except the sanctioned launcher re-ground (Phase-60 Option-A cascade). Gate all_accept:true (A0 verify-first+3rd-status [T0 weakest] · A1 batched-re-ground-no-op-expected · A2 re-open-sanctioned · A3 html-resync · A4 non-ship-except-launcher). Ledger: Phase-61 block. Grounded against aml-substrate@34400e2 + aml-casework@c6d8401 + corpus@472b44e (HEADs verified LIVE via git log). | high | 2026-06-20 |
| [[phases/phase-60-consume-landed-sibling-halves]] Phase 60 DIRECTION = consume the now-landed sibling halves (a code-verified cross-pillar review found BOTH siblings moved 3 phases past the Phase-59 pins: aml-substrate@9c75c03 [P18 — emits the real C8 party-bearing v0.2 bundle] + aml-casework@c6d8401 [P12 — C7/C8/C14 grounding_replay assertions; C26 honest null]). The user up-scoped to the COMBINED scope (coverage-map consume + the REAL e2e chain on substrate's C8 bundle → a casework SAR) over the recommended coverage-only focus, and chose "verify first, then freeze" on the C7 +78. The VERIFIED finding: only C7 moves (the one screening cap whose behavior emerges) → all 78 C7 buildable signals flip to reachable-now (62 direct + 16 proxy; `is_reachable` fires before the data_source_class branch) → reachable-now 93→171, the first real rise; needs-detector 62→0, needs-view-exposure 70→69, needs-behavior 296→281. C8/C14/C26 do NOT move — `behavior_emergence` (data-only/absent) is the 3rd leg of the AND, NEITHER emission nor assertion work; Phase 59's "both halves must land" sharpens to "only the cap whose behavior genuinely emerges reaches reachable-now." (+78 corrects a plan-stage +62 undercount, caught at planning — verify-first's value.) LITE, NON-ship; --check all 8/8, no sibling import, build.py never imports `signal_coverage_map.py`/`e2e_chain_check.py`, ZERO ship artifacts. Gate all_accept:true (A0 clean-C7-only-+78 [T0 weakest, VERIFY-FIRST] · A1 C8/C14/C26-don't-move/emergent-behavior-is-the-bottleneck · A2 e2e-chain-honest-either-way · A3 non-ship). Ledger: Phase-60 block. Grounded against aml-substrate@9c75c03 + aml-casework@c6d8401 + corpus@472b44e. | high | 2026-06-19 |
| [[phases/phase-58-corpus-substrate-coverage-map]] Phase 58 DIRECTION = the user's REFRAME at the dev-plan gate: the synthetic-data + detection layer (Pillar 1, aml-substrate) was built BOTTOM-UP (6 detectors, "chosen-not-measured" thresholds) with NO systematic join to the corpus catalog (2,251 indicators / 523 buildable); design the detection layer AGAINST the corpus, targeting 200+ GROUNDED corpus signals (backed by ~15-20 CAPABILITY detectors — capability-scaled grounding_replay, NOT 200 detector implementations) to demonstrate signal/atom monitoring + narrative generation at scale. Deliverable = the executable corpus→substrate coverage MAP (`scripts/signal_coverage_map.py` — tiered reachability, observable MEASURED against the vendored CASE-P-0010361 emission, behavioral REASONED-flagged) + the doctrine-safe mapping design (`docs/corpus-substrate-coverage.md` + the integration contract) + prioritized sibling build briefs (the 200+ build runs sibling-rooted, the P55/P56/P57 rhythm). DOCTRINE BOUNDARY (A0, the weakest assumption): corpus drives detectors + observable-exposure TOP-DOWN; data/labels stay emergent BOTTOM-UP; the map measures behavioral gaps, never stamps them (the abort-line for the briefs). NON-ship; 8 dists byte-frozen, --check all 8/8; build.py never imports the script. Gate all_accept:true (A0 doctrine-reconcilability [T0 weakest] · A1 map+briefs-here / build-sibling-rooted · A2 200+-grounded-signals-not-200-detectors · A3 pinned-real-data + honest-tiering · A4 non-ship). Ledger: Phase-58 block. Grounded against aml-substrate@df23bba + aml-casework@2381d71. | high | 2026-06-18 |
| [[phases/phase-57-neural-drafter-backends]] Phase 57 DIRECTION = make the Phase-56 workbench's "LIVE neural SAR/STR draft" actually true end-to-end from signal-watch (today it only runs the stub) + open the drafter (casework's pluggable Drafter Protocol) to multiple backends: claude (OAuth, exists) · an OpenAI-standard /v1 endpoint (local models direct) · opencode. The user picked the live-neural frontier + reframed to add OpenAI-standard backend support; at Q1 "opencode support" = DRIVE DRAFTING THROUGH opencode's agent loop (NOT a model behind /v1) → A2 REFRAMED → beat 2 carries TWO casework adapters (thin /v1 + the meatier opencode-agent-loop). The gate stays the oracle. serve_chain selects backend by NAME + passes creds via SERVER-SIDE env (no creds in the browser); subprocess + file handoff only (no sibling import); chain.html NOT a build target, dists byte-frozen, --check all 8/8. TWO-BEAT, two-repo: beat 1 spine + live-claude now / beat 2 openai+opencode adapters gated on casework. Gate all_accept:true (A0 opencode-feasibility [T0 weakest, the A2 reframe displaced the /v1-cost assumption] · A1 /v1-adapter-cheap · A2 live-claude-drivable-now · A3 server-side-creds-boundary · A4 two-beat). Ledger: Phase-57 block. Grounded against aml-substrate@df23bba + aml-casework@2381d71. | high | 2026-06-18 |
| [[phases/phase-56-chain-workbench]] Phase 56 DIRECTION = an HTML frontend (living in signal-watch) for the substrate→casework→verify chain. REFRAMED (the user's "detection doesn't need runtime gen") from "orchestrator that runs the pipeline" → an ANALYST CASE-WORKBENCH: detection pre-baked/upstream (substrate evidence bundles vendored like the corpus pin); per case the casework consume (6 verifiers + a LIVE neural SAR draft, ClaudeDrafter) + signal-watch's e2e_chain_check cross-pillar verify run LIVE, stage-streamed → CONNECTED + the audit walk. Dev-time companion (serve_chain.py stdlib + chain.html), NEVER a ship artifact (chain.html not a build target; offline dists byte-frozen). Subprocess + file handoff only (no sibling import). A3 (defer live neural) REJECTED → live drafting pulled into v1 → the one sibling prerequisite = a thin casework consume CLI (a brief). TWO-BEAT: signal-watch spine now / the LIVE run gated on the casework CLI. Gate all_accept:false (A4 casework-consume-CLI-is-cheap [T0 weakest] · A0 detection-pre-baked/live-verify-is-proof · A1' subprocess-no-import · A2 vendored-bundle-library · A5 server-side-key+stub-fallback). Ledger: Phase-56 block. | high | 2026-06-17 |
| [[phases/phase-55-cross-pillar-e2e-bridge]] Phase 55 DIRECTION = connect the 3-pillar full demo; the wiring executes in SIBLING sessions, the AGGREGATION + FINAL DELIVERY sit in signal-watch. Shape = "verifier harness + walkthrough" (PROVE the join via a re-runnable cross-repo check reading committed sibling outputs, NO sibling import — the file-contract/vendored-pin pattern) over briefs-only or the Option-C console feed. A3 pure-non-ship REJECTED → re-scoped: the launcher (dist/index.html, 8th build target) + data/pillar-status.json is the ONE ship-surface addition; the 5 existing artifacts byte-frozen, --check all→8/8. Vertical slice = a C4-structuring case (code-verified reachable on the existing IND-11 pin: substrate StructuringDetector C4 → fin-2026-alert001:IND-11 → casework grounding_replay C4). TWO-BEAT delivery: the SPINE (T1–T6) now / the --real verification gated on the siblings. Gate all_accept:false (A0 §5a-ratifiable-now [T0 weakest] · A1 read-committed/no-import · A2 C4-slice DEFENDED-by-code · A3 reject→launcher). Ledger: Phase-55 block. | high | 2026-06-17 |
| [[phases/phase-54-cd-tag-control]] Phase 54 DIRECTION = the user's REFRAME ("prototype the tag control" over a 4th C/D measurement [the context-matched-rater close-out] or the heading→capability determinism probe) + a rigor ESCALATION ("execute the independent effectiveness challenge ONCE this phase"). Build the blueprint §4–§5 measured-not-gated control class CONCRETE + EXECUTABLE for the C/D tag — a NON-ship SR-11-7-class control doc (`docs/cd-tag-control.md`, OSFI E-23 + SR-11-7 Pillar-2 vocab from the aml-wiki) + `cd_correctness.py` extended with Krippendorff-α + `--control-check`/`--control-freeze` over a committed baseline (PASS on current corpus, BREACH on injected drift) — closing the named-OPEN Phase-47 T2/T3 control-story blocker; the Phase 51-53 numbers are the standing baseline. Also EXECUTES the Phase-52/53 deferred follow-up once: a context-matched + cross-family local-Qwen rater → the clean reliability number + α (probe-gated, Qwen confirmed UP; named Opus-context-matched fallback). NON-ship, ship corpus byte-frozen; the model dependency isolated in a dev-time companion `cd_rate_independent.py` (build.py imports neither it nor cd_correctness.py); NO blueprint edit (cites §4–§5, dodges the blueprint-report.html hand-sync drift). Gate all_accept:true (A0 local-model availability+competence [T0 weakest, probe-gated + named fallback] · A1 context-matched=committed-inputs · A2 replay-core-stays-pure / model-isolated-at-the-boundary · A3 non-ship + no-blueprint-edit). Ledger: Phase-54 block. | high | 2026-06-16 |
| [[phases/phase-53-cd-disagreement-decomposition]] Phase 53 DIRECTION = DECOMPOSE the C/D disagreement (the measure-first workstream's 3rd number; FINISHES Phase 52, not a new dimension). The Phase-52 report's own closing paragraph names the gap (the ~⅓ random-stratum mismatch error-vs-defensible share is "unmeasured"; "a clean decomposition would require the forced-pairwise adjudication on the SAME random-stratum mismatches") — Phase 53 IS it: enlarge the random stratum + Cohen's kappa (closes the "not chance-corrected" gap), then adjudicate its mismatches forced-pairwise → error / defensible-neighbour / committed-questionable / un-adjudicable. Same-Claude-family adjudicator → reported as a BOUND (error=lower / scatter=upper). Chosen over: an independent-rater panel + Krippendorff's α (needs a genuinely non-Claude rater — deferred-with-owner) · the heading→capability determinism probe (new dimension, deferred) · rooting into casework (un-drivable here). NON-ship, read-only, ship corpus byte-frozen. Gate all_accept:true (A0 same-family-adjudicator-is-a-bound [T0 weakest] · A1 decomposition-validity · A2 larger-N/kappa feasibility · A3 non-ship byte-frozen). Ledger: Phase-53 block. | high | 2026-06-16 |
| [[phases/phase-52-cd-correctness-measurement]] Phase 52 DIRECTION = measure the UNGUARDED C/D dimension (the grounding gate never checks the per-indicator capability/data-source tags), the measure-first workstream's 2nd number + Phase-51 sibling. STRATIFIED frame chosen (over 213-divergences-only / random-only): one blind same-model rater, two deterministic strata — RANDOM corpus stratum (self-consistency / reproducibility, NOT validated correctness) + the 213 Phase-34 divergences (hard stratum, uphold-which-pole). NON-ship, read-only, ship corpus byte-frozen; consensus/independence + larger N deferred-with-owner. Gate all_accept:true (A0 self-consistency-not-correctness [load-bearing] · A1 blindness-by-construction · A2 n=24/seed-0 illustrative · A3 non-ship byte-frozen). Ledger: Phase-52 block. | high | 2026-06-16 |
| Phase 51 DIRECTION = corpus redundancy measurement (the §13 fm-1 frontier), HYBRID non-ship: deterministic (typology×capability)×regulator co-occurrence upper bound + sampled blind consensus-class semantic-equivalence estimate; ship corpus byte-frozen. DELIVERED 2026-06-16: co-occurrence ≤ 0.325, genuine equivalence est ~1.4% strict / ~11.5% band (single-rater illustrative, consensus deferred) — the corpus's no-dedup stance empirically vindicated. Gate all_accept:false (redundancy framing rejected on honesty grounds → re-scoped to the hybrid). Ledger: Phase-51 block. | high | 2026-06-16 |
| Phase 50 DIRECTION (a major user REFRAME) = build the REAL AML program (blueprint §3 design-stage workloads → real systems), spun out one real-system-class repo PER PILLAR; signal-watch = architecture home. Pillar 1 = data substrate → /Users/jwang/aml-substrate (Python). HYBRID engine (own data model + Canadian/EMT/FINTRAC; reuse AMLSim flow/topology in ph2; learn AMLgentex calibration); foundation-first; DATA-FIRST/EMERGENT doctrine (typologies emerge, never injected); SCRIPTED generation (no runtime LLM, LLM only authors scripts); grounded in researched real schemas+distributions. Gate all_accept: false (A1 foundation-first/emergence-ready · A2 build-direct [user override] · A3 small-first · A4 hybrid→deferred-ph2). Phase 1 DELIVERED 2026-06-13 (54 tests green). Ledger: assumption-ledger.md Phase-50 block; design: aml-substrate/DESIGN.md. | high | 2026-06-13 |
| [[decisions/phase-49-triage-loop-embryo]] D1–D8 (Phase 49 DIRECTION = the OFFERED recommendation taken as offered — first non-reframe in 3 gates) = §14's continuous adjudication loop as the FIFTH ship artifact triage.html → dist/triage (console BYTE-FROZEN, sibling not extension): D1 fifth ship artifact; D2 curated committed scenario dataset (console-cases layering — curate script reads probe-history at AUTHORING time only, rule text embedded, build.py reads only scenarios.json); D3 strata+controls first-class data, build-boundary validated; D4 §14 disposition grammar incl. need-more-info → C/D data-gap ledger; D5 decisions-not-correctness reveal + discovery-outputs ledger; D6 US-federal-only novel stratum (allowlist pre-committed); D7 NAMED evidence-panel verifier (structural sharing BY REFERENCE, template skeletons, ~16+4 ceiling, fired-rule state universal); D8 NO fake instrumentation (render-computed agreement arithmetic w/ measurement definitions; seeded LABELED second-rater replay; policy-gap grammar escape). | high | 2026-06-12 |
| [[decisions/phase-48-history-lfcm-blueprint-extension]] D1–D5 (Phase 48 DIRECTION, the user's OWN planning input + the Step-9 triage idea) = ADDITIVE blueprint extension + synthetic-history probe: D1 additive (vs re-centered v2 / separate lfcm.md, rejected); D2 LFCM = grounded signal LIBRARY + composition layer, never a Tier-1 monolith (the library IS the model inventory; LFCM = the program-level name); D3 dossier-now / score-deferred-with-owner; D4 triage loop = history-sourced scenarios (the A1 condition: decisions-not-correctness ground truth; process-inconsistency + policy-gap discovery), gap-discovery value floor, calibration upside, agreement self-instrumenting; D5 probe = advisory-shaped SYNTHETIC rulebook through the UNCHANGED gate w/ the honest shape caveat; D6 (USER ADDITION at plan close) the HTML blueprint report — docs/blueprint-report.html, self-contained offline NON-ship (booth precedent), full-blueprint coverage, system-flow + grounding-chain SVG centerpieces, DESIGN-labeled. | high | 2026-06-12 |
| [[decisions/phase-47-agentic-aml-program-design]] D1 (Phase 47 DIRECTION, user REFRAME at the dev-plan gate — 6th in 7 gates, ALTITUDE demo→program) = a program-DESIGN phase (the first; STANDARD ceremony): (a) engineering blueprint docs/program-blueprint.md, (b) the GATE CONSOLE — 4th single-file offline vision artifact (MVP: ONE gate class, C/D tag adjudication, committed licence-clean divergence dataset), (c) OSFI E-23 wiki gap closed FIRST. | high | 2026-06-12 |
| D2 (Phase 47 DESIGN SPINE — A1 REJECT-BY-REFRAME → A1′) = grounding is UNIVERSAL; what varies per workload is the grounding SUBSTRATE + verifier (advisory text → substring; monitoring → signals + transactional/non-transactional data, referential/lineage; SAR/STR → guidance/policies/signals/data, citation); grounding CHAINS — defensibility = the audit walk down the chain; nothing ungrounded survives a gate; each workload's substrate + verifier NAMED, never assumed. On top: measured dimensions, human-judgment graded gates, MANDATED-ACCOUNTABILITY class (A5 — SR 11-7 Pillar 3). | high | 2026-06-12 |
| D3 (Phase 47 AGENTIFICATION CRITERION) = the agent-runtime-adoption-probe rule program-wide: agentify only where a direct baseline measurably leaves recoveries (A/B probe first; n=1 caveat preserved); max-iteration caps mandatory; gates surface ALL scored dimensions (the news-lift FINDING: unsurfaced dimensions silently regress). | high | 2026-06-12 |
| D4 (Phase 47 REGULATORY ANCHOR) = layered: SR 11-7 / 2021 interagency statement structural backbone + OSFI E-23 audience-jurisdiction anchor (researched T1, primary source) + FINTRAC content layer. | high | 2026-06-12 |
| D5 (Phase 47 HONESTY TRANSITION — A4) = demo honesty constraints explicitly dispositioned (survive vs transform); "95/5" = design DIRECTION, never a stated target ratio (any ratio carries its measurement definition — the Phase-45 fake-lift deletion pattern forward); drift/ongoing-monitoring designed-now vs deferred-with-owner; HANDOFF "ships a demo, not a system" dispositioned in one line. | high | 2026-06-12 |
| [[decisions/phase-46-corpus-live-derivation]] D1 (Phase 46 DIRECTION) = corpus live derivation mode: the inverted extraction boundary extends to an automated local-model loop, companion-served like news live; opencode strictly as the derivation-loop RUNTIME. Outcome: direct + ONE violation-guided retry CHOSEN at the T1 checkpoint (opencode 3.1× wall, loop never engaged). | medium | 2026-06-10 |
| D2–D4 (Phase 46 HARNESS/MODEL/BOUNDARIES) = T1 probe-gated harness w/ user checkpoint; existing Qwen + FINTRAC /intel/ held-out doc (probe-local, not committed); /*LIVE_*/ strip byte-identity + DISPLAY/PROPOSE-only output. ALL held (A1–A4). | medium | 2026-06-10 |
(Earlier decision rows — Phase 45 and back — trimmed to the ~10-row cap at the Phase-47 plan, 2026-06-12: the full record lives in `articles/decisions/*`, the phase journals, and the archived tasks.md phase headers. Load-bearing carry-overs already live in `.claude/rules/working-knowledge.md` + the non-negotiables.)

## Blockers and Open Questions

- [RESOLVED 2026-06-21 · planning, by the Phase-64 gate A0–A4] The Phase-64 direction question (which Phase-63 follow-on) closed at the assumption gate (all_accept:true — warned + restated per gate protocol, an all-accept on a NEW mechanism direction): DIRECTION = the precedent-confidence GATING engine ("the LFCM elicitation-loop path") over agentic tool-calling (local) + the sibling-rooted C3/C15 alignment. A0 [T0 weakest] the live elicitation loop is the TARGET with a build-time honesty checkpoint (route via real §12 sample-growth; dispositions stay illustrative; if it can't be kept honest → scope to display-only batch routing, report don't force). A1 companion-only (serve_workbench extension, NOT a 9th ship target). A2 recompute from cases.json (records byte-frozen, no sibling import). A3 the Phase-62 §12/§14 boundary (NO §14 re-grounding from probe-history). A4 EXECUTE ONCE live over the 200-case slice.
- [RESOLVED 2026-06-21 · planning, by the Phase-63 gate A0–A3, then RE-SCOPED post-gate to option B] The five Phase-63 planning questions (artifact shape / clutter data source / confidence-precedent / beat-3 backend / ceremony, raised 2026-06-21 by the state loader) closed at the assumption gate (all_accept:true). ARTIFACT SHAPE → **COMPANION-ONLY** — extend the existing `serve_chain.py` companion with a new `workbench.html` (a SERVED page, NOT a build/dist target; the chain.html/Phase-56 precedent) — NO new build/dist target (the "dist/workbench" the gate floated did NOT survive — companion-only). CEREMONY → **LITE** (the Phase-49 new-ship→standard precedent does NOT fire — companion-only, no build target). CONFIDENCE/PRECEDENT → **DISPLAY-ONLY** (A1) — an illustrative "N similar prior dispositions, chosen-not-measured" badge per case (the auto-gating engine is a FOLLOW-ON). BEAT-3 BACKEND → **the EXISTING serve_chain `claude` backend** (A2 — casework's pluggable Drafter Protocol, server-side creds), NOT a new direct SDK path; wiring + a live-once CHECKPOINT (real debugging beyond creds is a surfaced finding). WEAKEST ASSUMPTION (A0) = verify-first the live finale (wire + run it live once early; the Phase-57 Claude path needing real debugging beyond creds → STOP-and-surface). Honest value framing (delivery-checked): clutter→clarity = grounding/defensibility/corroboration, NOT a higher catch rate (the substrate detection-lift triple-null governs). **CLUTTER DATA SOURCE — RE-SCOPED post-gate (the only changed disposition):** the gate closed on a HYBRID (marquee real-vendored CASE-P-0010361 + 2-3 local-synthetic variants); the user then chose option **(B) — feed the REAL (synthetic) substrate alert POPULATION.** VERIFIED feasible this session: aml-substrate@f90bd39 ALREADY emits the rich v0.2-PartyView investigator clutter at POPULATION scale (~500–2,000 deterministic synthetic bundles per 50k-client run) → NO substrate precursor phase. P63 vendors a deterministic BOUNDED slice (~100–300 cases, TOOL-USE / file-contract, pinned @f90bd39, meta.synthetic), REAL txn counterparty edges only (ownership-graph emission deferred to a substrate follow-on), the 4 named exemplars tagged WITHIN the population; an HONEST REAL coverage statistic is computed over the slice. 3 emission gaps handled in signal-watch (NOT a substrate change): synthetic display identity over real KYC; cross-account display aggregation; the txn-edge network. The other four dispositions (companion-only / LITE / confidence display-only / beat-3 backend) HOLD. Plan articles/phases/phase-63-investigator-case-workbench.md; ledger Phase-63 block; no spec / no separate decision articles (lite). (raised + resolved 2026-06-21; re-scoped to option B 2026-06-21)
- [RESOLVED 2026-06-20 · planning, by the Phase-62 gate A0–A3] The three Phase-62 planning questions (DIRECTION / BLAST RADIUS / RE-GROUND SCOPE, raised 2026-06-20) closed at the assumption gate (all_accept:false). DIRECTION → **consume the substrate P22 grounded probe-history END-TO-END** (the §12 measurement layer AND the §14 triage console) over re-ground-only / sibling-only. BLAST RADIUS → **YES, a sanctioned ship-artifact unfreeze** of `dist/triage` (USER OVERRIDE, the Phase-60 launcher / Phase-61 blueprint precedent; the OTHER 7 dists byte-frozen) — A1 was REJECTED then REVISED-ACCEPTED with a built-in T4 pause-checkpoint (no clean §14-strata map / broken build-boundary validation → PAUSE+report, do NOT force the fit). RE-GROUND SCOPE → **batch the verify-first coverage-map + e2e re-ground in** (T6, the known zero-tier-movement no-op absorbed; pins move to substrate@<P22, pinned at T1>). WEAKEST ASSUMPTION (A0) = verify-first the substrate P22 COMMITTED HEAD (`git -C /Users/jwang/aml-substrate`; pause+ask if uncommitted — running `--probe-history` is sanctioned tool-use, file-contract output, NOT lifecycle-driving). A2 = the C-code→TM-### map is ship-load-bearing + grounded/null-honest. A3 (honesty framing: grounded detection / illustrative dispositions) = DON'T-KNOW → routed to the DELIVERY-gate checkpoint (the user checks it before anything is called "grounded"). **DELIVERY OUTCOME (2026-06-21): the authorized §14 unfreeze was STOOD DOWN at the T4 pause-checkpoint** — the substrate's label-blind probe-history is the §12-right/§14-wrong source (the triage console needs adjudicable fact patterns the substrate doesn't emit); the user accepted the boundary → dist/triage stayed BYTE-FROZEN, NO re-curation; the only dist change is the launcher pin cascade. A3 framing accepted at delivery (adversarial overclaim audit found none). Plan articles/phases/phase-62-grounded-probe-history-consume.md; ledger Phase-62 block; no spec (lite). (raised + resolved 2026-06-20; delivered 2026-06-21)
- [RESOLVED 2026-06-20 · planning, by the Phase-61 gate A0–A4] The Phase-61 deliverable-shape + scope questions closed at the assumption gate (all_accept:true): DELIVERABLE → **C (report + deep blueprint revision)** — the audit doc `docs/blueprint-implementation-review.md` + the §3 marker true-up + §8/§11/§13 prose revision, over report-only / markers-only; SCOPE → **BATCH the cross-pillar re-ground in** (A1 up-scope — re-ground the coverage map + e2e pins to substrate@34400e2/casework@c6d8401, verify-first, NO-OP-expected) over my recommended report-only review boundary; HTML → **re-sync blueprint-report.html** revised sections (A3). WEAKEST ASSUMPTION (A0) = verify-first gates every blueprint edit + a THIRD honest status (pillar-build/synthetic-scale) where the binary built/design-stage would overclaim a synthetic pillar build — ACCEPTED (the spine; prevents the revision laundering "synthetic pillar build" into "built workload"). Re-opens the FROZEN-set blueprint (A2, USER OVERRIDE). Plan articles/phases/phase-61-blueprint-implementation-review.md; ledger Phase-61 block; no spec (lite). (raised + resolved 2026-06-20)
- [RESOLVED 2026-06-16 · planning, by the Phase-54 gate A0–A3] The Phase-54 direction closed at the assumption gate (all_accept:true): the user REFRAMED off a 4th C/D point-measurement (the context-matched-rater close-out / the heading→capability determinism probe) to **prototype the tag CONTROL** — the blueprint §4–§5 measured-not-gated control class made CONCRETE + EXECUTABLE for the C/D tag (a NON-ship SR-11-7-class doc + a runnable `--control-check`/`--control-freeze` harness), AND chose to EXECUTE the independent effectiveness challenge once this phase (a context-matched + cross-family local-Qwen rater, not just schedule it). WEAKEST ASSUMPTION (A0) = the local model is available + competent at context-matched closed-vocab C/D tagging — ACCEPTED probe-gated (Qwen confirmed UP; T1 `--probe` checks competence; named Opus-context-matched fallback if it fails, independence then scheduled). Plan articles/phases/phase-54-cd-tag-control.md; ledger Phase-54 block; no spec (lite). **This phase resolves the long-open Phase-47 T2/T3 control-story blocker below.**
- [RESOLVED 2026-06-16 · planning, by the Phase-53 gate A0–A3] The Phase-53 direction (which signal-watch-local measure-first candidate) closed at the assumption gate (all_accept:true): DIRECTION = DECOMPOSE the C/D disagreement (finish Phase 52 — the report's own named missing measurement — over an independent-rater α-panel / the heading→capability probe / rooting into casework); WEAKEST ASSUMPTION (A0) = a same-Claude-family adjudicator is independent-enough ONLY as a BOUND (error=lower / scatter=upper), ACCEPTED with genuine independence deferred-with-owner. Frame-check recorded: the higher-value casework audit-walk-to-source is un-drivable from a signal-watch-rooted session. Plan articles/phases/phase-53-cd-disagreement-decomposition.md; ledger Phase-53 block; no spec (lite).
- [RESOLVED 2026-06-12 · planning, by the Phase-49 gate A1–A4] The three Phase-49 planning questions (direction / deliverable shape / ceremony, raised 2026-06-12 by the state loader) closed at the assumption gate: DIRECTION = the §14 triage-loop embryo (the offered recommendation taken as offered — first non-reframe in 3 gates; the LFCM-path option that is BOTH demo-able and on the user's stated target vision); DELIVERABLE = demo-first FIFTH ship artifact triage.html → dist/triage (A1 demo-first accept; A4 ship-class discipline over the non-ship docs/ shape); DATA PATH = console-cases layering accepted after a don't-know round (A2: curate script reads probe-history at AUTHORING time only, build.py reads only the committed build-boundary-validated scenarios.json); CEREMONY = STANDARD (new ship artifact). Decision article articles/decisions/phase-49-triage-loop-embryo.md (D1–D8, confidence high); ledger block in assumption-ledger.md; spec specs/phase-49-triage-loop-embryo.md.
- [RESOLVED 2026-06-12 · planning, by the Phase-48 gate A1–A4 / D1–D5] The five Phase-48 planning questions closed at the assumption gate (all_accept: false — A1 [HIGH] ACCEPT WITH CONDITION, A2–A4 accept): HISTORY ROLE → all three, each a NAMED role with substrate + verifier — derivation substrate (the inverted extraction boundary) / §6 probe baseline (legacy TM = the A/B comparator) / outcome-feedback embryo (filings = biased Class-M material) [A3]; HISTORY HONESTY DOCTRINE → yes — the explicit blueprint line “history is evidence, never ground truth” [A3]; LFCM ARCHITECTURAL READING → a grounded signal LIBRARY + small composition layer; the library IS the model inventory (per-signal lifecycle, tiered validation); never a Tier-1 mega-model as the validation unit [A2/D2]; LFCM PLACEMENT → BOTH — a 6th §3 workload row (entity/event risk decisioning) + a program-level LFCM section (library-not-monolith, dossier-now/score-deferred-with-owner, 5 named failure modes, §11 chain-1 re-point) [D2/D3]; DELIVERABLE SHAPE → additive blueprint extension + a fully SYNTHETIC advisory-shaped probe through the EXISTING FROZEN gate (zero gate edits; outputs outside every build.py-read path) + the continuous adjudication loop section (the user's mini-triage idea; the A1 condition: history-sourced scenarios, decisions-not-correctness, process-inconsistency + policy-gap discovery) [D1/D4/D5]. Decision article articles/decisions/phase-48-history-lfcm-blueprint-extension.md (confidence high); ledger block in assumption-ledger.md; spec specs/phase-48-brownfield-history-lfcm.md.
- [RESOLVED 2026-06-12 · planning, by the Phase-47 gate A1′–A5 / D1–D5] The six Phase-47 planning questions closed at the assumption gate (all_accept: false — A1 [HIGH] rejected-by-reframe round 1 → A1′ accepted; A2–A5 accept): DELIVERABLE SHAPE → engineering blueprint docs/program-blueprint.md + the GATE CONSOLE 4th vision artifact + a spec contract; ceremony ESCALATED to STANDARD [D1]; REGULATORY ANCHOR → both layered — SR 11-7 backbone + OSFI E-23 (audience jurisdiction; wiki gap closed by T1, primary source) + FINTRAC content layer [D4]; 95/5 OPERATIONAL DEFINITION → the gate taxonomy (4 classes) decides which gates stay deterministic-binary vs non-binary human judgment, and the agent-runtime-adoption-probe rule IS the program-wide agentification criterion (caps + surface-all-dimensions) [D2/D3]; DEFERRED-LIST disposition → re-sequenced as capability roadmap inside the blueprint at T3 (the candidates stay live in the DEFERRED list above until a code phase picks them) [D5/T3]. Decision article articles/decisions/phase-47-agentic-aml-program-design.md (confidence high); ledger block in assumption-ledger.md; spec specs/phase-47-agentic-aml-program-design.md.
- [RESOLVED 2026-06-16 · Phase 54] CONTROL STORY for the measured-not-gated dimensions — the production-grade SR-11-7-class control DESIGN (cadence, sampling, ownership, where it sits in the 5% charter), named at Phase-47 T2/T3 but not written, is now **written + executable for the C/D tag**: `docs/cd-tag-control.md` (SR-11-7 Pillar-2 + OSFI E-23 grounded; scope/risk-tier/baseline/cadence/trip-wires/independent-challenge/ownership/escalation/audit-walk) + `cd_correctness.py --control-check`/`--control-freeze` (PASS on the frozen corpus, BREACH on injected drift). The first concrete instance; the reusable program-wide control PATTERN (other judgmental dims) + a human-rater panel stay deferred-with-owner. (raised 2026-06-12; resolved 2026-06-16)
- [OPEN · resolves at Phase-47 T3] HONESTY→EFFECTIVENESS measurement layer — the validation story (no-ground-truth doctrine, drift/ongoing-monitoring per SR 11-7 Pillar 2, what survives of the always-illustrative posture) gets its EXPLICIT designed-now vs deferred-with-owner disposition in T3; the committed quality-harness baseline (--check/--freeze) is the named embryo. (raised 2026-06-12)
- [RESOLVED 2026-06-10 · planning, by the Phase-46 gate D1–D4] The four Phase-46 planning questions closed at the dev-plan gate: TIMING + DIRECTION → the user REFRAMED off all 5 DEFERRED candidates (the 5th reframe in 6 gates) to **corpus live derivation mode** ("live mode with local model for the corpus demo as well, ideally with a better harness integration like opencode" + a pasted practitioner report) — a feedback-robust back-end/live-layer direction; post-presentation audience feedback surfaces against the active phase before T2 if it lands. The IF-(third-jurisdiction) AUSTRAC/UK derivation-surface research gap and the IF-(residuals) mojibake-repair measurement (fresh 2026-06-10: 161 derived + 501 md marker chars, below the carried ~2.2K; repair needs a conscious unfreeze) travel with their still-deferred candidates in the DEFERRED list above. Assumption gate closed 2026-06-10, all_accept: TRUE (A1–A4); decision article articles/decisions/phase-46-corpus-live-derivation.md; ledger block in assumption-ledger.md. KNOWLEDGE GAPS carried to impl: whether the FINTRAC /intel/ frontier doc parses under the existing rf_region anchors (T1 exercises it; extension REGRESSION-GATED); whether opencode + the existing Qwen can drive a reliable tool-calling agent loop at all (T1's whole job); full-advisory derivation wall-time at local-model speed (T1 measures — may force a short-doc demo shape or per-indicator staged reveal).
- [RESOLVED 2026-06-10 · planning, by the Phase-45 gate A1–A5] The five Phase-45 planning questions all closed at the assumption gate (all_accept: false — A1 don't-know round 1 → defended with worked real-value examples → accepted): LIFT SHAPE → R2 honest real-inventory counts (no performance claim, disclaimer removed, R1 zero-numbers copy-only fallback) [A1]; SHOWCASE CONSISTENCY → Act-5 byte-frozen, deliberate cross-artifact divergence accepted [A2]; INCONSISTENCIES INVENTORY → already measured by the two pre-gate review agents (findings ranked in articles/decisions/phase-45-corpus-presentation-polish.md; the gate fix is COPY + stagecraft, no interaction redesign [A3]); DELIVERY-REVIEW ARTIFACT → corpus.html copy edits + presenter/demo-path notes into tests/smoke-checklist.md [A4 attribution extension + A5]; FEEDBACK ABSORPTION → T5 user-walkthrough FREEZE checkpoint, dispositions ranked + time-boxed, overflow explicitly deferred [A5]. Decision article approved; ledger block in assumption-ledger.md.
- [RESOLVED 2026-06-10 · planning, by Phase-44 D1–D5] Phase-44 DIRECTION + the conditional IF-… sub-questions — the user REFRAMED off the offered candidates at the dev-plan gate (the 4th reframe in 5 gates): Phase 44 = LIVE EXTRACTION QUALITY — red-flag RECALL (obvious high-risk-country wire flags missed in limited real testing) + alias-assignment PRECISION (entities assigned aliases clearly not theirs) + a FRESH dedicated processing page + the user's own harness hypothesis accepted as the T1 frame + SPEED resolved as quality-gated T4. Measure-first/classification-first (D2: committed targeted harness w/ alias-OWNERSHIP scoring + synthetic wire notes embedding the user's sample sentences, local-only; both failures reproduced + classified before any fix); fixes through proven seams (D3: prompt-regression gate + the news_ground regate procedure; structural EXTRACT_SCHEMA change = a surfaced finding); quality-gated speed (D4); LIVE-region-only UI + lossless hygiene (D5). The IF-(hygiene) and IF-(verify-latency) sub-questions were ABSORBED into Phase 44 (T6/T4); the IF-(FINTRAC /intel/)/(third jurisdiction)/(fuzzy-merge)/(bulk scan) sub-questions travel with their candidates in the DEFERRED list above. Assumption gate closed 2026-06-10: A1 accept-with-conditions (sample sentences; speed → quality-gated T4), A2 accept, A3 don't-know→defended→accept, A4 accept; ledger block in assumption-ledger.md; decision article articles/decisions/phase-44-live-extraction-quality.md. KNOWLEDGE GAPS carried to impl: whether the user's wire-miss material reproduces (T1's job); the actual failure classes (T1); whether batched verify preserves quality (T4).
- [RESOLVED 2026-06-10 · planning, by Phase-43 D1–D5] Phase-43 DIRECTION + the conditional IF-(a)…(e) sub-questions — the user REFRAMED off all five carried candidates at the dev-plan gate (none picked; all stay in the DEFERRED list above, their open sub-questions traveling with them): Phase 43 = LIVE PIPELINE ROBUSTNESS + PROGRESSIVE PRESENTATION — a live test on a REAL investigation note timed out ~200s with NO intermediate feedback; extraction must work regardless of article size/complexity, and the page must show staged progress instead of an opaque wait. Measure-first (D2: the stress harness classifies read-timeout vs finish_reason=length truncation vs context overflow [/props n_ctx] vs intrinsic degradation; the ~200s failure class must be reproduced); rendering = stage-completion-driven, corpus-demo-style (D3 — A2' after a reject-by-reframe round 1: NO partial-JSON parsing, NO visible token stream; internal stream:true serves only the timeout fix + a token counter); scope = pure transport (D4: gate/prompt/schema/store-writes/fixtures FROZEN, call_llm signature preserved — the fixture seam); privacy (D5: the real note never committed, stress notes synthetic, commercial captures local-only, fixtures US-federal-only). Assumption gate closed 2026-06-10: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept; ledger row in assumption-ledger.md; decision article articles/decisions/phase-43-live-pipeline-robustness.md. KNOWLEDGE GAPS carried to impl: the server's actual n_ctx (measured at T1); whether stream:true composes with the json_schema strict grammar in the user's llama-cpp build (verified at T2); the actual failure class (T1's whole job).
- [RESOLVED 2026-06-10 · planning, by Phase-42 D1–D5] Phase-42 DIRECTION + the conditional IF-(a)…(f) sub-questions — the user picked (a) anchor dossier view + a per-scan network visualizer, with the (c) CLAUDE.md trim bundled as T6. The IF-(a) sub-question resolved IN the design: a surfaced CONFLICT = same anchor + same kind + different value (the both-kept rows per Phase-41 D2; presentation-only flag, never auto-resolve); the view = a `GET /anchor` companion route + a news.html LIVE-region panel (the watchlist-panel pattern, NOT a separate page), the per-scan graph as the navigation surface. The IF-(b)/(d)/(e)/(f) candidates stay in the DEFERRED list above (their open sub-questions — Python-side fuzzy scorer port-vs-move + reversible merge mechanics; rf_region anchor coverage for the /intel/ SBs; the third licence basis; bulk-scan wall-time/deliverable — travel with them). Assumption gate closed 2026-06-10: A1/A3 accept, A2 don't-know→A2' accept (evidence check: the 7 fixture articles are case-disjoint, zero cross-article entity overlap → a committed SYNTHETIC investigation-note seed replaces any re-scan-different-articles script), A4 accept-with-condition (SVG is the INITIAL implementation for demo testing), A5 scope constraint (pure consumption); ledger block in assumption-ledger.md; decision article articles/decisions/phase-42-anchor-dossier-network-view.md.
- [OPEN · planning, gated at Phase-38 T1 CHECKPOINT] Phase-38 QWEN-VS-CLAUDE DIVERGENCE ADJUDICATION — the Half-A evaluation reports a Qwen-vs-Claude INTER-RATER AGREEMENT + the actual divergences (which entities/flags one rater caught and the other missed) over the committed DOJ/OFAC articles. The Claude reference is a SECOND rater, NOT ground truth; the user adjudicates the divergences at the T1 checkpoint before the goldens lock. NO accuracy/precision/recall number is presented as real; the always-on badge stays. Resolved at the checkpoint (T1), not blocking the capture itself.
- [OPEN · planning, gated at Phase-37 T1] Phase-37 TYPOLOGY MODEL — single primary typology per indicator vs a multi-label SET vs an honest indicator-level "sector-general" bucket. The T0 risk: many sector-page indicators are genuinely CROSS-CUTTING (structuring serves drug-trafficking AND fraud AND TF), so forcing one typology may merely RELOCATE the catch-all to indicator level. The T1 measure-first probe (section→typology determinism + the cross-cutting fraction over the ~589 unique texts) decides this AT A CHECKPOINT with the user before any tagging; also open at the checkpoint: whether the 3 specific sector pages (casinos/real-estate/VC) fold into per-indicator tagging for consistency, and which closed-vocab EXTENSIONS (if any) are grounded. Do NOT tag before the checkpoint lands.
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- [CANDIDATE · future phase, raised 2026-06-08 by the user] **SearXNG-backed authoring search/fetch.** A self-hosted SearXNG meta-search + readability content-extraction capability for the BUILD-TIME authoring pipeline (corpus + news acquisition) — more robust than WebFetch/`curl`, which hit bot-protection on gov sites this phase (DOJ 403; we routed around it via the Wayback Machine). Already prototyped in the sibling `nanaclaw` project (Phase 5: `SEARXNG_URL` service + `web_search`/`web_extract` MCP tools); not currently running here (localhost:8888 refused). Authoring-only — NEVER in the ship artifact (offline non-negotiable). A future-phase candidate, NOT this phase.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)
- (Resolved pre-Phase-42 planning entries — ~20, each multi-KB — archived VERBATIM to [articles/state-archive-resolved-blockers.md](articles/state-archive-resolved-blockers.md) by the Phase-44 T6 hygiene trim; each is also in its decision article + journal.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus; Phase 24 added `jurisdiction` per source + `load_typology_map`/`validate_typology` (build-boundary typology gate) + merges typology/jurisdiction into `__CORPUS__`; Phase 25 added `_inline_article`/`_strip_provenance` (inlines each live doc's full source-md body into `__CORPUS__`) + a re-declared `red_flag` SHAPE boundary check in `validate_corpus_data`; **Phase 47** added `load_console_cases`/`validate_console_cases` + the `console` target wired into `all`/`--check`; **Phase 49** added `TRIAGE_*` + `load/validate/render/build/check_triage` (4 tamper + 2 drift classes fail-loud; probe-history NEVER read) + the `triage` target — `all`/`--check` now 7 artifacts | 2026-06-12 |
| data/typology-map.json | Phase 24 NEW cross-corpus typology OVERLAY (22-term closed vocab + 42-entry doc-id→typology map; jurisdiction from the source registry, not stored). A SEPARATE committed file — the source dirs + derived records stay byte-frozen; validated fail-loud at the build boundary | 2026-06-07 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>`; Phase 25 added an ADDITIVE `red_flag` SHAPE check to `check_record` (grounding normalize/rf_region/flag⊂md BYTE-UNCHANGED) + 3 `--selftest` fixtures | 2026-06-07 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (2.15MB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips). Phase 24 Documents/Typologies toggle + synthesis view; Phase 25 the per-doc "Read advisory" screen + the `red_flag` label through the 6-screen arc. **Phase 27 (shippability)**: `cleanArticle()` markitdown-sanitizes the displayed source (form-feeds / running headers / tab-soup; display-only, source md untouched); `highlightArticle` rewritten to normalize-both-sides + index-map (634/634 = 100% highlight); the Signal build-log ports the Act-4 `.run` working-pulse in a proposal grid + a combination-lift lift-side panel (firestat OMITTED); the progressive read types the WHOLE article (no 1600 cap, length-scaled ~6s) | 2026-06-07 |
| news.html + dist/news/index.html | M8 adverse-media stream (third ship artifact). Arc: Select → Read → Screen (real Jaro-Winkler, 0.85) → Disposition (human gate; deterministic SVG subject network + anchor dossier in LIVE mode) → Exposure. OFFLINE ship = static book only; ALL live client code in `/*LIVE_START*/…/*LIVE_END*/`, build-stripped (dist/news byte-identical). Phase 43 added the stage-completion LIVE preview (grounded flags FINAL + provisional chips + token counter — never a token stream); Phase 44 moved it onto a DEDICATED PROCESSING PAGE (in-page takeover, presenter keys guarded, Esc arm→abandon) and made grounding WRAP-TOLERANT (locate_span requote) + folds ambiguity-refusing/type-matched. Harness `tests/news-stream.test.mjs` (150, both motion modes + live overrides + strip assertion). | 2026-06-10 |
| scripts/{serve_news,news_ground,news_store,news_fetch}.py | The LIVE news companion (dev/authoring-time only; offline ship unaffected): serve_news = localhost page + llama-cpp proxy + NDJSON stage-stream `/extract` (Phase 43: call_llm STREAMS SSE w/ idle-gap 120s timeout, MAX_GEN_TOKENS 16384, failures NAMED in-stream, preflight_size vs /props n_ctx FAIL-OPEN, single-flight 409, disconnect-persists-nothing) + `/watchlist[/prune]` + `/anchor`; news_ground = the SHARED grounded-or-stripped gate (the one allowed build→companion import; vocab single-authority); news_store = gitignored DuckDB ANCHOR store (.venv-only; build.py never imports it); news_fetch = URL fetch ladder + standardizer + article-shape verifier. Replay: 13 US-federal fixtures (`tests/fixtures/news-live/`, FIXTURE_META allowlist), goldens deterministic from pinned captures; `tests/news_live_test.py` (system + .venv + --live). | 2026-06-10 |
| data/news/ | REAL US-federal gov-enforcement articles (DOJ/OFAC, verbatim 17 USC §105) `articles/*.md` + grounded `derived/*.json` + SYNTHETIC `book.json` (exact + near-matches + a common-name trap; non-negotiable #4). Phase 42 added the committed SYNTHETIC demo seed `docs/demo-investigation-note.md`. | 2026-06-10 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored. Phase 25 added a natural `red_flag` per indicator. **Phase 27** tightened 121 over-long verbatim `flag`s to crisp contiguous SUB-SPANS (faithfulness-guarded; 36 long-but-faithful kept whole) across 33 derived records + re-translated fin-2022-a001's 2 red_flags — byte-surgical (flag/red_flag VALUES only); mds + every corpus-status.json + the grounding core byte-frozen | 2026-06-07 |
| docs/program-blueprint.md + specs/phase-47-*.md + specs/phase-48-*.md | M9 design deliverables: the program blueprint §1–§15 (Phase 47 §1–§12: universal grounding spine, 6-workload substrate/verifier table [6th row Phase 48], G/M/J/A gate taxonomy, dual-stream human charter, probe-rule agentification, E-23×SR-11-7 control mapping, honesty disposition table, §11 capability roadmap; Phase 48 §12–§15: history-utilization [3 roles w/ substrate+verifier, "history is evidence, never ground truth"], LFCM library-not-monolith, continuous adjudication loop [params chosen-not-measured], probe citation) + the two spec contracts | 2026-06-12 |
| scripts/corpus_redundancy.py + data/corpus-redundancy/equiv-sample.json + docs/corpus-redundancy-report.md | **Phase 51 (2026-06-16)** — the measurement workstream's first MEASURED result (the §13 fm-1 frontier). NON-ship, stdlib, read-only over the frozen corpus. T1 deterministic cross-regulator co-occurrence UPPER BOUND (≤0.325, over the corpus's own typology/capability/agency labels) + T2 sampled consensus-class semantic-equivalence (24 single-rater blind judgments, integrity-verified; est genuine ~1.4% strict / ~11.5% band) + the non-ship report. `--selftest` GREEN; `--check all` 7/7 zero drift. FINDING: the corpus's no-dedup stance empirically vindicated | 2026-06-16 |
| docs/blueprint-sales-pitch.md + docs/governance-throughput-model.md | Program GTM + sustainability artifacts (2026-06-16, post 4-angle adversarial critique). Pitch v2: structural-disease→architectural-cure, honesty weaponized, the "what we do not claim yet" list (composition detection-lift now RETIRED per the triple-null). Throughput model: the G/M/J/A taxonomy AS the cost model (~2 FTE@5k / ~4–5@10k, conditional on three measurable params) — answers the per-signal-as-a-model lifecycle-burden concern with arithmetic, not assertion. NON-ship, honesty-disciplined | 2026-06-16 |
| docs/pillar-integration-contract.md | Pillar 1→Pillar 2 cross-pillar data contract (v0.1 DRAFT, 2026-06-16; references the FROZEN blueprint, not an edit). Grounded at aml-substrate HEAD 0daa3cc: the reference-by-path grounding spine (Alert→Dossier→STRRecord, replay-verified), the PROPOSED serialized record schemas + minted ids, the grounded NETWORK-STRUCTURE signal fix (composition's 2nd axis), honesty caveats (case-grouping oracle-only, derived ids, regenerate), the persist-in-Pillar-1 seam, what Pillar 2 owns (narrative + completeness flip + the verifiers), §7 open ratification questions | 2026-06-16 |
| data/probe-history/ + scripts/probe_history_stats.py + docs/probe-history.md + docs/blueprint-report.html | Phase 48 NON-ship artifacts: fully SYNTHETIC advisory-shaped legacy rulebook (12 rules) + 44-alert disposition history + derived record 12/12 gate-green through the BYTE-UNCHANGED gate (outside every build.py-read path); stdlib stats script (9 definition-carrying metrics, no selftest yet); shape-caveat writeup; 87.8KB self-contained offline DESIGN-labeled blueprint report (booth precedent — no build.py target, system-flow + grounding-chain SVG centerpieces) | 2026-06-12 |
| console.html + dist/console/index.html + data/console/cases.json + scripts/curate_console_cases.py | The 4th ship artifact (Phase 47): gate console — 213 REAL C/D adjudication cases (Phase-34 correction, byte-identity verified; 212 FINTRAC attributed, 1 US null) through Queue → Evidence → Disposition (graded + required rationale) → Record reveal (213/213 build-verified) → session-only Ledger; curate script = deterministic regeneration-only | 2026-06-12 |
| triage.html + dist/triage/index.html + data/triage/scenarios.json + scripts/curate_triage_scenarios.py | The 5th ship artifact (Phase 49, NEW — blueprint §14's continuous adjudication loop made demo-able): 20 SYNTHETIC scenarios (16 + 4 controls, 4 strata 10/3/3/4, panels shared BY REFERENCE, 6 labeled second-rater seeds, rule text embedded) deterministically curated from data/probe-history at AUTHORING time only; arc Queue → Evidence → Disposition (§14 grammar, rationale REQUIRED) → Reveal (decisions-not-correctness) → Discovery ledger (render-computed agreement w/ measurement definitions; JSON export; persists nothing); 96,686 B offline | 2026-06-12 |
| tests/corpus-explorer.test.mjs + tests/news-stream.test.mjs + tests/gate-console.test.mjs + tests/triage-console.test.mjs | Zero-dep Node DOM-shim harnesses — corpus 303, news-stream 150, gate-console 68, triage-console 93 (Phase 49, NEW). Per-phase growth history: the journal entries. |

## Session Journal (last 5)

- [2026-06-21] [[2026-06-21-phase-63-investigator-case-workbench|Phase 63 — Investigator Case Workbench (capstone sales-pitch demo) (lite, planned+re-scoped+delivered same session, READY FOR COMPLETION)]] — the capstone sales-pitch demo: a COMPANION-SERVED investigator case workbench (`workbench.html` + the NEW `scripts/serve_workbench.py`, reusing serve_chain's consume/verify/audit primitives; companion-only, NOT a 9th build/dist target — lite holds) over a REAL (synthetic) aml-substrate alert POPULATION. **POST-GATE RE-SCOPE to option B (USER OVERRIDE):** the direction gate had closed all-accept on a CURATED-4 / local-synthetic set; the user reviewed the built artifact + re-scoped to feed the real substrate population. **A0 FLIPPED → VERIFIED feasible** — aml-substrate@f90bd39's contract-v0.2 PartyView already emits the rich investigator clutter at population scale → NO precursor phase. Ran the substrate emit as TOOL-USE (file-contract; build.py imports NO sibling — the Phase-62 pattern) → a **200-case** deterministic slice vendored into `data/workbench/` (pinned @f90bd39, `meta.synthetic:true`); the 4 exemplars (mule CASE-P-0002174 / fp_trap CASE-P-0018660 / thin CASE-P-0008468 / ambiguous CASE-O-000008) tagged WITHIN the population; a synthetic display identity laid over the real KYC (substrate omits name/DOB by privacy design). **The T4 cross-pillar finding → the FAIL-CLOSED defensibility climax:** coverage is MEASURED (curate runs casework's stub over all 200 bundles, recording grounds_e2e per case) → **57/200 sign end-to-end**; composed cases (≥2 capabilities) fail on a real substrate↔casework **C3/C15 replay divergence** (casework's independent re-derivation can't reproduce the cited C3/C15; ~28% of composed cases sign). Rather than loosen the verifier, the demo EMBRACES the refusal: deciding a composed mule shows the gate refusing → escalate-to-human (the verifier was NEVER loosened, abort rule held; user accepted the framing). Precedent-confidence anchored to REAL fired-signal-combo frequency (gate funnel **129 auto-clear / 52 review / 19 human-gate**), but the DISPOSITION direction stays ILLUSTRATIVE ("chosen, not measured" — the Phase-62 grounded-detection / illustrative-disposition split). Three beats: clutter (real PartyView KYC + cross-account aggregation + txn detail + REAL counterparty edges) → signals-on clarity (grounding/corroboration; ZERO catch-rate number — the detection-lift triple-null governs) → live finale (serve_workbench → casework consume; default Claude, stub fallback, NDJSON stage reveal). **PLUS a thin PYTEST WRAPPER (USER OVERRIDE, mid-phase):** the user asked for proper Python tooling → resolved as a thin wrapper NOT full py-init (signal-watch ships HTML — no Python ships — so the src/+pytest+packaging restructure is the wrong fit): `pyproject.toml` ([tool.uv] package=false; pytest dev dep) + `tests/test_selftests.py` (17 parametrized cases shelling out to each `scripts/*.py --selftest` + `node tests/*.test.mjs`) + `uv.lock` → `uv run pytest` runs the whole suite, parity with the siblings, the dep-free paths stay the source of truth. **VERIFIED:** `uv run pytest` 17 passed; `build.py --check all` 8/8 ZERO dist drift; `tests/workbench.test.mjs` 61 assertions green; no runtime deps (pytest dev-only, gitignored .venv); no sibling import in build.py. **Ledger Phase-63: all_accept FLIPPED to false via the A0 re-scope** (verified-revised-accept); A1' txn-edges-only / A2' ~200 slice / A3' coverage-real-dispositions-illustrative HELD; the T4 finding bit productively (the live-finale checkpoint fired → fail-closed-as-defensibility + measured coverage). DEFERRED follow-ons: C3/C15 contract alignment (sibling-repo) · agentic tool-calling · the precedent-confidence GATING engine · the substrate ownership-graph emission. `workbench.html` (NEW) · `scripts/serve_workbench.py` (NEW) · `scripts/curate_workbench_cases.py` (NEW) · `data/workbench/` (NEW) · `tests/workbench.test.mjs` (NEW) · `tests/test_selftests.py` (NEW) · `pyproject.toml` + `uv.lock` (NEW) · `docs/case-workbench.md` (NEW) · `tests/smoke-checklist.md`.
- [2026-06-21] [[2026-06-21-phase-62-grounded-probe-history-consume|Phase 62 — Grounded probe-history consume (§12) + §14-frozen boundary + P22 pin re-ground (lite, planned 2026-06-20, delivered+accepted 2026-06-21)]] — consumed aml-substrate's P22 (loop-closure) grounded probe-history projector into the §12 non-ship measurement layer (`probe_history_stats.py`), REPLACING the SYNTHETIC Phase-48 fixture. PLANNED as a two-pronged END-TO-END consume (§12 measurement + a sanctioned USER-OVERRIDE unfreeze of `dist/triage`). **REDIRECT** from the loaded frame: the consume target is the §12 MEASUREMENT layer, NOT a coverage-map re-ground (a known zero-movement no-op). **A0 (verify-first the substrate committed HEAD) DID REAL WORK** — the loaded 2e5d0f0/P21 facts were stale; code-verify found P22 committed at **ae98924** (the cross-pillar re-ground rule vindicated). Ran the COMMITTED `--probe-history` projector (sanctioned tool-use) → `data/probe-history/grounded/alert-history.json`: **4,966 real label-blind firings** (C2/C3/C5/C15; C4/C6 absent), 618 entities, deterministic (sha256 `d9d1110e`), HEAD pinned. **The T4 A1 pause-checkpoint FIRED — the §14 unfreeze was STOOD DOWN.** Reading `curate_triage_scenarios.py` firsthand surfaced the **§12/§14 SOURCE BOUNDARY**: the substrate's label-blind probe-history is the RIGHT source for §12 (firing/disposition MEASUREMENT) but the WRONG source for §14 (the triage console needs adjudicable FACT PATTERNS the substrate doesn't emit; curate couples via only 5 metadata fields on 7/20 scenarios, the marquee TM-104 pair is C20 — no substrate detector). PAUSED + reported → **user accepted the boundary** → `dist/triage` stays BYTE-FROZEN, NO re-curation, NO redesign-to-force (the A1 fallback exactly). Scope shrank to **§12-measurement-only**; the ONLY dist change is the launcher pin cascade (`dist/index.html`, 1 line — Phase-60 Option-A). **Honesty split (A3 accepted at delivery):** the alert/entity/cited-evidence side GROUNDED, dispositions label-blind ILLUSTRATIVE (every disposition-derived metric tagged "[over illustrative dispositions]"; an adversarial overclaim audit found none). `silent_rules` redefined to CAPABILITY-level silence (the substrate detects per-capability, not per-rule-variant — a measured definitional adaptation, surfaced not hidden). Cross-pillar pins moved P20(34400e2)→P22(ae98924); **VERIFIED ZERO tier movement** (reachable-now 171). `--check all` 8/8; `triage-console.test.mjs` 93/93 (§14 unchanged); `probe_history_stats.py --selftest`/`--grounded` green (synthetic path byte-identical); no substrate import. Adversarial 4-lens verification (refute-boundary / honesty-overclaim / frozen-set / completeness) ALL HOLD. **Ledger Phase-62 revisit: A0 HELD (verify-first caught the stale pin), A1 checkpoint FIRED→boundary, A2 HELD (map selftested), A3 don't-know→delivery-accepted.** OPEN: a genuinely grounded §14 needs a fact-pattern-synthesizer phase; C4-structuring 0-firing is a substrate-side measurement question. `data/probe-history/grounded/**` (NEW) · `data/probe-history/capability-tm-map.json` (NEW) · `scripts/probe_history_stats.py` · `docs/probe-history.md` · `data/coverage-map/{substrate-pin,coverage}.json` · `scripts/e2e_chain_check.py` · `data/pillar-status.json` + `dist/index.html` (launcher re-ground) · `CLAUDE.md`.
- [2026-06-20] [[2026-06-20-phase-61-blueprint-implementation-review|Phase 61 — Blueprint review against what's been implemented: deep verified true-up + batched cross-pillar re-ground (lite, planned+delivered+accepted same session)]] — `docs/program-blueprint.md` was a Phase-47/48 DESIGN snapshot whose central §3 built-vs-design-stage partition had gone materially STALE by the doc's OWN honesty standard: the program split into pillars (P50) and built **aml-substrate P1–20** (@34400e2) + **aml-casework P1–12** (@c6d8401) + signal-watch's coverage map / e2e chain / C/D control / corpus-redundancy. Under ultracode the user chose **C — report + deep blueprint revision**, **batched the cross-pillar re-ground** (A1 up-scope over the recommended report-only boundary — the Phase-60 batching instinct), and **re-sync blueprint-report.html** (A3); HEADs verified LIVE via git log, not from loaded facts. **T1 the audit (96-agent fan-out)** → NEW `docs/blueprint-implementation-review.md`: per-§ status in a THREE-TIER vocab (demo-built / pillar-build-synthetic / design-stage), every built/pillar-built status NAMES a committed artifact verified at its pinned HEAD, **8 blueprint-stale + 6 impl-diverged** items, a re-grounded next-frontier ranking. **The central finding: the §3 binary was OUTGROWN** — 4 "(design-stage)" rows (TM / case-investigation / SAR-STR / LFCM-assist) now carry committed SIBLING code at pillar-build-synthetic scale (real fail-closed code, synthetic data, NOT deployed) → the blueprint was UNDER-claiming; a few items were OVER-stated (composition detection-lift RETIRED post-triple-null; §9 row-2/row-3; §11 /intel/ + REQUOTE-RETRY). **T2 the batched verify-first re-ground**: re-pinned `signal_coverage_map.py` + `e2e_chain_check.py` substrate 9c75c03 (P18) → 34400e2 (P20), casework unchanged @c6d8401; `git diff 9c75c03..34400e2` touched NO detector/view (only the composition stress-bench + a 51-line referential helper + 2 measurement probes; P19's C14 honest-null added no screening detector) → **genuinely ZERO tier movement** confirmed BEFORE re-freeze (coverage.json diff = the single pin line; `--check` byte-identical, reachable-now 171 held; `--selftest` green; e2e `--selftest` green). **T3 the verified-gated deep revision**: three-tier legend + §3 markers + §7/§8/§11/§13 prose trued-up with cross-repo refs + HEAD pins; `Status: DESIGN` + the §15 demo-charter PRESERVED. **T4 `blueprint-report.html`** revised sections re-synced + a md↔html consistency note. **T5 exit**: `--check all` 8/8; `git diff --stat HEAD -- dist/` = ONLY dist/index.html (1 line, the sanctioned launcher cascade — Phase-60 Option-A); regression green; `! grep import aml_substrate|aml_casework` clean. **Ledger Phase-61 revisit: A0 (T0 weakest) HELD — verify-first did real work both directions (the third status carried the outgrown rows honestly; it confirmed the zero movement + that no marker overclaimed); A1/A2/A3/A4 HELD by construction.** **The user accepted delivery and chose to switch to an aml-substrate session** — the next real reachable-now frontier is SIBLING-rooted (an emergence/§14-M-layer phase, undrivable from a signal-watch session; the coverage-map lever is near-ceiling here, needs-detector=0). `docs/blueprint-implementation-review.md` (NEW) · `docs/program-blueprint.md` · `docs/blueprint-report.html` · `data/coverage-map/{substrate-pin,coverage}.json` · `scripts/signal_coverage_map.py` (selftest goldens) · `scripts/e2e_chain_check.py` + `scripts/e2e/**` · `data/pillar-status.json` + `dist/index.html` (launcher re-ground).
- [2026-06-19] [[2026-06-19-phase-60-consume-landed-sibling-halves|Phase 60 — Consume the landed sibling halves: the C7 reachable-now rise + the real e2e chain (lite, planned+delivered+accepted same session)]] — a code-verified cross-pillar review found BOTH siblings 3 phases past the Phase-59 pins (aml-substrate@9c75c03 P18 emits the real C8 party-bearing v0.2 bundle; aml-casework@c6d8401 P12 = C7/C8/C14 grounding_replay assertions, C26 honest null). The user up-scoped to the COMBINED scope (coverage-map consume + the real e2e chain) over the recommended coverage-only, and chose verify-first then freeze. **RESULT: the first real reachable-now rise, 93→171 (+78)** — needs-detector exhausted 62→0, nve 70→69, nb 296→281. **The honest headline: all +78 is C7** (the one screening cap whose `behavior_emergence=="emerges"`; `is_reachable` precedes the data_source_class branch → all 78 C7 flip, 62 direct + 16 proxy). **C8/C14/C26 moved 0** — data-only/absent is the binding 3rd conjunct, NEITHER emission nor assertion work; Phase 59's "both halves must land" sharpened to "only the cap whose behavior genuinely emerges reaches reachable-now." **VERIFY-FIRST paid off:** caught a +62 plan-stage undercount → corrected to +78 before the freeze (A0 held-with-correction). **The real C8 chain CONNECTS:** casework consumed substrate's C8 v0.2 bundle CASE-P-0000251 (first real contact) → signed SAR, zero blocking violations, all 6 verifiers passed, no contract-version gap (A2 held, stronger than expected); e2e_chain_check --real CONNECTED (C4 too). **A3 BIT (the launcher cascade):** re-grounding the e2e artifact regenerates pillar-status.json (grounding_heads) → the launcher ship dist drifts; STOP-and-surfaced per the abort rule → user Option-A (the sanctioned launcher re-ground, Phase-55/57 pattern; 7/8 dists byte-identical, only dist/index.html moved). signal_coverage_map --check byte-identical + --selftest green (2 goldens re-grounded as landing anchors; C8/D1→needs-behavior proves an assertion alone doesn't move a data-only cap); no sibling import; --check all 8/8. Both sibling briefs re-grounded in their own trees (a sibling session commits them). **SOFT OBS: the coverage-map reachable-now lever is near its CEILING from signal-watch's side** (needs-detector exhausted; remaining gains sibling-rooted or honest ceilings) → the next reachable-now frontier is a sibling-rooted aml-substrate phase. Also: e2e --selftest silently clobbers the committed pillar-status.json (restore via --real) — a guard is a hygiene candidate. `data/coverage-map/{substrate-pin,coverage}.json` · `scripts/signal_coverage_map.py` (selftest goldens) · `scripts/e2e_chain_check.py` (GROUNDING_HEADS) · `data/pillar-status.json` + `dist/index.html` (launcher re-ground) · `docs/corpus-substrate-coverage.md` §3b · `docs/pillar-integration-contract.md` §8 · both sibling briefs.
- [2026-06-18] [[2026-06-18-phase-59-consume-substrate-phase-15|Phase 59 — Consume the substrate Phase 15 build into the coverage map (lite, planned+delivered+accepted same session)]] — `/dev-plan continue` landed on a failing `signal_coverage_map.py --check`: the working tree held a half-finished consume of a sibling landing. **aml-substrate Phase 15 (@5875241, DELIVERED)** shipped the SUBSTRATE HALF of Phase 58's brief — a label-blind PartyView (exposes D8 KYC + D12 pep) + 4 SCREENING_DETECTORS (C7/C14/C8/C26); the pin was re-grounded but `coverage.json` left un-frozen. **Direction A** (re-freeze + document the coupling, tiers unchanged) over re-tiering with a `blocked_on` field (C) / hold-revert (B); the user chose **"verify first, then re-confirm"** → A0 (pin↔5875241) + A1 (the 242→needs-behavior / 62-C7→needs-detector tiering is INTENDED classifier semantics) CODE-VERIFIED before the freeze, both HELD, re-confirmed. **FINDING: landing the detectors+views moved 0 signals into reachable-now** — reachability is a 3-way AND (`has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges"`); the substrate half satisfied only *exposure*. MEASURED (corpus@472b44e × substrate@5875241 × casework@4ac9523): needs-view-exposure **312→70**, needs-behavior **54→296**, reachable-now UNCHANGED **93**. The 242 D8/D12 are exposed-but-unmeasurable on the still-vendored txn-only emission (`modeled-inactive` → needs-behavior, an emission-sample limit NOT emergence work); the 62 C7 keep needs-detector pending the casework assertion → the tier NAMES went composite (documented in prose per A). `--selftest` surfaced 2 stale D8-unexposed golden values → re-grounded to the verified reality (a regression anchor, NOT a loosening — structural integrity tests untouched). T1 re-froze coverage.json (--check byte-identical) + bumped the casework pin `2381d71→4ac9523` (re-verified: still 5 assertions); T2 docs §3a + contract §8; T3 re-grounded both sibling briefs (substrate detector/view half DONE → party-bearing emission next; casework → 4 paired assertions; shared acceptance + doctrine constraint). `--check all` 8/8 zero drift; no sibling import; build.py never imports the script; ZERO ship artifacts. The 2 sibling-brief edits dirty the sibling trees (a sibling session commits them). Ledger Phase-59 revisit: A0 HELD (verified clean before freeze) · A1/A2/A3 HELD. `data/coverage-map/{substrate-pin,coverage}.json` · `scripts/signal_coverage_map.py` (selftest goldens) · `docs/corpus-substrate-coverage.md` · `docs/pillar-integration-contract.md` · `aml-substrate/docs/corpus-coverage-build-PLAN-BRIEF.md` + `aml-casework/docs/capability-assertions-PLAN-BRIEF.md` (sibling).
- [2026-06-17] [[2026-06-17-phase-56-chain-workbench-spine|Phase 56 — Chain workbench SPINE (beat 1) delivered + accepted + committed]] — the analyst case-workbench for the substrate→casework→verify chain (lite; planned prior session, implemented + accepted + committed this one). T1–T5 [x]: `data/chain-cases/CASE-P-0010361` vendored byte-identical (aml-substrate@df23bba, sha1 `bac574d…`, 5 alerts C4/C3/C2/C5/C15, 71 txns) + `validate_chain_cases.py` (reuses `check_substrate` + manifest↔bundle integrity); the casework consume-CLI brief (sibling, untracked — **surfaced the wrinkle**: `FixedDraftStub` is test-only + fixed-content → the CLI needs a src-resident bundle-derived stub, open risk moved to brief-A0); `serve_chain.py` (stdlib; `/cases` + `/run` NDJSON stages; casework=subprocess of `python -m aml_casework.ingest`, e2e verify=subprocess `--real` with **pillar-status.json snapshot/restore** so a run never drifts the launcher; `--selftest` STUBBED→CONNECTED offline; no sibling import); `chain.html` (dossier 4-node chain ledger + flag→corpus audit walk; line-buffered NDJSON = stage rendering; `tests/chain.test.mjs` 31/31); `docs/chain-workbench.md` + smoke row. `serve_chain` reads the signed-SAR FILE not the CLI stdout (de-risks the CLI's output contract); config injected via an HTML-comment marker (raw chain.html stays valid JS for the test). Invariants: `--check all` 8/8 · chain.html not a build target/not in dist/ · nothing persisted · key server-side only · no CLAUDE.md edit (non-ship). **TWO-BEAT: beat 1 (spine) done + committed; beat 2 (live neural run → `e2e_chain_check --real` CONNECTED) GATED on the casework consume CLI.** Ledger Phase-56 revisit: A0/A2/A1'/A5 held at the spine; A4 (T0 weakest) pending beat 2.
- [2026-06-17] [[2026-06-17-phase-55-full-chain-connected|Phase 55 — full 3-pillar chain CONNECTED (beat 2, adversarially verified)]] — the sibling sessions landed: bridge #1 (aml-substrate@df23bba — `--emit-evidence` + `monitor/evidence.py` + `CASE-P-0010361`; **A0 did NOT bite**, real emission matched byte-for-byte) + bridge #2 (aml-casework@85602c1 — registered C2/C3/C4/C5/C15 + `ingest.py` emitted the signed SAR). `e2e_chain_check --real` → **CONNECTED**; pillar-status all-green; --check all 8/8. **ADVERSARIALLY VERIFIED** (4-lens workflow — join re-derivation [flags byte-grounded in SOURCE md], casework integrity [6 verifiers genuinely run, `build_signed_sar` == committed byte-for-byte, non-no-op by perturbation], narrative faithful — all CONFIRM). Honesty notes (casework-side, by design): committed SAR narrative = `FixedDraftStub` draft (neural ClaudeDrafter `@integration`) · vendored 5/17 corpus subset (`in_sync`) · C3 count-based replay (bundle `counterparty_ref=null`). INCIDENT: a verifier agent overstepped "do not modify" + committed e5a3138 (incomplete flip) → undone (`reset --soft`) + redone clean in 10867ff with corrected grounding HEADs (df23bba/85602c1). Harness gained `check_substrate` + the substrate-side `--real` mode. Commits: bcc69f5 · 10867ff. **Phase 55 delivery gate: BOTH beats met — the 3-pillar full demo connects end to end.**
- [2026-06-17] [[2026-06-17-phase-55-cross-pillar-e2e-bridge|Phase 55 — Cross-pillar end-to-end bridge: the spine (aggregation + verification)]] — connect the 3-pillar full demo; wiring routes to SIBLING sessions, aggregation + final delivery sit here. SPINE (T1–T6) DELIVERED + committed (b37d7a0 + e95df82): T1 ratified the §2/§5a serialization contract (bundle-per-case json + deterministic sha1 id-mint, grounded in casework's `validate_bundle`); T2 `docs/e2e-acceptance.md`; T3 `scripts/e2e_chain_check.py` (NON-ship, stdlib, NO sibling import — `--selftest` fixture-proven, `--real` gated, emits `pillar-status.json`); T4 the launcher `dist/index.html` (8th build target, `--check all` 8/8, 5 existing dists byte-identical) + `tests/launcher.test.mjs` (23); T5 two sibling PLAN-BRIEFs; T6 `docs/e2e-walkthrough.md`. Review gate confirmed the id-mint rule BYTE-IDENTICAL across contract/harness/briefs + found 4 fixes (HIGH `--real` refuses synthetic fixtures; MED signoff required; 2 LOW) → e95df82. TWO-BEAT delivery: spine now / the `--real` chain green is the delivery gate, GATED on bridges #1 (substrate persist) + #2 (casework consume). Removed the obsolete M1 stale-`dist/index.html` deletion in build.py (DISCOVERY). Phase stays ACTIVE. Gate all_accept:false (A0 §5a-ratifiable-now · A1 no-import · A2 C4-slice DEFENDED-by-code · A3 reject→launcher).
- [2026-06-16] [[2026-06-16-phase-54-cd-tag-control|Phase 54 — C/D tag control: the measured-not-gated control made real (lite, planned+delivered+accepted same session)]] — the measure-first workstream PIVOTED from a 4th C/D number to the **control** the three measurements (51/52/53) were FOR; closes the named-OPEN Phase-47 T2/T3 control-story blocker. Direction = the user's REFRAME ("prototype the tag control") + a rigor ESCALATION ("execute the independent effectiveness challenge ONCE this phase"). NON-ship, --check all 7/7 zero drift, ZERO ship artifacts in the diff. **The independent challenge EXECUTED** (the Phase-52/53 deferred follow-up): a NEW dev-time companion `cd_rate_independent.py` called a LOCAL, genuinely cross-family **Qwen3.6-35B** rater, **context-matched** (source-doc region + the 28+20 posture + closed vocab — not the Phase-53 flag-only frame), over the same committed n=96 → **C 0.604** (κ 0.583) / **D 0.646** (κ 0.599); cross-rater C 0.677 / D 0.646; **Krippendorff's α** C 0.634 / D 0.618. FINDING: context-matched cross-family agreement is statistically indistinguishable from the same-family self-consistency (0.677; Wilson CIs overlap) → reliability is GENUINE (survives a family change, chance-corrected, ~0.6/axis, never validated-correct), and context-matching resolves the Phase-53 confound (an INFERENCE from the aggregate, not a Qwen forced-pairwise re-run — bounds direction not exact share). **A0 (the T0 weakest) HELD with a positive surprise** — the T1 probe was user-adjudicated COMPETENT (8/8 in-vocab, hit the named C8/C14 neighbour, flagged a plausible committed mis-tag), the named Opus fallback not needed; A1–A3 held. The CONTROL made real: `docs/cd-tag-control.md` (SR-11-7 Pillar-2 + OSFI E-23 grounded) + `cd_correctness.py --control-check` (PASS 7/7 frozen) / `--control-freeze` — BREACH demonstrated on injected drift (selftest + live perturb-and-restore). The model dependency isolated in the dev-time companion (build.py + cd_correctness.py import NEITHER — replay core stays pure stdlib). Deferred-with-owner: a HUMAN rater; the program-wide control PATTERN; a Qwen forced-pairwise decomposition. `scripts/cd_rate_independent.py` (NEW) · `scripts/cd_correctness.py` (extended) · `data/cd-correctness/{independent-sample,cd-control-baseline}.json` (NEW) · `docs/cd-tag-control.md` (NEW) · `docs/cd-correctness-report.md`.
- [2026-06-16] [[2026-06-16-phase-53-cd-disagreement-decomposition|Phase 53 — C/D disagreement decomposition: the named follow-up, delivered (lite, planned+delivered+accepted same session)]] — the measure-first workstream's THIRD number, FINISHING Phase 52: it ran the exact decomposition the Phase-52 report named as missing + added Cohen's κ. NON-ship, --check all 7/7 zero drift, both blind raters tool_uses:0. Enlarged random (n=96): self-consistency C/D **0.677**, **κ≈0.65≈raw** → genuine, not chance (reproduces n=24 0.625). Decomposition of the 62 mismatches: both-defensible **1/62 (1.6%)**, neither **0/62** → the Phase-52 "scatter" guess **REFUTED** (disagreements SHARP). Direction **confounded** by context asymmetry (flag-only re-rate vs full-doc committed): committed-better **37%** = clean LOWER bound, blind-better **61%** = UPPER bound (UNMEASURED error+confound mix, NOT a committed-error rate — the "share unmeasured" caution one level down). **A0 surprise:** the predicted both-defensible-inflation did NOT fire; the real confound is context-asymmetry (ledger A0 revisit = bit; A1–A3 held). Follow-up reframed → a CONTEXT-MATCHED independent rater. Inline artifact writing (anti-theatre on the hand-maintained docs); first blind rater dropped 1/96 (dup flag) → fresh single-item rater filled it. `scripts/cd_correctness.py` · `data/cd-correctness/{random-sample-large,decomposition-sample}.json` · `docs/cd-correctness-report.md`.
- [2026-06-16] [[2026-06-16-phase-52-cd-correctness-measurement|Phase 52 — C/D-tag reliability measurement: the unguarded dimension, measured (lite, planned+delivered+accepted same session)]] — the measure-first workstream's SECOND number, the sibling of Phase 51. Measured the ONE corpus dimension the grounding gate never checks (per-indicator C/D) via a STRATIFIED blind-rater design (NON-ship, ship corpus byte-frozen, --check all 7/7): RANDOM stratum (blind same-model free re-rate vs committed) C 0.625 / D 0.625 / both 0.417 — self-consistency, NOT validated correctness; DIVERGENCE stratum (blind pairwise over the 213 Phase-34 corrections) uphold-correction 17/24 (0.708) · original 6 · both 1 · neither 0. FINDING: the unguarded C/D dimension is *soft but adjudicable on the hard cases* — the random (free) and divergence (forced-pairwise) numbers are different samples/tasks, **NOT subtractable**; defensible adjacent-code scatter is plausibly one driver of the ~37.5% random mismatch but its share is unmeasured; the corrections lean 17-vs-6 on the hard subset (n=24, the two rates statistically indistinguishable). Both blind raters tool_uses:0 (A1 held); all assumptions HELD; independence (different model/human) + larger N + the heading→C residual deferred-with-owner. T1–T4; `scripts/cd_correctness.py` (reuses corpus_redundancy.load_indicators) · `data/cd-correctness/*` · `docs/cd-correctness-report.md`.
- [2026-06-16] [[2026-06-16-session-debrief-substream-review-measure-first|Session debrief — adversarial critique → two-workflow substream review → composition retired → Phase 51 measure-first delivered]] — full-session capture. The 4-angle blueprint/pitch critique → pitch v2 + governance-throughput model; the **two-workflow (11-agent) code-verified substream review** (WF1: aml-substrate P13 TRIPLE-NULL + aml-casework P2 + the audit-walk gap + initiative; WF2: steelmen + completeness critic dissolving the composition fork); user gates **composition detection-lift RETIRED + measure-first**; re-grounded the refuted artifacts + the re-ground-before-commit rule; **Phase 51 corpus redundancy measurement DELIVERED** (co-occurrence ≤0.325, genuine equiv ~1.4%, the corpus's no-dedup stance vindicated). Casework audit-walk-to-source picked up in parallel. 8 commits; ship artifacts + blueprint untouched.
- [2026-06-16] [[2026-06-16-substreams-review-composition-retired|Two-workflow review of both pillars through the new lenses — composition detection-lift RETIRED, measure-first direction]] — ultracode review (11 agents). aml-substrate reached **Phase 13 = a measured TRIPLE-NULL** (composition never required to detect laundering, all 3 axes; root cause: one ring ≈ one mule → redundant typology co-occurrence; the §6 levers nulled + counterproductive). The completeness critic dissolved the retire-vs-bet fork: the triple-null is a SHIPPED honesty asset, P14 is a tautology trap, composition's real value = evidence-assembly + corpus-redundancy + volume-inversion. **User gates: retire detection-lift (settle via one cheap structure-detector probe; defer P14); measure-first.** Re-grounded contract §3 + pitch + record this session; casework at Phase 2 (73 tests). Handoffs: casework audit-walk-to-source · corpus redundancy + one measured number · the probe.
- [2026-06-16] [[2026-06-16-pillar2-parallel-direction-integration-contract|Pillar 2 greenlit in parallel — full-chain case-investigation/SAR pillar + the integration contract]] — same-session continuation of the pillar-1 review. User reframed the program SERIAL→PARALLEL: "what comes after aml-substrate, can we not start it in parallel using a few synthetic investigations?" Pillar 2 = the case-investigation → composition → SAR/STR-narrative workload (§3 rows 4–6; the agentic capstone). First increment = the FULL chain, depth-first (one case → signed SAR) then stratified, REAL verifiers. Schema verification (HEAD 0daa3cc): grounding chain REAL + replay-verified, STRRecord.narrative=None a pre-built seam (validating); but composition has ONE grounded axis only (transaction-flow) → fix = a grounded NETWORK-STRUCTURE signal (dual-purpose: composition ground + 2nd detection axis for P12). 3 concurrent tracks, one coupling: `docs/pillar-integration-contract.md` (v0.1 DRAFT). signal-watch ship artifacts + blueprint untouched.
- [2026-06-16] [[2026-06-16-program-review-pillar1-p12-direction|Program review of pillar-1 (aml-substrate P7–P11) + P12 direction set]] — 2nd cross-pillar review (no signal-watch code change). Found the record **5 phases stale** (said "Phase 7 planning"; reality = **P7–P11 delivered**, 360 tests, HEAD 0daa3cc). P7 closed the P6 observable-network finding (counterparty edges + mirrored leg + leak fix + freeze guard); P9 baseline-locked. Dominant open finding: laundering is **single-feature-separable 3/3** (total_amount ROC ≈ 0.95; P10/P11 both failed to make it multivariate) → blocks the §13 LFCM composition north star. User set **P12 = the §6 realism lever** (make detection require ≥2 composed signals; measure-first sub-fork legit-overlap-cohort vs Hawkes/dormancy) over open-Pillar-2 / thin-chain-slice / hardening-only. Brief: `aml-substrate/docs/phase-12-multivariate-subtlety-PLAN-BRIEF.md`. Stale P7-planning facts re-synced.
- [2026-06-15] [[2026-06-15-program-review-pillar1-p7-direction|Program review of pillar-1 (aml-substrate P1–P6) + P7 direction set]] — cross-pillar review from the program-architecture home (no signal-watch code change): an 18-agent review workflow + deterministic verification confirmed aml-substrate **P1–P6 delivered** (239 tests, gen/ freeze byte-stable) and surfaced 3 code-verified findings (a recoverable `counterparty_ref` case-id leak; no positive freeze guard — ruff-format threatens frozen gen/; DESIGN §6 temporal spec outruns the code). User set **P7 = Observable network linkage** (the keystone gen/ unfreeze closing the P6 ground-truth-only-network finding) over loop-closure/hardening, and handed planning to an aml-substrate-rooted session (brief: `aml-substrate/docs/phase-7-observable-network-PLAN-BRIEF.md`). Stale signal-watch Phase-3 facts synced to P1–P6-delivered / P7-planning.
- [2026-06-13] [[2026-06-13-phase-50-aml-program-build-spinout|Phase 50 AML program build kickoff: the demo became the program — data-substrate pillar spun out to /Users/jwang/aml-substrate]] — a major user REFRAME: build the blueprint's §3 design-stage workloads as REAL systems, one real-system-class repo per pillar; signal-watch = architecture home. Interview + 3 parallel research agents grounded the design (AMLworld emergent method, ISO 20022 / Interac / Payments Canada / FINTRAC schemas, log-normal+Pareto / Hawkes distributions). Gate all_accept: false (A1 foundation-first / A2 build-on-research [override] / A3 small-first / A4 hybrid→deferred). DELIVERED in aml-substrate: **Phase 1 (Foundation)** 54 tests — data model + population/KYC graph + 6-channel background engine + EMT detail → parquet + realism; ~161s/1M. **Phase 2 (Emergence)** 83 tests — criminal/mule/shell designation + fresh-Python laundering engine + transitive labeling + coverage checklist + the A1 permutation-null separability gate (fires on injected artifacts) + realism; 1:21,657 calibration; structuring breaks Benford. PROCESS: user corrected mislabeled "assumptions" (direction choices) → re-ran a proper falsifiable-assumption gate; the A1 down-scope-to-measured caught its own naive threshold. signal-watch tidied to Phase-50, 5 ship artifacts FROZEN.
- [2026-06-12] [[2026-06-12-phase-49-triage-loop-embryo|Phase 49 Triage-loop embryo made demo-able: §14's continuous adjudication loop as the 5th ship artifact — triage console (STANDARD ceremony)]] (5 tasks, ALL [x] same-session 2026-06-12 — planned AND implemented today, work UNCOMMITTED → READY FOR COMPLETION, delivery gate pending) — T1 data/triage/scenarios.json (20 = 16 + 4 controls, strata 10/3/3/4, TM-104 pair shares P-HF-104 BY REFERENCE, 6 labeled second-rater seeds, regen-twice byte-identical; curate --selftest rejects 10 broken fixtures). T2 build boundary + `triage` target (4 tamper + 2 drift classes fail-loud; probe-history comments-only grep; 6 existing targets zero drift). T3 triage.html arc RED-first (57 core; USER OVERRIDE: dispatched subagent killed → inline). T4 harness 93/93 (hand-computed agreement fixture incl. the escalated-but-fired exclusion; FINAL dist freeze 96,686 B). T5 docs + FULL REGATE (--check all 7/7 zero drift; claim-shaped honesty greps; derive_signals.py + program-blueprint.md byte-untouched). Review gate 9/10 ACCEPT zero HIGH ("16 + 4" phrasing fixed inline; CLAUDE.md ~330 trim debt carried, 2 phases old). A1/A2/A4 held; A3 held-structural (believability adjudicated at the delivery gate).
- [2026-06-12] [[2026-06-12-phase-48-brownfield-history-lfcm|Phase 48 Brownfield history + LFCM: blueprint extension, triage-elicitation loop, synthetic-history probe + HTML blueprint report (STANDARD ceremony)]] (8 tasks, ALL [x] same-session 2026-06-12 — planned AND implemented today, work UNCOMMITTED → READY FOR COMPLETION, delivery gate pending) — T1 history-utilization §12 + §8 re-disposition + the doctrine line "history is evidence, never ground truth" (T1 success-field corrected pre-RED — DISCOVERY class, annotated in tasks.md). T2 LFCM §13 (library-not-monolith) + the 6th §3 workload row + count fixes. T3 continuous adjudication loop §14 (A1 condition written in: history-sourced scenarios, decisions-not-correctness, process-inconsistency + policy-gap discovery; params "chosen, not measured"). T4 synthetic probe material (12-rule advisory-shaped rulebook + 44-alert disposition history, data/probe-history/). T5a derivation through the UNCHANGED gate — 12/12 gate-green FIRST shot, zero check_record violations, ph33_apply-pattern deterministic downstream (BUILD_NOW 1 · COVERED 8 · ENHANCE 3). T5b scripts/probe_history_stats.py (9 definition-carrying metrics) + docs/probe-history.md (shape caveat). T6 docs/blueprint-report.html (87.8KB self-contained offline NON-ship, dossier theme, system-flow + grounding-chain SVG centerpieces; USER ADDITION at plan close). T7 integration + CLAUDE.md M9 line + HANDOFF §8 + FULL REGATE green (--check all 6/6 zero drift, derive_signals.py byte-unchanged). Review gate 9/10 ACCEPT (stale "five implementations" MEDIUM fixed inline, drift re-checked zero). A1–A4 ALL held.
- [2026-06-11] [[2026-06-11-news-opencode-lift-eval|News-side opencode lift eval (post-phase segment, after the Phase-46 close)]] (user-directed; ZERO repo-file changes — artifacts gitignored in .dev-wiki/tmp/ph46/ + the external aml-wiki inbox) — the staged A/B ran: opencode 1.17.3 agent loop vs the direct single-shot baseline on ofac-sinaloa-fentanyl (hardest committed fixture, 11.5K chars / 25 entities), committed news prompt/schema BOTH sides, Qwen3.6-35B, ph46_news_gate.py (build_record+ground_record untouched) as backpressure. The iterate-on-gate-failure loop ENGAGED and delivered real lift exactly where the corpus probe's corollary predicted: red flags 12/12 grounded vs 4/5 (3×, all 5 iteration drops recovered by requoting); relationships 22/23 vs 4/25 (5.5× — 18 dropped evidences recovered by exact-byte requoting, 2 ungroundable honestly pruned); entities 23/25 vs 24/25; BUT aliases 0 vs 22 (SILENT REGRESSION — the agent optimized what the gate report surfaced, neglected what it didn't) and NO self-termination (quality plateaued ~20 min, externally KILLED ~25 min; 12 gate runs / 27 tool calls / 0 tool-call failures) vs 129.5s single shot. VERDICT recorded as a FINDING, not an adoption: the corollary HELD (the loop earns its keep only where the baseline measurably leaves recoveries — news yes, corpus no); production use would mandate a max-iteration cap; the FOLD-IT-IN candidate = a deterministic news REQUOTE-RETRY pass (joins the DEFERRED list). Wiki capture: agent-runtime-adoption-probe → aml-wiki inbox (reviewer 9/10 accept; gains a one-line news-confirmation at absorb).
- [2026-06-11] [[2026-06-11-phase-46-corpus-live-derivation|Phase 46 Corpus live derivation mode: local-model agentic derivation through the frozen gate (M7×M8 convergence)]] (lite, 4 tasks, ALL [x] same-session overnight 2026-06-10→11 → READY FOR COMPLETION — delivery gate pending; presentation-safe: dist/corpus BYTE-IDENTICAL throughout) — T1 HARNESS PROBE (the headline): on the same held-out doc (FINTRAC-2024-OA001, LOCAL-ONLY) both harnesses extracted the IDENTICAL 17 indicators, both gate-green FIRST shot; direct 82.6s / 1 streamed strict-schema call vs opencode 1.17.3 255.1s (3.1×) / 7 tool calls; the iterate-on-gate-failure differentiator NEVER ENGAGED (Phase-44 pattern: failures gate-class, not model-class) → USER CHECKPOINT: direct + ONE violation-guided retry, opencode declined (subtraction test); cross-harness C/D agreement C 12/17 / D 15/17 (inside the Phase-34 band — tags stay the unguarded neural dimension); the held-out doc parses CLEAN under EXISTING rf_region anchors. T2 scripts/serve_corpus.py (NEW stdlib companion, port 8010: deterministic spec from the taxonomy + 3 FINTRAC few-shots, streaming strict-schema call_llm w/ named failures, the imported FROZEN check_record disposes, ONE violation-guided retry then grounded-or-dropped, staged NDJSON /derive single-flight 409 + disconnect-persists-nothing; live E2E 17 kept / 0 dropped, independent re-gate 0 violations; 2 bugs found+fixed live: _inline_article takes a PATH → _strip_provenance; arc field article_text). T3 corpus.html /*LIVE_*/ region (session-only LIVE_DOCS "UNREVIEWED" Select group + paste form, livePick through the EXISTING 6-screen arc, Phase-44-pattern processing takeover, token COUNTS never content; URL mode consciously omitted — /intel/ is PDF-shaped) + render_corpus strip → dist/corpus BYTE-IDENTICAL (--check all 5/5); harness 273→303. T4 docs/corpus-live.md + smoke notes + CLAUDE.md in place; FULL REGATE green across 9 suites. REVIEW GATE 9/10 ACCEPT zero HIGH (4 MEDIUMs: stray log deleted, stale probe path fixed, staleness → this debrief, CLAUDE.md 263 carried). Assumption revisit: A1–A4 ALL held (A3 without the swap clause firing). News-side opencode lift eval = SEPARATE, staged ready-to-run in .dev-wiki/tmp/ph46/.
(Earlier entries — the booth draw-in loop one-off ([[2026-06-10-booth-draw-in-loop|journal]] — USER OVERRIDE oneshot, no open phase: 32.8s three-act booth.html loop animation; FINTRAC indicators PARAPHRASED + attributed per the non-commercial-licence compliance call; NOT a ship artifact — outside build.py/tests, promote to a phase if reused); Phase 45 corpus presentation polish ([[2026-06-10-phase-45-corpus-presentation-polish|journal]] — the fake 18→64→83 LIFT template + "pending calibration" tag DELETED → honest R2 inventory counts client-side from __CORPUS__; FINTRAC attribution extended multi-doc to the quoting lens drills; load-time DISPLAY-ONLY fixEncoding [records/md byte-frozen]; landing hook reshaped onto "effective and regulatorily defensible"; corpus 239→273; DELIVERED + accepted + committed 324734e/7e9fa23 + pushed; presented to bank stakeholders 2026-06-11; A1–A5 held); Phase 44 live extraction quality ([[2026-06-10-phase-44-live-extraction-quality|journal]] — T1 classification verdict: missed wire flags = GATE-DROP not model recall → wrap-tolerant locate_span REQUOTE; alias misparent → ambiguity-refusing/type-matched folds; the committed quality harness + baseline [17 fixtures, --check/--freeze] = THE gate for prompt/gate/model changes; in-page processing takeover; hygiene archival convention [tasks.md active-phase-only]; news-stream 140→150; reviewer 9/10 ACCEPT); Phase 43 live pipeline robustness ([[2026-06-10-phase-43-live-pipeline-robustness|journal]] — the user's ~200s opaque live failure REPRODUCED + classified [OUTPUT-BUDGET primary w/ discarded finish_reason, read-timeout secondary; intrinsic degradation REFUTED] and fixed: streaming call_llm w/ idle-gap 120s + budget 16384 + named in-stream failures, stage-completion LIVE rendering, preflight n_ctx refusal, single-flight 409, disconnect-persists-nothing; 13/13 replay zero re-capture; news-stream 130→140; reviewer 9/10 ACCEPT, 1 HIGH fixed inline; RETRO at 30 phases: hygiene debt + user-reframe pattern high-signal); Phase 42 anchor dossier + network view ([[2026-06-10-phase-42-anchor-dossier-network-view|journal]] — pure consumption of the Phase-41 ER model: GET /anchor route, deterministic SVG network at Disposition, anchor dossier panel w/ both-kept conflicts, committed synthetic demo note; news-stream 103→130; reviewer 9/10 ACCEPT); Phase 41 entity-resolution schema enrichment ([[2026-06-10-phase-41-entity-resolution-schema|journal]] — aliases/properties/relationships enrichment through the shared gate; store ANCHOR redesign; class-aware alias screening; the prompt-regression gate FIRED → red_flags-FIRST schema-order fix; reviewer 9/10 ACCEPT); Phase 40 live red-flag extraction quality ([[2026-06-09-phase-40-live-redflag-extraction-quality|journal]] — the Phase-38 entity playbook applied to FLAGS: blind second-rater measurement + USER CHECKPOINT [residue = RECALL on commercial longreads], SYSTEM_PROMPT mechanism-checklist prompt-shaping w/ HOLDOUT eval [covR 0.40→0.55, positional decay eliminated; r3 rolled back on regression], shared-gate flag_dup_key dup-collapse, 3 new .ph40 federal fixtures); Phase 39 live news QOL ([[2026-06-09-phase-39-live-news-qol|journal]] — always-NDJSON `/extract` stage stream + companion-only news_fetch.py URL ladder/standardizer/verifier; A1 BIT: the accept-with-condition verifier proved load-bearing on the justice.gov Akamai interstitial); Phase 38 consolidate-live-news ([[2026-06-08-phase-38-consolidate-live-news|journal]] — replay-fixture capture, the denylist→subjects-only-prompt PIVOT, keep-biased second-pass verify on-by-default, watchlist view+prune; Qwen ≈ Opus once well-prompted); Phase 37 per-indicator typology ([[2026-06-08-phase-37-per-indicator-typology|journal]] — the Typologies lens regrouped by INDICATOR typology via the sparse data/indicator-typology-map.json overlay; the T1 measure-first probe dissolved the planned neural pass — Model C converged to A, 350 deterministic section-heading assignments; corpus 235→239); Phase 36 persistence + feedback watchlist ([[2026-06-08-phase-36-persistence-watchlist|journal]] — DuckDB store scripts/news_store.py + the escalated-only watchlist loop, companion/.venv-only, dist/news byte-identical); Phase 35 news live local-model backend ([[2026-06-08-phase-35-news-live-backend|journal]] — the served-by-companion live backbone: serve_news.py + the shared news_ground.py grounding gate + the stripped /*LIVE_*/ region, dist/news byte-identical; the CLAUDE.md 689→208 maintenance contract); Phase 34 C/D-assignment verification ([[2026-06-08-phase-34-cd-assignment-verification|journal]], DELIVERED + accepted + committed 83a79c3 — blind re-assignment over 589 unique texts, agreement C 74.4%/D 77.9%, 243 human dispositions, 213 byte-surgical corrections, consistency 30.5%→2.0%); Phase 33 corpus completeness + re-segmentation ([[2026-06-08-phase-33-corpus-completeness-resegmentation|journal]], committed 823c0c2 — 5th source data/fintrac-guidance, corpus 875→2,251 indicators / 42→56 records); Phase 32 news real-source elevation ([[2026-06-08-phase-32-news-real-source-elevation|journal]] — 4 real DOJ/OFAC articles, news.html rewritten to the corpus bar); Phase 30 data-source lens ([[2026-06-08-phase-30-data-source-lens|journal]] — the D-axis fourth Select mode, harness 190→217); Phase 29 capability-lens ([[2026-06-07-phase-29-capability-lens|journal]] — the C-axis third Select mode, taxonomy promoted to committed data/capability-taxonomy.json); Phase 28 corpus-completeness ([[2026-06-07-phase-28-corpus-completeness|journal]], committed 24e4a08 — re-extraction 634→875 indicators, the 28+20 coverage interview, streaming read, attribution footer).)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md · docs/program-blueprint.md (M9 design source-of-truth, §1–§15)
- docs/blueprint-report.html (Phase 48 — self-contained offline NON-ship blueprint report, DESIGN-labeled) ·
  docs/probe-history.md (Phase 48 — synthetic-history probe writeup + shape caveat)
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN, SR 11-7 + OSFI E-23 (the E-23 article NEW at Phase 47, primary-source).
  Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
