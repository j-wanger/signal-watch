# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-29 by /dev-debrief (Phase 86 DELIVERED + accepted) — **Phase 86 is COMPLETE** (DELIVERED + accepted 2026-06-29, impl `c8f32b8` + pushed to main, all 5 tasks [x]) — the agentification roadmap's **Stage 3**, the STR drafter behind the verifiers, the consistency-measured drafting agent (companion-only, STANDARD). The state-loader found the drafter + its six Class-G verifiers ALREADY SHIPPED in Phase 57 + the drafter has NO correctness oracle → the deliverable was the MEASUREMENT FRAME (the GATHER consistency-not-correctness class). The measured headline = the **gate-bounded 4/4 tie**: the live agent matched the deterministic stub 4/4 over the designed bundles (signed the same 3, fail-closed on the same narrative-seam case, 0 caught, 0 recovered) — the GATE, not the drafter, determines defensibility; vindicates propose→gate→decide from the DRAFTING side. Companion-only: all 9 dists byte-frozen, `vendor/aml-casework` + `evidence_requirements.py` git-unchanged, the 256/376 §12 funnel untouched. Next: `/dev-plan` for Phase 87 (leading candidate = agentification Stage 4, the §14 triage second-rater). Per-phase history → git log + `.dev-wiki/` journal + index.md (NOT appended here — the maintenance contract).

## Recommended Next Action

**Phase 86 DELIVERED + accepted 2026-06-29 (impl `c8f32b8`, pushed to main; all 5 tasks [x]). Run `/dev-plan` for Phase 87.** Phase 86 built the agentification roadmap's **Stage 3** — the STR drafter behind the verifiers, the consistency-measured drafting agent. The measured headline = the **gate-bounded 4/4 tie**: over 4 designed casefile bundles the live agent drafter matched the deterministic stub 4/4 (signed the same 3, fail-closed on the same narrative-seam case `CASE-P-0025128`, 0 fabrications caught, 0 recoveries) — the drafter measure is consistency-BOUNDED by the gate (the six verifiers refuse anything ungrounded → a competent agent and the stub converge at the gate → the GATE, not the drafter, determines defensibility). A tie is a real result; it vindicates propose→gate→decide from the DRAFTING side, mirroring Stage 2. Companion-only: all 9 dists byte-frozen, `vendor/aml-casework` + `evidence_requirements.py` git-unchanged, the 256/376 §12 funnel untouched; `workbench.test.mjs` 205→206, `uv run pytest` 32→34.

**Leading Phase-87 candidate: agentification Stage 4 — the §14 triage second-rater** (the roadmap's named next leg, the highest leverage): the §14 second-rater field is already plumbed end-to-end (curate→build→html→test) + an agreement metric; swap an agent into the slot (a live model constrained to the §14 disposition vocabulary), NO new gate code. Also viable: a deliberately-ungrounded adversarial drafter case (to fire the fabrication guard live, `caught>0` — the follow-on Phase 86 deliberately did NOT contrive, honesty over drama); the drafter-fixture `disposition` enrichment; or — after Stage 4 — a unified agent-evaluation SYNTHESIS report (all loop measures in one examinable artifact). The signal-watch-LOCAL consume frontier stays substrate-gated (Ask #3 = the 2nd corroborating leg, a measured-null at substrate HEAD `3716f77`/Phase 41) — the buildable frontier is the agentification track.

Phase-86 decisions: [[decisions/phase-86-gate-bounded-drafter-tie]] (high, debrief — the headline finding) · [[decisions/phase-86-direction-str-drafter-measurement]] · [[decisions/phase-86-no-oracle-consistency-measure]] · [[decisions/phase-86-stub-vs-live-narrative-seam-contrast]] · [[decisions/phase-86-companion-only-casework-unchanged]] (medium, plan); journal [[2026-06-29-phase-86-str-drafter-consistency-measure]]; plan [[phases/phase-86-str-drafter-consistency-measure]]; ledger Phase-86 (all 5 rows HELD).

## Active Phase

**[[phases/phase-86-str-drafter-consistency-measure|Phase 86 — STR drafter behind the verifiers: the consistency-measured drafting agent (Agentification Stage 3)]]** (status: ACTIVE — PLANNED 2026-06-29)

Entry criteria: MET — Phase 85 DELIVERED + accepted 2026-06-29 (impl `2008692`; all 6 tasks [x]; all 9 dists byte-frozen, `evidence_requirements.py` untouched). The agentification roadmap names Stage 3 (the STR drafter) as the next leg after Stage 2. STATE-LOADER FINDING (code-verified this session, surfaced pre-gate): the STR drafter + its verifier gate ALREADY SHIPPED in Phase 57 — the Drafter Protocol (casework's pluggable `Drafter`), the `--drafter {stub,claude,openai,opencode}` switch, the backend mapping `serve_chain.py:188`, the live-draft staged reveal in chain.html/workbench.html. A live model is up on `127.0.0.1:8080`. `serve_chain.casework_consume(bundle, out, drafter, disposition)` returns {signed, blocking_violations, narrative_present, completeness, drafter_effective} (`serve_chain.py:262-308`). The drafter has NO correctness oracle (free-text drafting).
Exit criteria: `python3 tests/drafter_quality_harness.py --check` PASS + in the uv-run-pytest umbrella; the live (or stub-only, honestly) counts-only headline recorded; `node tests/workbench.test.mjs` green (count grows); `python3 scripts/build.py --check all` 9/9 byte-frozen; `git diff --quiet vendor/aml-casework` + `git diff --quiet scripts/evidence_requirements.py` both empty; build.py companion-import grep clean (no `drafter_quality_harness`); the 256/376 §12 funnel unchanged; `docs/agentification-roadmap.md` Stage 3 marked BUILT + `docs/drafter-live.md` written; CLAUDE.md + HANDOFF trued IN PLACE; honesty swept (no catch-rate/lift/precision/recall).

Progress: 0% (PLANNED 2026-06-29, STANDARD — 5 tasks, NO L; T1 the dependency [M, `drafter_quality_harness.py` — the dep-free `score_drafts()` scorer + `--check`/`--freeze`]; T2 S [EXECUTE ONCE + the headline] depends T1; T3 S [the workbench/chain live-draft surface regression-guard] depends T1; T4 S [docs + roadmap Stage 3 BUILT] depends T2; T5 S [verify + drift guard + true-up] depends T1–T4).

Phase 86 turns the already-built deterministic stub STR drafter into a CONSISTENCY-MEASURED drafting agent — the deliverable is the MEASUREMENT FRAME, NOT new infra (the drafter + gate shipped Phase 57). `tests/drafter_quality_harness.py` mirrors `tests/gather_quality_harness.py`: a pure dep-free `score_drafts()` scorer (counts-only — stub-vs-live SIGN/REFUSE, fabrication-guard CATCH count from `blocking_violations`, grounding CONSISTENCY) + `--check` (replay a pinned live-drafter capture vs a frozen baseline; dep-free; in uv-run-pytest) + `--freeze` (run `casework_consume` stub+openai per bundle — needs the casework venv + the local model; refuse to freeze a regression). **THE DECISIVE DIFFERENCE FROM STAGES 1/2:** the STR drafter has NO correctness oracle (free-text drafting) → this is the roadmap's consistency-not-correctness class (the GATHER harness is the model), NOT oracle-scored; NO oracle firewall needed (no truth to hide — the drafter sees the bundle evidence by design; SIMPLER than Stages 1/2). The measure is NON-DEGENERATE because the deterministic STUB drafter fail-closes on the narrative-seam case (Phase-82 CASE-P-0025128, a txn-bearing C14) — the population (the committed `data/casefile/*.bundle.json`) spans SIGN and REFUSE; that case is the two-sided contrast (does the live agent sign where the stub couldn't, or get caught by the fabrication guard?). T1 measure-first CLASSIFIES the population first; an honest NULL is surfaced if degenerate (the deliberately-ungrounded fallback case). **LOAD-BEARING:** companion-only (all 9 ship dists byte-frozen; build.py imports nothing new; the vendored casework Drafter Protocol + the six grounding verifiers UNCHANGED — measure, don't modify, no re-vendor [pin `04cc335` == sibling HEAD]; `evidence_requirements.py` + the 256/376 §12 funnel UNTOUCHED — the agent drafts, the six verifiers gate, the human signs, propose→gate→decide); HONESTY (counts-only, the synthetic-substrate qualifier on every number, the word-ban no catch-rate/lift/precision/recall extends to the new markers + docs). The deliverable is the MEASUREMENT; the harness is deliberately thin. EXECUTE ONCE for the honest counts-only headline; no model on :8080 → ship the stub-only baseline + a named `--freeze` follow-on, NEVER fabricate.

[[phases/phase-85-determination-pre-proposer|Phase 85]] is **COMPLETE** (Agentification Stage 2 — the §12 determination pre-proposer, the SECOND oracle-scored agent: the live agent corrected all 727 KYC over-flags + recovered files [74 vs 50] but over-files the volume ML class [4482 vs 593] — a sensitivity trade that vindicates propose→gate→decide; the 6th companion live loop; impl `2008692`). [[phases/phase-83-merge-adjudicator-oracle-scored|Phase 83]] is **COMPLETE** (Agentification Stage 1 — the FIRST oracle-scored agent: the merge adjudicator matched the non-circular `GT-<hash>` oracle on 54 of 66 vs the spine baseline 33; the 5th companion live loop; impl db7e3ae). [[phases/phase-69-evidence-sufficiency-filing-control|Phase 69]] is **COMPLETE** (`scripts/evidence_requirements.py` — the byte-frozen sufficiency engine Phase 86 does NOT touch; the drafter is the downstream DECIDE beat). The Drafter Protocol + the `--drafter` switch (Phase 57) + `serve_chain.casework_consume` are the built machinery Phase 86 MEASURES.

## Active Phase Contract

Phase: 86 — STR drafter behind the verifiers: the consistency-measured drafting agent (Agentification Stage 3). signal-watch-local **ACTIVE — PLANNED 2026-06-29**, STANDARD, companion-only (NO L task — 5 tasks, T1 M, T2/T3/T4/T5 S). Transition: continue. Abort: if blocked >3 attempts, ask user: skip or abort. Objective: turn the already-built deterministic stub STR drafter into a CONSISTENCY-MEASURED drafting agent — a drafter quality harness (`tests/drafter_quality_harness.py`, the `gather_quality_harness.py` pattern) measuring the live (local) drafter vs the deterministic stub over the committed casefile bundles, counts-only (stub-vs-live sign/refuse + fabrication-guard catch + grounding consistency), pinned as a `--check`/`--freeze` regression gate, with an honest counts-only headline. The deliverable is the MEASUREMENT FRAME + marking roadmap Stage 3 BUILT. Direction = Stage 3 STR drafter, deliverable = the measurement frame (AskUserQuestion 2026-06-29; 3 assumptions ACCEPTED, all_accept NOT silent). **STATE-LOADER FINDING (the reframe):** the drafter + verifier gate ALREADY SHIPPED in Phase 57; the drafter has NO correctness oracle → consistency-not-correctness (the GATHER class), NOT an oracle headline. **A1 [the T0 weakest, non-degeneracy]:** the measure is two-sided because the deterministic STUB drafter fail-closes on the narrative-seam case (Phase-82 CASE-P-0025128, a txn-bearing C14) — the committed `data/casefile/*.bundle.json` population spans SIGN and REFUSE. If false (the stub signs every bundle → degenerate): T1 measure-first surfaces the honest NULL + the deliberately-ungrounded fallback case. **A2 [no oracle / consistency-only]:** the measure is counts-only — stub-vs-live SIGN/REFUSE + fabrication-guard CATCH count + grounding CONSISTENCY — NEVER an accuracy/catch-rate/precision/recall; a hand-authored "gold narrative" oracle REJECTED (synthetic gold is judgment, not truth). **A3 [measure/execute-once]:** the stub baseline is ALWAYS available; the live capture is best-effort — no model on :8080 → stub-only baseline + the named follow-on, NEVER fabricate. **A4 [companion-only / casework-unchanged, NON-NEGOTIABLE]:** all 9 dists byte-frozen; the vendored casework Drafter Protocol + the six grounding verifiers UNCHANGED (measure, don't modify — no re-vendor, pin `04cc335` == sibling HEAD); `evidence_requirements.py` + the 256/376 §12 funnel UNTOUCHED — the agent drafts, the six verifiers gate, the human signs. **HONESTY:** counts-only; the synthetic-substrate qualifier on every number; NO catch-rate/lift/precision/recall on the new markers + docs; the always-on badge stays (nothing new ships).
FROZEN (signal-watch, held this phase): ALL 9 ship dists byte-identical (`--check all` 9/9 — the workbench/chain are companion-only, touch NO dist); the vendored casework Drafter Protocol + the six grounding verifiers UNCHANGED (`git diff --quiet vendor/aml-casework` empty — no re-vendor); `evidence_requirements.py` UNTOUCHED (`git diff --quiet` empty — the drafter is the downstream DECIDE beat); the 256/376 casework signing funnel byte-unchanged. Phase 86 adds `tests/drafter_quality_harness.py` (the dep-free `score_drafts()` scorer + `--check`/`--freeze`, pinned under `tests/fixtures/drafter-quality/`) + the live-draft sign/refuse regression assertion in `tests/workbench.test.mjs` (+ `tests/chain.test.mjs` if present; MINIMAL new surfacing in `workbench.html`/`chain.html` only-if-needed); writes `docs/drafter-live.md` + marks `docs/agentification-roadmap.md` Stage 3 BUILT; trues `CLAUDE.md` + `HANDOFF.md`. `build.py` imports NO companion module (a grep guard: no `drafter_quality_harness`); the workbench/chain/merge/news/corpus arcs must not regress. The seam: `serve_chain.casework_consume(bundle, out, drafter, disposition)` → {signed, blocking_violations, narrative_present, completeness, drafter_effective} (`serve_chain.py:262-308`); the openai drafter defaults to `127.0.0.1:8080` (`serve_chain.DEFAULT_OPENAI_BASE`, no env).
Program context — Phase 50 — AML program build: the data-substrate pillar (spun out). The real build lives in /Users/jwang/aml-substrate (Python) + /Users/jwang/aml-casework; signal-watch is the program-ARCHITECTURE home (docs/program-blueprint.md) + this lifecycle record + the demo artifacts. aml-casework is VENDORED at `04cc335` (== sibling HEAD, Phase 21 — no newer drafter work landed; no re-vendor this phase). Phase 86 is a signal-watch-internal agentification phase (the consistency-not-correctness measure of an existing live loop — the STR drafter shipped Phase 57); it MEASURES the built drafter, it does not modify casework.
Abort: any casework src edit / a re-vendor / a build.py companion import (`drafter_quality_harness`) / any of the 9 ship dists not byte-identical (`--check all`) / an `evidence_requirements.py` or 256/376-§12-funnel change / a fabricated live drafter number / a degenerate measure presented as a contrast (or any accuracy/catch-rate/precision/recall framing) → STOP-and-surface. Measure-first / execute-once: NO model on :8080 for the live freeze → ship the stub-only baseline + flag the live capture a named follow-on with the one-command `--freeze`; NEVER fabricate. A one-sided/degenerate population → surface the honest NULL (the deliberately-ungrounded fallback case). The synthetic-substrate qualifier governs every count.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| [[decisions/phase-86-direction-str-drafter-measurement]] Phase 86 — Direction = Stage 3 STR drafter, but (state-loader finding) the infra shipped Phase 57 + there is no correctness oracle → the deliverable is the MEASUREMENT FRAME (a drafter quality harness + the counts-only headline + roadmap Stage 3 BUILT), not new infra; the roadmap's consistency-not-correctness class (GATHER), NOT a Stage-1/2 oracle headline. Rejected: a decision-table render of the Phase-85 §12 policy (narrower); an abstention ablation on the Stage-2 agent (analysis-only); a base-rate-aware proposer (overfit hazard, no held-out set). | medium | 2026-06-29 |
| [[decisions/phase-86-no-oracle-consistency-measure]] Phase 86 — the STR drafter has NO correctness oracle (free-text drafting); the measure is counts-only — stub-vs-live SIGN/REFUSE + fabrication-guard CATCH count + grounding CONSISTENCY — NEVER an accuracy/catch-rate/precision/recall (the consistency-not-correctness class). Rejected: a hand-authored "gold narrative" oracle (synthetic gold is judgment, not truth — overfit/authorship/honesty risk). User-positioned (Q2 = Accept). | medium | 2026-06-29 |
| [[decisions/phase-86-stub-vs-live-narrative-seam-contrast]] Phase 86 — the measure is non-degenerate because the deterministic STUB drafter fail-closes on the narrative-seam case (Phase-82 CASE-P-0025128, a txn-bearing C14) — the committed `data/casefile/*.bundle.json` population spans SIGN and REFUSE (the two-sided contrast). T1 measure-first CLASSIFIES first; honest NULL surfaced if degenerate (the deliberately-ungrounded fallback). Scale via a slice sample deferred. User-positioned (Q1 + Q3 = Accept). | medium | 2026-06-29 |
| [[decisions/phase-86-companion-only-casework-unchanged]] Phase 86 — companion-only: all 9 dists byte-frozen; build.py imports nothing new; the vendored casework Drafter Protocol + the six grounding verifiers UNCHANGED (measure, don't modify — no re-vendor, pin `04cc335` == sibling HEAD); `evidence_requirements.py` + the 256/376 §12 funnel UNTOUCHED. The agent drafts, the six verifiers gate, the human signs. No oracle firewall needed (no truth to hide — simpler than Stages 1/2). | medium | 2026-06-29 |
| [[decisions/phase-85-direction-determination-pre-proposer]] Phase 85 — Direction = the §12 determination PRE-PROPOSER (Agentification Stage 2, the 6th live loop), the only high-value LOCAL move: the consume/render frontier is closed + every substrate-gated path is a verified dead-end at HEAD `3716f77`. Mirrors Phase 83; every seam exists. Rejected: an honest-determination-panel render (lower leverage, framing risk ≈358/376 needs-more-info); Stage 3 STR drafter (roadmap-sequenced after Stage 2); hold-for-substrate (no new emit → no-op). | medium | 2026-06-29 |
| [[decisions/phase-85-over-flag-headroom-not-miss-recovery]] Phase 85 — The measurement is TWO-SIDED; the agent's measurable headroom is precision-recovery on the engine's structural OVER-FLAG (clear side), NOT recall-recovery on the file-MISSES (a DATA gap — substrate Ask #3 measured-null; reasoning can't recover what isn't in the bundle). A null on the miss side IS the finding. Abstention = coverage SEPARATE from accuracy. Do NOT promise a Phase-83 "54-vs-33"-shaped story. | medium | 2026-06-29 |
| [[decisions/phase-85-oracle-firewall-non-circular]] Phase 85 — The agent provably never sees `intended_disposition` (`proposer_input()` strip + `assert_no_oracle_leak()` reusing the Phase-78 allow-list; the served response carries no oracle field on the wire). Non-circular by construction — the Phase-77 circular-oracle trap avoided. Asserted in `determination_proposer`/`serve_workbench`/`workbench.test.mjs`. | medium | 2026-06-29 |
| [[decisions/phase-85-propose-gate-decide-a1-frozen]] Phase 85 — propose→gate→decide held: the agent proposes, the deterministic sufficiency engine still LICENSES, the human still DECIDES. `evidence_requirements.py` byte-unchanged; the 256/376 casework signing funnel unchanged; build.py imports nothing new; all 9 dists byte-frozen (companion-only). The proposal is presentation/measurement-only. | medium | 2026-06-29 |
| [[decisions/phase-84-render-drop-not-emit-gap]] Phase 84 — the "no counterparty names" gap is a STALE RENDER PATH in `workbench.html`, NOT a substrate emit gap, NOT a curation drop: the rich identity IS emitted (`counterparty_name` 372/376, `display_name` 56/56), carried by curate + delivered by `serve_workbench.py` verbatim, DROPPED only at render (`:556,580` read the code). Verified by parallel code-level agents on both pillars. Corrects the stale SUB-1 "bare codes" claim in `rich-case-target-contract.md`. | high | 2026-06-29 |
| [[decisions/phase-84-adapter-gated-by-probe]] Phase 84 — light the rich `showcaseSurface` for slice cases via a slice→showcase shape ADAPTER in `serve_workbench.case_detail` (reuse `scMoneyFlow`/`scResolution`/`scBOGraph`), NOT a one-line `showcase:True` flip (the slice flat shape differs) — but GATE the flip on a T1 feasibility probe (graceful degradation on heterogeneous slice data); fall back to an in-place name fix if it fails. Reuse over duplication, but measure-first before the structural flip. | medium | 2026-06-29 |
| [[decisions/phase-84-decisiveness-substrate-gated]] Phase 84 — slice cases will LOOK like northstar after names but NOT RESOLVE like northstar: the 2nd corroborating FILE-side leg is a Phase-41 measured-null + `ownership_edges` is CLI-null (verified substrate HEAD `3716f77`). Decisiveness (FILE/CLEAR) is OUT OF SCOPE — substrate-gated; this phase re-sharpens the handoff brief instead. User positioned (chose "re-sharpen the substrate brief"). | high | 2026-06-29 |
| [[decisions/phase-84-names-honest-without-implied-ER]] Phase 84 — surface counterparty names honestly: the money-flow graph groups WITHIN-account/by-ref with an explicit no-cross-account-ER badge — substrate's counterparties are per-account-local synthetic names; never imply cross-account entity resolution (the >90%-FP discipline). No fuzzy name-matching in any new render. | high | 2026-06-29 |
| [[decisions/phase-82-grounded-evidence-consume-path]] Phase 82 — consume P39 predicate + P40 mitigation as bundle DATA read by `serve_workbench.determine_case`, NOT a rule edit (the engine already exposes the params, line 310-312); the A1 guard holds by construction. Result: 1 KYC determination + 17 ML `cleared`, was 0/0. Rejected: editing the engine rule (A1 violation); re-attempting the C17 leg (degenerate, Phase 81). | high | 2026-06-29 |
| [[decisions/phase-82-merge-org-abort-no-resolution-handle]] Phase 82 — the merge-org track aborted measure-first a 3rd time: P38 built the org fork (364 O-FRAG) but the fragments share NO resolution handle with their base → 0 uphold → one-sided. `dist/merge` BYTE-FROZEN; the brief sharpened to ask for a RETAINED shared identifier. Rejected: fuzzy name-matching (the wiki ER caveat). | high | 2026-06-29 |
| [[decisions/phase-82-kyc-signs-honest-relaxation]] Phase 82 — the Phase-72 "kyc always signs" assertion honestly relaxed (a txn-bearing C14 MAY fail-closed at the narrative seam; ≥1 MUST sign preserved); NOT a code regression (signing is casework's grounding_replay). Rejected: pinning the test to the old count (would hide the honest fail-close). | high | 2026-06-29 |
| [[decisions/phase-79-merge-supersede-substrate-scored]] Phase 79 — the merge console SUPERSEDED the consensus-66 with 29 substrate-anchored SCORED cases; both populations scored, split by oracle PROVENANCE via the non-circular `GT-<hash>` fork (`entity_ref ≠ cluster`). Rejected: keep the 66 as a 3rd consensus population (an independent oracle now exists). | high | 2026-06-27 |
| [[decisions/phase-78-bundle-only-non-circular-validation]] Phase 78 — score the BUNDLE-ONLY signal structure (mechanism + ≥2 legs), not the human-gated verdict — the non-circular validation frame; the oracle never enters an engine input (`assert_no_oracle_leak`). A per-class confusion structure, not an accuracy. | high | 2026-06-26 |

Older decisions (Phase 73–82 — invert-pillars, the entity spine, the merge console, the three-consumes split, the sanctions arc, the §12 grounded-evidence loop) live in `articles/decisions/` + index.md.

## Blockers and Open Questions

- [RESOLVED 2026-06-29 · planning, by the Phase-86 gate + the 4 decisions] The three Phase-86 planning questions (what is the DELIVERABLE for the STR drafter · what honest MEASURE replaces the absent correctness oracle · is the drafter the highest-value Phase 86 vs the 3 alternatives) closed at the gate (all_accept: true, NOT silent; AskUserQuestion — Direction "Stage 3 STR drafter, deliverable = the measurement frame"; Q1+Q3 "Accept — the narrative-seam stub fail-close is the two-sided contrast, population = the committed casefile bundles"; Q2 "Accept — counts-only consistency, no gold-narrative oracle"). **DELIVERABLE = the MEASUREMENT FRAME, not new infra:** the STATE-LOADER FINDING (code-verified) is that the STR drafter + its verifier gate ALREADY SHIPPED in Phase 57 — the Drafter Protocol (`vendor/aml-casework/src/aml_casework/narrative_generator.py`), the `--drafter {stub,claude,openai,opencode}` switch + the four backend adapters, the backend mapping `serve_chain.py:188`, the live-draft staged reveal; a model is UP on :8080. **MEASURE = consistency-not-correctness (no oracle):** free-text drafting has NO committed narrative reference → counts-only stub-vs-live SIGN/REFUSE + fabrication-guard CATCH count (`blocking_violations`) + grounding CONSISTENCY, the GATHER `tests/gather_quality_harness.py` pattern (NOT a Stage-1/2 oracle headline; a hand-authored gold narrative REJECTED). **NON-DEGENERATE:** the stub fail-closes on the narrative-seam case (Phase-82 CASE-P-0025128) → the population spans SIGN and REFUSE. **vs the alternatives:** the decision-table render (narrower — one agent's artifact), the abstention ablation (analysis-only), the base-rate-aware proposer (overfit hazard, no held-out set, [[decisions/phase-85-base-rate-remeasure-not-overfit]]) all rejected; the drafter is roadmap-sequenced + now the right move. RESOLVED into the 5-task plan: T1 `tests/drafter_quality_harness.py` (the dep-free `score_drafts()` scorer + `--check`/`--freeze`, the dependency, M), T2 EXECUTE ONCE + the headline (S), T3 the workbench/chain live-draft surface regression-guard (S), T4 docs + roadmap Stage 3 BUILT (S), T5 verify + drift guard + true-up (S). ONE STANDARD phase (companion-only; NO L; NO oracle firewall needed — no truth to hide, simpler than Stages 1/2). Decision articles [[decisions/phase-86-direction-str-drafter-measurement]] / [[decisions/phase-86-no-oracle-consistency-measure]] / [[decisions/phase-86-stub-vs-live-narrative-seam-contrast]] / [[decisions/phase-86-companion-only-casework-unchanged]] (medium, source plan); plan [[phases/phase-86-str-drafter-consistency-measure]]; ledger Phase-86; spec = `## Formal Spec` IN the phase article (standard ceremony, no separate /spec round). KNOWLEDGE GAPS carried to impl: (1) the exact live-drafter outcome on the narrative-seam case (does the local model sign where the stub fail-closed, or get caught by the fabrication guard?) — resolved by T2 EXECUTE ONCE; (2) whether the existing workbench/chain live-draft UI already surfaces the sign/refuse + fabrication-catch climax — resolved by T3 (minimal add only if not); (3) the precise `--check` replay shape given the casework subprocess boundary (pin consume results vs re-run) — resolved in T1 (pin the consume results; the pure scorer is the dep-free guard). (raised + resolved 2026-06-29)
- [RESOLVED 2026-06-29 · planning, by the Phase-85 gate + the 4 decisions] The Phase-85 direction (what to do after Phase 84 — the consume/render frontier is closed, every substrate-gated path a verified dead-end) closed at the gate (all_accept: true, NOT silent; AskUserQuestion, two rounds — Direction "§12 determination pre-proposer (Agentification Stage 2)"; Q1 "Accept" + the substrate-recency RIDER; Q2 "stub-complete, live if a model is up"; Q5 "coverage separate from accuracy"). A1's RIDER DISCHARGED this session: aml-substrate HEAD code-verified **3716f77** — UNCHANGED since Phase 84's pin (`3716f77..HEAD` empty; last substrate phase still Phase 41, Ask #3 = a MEASURED-NULL) → the miss-side data gap is REAL, not a stale pin. SEAMS verified built (code-level): `determine_case(named_risk=, mitigation_established=)` override kwargs; the Phase-78 non-circular exogenous `intended_disposition` oracle + `assert_no_oracle_leak` + the `determination_validation_harness`; the Phase-83 replay-harness (`--check`/`--freeze`) + build-stripped `/*LIVE_*/` overlay pattern. **THE ASYMMETRY (load-bearing, NOT Phase-83's symmetric headroom):** the §12 engine's error mass is structural OVER-FLAG (Phase-78 pre-measured 727 KYC-pure file-ready-but-oracle-clear + 593/6087); the file-MISS side is a DATA gap (71/121 missed oracle-file cases miss because the 2nd leg isn't in the bundle — substrate Ask #3 measured-null) → the agent's headroom is precision-recovery on the CLEAR side, the file-misses HONESTLY NULL (that null IS the finding). RESOLVED into the 6-task plan: T1 the proposer + firewall + scorer (the dependency, M), T2 the dep-free `--check` quality harness (M), T3 the `/propose-determination` route (S), T4 the workbench proposal panel (M), T5 EXECUTE ONCE + the headline (S), T6 docs + drift guard + roadmap Stage 2 BUILT + CLAUDE.md/HANDOFF (S). ONE STANDARD phase (companion-only, agentic; NO L). Decision articles [[decisions/phase-85-direction-determination-pre-proposer]] / [[decisions/phase-85-over-flag-headroom-not-miss-recovery]] / [[decisions/phase-85-oracle-firewall-non-circular]] / [[decisions/phase-85-propose-gate-decide-a1-frozen]] (medium, source plan); plan [[phases/phase-85-determination-pre-proposer]]; ledger Phase-85; spec = `## Formal Spec` IN the phase article (standard ceremony, no separate /spec round). KNOWLEDGE GAPS carried to impl: none blocking — all seams code-verified this session; the ONE open item is whether a model is reachable on :8080 at T5 (if not, the StubProposer baseline ships + the live capture is a named follow-on, never fabricated). (raised + resolved 2026-06-29)
- [RESOLVED 2026-06-29 · planning, by the Phase-84 gate + the 4 decisions] The Phase-84 scope questions (slice TX-table coded render vs the showcaseSurface gate · adapter vs gate-flip · the substrate-blocked BO graph · whether the "no names" premise is partly false · render-vs-signing decoupling) closed at the direction gate (two AskUserQuestion rounds, all_accept, NOT silent — scope = render-parity, decisiveness = re-sharpen-the-brief). DIAGNOSIS verified by parallel code-level agents on BOTH pillars: the gap is a STALE RENDER PATH in `workbench.html`, not an emit gap — `counterparty_name` 372/376 (≈89% of legs), `related_parties[].display_name` 56/56, carried by curate (`curate_workbench_cases.py:277,281`) + delivered by `serve_workbench.py:132-133` verbatim, DROPPED at render (`txTable` :580 / `counterpartySummary` :556 read `counterparty_ref`; the rich `showcaseSurface` is hard-gated to the 2 authored cases at `serve_workbench.py:119`). aml-substrate HEAD verified `3716f77`: identity emitted since substrate Phase 27; Ask #3 (2nd FILE-side leg) = Phase-41 measured-null, Ask #4 (`ownership_edges` multi-hop) = CLI-null (the BO blocker). RESOLVED into the 6-task plan: T1 the feasibility probe (the measure-first gate; branches adapter-via-showcaseSurface vs in-place vs surface-skip), T2 the slice→showcase adapter in `case_detail`, T3 the rich render (named ledger + money-flow + the no-cross-account-ER honesty badge + resolution graph), T4 named BO + the "Ask #4 pending" multi-hop marker, T5 the substrate handoff brief re-sharpen + the SUB-1 true-up, T6 verification + honesty sweep + CLAUDE.md/HANDOFF. ONE STANDARD phase (companion-only RENDER; NO L). Decision articles [[decisions/phase-84-render-drop-not-emit-gap]] (high) / [[decisions/phase-84-adapter-gated-by-probe]] (medium) / [[decisions/phase-84-decisiveness-substrate-gated]] (high) / [[decisions/phase-84-names-honest-without-implied-ER]] (high); plan [[phases/phase-84-workbench-rich-case-render-at-scale]]; ledger Phase-84. KNOWLEDGE GAP carried to impl: whether the rich graphs degrade gracefully on real heterogeneous slice data — RESOLVED by the T1 feasibility probe (the branch point between adapter and in-place name fix). (raised + resolved 2026-06-29)
- [RESOLVED 2026-06-29 · planning, by the Phase-83 gate + the 4 decisions] The Phase-83 direction (what to do after Phase 82 — the local consume frontier is exhausted, the siblings stalled at the Phase-82 pins) closed at the gate (all_accept: true, NOT silent; AskUserQuestion, two rounds — Frontier "Agentification S1: merge adjudicator" · Q1 "Accept — measure, report by quadrant" · Q2 "4-way + count deferrals" · Q3 "Add a served surface now" · Q3b "(A) companion live mode"). Siblings code-verified LIVE this session as STALLED ([[cross-pillar-review-verify-sibling-repo]]): aml-substrate `294d3e5`/Phase 40, aml-casework `04cc335`/Phase 21 — no new emission to consume → the agentification roadmap (`docs/agentification-roadmap.md`, authored post-Phase-82) is the signal-watch-internal forward path. The seams Phase 83 builds on were code-verified (file:line): `resolution_scorer.candidate_pairs()` + the KLASS_* quadrants (33-right/33-wrong stub baseline confirmed) + `assert_no_cluster_leak`; `osint_tools.call_openai`/`parse_llm_json`; the `gather_quality_harness.py` `--check`/`--freeze` pattern; the `serve_corpus.py` + `/*LIVE_*/` build-strip pattern. RESOLVED into the 8-task plan: T1 the adjudicators + firewall + scorer (dependency), T2 the dep-free `--check` quality harness, T3 `serve_merge.py` (the one L), T4 the `merge.html` LIVE overlay + build-strip, T5 `merge-console.test.mjs` live-branch, T6 the drift sweep + firewall grep, T7 EXECUTE ONCE (live `--freeze` if a model is on :8080, else the stub baseline + a named follow-on), T8 docs + CLAUDE.md + honesty sweep. ONE STANDARD phase (agentic). Decision articles [[decisions/phase-83-merge-adjudicator-stage1-frame]] / [[decisions/phase-83-companion-live-not-baked]] / [[decisions/phase-83-4way-vocab-count-deferrals]] / [[decisions/phase-83-measure-by-quadrant-two-sided-baseline]] (medium, source plan); plan [[phases/phase-83-merge-adjudicator-oracle-scored]]; ledger Phase-83; spec `specs/phase-83-merge-adjudicator-oracle-scored.md`. KNOWLEDGE GAPS carried to impl: none blocking — all seams code-verified this session; the ONE open item is whether a model is reachable on :8080 at T7 (if not, the stub baseline ships + the live capture is a named follow-on, never fabricated). (raised + resolved 2026-06-29)
- [RESOLVED 2026-06-29 · planning, by the Phase-82 gate + the 3 decisions] The Phase-82 direction (how to proceed after Phase 81 — the consume frontier re-opened with substrate P38/39/40 + casework P20) closed at the gate (all_accept, NOT silent; AskUserQuestion — scope "Both clusters (full batch)" · §12 "measure-first-with-fallback, rule frozen" · merge "measure two-sidedness on our own path, one-sided → abort" · casework "re-vendor + funnel re-measure"). Sibling HEADs code-verified LIVE this session ([[cross-pillar-review-verify-sibling-repo]], 3 background investigators, file:line): aml-substrate f7fbdb0→**294d3e5** (Phase 40 — P38 org-fragment `a9a088a` un-aborts Phase-81's merge-org track; P39 predicate-reference `1483c84` closes the measured 0-of-376 named-predicate gap, Ask #1 of the northstar-evidence brief; P40 affirmative-mitigation `978c8fe`, Ask #2, the clear-side mirror), aml-casework 076fb8e→**04cc335** (Phase 21 — P20 C15/C4 reconcile `a059fc5` [~55 fail-close→sign] + P21 drift-hardening). A1 VERIFIED line 310-312: `evidence_requirements.py` already exposes the `named_predicate_risk` + `mitigation_established` params → P39/P40 are bundle DATA, the A1 guard holds by construction (the consume is curate/serve-layer). The 4 sub-questions (org two-sidedness re-measure / predicate vs C17 / mitigation clear-path / casework re-vendor + funnel / scope-ceremony) RESOLVED into the 8-task plan: T1 emit + re-pin, T2 the four rigorous measure-first deltas (RIGOROUS engine, never a proxy — the Phase-81 T1b lesson), T3 merge-org (gated T2a), T4 §12 predicate (gated T2b), T5 §12 mitigation (gated T2c), T6 casework re-vendor (T2d funnel direction), T7 brief true-ups, T8 verify. ONE STANDARD phase (the batch-consume precedent; each track independently gated). Decision articles [[decisions/phase-82-northstar-evidence-at-scale-frame]] / [[decisions/phase-82-measure-first-four-deltas]] / [[decisions/phase-82-full-batch-four-tracks]] (medium, source plan); plan [[phases/phase-82-consume-sibling-northstar-evidence-at-scale]]; ledger Phase-82; spec `specs/phase-82-consume-sibling-northstar-evidence-at-scale.md`. (raised + resolved 2026-06-29)
- [RESOLVED 2026-06-28 · planning, by the Phase-81 gate + the 2 decisions] The Phase-81 direction (consume the substrate delta + plan for open sanctions datasets, non-commercial) closed at the gate. Sibling HEAD code-verified LIVE this session ([[cross-pillar-review-verify-sibling-repo]]): aml-substrate advanced 1f5901e→**f7fbdb0** (Phase 37 — 3 phases ahead: P35 org-name OFAC `4f49e53` + P36 exposure-via-ownership C17 `1651b1e` + P37 geo `5b5cf32`; substrate already ships a REAL OFAC SDN watchlist `data/reference/watchlist_ofac.csv` [2,500 names, US-Gov public domain, label-blind false-positive trap]; substrate CUT the old TF + broader-C7 asks this session — `already-null` / `tell-unavoidable`); aml-casework **076fb8e** (Phase 19 — unchanged; does NOT ground C17). Licensing reality verified (opensanctions.org/licensing): the consolidated dataset is CC-BY-NC 4.0 — "any business purposes requires a licensing agreement"; the demo's buy-in purpose is arguably commercial → the CC-BY-NC consolidation is no-ship; the underlying public-domain/open-gov SOURCE lists (OFAC PD / UK HMT OGL) are the clean path. The engine derives legs from capabilities via profile DATA → the C17 leg is A1-preservable (profile atom + companion assembly, `evidence_requirements.py` byte-frozen). DECISION: consume all three substrate phases (the merge ORG name-collision class [T1a measure-first gated] + the C17 exposure leg as a new evidence atom [T1b measure-first gated, "evidence-advance, rule frozen"] + P37 geo render) + a PLAN-ONLY open-sanctions data-fork brief (per-source license matrix + the non-commercial boundary). Decision articles [[decisions/phase-81-consume-sanctions-arc-all-three]] / [[decisions/phase-81-exposure-leg-evidence-advance]] (medium, source plan); plan [[phases/phase-81-consume-substrate-sanctions-arc]]; ledger Phase-81; spec `specs/phase-81-consume-substrate-sanctions-arc.md`. (raised + resolved 2026-06-28)
- [RESOLVED 2026-06-27 · planning, by the Phase-80 gate + the 2 decisions] The Phase-80 direction (how to proceed after Phase 79 — "double check sibling work to see where things are") closed at the gate. Both sibling HEADs code-verified LIVE this session ([[cross-pillar-review-verify-sibling-repo]]): aml-substrate advanced to **1f5901e** (Phase 34 — seam-5 sanctions-screening realism; Phases 32/33 already consumed in the Phase-79 slice, verified by the "Anna Vega"/"Anna eVga" fragment pairs in `data/merge/cases.json`); aml-casework sits at **076fb8e** (Phase 19 — the Phase-79 vendor pin, nothing new). Phase 34's sanctions layer is the ONE unconsumed emission; everything else is substrate P35+ (TF/C1[a measured null]/broader-C7/org-sanctions/Stage-2-3 — NOT yet built). DECISION: a full consume (merge + workbench) — the OFAC watchlist name-collision MERGE case class (MEASURE-FIRST gated; one-sided → abort to workbench-only + brief, user-positioned) + the non-tautological sanctions-driven C14 §12 workbench leg + the substrate P35 brief. Decision articles [[decisions/phase-80-merge-plus-workbench-consume]] / [[decisions/phase-80-sanctions-oracle-measure-first]] (medium, source plan); plan [[phases/phase-80-consume-substrate-sanctions-screening]]; ledger Phase-80; spec `specs/phase-80-consume-substrate-sanctions-screening.md`. DRIFT corrected: casework's txn-less C14 party-leaf is NO LONGER fails-closed (completes via the resolving party leaf, test reaches `signed`) — CLAUDE.md's Phase-72 "kyc signing is the txn-contract frontier" note is partially stale (T5 corrects it). (raised + resolved 2026-06-27)
- [RESOLVED 2026-06-27 · planning, by the Phase-79 gate + the 2 decisions] The Phase-79 scope questions (which consume(s) / sequence · the merge-oracle honesty re-check · the dist boundary · re-vendor staleness · ceremony) closed at the direction gate ("Bundle, gated upside" via AskUserQuestion; all_accept warned-not-silent). Both Phase-77-deferred blocks verified RESOLVED sibling-side this session (file:line, [[cross-pillar-review-verify-sibling-repo]]): casework b3546d4→**076fb8e** (Phase 19 `_c3_fan_in`); substrate f2da3e4→**c099259** (Phase 33; the Phase-32 anchored fork @**31cb439** mints `entity_ref ≠ cluster` fragments — the circular-oracle cure). DECISION: a floor + gated-upside bundle — the Lakeshore fan-in floor committed (companion-only); the merge real-data oracle measure-first companion-only, the `dist/merge` re-freeze GATED on a clean two-sided result (the live `--anchored --emit-eval-oracles` run CRASHED today → measure-first de-risks both the emit and the dist boundary; the Phase-77 abort rule governs). Decision articles [[decisions/phase-79-floor-plus-gated-upside-bundle]] / [[decisions/phase-79-merge-measure-first-before-dist]] (medium, source plan); plan [[phases/phase-79-consume-sibling-emissions]]; ledger Phase-79; spec `specs/phase-79-consume-sibling-emissions.md`. (raised + resolved 2026-06-27)
- [RESOLVED 2026-06-26 · planning, by the Phase-78 gate A1–A5] The Phase-78 direction (consume substrate Phase 31's CLI-wired disposition oracle) closed all-accept: (Q1) bundle-only non-circular score frame; (Q2) measure→control (the §12 discovery feed). T2 measure-first PASSED non-degenerate. Ledger Phase-78. (raised + resolved 2026-06-26)
- [RESOLVED 2026-06-26 · planning, by the Phase-77 gate] Consume the three sibling emissions — 1 landed (casework `cleared` via a C5 proxy), 2 deferred to named briefs (both NOW resolved sibling-side and consumed in Phases 78–79). Ledger Phase-77. (raised + resolved 2026-06-26)
- (Older resolved-planning blockers Ph71–Ph76 archived to their ledger blocks + decision articles + index.md.)
- [RESOLVED 2026-06-16 · Phase 54] CONTROL STORY for the measured-not-gated dimensions — the production-grade SR-11-7-class control DESIGN (cadence, sampling, ownership, where it sits in the 5% charter), named at Phase-47 T2/T3 but not written, is now **written + executable for the C/D tag**: `docs/cd-tag-control.md` (SR-11-7 Pillar-2 + OSFI E-23 grounded; scope/risk-tier/baseline/cadence/trip-wires/independent-challenge/ownership/escalation/audit-walk) + `cd_correctness.py --control-check`/`--control-freeze` (PASS on the frozen corpus, BREACH on injected drift). The first concrete instance; the reusable program-wide control PATTERN (other judgmental dims) + a human-rater panel stay deferred-with-owner. (raised 2026-06-12; resolved 2026-06-16)
- [OPEN · resolves at Phase-47 T3] HONESTY→EFFECTIVENESS measurement layer — the validation story (no-ground-truth doctrine, drift/ongoing-monitoring per SR 11-7 Pillar 2, what survives of the always-illustrative posture) gets its EXPLICIT designed-now vs deferred-with-owner disposition in T3; the committed quality-harness baseline (--check/--freeze) is the named embryo. (raised 2026-06-12)
- [RESOLVED 2026-06-10 · planning, by the Phase-46 gate D1–D4] The four Phase-46 planning questions closed at the dev-plan gate: TIMING + DIRECTION → the user REFRAMED off all 5 DEFERRED candidates (the 5th reframe in 6 gates) to **corpus live derivation mode** ("live mode with local model for the corpus demo as well, ideally with a better harness integration like opencode" + a pasted practitioner report) — a feedback-robust back-end/live-layer direction; post-presentation audience feedback surfaces against the active phase before T2 if it lands. The IF-(third-jurisdiction) AUSTRAC/UK derivation-surface research gap and the IF-(residuals) mojibake-repair measurement (fresh 2026-06-10: 161 derived + 501 md marker chars, below the carried ~2.2K; repair needs a conscious unfreeze) travel with their still-deferred candidates in the DEFERRED list above. Assumption gate closed 2026-06-10, all_accept: TRUE (A1–A4); decision article articles/decisions/phase-46-corpus-live-derivation.md; ledger block in assumption-ledger.md. KNOWLEDGE GAPS carried to impl: whether the FINTRAC /intel/ frontier doc parses under the existing rf_region anchors (T1 exercises it; extension REGRESSION-GATED); whether opencode + the existing Qwen can drive a reliable tool-calling agent loop at all (T1's whole job); full-advisory derivation wall-time at local-model speed (T1 measures — may force a short-doc demo shape or per-indicator staged reveal).
- [RESOLVED 2026-06-10 · planning, by the Phase-45 gate A1–A5] The five Phase-45 planning questions all closed at the assumption gate (all_accept: false — A1 don't-know round 1 → defended with worked real-value examples → accepted): LIFT SHAPE → R2 honest real-inventory counts (no performance claim, disclaimer removed, R1 zero-numbers copy-only fallback) [A1]; SHOWCASE CONSISTENCY → Act-5 byte-frozen, deliberate cross-artifact divergence accepted [A2]; INCONSISTENCIES INVENTORY → already measured by the two pre-gate review agents (findings ranked in articles/decisions/phase-45-corpus-presentation-polish.md; the gate fix is COPY + stagecraft, no interaction redesign [A3]); DELIVERY-REVIEW ARTIFACT → corpus.html copy edits + presenter/demo-path notes into tests/smoke-checklist.md [A4 attribution extension + A5]; FEEDBACK ABSORPTION → T5 user-walkthrough FREEZE checkpoint, dispositions ranked + time-boxed, overflow explicitly deferred [A5]. Decision article approved; ledger block in assumption-ledger.md.
- [RESOLVED 2026-06-10 · planning, by Phase-44 D1–D5] Phase-44 DIRECTION + the conditional IF-… sub-questions — the user REFRAMED off the offered candidates at the dev-plan gate (the 4th reframe in 5 gates): Phase 44 = LIVE EXTRACTION QUALITY — red-flag RECALL (obvious high-risk-country wire flags missed in limited real testing) + alias-assignment PRECISION (entities assigned aliases clearly not theirs) + a FRESH dedicated processing page + the user's own harness hypothesis accepted as the T1 frame + SPEED resolved as quality-gated T4. Measure-first/classification-first (D2: committed targeted harness w/ alias-OWNERSHIP scoring + synthetic wire notes embedding the user's sample sentences, local-only; both failures reproduced + classified before any fix); fixes through proven seams (D3: prompt-regression gate + the news_ground regate procedure; structural EXTRACT_SCHEMA change = a surfaced finding); quality-gated speed (D4); LIVE-region-only UI + lossless hygiene (D5). The IF-(hygiene) and IF-(verify-latency) sub-questions were ABSORBED into Phase 44 (T6/T4); the IF-(FINTRAC /intel/)/(third jurisdiction)/(fuzzy-merge)/(bulk scan) sub-questions travel with their candidates in the DEFERRED list above. Assumption gate closed 2026-06-10: A1 accept-with-conditions (sample sentences; speed → quality-gated T4), A2 accept, A3 don't-know→defended→accept, A4 accept; ledger block in assumption-ledger.md; decision article articles/decisions/phase-44-live-extraction-quality.md. KNOWLEDGE GAPS carried to impl: whether the user's wire-miss material reproduces (T1's job); the actual failure classes (T1); whether batched verify preserves quality (T4).
- [RESOLVED 2026-06-10 · planning, by Phase-43 D1–D5] Phase-43 DIRECTION + the conditional IF-(a)…(e) sub-questions — the user REFRAMED off all five carried candidates at the dev-plan gate (none picked; all stay in the DEFERRED list above, their open sub-questions traveling with them): Phase 43 = LIVE PIPELINE ROBUSTNESS + PROGRESSIVE PRESENTATION — a live test on a REAL investigation note timed out ~200s with NO intermediate feedback; extraction must work regardless of article size/complexity, and the page must show staged progress instead of an opaque wait. Measure-first (D2: the stress harness classifies read-timeout vs finish_reason=length truncation vs context overflow [/props n_ctx] vs intrinsic degradation; the ~200s failure class must be reproduced); rendering = stage-completion-driven, corpus-demo-style (D3 — A2' after a reject-by-reframe round 1: NO partial-JSON parsing, NO visible token stream; internal stream:true serves only the timeout fix + a token counter); scope = pure transport (D4: gate/prompt/schema/store-writes/fixtures FROZEN, call_llm signature preserved — the fixture seam); privacy (D5: the real note never committed, stress notes synthetic, commercial captures local-only, fixtures US-federal-only). Assumption gate closed 2026-06-10: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept; ledger row in assumption-ledger.md; decision article articles/decisions/phase-43-live-pipeline-robustness.md. KNOWLEDGE GAPS carried to impl: the server's actual n_ctx (measured at T1); whether stream:true composes with the json_schema strict grammar in the user's llama-cpp build (verified at T2); the actual failure class (T1's whole job).
- [RESOLVED 2026-06-10 · planning, by Phase-42 D1–D5] Phase-42 DIRECTION + the conditional IF-(a)…(f) sub-questions — the user picked (a) anchor dossier view + a per-scan network visualizer, with the (c) CLAUDE.md trim bundled as T6. The IF-(a) sub-question resolved IN the design: a surfaced CONFLICT = same anchor + same kind + different value (the both-kept rows per Phase-41 D2; presentation-only flag, never auto-resolve); the view = a `GET /anchor` companion route + a news.html LIVE-region panel (the watchlist-panel pattern, NOT a separate page), the per-scan graph as the navigation surface. The IF-(b)/(d)/(e)/(f) candidates stay in the DEFERRED list above (their open sub-questions — Python-side fuzzy scorer port-vs-move + reversible merge mechanics; rf_region anchor coverage for the /intel/ SBs; the third licence basis; bulk-scan wall-time/deliverable — travel with them). Assumption gate closed 2026-06-10: A1/A3 accept, A2 don't-know→A2' accept (evidence check: the 7 fixture articles are case-disjoint, zero cross-article entity overlap → a committed SYNTHETIC investigation-note seed replaces any re-scan-different-articles script), A4 accept-with-condition (SVG is the INITIAL implementation for demo testing), A5 scope constraint (pure consumption); ledger block in assumption-ledger.md; decision article articles/decisions/phase-42-anchor-dossier-network-view.md.
- [OPEN · planning, gated at Phase-38 T1 CHECKPOINT] Phase-38 QWEN-VS-CLAUDE DIVERGENCE ADJUDICATION — the Half-A evaluation reports a Qwen-vs-Claude INTER-RATER AGREEMENT + the actual divergences (which entities/flags one rater caught and the other missed) over the committed DOJ/OFAC articles. The Claude reference is a SECOND rater, NOT ground truth; the user adjudicates the divergences at the T1 checkpoint before the goldens lock. NO accuracy/precision/recall number is presented as real; the always-on badge stays. Resolved at the checkpoint (T1), not blocking the capture itself.
- [OPEN · planning, gated at Phase-37 T1] Phase-37 TYPOLOGY MODEL — single primary typology per indicator vs a multi-label SET vs an honest indicator-level "sector-general" bucket. The T0 risk: many sector-page indicators are genuinely CROSS-CUTTING (structuring serves drug-trafficking AND fraud AND TF), so forcing one typology may merely RELOCATE the catch-all to indicator level. The T1 measure-first probe (section→typology determinism + the cross-cutting fraction over the ~589 unique texts) decides this AT A CHECKPOINT with the user before any tagging; also open at the checkpoint: whether the 3 specific sector pages (casinos/real-estate/VC) fold into per-indicator tagging for consistency, and which closed-vocab EXTENSIONS (if any) are grounded. Do NOT tag before the checkpoint lands.
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- [CANDIDATE · future phase, raised 2026-06-08 by the user] **SearXNG-backed authoring search/fetch.** A self-hosted SearXNG meta-search + readability content-extraction capability for the BUILD-TIME authoring pipeline (corpus + news acquisition) — more robust than WebFetch/`curl`, which hit bot-protection on gov sites this phase (DOJ 403; we routed around it via the Wayback Machine). Already prototyped in the sibling `nanaclaw` project (Phase 5: `SEARXNG_URL` service + `web_search`/`web_extract` MCP tools); not currently running here (localhost:8888 refused). Authoring-only — NEVER in the ship artifact (offline non-negotiable). A future-phase candidate, NOT this phase.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)
- (Resolved pre-Phase-42 planning entries — ~20, each multi-KB — archived VERBATIM to [articles/state-archive-resolved-blockers.md](articles/state-archive-resolved-blockers.md) by the Phase-44 T6 hygiene trim; each is also in its decision article + journal.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus; Phase 24 added `jurisdiction` per source + `load_typology_map`/`validate_typology` (build-boundary typology gate) + merges typology/jurisdiction into `__CORPUS__`; Phase 25 added `_inline_article`/`_strip_provenance` (inlines each live doc's full source-md body into `__CORPUS__`) + a re-declared `red_flag` SHAPE boundary check in `validate_corpus_data`; **Phase 47** added `load_console_cases`/`validate_console_cases` + the `console` target wired into `all`/`--check`; **Phase 49** added `TRIAGE_*` + `load/validate/render/build/check_triage` (4 tamper + 2 drift classes fail-loud; probe-history NEVER read) + the `triage` target; **Phase 76** added `MERGE_*` + `load_merge_cases` + a STANDALONE `validate_merge_cases` (closed adjudication vocab + referential integrity + the resolver-input firewall `MERGE_TRUTH_LEAK_KEYS` + the consensus/scored honesty split) + `render/build/check_merge` + the `merge` target — **`all`/`--check` now 9 artifacts** (the 8 existing byte-frozen except the sanctioned launcher cascade); build.py imports NO spine/scorer/sibling/curate | 2026-06-25 |
| data/typology-map.json | Phase 24 NEW cross-corpus typology OVERLAY (22-term closed vocab + 42-entry doc-id→typology map; jurisdiction from the source registry, not stored). A SEPARATE committed file — the source dirs + derived records stay byte-frozen; validated fail-loud at the build boundary | 2026-06-07 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>`; Phase 25 added an ADDITIVE `red_flag` SHAPE check to `check_record` (grounding normalize/rf_region/flag⊂md BYTE-UNCHANGED) + 3 `--selftest` fixtures | 2026-06-07 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (2.15MB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips). Phase 24 Documents/Typologies toggle + synthesis view; Phase 25 the per-doc "Read advisory" screen + the `red_flag` label through the 6-screen arc. **Phase 27 (shippability)**: `cleanArticle()` markitdown-sanitizes the displayed source (form-feeds / running headers / tab-soup; display-only, source md untouched); `highlightArticle` rewritten to normalize-both-sides + index-map (634/634 = 100% highlight); the Signal build-log ports the Act-4 `.run` working-pulse in a proposal grid + a combination-lift lift-side panel (firestat OMITTED); the progressive read types the WHOLE article (no 1600 cap, length-scaled ~6s) | 2026-06-07 |
| news.html + dist/news/index.html | M8 adverse-media stream (third ship artifact). Arc: Select → Read → Screen (real Jaro-Winkler, 0.85) → Disposition (human gate; deterministic SVG subject network + anchor dossier in LIVE mode) → Exposure. OFFLINE ship = static book only; ALL live client code in `/*LIVE_START*/…/*LIVE_END*/`, build-stripped (dist/news byte-identical). Phase 43 added the stage-completion LIVE preview (grounded flags FINAL + provisional chips + token counter — never a token stream); Phase 44 moved it onto a DEDICATED PROCESSING PAGE (in-page takeover, presenter keys guarded, Esc arm→abandon) and made grounding WRAP-TOLERANT (locate_span requote) + folds ambiguity-refusing/type-matched. Harness `tests/news-stream.test.mjs` (150, both motion modes + live overrides + strip assertion). | 2026-06-10 |
| scripts/{serve_news,news_ground,news_store,news_fetch}.py | The LIVE news companion (dev/authoring-time only; offline ship unaffected): serve_news = localhost page + llama-cpp proxy + NDJSON stage-stream `/extract` (Phase 43: call_llm STREAMS SSE w/ idle-gap 120s timeout, MAX_GEN_TOKENS 16384, failures NAMED in-stream, preflight_size vs /props n_ctx FAIL-OPEN, single-flight 409, disconnect-persists-nothing) + `/watchlist[/prune]` + `/anchor`; news_ground = the SHARED grounded-or-stripped gate (the one allowed build→companion import; vocab single-authority); news_store = gitignored DuckDB ANCHOR store (.venv-only; build.py never imports it); news_fetch = URL fetch ladder + standardizer + article-shape verifier. Replay: 13 US-federal fixtures (`tests/fixtures/news-live/`, FIXTURE_META allowlist), goldens deterministic from pinned captures; `tests/news_live_test.py` (system + .venv + --live). | 2026-06-10 |
| data/news/ | REAL US-federal gov-enforcement articles (DOJ/OFAC, verbatim 17 USC §105) `articles/*.md` + grounded `derived/*.json` + SYNTHETIC `book.json` (exact + near-matches + a common-name trap; non-negotiable #4). Phase 42 added the committed SYNTHETIC demo seed `docs/demo-investigation-note.md`. | 2026-06-10 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored. Phase 25 added a natural `red_flag` per indicator. **Phase 27** tightened 121 over-long verbatim `flag`s to crisp contiguous SUB-SPANS (faithfulness-guarded; 36 long-but-faithful kept whole) across 33 derived records + re-translated fin-2022-a001's 2 red_flags — byte-surgical (flag/red_flag VALUES only); mds + every corpus-status.json + the grounding core byte-frozen | 2026-06-07 |
| docs/program-blueprint.md + specs/phase-47-*.md + specs/phase-48-*.md | M9 design deliverables: the program blueprint §1–§15 (Phase 47 §1–§12: universal grounding spine, 6-workload substrate/verifier table [6th row Phase 48], G/M/J/A gate taxonomy, dual-stream human charter, probe-rule agentification, E-23×SR-11-7 control mapping, honesty disposition table, §11 capability roadmap; Phase 48 §12–§15: history-utilization [3 roles w/ substrate+verifier, "history is evidence, never ground truth"], LFCM library-not-monolith, continuous adjudication loop [params chosen-not-measured], probe citation) + the two spec contracts | 2026-06-12 |
| scripts/corpus_redundancy.py + data/corpus-redundancy/equiv-sample.json + docs/corpus-redundancy-report.md | **Phase 51 (2026-06-16)** — the measurement workstream's first MEASURED result (the §13 fm-1 frontier). NON-ship, stdlib, read-only over the frozen corpus. T1 deterministic cross-regulator co-occurrence UPPER BOUND (≤0.325, over the corpus's own typology/capability/agency labels) + T2 sampled consensus-class semantic-equivalence (24 single-rater blind judgments, integrity-verified; est genuine ~1.4% strict / ~11.5% band) + the non-ship report. `--selftest` GREEN; `--check all` 7/7 zero drift. FINDING: the corpus's no-dedup stance empirically vindicated | 2026-06-16 |
| docs/blueprint-sales-pitch.md + docs/governance-throughput-model.md | Program GTM + sustainability artifacts (2026-06-16, post 4-angle adversarial critique). Pitch v2: structural-disease→architectural-cure, honesty weaponized, the "what we do not claim yet" list (composition detection-lift now RETIRED per the triple-null). Throughput model: the G/M/J/A taxonomy AS the cost model (~2 FTE@5k / ~4–5@10k, conditional on three measurable params) — answers the per-signal-as-a-model lifecycle-burden concern with arithmetic, not assertion. NON-ship, honesty-disciplined | 2026-06-16 |
| docs/pillar-integration-contract.md | Pillar 1→Pillar 2 cross-pillar data contract (v0.1 DRAFT, 2026-06-16; references the FROZEN blueprint, not an edit). Grounded at aml-substrate HEAD 0daa3cc: the reference-by-path grounding spine (Alert→Dossier→STRRecord, replay-verified), the PROPOSED serialized record schemas + minted ids, the grounded NETWORK-STRUCTURE signal fix (composition's 2nd axis), honesty caveats (case-grouping oracle-only, derived ids, regenerate), the persist-in-Pillar-1 seam, what Pillar 2 owns (narrative + completeness flip + the verifiers), §7 open ratification questions | 2026-06-16 |
| data/probe-history/ + scripts/probe_history_stats.py + docs/probe-history.md + docs/blueprint-report.html | Phase 48 NON-ship artifacts: fully SYNTHETIC advisory-shaped legacy rulebook (12 rules) + 44-alert disposition history + derived record 12/12 gate-green through the BYTE-UNCHANGED gate (outside every build.py-read path); stdlib stats script (9 definition-carrying metrics, no selftest yet); shape-caveat writeup; 87.8KB self-contained offline DESIGN-labeled blueprint report (booth precedent — no build.py target, system-flow + grounding-chain SVG centerpieces) | 2026-06-12 |
| console.html + dist/console/index.html + data/console/cases.json + scripts/curate_console_cases.py | The 4th ship artifact (Phase 47): gate console — 213 REAL C/D adjudication cases (Phase-34 correction, byte-identity verified; 212 FINTRAC attributed, 1 US null) through Queue → Evidence → Disposition (graded + required rationale) → Record reveal (213/213 build-verified) → session-only Ledger; curate script = deterministic regeneration-only | 2026-06-12 |
| triage.html + dist/triage/index.html + data/triage/scenarios.json + scripts/curate_triage_scenarios.py | The 5th ship artifact (Phase 49, NEW — blueprint §14's continuous adjudication loop made demo-able): 20 SYNTHETIC scenarios (16 + 4 controls, 4 strata 10/3/3/4, panels shared BY REFERENCE, 6 labeled second-rater seeds, rule text embedded) deterministically curated from data/probe-history at AUTHORING time only; arc Queue → Evidence → Disposition (§14 grammar, rationale REQUIRED) → Reveal (decisions-not-correctness) → Discovery ledger (render-computed agreement w/ measurement definitions; JSON export; persists nothing); 96,686 B offline | 2026-06-12 |
| merge.html + dist/merge/index.html + data/merge/cases.json + scripts/curate_merge_cases.py | The **6th ship artifact (Phase 76 — the blueprint's Class-J merge-adjudication console)**: the human gate over entity-resolution candidate links. **Phase 79 SUPERSEDED the consensus-66 with 29 substrate-anchored SCORED cases:** BOTH populations now scored, split by oracle PROVENANCE (substrate-scored via the non-circular `GT-<hash>` anchored oracle, `entity_ref ≠ cluster`; synthetic-scored via `true_entities`) — the consensus-no-oracle framing retired (substrate data was never production-real; it lacked an oracle until the anchored fork). The substrate population scores the DEMOTED spine → refuses all 29 → the oracle discriminates (13 fragmentation-gap uphold / 16 correct-rejection reject; constant-verdict limitation stated in-console); only the synthetic-13 spans all four quadrants. Arc Queue (by basis) → Evidence (NEUTRAL — no truth pre-adjudication) → Adjudication (rationale REQUIRED) → Verdict (latent-truth oracle + match indicator + synthetic-substrate-anchored qualifier) → Ledger. `curate_merge_cases.py` (`enumerate_substrate_scored`) = companion authoring tool (reuses entity_spine + resolution_scorer; NOT imported by build.py); `validate_merge_cases` mirrors it in EXACT parity + a masking firewall; ~91 KB offline. dist/merge RE-FROZEN. | 2026-06-27 |
| scripts/{merge_adjudicator,serve_merge}.py + tests/merge_adjudicator_quality_harness.py + tests/fixtures/merge-adjudicator/ + the merge.html `/*LIVE_*/` overlay | **Phase 83 NEW — the agentification roadmap's Stage 1, the 5th companion LIVE loop (companion-only; NOT a ship/dist target; build.py imports none).** `merge_adjudicator.py` = StubAdjudicator (echo `spine_verdict`→uphold/reject; the dep-free two-sided 33/66 baseline) + LiveAdjudicator (over `osint_tools.call_openai`→`127.0.0.1:8080`, `parse_llm_json` fail-closed, the 4-way closed vocab + rationale) + the oracle FIREWALL (`adjudicator_input()` strip + `assert_no_oracle_leak()` on `_TRUTH_LEAK_KEYS`, mirroring `resolution_scorer.assert_no_cluster_leak`) + `score_adjudications()` (counts by quadrant/klass + provenance; both_defensible/escalate counted DEFERRED, NOT scored; synthetic qualifier); `--selftest` dep-free. `serve_merge.py` = the companion server (stdlib, 127.0.0.1, port 8040, the `serve_corpus.py` pattern); `/adjudicate` proxies the adjudicator (stub-offline / agent-on-:8080 / degrade-to-stub + a named "no model" note); the oracle NEVER in the pre-disposition payload. `merge_adjudicator_quality_harness.py` = `--check` (dep-free replay + the stub baseline) / `--freeze` (one live capture); in `tests/test_selftests.py`. **THE MEASURED HEADLINE** (a model WAS on :8080): the live agent matched the oracle on **54 of 66** vs the stub's **33** (21 of the 33 the spine got wrong recovered: 18/30 fragmentation-gaps + all 3 over-merge-traps); 0 deferrals; pinned to `tests/fixtures/merge-adjudicator/adjudicator.replay.json` (`live_expected.calls = {reject_as_shares:45, uphold_merge:21}`). The `merge.html` `/*LIVE_*/` overlay (build-stripped → `dist/merge` byte-identical) installs by WRAPPING `render()` in-region + injecting `<style>` via JS; doc `docs/merge-live.md`. | 2026-06-29 |
| scripts/determination_proposer.py + tests/determination_proposer_quality_harness.py + tests/fixtures/determination-proposer/ + the serve_workbench `/propose-determination` route + the workbench.html pre-proposer panel | **Phase 85 NEW — the agentification roadmap's Stage 2, the 6th companion LIVE loop + the SECOND oracle-scored agent (companion-only; NOT a ship/dist target; build.py imports none).** `determination_proposer.py` = StubProposer (echo the engine verdict — the dep-free baseline) + LiveProposer (over an openai-compatible model on `127.0.0.1:8080`, proposes `file`/`cleared`/`needs-more-info` + rationale from the §12 bundle evidence ONLY) + the oracle FIREWALL (`proposer_input()` strip + `assert_no_oracle_leak()`, REUSED from the Phase-78 `determination_validation_harness`) + the two-sided/abstention scorer; the agent is a THIN layer over the Phase-78 harness (reuses its proposer_input/classify/CAPTURE); proposes per cap-signature so **46 signatures cover all 6935 cases** (full population, no sampling); `--selftest` dep-free. `determination_proposer_quality_harness.py` = `--check` (dep-free replay-by-signature + the stub baseline) / `--freeze` (one live capture); registered in `tests/test_selftests.py` (the uv-run-pytest umbrella, +2). `serve_workbench.py` gained the single-flight `/propose-determination` route (no oracle on the wire; stub/live/degrade; the inline transport-error fix mirrors `_gather`). `workbench.html` gained the pre-proposer panel beside the UNCHANGED human gate ("proposed, not decided" + the synthetic qualifier; `workbench.test.mjs` 195→205). **THE MEASURED HEADLINE** (a model WAS on :8080, base-rate-informed prompt; counts-only, synthetic substrate slice): the agent eliminated all **727** KYC over-flags + committed `file` on **74** (engine **50**) but OVER-files the volume ML class **4482** (engine **593**) — a sensitivity TRADE, not clean precision-recovery; vindicates propose→gate→decide. `evidence_requirements.py` BYTE-UNCHANGED; all 9 dists byte-frozen; doc `docs/determination-live.md`. | 2026-06-29 |
| tests/drafter_quality_harness.py + tests/fixtures/drafter-quality/drafter.replay.json + docs/drafter-live.md | **Phase 86 NEW — the agentification roadmap's Stage 3, the STR drafter behind the verifiers, consistency-measured (companion-only; NOT a ship/dist target; build.py imports none).** The state-loader found the STR drafter + its six Class-G verifiers ALREADY SHIPPED in Phase 57 (the Drafter Protocol, the `--drafter {stub,claude,openai,opencode}` switch); the drafter has NO correctness oracle (free-text drafting) → the deliverable is the MEASUREMENT FRAME, the GATHER consistency-not-correctness class. `drafter_quality_harness.py` = a pure dep-free `score_drafts()` scorer (counts-only: stub-vs-live SIGN/REFUSE + fabrication-guard CATCH from `blocking_violations` + grounding CONSISTENCY; NEVER accuracy/catch-rate/precision/recall) + `--check` (replay the pinned subprocess-result capture through the scorer, dep-free, no casework subprocess, no model; registered in `tests/test_selftests.py`, uv-run-pytest 32→34) + `--freeze` (run `serve_chain.casework_consume` stub+openai per casefile bundle; refuse to freeze a regression). NO oracle firewall needed (no truth to hide — simpler than Stages 1/2). **THE MEASURED HEADLINE** (a model WAS on :8080; counts-only, small-synthetic-qualified): over 4 designed bundles the live agent matched the stub **4/4** — signed the same 3 (verifier-acceptable narratives) + fail-closed on the same 1 (the narrative-seam `CASE-P-0025128` → casework `needs_more_info`), 0 fabrications caught + 0 recoveries; the GATE-BOUNDED tie (the gate, not the drafter, determines defensibility — vindicates propose→gate→decide from the drafting side). `vendor/aml-casework` + `evidence_requirements.py` git-unchanged; all 9 dists byte-frozen; `workbench.test.mjs` 205→206; doc `docs/drafter-live.md`. | 2026-06-29 |
| tests/corpus-explorer.test.mjs + tests/news-stream.test.mjs + tests/gate-console.test.mjs + tests/triage-console.test.mjs + tests/merge-console.test.mjs | Zero-dep Node DOM-shim harnesses — corpus 303, news-stream 150, gate-console 68, triage-console 93, merge-console ~100 (Phase 83 — the offline-strip + live-branch + client-firewall additions, was 73). Per-phase growth history: the journal entries. |
| workbench.html + scripts/{serve_workbench,serve_chain,curate_workbench_cases,evidence_requirements,osint_tools}.py + data/{workbench,casefile,osint}/ | The COMPANION investigator workbench (NOT a ship/dist target; build.py imports none of it). The live arc CLUTTER→SIGNALS→GATHER→DETERMINATION→DECIDE over a vendored substrate slice (`data/workbench/`, **376 cases @ substrate 294d3e5**, coverage **256/376** post casework 04cc335). **Phase 85 — the §12 determination PRE-PROPOSER panel** (companion-only, all 9 dists byte-frozen, `evidence_requirements.py` UNCHANGED): `serve_workbench.py` gained the `/propose-determination` route + `workbench.html` a pre-proposer panel beside the UNCHANGED human gate (the agent PROPOSES, the engine LICENSES, the human DECIDES); `tests/workbench.test.mjs` 195→205. See the `determination_proposer.py` row. **Phase 84 — RICH RENDER AT SCALE (companion-only, all 9 dists byte-frozen, `serve_workbench.py`/`evidence_requirements.py` UNCHANGED):** the slice render now surfaces the EMITTED identity the bundles already carried (the gap was a STALE RENDER PATH in `workbench.html`, not an emit/curate gap) — the ledger + counterparty chips read `counterparty_name` (+country, em-dash code-fallback on CASH/internal legs); `boGraphHTML` reads `display_name`; NEW CLIENT-SIDE adapter `sliceShowcaseDetail`+`sliceNetworks` REUSES the northstar sc* graph builders (`scMoneyFlowGraph`+`scResolutionGraph`) with an explicit "no cross-account ER implied" honesty note (substrate counterparties are per-account-local) + a "multi-hop ownership chain pending substrate emission" BO degradation marker; new `.cpcc` CSS. The DISCOVERY: the adapter went client-side rather than the planned server-side `showcaseSurface` route (which would have stripped the §12 surface). `tests/workbench.test.mjs` 184→195. DECISIVENESS (slice cases FILE/CLEAR) stays substrate-gated (Ask #3 measured-null / Ask #4 CLI-null). **Phase 82 — the §12 loop closes from GROUNDED decision evidence:** `serve_workbench.determine_case` (`_bundle_evidence`) READS the P39 prior-STR predicate (`named_predicate_risk` via a `flagged` resolution edge) + the P40 affirmative mitigation (`mitigation_evidence`) from each bundle — read-from-a-record DATA, NOT a rule edit (the engine already exposes the params at `evidence_requirements.py` line 310-312, BYTE-UNCHANGED, A1) → the slice resolves **1 KYC-integrity determination** (31 over the full 23,651-customer population) + **17 ML affirmative `cleared`**, was 0/0; `workbench.html` `paintDet` gained the grounded-evidence panel + the `.detv.clear` branch; `curate_workbench_cases` SUBSTRATE_HEAD fc98b09→294d3e5 + the flagged-edge validator + an honest CONTRACT-boundary-vs-NARRATIVE-SEAM `e2e_note`. casework re-vendored 076fb8e→**04cc335** (P20 C15/C4 reconcile, coverage 128→256); the kyc-sign 2→1 honest relaxation (a txn-bearing C14 MAY fail-closed at the narrative seam; ≥1 must sign preserved). The ML FILE loop stays blocked on substrate Ask #3 (a second corroborating leg as a fired signal). `tests/workbench.test.mjs` 178→184. **Phase 77:** `serve_workbench`/`serve_chain` gained `--disposition` (file\|cleared) plumbing on the casework DECIDE subprocess + `cleared_demo_consume` + `CLEARED_DEMO_BUNDLE` (a casework-gated `cleared` check in `--selftest`); NEW companion data `data/casefile/cleared-demo.bundle.json` = a SYNTHETIC C5 cash-placement bundle (exculpatory:true txn + grounded exculpatory claim, NO crime_type/inculpatory, grounded on vendored `fin-2023-alert001:IND-08`) → casework SIGNS `cleared` end-to-end (the casework file/determination bar BYTE-UNCHANGED; `evidence_requirements.py` byte-unchanged). The north-star Lakeshore CASE-B fails-closed at casework's fan-OUT-only C3. **Phase 75:** `curate_workbench_cases.py` SUBSTRATE_HEAD f15c241→fc98b09 (v0.4/v0.5 additive read; `validate_v05_bundle` wired into `validate()`; a disclosed cross-case CO-REFERENCE selection pass → 36 real co-references; casework gets a relabeled v0.3 VIEW of the v0.5 bundle); `serve_workbench.py` gained `_observe_substrate_party` + `substrate_memory` + a `/memory` route; `workbench.html` gained the "Persistent entity intelligence" panel (36 co-references + 66 over-merge-refused + the casefile short-circuit). **Phase 79:** casework re-vendored b3546d4→**076fb8e** (Phase 19 `_c3_fan_in`); Lakeshore CASE-B now SIGNS `cleared` end-to-end via fan-in C3 (NEW `data/casefile/case-b.bundle.json` — window-compressed ≤7d, IND-02-grounded + `serve_workbench.lakeshore_cosign_consume`) → the matched pair BOTH route through casework; `evidence_requirements.py` BYTE-UNCHANGED (A1). **Phase 73:** `data/casefile/{case.json,schema.md}` — the AUTHORED north-star matched FILE/DISMISS pair (Northgate files / Lakeshore clears; the artifact-is-the-spec, substrate/casework parked); `evidence_requirements.py` gained the affirmative-`cleared` verdict + a `read`-from-file evidence source + predicate-from-register (**the file/determination bar BYTE-UNCHANGED — the A1 guard**); `serve_workbench.py` gained the casefile path (the pair LEADS the queue; verdicts are LIVE-ENGINE OUTPUT, the authored `expected_*` a regression oracle via a fixture-drift bridge); `workbench.html` gained `showcaseSurface` (3 graphs). **Phase 74:** `serve_workbench.py` gained the grade-gated read path (`_cf_read_manifest` + per-decision `read_manifest`; strong/weak ADMITS, reject/empty QUARANTINES — excluded, never down-weighted), the genuine gitignored DuckDB write seam + `casefile_memory` (the re-surfacing CASE-C measured short-circuit + the stale-prior guard); `data/casefile/case.json` gained the `resurfacing` block; `evidence_requirements.py` BYTE-UNCHANGED (the file bar held). Tests: `tests/workbench.test.mjs` (195, Phase 84) + `tests/fixtures/casefile/*`. | 2026-06-29 |
| `/sanctions-c17-exposure` route (`scripts/serve_workbench.py`) + `sanctionsC17PanelHTML` (`workbench.html`) + `data/casefile/sanctions-c17-exposure-demo.bundle.json` | **Phase 81 — the C17 exposure-via-ownership OBSERVABLE** (companion-only; NO dist/engine change). The planned §12 evidence-advance was DEGENERATE (the 13 sanctioned-BO cohort carries only C8/C14, no ML mechanism → DELTA=0 to the determination bar) → per USER OVERRIDE the C17 consume ships observable-only: the route + panel surface the sanctioned-BO exposure and the LIVE engine SHOWS the case does NOT reach the bar (the §12 discovery feed classes all 13 as over-flag — a defensive-exposure basis, not a latent-laundering discriminator; `sanctions_flag` is label-blind, corr≈0 by design). `evidence_requirements.py` BYTE-UNCHANGED (A1); demo bundle `resolution_edges` emails masked to example.test; the panel honesty-word-banned ("N pct" not "N%"). `workbench.test.mjs` 167→178 (the C17 block). | 2026-06-28 |
| scripts/{entity_spine,resolution_scorer,measure_xcase_overlap}.py + data/entity-spine/ | **Phase 74 NEW + Phase 75** — the COMPANION persistent entity intelligence spine (NOT a ship/dist target; build.py imports none; the M8 `news_store` byte-untouched). `entity_spine.py` = the pillar-neutral spine: observations / append-only bitemporal `resolution_links` / `persistent_entities`; deterministic strong/weak/REJECT linkage (name-only REJECTED; 2+ distinct strong → refuse-as-ambiguous); reversible `retract_link` (version bump + cascade-invalidation "re-decision required", audit row preserved); conflicting-values-both-kept; event-driven stale-prior; fail-closed-to-weakest; the directional no-news-import firewall. **Phase 75 added** `entity_ref` to `STRONG_KINDS` (the declared-identity strong key — substrate email/phone DEMOTED to weak candidate-SHARES, never a merge key) + a pillar-neutral `entities_in_multiple_records` cross-case query (deterministic stable sort). `resolution_scorer.py` = pairwise P/R + B-cubed over synthetic `data/entity-spine/true_entities.json` (committed) behind a schema-boundary resolver-input firewall (no cluster field; a 1:1 surrogate REJECTED), every number synthetic-only-qualified; cites aml-substrate `resolve/measure.py`. **`measure_xcase_overlap.py` (Phase 75 NEW)** = the measure-first gate — re-emits substrate v0.5 (seed 0) and measures entity_ref re-surfacing (real co-reference) vs shared-strong-id across distinct entity_refs (the over-merge trap); `--selftest`. **Phase 79 NEW** `data/entity-spine/substrate-anchored-slice.json` = the committed no-substrate-replayable merge oracle capture (32 candidate-relevant obs + the non-circular `GT-<hash>` cluster truth; emails masked to example.test, email/phone demoted to weak) — `resolution_scorer` scores it (demoted spine, 29 candidate pairs, two-sided 13/16) → feeds `curate_merge_cases.enumerate_substrate_scored`. `data/entity-spine/store/` gitignored. All `--selftest` under .venv (in PY_SELFTESTS). | 2026-06-25 |

## Session Journal (last 5)

- [2026-06-29] [[2026-06-29-phase-86-str-drafter-consistency-measure|Phase 86 — Agentification Stage 3: the STR drafter behind the verifiers, consistency-measured (the gate-bounded 4/4 tie is the finding) (standard, companion-only, planned+delivered same session)]] — built the roadmap's Stage 3. The state-loader surfaced PRE-GATE that the STR drafter + its six Class-G verifiers ALREADY SHIPPED in Phase 57 + the drafter has NO correctness oracle (free-text drafting) → the deliverable was reshaped to the MEASUREMENT FRAME: `tests/drafter_quality_harness.py` (the GATHER consistency-not-correctness pattern — a pure dep-free `score_drafts()` scorer [counts-only stub-vs-live SIGN/REFUSE + fabrication-guard CATCH + grounding CONSISTENCY] + `--check` replay + `--freeze`). NO oracle firewall needed (no truth to hide — simpler than Stages 1/2). **EXECUTE ONCE landed for real (a model WAS on :8080) — and the measure is the gate-bounded 4/4 TIE:** over 4 designed casefile bundles the live agent matched the deterministic stub **4/4** — signed the SAME 3 (verifier-acceptable narratives), fail-closed on the SAME 1 (the narrative-seam `CASE-P-0025128` → casework `needs_more_info`), **0 fabrications caught + 0 recoveries**; it did NOT hallucinate a narrative to force a file. THE FINDING: the drafter measure is consistency-BOUNDED by the gate — the six verifiers refuse anything ungrounded → a competent agent drafter and the stub CONVERGE at the gate → the GATE (not the drafter) determines defensibility. A tie is a real result; it vindicates propose→gate→decide from the DRAFTING side, mirroring Stage 2's vindication from the determination side. We deliberately did NOT contrive a deliberately-ungrounded adversarial case to force `caught>0` (honesty over drama — the scripted fail-closed beat already demos the guard; that case is a named follow-on). Companion-only — all 9 dists byte-frozen (`--check all` 9/9), `vendor/aml-casework` + `evidence_requirements.py` git-UNCHANGED, the 256/376 §12 funnel untouched, build.py imports nothing new; `workbench.test.mjs` 205→206, `uv run pytest` 32→34 (the harness `--selftest` + `--check`). Pre-delivery adversarial review: 2 MINORs FIXED inline (an over-attributed seam-case refusal cause — the docs now state only what the capture proves: the bundle has 36 txns; "lacks transaction_details" misread casework's OUTPUT completeness flag as missing INPUT data). Escape hatches: DISCOVERY (planning — the drafter shipped Phase 57 → reshape to the measurement frame) + DISCOVERY (impl — the 2 review MINORs). Ledger Phase-86 all 5 rows HELD (A1 held with the honest nuance: the population WAS non-degenerate at the population level but the live-vs-stub CONTRAST resolved to a tie — the tie reported straight). Decisions: [[phase-86-gate-bounded-drafter-tie]] (high, NEW) · [[phase-86-no-oracle-consistency-measure]] · [[phase-86-stub-vs-live-narrative-seam-contrast]] · [[phase-86-companion-only-casework-unchanged]] · [[phase-86-direction-str-drafter-measurement]]. **LESSON:** when an agent's output passes through a strong deterministic verifier gate before it counts, the GATE — not the agent — determines defensibility; a stub-vs-live TIE is a REAL result, and the consistency-not-correctness measure (counts-only, no gold-narrative oracle) is the right frame for free-text output with no reference oracle. `tests/drafter_quality_harness.py` (NEW) · `tests/fixtures/drafter-quality/drafter.replay.json` (NEW) · `tests/test_selftests.py` · `tests/workbench.test.mjs` · `docs/drafter-live.md` (NEW) · `docs/agentification-roadmap.md` · `CLAUDE.md` · `HANDOFF.md`. `vendor/aml-casework` + `evidence_requirements.py` + all 9 dists UNCHANGED.
- [2026-06-29] [[2026-06-29-phase-85-determination-pre-proposer|Phase 85 — Agentification Stage 2: the §12 determination pre-proposer, oracle-scored (the 6th companion live loop; the A1 over-flag prediction was contradicted + re-measured honestly) (standard, planned+delivered same session)]] — built the roadmap's Stage 2: a THIN companion agent (`scripts/determination_proposer.py`, a layer over the Phase-78 validation harness) PROPOSES `file`/`cleared`/`needs-more-info` + rationale from the §12 bundle evidence ONLY (the oracle FIREWALL: `proposer_input` strip + `assert_no_oracle_leak`, REUSED; the served `/propose-determination` carries no oracle on the wire), per cap-signature so **46 signatures cover all 6935 cases** (full population, no sampling). **T5 EXECUTE ONCE landed for real (a model WAS on :8080) — and the measurement CONTRADICTED the A1 prediction:** the agent corrected the structural KYC over-flag (abstained on all **727**) + recovered MORE files (**74** vs the engine's **50**), BUT OVER-files the volume ML class (**4482** vs the engine's **593**) — it files the dominant `C2|C3|C8` signature (4040 cases, 4029 benign) reasoning from per-case red-flag CO-OCCURRENCE, blind to the POPULATION base rate the calibrated rule encodes. The orchestrator SURFACED the contradiction mid-build (not silent); the user's call = add publicly-grounded base-rate context + re-measure ONCE (NOT iterated-to-fit; the over-file persisted). The honest two-sided finding (a sensitivity TRADE) VINDICATES propose→gate→decide: a sensitivity-rich PROPOSER, the deterministic engine + the human gate supplying the population-calibrated discipline. Companion-only — all 9 ship dists byte-frozen (`--check all` 9/9), `evidence_requirements.py` BYTE-UNCHANGED (git-diff empty), build.py imports nothing new (grep clean), the 256/376 funnel unchanged; `workbench.test.mjs` 195→205, uv-run-pytest +2 (the proposer `--selftest` + the harness `--check`). Pre-push adversarial review (6 dims): 0 blockers / 0 majors / 1 nit FIXED inline (the live-transport error path on `/propose-determination` now returns a named error, mirroring `_gather`). Escape hatches: USER OVERRIDE (the base-rate re-measure) + DISCOVERY (the review nit). Ledger Phase-85 REVISITED — A1 held-in-spirit/prediction-corrected; A2–A5 HELD. Decisions: [[phase-85-direction-determination-pre-proposer]] · [[phase-85-oracle-firewall-non-circular]] · [[phase-85-propose-gate-decide-a1-frozen]] · [[phase-85-over-flag-headroom-not-miss-recovery]] (corrected-at-impl) · [[phase-85-base-rate-remeasure-not-overfit]] (NEW). **LESSON:** when a live agent measurement contradicts a planning prediction, SURFACE it, make at most ONE publicly-grounded revision, re-measure, report the honest outcome — don't grind the prompt against the single synthetic oracle. `scripts/determination_proposer.py` (NEW) · `tests/determination_proposer_quality_harness.py` (NEW) · `scripts/serve_workbench.py` · `workbench.html` · `tests/workbench.test.mjs` · `tests/{test_selftests.py,fixtures/determination-proposer/**}` · `docs/{determination-live.md (NEW),agentification-roadmap.md}` · `CLAUDE.md` · `HANDOFF.md`. `evidence_requirements.py` + all 9 dists UNCHANGED.
- [2026-06-29] [[2026-06-29-phase-84-rich-render-at-scale|Phase 84 — Rich render at scale: surface the workbench slice cases' emitted identity/network + re-sharpen the substrate decisiveness handoff (standard, planned+delivered same session)]] — closed a signal-watch-side RENDER DROP in the companion investigator workbench. The user's complaint ("no real counterparty names; no case quality matches the northstar") was DIAGNOSED via parallel code-verification of BOTH pillars as a STALE RENDER PATH in `workbench.html` — NOT a substrate emit gap, NOT a curation drop: the committed slice bundles ALREADY carry `counterparty_name` (372/376 ≈89% of legs), `counterparty_country`, `related_parties[].display_name` (56/56), `ownership_pct`; `curate_workbench_cases.py` carries them whole, `serve_workbench.py` delivers verbatim — only the slice render fns read the CODE (`txTable`:580 / `counterpartySummary`:556 → `counterparty_ref`; `boGraphHTML`:733/752 → `party_id`). **The T1 probe resolved GO with a DISCOVERY that REFINED the design:** the planned SERVER-SIDE adapter (route slice cases through the northstar `showcaseSurface`) went CLIENT-SIDE instead (`sliceShowcaseDetail`/`sliceNetworks` in `workbench.html`) — the existing slice surface is already a rich §12 investigation surface (risk→KYC→counterparties→BO→audit-walk→gather→determine→decide), so `showcaseSurface` would have STRIPPED it; the client adapter REUSES the sc* graph builders (`scMoneyFlowGraph`+`scResolutionGraph`) in place, fixes the 3 coded renderers, and leaves `serve_workbench.py` UNTOUCHED (the bundle is delivered whole). Rendered: named ledger (NAME + country, em-dash code-fallback on CASH/internal legs), the reused money-flow + resolution panels, named BO graph (`display_name`), all with an explicit "no cross-account entity resolution implied" honesty note (per-account-local synthetic names → the >90%-FP ER discipline applied to RENDER) + a "multi-hop ownership chain pending substrate emission" degradation marker. Re-sharpened the substrate handoff for the DECISIVE half (substrate-gated): `docs/substrate-northstar-evidence-emission-PLAN-BRIEF.md` re-grounded to HEAD `3716f77` (Ask #3 = 2nd-leg Phase-41 measured-null; Ask #4 = `ownership_edges` CLI-null) + the stale SUB-1 "bare codes" claim in `docs/rich-case-target-contract.md` trued. Companion-only; all 9 dists byte-frozen (`--check all` 9/9), `evidence_requirements.py` byte-unchanged (git-diff empty), build.py imports nothing new, the 256/376 signing funnel unchanged; `tests/workbench.test.mjs` 184→195 (+11 P84 assertions); `serve_workbench --selftest` PASS (378 cases). Escape hatch: DISCOVERY (client-side adapter, a within-scope refinement — smaller + safer, preserves the §12 surface). Ledger Phase-84 all 4 rows HELD; none bit. Decisions: [[phase-84-render-drop-not-emit-gap]] (high) · [[phase-84-adapter-gated-by-probe]] (medium→high — the client-side DISCOVERY) · [[phase-84-decisiveness-substrate-gated]] (high) · [[phase-84-names-honest-without-implied-ER]] (high). **LESSON:** diagnose the FULL pipeline (emit→curate→serve→render) before blaming upstream — a "missing data" complaint was a render lag behind what the bundles carried; and reuse the BUILDERS, not the whole surface (the existing surface may already do more than the one you'd route to). `workbench.html` · `tests/workbench.test.mjs` · `docs/substrate-northstar-evidence-emission-PLAN-BRIEF.md` · `docs/rich-case-target-contract.md` · `CLAUDE.md` · `HANDOFF.md`. `serve_workbench.py` + `evidence_requirements.py` + all 9 dists UNCHANGED.
- [2026-06-29] [[2026-06-29-phase-83-merge-adjudicator-stage1|Phase 83 — Agentification Stage 1: the merge adjudicator agent, oracle-scored (the 5th companion live loop; agent 54/66 vs spine 33) (standard, planned+delivered same session)]] — built the agentification roadmap's Stage 1 — the FIRST measurable agent + the 5th companion LIVE loop. `scripts/merge_adjudicator.py` (StubAdjudicator echo `spine_verdict` + LiveAdjudicator over `osint_tools.call_openai`→`127.0.0.1:8080`) proposes one of `{uphold_merge, reject_as_shares, both_defensible, escalate}` + rationale per merge case, MEASURED against the committed non-circular `GT-<hash>` oracle in `data/merge/cases.json` (the ONE gate with a correctness oracle). **THE FIREWALL:** `adjudicator_input()` strips to the evidence surface + `assert_no_oracle_leak()` raises on any `_TRUTH_LEAK_KEYS` field (mirroring `resolution_scorer.assert_no_cluster_leak`); the served `/adjudicate` carries no oracle pre-disposition. Surfaced as `scripts/serve_merge.py` (port 8040, stub-offline / agent-on-:8080 / degrade-to-stub) + a build-stripped `/*LIVE_*/` overlay in `merge.html` → `dist/merge` BYTE-IDENTICAL (`--check merge`). **T7 EXECUTE ONCE landed for real (a model WAS on :8080 → the live capture landed at T2 `--freeze`, folding T7 forward):** the agent matched the oracle on **54 of 66** scored cases vs the StubAdjudicator baseline's **33** — recovering 21 of the 33 the spine got wrong (18 of 30 fragmentation-gaps + all 3 over-merge-traps); **0 deferrals**; counts-only, by quadrant + provenance, synthetic-substrate-qualified, no catch-rate/lift/precision/recall. **Two build-strip byte-identity guards surfaced + fixed at T4:** the overlay installs by WRAPPING the reassignable `render()` declaration in-region (static live CSS/HTML survives the strip; `CASE_SCREENS` holds stale references → wrap the call site, not the array); the LIVE region must carry NO trailing blank line (`LIVE_REGION_RE` eats the leading newline → a trailing blank = a +1-newline drift, caught by `--check merge`). Companion-only; build.py imports nothing new (grep clean); `evidence_requirements.py` untouched (this is merge, not §12); all 9 dists byte-frozen (`--check all` 9/9); `uv run pytest` 27→30, `merge-console.test.mjs` ~76→100. Ledger Phase-83 all 5 rows HELD (NET a positive two-sided measured result). Decisions: [[phase-83-merge-adjudicator-stage1-frame]] · [[phase-83-companion-live-not-baked]] · [[phase-83-4way-vocab-count-deferrals]] · [[phase-83-measure-by-quadrant-two-sided-baseline]] (→high) + [[phase-83-live-overlay-via-render-wrap]] · [[phase-83-live-region-newline-discipline]] (NEW). **LESSON:** a thin LLM adjudicator reading the same evidence the deterministic resolver sees CAN out-judge it on the ambiguous quadrants the human gate exists for — the first oracle-scored agent number in the program (the agentification roadmap's credibility headline). `scripts/merge_adjudicator.py` (NEW) · `scripts/serve_merge.py` (NEW) · `tests/merge_adjudicator_quality_harness.py` (NEW) · `merge.html` · `scripts/build.py` · `tests/merge-console.test.mjs` · `tests/fixtures/merge-adjudicator/adjudicator.replay.json` (NEW) · `docs/merge-live.md` (NEW) · `docs/agentification-roadmap.md` · `CLAUDE.md` · `HANDOFF.md`. NO dist change (`dist/merge` byte-identical via the strip).
- [2026-06-29] [[2026-06-29-phase-82-consume-northstar-evidence-at-scale|Phase 82 — Consume sibling emissions: north-star evidence AT SCALE (§12 grounded-evidence loop closes; merge-org aborts again; casework re-vendor 128→256) (standard, planned+delivered same session)]] — consumed FOUR sibling emissions (substrate f7fbdb0→**294d3e5**: P38 org-fragment / P39 predicate / P40 mitigation; casework 076fb8e→**04cc335**: P20 C15/C4 reconcile) so the GENERATED 376-case slice carries north-star-quality determinations at scale. Every gated build ran a RIGOROUS-engine measure FIRST (the Phase-81 lesson). **T4/T5 (the keystone) landed:** consumed P39 predicate + P40 mitigation as bundle DATA read by `serve_workbench.determine_case` (`_bundle_evidence`), NOT a rule edit — the engine already exposes the params at `evidence_requirements.py` line 310-312 → the slice resolves **1 KYC-integrity determination** (31 over the full 23,651-customer population) + **17 ML affirmative `cleared`**, was 0/0; the human still adjudicates, the evidence is grounded-not-typed; `paintDet` gained the grounded-evidence panel + the `.detv.clear` branch (slice cases that affirmatively clear now render a documented-dismissal, was mis-rendered as needs-more-info). **T3 (merge-org) ABORTED again, sharper reason than Phase 81:** P38 DID build the org fork (364 O-FRAG fragments + 16 flag-intersected) but the fragments share NO resolution handle with their base (perturbed name + fresh corp#/address → 0 of 364 pairs share a unique id) → `candidate_pairs()` never proposes the base→fragment merge → 0 uphold → one-sided on signal-watch's OWN distill/scorer path (5 sanctions-touching candidates, all correct-rejection) → `dist/merge` BYTE-FROZEN, the org-fragment-emit brief sharpened (ask: retain a shared identifier); no fuzzy name-matching added (the wiki ER caveat). **T6 (casework re-vendor 04cc335):** coverage 128→256; the funnel re-measure was two-sided as A3 required (dominant move fail-close→sign) — surfaced ONE honest sign→fail-close at the C14 narrative-seam frontier (the new emit's CASE-P-0025128 makes casework's stub drafter fail narrative verification, "seam left open") → kyc-sign 2→1; the Phase-72 "always signs" honestly RELAXED, "≥1 must sign" preserved (CASE-P-0034054); NOT a code regression (signing is casework's grounding_replay, separate from determine_case; C14 grounding unchanged); curate now distinguishes a CONTRACT-boundary vs a NARRATIVE-SEAM rejection in the e2e_note. **Always-landed:** substrate re-pinned 294d3e5 (376 `data/workbench/bundles/*.json` re-emitted with the predicate + mitigation field families); the brief true-ups + cross-pillar; verify + CLAUDE.md trued in place. Companion-only; A1 held (`evidence_requirements.py` BYTE-UNCHANGED, `git diff --quiet`); `--check all` 9/9 (8 non-merge byte-frozen + `dist/merge` UNTOUCHED); build.py imports no spine/scorer/sibling/curate/casework (firewall clean); `workbench.test.mjs` 178→184, `uv run pytest` 27. Escape hatch: DISCOVERY (the txn-bearing-C14 narrative-seam — the Phase-72 "always signs" relaxed to accommodate it honestly). Decisions: [[phase-82-grounded-evidence-consume-path]] · [[phase-82-merge-org-abort-no-resolution-handle]] · [[phase-82-kyc-signs-honest-relaxation]]. **LESSON:** a §12 advance can rest on read-from-a-record GROUNDED evidence with the rule frozen (params-as-DATA); the ML FILE loop is the next sibling-rooted frontier (substrate Ask #3 — a second corroborating leg as a fired signal). `scripts/serve_workbench.py` · `scripts/curate_workbench_cases.py` · `workbench.html` · `data/workbench/bundles/*.json` (376 re-emitted) · `vendor/aml-casework/**` (→04cc335) · `tests/workbench.test.mjs` · `docs/*-PLAN-BRIEF.md` · `docs/cross-pillar-build-order.md` · `CLAUDE.md`. `data/merge/cases.json` + `dist/merge/**` UNTOUCHED; `scripts/evidence_requirements.py` BYTE-UNCHANGED.
## Cross-References

- HANDOFF.md · CLAUDE.md · README.md · docs/program-blueprint.md (M9 design source-of-truth, §1–§15)
- **Agentification track (the forward roadmap):** docs/agentification-roadmap.md (Stage 1 = the merge adjudicator → BUILT, agent 54/66 vs spine 33; Stage 2 = the §12 determination pre-proposer → BUILT (Phase 85, the 6th live loop): the agent corrected all 727 KYC over-flags + recovered files [74 vs 50] but over-files the volume ML class [4482 vs 593] — a sensitivity trade that vindicates propose→gate→decide; **Stage 3 = the STR drafter behind the verifiers → BUILT + MEASURED (Phase 86): the gate-bounded 4/4 tie — the live agent matched the deterministic stub 4/4 over the designed bundles (signed the same 3, fail-closed on the same narrative-seam case, 0 caught, 0 recovered); the GATE determines defensibility, not the drafter — vindicates propose→gate→decide from the DRAFTING side. Now two consistency-not-correctness harnesses (GATHER + the drafter)**; Stage 4 = the §14 triage second-rater, the named next leg) · docs/merge-live.md (the 5th live loop walkthrough) · docs/determination-live.md (the 6th live loop walkthrough) · docs/drafter-live.md (the Stage-3 consistency-measure walkthrough, the `docs/merge-live.md` pattern)
- docs/{case-workbench,evidence-driven-filing}.md (the companion workbench + the evidence-sufficiency control) ·
  docs/rich-case-target-contract.md (Phase 73 — the cross-pillar handoff: what substrate must EMIT + casework must SIGN/REFUSE/CLEAR; **Phase 84 trued the stale SUB-1 "bare codes" claim — counterparty names emitted since substrate Phase 27; SUB-1-remainder per-rail email/phone + SUB-4 cross-account counterparty resolution remain substrate-gated**)
- **Phase 84 — workbench rich render at scale (companion-only RENDER):** the slice render surfaces the emitted identity (names, money-flow + resolution graphs, named BO) via the client-side `sliceShowcaseDetail`/`sliceNetworks` adapter; the DECISIVE half (FILE/CLEAR) stays substrate-gated → docs/substrate-northstar-evidence-emission-PLAN-BRIEF.md (re-grounded to HEAD `3716f77`: Ask #3 = 2nd-leg measured-null, Ask #4 = `ownership_edges` CLI-null)
- **Phase 74 standards** (the cross-repo entity-intelligence contract): docs/{resolution-link-schema, identity-grade-grammar, confidence-as-provenance-contract, true-entities-scorer-contract}.md — one shared strong/weak/reject grade vocabulary
- **Sibling emission briefs** (RE-GROUNDED to verified live HEADs; sibling-executed): docs/{substrate-graded-counterparty-identifiers (aml-substrate@fc98b09 — LARGELY BUILT), substrate-exogenous-disposition-label (aml-substrate@fc98b09 — NOT BUILT), casework-confidence-graded-resolution (aml-casework@4a858e6 — NOT BUILT, graded-resolution still outstanding; `cleared` now BUILT @b3546d4 + CONSUMED via a C5 proxy), **substrate-true-entities-emission (Phase 76 — aml-substrate@fc98b09; emits slice-aligned `true_entities` so the merge console can SCORE the real 66)**}-PLAN-BRIEF.md
- **Phase 77/79 sibling handoff briefs** (Phase 79 ANNOTATED all three with resolving commits): docs/{**casework-c3-fan-in ✅ CLOSED** (`ed93a0d`/076fb8e — fan-IN C3 built + CONSUMED: Lakeshore CASE-B now co-signs `cleared`), **substrate-emit-cli-wiring ✅ RESOLVED** (`9677a37` + the `--anchored` fork — the disposition + anchored `entity_ref ≠ cluster` oracles are CLI-reachable), **substrate-open-reference-data-fork ◐ PARTLY LANDED** (Stage-1 `f9e63e7`; Stages 2/3 sanctions/FATF anchors + paid-landmine routing OPEN — the next merge-oracle realism frontier)}-PLAN-BRIEF.md · docs/cross-pillar-build-order.md (trued-up to casework 076fb8e/Phase 19, substrate f7fbdb0/Phase 37 + the Phase-81 consume note)
- **Phase 81 sibling handoff briefs** (the sanctions-arc consume's two measure-first abort destinations + the open-sanctions plan): docs/{**open-sanctions-data-fork** (NEW — the per-source license matrix: OFAC PD / UK HMT OGL = ship-clean vs the OpenSanctions CC-BY-NC consolidation = no-ship, the non-commercial boundary), **substrate-org-fragment-emit** (NEW — T1a's abort destination: substrate's anchored fragment overlay is PERSON-ONLY [0 org GT clusters], so the merge org-collision oracle is structurally one-sided; the ask is an org-fragment overlay so a flagged org can have a same-org fragment), **substrate-exposure-signal** (NEW — T1b's degeneracy frontier: `sanctions_flag` is label-blind [corr≈0], so C17 exposure is an OBSERVABLE not a determination leg; the ask is a discriminating, non-label-blind exposure SIGNAL)}-PLAN-BRIEF.md · docs/substrate-p35-determination-signals-PLAN-BRIEF.md (reconciled — TF + broader-C7 substrate-CUT; org-name DONE)
- docs/blueprint-report.html (Phase 48 — self-contained offline NON-ship blueprint report, DESIGN-labeled) ·
  docs/probe-history.md (Phase 48 — synthetic-history probe writeup + shape caveat)
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN, SR 11-7 + OSFI E-23 (the E-23 article NEW at Phase 47, primary-source).
  Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
