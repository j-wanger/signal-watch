ceremony: lite

# Notes
Small, single-artifact demo (one self-contained HTML file). The project's own
HANDOFF.md mandates "do not over-engineer" and optimizes for stage reliability.
Lite ceremony fits: streamlined flow, self-check as the quality gate, no subagent
reviewer dispatch. Revisit to `standard` only if a phase grows genuinely complex
(e.g. a real live-backend in M4).
