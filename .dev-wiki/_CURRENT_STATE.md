# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-06 by /dev-debrief (Phase 20 DELIVERED — all 6 tasks [x], exit criteria GREEN, READY FOR COMPLETION; delivery gate pending commit)

## Recommended Next Action

**Commit Phase 20, then flip the delivery gate.** The multi-source spine is delivered and accepted (unified
reviewer 9/10 ACCEPT, two MEDIUM both addressed) but NOT yet committed (commit-only-when-asked) — so the delivery
gate stays pending until the commit verifiably lands. After committing, mark `delivery=accepted` in tasks.md +
flip the active-phase.md delivery gate to `[x]`. **No remaining internal work without fabrication risk:** M0–M7 +
the corpus-explorer arc + durability + the multi-source spine are all complete; the demo is at Definition of Done.
The natural FOLLOW-ON (cheap, NOT a phase) is incremental derivation of the 11 derivable-but-not-yet-derived alerts
via the inverted loop (LLM extracts → `--check-derived` disposes) — no new architecture. The documented NEXT
SOURCE is OFAC (US-federal, also 17 USC §105), but adopting it requires extending the verbatim non-negotiable from
FinCEN-only to US-federal-public-domain — a compliance sign-off, flagged not taken. Run `/dev-plan` only for a
net-new stakeholder ask or once the OFAC sign-off lands; the carried showcase-debt true-up stays deferred (declined
at 5+ gates — see the Phase-20 retro). NEVER fabricate; the always-on "Illustrative data & outputs" badge + the
verbatim public-domain attribution stay.

## Active Phase

**[[phase-20-multi-source-spine|Phase 20: Multi-source spine, proven with FinCEN Alerts]]** (status: completed — all 6 tasks [x], exit criteria GREEN, delivered + accepted 2026-06-06; delivery gate pending commit)

Entry criteria: MET (Phase 19 complete + accepted; impl commit ab0739a — the corpus-explorer test harness is
committed + `_rf_triage` pinned; M0–M7 roadmap + the arc + durability all complete and the demo is at Definition
of Done). Direction approved scaling via OTHER FinCEN publication types (Alerts first) over other agencies (OFAC) /
a new showcase typology — the user reframed at the gate ("try FinCEN articles first, other than advisories").
Exit criteria: a thin SOURCES registry merges per-source `corpus-status.json` + `derived/*.json` into one
`__CORPUS__` with `data/fincen/` byte-frozen · acquisition retargets to the alerts hub → `data/fincen-alerts/`
index.json + ≥5 committed alert mds (checkpoint: convert one first, confirm a groundable rf_region) · ≥4 alert
records `--check-derived` clean via the inverted loop, any un-groundable alert honestly skipped · `corpus.html`
shows a unified advisories+alerts menu with honest Advisory/Alert type chips, the 5-screen arc unchanged per record ·
`--check all` zero drift, the harness extended for the multi-source menu + an alert walking the arc, the showcase +
`data/fincen/` byte-frozen · README + CLAUDE document the registry, Alerts as source #2, the unchanged gate, the
no-non-negotiable-change rationale, and OFAC as the next-source candidate.

Progress: 100% (all 6 tasks [x]; harness 40/40, --selftest PASS, --check all 4-artifact zero drift, frozen set git-diff-empty)

## Active Phase Contract

Phase: 20 - Multi-source spine, proven with FinCEN Alerts
Tasks: 6 (see tasks.md) — T1 (L) the multi-source SOURCES registry in build.py + derive_signals.py (merge by id;
gate UNCHANGED; data/fincen/ byte-frozen) · T2 (M) retarget acquisition to the FinCEN alerts hub + acquire/convert
the batch, with a convert-one-first rf_region CHECKPOINT · T3 (M) derive ~5 alerts via the inverted loop
(`--check-derived` clean; skip un-groundable honestly) · T4 (S) corpus.html unified menu + honest type chips · T5
(S) regen per-source status + rebuild + drift guard + extend the harness · T6 (S) docs.
Transition: continue (lite). UNFREEZE: `scripts/build.py`, `scripts/derive_signals.py`, `scripts/crawl_fincen.py`,
`scripts/acquire_fincen.py`, `data/fincen-alerts/**` (NEW source), `corpus.html`, `dist/corpus/index.html`,
`tests/**`, `README.md`, `CLAUDE.md`. FROZEN byte-untouched: `index.html`, `config/**`, the 3 typology dists, AND
`data/fincen/**` (the fincen-advisories source — corpus-status.json + the 14 mds + 12 derived stay byte-identical;
prove multi-source via the MERGE, not a migration). The quote-grounding gate (check_record/rf_region/normalize) is
reused UNCHANGED. NO non-negotiable change (FinCEN-only verbatim rail untouched).
Abort: T2 carries the CHECKPOINT — convert ONE alert, confirm rf_region not None before the batch; degrade = pivot
to FinCEN Financial Trend Analyses or derive fewer-but-deeper. If the registry can't merge without growing as
complex as a per-source migration (subtraction MOVES not shrinks), narrow to advisories-only + a registry stub.
NEVER fabricate. If blocked >3 attempts, ask user: skip or abort.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| Phase 20 = scale via OTHER FinCEN publication types (Alerts first), NOT other agencies (OFAC) and NOT a new showcase typology. Rationale: the user reframed at the gate ("try FinCEN articles first, other than advisories") — this stays inside the verbatim public-domain regime so the "FinCEN-only verbatim" non-negotiable needs NO change, the quote-grounding gate is reused unchanged, and the multi-source generalization is still exercised by genuinely heterogeneous content. OFAC (US-federal, also 17 USC §105) remains the documented next-source candidate if/when this proves out | high | 2026-06-06 |
| Thin multi-source SOURCES registry decoupling source-id from dir; data/fincen/ stays the `fincen-advisories` source BYTE-FROZEN, data/fincen-alerts/ is source #2. Rationale: renaming 14 mds + 12 derived + corpus-status to fit a scheme is churn that doesn't earn it (subtraction test) — make the scheme accommodate the existing dir and prove multi-source via the merge, not a migration. Registry is ready for {fincen-advisories, fincen-alerts, …ofac} and no more (not a plugin framework). The gate (check_record/rf_region/normalize) is reused UNCHANGED | high | 2026-06-06 |
| Proof batch ~5 alerts that ground cleanly; the remaining alerts are cheap follow-on. T2 carries a CHECKPOINT (convert one alert, confirm a groundable rf_region exists) before committing the batch; degrade path = pivot the source to FinCEN Financial Trend Analyses (indicator-heavy) or derive fewer-but-deeper if alerts convert mostly un-groundable. Never fabricate; the always-on "Illustrative data & outputs" badge + the verbatim public-domain attribution stay | medium-high | 2026-06-06 |
(Earlier Phase 18–19 decisions — see `[[2026-06-06-phase-19-durability-closeout|Ph19]]`, `[[2026-06-06-phase-18-corpus-explorer-arc|Ph18]]` journals. Load-bearing carry-overs: Ph19 DURABILITY CLOSEOUT — a committed ZERO-DEP Node `vm`+DOM-shim harness (`tests/corpus-explorer.test.mjs`, 28 assertions, NEVER jsdom — file:// offline ethos) that loads the COMMITTED `dist/corpus/index.html` so it doubles as a build-output smoke test (EXTEND this in Ph20 T5, don't replace); `_rf_triage` is PINNED + DISCLOSED NOT rewritten (an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today). Ph18 THE ARC — the corpus explorer's 5-screen arc (Select → Coverage → Build recs/GATE → Signal → Close the loop), div-toggle human gate folded into Build-recs (NOT `<input>` — keyboard-safe), COVERAGE close-the-loop payoff via `coverageIndex()` NOT precision combination-lift (precision-lift EXPLICITLY REJECTED — records carry no precision/lift numbers; porting it fabricates ~12 per-advisory stats, violating "never present synthetic numbers as real"). The Ph20 alert records reuse this arc unchanged per record.)
(Earlier Phase 16–17 decisions — see `[[2026-06-06-phase-17-complete-corpus-derivation|Ph17]]`, `[[2026-06-06-phase-16-invert-extraction|Ph16]]` journals. Load-bearing carry-overs: Ph17 the REAL subtraction — `extract_red_flags` + the whole `--scaffold`/`--draft`/`--scaffold-derived` authoring stack + `anthropic`/`os` DELETED (1202→600, stdlib-only); the inverted loop is the SOLE derivation path; `_rf_triage` counter reuses the `rf_region` span; corpus 12/14 live (only the 2 FATF non-derivable). Ph16 INVERTED the extraction boundary — the LLM EXTRACTS, the deterministic layer GATES (normalize(flag) ⊂ normalize(md) = the traceability authority, replacing src_line∈extractor) + a coarse `rf_region()` section-cite relevance guard + the cover×data matrix + BUILD_NOW⇒full-build_logic; corpus-status SHAPE preserved; `derivable: false` = the 2 FATF advisories.)
(Earlier Phase 12–15 decisions — see `[[2026-06-05-phase-15-harden-extraction-faithfulness|Ph15]]`, `[[2026-06-05-phase-14-scale-corpus-derivation|Ph14]]`, `[[2026-06-05-phase-13-corpus-explorer|Ph13]]`, `[[2026-06-05-phase-12-fincen-corpus-derivation|Ph12]]` journals. Load-bearing carry-overs: Ph15 harden-FAITHFULNESS (footnote-resume; glued-no-separator stays FLAGGED, no safe deterministic split; derived records store RAW text, `corpus.html` esc() the sole escaper); Ph14 PURE-AUTHORING scaling (build.py makes an advisory live by the presence of `derived/<id>.json`); Ph13 standalone STAGED 4-screen corpus explorer (`dist/corpus/` via `corpus.html`, all-14-honest status, six-act showcase byte-frozen); Ph12 SINGULAR corpus-backed demo (user picks 1 of 14; per-advisory `derived/*.json` the analytical artifact, the 3 typologies stay the showcase), build.py DECOUPLED from authoring (reads committed corpus-status.json + derived/*.json, never imports derive_signals.py), LLM backend = THIS session not `--draft`, validate at the build boundary.)

(Earlier Phase 7–11 decisions: see `[[phase-11-automated-derivation]]`, `[[phase-10-fincen-corpus-crawler]]`, `[[phase-08-doc-true-up]]` + journal. Load-bearing carry-overs: Phase 11 AUTOMATE → variant B (LLM-drafted signal NOW), boundary-preserving (LLM proposes; build.py + schema + 2 human gates dispose); authoring-time vs ship-artifact split (build-time tools; ship file stays single-file/offline/no-fetch, HANDOFF §4/§4.5); validate at the build boundary (deterministic validator, fail loud); FinCEN-only verbatim public-domain exception (17 USC §105, NOT FINTRAC); lite ceremony.)

## Blockers and Open Questions

- [RESOLVED 2026-06-06 · planning] Phase-20 direction → **scale the corpus beyond FinCEN *advisories* to other FinCEN *publication types* (Alerts first) via a THIN multi-source SOURCES registry — staying entirely inside the verbatim public-domain regime so NO non-negotiable change** (still FinCEN, still 17 USC §105, still verbatim; the quote-grounding gate is reused UNCHANGED). User reframed at the gate ("try FinCEN articles first, other than advisories") — chose OTHER FinCEN publication types over other agencies (OFAC — documented next-source candidate) / a new showcase typology. Architecture: a thin SOURCES registry decouples source-id from storage dir; `build.py`'s `render_corpus` iterates sources, reads each source's `corpus-status.json` + `derived/*.json`, merges by id into one `__CORPUS__`; `derive_signals.py --corpus-status` takes an optional source path. ZERO churn on what works: `data/fincen/` stays the `fincen-advisories` source BYTE-FROZEN (prove multi-source via the MERGE, not a migration — the subtraction test rejects renaming 14 mds + 12 derived + corpus-status); alerts land in a NEW `data/fincen-alerts/`. Proof = ~5 alerts grounded via the inverted loop (LLM extracts, `--check-derived` disposes); the remaining alerts are cheap follow-on. Open sub-question, resolved at impl by the T2 CHECKPOINT: do alert PDFs convert to a groundable red-flag region? — convert ONE alert and confirm `rf_region(md)` is not None before committing the batch; degrade path = pivot to FinCEN Financial Trend Analyses (indicator-heavy) or derive fewer-but-deeper. If the registry can't merge without growing as complex as a per-source migration (subtraction MOVES not shrinks), narrow to advisories-only + a registry stub. NEVER fabricate; the always-on badge + the verbatim public-domain attribution stay
- [RESOLVED 2026-06-06 · planning] Phase-18 direction → **give the corpus explorer (`dist/corpus/`, from `corpus.html`) a dramatic 5-screen arc — Select → Coverage → Build recs/GATE → Signal → Close the loop — mirroring the showcase's Act 3 (human gate) + Act 6 (loop closes), grounded ENTIRELY in existing data, NO fabricated numbers**. User chose the corpus-explorer arc over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate. The payoff is COVERAGE close-the-loop (the existing `coverageIndex()` recompute with picked gaps flipped to covered), NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (the corpus records carry no precision/lift numbers; porting it would fabricate ~12 per-advisory illustrative stats, violating the "never present synthetic numbers as real" non-negotiable). Scope contained to `corpus.html` (+ dist/corpus rebuild + docs): the arc reuses existing fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card) → NO schema/data/build.py change; the gate uses div-toggles (NOT `<input>`) to avoid keyboard-nav collision + preserve determinism; the gate folds into the existing Build-recs screen (subtraction test). Open sub-question, resolved at impl: can interactive selection be made keyboard-safe + deterministic without growing complex? Abort/degrade path = close-the-loop-only (no gate; coverage close over ALL BUILD_NOW — the payoff still lands); if the coverage math can't be honest, keep the close screen informational; NEVER fabricate precision/lift
- [RESOLVED 2026-06-06 · planning] Phase-17 direction → **complete the corpus derivation (7/14 → 12/14) + the REAL subtraction (delete `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack), unblocked by a cheap `rf_region`-bounded triage counter**. User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table (it would leave un-regenerable stale triage). The debrief had the dependency backwards — `extract_red_flags` is off the correctness path but is the SOLE producer of the extraction/flag_count triage the byte-frozen `corpus.html` renders; the ~12-line counter (reuses the existing `rf_region` span) replaces that role, keeps `--corpus-status` regenerable, and is materially smaller. Subtraction-FIRST ordering: delete + counter + reshape (T1), prove the 7/14 still builds clean on the stripped spine, then scale (T2–T4). Open sub-question, resolved at impl: can the `rf_region`-bounded counter preserve corpus-status.json's SHAPE / pass build.py validation without growing as complex as `extract_red_flags`? Abort path = STOP the deletion, keep `extract_red_flags`, narrow to scale-only (subtraction test must SHRINK complexity, not move it). Degrade-path for a LOW/glued advisory that won't ground faithfully = scale-what-passes (the deletion still lands — counter-gated, not scale-gated)
- [RESOLVED 2026-06-06 · planning] Phase-16 direction → **INVERT the extraction architecture** (the LLM extracts candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE — normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority — + a cheap section-cite relevance guard) **+ scale as proof to 7/14** (COVID-EIP fin-2021-a002 + ≥1 previously-LOW/glued advisory). User chose this over pure breadth-scaling / a structure-preserving converter (DISSOLVED). Decisions A (groundedness + section-cite, not pure-groundedness, not a full parser), B (demote `extract_red_flags`, preserve corpus-status SHAPE, derivable false only for the 2 FATF advisories), C (2 records as proof over count) confirmed. Open sub-question, resolved at impl: can `normalize()` ground the escrow STRESS case (header-glued + hyphen-truncated raw L499) without becoming as complex as the extractor it replaces? Abort path = keep quote-grounding for the clean records, leave `extract_red_flags` as-is for the rest, narrow breadth to COVID-EIP only (subtraction test must SHRINK complexity, not move it)
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus | 2026-06-06 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>` | 2026-06-06 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (33 FinCEN publications, 18 derived live = 12 adv + 6 alert; unified menu, Advisory/Alert chips) | 2026-06-06 |
| data/fincen/ + data/fincen-alerts/ | Source #1 `fincen-advisories` (14 md + 12 derived, BYTE-FROZEN by Ph20) + source #2 `fincen-alerts` (19 md + 6 derived; raw/ gitignored); each {*.md, derived/*.json, corpus-status.json} | 2026-06-06 |
| tests/corpus-explorer.test.mjs | Zero-dep Node DOM-shim arc harness — Phase 20 extended to 40 assertions (multi-source menu + an alert walking the 5-screen arc) | 2026-06-06 |

## Session Journal (last 5)

- [2026-06-06] [[2026-06-06-phase-20-multi-source-spine|Phase 20 Multi-source spine, proven with FinCEN Alerts (M7)]] (lite, 6 tasks, DELIVERED + accepted — READY FOR COMPLETION at the delivery gate; reviewer 9/10 ACCEPT, no CRITICAL/HIGH) — the first real SCALE move after M0–M7 hit Definition of Done, set by a USER REFRAME at the gate ("try FinCEN articles first, other than advisories") choosing OTHER FinCEN publication TYPES (Alerts) over other agencies (OFAC) / a new showcase typology. The win: staying inside the verbatim public-domain regime means NO non-negotiable change (the "FinCEN-only verbatim" rail untouched) and the quote-grounding gate (`check_record`/`rf_region`/`normalize`) is reused UNCHANGED, while the multi-source generalization is still exercised by heterogeneous content. **T1 the spine** (PURE byte-identical refactor): a thin `CORPUS_SOURCES` registry in build.py decouples source-id from storage dir (`data/fincen/` stays the `fincen-advisories` source BYTE-FROZEN — multi-source via the MERGE, not a migration); `render_corpus` split into per-source `_load_source` (manifest+derived merge, orphan check, graceful missing-derived-dir) + a thin iterate-merge loop, `doc_type` stamped at merge; `derive_signals.py` gained `source_dir` params. **T2 acquisition generalized**: `crawl_fincen.py` `parse_alerts` + `--alerts` (alerts hub lists PDFs DIRECTLY → zero-hop, literal-space hrefs %20-encoded), `acquire_fincen.py`/`pdf_to_md.py` `--source` (manifest url ending .pdf = direct download); the convert-one-first CHECKPOINT passed on DeepFakes (rf_region not None), then ALL 19 alerts acquired+converted (raw/ gitignored, md the source of truth). **T3 derivation**: 6 alerts via the inverted loop — one extraction SUBAGENT per alert as the LLM-backend role, each self-gating `--check-derived` clean then INDEPENDENTLY re-checked (exceeds ≥4; 72 ind / 19 BUILD_NOW; honest enrichment-heavy distributions kept, e.g. Russian-elite 17 ind / 2 BUILD_NOW). 2 alerts honestly non-derivable (no enumerated red-flag list). **T4–T6**: corpus.html unified menu + doc_type chips (Advisory/Alert), "advisory"→"document" neutralized across the shared 5-screen arc; dist/corpus rebuilt (33 publications, 18 derived live = 12 adv + 6 alert); harness 28→40; README + CLAUDE document the registry + alerts as source #2 + OFAC-next. CLEAN phase — no escape hatches, no blocked tasks, the T2 degrade path never needed. All gates GREEN: derive + crawl `--selftest` PASS, all 18 derived `--check-derived` clean, `--check all` 4-artifact ZERO DRIFT, frozen set (index.html/config/data/fincen/3-typology-dists) byte-clean. Review gate: unified reviewer 9/10 ACCEPT, two MEDIUM — the non-derivable chip refined to "no enumerated red-flag list" (FIXED), the `_source_arg` near-twin accepted (standalone-authoring-tools convention). NO commit this session → delivery gate stays pending. Retro TRIGGERED (Phase 20 milestone): Dim 3 recurring USER CORRECTION — the user reframes the gate toward scale/architecture/honesty over the carried showcase-debt true-up (5+ phases); recommendation: stop offering the true-up as a default option. SOFT OBS: 11 derivable alerts are cheap incremental follow-on (not a phase) · OFAC needs a verbatim-non-negotiable extension (compliance sign-off) · `_rf_triage` region counts coarse for some alerts (harmless) · the six-act showcase still has no harness.
- [2026-06-06] [[2026-06-06-phase-19-durability-closeout|Phase 19 Durability closeout — commit corpus-explorer test harness + pin _rf_triage (M7)]] (lite, 3 tasks, DELIVERED + accepted; impl ab0739a + gate 32185dc) — a durability closeout at the END of the completed M0–M7 roadmap (demo at Definition of Done): no net-new feature value without fabrication risk (combination-lift rejected Ph18), no FinCEN scale frontier (12/14; the 2 FATF have no red-flag list), so the one real-value move was locking in the corpus explorer's behavior. **T1 the harness:** committed `tests/corpus-explorer.test.mjs`, a ZERO-DEP Node `vm`+DOM-shim runner (hand-rolled document/getElementById/querySelectorAll/window/requestAnimationFrame/matchMedia/setTimeout — NEVER jsdom, file:// offline ethos + the dep-free `--selftest`/`--check` idiom) that loads the COMMITTED `dist/corpus/index.html` (doubling as a build-output smoke test) and drives the real div-toggle onclick wiring — resolving the phase's open sub-question (keyboard-safe + deterministic without a real DOM lib?) YES. **28 assertions pass** (plan estimated ~15): all 5 screens render · gate defaults all-BUILD_NOW-selected + a div-toggle onclick really flips `selected` · Signal = selected∩buildable w/ both honest empty states · close-the-loop before→after coverage math + the `Object.assign` no-mutation invariant · reduced-motion single-paint AND the animated path landing final after a rAF/timer flush (closes the Ph18 "animated close untested" soft-obs) · 0-picked flat-hold. **T2 pin `_rf_triage`:** the Ph17 reviewer MEDIUM reframed as PIN + DISCLOSE NOT rewrite (an accurate glued counter REINTRODUCES the Phase-17-deleted parser — anti-subtraction; harmless today: live records render from indicators, build.py ignores flag_count for live) — a bidirectional `--selftest` assertion (same 3 flags → `('low',1)` glued vs `('clean',3)` separated) + a docstring disclosure, NO output/logic change (corpus-status.json + dist/corpus byte-identical). **T3:** README (`## Test`) + CLAUDE ("How to run" Test bullet) + `tests/smoke-checklist.md` all name `node tests/corpus-explorer.test.mjs` as the structural pre-present check. Two carried candidates resolved at planning: the `anthropic` pin is DEAD (`requirements-authoring.txt` gone since Ph17), the `tests/` gap REAL. CLEAN phase — no escape hatches, no blocked tasks, degrade path never needed. Exit criteria all GREEN: harness 28/28, `--selftest` PASS, `--check all` 4-artifact ZERO DRIFT, frozen set (index.html/corpus.html/build.py/config/data/dist) git-diff-empty. NO commit this session (commit-only-when-asked) → delivery gate stays UNCHECKED until commit. Retro NOT triggered (17 completed phases; 17 % 5 ≠ 0). SOFT OBS: the harness tests the COMMITTED dist → editing corpus.html without rebuilding tests stale bytes (mitigated by the documented `--check all` → harness → smoke-checklist sequence; a build-then-test wrapper is a future hygiene item) · the 0-BUILD_NOW close path is still unexercised by real data (all 12 live have ≥1 BUILD_NOW; the same flat-hold code IS covered via 0-picked deselect-all) · the six-act showcase has no equivalent harness (a future durability pass could extend the same `vm`+shim).
(Earlier entries — Phase 18 corpus-explorer-arc ([[2026-06-06-phase-18-corpus-explorer-arc|journal]]) — gave the corpus explorer the showcase's two missing beats: a 5-screen ARC (Select → Coverage → Build recs/GATE → Signal → Close the loop), the BUILD_NOW rows as div-toggle human gate (NOT `<input>` — keyboard-safe), the COVERAGE close-the-loop payoff via `coverageIndex()` NOT precision combination-lift (precision-lift EXPLICITLY REJECTED — fabricates ~12 per-advisory stats); corpus.html-only scope, dist/corpus rebuilt, `--check all` zero drift. Phase 17 complete-corpus-derivation ([[2026-06-06-phase-17-complete-corpus-derivation|journal]]) — the REAL subtraction: deleted `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` stack + `anthropic`/`os` (1202→600, stdlib-only); the ~14-line `_rf_triage` counter replaced the extractor's triage role; scaled 7→12/14 via the inverted loop (only the 2 FATF non-derivable); `--check all` zero drift; RETRO Dim 3 = the recurring user override toward scale/architecture over the showcase-debt true-up. Phase 16 invert-extraction (LLM extracts → deterministic groundedness gate disposes; one `normalize()` rule absorbed the closed FinCEN-md artifact set; ransomware previously-unreachable glued advisory derived; 5/14→7/14; SOFT OBS that the file grew → Phase-17 deletion), Phase 15 harden-extraction-faithfulness (footnote-resume + esc() sweep), Phase 14 scale corpus derivation (2/14→5/14, pure authoring), Phase 13 corpus explorer THE PAYOFF (`dist/corpus/` staged 4-screen, the artifact Phase 18 gave an arc), Phase 12 corpus-derivation foundation, Phase 11 automated derivation, Phase 9 build-drift guard, Phase 8 doc true-up, M6 pipeline walking skeleton, M5 ship, M3/M2/M1 — see `index.md` journal list + their journal articles. Trimmed to last 2 full entries per the _CURRENT_STATE size budget overflow rule.)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN/E-23. Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
