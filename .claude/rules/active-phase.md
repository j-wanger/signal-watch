# Active Phase Context

Phase: 50 — AML program build: the demo became the program. A major user REFRAME at the dev-plan gate (2026-06-12): build the blueprint's §3 design-stage workloads as REAL systems, **one real-system-class repo per pillar**. signal-watch is now the program-ARCHITECTURE home (blueprint + this lifecycle record only); the build lives in sibling repos.

Pillar 1 = the **data substrate** → **`/Users/jwang/aml-substrate`** (Python; ultra-realistic synthetic Canadian retail-banking data where ML typologies EMERGE from modeled behavior). Its DESIGN.md holds the architecture, schema + distribution spec, gate, and cited sources. **aml-substrate Phases 1–11 DELIVERED 2026-06-13→16** (HEAD 0daa3cc; 360 tests green; gen/ freeze byte-stable). Highlights: P1 Foundation (population/KYC graph + 6-channel background engine + EMT) · P2 Emergence (criminal/mule/shell designation + fresh-Python laundering engine + transitive labeling + the A1 permutation-null separability gate; 1:21,657 imbalance; structuring breaks Benford) · P3 monitoring/grounding-chain · P4 1M-scale (19.38GB) · P5 entity-resolution (pw-F1 0.876) · P6 the ER⊥laundering NEGATIVE finding · **P7 Observable network linkage** (closed P6: `counterparty_account_id` populated + mirrored credit leg + case-id leak fixed + a positive freeze guard `scripts/check-gen-freeze.sh`; mule network now observable, recall 1.0) · P8 network reconstruction · P9 realism-validated + baseline-locked (`measure-baseline.json`, `--validate --check/--freeze`) · P10/P11/P12/P13 four subtlety/composition phases → the measured **TRIPLE-NULL** (HEAD bafc67d, 397 tests, gen byte-frozen #5): composition is NEVER required to detect laundering — amount-ROC (multivariate −0.34 vs total_amount 0.95), network-ROC (composed 0.916 vs best single 0.886, below the sealed 0.05 margin), network-precision (AP 0.043 vs 0.033, below the sealed 0.083 floor). Root cause STRUCTURAL: one ring ≈ one mule doing every typology → typologies co-occur REDUNDANTLY (any single detector catches the case). The §6 levers (Hawkes/dormancy temporal engine, legit-overlap cohort) were measured/predicted null AND counterproductive (a dormancy-burst drives velocity to a near-perfect single-feature tell). **Composition DETECTION-LIFT is RETIRED (user gate 2026-06-16)** — the triple-null is a PUBLISHED honesty asset, not a gap; composition's value pivots to evidence-assembly + corpus-redundancy + volume-inversion (none needs detection-lift). **Next: a cheap measure-only STRUCTURE-DETECTOR reachability probe** — the one config the triple-null never tested (a purpose-built network-structure signal over the P7/P8 graph; NO gen-unfreeze; pre-registered two-baseline, held-out) → null = retire unconditionally / clear = build the structure signal (still NOT P14). **P14 case-construction DEFERRED INDEFINITELY** (a self-engineering tautology + emergence-doctrine violation). The Hawkes/dormancy redesign survives only as a §6 temporal-FIDELITY lever (Leg B 0/3), never a composition lever. (Superseded brief `aml-substrate/docs/phase-12-multivariate-subtlety-PLAN-BRIEF.md` — P12/P13 consumed + nulled it.)

Pillar 2 = the **case-investigation → composition → SAR/STR-narrative** workload (blueprint §3 rows 4–6; aml-substrate's DESIGN names it "pillar #6 — the agentic capstone"). **Greenlit 2026-06-16 to run in PARALLEL** with Pillar 1 (the program is now parallel-pillar, not serial — the judgment/composition/narrative end doesn't wait on transaction-detection realism). First increment = the FULL chain (composition + judgment + SAR/STR narrative + completeness checklist + Class-A sign-off seam), built DEPTH-FIRST (one case → signed SAR, then a stratified set), with REAL deterministic verifiers (citation + completeness + grounding-replay). Pillar 2 repo = **`/Users/jwang/aml-casework`** (created + scaffolded 2026-06-16, genesis commit 08b06df; its own dev-wiki + dev-init pending in a rooted session). Bootstrapped on SYNTHETIC evidence bundles following the contract (user's call — unblocks Pillar 2 without waiting on Pillar 1's persist step): the executable contract + `validate_bundle` (first deterministic verifier), the depth-first thin-slice fixture (real corpus grounding, synthetic banking data — C4 structuring + C15 shell, two non-redundant axes), tests GREEN. **Pillar 2 has since reached Phase 2** (HEAD 8c8b3f8, 73 tests, mypy --strict clean): grounding-replay + completeness + citation + Class-A signoff seam + 5 stratified fixtures; the neural narrator is deferred-with-owner. KEY GAP (next work): the audit-walk stops at the bundle's own grounding block — no verifier reads any corpus file (grounding is authored-real, NOT enforced-real); the cheap fix is to wire the citation verifier to read the FROZEN corpus `derived/*.json` (normalize/rf_region pattern; IND-11/IND-04 exist there). signal-watch owns the cross-pillar **integration contract** `docs/pillar-integration-contract.md` — **§3 re-grounded 2026-06-16** to the triple-null: composition = redundancy-aware evidence-assembly + volume-inversion, detection-lift retired; the network-structure signal is the one un-refuted nuance, deferred pending the structure-detector probe.

**NO ACTIVE signal-watch-local build.** The measure-first workstream's two delivered numbers both live here as NON-ship analyses (read-only over the FROZEN corpus; ship corpus byte-frozen):
- **Phase 52 — C/D-tag reliability (DELIVERED 2026-06-16):** the unguarded dimension (the grounding gate never checks the per-indicator C/D codes) measured stratified + blind: random same-model re-rate reproduces C/D ~62.5%/axis (self-consistency, NOT validated correctness) · blind pairwise upholds the Phase-34 corrections 70.8% (~3:1, adjudicable, 0 neither). Artifacts: `scripts/cd_correctness.py` · `data/cd-correctness/*` · `docs/cd-correctness-report.md`.
- **Phase 51 — corpus redundancy (DELIVERED 2026-06-16):** cross-regulator co-occurrence ≤ 0.325; genuine equivalence est ~1.4% strict / ~11.5% band (single-rater illustrative). `scripts/corpus_redundancy.py` · `docs/corpus-redundancy-report.md`.

Remaining measure-first candidates route to SIBLING repos (a signal-watch-rooted session can't drive their lifecycle): casework audit-walk-to-source (`aml-casework/docs/audit-walk-to-source-PLAN-BRIEF.md`, cheap-wiring VERIFIED); the structure-detector reachability probe (aml-substrate). Next signal-watch-local candidates (deferred-with-owner, from the Phase-52 journal): a genuinely INDEPENDENT C/D rater (different model family / human) to convert self-consistency → reliability; larger N; the deterministic section-heading→capability residual (the Phase-37 thread).

Doctrine (aml-substrate): data-first/emergent (typologies emerge, NEVER injected/stamped); deterministically SCRIPTED generation, no runtime LLM (small statistical models OK; LLM only authors scripts); grounded in researched REAL schemas + distributions; everything SYNTHETIC, no real customer data, ever.

Note: aml-substrate has its OWN `.dev-wiki/` (P1–P13 delivered, the triple-null; composition detection-lift retired; next = the structure-detector probe) and aml-casework has its own (P2 delivered). Run the dev-* skills from a session ROOTED in each (their hooks fire only there; a signal-watch-rooted session can't drive their lifecycle). DESIGN.md + docs/phase-*.md remain durable. **This file drifts between cross-pillar reviews and the drift turned MATERIAL once (the contract shipped a premise the substrate had refuted) — trust each sibling repo's CURRENT HEAD for its live state. PROCESS RULE (2026-06-16): any cross-pillar artifact [integration contract, sales pitch, governance model, _CURRENT_STATE pillar facts] MUST be re-grounded against the sibling repo's current measured verdict before it is committed or consumed; record the grounding HEAD inline.**

FROZEN (signal-watch — no further demo work this track unless re-opened): the 5 ship artifacts + dists byte-identical (index.html + config + 3 typology dists; corpus.html + dist/corpus; news.html + dist/news; console.html + dist/console; triage.html + dist/triage); derive_signals.py; news pipeline; all committed derived data + the 3 overlays; docs/program-blueprint.md + blueprint-report.html.

Abort rule: existing signal-watch dists drift → STOP and surface (never re-baseline).

Gates (Phase 50, program-kickoff direction gate — closed 2026-06-12, all_accept: false):
- [x] Direction confirmed by user (A1 foundation-first/emergence-ready accept · A2 build-on-research [user override of review-first] · A3 1M-scale reject→small-first · A4 hybrid eyes-open→deferred to phase 2; ledger Phase-50 block, revisit-status recorded at Phase-1 delivery)
- [x] aml-substrate Phase 1 (Foundation) delivered 2026-06-13 (54 tests; A1 proven, A3 validated, A2 held, A4 deferred)
- [x] aml-substrate Phase 2 (Emergence) delivered 2026-06-13 (83 tests; A1 measured-not-assumed via the separability gate, A2 bounded, A3 fresh works, A4 1:21k validated at scale, A5 no migration)
- [x] aml-substrate Phases 3–6 delivered 2026-06-13→14 (P3 monitoring 154 tests · P4 1M-scale 19.38GB · P5 ER pw-F1 0.876 · P6 the ER⊥laundering negative finding; 239 tests total) — per-phase gates tracked in that repo
- [x] aml-substrate Phases 7–11 delivered 2026-06-15→16 (P7 observable network linkage [closed the P6 finding] · P8 reconstruction · P9 realism-validated + baseline-locked · P10/P11 subtlety; 360 tests total, HEAD 0daa3cc) — per-phase gates tracked in that repo
- [x] aml-substrate Phases 12–13 delivered 2026-06-16 → the measured TRIPLE-NULL (composition never required, all 3 axes; 397 tests, HEAD bafc67d) — the §6 levers nulled + counterproductive; per-phase gates in that repo
- [x] Pillar 2 greenlit PARALLEL + repo created/scaffolded 2026-06-16 → /Users/jwang/aml-casework; has since reached Phase 2 (HEAD 8c8b3f8, 73 tests, mypy --strict clean)

Direction gate (2026-06-16, two-workflow review through the new lenses — composition retired, measure-first):
- [x] Composition DETECTION-LIFT retired by user — pivot to evidence-assembly + corpus-redundancy + volume-inversion; triple-null kept as a published pitch asset; P14 deferred indefinitely; settle via one cheap structure-detector probe
- [x] Audience = measure-first (no imminent sell conversation) — close a frontier with a MEASURED result before more pitch/architecture polish
- [x] Re-grounded the refuted artifacts (contract §3 + pitch composition claim) + added the re-ground-before-commit process rule [done this session, architecture home]
- [ ] casework audit-walk-to-source verifier (wire citation verifier to the frozen corpus) → an aml-casework-rooted session
- [x] corpus redundancy measurement PLANNED as **Phase 51** (2026-06-16) — direction gated to the HYBRID non-ship artifact (honesty-axis decision: in-constraint deterministic upper bound + sampled consensus-class semantic estimate; ship corpus untouched); tasks T1–T4 + the ledger Phase-51 block written; the measurement workstream's other number (C/D-correctness over the 213 console divergences) stays a parallel measure-first candidate
- [x] Phase 51 DELIVERED 2026-06-16 (T1–T4): cross-regulator co-occurrence ≤ 0.325 (deterministic) · genuine equivalence est ~1.4% strict / ~11.5% band (single-rater illustrative, consensus deferred) — the §13 fm-1 frontier measured honestly; the corpus's no-dedup stance empirically vindicated; ship corpus byte-frozen (--check all 7/7 zero drift). Artifacts: scripts/corpus_redundancy.py · data/corpus-redundancy/equiv-sample.json · docs/corpus-redundancy-report.md
- [ ] the structure-detector reachability probe (measure-only) → an aml-substrate-rooted session
- [x] Phase 52 DELIVERED + accepted 2026-06-16 (T1–T4; both gates accepted, all assumptions HELD): the unguarded C/D dimension measured honestly — blind same-model re-rate reproduces C/D ~62.5%/axis (self-consistency, NOT validated correctness; independence deferred-with-owner) · blind pairwise upholds the Phase-34 corrections 17/24 on the hard subset (adjudicable, 0 neither). The random and divergence numbers are different samples/tasks, **NOT subtractable** — defensible adjacent-code scatter is plausibly one driver of the random mismatch, share unmeasured (n=24, the two rates statistically indistinguishable; the original "gap = scatter, not error" finding was tightened post-delivery). NON-ship; ship corpus byte-frozen (--check all 7/7 zero drift). Artifacts: scripts/cd_correctness.py · data/cd-correctness/{random,divergence}-sample.json · docs/cd-correctness-report.md.

Per-pillar/per-phase gates for the sibling repos are tracked there (their `.dev-wiki` + DESIGN.md §8 + docs/).
