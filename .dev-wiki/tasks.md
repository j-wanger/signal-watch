# Tasks

> Last updated: 2026-06-06 by /dev-plan (Phase 16)

<!-- phase:phase-16-invert-extraction -->
<!-- gate-log:phase-16 direction=approved delivery=accepted -->

## Phase 16: Invert extraction (LLM extracts, deterministic gate disposes) + scale as proof — M7

Apply the SUBTRACTION TEST to the extraction spine. The deterministic `extract_red_flags` has accreted format special-casing every phase (two-tier anchors → footnote-resume → citation signatures → glued-handling, ~130 lines of `_SECTION_STOP`/`_FOOTNOTE_STOP`/`_CITATION` machinery) and still only cleanly parses 7/14 advisories; we already concede "the spine ASSISTS, it does not AUTOMATE — complete records need LLM authoring." Evidence the user surfaced: the extractor's raw output is noise the LLM already cleans (fin-2025-a003 raw L499 = `"FINCEN ADVISORY A customer that is a U.S.-based escrow company receives funds from an unaffiliated, foreign-"` — running-header glued on front, hyphen-truncated mid-sentence; the clean `flag` text in the record was LLM-authored by stitching across the page break). So INVERT: the **LLM (this model session as backend) EXTRACTS candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE that DISPOSES**. This is "deterministic validators at boundaries over neural judges at the end" — LLM at the start (extraction, a generative task it is good at), deterministic validator at the boundary (the gate). Complexity relocates from brittle section-PARSING (an open problem — every advisory's structure differs) to md NORMALIZATION (a closed problem — a finite known artifact set: running headers, form-feeds, hyphen-line-breaks, smart quotes, whitespace). Traceability moves from src_line (∈ extractor output) to VERBATIM QUOTE (normalized `flag` ∈ normalized md) — simpler AND stronger (checks actual text, format-agnostic); each derived-record indicator ALREADY carries the verbatim span in its `flag`, so migrating the 5 committed records is nearly free. matrix-consistency (`build_rec_category` cover×data), BUILD_NOW⇒full-build_logic, and shape checks are UNCHANGED — they stay deterministic dispose-logic. The named TRADE: groundedness proves a quote is REAL TEXT, not that it is a GENUINE red flag — decision A's cheap section-cite check buys back section-level relevance, the two human gates dispose on residual relevance. Standing non-negotiables PRESERVED: "LLM proposes, deterministic checks dispose" (strengthened — LLM now proposes extraction too); no authoring tool imported by index.html/build.py; ship artifact never calls an LLM (extraction is build-time, committed); derived records store RAW text, corpus.html `esc()` sole escaper; byte-frozen showcase (index.html, config/**, 3 typology dists — AND corpus.html, since corpus-status.json shape is preserved); deterministic layer stdlib-only, `import anthropic` lazy. Direction approved by user 2026-06-06 (invert the architecture + scale as proof; glued-converter option DISSOLVED).

- [x] T1 · Groundedness gate + md normalizer (the core) | scope: scripts/derive_signals.py | success: a pure `normalize(text)` handles the closed FinCEN-md artifact set (running headers `FINCEN ADVISORY` + letter-spaced `F I N C E N` variants, form-feed page breaks, hyphenated line-breaks rejoined, smart/curly quotes folded, whitespace collapsed); `check_record` traceability is REWIRED so each indicator's normalized `flag` must be a substring of the normalized source md (quote-grounding is the new traceability authority; src_line kept only as an optional hint); a section-cite relevance guard asserts the indicator's `section` is locatable in the md AND the quote falls within that section's span; matrix-consistency (`build_rec_category`) + BUILD_NOW⇒full-build_logic + shape checks unchanged; the escrow STRESS case (fin-2025-a003 IND-18, header-glued + hyphen-truncated at raw src_line 499) grounds cleanly AND a fabricated non-substring flag is REJECTED (assert BOTH in `--selftest`); `python3 scripts/derive_signals.py --selftest` still EFE 12+12 + all checks; stdlib-only, `import anthropic` still lazy; not imported by index.html/scripts/build.py | size: L ✓ PASS (the SUBTRACTION won decisively: ONE rule — `normalize(text) = re.sub(r'[^a-z0-9]+','',text.lower()).replace('fincenadvisory','')` — absorbs EVERY md artifact at once [line wraps, word-wrap hyphens `foreign-\nbased`==`foreign-based`, smart quotes, punctuation, footnote-ref digits, AND the page-break running header, which collapses to a droppable `fincenadvisory` token incl. the letter-spaced variant]. The header-glued escrow case grounds WITHOUT special-casing because the header sits at the line boundary, not mid-flag. `check_record` rewired: deleted `flag_lines = {f['line'] for f in extract_red_flags(md)}` + the src_line∈extractor check → `normalize(flag) in normalize(md)` [GROUNDING = the traceability authority] + a coarse `rf_region(md)` relevance guard [first `_RF_*` anchor → first `_RF_REGION_END` terminal; src_line must sit inside, so an overview/SAR quote is rejected] + a distinct-flag-text check [replaces distinct-src_line]; matrix/shape/BUILD_NOW checks UNCHANGED. `_checks_selftest` extended: records now carry verbatim `flag`, a FABRICATED flag → grounding violation [proven: `'totally invented…'` → rejected], normalizer invariants [hyphen-wrap + header-strip] + the escrow grounding stress all assert. `--selftest` EFE 12+12 + all checks PASS. PROBE: all 5 committed records ALREADY pass the new gate unedited [T2 is near-free] + a real grounded flag passes + a fabricated one is rejected. py_compile OK; stdlib-only [no col-0 non-stdlib import]; anthropic lazy [L534 indented]; build.py/index.html do NOT import derive_signals [the `from derive_signals` grep hit is prose in a build.py comment]. Net: ~3-line normalizer + ~12-line `rf_region` REPLACE the src_line traceability that depended on the ~130-line extractor — complexity SHRANK, didn't move.)
- [x] T2 · Migrate the 5 committed derived records to the groundedness gate | scope: data/fincen/derived/*.json | success: all 5 existing records (fin-2020-a008, fin-2022-a001, fin-2024-a002, fin-2025-a002, fin-2025-a003) pass `python3 scripts/derive_signals.py --check-derived <rec>` under the NEW gate — each indicator's `flag` grounds via normalize+substring AND satisfies the section-cite guard; any `flag` that was over-cleaned into a non-grounding paraphrase is adjusted to a grounded verbatim span (text stays RAW, esc()-sole-escaper convention preserved); provenance wording updated where it referenced src_line-based traceability; index.html/config/**/corpus.html + the 3 typology dists byte-untouched (`git diff index.html corpus.html config` empty after) | size: M ✓ PASS (migration was NEAR-FREE as predicted — all 5 records pass `--check-derived` under the new gate UNEDITED: their `flag` fields were transcribed verbatim by the prior loops so they ground by construction, and their src_lines already sit inside rf_region. The only edit was a surgical raw-text provenance swap [`every indicator traces to an extracted red-flag md line` → `every indicator's flag is quote-grounded (verbatim) in the source md within the red-flag region (Phase 16)`] across all 5, done as a position-preserving string replace [no JSON reformat]. Re-`--check-derived` 5/5 OK; `git diff index.html corpus.html config/ + 3 typology dists` EMPTY — only derived/*.json + scripts changed. The cheap migration is the payoff of quote-based traceability over line-based.)
- [x] T3 · Demote the extractor + shift corpus-status semantics (shape preserved) | scope: scripts/derive_signals.py, data/fincen/corpus-status.json | success: `extract_red_flags` + its `_SECTION_STOP`/`_FOOTNOTE_STOP`/`_CITATION` machinery retained ONLY as the EFE `--selftest` anchor + optional scaffold hint (a docstring/comment records the demotion — no longer the traceability authority, stops growing); `python3 scripts/derive_signals.py --corpus-status` regenerated keeping the SAME JSON SHAPE (14 entries, same fields) so `python3 scripts/build.py corpus` + corpus.html need NO edit, but `derivable` now means has-or-could-have a red-flag list (false only for the 2 FATF advisories fin-2020-a009/fin-2021-a003) and live keys on a committed gate-passing record; CLEAN/LOW/NEEDS stays as an informational hint; `python3 scripts/derive_signals.py --selftest` EFE 12+12; stdlib-only, anthropic lazy; `! grep -nq 'import.*derive_signals\|from.*derive_signals' scripts/build.py` still holds (comment-only refs OK) | size: M ✓ PASS (extract_red_flags docstring gains a Phase-16 DEMOTED note [no longer the traceability authority — that moved to quote-grounding in check_record; retained as the EFE selftest anchor + --scaffold-derived/--corpus triage hint; do NOT grow it]. corpus-status `derivable` rebased onto `rf_region(md) is not None` — empirically the CLEANEST separator: it returns None for EXACTLY the 2 FATF advisories [no red-flag heading] and a region for the 2 glued advisories [heading survived, only bullets dropped], so the glued ransomware/health-care correctly flip false→true [reachable via the inverted loop]. `--corpus-status` diff = ONLY the `_note` [Phase-16 semantics] + 2 `derivable` false→true [the glued pair] + 2 FATF stay false; SHAPE byte-identical [same fields/order], summary unchanged [7c/3l/4n, `extraction` is now a hint]. module docstring CORPUS DERIVATION block rewritten for the inverted boundary; `--check-derived` msg `traceable`→`quote-grounded`. selftest EFE 12+12; --corpus runs; stdlib-only; anthropic lazy [L551]; build.py no import [prose-only refs]; index.html/corpus.html/config/+3 dists byte-frozen.)
- [x] T4 · Scale as proof — derive 2 records incl. ≥1 previously-unreachable | scope: data/fincen/derived/*.json | success: 2 new committed records via the inverted loop — COVID-EIP fin-2021-a002 (clean) + ONE previously-LOW/glued advisory (ISIS fin-2025-a001 OR ransomware fin-2021-a004) whose record explicitly notes which old extractor failure mode it bypassed; each passes `python3 scripts/derive_signals.py --check-derived <rec>` under the new gate (every flag grounds verbatim + section-cited + matrix-consistent, ≥1 BUILD_NOW with full build_logic); provenance marks them LLM-EXTRACTED + gate-checked (NOT a ship typology config); pruning/judgment is LLM-backend authoring by this model session | size: M ✓ PASS (THE PROOF landed. **ransomware fin-2021-a004** = the previously-unreachable headline: the deterministic extract_red_flags returned ZERO flags [glued-no-separator — markitdown dropped bullets AND blank lines, was extraction=none/derivable=false], yet the LLM backend read the source and EXTRACTED ALL 12 genuine red flags, every one GROUNDING verbatim under the new gate — proving the inverted loop reaches an advisory the structural parser cannot, with NO converter + NO post-hoc splitter [the Phase-15 deferral dissolved]. Honest enrichment-heavy spread: BUILD_ENRICH=4 · BUILD_NOW=3 [DFIR-passthrough-to-CVC-exchange · no-history-large-CVC · rapid-AEC-chainhop] · ENHANCE=2 · MONITOR=1 · SOURCE_DATA=2 [cyber IOC telemetry + Tor comms = data an FI doesn't hold]. **COVID-EIP fin-2021-a002**: the LLM CONSOLIDATED the extractor's 7 fragmented (i)/(ii) sub-clause blocks → 3 genuine indicators [2 BUILD_NOW: funnel-nonholder-deposits + same-address-EIP-aggregation, 1 BUILD_ENRICH]. Both `--check-derived` clean + pass build.py validate_corpus_data. provenance marks LLM-EXTRACTED [ransomware notes the bypassed glued failure mode]. DISCOVERY [escape-hatch, refines T1]: broadened `_RF_REGION_END` `reminder of relevant`→`reminder of\b` so ransomware's `Reminder of Regulatory Obligations` terminates its relevance region [tightened (283,405)→(283,339)]; selftest + ALL 7 records re-pass, EFE region unchanged — no regression. 7/7 derived → corpus 5/14→7/14 live. Showcase + corpus.html byte-frozen.)
- [x] T5 · Rebuild dist/corpus + drift guard + docs | scope: dist/corpus/index.html, README.md, CLAUDE.md, scripts/derive_signals.py | success: `python3 scripts/derive_signals.py --corpus-status` regenerated; `python3 scripts/build.py corpus` → dist/corpus rebuilt (now 7/14 live); `python3 scripts/build.py --check corpus` ok; `python3 scripts/build.py --check all` 4-artifact zero drift; `git diff index.html corpus.html config` empty (corpus.html unchanged — status JSON shape preserved); the 7 derived advisories render through all 4 screens; README + CLAUDE document the inverted architecture (LLM extracts → deterministic groundedness gate disposes → two human gates) + the honesty shift (the spine GATES, it no longer claims to ASSIST extraction) + that glued advisories are now reachable via LLM extraction (converter no longer needed); `python3 scripts/derive_signals.py --selftest` EFE 12+12 | size: S ✓ PASS (corpus-status regen [byte-stable — md set unchanged]; `build.py corpus` → dist/corpus/index.html 95,188→109,937 B [the 2 new records]; `--check corpus` ok; `--check all` 4-artifact ZERO DRIFT; `git diff index.html corpus.html config/ + 3 typology dists` EMPTY [corpus.html needed NO edit — the corpus-status JSON shape was preserved, as designed]. HEADLESS on the built file: 14 advisories · 7 live [the 5 + fin-2021-a002 + fin-2021-a004] · COVID-EIP 3 ind/2 BUILD_NOW · ransomware 12 ind/3 BUILD_NOW, both with build_logic + all src_line-traced; node --check VALID self-contained JS; illustrative badge present. Honest status holds: ransomware [glued] now LIVE, health-care fin-2026-a001 [glued] shows derivable=true/not-yet-derived, FATF fin-2020-a009 non-derivable. DOCS: README + CLAUDE both rewritten for the inverted boundary [LLM extracts → quote-grounding gate disposes → 2 human gates], the honesty shift [extract_red_flags DEMOTED; no longer ASSISTS — it GATES], the converter-DISSOLVED [glued reachable via the loop], and 5/14→7/14 [no stray 5/14; grep corpus README:25/CLAUDE:20]. --selftest EFE 12+12.)

> Exit (phase-16): groundedness gate + normalizer land (escrow grounds, fabricated flag rejected, `--selftest` EFE 12+12) · 5 existing records migrate + pass under the new gate · extractor demoted to selftest-anchor + hint, corpus-status semantics shifted with SHAPE preserved (FATF non-derivable, build.py/corpus.html byte-untouched) · 2 new records (incl. ≥1 previously-unreachable) gate-pass → 7/14 live · dist/corpus rebuilt, `--check all` zero drift, showcase byte-frozen · docs record the inverted architecture + honesty shift.
> Abort: if the normalizer cannot ground the escrow/glued cases without becoming as complex as the extractor it replaces (complexity merely MOVES rather than shrinks — the subtraction test FAILS), STOP — keep quote-grounding for the clean records, leave `extract_red_flags` as-is for the rest, narrow breadth to COVID-EIP only. Do not trade one brittle component for another. Blocked >3 attempts on a task → ask the user: skip or abort.

<!-- phase:phase-15-harden-extraction-faithfulness -->
<!-- gate-log:phase-15 direction=approved delivery=accepted -->

## Phase 15: Harden extraction faithfulness + fix shipped defects — M7

Spine robustness, scoped by MEASUREMENT (not assumption). The LOW/NEEDS advisories fail for 3 distinct reasons: (a) footnote-interruption — a real, well-formed list hard-stopped mid-list by a page-boundary footnote run, orphaning everything after (fin-2025-a003 L499 escrow flag; fin-2025-a001 ISIS); (b) glued-no-separator — markitdown dropped both bullets AND blank lines so flags+intro-caveat fuse into one block (fin-2021-a004 ransomware, fin-2026-a001 health-care) — NO safe deterministic split signal; (c) partial/noise (fin-2021-a001, fin-2024-a001). The extractor's contract is extract-or-honestly-flag and it already honors (b)/(c); the only TRUE defect is the SILENT MISS in a CLEAN advisory (fin-2025-a003 reports CLEAN but drops L499 — a faithfulness lie) + the shipped fin-2022-a001 esc() double-escape render bug. Fix the true defects; do NOT build a brittle splitter. Glued-no-separator splitting DEFERRED (investigated, no safe signal → stays FLAGGED, re-confirming the Phase-12 flag-don't-force decision). Direction approved by user 2026-06-05 (harden spine + fix defects; defer glued-splitting).

- [x] T1 · Footnote-resume fix in extract_red_flags | scope: scripts/derive_signals.py | success: a mid-list footnote/citation run no longer permanently ends a section — the span skips the footnote run (already dropped by the `_CITATION` filter) and RESUMES capturing real flag blocks up to the next anchor or a TRUE terminal stop (Reminder of Relevant BSA Obligations / SAR filing / numbered major section); `python3 scripts/derive_signals.py --list data/fincen/fin-2025-a003.md` now includes the L499 escrow flag (extraction 17→18); `python3 scripts/derive_signals.py --selftest` still passes (EFE 12+12 + deterministic checks); newly-captured flags spot-checked genuine (not footnote prose); stdlib-only, `import anthropic` still lazy; not imported by index.html/build.py | size: M ✓ PASS (MEASUREMENT-driven, 3 iterations. Split _SECTION_STOP into [a] _SECTION_STOP = TRUE terminals only [reminder/sar/faq/numbered-major-section, always break] + [b] a new _FOOTNOTE_STOP `^\d+\.(?:\s|$)` handled CONDITIONALLY in the span loop: with a next anchor [next_boundary set] a mid-list page-break footnote run is TRANSIENT → skip it + resume to the next anchor [captures L499]; with NO next anchor the footnotes are TERMINAL → break [no over-run]. First attempt [remove footnote-stop entirely] was TOO BLUNT — regressed last-section advisories a008 11→28, a021a002 7→22, a022a001 5→11 by swallowing trailing footnotes; the bounded next_boundary rule fixed that. Then 2 footnote-TAIL leaks remained [a003 L445 + PRC L578 — footnote continuations wrapped past an internal blank, no leading "N." marker, NOT structurally separable since the genuine escrow L499 also sits post-footnote-pre-anchor], so hardened _CITATION with 2 targeted signatures: a federal case-docket `\d:\d{2}-[a-z]{2,3}-\d+` [L445 "2:23-cr-258"] + a no-day "(Mon YYYY)" paren-date end [L578 "(Nov. 2006)."]. FINAL: a003 17→18 [exactly the original 17 src_lines + escrow L499, no leak]; PRC 14 [no leak]; ALL 12 other advisories BYTE-IDENTICAL to before; summary still 7C/3L/4N; --selftest EFE 12+12. Surgical: 1 genuine recovered flag, 0 collateral. stdlib-only; anthropic lazy [no top-level import]; build.py refs are comments-only, no import [Phase-13 decoupling holds]; index.html clean; py_compile OK. ISIS fin-2025-a001 stays LOW [single-section, footnotes terminal] — correctly flagged, not a regression.)
- [x] T2 · Pre-escaped-entity sweep across derived records | scope: data/fincen/derived/*.json | success: no derived record's build_logic stores HTML-pre-escaped entities — `! grep -lE '&(gt|lt|amp|quot|#39);' data/fincen/derived/*.json` finds none (fin-2022-a001's `&gt;= 2` → raw text/words); every touched record still passes `python3 scripts/derive_signals.py --check-derived <rec>`; convention recorded: derived records store RAW text, corpus.html esc() is the sole escaper | size: S ✓ PASS (2 Phase-12 records affected: fin-2022-a001 [2× `&gt;=`] + fin-2024-a002 [5× `&gt;=`/`&lt;=`]; my Phase-14 records already avoided them. Swept via html.unescape over every string value [always correct for render-through-esc() data] → raw `>=`/`<=`. grep finds zero pre-escaped entities across all derived/*.json; both records re-`--check-derived` clean [a001 5 ind/2 BUILD_NOW · a002 14 ind/4 BUILD_NOW]. corpus.html esc() now escapes them correctly on render — the SIGNAL screen will show `>= 2`, not the double-escaped `&gt;= 2`. Convention: derived records store RAW text; esc() is the sole escaper.)
- [x] T3 · Add the recovered escrow flag as fin-2025-a003's 18th indicator | scope: data/fincen/derived/fin-2025-a003.json | success: the L499 escrow flag (a U.S.-based escrow company receives funds from an unaffiliated foreign-based shell to purchase U.S. real estate) added with status/data/build_rec (cover×data matrix) + rationale (+ build_logic if BUILD_NOW), src_line tracing to the now-extracted L499; record = 18 indicators; `python3 scripts/derive_signals.py --check-derived data/fincen/derived/fin-2025-a003.json` clean | size: S ✓ PASS (appended IND-18: the escrow/real-estate flag, src_line 499, status=gap/data=partial → BUILD_ENRICH [the foreign-shell classification needs corporate-registry enrichment — consistent with IND-17 "shell-like characteristics" and the Iran shell indicators], section "redflag" matching the existing 17. --check-derived OK: 18 indicators, all matrix-consistent + traceable, 5 BUILD_NOW. Spread now BUILD_NOW=5 · BUILD_ENRICH=5 · ENHANCE=4 · SOURCE_DATA=2 · COVERED=1 · MONITOR=1. Extraction ↔ derivation now consistent: 18 extracted = 18 derived.)
- [x] T4 · Regen --corpus-status + rebuild dist/corpus + drift guard | scope: data/fincen/corpus-status.json, dist/corpus/index.html | success: `python3 scripts/derive_signals.py --corpus-status` regenerated (document movement: fin-2025-a003 17→18 + any LOW→CLEAN); the 2 FATF advisories still non-derivable; `python3 scripts/build.py corpus` → dist/corpus; `python3 scripts/build.py --check corpus` ok; `python3 scripts/build.py --check all` zero drift; `git diff index.html corpus.html config` empty; the 5 derived advisories still render through all 4 screens (fin-2025-a003 now 18 ind) | size: S ✓ PASS (manifest regenerated: a003 flag_count 17→18, summary still 7C/3L/4N [no LOW→CLEAN movement — the fix targeted a silent miss in a CLEAN advisory, not the glued LOW/NEEDS], FATF fin-2020-a009/fin-2021-a003 still extraction=none/derivable=false. dist/corpus rebuilt [94,406→95,188 B]; --check corpus ok; --check all 4-artifact ZERO DRIFT. Headless payload: 5 live [a008=10·a022a001=5·a024a002=14·a025a002=16·a025a003=18], escrow IND-18 BUILD_ENRICH present + renders; node --check valid. esc() fix verified end-to-end IN THE BUILT FILE: data now stores raw ">= 2" [2×], pre-escaped "&gt;=" gone [0] → esc() escapes once at render → browser shows ">=". byte-frozen: git diff index.html/corpus.html/config/ + 3 typology dists EMPTY. --selftest 12+12.)
- [x] T5 · Docs — faithfulness fix + glued-deferral rationale | scope: scripts/derive_signals.py, README.md, CLAUDE.md | success: derive_signals docstring + CLAUDE note the footnote-resume fix; README/CLAUDE record that glued-no-separator advisories stay FLAGGED (no safe deterministic split — the deliberate Phase-12 decision, re-confirmed by Phase-15 measurement); `grep -niE 'corpus' README.md CLAUDE.md` still finds the build instructions in both; `python3 scripts/build.py --check all` still zero drift | size: S ✓ PASS (derive_signals docstring CORPUS DERIVATION block notes the footnote-resume [_FOOTNOTE_STOP transient-when-section-follows] + glued-stays-FLAGGED; CLAUDE gained a Phase-15 extraction-faithfulness bullet [footnote-resume + glued-no-separator-not-force-parsed + the RAW-text/esc()-sole-escaper convention]; README gained a footnote-resume + glued-flagged paragraph. py_compile OK; --selftest 12+12. grep corpus README:24/CLAUDE:23, build.py corpus present in both; footnote/glued documented in all 3 files; --check all 4-artifact zero drift.)

> Exit (phase-15): the footnote-resume fix lands — fin-2025-a003 extracts the L499 flag (no silent miss in a CLEAN advisory), `--selftest` EFE 12+12 unchanged · no derived record stores pre-escaped HTML entities · fin-2025-a003 derived record = 18 `--check-derived`-clean indicators · `--corpus-status` regenerated + `dist/corpus` rebuilt, `--check all` zero drift, index.html/corpus.html/config/** + 3 typology dists byte-untouched · the glued-no-separator deferral documented (stays FLAGGED, no safe split).
> Abort: if the footnote-resume fix can't capture the L499 flag without regressing EFE 12+12 or capturing footnote prose as flags (no clean separation of footnote-run from resumed-list), revert it and keep the silent-miss FLAGGED as a known limitation (document it) rather than ship a fragile span heuristic. Blocked >3 attempts on a task → ask user: skip or abort.

<!-- phase:phase-14-scale-corpus-derivation -->
<!-- gate-log:phase-14 direction=approved delivery=accepted -->

## Phase 14: Scale corpus derivation (3 more CLEAN advisories → 5/14 live) — M7

Pure AUTHORING, no engineering: the spine tools (`--scaffold-derived`, `--check-derived`, `build.py corpus`) and the front-end (`corpus.html`) already exist from Phases 12–13. build.py makes an advisory "live" in the corpus explorer simply by the presence of `data/fincen/derived/<id>.json` — so deriving 3 more CLEAN advisories + rebuilding raises the explorer's live menu from 2/14 to 5/14, with ZERO changes to index.html, corpus.html, config/**, scripts/**, or the 3 typology dists. Per advisory, the proven loop: `--scaffold-derived <id> <md>` → author per-indicator judgment (status/data → build_rec via the cover×data matrix → rationale; build_logic for each BUILD_NOW gap) → PRUNE residual extraction noise → `--check-derived` passes (matrix-consistent + every indicator traces to a red-flag md line + BUILD_NOW⇒full definition). Picks (strong topical spread): fin-2020-a008 human trafficking (11), fin-2025-a003 Chinese money-laundering networks (17), fin-2025-a002 Iran illicit finance (16). EFE (fin-2022-a002, 24) excluded — already the full showcase elder typology; COVID EIP (fin-2021-a002, 7) deferred — dated. Direction approved by user 2026-06-05 (scale derivation over spine-robustness / corpus-wow-beat / showcase-debt; 3 advisories, EFE/COVID out).

- [x] T1 · Derive fin-2020-a008 (human trafficking) — LLM-backend authoring + check | scope: data/fincen/derived/fin-2020-a008.json | success: `python3 scripts/derive_signals.py --scaffold-derived fin-2020-a008 data/fincen/fin-2020-a008.md` then author each indicator's status/data/build_rec/rationale (build_rec per the cover×data matrix `build_rec_category`) + `build_logic` for every BUILD_NOW gap; residual extraction noise pruned (every indicator is a real advisory red flag, not an intro/header/citation artifact); provenance marks it LLM-backend-derived (this session, no key) + checked, NOT a ship typology config; `python3 scripts/derive_signals.py --check-derived data/fincen/derived/fin-2020-a008.json` passes AND ≥1 indicator is BUILD_NOW with full build_logic | size: M ✓ PASS (scaffold = 11 indicators; pruned IND-01 = the intro-tail noise line ["...identified in the 2014 Advisory... the FATF report... also provides numerous indicators"], renumbered the 10 genuine red flags IND-01..10 [the advisory states "FinCEN has identified 10 new financial red flag indicators" — extraction got all 10 + 1 noise]. Authored a credible AML-estate spread: BUILD_NOW=2 [S-VICTIM-MAINTENANCE-BASKET, S-CASHIN-NO-ELECTRONIC-OUT] · BUILD_ENRICH=3 · ENHANCE=2 · SOURCE_DATA=1 [escort-site identifier linkage, data we don't source] · MONITOR=1 · COVERED=1 [ID/CTR avoidance = structuring, mature control]. build_logic phrased threshold in words ["at least three"] to avoid the esc()-double-escape trap. --check-derived OK: 10 indicators, all matrix-consistent + traceable, 2 BUILD_NOW w/ build_logic.)
- [x] T2 · Derive fin-2025-a003 (Chinese money-laundering networks) — LLM-backend authoring + check | scope: data/fincen/derived/fin-2025-a003.json | success: same loop as T1 on fin-2025-a003 (`--scaffold-derived` → author status/data/build_rec/rationale + build_logic on BUILD_NOW → prune noise); `python3 scripts/derive_signals.py --check-derived data/fincen/derived/fin-2025-a003.json` passes AND ≥1 BUILD_NOW with full build_logic AND provenance set | size: M ✓ PASS (scaffold = 17 indicators across the 2 CMLN sub-typologies [Money Mules L452–490, TBML L505–533], ALL genuine red flags — clean extraction, no noise to prune. Authored via a matrix-merge script [build_rec auto-derived from build_rec_category so it can't drift] → full 6-category spread: BUILD_NOW=5 [student-mule funnel · cash-layer-to-high-risk dispersal · marketplace-income-no-inventory · inbound-aggregation card mirror · business-pays-third-party-cards] · BUILD_ENRICH=4 · ENHANCE=4 · SOURCE_DATA=2 [counterfeit passport/visa image forensics; source-of-funds evasiveness = case-note data] · COVERED=1 [pass-through churn] · MONITOR=1 [luxury card spend]. --check-derived OK: 17 indicators, all matrix-consistent + traceable, 5 BUILD_NOW w/ build_logic. FINDING [Phase-15 extractor item]: the extractor MISSED a real 18th red flag at L499 [U.S.-escrow-company / foreign-shell] — glued to a page-break "FINCEN ADVISORY" running header right after a footnote block, so it fell between sections; not extracted ⇒ can't trace ⇒ honestly omitted, not faked.)
- [x] T3 · Derive fin-2025-a002 (Iran illicit finance) — VALIDATE scaffold first, then author + check | scope: data/fincen/derived/fin-2025-a002.json | success: FIRST spot-check the scaffold's extracted flags against data/fincen/fin-2025-a002.md (Iran extraction was not spot-verified in Phase 12) — if faithful, author + `python3 scripts/derive_signals.py --check-derived data/fincen/derived/fin-2025-a002.json` passes (≥1 BUILD_NOW w/ full build_logic, noise pruned, provenance set); if the extraction is too noisy/unfaithful (heavy pruning or missed flags), SWAP to fin-2021-a002 (COVID EIP, cleaner 7) — derive + check that instead and note the deviation in the commit | size: M ✓ PASS (validate-first gate PASSED — all 16 extracted flags faithful + genuine across the 3 sub-typologies [Oil Smuggling L328–354, Shadow Banking L361–382, Weapons Procurement L390–414]; no noise, no missed flags, the 3 sub-headers correctly used as boundaries. NO swap needed. Authored an enrichment-hungry spread [the honest Iran story — maritime DBs, corporate registries, trade/AIS data] → BUILD_ENRICH=7 · BUILD_NOW=4 [oildoc-missing-consignee · exchange-house layering · CDD-sector-flow-mismatch · shell-cluster network] · SOURCE_DATA=2 [AIS-track data; correspondent-bank document forensics] · ENHANCE=1 · MONITOR=1 · COVERED=1 [incomplete wire originator info = Travel-Rule control]. Deliberate contrast vs CMLN's BUILD_NOW-heavy profile. --check-derived OK: 16 indicators, all matrix-consistent + traceable, 4 BUILD_NOW w/ build_logic.)
- [x] T4 · Rebuild dist/corpus + verify the 3 render + drift guard | scope: dist/corpus/index.html | success: `python3 scripts/derive_signals.py --corpus-status` (manifest still 14 entries; status/derivable unchanged) AND `python3 scripts/build.py corpus` → dist/corpus/index.html AND `python3 scripts/build.py --check corpus` ok AND the 3 newly-derived advisories show DERIVED/live and each renders through all 4 screens (coverage gauge + build-rec matrix BUILD_NOW-first + ≥1 signal-spec card) — verified by `node --check` + headless DOM assertions or screenshots AND `python3 scripts/build.py --check all` zero drift AND `git diff index.html corpus.html config` empty | size: S ✓ PASS (--corpus-status regenerated, manifest byte-stable [7c/3l/4n, 14 entries — derivation doesn't change extraction status]. build.py corpus → dist/corpus/index.html [94,406 B], passing build.py's validate_corpus_data boundary [build_rec ∈ enum; BUILD_NOW⇒full build_logic]. --check corpus ok; --check all 4-artifact ZERO DRIFT. Headless verification [brace-balanced extraction of the const CORPUS payload from the built file]: 14 advisories, 5 derived/live [the 2 existing + the 3 new], 9 honestly not-yet-derived; each new record renders coverage [status] + build-rec [build_rec + src_line] + ≥1 BUILD_NOW signal card — fin-2020-a008 10ind/2 cards · fin-2025-a002 16ind/4 cards · fin-2025-a003 17ind/5 cards; illustrative badge present. node --check on the inlined script [data+app, 82 KB] = valid JS. BYTE-FROZEN confirmed: git diff index.html corpus.html config/ scripts/ + 3 typology dists EMPTY.)
- [x] T5 · Docs — bump corpus live-count 2/14 → 5/14 | scope: README.md, CLAUDE.md | success: README + CLAUDE reflect the corpus explorer now ships 5/14 derived live (was 2/14), naming the 3 new advisories; `grep -niE 'corpus' README.md CLAUDE.md` still finds the build instructions in both AND `python3 scripts/build.py --check all` still zero drift | size: S ✓ PASS (README "ships with 5 of 14" + the varied-menu framing [CMLN = 5 buildable signals; Iran = enrichment-hungry]; CLAUDE "ships with 5/14 derived (Phase 14 scaled the 2-record proof slice to 5 — …)". grep corpus README:24/CLAUDE:21, build.py corpus + --check corpus instructions intact in both; 5/14 reflected, 2/14 gone. --check all 4-artifact zero drift; --selftest still EFE 12+12 + checks.)

> Exit (phase-14): 3 new `data/fincen/derived/{fin-2020-a008,fin-2025-a003,fin-2025-a002}.json` records (or COVID-EIP swap per T3), each `--check-derived`-clean with ≥1 BUILD_NOW carrying full build_logic and every indicator traceable to a red-flag md line · `dist/corpus/index.html` rebuilt showing 5/14 live, each new advisory rendering coverage → build-rec → signal through all 4 screens · `git diff index.html corpus.html` empty, config/** + the 3 typology dists byte-untouched, `build.py --check all` zero drift · README + CLAUDE reflect 5/14.
> Abort: if ≥2 of the 3 chosen advisories extract too noisily/unfaithfully to author a demo-quality traceable record, narrow to fewer (quality over count) and raise an extractor-improvement (glued-list splitting) Phase-15 item rather than shipping filler. Blocked >3 attempts on a task → ask user: skip or abort.

<!-- phase:phase-13-corpus-explorer -->
<!-- gate-log:phase-13 direction=approved delivery=accepted -->

## Phase 13: Corpus explorer (advisory-selection front-end + per-indicator build-rec render) — M7 — THE PAYOFF

Demo scope expansion: a NEW standalone ship artifact `dist/corpus/index.html` — a FinCEN CORPUS EXPLORER, driven by the Phase-12 derived records. Single self-contained file, offline, no fetch (same non-negotiable as the showcase). A STAGED 4-screen flow (chosen by user over a single dense dashboard): (1) SELECT — all 14 advisories with HONEST status chips (DERIVED=live/clickable · CLEAN/LOW/NEEDS="not yet derived" · FATF=non-derivable); (2) COVERAGE — chosen advisory's coverage gauge (covered=1·partial=0.5·gap=0) + indicator list (reuses the Act-0 gauge/map); (3) BUILD RECOMMENDATIONS (the NEW centerpiece) — per-indicator cover×data→build_rec matrix (BUILD_NOW/ENHANCE/BUILD_ENRICH/MONITOR/SOURCE_DATA/COVERED) + rationale, sorted BUILD_NOW-first, each row traceable to its red-flag src_line (reuses the Act-2 matrix + a build_rec column); (4) SIGNAL SPEC — the BUILD_NOW signal card(s) from build_logic (reuses the Act-4 spec card). No fabricated lift/stats; the always-on "Illustrative data & outputs" badge stays. The six-act engine `index.html`, the 3 typology configs, and their dists stay BYTE-UNTOUCHED — `corpus.html` owns its own copy of the dossier theme CSS (no shared include). build.py stays DECOUPLED from the authoring layer: it reads committed data artifacts (corpus-status.json + derived/*.json) and never imports derive_signals.py. Direction approved by user 2026-06-05 (standalone artifact + staged 4-screen flow + all-14 honest status, over fold-into-index.html / dashboard / only-2-derived).

- [x] T1 · Corpus status manifest — `derive_signals.py --corpus-status` | scope: scripts/derive_signals.py, data/fincen/corpus-status.json | success: `python3 scripts/derive_signals.py --corpus-status` writes committed `data/fincen/corpus-status.json` with 14 entries — each `{id, advisory_no, title, source, status (CLEAN/LOW/NEEDS from --corpus), flag_count, derivable}` (titles pulled from data/fincen/index.json) AND `python3 scripts/derive_signals.py --selftest` still passes (EFE 12+12) AND no non-stdlib import added to the deterministic path (`! grep -nE '^import (anthropic|requests|bs4|markitdown)' scripts/derive_signals.py`) | size: M
- [x] T2 · corpus.html standalone template — staged 4-screen explorer | scope: corpus.html | success: standalone self-contained explorer — own copy of theme CSS, a `__CORPUS__` injection point, render JS for the staged 4-screen flow (select → coverage → build-recs → signal-spec), reduced-motion + keyboard (←/→/Space/Esc) parity, always-on "Illustrative data & outputs" badge, honest status chips, defensive rendering (malformed/missing data → labeled placeholder, never a blank stage); `node --check` passes on the inlined script (or the built artifact renders); selection lists 14 with the 2 derived live; reduced-motion collapses staged reveals | size: L
- [x] T3 · build.py corpus path — render/build/check corpus | scope: scripts/build.py | success: add render_corpus / build_corpus / check_corpus + resolve the "corpus" target specially (NOT a config/typologies/*.json) + a corpus-data boundary validator (fail loud: every indicator's build_rec ∈ the known enum; BUILD_NOW ⇒ build_logic present with the full definition shape); assemble `__CORPUS__` from corpus-status.json + data/fincen/derived/*.json; `python3 scripts/build.py corpus` → dist/corpus/index.html that is self-contained (no fetch / external script / ES module) AND `python3 scripts/build.py --check corpus` reports ok AND `python3 scripts/build.py --check all` (typologies) still zero drift AND `git diff index.html` empty AND build.py does NOT import derive_signals.py (`! grep -nq derive_signals scripts/build.py`) | size: M
- [x] T4 · Build + verify end-to-end | scope: dist/corpus/index.html | success: `python3 scripts/build.py corpus` → dist/corpus/index.html that opens from file:// and renders the selection (14 advisories, honest status); both derived advisories (fin-2022-a001, fin-2024-a002) render coverage gauge + build-rec matrix (BUILD_NOW-first) + ≥1 spec card through all 4 screens; non-derived show honest status; `node --check` on the built file passes | size: S
- [x] T5 · Docs — README + CLAUDE document the corpus explorer | scope: README.md, CLAUDE.md | success: README + CLAUDE document the new ship target dist/corpus (`build.py corpus` + `--check corpus`), the `--corpus-status` manifest, the staged 4-screen flow, and the build.py-stays-decoupled boundary; current-state milestone note bumped to M7 corpus-backed demo; `grep -niE 'corpus' README.md CLAUDE.md` finds the build instructions in both AND `python3 scripts/build.py --check all` still zero drift | size: S

> Exit (phase-13): `dist/corpus/index.html` is a NEW self-contained offline ship artifact (no fetch/ES-module/external-script) rendering the staged 4-screen corpus explorer — SELECT (14 advisories, honest status chips, 2 derived live) → COVERAGE (gauge + indicator list) → BUILD RECOMMENDATIONS (cover×data→build_rec matrix, BUILD_NOW-first, src_line-traceable) → SIGNAL SPEC (BUILD_NOW cards from build_logic) · `derive_signals.py --corpus-status` emits committed corpus-status.json (14 entries) · `build.py corpus`/`--check corpus` work, build.py does NOT import derive_signals.py · the always-on "Illustrative data & outputs" badge + reduced-motion + keyboard parity present · `git diff index.html` empty, config/** + the 3 typology dists byte-untouched, `build.py --check all` zero drift · documented in README + CLAUDE.
> Abort: if the derived-record shape can't drive the coverage gauge / build-rec matrix / spec card without engine edits — keep corpus.html fully standalone (duplicate only what's renderable), don't reach into index.html. If reusing the Act-component markup forces an index.html change — re-implement the component in corpus.html's own CSS/JS instead (the showcase stays byte-frozen). Blocked >3 attempts on a task → ask user: skip or abort.

<!-- phase:phase-12-fincen-corpus-derivation -->
<!-- gate-log:phase-12 direction=approved delivery=accepted -->

## Phase 12: FinCEN corpus derivation foundation (deterministic spine all-14 + LLM proof slice) — COMPLETED + accepted (impl commit 90939b4)

Build the backend for an EXPANDED, singular FinCEN demo (eventual: user picks one of 14 advisories, watches the loop derive its coverage → build recommendations → signal). This phase = the **deterministic spine validated across all 14** + the **LLM-backend (this session, NO API key) derivation proven on a 2–3 advisory slice**. Boundary preserved (Phase-11 principle, extended): the LLM proposes the build recommendation + build logic; the deterministic spine disposes (schema/shape · build-rec consistency vs cover×data · traceability to a red-flag md line). The 14 corpus md's are ALREADY on disk (gitignored) + network is blocked in-session — so no acquisition; T1 commits the existing corpus. Demo expansion (advisory-selection UI + build-rec render) deferred to Phase 13. Engine `index.html` + `build.py` UNTOUCHED (backend-only). Direction approved by user 2026-06-05 (backend-only over a minimal selectable view).

- [x] T1 · Commit the FinCEN corpus (14 md) — un-gitignore the derivation surface | scope: .gitignore, data/fincen/*.md | success: all 14 `data/fincen/<id>.md` (the manifest ids fin-2020-a008 … fin-2026-a001) are git-tracked + committed (un-ignore `data/fincen/*.md`; keep `raw/` + `*.draft.json` ignored); each md non-empty + advisory-shaped; `git diff index.html` empty; `build.py --check all` zero drift | size: S ✓ PASS (.gitignore: dropped the `data/fincen/*.md` ignore + the `!fin-2022-a002.md` exception — the full corpus md is now the committed derivation surface; `raw/` stays local. 13 new md staged [fin-2022-a002 already committed] → 14/14 tracked, all non-empty; index.html clean; `--check all` zero drift. Commit lands at the delivery gate per project pattern.)
- [x] T2 · Generalize `extract_red_flags` for the corpus + `--corpus` batch mode + report | scope: scripts/derive_signals.py | success: `extract_red_flags` recognizes ≥2 FinCEN red-flag section formats beyond EFE's exact "Behavioral/Financial red flags … may include" phrasing (the corpus is heterogeneous — e.g. `fin-2020-a009`, `fin-2021-a003` have 0 "red flag/indicator" grep hits); a `--corpus` mode runs extraction across all 14 committed md and prints a per-advisory report (id → section-found + extracted-flag-count, or NEEDS-ATTENTION); EFE `--selftest` still passes (behavioral=12, financial=12); stdlib-only, `import anthropic` still LAZY; not imported by index.html/build.py | size: L ✓ PASS (rewrote `extract_red_flags` as a section-FINDER: anchors on INTRO sentences [forward "red flags … may include/as follows/listed below" + reverse "the following … red flag"] AND short HEADERS [Title-case+connectors, no "of <topic>" clause = title], coalesces a same-label header+intro pair [≤15 lines], spans to the next anchor/footnote/stop. `--corpus` gives a 3-way report — CLEAN/LOW-CONFIDENCE/NEEDS-ATTENTION via `extraction_quality` [≥3 flags AND max block ≤500 chars; calibrated — a legit EFE flag tops ~414, glued blocks 600–1300]. Result: **3 CLEAN** [EFE 12+12 "may include"; fin-2024-a002 14 (profile=8·transactional=6) header; fin-2022-a001 6 "the following"] · 6 LOW [section found but list not blank-separated — markitdown dropped bullet glyphs; honestly flagged, not forced] · 5 NEEDS-ATTENTION. EFE `--selftest` still 12+12. DISCOVERY: the corpus is markedly heterogeneous (no-blank-separator lists, letter-spaced "F I N C E N A D V I S O R Y" page headers, footnotes glued to lists) — the abort-rule section-FINDER-that-FLAGS is the right call vs a brittle universal regex. stdlib-only; anthropic lazy; absent from engine/build; index.html clean; --check all zero drift. POST-IMPL WIDENING (user ask, refines this task): (a) **two-tier anchors** — a Tier-2 fallback [loose "Red Flags <Related to / Potentially Indicative of / of …>" headers + a weak "identified red flags" intro] runs ONLY when no Tier-1 anchor exists, so EFE + the clean advisories are untouched while a purely-titled advisory (fin-2025-a003) still anchors its blank-separated list; (b) added "described below" lead-in (fin-2021-a002); (c) intro-noise filter dropping the standard FinCEN caveat boilerplate; (d) recalibrated `_MAX_FLAG_CHARS` 500→600 [corpus-wide: genuine single flags top 573, glued blocks ≥630 — a clean gap]. Corpus **3→8 CLEAN** [+fin-2020-a008 (11, verified real trafficking flags), +fin-2021-a002 (7), +fin-2025-a003 (17, real CMLN flags), +fin-2025-a001/a002 from the boilerplate filter] · 2 LOW [count-based: fin-2021-a001=2, fin-2024-a001=1] · 4 NEEDS [fin-2020-a009 + fin-2021-a003 = FATF jurisdiction advisories with NO red-flag list = CORRECTLY flagged; fin-2021-a004 + fin-2026-a001 = glued no-blank-separator lists]. New extractions spot-verified genuine (not noise). POST-REVIEW FILTERS (user ask, after inspecting all 103 extracted rows): drop blocks that are themselves a red-flag HEADER (killed 3 "Red Flags Associated with…" sub-headers mis-counted in fin-2025-a002, 19→16) + drop FOOTNOTE/CITATION blocks (section-stop now catches a bare "81." marker; a `_CITATION` filter drops "(Mon DD, YYYY)"-ending cites — killed the IC3 citation in fin-2025-a001). HONEST consequence: fin-2025-a001 8→7 CLEAN (it dropped to 2 real flags → LOW; it had only been "clean" because the citation padded the count — truer now). **Final corpus: 7 CLEAN · 3 LOW · 4 NEEDS** [2 NEEDS = FATF jurisdiction advisories, correct; 2 = glued no-separator lists]. NOTED in CLAUDE + README: the spine ASSISTS but does not AUTOMATE — a complete, demo-quality derived record still requires LLM-backend authoring (judgment + build logic + pruning residual artifacts); the deterministic layer extracts/flags/validates, the model session authors, the two human gates dispose. EFE still 12+12; both derived records still trace-pass.)
- [x] T3 · Deterministic checks — build-rec consistency + traceability | scope: scripts/derive_signals.py | success: pure `build_rec_category(cover, data)` encodes the allowed-recommendation matrix (covered→COVERED · gap+available→BUILD_NOW · gap+partial→BUILD_ENRICH · gap+insufficient→SOURCE_DATA · partial+*→ENHANCE/MONITOR); a consistency check REJECTS a recommendation whose category contradicts cover×data; a pure traceability check asserts each derived signal/indicator references a red-flag md line present in the source; a `--selftest` (or fixtures) asserts both; offline, stdlib | size: M ✓ PASS (pure `build_rec_category(status,data)` = the 3×3 matrix; `check_build_rec` returns ''=ok / a violation msg rejecting any rec that contradicts it; `check_record(record, md)` disposes a derived record on 3 axes — build-rec matrix-consistency, traceability [src_line must be a line `extract_red_flags` flagged], and BUILD_NOW⇒build_logic-with-full-definition-shape while COVERED/SOURCE_DATA⇒no-logic. `_checks_selftest` folded into `--selftest`: matrix total over the enum product, consistency catches covered→BUILD_NOW + accepts gap+available→BUILD_NOW, a valid EFE-traced record passes, a tampered one [wrong rec + bad src_line] yields ≥2 violations. Offline, stdlib, PURE; selftest exit 0.)
- [x] T4 · LLM-backend derivation — 2–3 advisory proof slice | scope: scripts/derive_signals.py, data/fincen/derived/*.json | success: for 2–3 corpus advisories (include ≥1 whose red-flag format differs from EFE), a committed `data/fincen/derived/<id>.json` record carrying per-indicator status + build recommendation (action category + free-text rationale) + build logic (signal definition) for the buildable gaps — produced by THIS session as the LLM backend (no API key, no `--draft`); each record passes the deterministic checks (build-rec-consistent + signal traceable + shape-valid); records carry provenance marking them LLM-backend-derived + check-passed (NOT a ship typology config) | size: M ✓ PASS (tool support: `--scaffold-derived <id> <md>` writes a skeleton [indicators from extract_red_flags, src_line traceable, empty judgment] to `data/fincen/derived/<id>.json`; `--check-derived <rec>` DISPOSES via `check_record`. 2 records, both non-EFE formats: **fin-2022-a001** [kleptocracy, "the following" intro, 5 indicators, 2 BUILD_NOW] + **fin-2024-a002** [PRC precursor chemicals, header format, 14 indicators, 4 BUILD_NOW; full spread BUILD_ENRICH=2·BUILD_NOW=4·COVERED=1·ENHANCE=4·MONITOR=1·SOURCE_DATA=2]. Each indicator has status/data/build_rec/rationale + build_logic on the BUILD_NOW gaps; authored by THIS session as the LLM backend [no key, no --draft]. Both pass `--check-derived` [matrix-consistent + traceable]. BOUNDARY proven: tampering IND-12 covered→BUILD_NOW is REJECTED. DISCOVERY (escape-hatch, refines T2): added an intro-noise filter dropping the standard FinCEN caveat boilerplate ["no single … determinative", "detecting, preventing, and reporting", "risk-based approach"] that bled into the first block — EFE still 12+12, CLEAN corpus count rose 3→5. selftest PASS; index.html+build.py clean; --check all zero drift; anthropic lazy.)
- [x] T5 · Docs + verify | scope: scripts/derive_signals.py, README.md, CLAUDE.md | success: module docstring + README + CLAUDE document the `--corpus` mode, the deterministic-spine-on-all-14, the LLM-backend (session, not API) proof-slice, the build-rec-consistency + traceability checks, and the `data/fincen/derived/` output; `grep -n corpus README.md CLAUDE.md` hits; `git diff index.html` empty; `build.py --check all` zero drift; `--corpus` report runs across 14 + `--selftest` still passes | size: S ✓ PASS (docstring Usage block + a CORPUS DERIVATION paragraph: --corpus/--scaffold-derived/--check-derived + LLM-proposes/checks-dispose + LLM-backend-may-be-the-session. README: corpus commands in the authoring block + a "Corpus derivation" paragraph. CLAUDE: header M6→M7 + a Phase-12 corpus-derivation bullet. grep corpus README:8/CLAUDE:6, derived README:4/CLAUDE:2; index.html clean; --check all zero drift; --selftest PASS; --corpus runs 5 clean·2 low·7 needs/14. No AGENTS.md → CLAUDE.md edited directly per M6/Phase-8/11 precedent.)

> Exit (phase-12): 14 corpus md committed · `extract_red_flags` generalized (≥2 formats) + `--corpus` report across all 14 + EFE `--selftest` still 12+12 · deterministic build-rec-consistency + traceability checks w/ selftest · LLM-backend proof slice (2–3) in `data/fincen/derived/*.json`, each check-passing · `git diff index.html` empty, `--check all` zero drift, stdlib-only deterministic layer, anthropic LAZY · documented in docstring + README + CLAUDE.
> Abort: if the corpus red-flag formats are too heterogeneous for a deterministic extractor to cover ≥2 cleanly — narrow to a robust section-FINDER that FLAGS non-conformers (the report is the deliverable), don't force a brittle universal regex. If a derived record can't pass the checks — the boundary holds (reject it), fix the check or the derivation. Blocked >3 attempts on a task → ask user: skip or abort.

<!-- phase:phase-11-automated-derivation -->
<!-- gate-log:phase-11 direction=approved delivery=accepted -->

## Phase 11: Automated derivation (LLM-drafted signal config) — AUTOMATE — COMPLETED + accepted (impl commit c37dc39)

Automate the **article→signal derivation** step proven MANUALLY in Phase 7 (EFE md → hand-derived elder config). One new authoring-only tool `scripts/derive_signals.py`, two layers: a **deterministic layer** (stdlib-only, offline, `--selftest`/`--scaffold`) that extracts the FinCEN red flags and scaffolds a schema-shaped config SKELETON, + a **neural layer** (`--draft`, env-keyed) that proposes the judgment fields (status per indicator, exactly one `target:true` indicator + candidate, the signal `definition`) by calling the Anthropic API. **Boundary preserved:** the LLM PROPOSES a `config/typologies/<id>.draft.json`; the deterministic validator (`build.py` + schema) + the two human gates DISPOSE — no neural judge at the build boundary, committed configs stay deterministic + human-reviewed. `derive_signals.py` is NEVER imported by `index.html`/`build.py`; the ship artifact never calls an LLM (HANDOFF §4/§4.5 hold). The `anthropic` SDK lives in the gitignored authoring venv (markitdown precedent), LAZY-imported only inside `--draft`. Direction approved by user 2026-06-05 (AUTOMATE → variant B over the planner's finish-first + deterministic-only recommendations).

- [x] T1 · Pure `extract_red_flags(md)` + `--selftest` | scope: scripts/derive_signals.py | success: new authoring-only `scripts/derive_signals.py`; `extract_red_flags(md) -> list[dict]` is a PURE function (no I/O) emitting `{section: 'behavioral'|'financial', n, text, line}` keyed on the FinCEN section anchors ("Behavioral red flags ... may include:" / "Financial red flags ... may include:"); `python3 scripts/derive_signals.py --selftest` parses the committed `data/fincen/fin-2022-a002.md` and asserts the known EFE red-flag counts (≥1 financial AND ≥1 behavioral; pin the actual counts once observed), exits 0 on pass / non-0 on fail; stdlib-only with any anthropic import kept LAZY so `--selftest` needs no SDK; not imported by `index.html` or `build.py`. VERIFY: `python3 scripts/derive_signals.py --selftest` exits 0 · `! grep -nE '^[[:space:]]*import (anthropic|requests|bs4|markitdown)' scripts/derive_signals.py` (no top-level non-stdlib import) · `! grep -nq derive_signals index.html scripts/build.py` | size: M ✓ PASS (anchor + blank-line-block parser: locates the behavioral/financial "may include:" intros, spans each section to the next header/footnote, groups blank-separated blocks while stripping page-number + "FINCEN ADVISORY" running-header artifacts; pinned counts behavioral=12 + financial=12 = the 24 known EFE flags; --selftest exit 0; --list. DISCOVERY: markitdown injects 14 form-feed page breaks → `str.splitlines()` split on them and drifted line numbers +9 vs the \n-based editor view; switched to `split("\n")` + form-feed strip so `line` traces correctly — financial #1 "Dormant accounts" now reports L507, matching `sed -n 507p` and the elder IND-02 traceability note. stdlib-only; no top-level non-stdlib import; absent from engine/build; py_compile OK)

- [x] T2 · Pure `scaffold_config()` + `--scaffold` write | scope: scripts/derive_signals.py, config/typologies/*.draft.json | success: pure `scaffold_config(id, flags, meta) -> dict` emits a schema-shaped config SKELETON — `coverage.indicators[]` (one per financial red flag, status defaulted to "gap", `sub` = md-line traceability), `candidates[]` stubs, `advisory_full.text_file` wired to the md, anchor/lift/stats as explicit TODO placeholders; NO `target:true`, NO signal definition; a `--scaffold <id> <md-path>` write path emits `config/typologies/<id>.draft.json` (a DRAFT, never auto-promoted to a shipped config); the draft is intentionally build-INVALID until judgment is filled. VERIFY: `python3 scripts/derive_signals.py --scaffold elder-draft data/fincen/fin-2022-a002.md` writes a `.draft.json` that loads as JSON, carries ≥1 indicator with a line-traceable `sub`, and has no `target` flag set | size: M ✓ PASS (scaffold_config pure: 12 financial flags → IND-01..12 + C1..12; status/cover/data neutral defaults, sub="src: FIN-2022-A002 financial red flag #N · md L<line>" (IND-01 traces L507), advisory_full.text_file wired, anchor/advisory_stream/lift/stats TODO placeholders; canonical 7-act steps/labels/hints templated; 0/0 targets. --scaffold writes elder-draft.draft.json (12 indicators, 12 line-traceable subs, loads as JSON, no target). DISCOVERY: build.py `resolve_targets("all")` globs `*.json` + uses `Path.stem`, so a lingering `<id>.draft.json` builds as bogus id `<id>.draft` → gitignored `config/typologies/*.draft.json` (transient scratch, matches Phase-10 batch-artifact precedent) + --scaffold prints "remove before build.py all"; build.py untouched (out of scope). BOUNDARY confirmed: `build.py elder-draft.draft` DIES naming exactly the 2 judgment fields ("exactly one indicator/candidate must have target:true (found 0)"), all else valid)

- [x] T3 · LLM `draft_definition(...)` + `--draft` mode | scope: scripts/derive_signals.py, scripts/requirements-authoring.txt | success: a build-time-only `--draft <id> <md-path>` mode adds the judgment fields the schema flags as decisions — proposes a status per indicator, selects exactly one indicator + one candidate with `target:true` (the buildable gap = `cover:"gap"` AND `data:"available"`), and drafts the signal `definition` {signal_name, class, features[], logic, window, source, route} — by calling the Anthropic API per the **claude-api reference** (consult it for the current model id + Anthropic Python SDK + structured-output/tool-use; do NOT guess); ANTHROPIC_API_KEY read from env (never hardcoded/committed); the model is GROUNDED on the extracted red flags + the schema definition spec + existing configs (fentanyl/elder) as few-shot, constrained to schema-valid structured output; the `anthropic` import is LAZY (only inside `--draft`), added to `scripts/requirements-authoring.txt` and installed in the gitignored authoring venv; `derive_signals.py` still never imported by engine/build.py. VERIFY: `grep -nq 'ANTHROPIC_API_KEY' scripts/derive_signals.py` (env-keyed) · `! grep -nE 'sk-ant' scripts/derive_signals.py` (no committed key) · `! grep -nE '^[[:space:]]*import anthropic' scripts/derive_signals.py` (lazy import) · `grep -nq anthropic scripts/requirements-authoring.txt`. The `--draft` run itself is a recorded MANUAL authoring run (never in smoke/CI); behavioral correctness verified in T4 | size: L ✓ PASS (neural layer: `draft_judgment` lazy-imports anthropic + reads ANTHROPIC_API_KEY from env, calls claude-opus-4-8 with `output_config.format` json_schema [per the claude-api reference — model id + structured-output consulted, not guessed], grounded on the extracted financial red flags + the schema definition contract + fentanyl/elder configs as few-shot; `_apply_judgment` writes the proposed status/targets/definition onto the skeleton, FORCING the target candidate cover:gap+data:available [validator-disposes guard]. Static VERIFY: env-keyed yes · no `sk-ant` · no TOP-LEVEL import [the lone `import anthropic` is indented inside the fn — lazy] · in requirements-authoring.txt. anthropic 0.105.2 installed in the gitignored .venv. env-guards: `--draft` w/ key unset → clean exit-1 "set ANTHROPIC_API_KEY"; under stdlib python → clean exit-1 "install in authoring venv"; deterministic --selftest stays SDK-free. DISCOVERY: the task's VERIFY regex `^[[:space:]]*import anthropic` over-matches an indented lazy import [`[[:space:]]*`=0+ spaces] — corrected check is `^import anthropic` [column-0], which finds none. KEY-FREE PROOF of the plumbing: synthetic-judgment → `_apply_judgment` → 1 indicator + 1 buildable candidate target + definition → `build.py` VALID. LIVE model call awaits Jake's key [recorded manual run, like Phase-7 convert / Phase-10 batch])

- [x] T4 · End-to-end proof + boundary check | scope: scripts/derive_signals.py, config/typologies/*.draft.json | success: a recorded manual authoring run extract→scaffold→draft on `data/fincen/fin-2022-a002.md` → a `.draft.json`, demonstrating the boundary holds — EITHER (a) the draft is human-reviewed/edited into a build-VALID config that `python3 scripts/build.py <id>` accepts (proving the pipe end to end), OR (b) `build.py`/schema REJECT an invalid draft (LLM proposes, validator disposes); engine/ship untouched. VERIFY: the draft round-trips `build.py` (valid → builds; invalid → rejected with a named error) · `git diff --exit-code index.html` clean · `python3 scripts/build.py --check all` zero drift (engine/committed configs untouched) | size: S ✓ PASS (BOTH boundary directions proven KEY-FREE: (b) the bare scaffold skeleton → `build.py` DIES naming exactly the 2 missing judgment fields [T2]; (a) a synthetic judgment → `_apply_judgment` → 1 indicator + 1 buildable candidate target + definition → `build.py` ACCEPTS — i.e. the LLM proposes, build.py disposes. `git diff index.html` CLEAN; `build.py --check all` → zero drift on all 3 shipped typologies, exit 0; derive_signals absent from engine/build; no `.draft.json` present or committed [gitignored transient]; working tree exactly in scope [new derive_signals.py + requirements-authoring.txt + .gitignore + dev-wiki bookkeeping — no index.html/config/dist edits]. LIVE `--draft` RUN (per user call, no API key — Opus 4.8 acted as the model backend, faithful since `--draft` calls the same model family): real end-to-end via `write_draft`→`_apply_judgment` with the judgment substituted for the network call → `elder-draft.draft.json` with target IND-01/C1 (the dormant-reactivation-drain blind spot, matching the proven elder anchor), signal `S-DORMANT-REACTIVATION-DRAIN` (5 concrete features), coverage spread covered:3/partial:4/gap:5, exactly one indicator+candidate target → `build.py elder-draft.draft` VALID. Scratch cleaned (gitignored). The genuine derivation builds — full pipe proven end-to-end. Only the literal Anthropic network call is unexercised (no key); the code matches the claude-api reference)

- [x] T5 · Docs | scope: scripts/derive_signals.py, README.md, CLAUDE.md | success: `derive_signals.py` module docstring documents the two-layer model (deterministic `--selftest`/`--scaffold` + neural `--draft`), the ANTHROPIC_API_KEY env requirement, the LLM-proposes/validator-disposes boundary, and anthropic-in-authoring-venv (not a ship dep); README "How to run" + CLAUDE.md "Current state (M6 ...)" each carry a one-line entry for `derive_signals.py` + the authoring-only LLM-draft boundary. VERIFY: `grep -n derive_signals README.md CLAUDE.md` hits both · `git diff --exit-code index.html` clean | size: S ✓ PASS (module docstring + Usage block: two-layer model, ANTHROPIC_API_KEY env req, LLM-proposes/validator-disposes boundary, anthropic-in-authoring-venv-not-ship. README authoring-pipeline block extended with --selftest/--scaffold/--draft + the two-layer/boundary prose + Status line; CLAUDE M6 pipeline chain extended crawl→acquire→pdf_to_md→**derive_signals**→hand-review, anthropic added to the venv note + env-key + never-imported/never-LLM caveat. grep derive_signals README:5/CLAUDE:1; index.html CLEAN; selftest PASS + --check all zero drift. NOTE: no AGENTS.md in repo → CLAUDE.md edited directly, consistent with Phase 8/M6 precedent)

> Exit (phase-11): `derive_signals.py --selftest` extracts the known EFE red-flag counts from the committed md, exits 0 (deterministic layer, offline) · `--scaffold` emits a schema-shaped `<id>.draft.json` skeleton (indicators line-traced, no target/definition) · `--draft` (env-keyed; anthropic lazy-imported from the authoring venv) proposes the judgment fields incl. a schema-valid signal `definition`, grounded on red flags + schema + few-shot · the draft round-trips the deterministic boundary (build.py/schema accept a human-reviewed valid config OR reject an invalid draft; committed configs stay deterministic + human-reviewed) · `git diff index.html` empty; deterministic layer stdlib-only; anthropic import LAZY; tool absent from engine/build imports; ship artifact never calls an LLM · documented in docstring + README + CLAUDE (two-layer model + boundary + env-key).
> Abort: if the LLM can't be coaxed to schema-valid output that build.py accepts (after consulting the claude-api reference for proper structured-output) — fall back to variant A (deterministic scaffolder only; human fills the judgment fields), still a usable increment. If the red-flag list can't be deterministically anchored from the post-markitdown md — thinner section-bounded bullet capture. Blocked >3 attempts on a task → ask the user: skip or abort.

<!-- phase:phase-10-fincen-corpus-crawler -->
<!-- gate-log:phase-10 direction=approved delivery=accepted -->

## Phase 10: FinCEN corpus crawler (SCALE) — COMPLETED + accepted (commit 0c87c47)

Widen the authoring scraper from one hand-registered advisory to the **discovered FinCEN advisory corpus**. The static 1-entry `REGISTRY` in `acquire_fincen.py` becomes a **generated manifest** (`data/fincen/index.json`: `{id, title, date, type, url}`). Core value is **discovery**, not mass-download: a new authoring-only `crawl_fincen.py` with a **pure `parse_index(html)→entries`** (deterministic, `--selftest` against a saved fixture — same ethos as Phase-9 `build.py --check`) + a **thin live `fetch_index()` shell** (manual authoring run, never in smoke/CI). The committed manifest is produced by `parse_index(saved_fixture)` so it's reproducible **offline**. The widened pipe is proven on a **bounded batch (~2–3)**, not the whole corpus. Engine/ship artifact untouched; `raw/` stays gitignored; commit the manifest + only the md we derive from — no bulk-md commit. Direction approved by user 2026-06-05.

- [x] T1 · Discovery probe + saved fixture | scope: tests/fixtures/** | success: the real FinCEN advisories index page is saved to `tests/fixtures/fincen-index.html` (manual authoring fetch) and its source URL recorded (comment/README line); the fixture is non-empty and lists multiple advisories with links + dates. VERIFY: `test -s tests/fixtures/fincen-index.html && [ "$(grep -ciE '/system/files|advisor' tests/fixtures/fincen-index.html)" -ge 5 ]`. ABORT: if the index is JS-rendered / requires auth / blocks scraping so no deterministic parser can be built from a saved page — PAUSE and pivot to a hand-curated manifest (still an upgrade over the 1-entry stub) | size: S ✓ PASS (fixture 49KB, server-rendered HTML table; 14 advisories `fin-2020-a008`…`fin-2026-a001` incl. EFE `fin-2022-a002`; provenance-stamped; source `…/advisoriesbulletinsfact-sheets/advisories`; VERIFY 60≥5. ABORT not triggered. DISCOVERY below)

> DISCOVERY (T1, escape-hatch — refines T2/T3): the listing yields **detail-page URLs** (`/resources/advisories/fincen-advisory-fin-YYYY-aXXX`, id in the slug) + title (`<a>` text) + a `MM/DD/YYYY` release-date column — NOT direct PDF URLs (FinCEN PDF filenames are unpredictable, e.g. EFE `…FINAL%20508.pdf`). So manifest `url` = the **detail page** (deterministic from the listing → offline-reproducible); resolving the actual PDF is an **acquire-time hop** (fetch detail page → regex the `/system/files/…pdf` link). The static EFE entry (direct PDF) stays merged as a fallback/override so `acquire fin-2022-a002` is zero-hop + backward-compatible.
- [x] T2 · `crawl_fincen.py` — pure parser + `--selftest` + manifest writer | scope: scripts/crawl_fincen.py, data/fincen/index.json | success: new authoring-only script; `parse_index(html) -> list[dict]` is a PURE function (no I/O) emitting `{id, title, date, type, url}`; `python3 scripts/crawl_fincen.py --selftest` parses `tests/fixtures/fincen-index.html` and asserts ≥3 well-formed entries (exits 0/non-0); a write path generates `data/fincen/index.json` from the saved fixture (reproducible offline) and merges the static EFE entry so `fin-2022-a002` is always present; stdlib-only; not imported by `index.html` or `build.py`. VERIFY: `python3 scripts/crawl_fincen.py --selftest` exits 0 · `python3 -c "import json;d=json.load(open('data/fincen/index.json'));assert isinstance(d,list) and len(d)>=3 and all({'id','title','date','url'}<=set(e) for e in d) and any(e['id']=='fin-2022-a002' for e in d)"` · `! grep -qE 'import (requests|bs4|markitdown)' scripts/crawl_fincen.py` | size: M ✓ PASS (parse_index pure: per-`<tr>` link/subject/`<time datetime>` regex extraction, sorted-by-id determinism, EFE `setdefault` guarantee; `--selftest`/`--write`/`--list`/`--fetch` modes; --selftest 14 entries/0 malformed/exit 0; --write → index.json 14 advisories incl EFE; structural assert PASS; stdlib-only; absent from engine/build; py_compile OK)
- [x] T3 · Wire `acquire_fincen.py` to the manifest | scope: scripts/acquire_fincen.py | success: the manifest (`data/fincen/index.json`, id→**detail-page URL**) is loaded for `--list`; the static EFE entry (direct PDF URL) is merged as a guaranteed override/fallback so `acquire fin-2022-a002` resolves zero-hop to the same canonical PDF; for a manifest id with only a detail-page URL, acquire resolves the PDF via a small `resolve_pdf(detail_url)` hop (fetch detail page → regex the `/system/files/…pdf` link); stdlib-only preserved. VERIFY: `python3 scripts/acquire_fincen.py --list` lists ≥3 ids including `fin-2022-a002`; backward-compatible (no live fetch in the automated check; the detail→PDF hop is exercised by T4's batch) | size: S ✓ PASS (REGISTRY→manifest: `load_manifest` reads index.json id→detail-url; `DIRECT_PDF` overrides keep EFE zero-hop + canonical URL byte-preserved; `resolve_pdf` detail→`/system/files/*.pdf` hop for manifest-only ids; `--list` shows 14 ids, EFE tagged `[direct-pdf]`; stdlib-only; py_compile OK; backward-compatible)
- [x] T4 · Bounded batch proof + docs | scope: scripts/crawl_fincen.py, scripts/acquire_fincen.py, README.md, CLAUDE.md | success: a bounded batch (~2–3 ids from the manifest) is acquired + converted as a recorded manual authoring run (like Phase 7) → `data/fincen/raw/<id>.pdf` (gitignored) + `data/fincen/<id>.md` for the sampled ids, proving the widened pipe; the crawler + generated-manifest model is documented in `crawl_fincen.py`/`acquire_fincen.py` docstrings + README "How to run" + CLAUDE.md "Current state"; engine/ship artifact untouched. VERIFY: `grep -n 'crawl_fincen' README.md CLAUDE.md` hits both · `git diff --exit-code index.html` clean · sampled `data/fincen/<id>.md` exist and are non-empty | size: S ✓ PASS (live batch via the new resolve_pdf hop — `fin-2021-a004` 672KB→34KB md/15 indicator hits, `fin-2024-a002` 565KB→56KB md/14 hits; the two unpredictable PDF filenames `…Ransomware%20Advisory_FINAL_508_.pdf` vs `…Supplemental-Advisory-on-Fentanyl-508C.pdf` validate the detail-page hop; docs README:5/CLAUDE:1 crawl_fincen; index.html CLEAN; manifest offline-reproducible re-write→no diff; build.py --check all zero drift. NOTE: batch md + raw PDFs DELETED per user call (proof in this record + journal; fully regenerable via acquire+convert) — keeps the tree minimal, only the EFE anchor corpus committed)

> Exit (phase-10): `crawl_fincen.py` (authoring-only, stdlib) discovers the FinCEN advisory index → committed `data/fincen/index.json` manifest, reproducible offline via `--selftest`/`parse_index(fixture)`; `acquire_fincen.py` reads the manifest (EFE fallback merged, backward-compatible); the widened pipe proven on a bounded batch (sampled raw→md); crawler documented in docstrings + README + CLAUDE; engine/ship artifact untouched (`git diff index.html` empty); `raw/` still gitignored, no bulk-md commit.
> Abort: if FinCEN's index can't be deterministically parsed from a saved page (JS-rendered / auth / anti-scraping) — PAUSE, report, pivot to a hand-curated manifest. Blocked >3 attempts on a task → ask user: skip or abort.

<!-- phase:phase-09-build-drift-guard -->
<!-- gate-log:phase-09 direction=approved delivery=accepted -->

## Phase 9: Build-drift guard (zero-drift invariant) — COMPLETED + accepted

Turn the M5 zero-drift invariant (committed `dist/<id>/index.html` == fresh build) — which silently broke in Phase 7, caught only by accident in Phase 8 — into a runnable, non-mutating guard wired into the smoke-checklist. Doc + build-script-glue only; engine (`index.html`) untouched, no config changes → all 3 `dist/` stay byte-identical. Mechanism = in-process `build.py --check` (render + byte-compare vs committed dist), pure-stdlib + git-agnostic. Direction approved by user 2026-06-05.

- [x] T1 · Add `--check` drift guard to build.py | scope: scripts/build.py | success: refactor extracts `render_one(typ, template) -> str` (validate + inline + self-contained guard, returns the output string) with a thin writer calling it; `main` accepts `--check` with an optional `all`/`<id>` arg; the check path byte-compares each render against the committed `dist/<id>/index.html`, prints a per-typology drift report, exits 0 when all match and non-zero on any mismatch or missing built file. VERIFY: `python3 scripts/build.py --check all` exits 0 on clean HEAD; a trivial un-rebuilt edit to one config makes it exit non-zero NAMING that typology (then revert); `python3 scripts/build.py all && git diff --exit-code dist/` clean (refactor output-neutral); `node --check` PASS on a freshly built dist | size: S
- [x] T2 · Wire guard into smoke-checklist + fix stale prose | scope: tests/smoke-checklist.md | success: the manual zero-drift prose item (≈L72, currently "build.py all reproduces both dist byte-identical") is replaced with the runnable `python3 scripts/build.py --check all` command; the stale "both dist"/2-typology count is corrected to all 3 typologies (fentanyl, trade-based, elder-financial-exploitation); the `git status --porcelain dist/` complement is noted. VERIFY: `grep -n "build.py --check all" tests/smoke-checklist.md` hits; `grep -niE "both dist|both typolog" tests/smoke-checklist.md` returns nothing | size: S
- [x] T3 · Document `--check` usage | scope: scripts/build.py, README.md | success: build.py module docstring Usage block lists the `--check [all|<id>]` mode with a one-line description; README "How to run" carries a one-line drift-guard command. VERIFY: `grep -n "\-\-check" scripts/build.py README.md` hits both files | size: S

> Exit (phase-09): `build.py --check all` exits 0 on clean HEAD, non-zero + names the typology on un-rebuilt drift · `build.py all && git diff --exit-code dist/` clean (output-neutral) · smoke-checklist carries the runnable `--check all` guard, no stale "both dist"/2-typology count, 3 typologies + `git status --porcelain dist/` complement · `--check` in build.py docstring + README · `git diff index.html` empty.
> Abort: if making the guard non-flaky requires the build to become deterministic in a way that CHANGES dist bytes (build is non-deterministic) — PAUSE and report; that's outside this slice. Blocked >3 attempts → ask user: skip or abort.

<!-- phase:phase-08-doc-true-up -->
<!-- gate-log:phase-08 direction=approved delivery=accepted -->

## Phase 8: Doc true-up + provenance fix (M6 debt) — COMPLETED + accepted

Close the three doc/provenance debts deferred from Phase 7. Unifying thread: the always-loaded docs must tell the truth about what the shipped artifact now is and cites. Doc/config-string only — engine (`index.html`) untouched (`git diff index.html` empty). Direction approved by user 2026-06-04; provenance fix = remove unverifiable FinCEN cites, attribute fentanyl to FINTRAC.

- [x] T1 · Rebrand branded `Signal Engine` → `Signal Watch` in docs | scope: CLAUDE.md, HANDOFF.md, README.md, tests/smoke-checklist.md | success: `! grep -rIn 'Signal Engine' CLAUDE.md HANDOFF.md README.md tests/smoke-checklist.md` — doc H1 → "Signal Watch — AML Vision Demo"; smoke-checklist:31 header line matches the shipped "Signal Watch" brand; lowercase technical "engine" preserved (no `sed` blind replace) ✓ PASS (4 sites: CLAUDE/HANDOFF×2/README H1 + smoke-checklist:31; technical "engine" intact 4/15/4)
- [x] T2 · Amend the paraphrase non-negotiable with the FinCEN-verbatim public-domain exception | scope: CLAUDE.md, HANDOFF.md | success: CLAUDE.md non-negotiables + HANDOFF §4.4 both state advisory text is paraphrased by default EXCEPT FinCEN federal advisories (public domain, 17 USC §105, reproduced verbatim WITH attribution, kept visually separate from the illustrative badge), and that this does NOT extend to FINTRAC (Crown copyright → still paraphrase); `grep -qi '17 USC' CLAUDE.md HANDOFF.md && grep -qi 'verbatim' CLAUDE.md HANDOFF.md` ✓ PASS (both bullets rewritten with default-rule + FinCEN-only exception + FINTRAC carve-out; CLAUDE.md cite cleared in the same rewrite)
- [x] T3 · Fix fentanyl provenance — attribute to FINTRAC only | scope: config/typologies/fentanyl.json, CLAUDE.md, HANDOFF.md, README.md (+ tests/smoke-checklist.md via sweep) | success: `! grep -rIn 'FIN-2019-A006\|FIN-2024-A002' config/ CLAUDE.md HANDOFF.md README.md` — fentanyl attributed solely to the FINTRAC Jan-2025 Operational Alert in `anchor.source` + all three docs; plus reframing sweep for any other prose still describing the pre-verbatim/paraphrase-only world ✓ PASS (5 sites: fentanyl.json:15, HANDOFF:100+:120, README:64; DISCOVERY via sweep: 2 more bad-cite sites in tests/smoke-checklist.md:62+:75 also fixed; fentanyl.json still valid JSON. README:38/:63 paraphrase lines left intact — TRUE for the fentanyl/trade-based typologies README covers)
- [x] T4 · Rebuild dist + verify (config content changed) | scope: dist/**, scripts/build.py | success: `python3 scripts/build.py all` clean; self-contained guard reports 0 tokens; `node --check` on the inlined engine PASS; `git diff --stat index.html` empty (engine untouched) ✓ PASS (build all 3 clean; guard 0 forbidden tokens ×3; node v22 --check PASS ×3; no external refs beyond Google Fonts; index.html diff empty). DISCOVERY (user-approved scope widen): rebuild revealed Phase 7 committed STALE dist/fentanyl + dist/trade-based (missing engine highlights/esc()/.hl/460px feature; only elder was current) → all fresh dist staged to restore the `build.py all == committed dist` invariant; noted as DISCOVERY for the commit message

> Exit (phase-08): no branded "Signal Engine" in the four docs · paraphrase non-negotiable carries the FinCEN-only verbatim exception · unverifiable fentanyl FinCEN cites removed (FINTRAC sole attribution) across config + docs + smoke-checklist · all 3 dist rebuilt clean (engine drift corrected; user-approved) · engine diff empty. ALL EXIT CRITERIA MET (working tree; delivery gate pending acceptance).

<!-- phase:phase-07-pipeline-walking-skeleton -->
<!-- gate-log:phase-07 direction=approved delivery=accepted -->

## Phase 7: Pipeline walking skeleton (M6) — COMPLETED + accepted

Thin vertical slice of the "Signal Watch" ingestion pipeline on ONE real FinCEN advisory (EFE FIN-2022-A002 — 24 enumerated red flags, cleanest single-signal derivation), end to end: acquire PDF → convert PDF→markdown (persisted as source of truth) → hand-derive ONE schema-valid signal config → render the FULL verbatim advisory in Act 1's existing SOURCE DOCUMENT panel. Authoring-time vs ship-artifact split is load-bearing: scraper/converter/derivation are build-time tools (output persisted + inlined); the ship file stays single-file, offline, zero runtime deps, NO fetch. "Signal Engine" → "Signal Watch" rebrand rides along. FinCEN-only verbatim exception (17 USC §105) — does NOT extend to FINTRAC.

- [x] T1 · Acquire the EFE FIN-2022-A002 advisory PDF (authoring-only) | scope: scripts/**, data/fincen/raw/** | success: `head -c4 data/fincen/raw/fin-2022-a002.pdf | grep -q '%PDF' && test -s data/fincen/raw/fin-2022-a002.pdf && ! grep -qiE 'fetch\(|XMLHttpRequest|import\(' index.html` — raw PDF persisted (non-empty, starts with %PDF); acquisition runs at authoring only, NO runtime fetch added to the ship file | size: S ✓ PASS (824,692 bytes, %PDF-1.7; kebab filename per project id convention; engine untouched)
- [x] T2 · Convert PDF→markdown + quality CHECKPOINT (de-risk gate) | scope: scripts/**, data/fincen/*.md | success: `test -s data/fincen/fin-2022-a002.md && grep -qiE 'red flag|indicator' data/fincen/fin-2022-a002.md` — markdown exists, non-empty, verbatim text + enumerated red-flag list readable/intact on manual inspection; converter confined to scripts/ + documented, NOT in the ship file. GATE: if markdown is mangled (column-soup / broken list), switch converter (markitdown↔pymupdf4llm) before proceeding to T4 | size: S ✓ PASS (markitdown[pdf] in gitignored uv venv — homebrew py3.14 pyexpat is broken, so authoring runs on uv-managed py3.12; 47KB/793 lines; all 24 red flags intact + in order; corpus is advisory-named fin-2022-a002.md; minor running-header/page-number artifacts, body kept verbatim/reproducible)
- [x] T3 · Make `advisory_full` a first-class field + rebrand to Signal Watch | scope: index.html, config/schema.md, scripts/build.py | success: `python3 scripts/build.py fentanyl && python3 scripts/build.py trade-based && grep -q 'Signal Watch' index.html && grep -qi 'advisory_full' config/schema.md && grep -qi 'advisory_full' scripts/build.py` — Act 1's SOURCE DOCUMENT panel renders config.advisory_full in a bounded scrollable region (max-height + overflow-y) with a "public domain · verbatim · FinCEN FIN-2022-A002" attribution VISUALLY DISTINCT from the "Illustrative data & outputs" badge; validateConfig() defaults advisory_full so existing fentanyl/trade-based configs still render; build.py validates advisory_full at the boundary; "Signal Watch" in header + <title> + schema/validateConfig defaults; schema.md documents advisory_full + the brand default; both existing configs still build clean | size: M ✓ PASS (engine: ADVISORY_FULL const + validateConfig default + act1/streamAdvisory branch (docfull scrollable, .docsrc attribution distinct from badge); build.py: optional advisory_full validation + text_file→inline resolution (md stays single source of truth); schema documented; rebrand in <title>+header+JS default, 0 'Signal Engine' left in dist. NOTE: doc rebrand (README/HANDOFF/CLAUDE/smoke-checklist) + paraphrase→verbatim non-negotiable change = deferred doc true-up, out of T3 scope by design)
- [x] T4 · Hand-derive the schema-valid EFE typology config from the markdown | scope: config/typologies/elder-financial-exploitation.json | success: `python3 scripts/build.py elder-financial-exploitation` validation passes; exactly one indicator and one candidate with `target:true`; the target candidate has a full signal `definition`; advisory_full carries the verbatim EFE text; the derived signal traces to an enumerated red flag in data/fincen/fin-2022-a002.md (traceable, hand-authored — NOT auto-extracted) | size: S ✓ PASS (target S-DORMANT-DRAIN-ELDER ← md line 507 "Dormant accounts with large balances begin to show constant withdrawals" + line 516; 1 indicator target IND-02, 1 candidate target C2 (gap+available) with full definition; advisory_full via text_file→inline (47KB verbatim, text_file dropped from dist); dist 94KB self-contained, guard held)
- [x] T5 · Build + offline file:// verification | scope: dist/**, scripts/build.py | success: `python3 scripts/build.py elder-financial-exploitation` produces dist/elder-financial-exploitation/index.html and the self-contained guard passes (no fetch/script-src/module introduced); opening it from file:// shows the verbatim advisory rendered scrollable in Act 1, the Act 0 coverage map + both human gates + combination-lift reveal intact, the illustrative badge present AND separated from the verbatim-source attribution, no console errors; result recorded | size: S ✓ DETERMINISTIC PASS (build all 3 OK; self-contained guard 0 tokens all 3; node --check on inlined engine PASS — template-literal edit sound; docfull/.docsrc/bounded-scroll CSS present; badge vs 'public domain · verbatim' distinct; EFE carries 47K verbatim, legacy falls back; both gates + lift + 7 act renderers intact). Live browser file:// render + console = human sign-off (delivery gate) — opened for Jake; Playwright deliberately skipped, runtime is a human check per M3/M5 precedent.

> Exit (HANDOFF §1.2 + phase-07): EFE FIN-2022-A002 PDF acquired → data/fincen/<id>.md (source of truth) → schema-valid config/typologies/elder-financial-exploitation.json hand-derived → Act 1 renders the FULL verbatim advisory (attributed, 17 USC §105) scrollable + separated from illustrative badge → "Signal Watch" rebrand → all three built dist run offline from file://, no console errors. Authoring deps stay in scripts/, out of the ship file.

<!-- phase:phase-06-ship -->
<!-- gate-log:phase-06 direction=approved delivery=accepted -->
<details>
<summary>Phase 6: Ship (M5) — COMPLETED + accepted (doc/verify only, zero engine edits)</summary>

Finalize for stage: true-up two stale + fentanyl-only docs to post-M3 / per-typology reality, then run the compliance + offline `file://` hard gate against BOTH built dist. M4 (live/pre-gen) skipped by decision. Closing "ask" slide + rename + Playwright deferred. Human sign-off = delivery gate.

- [x] T1 · Parameterize `tests/smoke-checklist.md` per typology | scope: tests/smoke-checklist.md | success: `! grep -q 'dist/index.html' tests/smoke-checklist.md && grep -qi 'reduced-motion' tests/smoke-checklist.md && grep -qi 'trade-based' tests/smoke-checklist.md` — stale single-file path gone, M3 controls now active verify items, per-typology fill table covers both typologies ✓ PASS
- [x] T2 · Refresh `README.md` (M3 shipped, both typologies) | scope: README.md | success: `! grep -qi 'planned for M3\|Next: M3\|Next.*M3' README.md && grep -qi 'Esc' README.md && grep -qi 'trade-based' README.md` — status reflects shipped M3 controls (←/→/Space/Esc/↺/reduced-motion); compliance names both fentanyl AND trade-based advisory sources ✓ PASS
- [x] T3 · Compliance self-check + offline `file://` verification (HARD GATE) | scope: dist/**, tests/smoke-checklist.md | success: `for f in dist/fentanyl/index.html dist/trade-based/index.html; do grep -q 'Illustrative data' "$f" || exit 1; done && ! grep -riE 'api[_-]?key|secret|token|password|sk-' dist/` — badge present every act both dist, advisories paraphrased+attributed, no secrets, no real-data patterns; both open from file:// offline, no console errors; pass/fail recorded in checklist. ✓ PASS (zero drift; runtime render carries from M3 on byte-identical dist; record in smoke-checklist.md)

> Exit (HANDOFF §1.2): README run/present/add-typology · compliance self-check passes · both dist run offline from file:// · smoke-checklist parameterized · human sign-off (Jake, at delivery gate).

</details>

<!-- phase:phase-04-presenter-polish -->
<!-- gate-log:phase-04 direction=approved delivery=accepted -->
<details>
<summary>Phase 4: Presenter polish (M3) — COMPLETED + accepted (engine-only)</summary>

Pure-engine M3: keyboard nav + reset + reduced-motion. NO config/schema edits. Speaker notes deferred. Chrome (macOS) target.

- [x] T1 · Centralize nav + reset + keys — `advance()`/`back()`/`reset()`; keys (→/Space/←/Esc) honor both gates via the `nextBtn.disabled` guard; ↺ Reset control + key legend; `reset()`→clean Act 0 (`maxReached=0`), shared by Esc + Act 6 "Run again"
- [x] T2 · prefers-reduced-motion — CSS `@media` (duration:0s, keeps `.sig` forwards-fill) + `REDUCED` flag (synchronous `T()`, `animVal` short-circuit); every reveal lands final in one paint
- [x] T3 · Rebuild both dist + verify — `build.py all`; both self-contained; behavioral harness on both dist × both motion modes (gates hold, no Act 5 without confirm, Esc resets, 0 pending timers under reduced-motion); real Chrome 149 renders Act 0

> M3 exit MET: keys-only nav, both gates intact, reduced-motion final-state, both dist self-contained. Engine intentionally edited (M2 zero-diff rule did not carry over); `config/` + `build.py` byte-identical.

</details>

<!-- phase:phase-03-multi-typology -->
<details>
<summary>Phase 3: Multi-typology (M2) — COMPLETED + accepted (commit 61a9cca)</summary>

Second typology = **Trade-based ML (TBML)**; switch = **build-time** (`dist/<id>/index.html`). Engine untouched.

- [x] T1 · `config/typologies/trade-based.json` authored — TBML from paraphrased public advisories (FinCEN Apr-2025, FATF 2024); within schema; price-anomaly target (gap+available); 2%/42% asymmetry as advisory highlight
- [x] T2 · `scripts/build.py` — per-typology `dist/<id>/index.html` + boundary validation (fails loud; caught a schema-doc bug: hints length); `all` mode; stale single-file removed
- [x] T3 · TBML verified — 7 acts render, target-derived signal `S-PRICE-ANOMALY-TRADE`, self-contained, inlined CONFIG deep-equals config
- [x] T4 · Regression + no-engine-edits proof — `git diff index.html` EMPTY (engine untouched); fentanyl dist byte-identical to baseline; both typologies pass validation

> M2 exit criteria met: 2 typologies, build-time switchable, **zero engine edits**. The schema generalized cleanly (one doc fix, no engine change).
> DISCOVERY: schema doc said `hints[7]`; baseline carries 8 (trailing unused). Fixed validator to steps==7 / next_labels,hints ≥7.

</details>

<!-- phase:phase-02-config-driven-refactor -->
<details>
<summary>Phase 2: Config-driven refactor (M1) — COMPLETED (commit 99899ad)</summary>

- [x] T1 schema · T2 fentanyl.json · T3 generic engine · T4 defensive · T5 build.py · T6 verified (byte-identical to baseline)

</details>

<!-- phase:phase-01-bootstrap -->
<details>
<summary>Phase 1: Bootstrap (M0) — COMPLETED (commit c56b82e)</summary>

- [x] git init · project docs · baseline imported + verified · committed

</details>

<!-- phase:future -->
<details>
<summary>Future phases / backlog (plan when active)</summary>

**Future candidate · Elder presentation-values true-up (discovered Phase 9; deprioritized at the Phase-11 gate, not dropped)** — the smoke-checklist per-typology expected-values table (≈L15, 2 columns) and compliance attribution (≈L62) still cover only fentanyl + trade-based; `elder-financial-exploitation` (shipped Phase 7) has no walk-row. Needs elder's derived values (coverage %, signal `S-DORMANT-DRAIN-ELDER`, fire-stats, lift bars, delta chip). Doc-slice work. (Planner recommended finishing this first; user chose AUTOMATE at the Phase-11 gate.)
**Future candidate · Re-point fentanyl to the discoverable `fin-2024-a002` Supplemental Fentanyl Advisory (discovered Phase 10; deprioritized at the Phase-11 gate, not dropped)** — verbatim-upgrade the fentanyl typology onto the now-manifest-discoverable FinCEN advisory.
**~~Phase · Automate article→signal derivation (discovered M6)~~ → NOW Phase 11 (ACTIVE, AUTOMATE).**
**Phase · Optional manifest `--fetch` refresh cadence (discovered Phase 10)** — periodic re-crawl of the FinCEN index to refresh `data/fincen/index.json`. Manual authoring run; never in smoke/CI.

</details>
