# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-09 by /dev-debrief (Phase 40 — Live red-flag extraction quality (measure-first) — DELIVERED, all 6 lite tasks T1–T6 [x] (T5 SKIPPED-WITH-REASON: the sectioning trigger didn't fire), READY FOR COMPLETION pending the delivery gate. The Phase-38 entity playbook applied to FLAGS: a 12-commercial (6 cal/6 hold, LOCAL-ONLY/never committed) + 7-federal stress corpus → deterministic proxies + a BLIND second-rater reference extraction (consensus, never ground truth): residue = RECALL on commercial longreads (covR 0.40 vs federal 0.67; covQ 0.80–0.86 everywhere — precision healthy); clusters C1–C4 user-adjudicated at the T2 checkpoint; the user's registry-scoring REFRAME (covQ measures existence, not extraction quality) → SYSTEM_PROMPT rewritten: 20-family mechanism-registry checklist + granularity contract + committed-record exemplars + the 12-200→[12,240] prompt/gate drift fixed TO the gate (THREE calibration rounds — r3 regressed and was ROLLED BACK, r2 shipped; HOLDOUT eval #1 of a 2-eval budget, closed at 1: covR 0.40→0.55, agreement 0.54→0.62, mechCovR 0.46→0.63, positional decay ELIMINATED second-half miss 0.64→0.43; federal layer unregressed agr 0.73→0.74) → shared-gate dup-collapse: news_ground gains `flag_dup_key` (normalized quote + normalized category) — live ground_record DROPs (first survives), build validate_news_data CHECKs fail-loud; same-quote-DIFFERENT-category KEPT, both fixture-pinned; the 4 committed records pass CLEAN, replay goldens valid WITHOUT regeneration → 3 NEW `.ph40` federal fixtures (10 total, goldens deterministic) + docs/news-live.md `## Red-flag quality` + smoke-checklist + CLAUDE.md in place. FULL REGATE GREEN: --check all 5/5 ZERO DRIFT (NO client/news.html change — Python-side only), node news-stream 90 + corpus, all selftests, news_live_test (system + .venv + --live real-Qwen smoke), remeasure.json assertion PASS. DISCOVERIES: the negative-news corpus has NO `extraction: full` frontmatter (2,256/2,314) — the selection filter was rebuilt on the committed news_fetch standardize→verify gate + a measured anti-listing rule, still deterministic; ~/.claude/enforce hooks arrived MID-PHASE → satisfied via /spec --internal (specs/phase-40-live-redflag-quality.md, nana:approved; `.claude/.memory-consulted` is an untracked session marker — never commit it). Assumption revisit: A1/A3/A4 held, A2 held-with-deviation. Open residue → Phase-41 candidates: semantic instance-dups (15/33 reworded retellings — a batched keep-biased merge pass or next prompt iteration) · a denial-quote prompt micro-rule · a committed registry-scoring harness. Prior: Phase 39 — Live news QOL DELIVERED + accepted + committed 3786042 + 3c35902 + pushed to main: /extract is ALWAYS an NDJSON stage stream (HTTP/1.0 body-until-close per-line write+flush; extract(on_progress=None) kept the replay fixtures green without re-capture) + one-shot URL acquisition via companion-only scripts/news_fetch.py (fetch ladder urllib→curl→markitdown + standardizer + article-shape VERIFIER; D5 a rung wins only by passing the verifier; D6 cookie + one same-host meta-refresh; measured 42.7s end-to-end, 16 entities + 8 flags grounded). Prior: Phase 38 — Consolidate the live news subsystem DELIVERED + accepted + committed 7df3ce4 + f27f99b + pushed to main: verified the live Qwen agent backend + captured replay fixtures, PIVOTED entity precision from an overfit denylist to context shaping [subjects-only system prompt] after a stress test on 3 new articles, wired in a keep-biased second-pass verify on-by-default, completed the Phase-36 watchlist loop with a view+prune [companion-only, offline dist/news byte-frozen]; news-stream 76→81, --check all 5/5 zero drift, all selftests green, NO non-negotiable change. Prior: Phase 37 — Per-indicator typology for the corpus Typologies lens DELIVERED + accepted + committed 798fe28 + pushed, 5 lite tasks T1–T5 [one L = T2]; the lens groups each DOC by one money-laundering typology, collapsing 7 of the 10 FINTRAC sector pages into a catch-all `fintrac-sector-baselines`; Phase 37 tags typology PER INDICATOR via a NEW overlay `data/indicator-typology-map.json` [the 56 derived records stay byte-frozen], MEASURE-FIRST [the T0 risk: cross-cutting indicators may relocate the catch-all to indicator level], assign-per-unique-text → blind-agreement → human cluster adjudication → rewire the lens to group by indicator typology. Phase 36 — Persistence (DuckDB→parquet) + feedback watchlist DELIVERED + accepted + committed e96f31d + 71e0989, pushed to main; Phase 35 — News live local-model backend committed + pushed to main 4408dd9; Phase 34 closed + committed 83a79c3 + pushed to main. — Phase 34 recap: verified + corrected the 1,376 NEW capability(C)/data_source(D) assignments Phase 33 added — the ONE neural step, gated only for vocabulary VALIDITY, never CORRECTNESS — MEASURE-FIRST and HUMAN-adjudicated, a DATA-correctness phase (only the 14 new derived records changed; corpus.html/build.py are data-driven). T1 a deterministic consistency audit (no LLM) → 30.5% (419/1,376) in same-text-different-code contradictions (the FINTRAC sector-page common spine). T2 [L] a BLIND re-assignment workflow over the 589 UNIQUE TEXTS (a method refinement — fewer judgments + one canonical code per text prevents fresh inconsistency) → INTER-RATER AGREEMENT C 74.4% / D 77.9% / both 63.9% (reported as agreement, NEVER "proven correct" — a 2nd LLM pass is consensus, not ground truth). T3 cluster + human disposition (243 dispositions; two user RULINGS — adverse-media C19/D13 ≠ KYC C14/D8, cash C5/D2 ≠ PEP C17; ambiguous clusters kept existing). T4 a byte-surgical apply (ph34_apply.py + a synonym-aware straggler pass) reusing ph33_apply.py's deterministic downstream → 213 indicators corrected (114 C-moves + 129 D-moves + 3 client/customer wording-drift stragglers); flag/red_flag/section/src_line/id byte-identical; consistency 30.5% → 2.0%. T5 rebuilt dist/corpus (~4.88MB) + full regate + docs. 10 of 14 new records changed. --check all 5/5 ZERO DRIFT; --selftest PASS; 56/56 --check-derived clean; corpus harness 235 + news 65 (both byte-frozen); frozen set byte-clean. The measured agreement + 213 corrections are a journal/quality artifact, NOT a new demo number (the always-on illustrative badge stays the only claim). FROZEN byte-clean: the showcase (index.html + config/** + 3 typology dists), the entire news stream, data/typology-map.json, data/capability-taxonomy.json, derive_signals.py, build.py, and the 42 existing records / 875 old indicators. NO non-negotiable change. Phase 33 (corpus completeness + re-segmentation) closed (committed 823c0c2 + a post-phase QOL polish b7d942c, pushed to main).)

## Recommended Next Action

**Phase 40 (live red-flag extraction quality) is DELIVERED — all 6 tasks [x] (T5 skipped-with-reason), full regate green; run the DELIVERY GATE next.** Present the delivery gate, commit + push to main (the commits-phases-to-main convention), then flip `delivery=accepted` post-commit-verify (delivery-flow D3). Commit hygiene: `.claude/.memory-consulted` is an UNTRACKED session marker — do NOT commit it; commercial stress captures (`.dev-wiki/tmp/ph40/`, `data/news/.live/`) stay gitignored/local-only; `specs/phase-40-live-redflag-quality.md` + the 3 new `.ph40` fixture pairs DO commit. After acceptance, `/dev-plan` Phase 41 — candidates: the Phase-40 residue (a batched semantic instance-dup merge pass [15/33 reworded retellings] · a denial-quote prompt micro-rule · a committed registry-scoring harness · a registry-aligned second-rater for sharper catAgr) + the carried set (FINTRAC /intel/ depth · a third jurisdiction AUSTRAC/UK · the CLAUDE.md trim — NOW 279 lines, still growing · verify-latency batching). NOTE: the global enforce hooks (~/.claude/enforce) are now active — future phases need an approved spec BEFORE implementation edits; consider folding /spec --internal into this project's lite dev-plan flow.

**Phase 39 (live news QOL) DELIVERED + accepted + committed 3786042 + 3c35902 + pushed to main (2026-06-09).** Streamed stage-level progress: `/extract` is ALWAYS an NDJSON stage stream (received → [fetching → converting] → extracting → grounding → verifying i/N → done); HTTP/1.0 body-until-close made per-line write+flush stream with NO chunked framing — the polling fallback was never needed (D7); `extract(on_progress=None)` preserved the Phase-38 fixture seam (zero re-capture). One-shot URL acquisition: NEW companion-only `scripts/news_fetch.py` — fetch ladder (urllib→curl→markitdown) + deterministic standardizer + article-shape verifier, with D5 THE LADDER RULE (a rung wins only by passing the verifier — the justice.gov Akamai-interstitial discovery) + D6 the interstitial two-step (cookie + one same-host meta-refresh, 2.5KB guard → 102KB article, verified live); `/extract` takes {url} OR {text} (text wins, D8; the early `converted` event fills the textarea = the recovery path per A1's accept-with-condition). Measured live end-to-end: 42.7s, 16 entities + 8 flags grounded, 0 dropped (treasury.gov jy2735). Health: news-stream 81→90 + corpus green; `--check all` 5/5 ZERO DRIFT; news_live_test PASS (system + .venv + `--live` real-Qwen smoke); `news_fetch --selftest` NEW PASS dep-free (+ real markitdown under .venv); all selftests PASS. Assumption revisit: A1 BIT (the condition proved load-bearing — it VALIDATED the design), A2–A4 held. Impl decisions D5–D8 recorded in the journal (lite ceremony).

**Phase 38 (consolidate the live news subsystem) DELIVERED + accepted + committed 7df3ce4 + f27f99b + pushed to main.** Verified the live Qwen agent backend end-to-end over all 4 committed DOJ/OFAC articles + captured `<id>.qwen.json`/`<id>.golden.json` fixtures under `tests/fixtures/news-live/` + dep-free offline replay (parse→build→ground→screen, 34 drops reproduced, no model) + an opt-in `--live` smoke OFF the default run. **The headline pivot:** a deterministic noise denylist (T2, hit OFAC 33→14 on 4 calibration articles) OVERFIT — a stress test on 3 NEW federal articles (T4) showed 38/69 entities still noise (it can't enumerate the open vocabulary of officials/prosecutors/agencies). Adopted CONTEXT SHAPING instead: a SUBJECTS-ONLY rule in `serve_news.SYSTEM_PROMPT` (Qwen noise 38→~3, generalizes) + trimmed `news_ground.screen_entities` to its structural rules (alias-dedup/source-line/judicial/moniker) + dropped the denylist. **Qwen-vs-Opus verdict:** under the same prompt + same pipeline a well-prompted LOCAL Qwen ≈ Opus (35 vs 29 entities) — the gap was INSTRUCTION CLARITY, not capability; NO API/Opus backend needed (keeps offline/no-key). A keep-biased second-pass entity verify is WIRED IN on-by-default (`serve_news.verify_entities`, LIVE-only, fail-OPEN=KEEP, layered ON TOP of the deterministic `build_record`; `--no-verify-entities` disables) — the naive forced-choice variant lost 6 real designated parties; the keep-biased variant drops residual institutional noise with ZERO subject loss. **Half B (watchlist loop):** `news_store.prune(name)` un-escalate + a `POST /watchlist/prune` route + a `news.html` live-region watchlist panel (escalated surface + provenance + a ✕ Prune per row) — companion-only, inside the stripped live region. Tests: news-stream 76→81; `--check all` 5/5 ZERO DRIFT (offline `dist/news` byte-identical); news_ground/serve_news/news_store/derive_signals `--selftest` PASS; node corpus green. The phase grew 5→7 tasks (T2 + T4 added mid-phase per user adjudication). NO dist/corpus/showcase changes; build.py still never imports the live/store layer. NO non-negotiable change. Decisions D1–D3 (Phase 38, lite, below).

**Phase 37 (per-indicator typology) DELIVERED + accepted + committed 798fe28 + pushed to main.** The corpus Typologies lens now groups by INDICATOR typology: a multi-typology FINTRAC sector page distributes across the real typology clusters; `corruption` (293 ind / 11 docs) + `terrorist-financing` (169 ind / 14 docs) are CROSS-JURISDICTION US+Canada; `fintrac-sector-baselines` RETIRED → an honest `cross-cutting-indicators` bucket (746 ind). New corpus frozen baseline `dist/corpus/index.html` (~4.98MB); corpus harness 239; news 81 (Phase 38). The `data/indicator-typology-map.json` overlay carries the per-indicator typology; all 56 derived records + `derive_signals.py` stayed byte-frozen.

**DEFERRED candidates (offered, not chosen — for a future /dev-plan):** the derivable FINTRAC /intel/ frontier (OA001 tax-evasion-real-estate + sanctions-evasion SB + Russia-linked-ML SB + dual-use advisory — genuine source scale, the Phase 22/23 extend pattern) · a third jurisdiction (AUSTRAC CC BY / UK OGL — the standing scale frontier now the Canadian source frontier is exhausted) · a CLAUDE.md trim pass (NOW 279 lines, over the ~200 maintenance-contract target and more pressing — suggested as a Phase-N hygiene half-task) · a recorded-fixture for the second-pass verify + a verify-latency optimization (batch/smaller model — A3 held: Phase 39 made the wait LEGIBLE, not shorter) + growing the replay-fixture corpus · a structural pre-H1 head-matter strip in news_fetch's standardizer (the .gov "official website" banner survives into the model input + grounding surface — harmless today; one STRUCTURAL rule, NOT an enumerated denylist per the Phase-38 lesson) · URL-mode batch material: the negative-news wiki holds 2,313 raw articles with source_url frontmatter (a bulk-scan/feed phase — the M8 "compose" north-star adjacency). · Phase-40 residue: a batched keep-biased semantic instance-dup MERGE pass (15/33 of calibration's unmatched flags were reworded retellings) · a denial-quote prompt micro-rule (never quote a denial/rebuttal as evidence) · a committed registry-scoring harness (category agreement / span tightness / mechanism coverage — currently gitignored scratch) · sectioned extraction IF a future phase re-measures positional decay on >30K-char material.

## Active Phase

**[[phase-40-live-redflag-extraction-quality|Phase 40: Live red-flag extraction quality (measure-first)]]** (status: ready-for-completion, 100% — all 6 tasks [x], T5 skipped-with-reason; delivery gate PENDING, flips post-commit per the delivery flow). Phase 39 (Live news QOL) DELIVERED + accepted + committed 3786042 + 3c35902 + pushed to main.

Entry criteria: MET (Phase 39 delivered + accepted + committed + pushed. The live companion's flag extraction passes the deterministic faithfulness/grounding gate, but completeness / span quality / translation register / consistency are UNGUARDED — no structured prompt guidance for flags, no second pass — and the user sees inconsistent flags in real live tests. Direction user-set at the Phase 39 delivery gate. Known drift: SYSTEM_PROMPT says red_flag 12–200 chars vs the news_ground gate [12,240]. rf_region does NOT transfer to news — no enumerated red-flag list — so completeness has no deterministic anchor; hence the blind second-rater design. See Recent Decisions Phase 40 D1–D5.)
Exit criteria: measured characterization (deterministic proxies — flag count, span lengths, near-dup rate, grounded-drop rate, register shape — PLUS a BLIND second-rater reference extraction → per-dimension inter-rater agreement reported as CONSENSUS, never ground truth, no accuracy number presented as real) over a ~12-commercial-article stress corpus (negative-news wiki, deterministic frontmatter filter, calibration/holdout split; captured outputs LOCAL-ONLY) + the 7 committed federal fixture articles, user-adjudicated at the T2 checkpoint; SYSTEM_PROMPT context-shaped (few-shot exemplars from committed gate-passing records + aml-wiki mechanism vocabulary; prompt char bound moved TO the gate [12,240]) with HOLDOUT improving or holding; measurement-earned deterministic flag-quality checks in the SHARED `news_ground.py` (companion + build.py), the 4 committed news records passing the extended gate (failures adjudicated, never loosened), goldens regenerated deterministically from the pinned qwen.json captures; the conditional batched keep-biased per-flag verify built ONLY on measured residue (`--no-verify-flags` off-switch; skipped-with-reason otherwise); replay fixtures green WITHOUT re-capture + NEW captured US-federal fixtures under the new prompt; `--check all` 5/5 zero drift (dists byte-identical — Python-side only); node news-stream + corpus harnesses green; all `--selftest`s green; the always-on badge stays; NO non-negotiable change.

Progress: 100% (6/6 tasks — T1 stress corpus + baseline capture [x] · T2 measure + USER CHECKPOINT [x] · T3 SYSTEM_PROMPT context-shape [x] (r2 shipped after r3 rollback; holdout improved) · T4 shared-gate dup-collapse [x] · T5 [SKIPPED-WITH-REASON — the sectioning trigger didn't fire] · T6 regate + 3 new .ph40 fixtures + docs [x]). READY FOR COMPLETION pending the delivery gate.

## Active Phase Contract

Phase: 40 - Live red-flag extraction quality (measure-first)
Tasks: 6 (see tasks.md) — T1 (M) stress corpus + baseline capture: ~12 commercial articles from the negative-news wiki raw/ via deterministic frontmatter filter (extraction: full + source_score floor), calibration/holdout split; run the live pipeline (text-mode from stored bodies; URL-mode where links resolve) over them + the 7 committed fixture articles as federal baseline; captured outputs gitignored/LOCAL-ONLY, nothing new committed under data/ or tests/ · T2 (M) MEASURE: deterministic proxies (flag count, span lengths, near-dup rate, grounded-drop rate, register shape) + a BLIND second-rater reference extraction (Claude, blind to Qwen's output) → per-dimension inter-rater agreement (completeness/span/register) + divergence clusters; USER CHECKPOINT — adjudicate clusters, call the T5-verify residue AND which deterministic checks earn gate status · T3 (M) context-shape `serve_news.SYSTEM_PROMPT`: structured flag guidance + few-shot exemplars from committed gate-passing records (e.g. doj-ravenell red_flags) + mechanism vocabulary (aml-wiki money-laundering-red-flags register); fix the 12-200→[12,240] prompt/gate drift (prompt moves TO the gate); re-run calibration AND holdout, report before/after agreement delta · T4 (M) measurement-earned deterministic flag-quality checks (e.g. near-dup collapse, span bounds) in `news_ground.py`, SHARED by companion + build.py's validate_news_data; the 4 committed records must pass (a failure = an adjudicated FINDING, never a silent loosening); goldens regenerated deterministically from the pinned qwen.json captures · T5 (S, CONDITIONAL on the T2/T3 checkpoint) batched keep-biased per-flag verify mirroring verify_entities (fail-OPEN=KEEP, LIVE-only, atop deterministic build_record, `--no-verify-flags` off-switch, ONE batched call — not N) · T6 (S) regate + docs: NEW captured US-federal fixtures under the new prompt; docs/news-live.md; smoke-checklist; CLAUDE.md `## Current state` IN PLACE.
Transition: continue (lite). UNFREEZE: `scripts/serve_news.py`, `scripts/news_ground.py` (measurement-earned checks only), `scripts/build.py` (the shared-gate consequence only), `tests/news_live_test.py`, `tests/fixtures/news-live/**` (goldens regenerated deterministically + NEW US-federal fixtures), `docs/news-live.md`, `tests/smoke-checklist.md`, CLAUDE.md (T6 — in-place `## Current state` snapshot edit), `.dev-wiki/tmp/**` + `data/news/.live/**` (local-only measurement artifacts), `.claude/rules/active-phase.md`.
FROZEN byte-clean: the six-act showcase (`index.html` + `config/**` + the 3 typology dists), the ENTIRE corpus (`corpus.html` + `dist/corpus` + all source dirs + every derived record + the 3 overlays), the grounding core `derive_signals.py`, `news.html` + the OFFLINE `dist/news` (NO client change this phase — Python-side only, dists stay byte-identical), `scripts/news_fetch.py` + `scripts/news_store.py` (logic-stable). build.py NEVER imports the live layer (it DOES import news_ground by design — that's the shared gate). Replay fixtures green WITHOUT re-capture (golden regeneration is deterministic from the pinned qwen.json; T6 ADDS fixtures, US-federal-only). Captured commercial stress outputs LOCAL-ONLY/never committed (Phase-39 compliance posture). Agreement reported as CONSENSUS, never ground truth; NO accuracy number presented as real; the always-on badge stays; NO non-negotiable change.
Abort: an extended gate check failing a committed record is a FINDING to adjudicate, never a silent loosening. Holdout degrades under the new prompt → roll back + report (never tune on holdout — the Phase-38 overfit trap). The blind second-rater design can't yield a meaningful signal → present that honestly at the T2 checkpoint, don't invent a metric. Blocked >3 attempts on a task → mark [blocked: …] + ask the user: skip or abort.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| D1 (Phase 40 DIRECTION) = user-set at the Phase 39 delivery gate: improve LIVE red-flag extraction quality, measure-first (URL/text-mode stress corpus → characterize → context-shape → verify only if residue). The grounding gate guards faithfulness only; completeness/span/register/consistency are the unguarded dimensions. Carried candidates deferred again: FINTRAC /intel/ depth, third jurisdiction (AUSTRAC/UK), CLAUDE.md trim, verify-latency batching. | high | 2026-06-09 |
| D2 (Phase 40 MEASUREMENT — A3' as accepted after round-1 reject) = deterministic proxies (flag counts, span lengths, near-dup rate, grounded-drop rate, register shape) PLUS a BLIND second-rater reference extraction (Claude, blind to Qwen's output) over the same stress articles → INTER-RATER AGREEMENT per dimension (completeness/span/register); divergence clusters human-adjudicated at a T2 checkpoint; agreement reported as CONSENSUS, never ground truth; NO accuracy number presented as real; the conditional-verify trigger is a measured agreement delta. (Phase 34/38 D2 method applied to flags.) | high | 2026-06-09 |
| D3 (Phase 40 PROMPT METHOD — A1/A2) = context-shape serve_news.SYSTEM_PROMPT with few-shot exemplars drawn from the COMMITTED gate-passing news records (e.g. doj-ravenell red_flags — real exemplars per the anchor-style-to-reference rule) + mechanism vocabulary (aml-wiki money-laundering-red-flags register: structuring, rapid movement no economic purpose, shell/opaque ownership, intermediary misuse…); fix the prompt/gate drift (prompt says 12–200 chars, gate enforces [12,240] — prompt moves TO the gate); calibration/holdout split over the stress corpus guards the Phase-38 overfit trap. Stress corpus: ~12 commercial articles selected from the negative-news wiki's 2,313 raw files by deterministic frontmatter filter (extraction: full + source_score floor) run text-mode from stored bodies (URL-mode opportunistically where links resolve) + the 7 committed federal fixture articles as baseline; captured outputs LOCAL-ONLY/never committed; fixture promotion stays US-federal-only (Phase-39 compliance posture). | high | 2026-06-09 |
| D4 (Phase 40 GATE — A4' as accepted after round-1 reject) = news_ground.py UNFREEZES: measurement-EARNED deterministic flag-quality checks (e.g. near-dup collapse, span-quality bounds) land in the SHARED gate (companion + build.py's validate_news_data). The 4 committed news records must pass the extended gate — a failure is an adjudicated FINDING, never a silent loosening; replay goldens regenerate DETERMINISTICALLY from the pinned qwen.json captures (no model re-capture); full regate incl. --check all 5/5; dists stay byte-identical (Python-side only). | high | 2026-06-09 |
| D5 (Phase 40 CONDITIONAL VERIFY) = a keep-biased per-flag second-pass verify ONLY if measured residue remains after T3+T4 (user's call at the T2/T3 checkpoint): mirrors Phase-38 verify_entities (fail-OPEN=KEEP, LIVE-only, layered atop deterministic build_record, --no-verify-flags off-switch) but BATCHED — one model call over all flags, not N calls (the per-entity verify loop is already the wall-time majority; Phase-39 A3 held: legible, not shorter — don't double it). | high | 2026-06-09 |
| D1 (Phase 39 DIRECTION) = user-set objective: fix the no-feedback extraction UX + replace paste with direct URL input. Both confined to the live subsystem (companion-served, dev/authoring-time only); offline artifacts untouched. | high | 2026-06-09 |
| D2 (Phase 39 PROGRESS ARCHITECTURE) = real stage-level chunked-NDJSON progress over the existing `POST /extract` (stdlib server-side; fetch+ReadableStream client-side — EventSource is GET-only, so POST flows use fetch+ReadableStream; WebSockets unjustified). Stage granularity chosen because the per-entity verify loop is the wall-time majority — token streaming REJECTED (raw JSON isn't user-meaningful + misses the verify loop), client-only spinner REJECTED (no stage truth). Job-id+polling is the DESIGNED fallback if stdlib chunked flush fails under ThreadingHTTPServer (verify at impl). `extract(on_progress=None)` preserves the Phase-38 replay-fixture seam (call_llm stubbing, deterministic build_record) — fixtures green without re-capture. | high | 2026-06-09 |
| D3 (Phase 39 ACQUISITION) = ONE-SHOT URL→extract (the user rejected preview-then-run, round 1), mitigated by: a NEW companion-only `scripts/news_fetch.py` multi-method fetch LADDER (urllib browser-like headers → curl subprocess → markitdown convert(url); bot guards expected) + a deterministic STANDARDIZER (strip nav/link-list/image boilerplate, collapse whitespace) + a VERIFIER gate (min prose length/ratio; bot-guard/captcha/paywall/JS-wall detection) BEFORE extraction; the converted text streams back in an early event and fills the textarea (trim + re-run = the passive recovery); honest streamed failure → paste fallback; markitdown `.venv`-only lazy import + graceful degrade (the news_store/DuckDB pattern). Acquisition proposes, the scripted gate disposes — the project spine, applied to fetch. | high | 2026-06-09 |
| D4 (Phase 39 INVARIANTS) = strip/byte-identical (`/*LIVE_START*/…/*LIVE_END*/` → offline dist/news BYTE-IDENTICAL, `--check news` zero drift) + build.py-never-imports-the-live-layer (news_fetch companion-only) + the fixture seam + the always-on badge ALL carry forward unchanged (accepted at the assumption gate). | high | 2026-06-09 |
| D1 (Phase 38 DIRECTION) = CONSOLIDATE the Phase-35/36 LIVE news subsystem (verify the agent backend + watchlist management) rather than EXTEND source scale. The user chose consolidate/durability at the dev-plan gate. Rationale: the live backend had never been exercised against a real model (`call_llm` stubbed with a CANNED dict); the Qwen model (Qwen3.6-35B-A3B, llama.cpp) is now LIVE on 127.0.0.1:8080 and a planning smoke confirmed it end-to-end (4 entities + 5 grounded flags in 17.6s; the gate dropped a 6th ungrounded ellipsis flag) — so verification is a CAPTURE-AND-LOCK, and the escalated-only watchlist (Phase 36) has no view/prune UI (it only grows). Alternatives considered + DEFERRED: FINTRAC /intel/ depth (OA001 + sanctions-evasion/Russia-ML/dual-use SBs) and a third jurisdiction (AUSTRAC CC BY / UK OGL breadth). Consistent with the memory note that Jake picks engineering rigor/durability when no scale frontier is pressing. | high | 2026-06-08 |
| D2 (Phase 38 EVALUATION/HONESTY) = the agent-backend evaluation is TWO-LAYER: (1) deterministic GROUNDING = faithfulness (automatic; catches hallucination), PLUS (2) a CLAUDE-REFERENCE comparison reported as INTER-RATER AGREEMENT (consensus, NEVER Claude-as-ground-truth; the user adjudicates the divergences; NO accuracy/precision/recall number presented as real; the always-on "Illustrative data & outputs" badge stays). Mirrors the Phase 34/37 agreement-as-consensus + human-adjudication method. Rationale: grounding gate ≠ completeness gate ≠ correctness gate — the honesty constraint forbids a fabricated accuracy figure, and a second LLM rater is consensus, not truth. Capture = run the real Qwen over the committed DOJ/OFAC articles → commit each raw output as a fixture under `tests/fixtures/news-live/` → offline replay tests (parse→build→ground, dep-free) pin the contract incl. the ungrounded-elision drop + an opt-in `--live` smoke OFF the default run (the `.venv`-gated DuckDB pattern). | high | 2026-06-08 |
| D3 (Phase 38 ARCHITECTURE) = the watchlist VIEW + prune is COMPANION-ONLY; the OFFLINE `dist/news` stays BYTE-FROZEN. All new live + watchlist-view code (the panel + the prune wiring) lives inside `/*LIVE_START*/…/*LIVE_END*/` so `render_news` STRIPS it → dist/news byte-identical, zero network/store code offline (screens the synthetic book only). build.py NEVER imports the live/persistence layer (`serve_news`/`news_store`/`news_ground` stay companion-only). A `news_store` prune/un-escalate method + a `serve_news` prune route + a `news.html` live-region panel complete the Phase-36 feedback loop (escalate grows the surface; prune shrinks it — the human Disposition gate is now bidirectional + inspectable). Mirrors the Phase 36 strip + no-store-on-the-ship-path invariants. | high | 2026-06-08 |
| D1 (Phase 37 DIRECTION) = fix the corpus Typologies lens by tagging typology PER INDICATOR (not per doc). The lens groups each DOC by one typology; 7 of 10 FINTRAC sector pages collapse into a catch-all `fintrac-sector-baselines` because a sector page enumerates indicators across MANY typologies. The user (eyeballing the BUILT demo, the recurring reframes-to-output-quality move) chose this PROPER fix over (a) a lite relabel/lens-note of the catch-all and (b) adding the derivable FINTRAC /intel/ special-bulletin scale (OA001 + 3 SBs/advisories) — both offered + rejected at the gate. Investigation established the FINTRAC SOURCE side is complete (11 per-sector pages all acquired, 10 derived, crown-agents skipped; STR + sanctions guidance pages enumerate nothing). Consistent with corpus-is-the-primary-demo + ground-judgments-in-a-user-interview (audit/adjudicate the neural tag, never assume correctness). | high | 2026-06-08 |
| D2 (Phase 37 METHOD/ARCHITECTURE) = MEASURE-FIRST + OVERLAY + AGREEMENT-HONEST. (1) MEASURE-FIRST (the T0 risk: many indicators are cross-cutting — structuring serves drug-trafficking AND fraud AND TF — so per-indicator-one-typology may merely RELOCATE the catch-all to indicator level): the T1 probe sizes section→typology determinism + the cross-cutting fraction and CHECKPOINTS the model (single primary typology / multi-label set / honest indicator-level "sector-general" bucket) before any tagging. (2) OVERLAY: a NEW `data/indicator-typology-map.json` (indicator-id→typology) carries the assignment so ALL 56 derived records + `derive_signals.py` stay BYTE-FROZEN; build-time an indicator's typology = overlay value ELSE inherits its doc's typology-map value (the ~1,231 single-typology indicators inherit free; only the ~1,020 sector-page indicators, ~589 unique, need neural assignment); validated at the build boundary (closed vocab + referential integrity), where typology-map/capability-taxonomy validation already lives. (3) AGREEMENT-HONEST (the Phase-34 guard): assign per UNIQUE text → blind re-assignment → an INTER-RATER AGREEMENT rate (consensus, NEVER "proven correct") → the user adjudicates ambiguous CLUSTERS; the rate is a journal/quality artifact, NOT a new demo number. Combined coverage stays honest union arithmetic; no lift/similarity/cross-regulator dedup; the always-on badge stays. T2 is a Workflow candidate (the Phase 33/34 fan-out). | high | 2026-06-08 |
| D1 (Phase 36 DIRECTION/SCOPE) = persistence (DuckDB→parquet) + the feedback watchlist, ESCALATED-ONLY. REFINES D4 (which recorded "book ∪ all prior-scanned"): the watchlist = book ∪ entities the analyst ESCALATES at the existing Disposition gate, NOT every scanned entity. Rationale (the user chose this at the dev-plan gate): a domain-honest CURATED watchlist (not a dump of incidental names that degrades screening precision), it makes the existing human Disposition gate CONSEQUENTIAL (escalate → future scans catch it), and it closes the "working instance of the loop, not a dramatization" arc D1/Phase-35 named. Alternative rejected: "all-scanned per D4" (simplest wiring but compounding noise). | high | 2026-06-08 |
| D2 (Phase 36 ARCHITECTURE) = a new `scripts/news_store.py` OWNS DuckDB — build.py NEVER imports it. Normalized schema scans / entities(+disposition column) / red_flags; native DuckDB `COPY … (FORMAT parquet)` export. DuckDB → the existing gitignored uv `.venv` (companion/authoring-time only, never ship); store file + parquet exports gitignored (the `data/*/raw/` convention). New companion endpoints `GET /watchlist` (book ∪ escalated, reconciled to `{name,type,kind,provenance}`) + `POST /disposition {scan_id,entity_id,decision}` + a `--export-parquet <dir>` CLI flag. All client wiring inside `/*LIVE_START*/…/*LIVE_END*/` → stripped offline → dist/news byte-identical. The companion store is OPTIONAL at startup (graceful warn+disable if duckdb missing). Concurrency: serialize writes (a lock / single-writer connection — ThreadingHTTPServer `/extract` appends are concurrent). | high | 2026-06-08 |
| D3 (Phase 36 HONESTY) = honesty invariants carry forward — real client-side fuzzy scores, synthetic book, the always-on "Illustrative data & outputs" badge, nothing shown that isn't grounded in a submitted source (prior-scanned watchlist entries are REAL grounded extractions). Escalation provenance shown in the screen/exposure view ("escalated from <article>, <date>"). Escalation can target ANY extracted entity (proactive watchlisting), not only book-matches. | high | 2026-06-08 |
| D1 (Phase 35 DIRECTION) = the M8 news stream gains a LIVE local-model backend for REAL-TIME on-demand extraction (the long-anticipated "live mode" the non-negotiable already sanctions — optional, isolated, off by default, scripted fallback). The user steered here at the dev-plan gate, off the queued C/D-quality candidates. Backend = llama-cpp serving a Qwen ~30B-A3B-class MoE (model swappable via the OpenAI-compatible /v1 endpoint). Real-time = on-demand SUBMIT (paste/pick an article), not a continuous feed. Split into two phases: Phase 35 = the live-extraction BACKBONE (screen vs the static book); Phase 36 = persistence + the feedback watchlist. This is the project's first move from a scripted DRAMATIZATION to a working INSTANCE of the loop. | high | 2026-06-08 |
| D2 (Phase 35 ARCHITECTURE) = SERVED-BY-COMPANION (user choice). A thin stdlib Python companion `scripts/serve_news.py` serves news.html over http://localhost AND proxies llama-cpp — same-origin → NO CORS/mixed-content. The offline single-file `dist/news` stays the default + scripted fallback, BYTE-IDENTICAL: the live branch lives in a marked `/*LIVE_START*/…/*LIVE_END*/` region that `render_news` STRIPS for the offline build, so the offline file keeps ZERO network code and the existing self-contained `die("fetch(")` guard holds (the strip actually STRENGTHENS the non-negotiable). GROUNDING STAYS IN PYTHON server-side — a shared stdlib `scripts/news_ground.py` (normalize + raw/normalize-substring grounding + the article-body transform), reused by BOTH build.py's gate AND the companion; live model output is JSON-schema/GBNF-constrained then DETERMINISTICALLY GROUNDED (ungrounded drops) — "LLM proposes, the gate disposes", moved to runtime. NOT re-ported to JS (REVISED from the direction-gate's initial "port to JS" framing — the companion choice makes server-side grounding the clean, DRY answer). news_ground.py is stdlib grounding PRIMITIVES, NOT the authoring/LLM layer: build.py STILL never imports derive_signals/markitdown/an LLM client. ZERO new pip deps this phase (stdlib + urllib). | high | 2026-06-08 |
| D3 (Phase 35 HONESTY) = the "LLM proposes, the deterministic gate disposes" spine, moved to runtime. Live model output is schema-constrained then server-side grounded (an entity name / attribute / red-flag `flag` must be a RAW substring of the submitted article body AND normalize-substring; `red_flag` present/distinct/bounded) — ungrounded items DROP, nothing shown that isn't in the source. Fuzzy scores stay REAL (client-side Jaro-Winkler). The always-on "Illustrative data & outputs" badge stays; the book stays synthetic; entities come from real public-domain articles or user-pasted public/synthetic text. NO fabricated output. | high | 2026-06-08 |
| D4 (Phase 35→36 PERSISTENCE, direction recorded) = entity store = companion → DuckDB (row-level appends) → parquet EXPORT (raw single-file parquet isn't append-friendly; DuckDB gives appends + parquet as the interchange format). Watchlist (Phase 36) = book ∪ all prior-scanned entities — the SCREEN surface GROWS each scan (a real compounding capability). DuckDB is the one new pip dep (Phase 36, in a venv, never in the ship path). Phase 35 screens vs the STATIC book; Phase 36 wires the store + the feedback. | high | 2026-06-08 |
| D1 (Phase 34 DIRECTION) = VERIFY the 1,376 new C/D assignments. At the dev-plan priority gate the user picked this over "add a 3rd jurisdiction" and "a proper Sector axis" (2026-06-08). Rationale: Phase 33 tripled the corpus on UNVERIFIED neural assignments — the capability(C)/data_source(D) tagging was the ONE neural step in Phase 33, gated only for vocabulary VALIDITY (in-set; ph33_apply.py reported 0 flagged), NEVER correctness. Measure/correct the neural step before scaling further (measurement before optimization). Ties to the ground-judgments-in-a-user-interview principle (audit CORRECTNESS, not just validity) — and the memory "a grounding gate ≠ a completeness gate; audit recall, not just faithfulness": the grounding gate checks the verbatim flag's faithfulness, never the C/D tag. The C/D codes drive status/data/build_rec/build_logic AND the Phase-29 capability + Phase-30 data-source executive lenses, so a mis-assignment silently shows a wrong posture in the primary demo. | high | 2026-06-08 |
| D2 (Phase 34 METHOD) = MEASURE-FIRST + HONEST-AS-AGREEMENT. The verification is a deterministic consistency audit (no LLM, sizes the problem) + a BLIND neural re-assignment (BLIND to the existing code) reported as an INTER-RATER AGREEMENT rate (consensus), NEVER as ground-truth correctness — the key honesty guard against a second-LLM pass becoming false confidence. The user adjudicates DISAGREEMENTS at the CLUSTER level (the user is the accepted truth source, the Phase-28 interview model). No new demo number: the agreement rate is a journal/quality artifact; the always-on illustrative badge stays the only claim. The byte-surgical apply reuses ph33_apply.py's deterministic downstream (cover×data matrix + per-capability build_logic templates) — flag + red_flag byte-identical, only C/D + their deterministic consequences move; do NOT re-author build_logic neurally. | high | 2026-06-08 |
| D3 (Phase 34 SCOPE) = the 14 NEW derived records only (1,376 new indicators). The 42 EXISTING records / 875 old indicators stay FROZEN — they had the fuller Phase-28 interview treatment (the C/D codes there came from the structured 28+20 y/n/partial interview). A DATA-correctness phase: corpus.html + build.py are data-driven, so corrected codes flow through on rebuild — NO UI feature work, NO build change. Folding the 875 old indicators in is a FOLLOW-UP, only IF T2's measured agreement rate on the new ones comes back ugly. | high | 2026-06-08 |
| Phase 33 = CORPUS COMPLETENESS (maximal) + FULL TYPOLOGY RE-SEGMENTATION, workflow-driven. The user (Phase-32 gate) reframed to fixing the BUILT corpus's incomplete SOURCE SET — a DIFFERENT defect from Phase-28's within-doc extraction completeness (there each doc shipped too few of its OWN flags; here the corpus is missing whole DOCUMENTS): missing FinCEN advisories "especially the latest" + FINTRAC "way way off — so much in the Obligations/Guidance section we didn't pull". At the Phase-33 goal gate the user chose MAXIMAL on BOTH axes: FINTRAC = "All 11 sector pages" (over financial-entities-master-only / master+distinctive-tails), and typology = "Full vocabulary review" (over TBML-targeted). Consistent with the prioritizes-scale-over-showcase-polish posture. The user explicitly called for "a dedicated dynamic workflow" — the acquire→convert→derive→ground fan-out over ~16 new docs (5 FinCEN + 11 FINTRAC, roughly DOUBLING the corpus ~875 → ~1500+ indicators) + the typology-map re-segmentation is exactly a Workflow. Research-confirmed gaps: FinCEN = a discovery-WINDOW miss (crawler floor 2020, no re-crawl since 2026-03) → FIN-2026-A002 (latest, 18 red flags) + FIN-2019-A006/A005/A003 + FIN-2018-A003 (PEP, 14 red flags); skip the superseded 2020 COVID cluster. FINTRAC = a source-AREA miss (all 10 current docs are /intel/ strategic-intel; ZERO from /guidance-directives/ — the 11 sector "ML/TF indicators" pages, 50-100+ indicators each, HTML not PDF, heavy shared-spine duplication). | high | 2026-06-08 |
| Phase 33 ARCHITECTURE: new FINTRAC guidance lives in a NEW `data/fintrac-guidance/` dir + a NEW CORPUS_SOURCES registry entry in build.py (the Phase-20 multi-source pattern — keeps the OA dir `data/fintrac/` + its 10 derived records BYTE-FROZEN; multi-source via the merge, not a migration). Needs a new HTML→md acquisition path (guidance pages are HTML, not the Phase-22 `<page>.pdf` sibling); authoring-time only, the ship stays offline. `derive_signals.py` (the grounding core, byte-frozen since Phase 17) is touched ONLY IF the FINTRAC guidance heading form doesn't ground under the existing `_RF_HEADER_FINTRAC` anchors — and ONLY as a regression-gated rf_region anchor ADD (the Phase-21/22/23 precedent: every existing rf_region byte-unchanged, all 42 existing records still `--check-derived` clean, the grounding logic/`normalize`/`check_record` byte-unchanged). If it grounds as-is, the core stays byte-frozen. NOT a non-negotiable change either way (both bases established: FinCEN US-federal §105 + FINTRAC Crown-copyright non-commercial-with-attribution). | high (arch); medium (heading-grounds-as-is, resolved in T2) | 2026-06-08 |
| Phase 33 HONESTY: new-indicator coverage posture is INHERITED-or-INTERVIEWED, never fabricated — new indicators inherit status/data/build_rec deterministically from their capability(C1-C28)/data_source(D1-D20) code via the user's Phase-28 interview posture + the cover×data matrix; the derivation workflow FLAGS any indicator that maps to no existing C/D code, and flagged ones are resolved by a SHORT targeted y/n/partial interview that extends `data/capability-taxonomy.json` with GROUNDED posture (ground-judgments-in-a-user-interview; honesty-over-demo-drama). Zero fabricated posture. DEDUP honesty: the 11 sector pages share a common spine → ingested as DISTINCT per-doc publications (each separately attributed), the overlap DISCLOSED, NEVER claimed as cross-source de-duplication (the Phase-24 gate — indicators are not matched/de-duped across sources, which would need fabricated matching); within-doc duplicates removed byte-surgically (the Phase-28 precedent). | high | 2026-06-08 |
| (Phase 32 decisions — NEWS-STREAM ELEVATION + REAL GOV-ENFORCEMENT SOURCE SWITCH (lite, DELIVERED + accepted + committed afb24f4): the user reframed away from the four offered forward lanes to FIXING THE BUILT ARTIFACTS — the news stream "repeats the staged-slideshow mistake" (the Phase-27 corpus failure); "do both" → split by frozen set into Phase 32 = news + Phase 33 = corpus. LOAD-BEARING SUB-DECISION: the user asked to switch to REAL online news + "ignore copyright, non-commercial"; PUSHBACK held + reframed — verbatim commercial news = copyright reproduction + defamation of real NAMED persons + undercuts the demo's own compliance story + a HANDOFF §4 edit; the CLEAN path = US-FEDERAL GOV-ENFORCEMENT (DOJ/OFAC/Treasury-FinCEN/SEC), PUBLIC DOMAIN 17 U.S.C. §105 (the corpus's exact basis, Phase 21), the MOST AML-relevant adverse media. Chose "Gov enforcement — verbatim"; the COUNTERPARTY BOOK STAYS SYNTHETIC (#4 held — REAL adverse-media entity × SYNTHETIC book). PRESENTATION ELEVATION to the corpus's bar (streaming Read + entity cards + a visible scan PROCESS + the full dossier theme). See the `[[2026-06-08-phase-32-news-real-source-elevation|Ph32]]` journal.) | — | 2026-06-08 |
| Phase 31 = a SCOPE EXPANSION (the user reframed from the three offered lanes — third jurisdiction / corpus dedup-gate guard / eyeball-the-built-demo): open a SECOND atom stream over AML-relevant news — entity extraction + adverse-media red-flag identification + fuzzy-match against a client/counterparty table for exposure. Two sub-decisions at the goal gate: (a) news source = SYNTHETIC illustrative (fictional entities) over paraphrased-real-adjudicated / hybrid — cleanest, zero defamation/copyright surface, lets the near-match + false-positive trap be DESIGNED IN so the fuzzy-match + human-gate beat lands honestly under the always-on badge; (b) shape = WALKING SKELETON as a NEW STANDALONE artifact dist/news (from news.html, mirroring how dist/corpus was added) over folding into the corpus explorer / a full-M8-now build — keeps the showcase + corpus byte-frozen and proves the ONE new muscle (entity extraction → fuzzy-match → exposure + human disposition; article-reading/highlighting/build-time grounding are corpus-proven). Architecture: build-time committed data/news/{articles,derived,book} grounded via normalize-substring + validated at the BUILD BOUNDARY (validate_news_data in build.py — derive_signals.py byte-FROZEN); runtime = a pure client-side Jaro-Winkler+token-sort matcher (REAL scores, offline, file://). The M8 north star (the adverse-media atom COMPOSING with the transaction-signal atom) is NAMED + scoped OUT. Grounded in-session: AUSTRAC confirmed as the standout third jurisdiction (enumerated sector indicator guides, CC BY — NOT this phase); aml-wiki carries the adverse-media + entity-resolution domain articles. HONESTY: synthetic data + badge; REAL fuzzy scores; honest counts; NO fabricated number. build.py edited ADDITIVELY; the showcase + the entire corpus + the grounding core stay byte-frozen. | high | 2026-06-08 |
| Phase 30 = the data-source lens (D1–D20) ONLY, chosen at the goal gate. The user first reframed to "data-source lens + OSFI as a new source." A read-only feasibility check DISPOSED OSFI: OSFI Guideline B-8 is principles-based SUPERVISORY guidance that DEFERS to FINTRAC/FATF for the actual indicators — it publishes NO enumerated red-flag list of its own, so it cannot be a red-flag corpus source honestly (deriving from it would reproduce FINTRAC's indicators [cross-doc reproduction, explicitly avoided in Phase 23] or fabricate). OSFI's compliance basis IS fine (non-commercial reproduction with attribution — the same Crown-copyright family as FINTRAC). The user then asked for "another Canadian regulator to complete the multi-jurisdiction setup"; a documentation + landscape check found NO clean second Canadian indicator-source exists — FINTRAC is effectively Canada's SOLE publisher of enumerated ML/TF indicators (OSFI, CIRO, AMF all defer to it). "Complete the multi-jurisdiction setup" honestly points to a THIRD jurisdiction (AUSTRAC [CC BY] or UK [Open Government Licence] were offered as the best derivable + cleanly-licensed candidates). The user chose "No new source — D-lens only" — the certain, nearly-free win that completes the lens pair without source-add risk. | high | 2026-06-08 |
| Phase 30 needs NO new committed artifact and NO build.py change — the Phase-29 taxonomy already carries the `data_sources` axis (20 entries {id,name,desc,posture}; posture 9 y / 4 partial / 7 n) and build.py already validates (`validate_capability_taxonomy` enforces every live indicator's `data_source` resolves) + inlines `taxonomy.data_sources` + the per-indicator codes into `__CORPUS__`; corpus.html already has `DS_BY`/`DSRC`/`dsNum` helpers. The whole feature is `corpus.html` (a MIRROR of the capability mode onto the D-axis) + harness + docs + dist rebuild. A tighter phase than Phase 29 (which had to commit + thread the taxonomy). The frozen set GROWS to include `data/capability-taxonomy.json` AND `scripts/build.py` (both byte-untouched this phase) on top of the standard frozen set. | high | 2026-06-08 |
| Phase 29 = capability-lens UI (chosen at the goal gate). A read-only grounding inspection showed the two engineering-rigor candidates the Phase-28 debrief left behind are MOSTLY ALREADY DONE — exact-equality dedup already exists at `derive_signals.py:353-361` (indicator-id uniqueness + normalized-flag distinctness within a record; only the harder overlap/near-dup case remains, low ROI) and the full-motion streaming harness path already exists at `tests/corpus-explorer.test.mjs:654-685` (boots non-reduced-motion, `__drain`s the read chain, asserts progressive highlight + caret removal). Meanwhile the Phase-28 `capability`/`data_source` taxonomy from the interview is entirely UNUSED in the ship artifact. So the highest genuine value is surfacing the taxonomy as a capability lens — the executive buy-in view that answers coverage PER DETECTION CAPABILITY across the whole corpus (vs PER DOCUMENT today), cross-referenced against the institution's interview posture. Alternatives considered + rejected at the gate: gate/harness hardening (rigor lane, but the sliver is small after grounding); eyeball-the-built-demo-first (the user's recurring reframe lane). The user chose the capability lens. | high | 2026-06-07 |
| Phase 29 = commit the taxonomy as `data/capability-taxonomy.json` (the Phase-24 overlay pattern). The taxonomy labels + interview posture live only in `.dev-wiki/tmp` (intermediate, non-ship-grade) and `build.py` must NEVER read `.dev-wiki/`. Promote to a committed `data/capability-taxonomy.json` (code → {name, posture}, 28 capabilities + 20 data sources) validated at the BUILD BOUNDARY (closed vocab + referential integrity: every `capability`/`data_source` code in the 42 derived records exists in the taxonomy; posture ∈ {y,n,partial}; fail-loud) — where derived-shape + typology-map validation already live; the grounding core `derive_signals.py` and all 42 derived records stay BYTE-FROZEN. Alternative considered: two separate files (taxonomy labels vs posture) — rejected for the subtraction test (one file, one validator). PRE-VERIFIED this round: all 875 indicators carry both codes, every code used exists in the taxonomy, every used code has a posture — referential integrity holds, the abort precondition is satisfied. | high | 2026-06-07 |
(Phase 28 decisions — CORPUS COMPLETENESS + GROUNDED-COVERAGE INTERVIEW (lite, no decision articles): the user found the CORE defect Phase-27's assessment MISSED — verbatim red-flag EXTRACTION was grossly INCOMPLETE (the grounding gate verified each flag was REAL, never that we got them ALL; opioid doc shipped 15 of ~80 — audit RECALL not just faithfulness). Complete re-extraction 634→903, dedup →875; coverage moved FABRICATED→GROUNDED via a 28-capability+20-data-source taxonomy + the user's 28+20 y/n/PARTIAL interview (the SOURCE of the Phase-29 capability lens — the codes every record now carries); 28 templated build_logic; a FULL-MOTION STREAMING "agent reading" render; FINTRAC attribution RELOCATED to a per-doc page footer (non-negotiable HELD); "AML Corpus Explorer" branding; de-piped tables. Harness 148→165; grounding core byte-unchanged. See the `[[2026-06-07-phase-28-corpus-completeness|Ph28]]` journal.)
(Phase 27 decisions — SHIPPABILITY (lite): the user judged the BUILT Phase-26 corpus NOT shippable (Read-advisory "brutally bad"; build animation "not in place") and floated foregoing the grounding system. Chose ASSESS-ONLY-FIRST; a read-only 44-agent assessment DISPOSED the framing — the brutality was PRESENTATION not the grounding system (39/42 PRESENTATION_ONLY; register held; verbatim flags real). Chose broader re-extract at the build gate (faithfulness-guarded): cleanArticle + normalize-both-sides highlight (100%) + the Act-4 build-log/lift-side beats + whole-article progressive read + a 72-agent re-extraction tightening 121 over-long flags to crisp grounded sub-spans (transitive grounding; 36 long-but-faithful kept whole). Established "the corpus IS the primary demo". Grounding gate logic byte-unchanged; NO non-negotiable change. See the `[[2026-06-07-phase-27-corpus-shippability|Ph27]]` journal.)
(Phase 26 decisions — SHOWCASE-QUALITY ELEVATION (all-in, workflow-driven): reviewing the BUILT Phase-25 artifact the user named five quality gaps at once (prose-register red_flags, static article render, no Signal wow, ungrouped docs/red-flags, no landing). Chosen over substance-first / experience-first cuts. REGISTER BAR = the `red_flag` must read like the showcase's `config/typologies/fentanyl.json` indicators (terse, mechanism-named; Phase 25 missed it by never anchoring to fentanyl.json); SHOW-BOTH stays (verbatim beside the translation); only `red_flag` VALUES change (grounding + the SHAPE gate byte-unchanged). WOW-NUMBERS HONESTY (user-approved, REVERSES the Ph18 no-fabricated-lift call): the build-log animates the REAL build_logic; the combination-lift is a GENERIC illustrative TEMPLATE behind a loud "pending calibration" tag — NEVER 42 fabricated per-doc findings. See the `[[2026-06-07-phase-26-corpus-elevation|Ph26]]` journal.)
(Phase 25 decisions — CORPUS OUTPUT QUALITY (extract → translate): brought the corpus to the showcase's two-layer red-flag model — keep the grounded VERBATIM `flag` (evidence) + ADD a natural-AML `red_flag` TRANSLATION beside it + a per-doc "Read advisory" screen rendering the FULL article (grounded phrases highlighted → translated); user chose FULL RE-DERIVE NOW (all 42 docs) over exemplar-first / presentation-only + the corpus extract→translate + full-article cut over bundling the landing page. HONESTY MODEL = the verbatim stays the GROUNDED AUTHORITY shown BESIDE the red_flag, the grounding gate logic BYTE-UNCHANGED, the new check SHAPE-only (present/non-empty/distinct/12–240 chars) in BOTH check_record + validate_corpus_data; translation faithfulness the one accepted neural step (mitigated by show-both + the EFE oracle + per-doc re-check + the always-on badge), paraphrase the compliance DEFAULT → NO non-negotiable change. The full article inlined at the BUILD boundary (_inline_article/_strip_provenance), render_one + the 3 typology dists byte-frozen. See the `[[2026-06-07-phase-25-corpus-translation|Ph25]]` journal — and NOTE: Phase 26 found the Ph25 register MISS [never anchored to fentanyl.json].)
(Phase 24 decisions — CROSS-CORPUS SYNTHESIS: turned the 4-source/2-jurisdiction corpus into an analytical tool — group by TYPOLOGY, show COMBINED coverage across a cross-jurisdiction cluster ("no single advisory covers a typology; the combined corpus does"); user chose synthesis over navigability/durability/more-scale. HONESTY GATE (ties to the Ph18 precision-lift rejection) = combined coverage is honest UNION arithmetic + honest per-jurisdiction counts + per-indicator traceability; NO similarity/overlap/lift number. SUBTRACTION + GATE LOCATION = a SEPARATE committed overlay `data/typology-map.json` (22-term vocab + 42-entry map; jurisdiction from the source registry, not stored), NOT 42 derived-record edits → source dirs + grounding core byte-frozen; validated at the BUILD BOUNDARY (load_typology_map + validate_typology). 5 cross-jurisdiction + 2 cross-agency clusters + 11 singletons; harness 74→98. See the `[[2026-06-07-phase-24-cross-corpus-synthesis|Ph24]]` journal.)
(Phase 23 decisions — FINTRAC depth, grew source #4 from 3 → 10 strategic-intel products (9 OAs + 1 real-estate Operational Brief) for the Canadian-bank audience; the genuinely new gate work was the INVERTED "Indicators of <X>" anchor `_RF_HEADER_FINTRAC_INV` (two narrow branches + boilerplate exclusion; NOT the planned literal "TABLE OF INDICATORS" — a misread), 0/39 rf_region shift, grounding core byte-untouched, build.py/corpus.html UNCHANGED (data-driven); 225 ind / 50 BUILD_NOW honest yield, full lists kept, only the human-trafficking 2016-appendix dropped (cross-doc double-count); NO non-negotiable change (depth within the Ph22 Crown-copyright basis). Chose narrow-global-anchor widening over a source-scoped rf_region refactor (DEFERRED — a 5th heading form triggers it). See the `[[2026-06-06-phase-23-fintrac-depth|Ph23]]` journal.)
(Phase 22 decisions — FINTRAC as corpus source #4, the demo's FIRST cross-jurisdiction source (US Treasury FinCEN + OFAC → +Canada FINTRAC), via the VERBATIM-corpus path; a NARROW ML/TF-qualified "indicators" anchor add (`_RF_HEADER_FINTRAC`/`_RF_INTRO_FINTRAC`, 0-collision across all 36 mds, 0 FinCEN/OFAC shift); the verbatim non-negotiable extended to add FINTRAC as a Crown-copyright NON-COMMERCIAL reproduction LICENCE (NOT public domain — distinct from US-federal 17 USC §105; FINTRAC attribution required; not commercial redistribution; other non-US/non-FINTRAC still paraphrase) — user signed off "proceed"; hand-curated `<page>.pdf` acquisition (`_to_pdf_url` needed no tweak) + source-aware provenance + a 4-type SOURCE-AWARE menu. See the `[[2026-06-06-phase-22-fintrac-corpus-source|Ph22]]` journal.)
(Phase 21 decisions — OFAC as corpus source #3 WITH a regression-gated rf_region anchor widening ("Risk Indicators"/"Deceptive Practices"/"Risk Factors" + the risk-indicator intro; 0 FinCEN rf_region shift, all 29 records clean); the verbatim public-domain non-negotiable extended FinCEN-only → US-federal (17 USC §105 — FinCEN + OFAC + US federal/Treasury; FINTRAC still excluded then); hand-curated OFAC acquisition (JS-SPA, no crawl) via --source + a `_to_pdf_url` tweak + the parameterized issuer; honest 0-BUILD_NOW allowed (sanctions/vessel often non-FI-observable). See the `[[2026-06-06-phase-21-ofac-corpus-source|Ph21]]` journal.)
(Earlier Phase 20 decisions — scale via OTHER FinCEN publication TYPES (Alerts) inside the verbatim regime (NO non-negotiable change); the thin CORPUS_SOURCES registry decoupling source-id from storage dir (data/fincen/ byte-frozen, ready for {advisories, alerts, …ofac}); the gate reused UNCHANGED. See the `[[2026-06-06-phase-20-multi-source-spine|Ph20]]` journal.)
(Earlier Phase 18–19 decisions — see `[[2026-06-06-phase-19-durability-closeout|Ph19]]`, `[[2026-06-06-phase-18-corpus-explorer-arc|Ph18]]` journals. Load-bearing carry-overs: Ph19 DURABILITY CLOSEOUT — a committed ZERO-DEP Node `vm`+DOM-shim harness (`tests/corpus-explorer.test.mjs`, 28 assertions, NEVER jsdom — file:// offline ethos) that loads the COMMITTED `dist/corpus/index.html` so it doubles as a build-output smoke test (EXTEND this in Ph20 T5, don't replace); `_rf_triage` is PINNED + DISCLOSED NOT rewritten (an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today). Ph18 THE ARC — the corpus explorer's 5-screen arc (Select → Coverage → Build recs/GATE → Signal → Close the loop), div-toggle human gate folded into Build-recs (NOT `<input>` — keyboard-safe), COVERAGE close-the-loop payoff via `coverageIndex()` NOT precision combination-lift (precision-lift EXPLICITLY REJECTED — records carry no precision/lift numbers; porting it fabricates ~12 per-advisory stats, violating "never present synthetic numbers as real"). The Ph20 alert records reuse this arc unchanged per record.)
(Earlier Phase 16–17 decisions — see `[[2026-06-06-phase-17-complete-corpus-derivation|Ph17]]`, `[[2026-06-06-phase-16-invert-extraction|Ph16]]` journals. Load-bearing carry-overs: Ph17 the REAL subtraction — `extract_red_flags` + the whole `--scaffold`/`--draft`/`--scaffold-derived` authoring stack + `anthropic`/`os` DELETED (1202→600, stdlib-only); the inverted loop is the SOLE derivation path; `_rf_triage` counter reuses the `rf_region` span; corpus 12/14 live (only the 2 FATF non-derivable). Ph16 INVERTED the extraction boundary — the LLM EXTRACTS, the deterministic layer GATES (normalize(flag) ⊂ normalize(md) = the traceability authority, replacing src_line∈extractor) + a coarse `rf_region()` section-cite relevance guard + the cover×data matrix + BUILD_NOW⇒full-build_logic; corpus-status SHAPE preserved; `derivable: false` = the 2 FATF advisories.)
(Earlier Phase 12–15 decisions — see `[[2026-06-05-phase-15-harden-extraction-faithfulness|Ph15]]`, `[[2026-06-05-phase-14-scale-corpus-derivation|Ph14]]`, `[[2026-06-05-phase-13-corpus-explorer|Ph13]]`, `[[2026-06-05-phase-12-fincen-corpus-derivation|Ph12]]` journals. Load-bearing carry-overs: Ph15 harden-FAITHFULNESS (footnote-resume; glued-no-separator stays FLAGGED, no safe deterministic split; derived records store RAW text, `corpus.html` esc() the sole escaper); Ph14 PURE-AUTHORING scaling (build.py makes an advisory live by the presence of `derived/<id>.json`); Ph13 standalone STAGED 4-screen corpus explorer (`dist/corpus/` via `corpus.html`, all-14-honest status, six-act showcase byte-frozen); Ph12 SINGULAR corpus-backed demo (user picks 1 of 14; per-advisory `derived/*.json` the analytical artifact, the 3 typologies stay the showcase), build.py DECOUPLED from authoring (reads committed corpus-status.json + derived/*.json, never imports derive_signals.py), LLM backend = THIS session not `--draft`, validate at the build boundary.)

(Earlier Phase 7–11 decisions: see `[[phase-11-automated-derivation]]`, `[[phase-10-fincen-corpus-crawler]]`, `[[phase-08-doc-true-up]]` + journal. Load-bearing carry-overs: Phase 11 AUTOMATE → variant B (LLM-drafted signal NOW), boundary-preserving (LLM proposes; build.py + schema + 2 human gates dispose); authoring-time vs ship-artifact split (build-time tools; ship file stays single-file/offline/no-fetch, HANDOFF §4/§4.5); validate at the build boundary (deterministic validator, fail loud); FinCEN-only verbatim public-domain exception (17 USC §105, NOT FINTRAC); lite ceremony.)

## Blockers and Open Questions

- [RESOLVED 2026-06-09 · planning, by Phase-40 D2–D5] Phase-40 STRESS-CORPUS SELECTION / MEASUREMENT RUBRIC / FEW-SHOT BUDGET / FIXTURE POSTURE — all four resolved at the assumption gate (A1/A2 accept round 1; A3/A4 reject→revised→accept round 2): selection = deterministic frontmatter filter over the negative-news wiki (extraction: full + source_score floor) → ~12 commercial articles, calibration/holdout split, text-mode from stored bodies (URL-mode opportunistic), captured outputs LOCAL-ONLY/never committed, fixture promotion US-federal-only (D3); measurement = deterministic proxies + a BLIND second-rater agreement per dimension, consensus-honest, divergence clusters human-adjudicated at the T2 checkpoint, the conditional-verify trigger a measured agreement delta (D2); exemplars = real committed gate-passing records + aml-wiki mechanism vocabulary, granularity/budget resolved empirically at T3 (the one carried knowledge gap; D3); fixtures = goldens regenerate DETERMINISTICALLY from the pinned qwen.json (no re-capture), T6 ADDS new US-federal fixtures under the new prompt, and the per-flag verify (if the checkpoint calls residue) mirrors verify_entities but BATCHED — one call, not N (D4/D5).
- [RESOLVED 2026-06-09 · planning, by Phase-39 D1–D4] Phase-39 PROGRESS-FEEDBACK MECHANISM / URL-FETCH DEPENDENCY POSTURE / URL FLOW SHAPE / COMPLIANCE NOTE — all four resolved at the assumption gate: progress = streamed stage-level chunked NDJSON over `POST /extract` (fetch+ReadableStream client; job-id+polling the designed fallback; NO token streaming) with `extract(on_progress=None)` preserving the fixture seam (D2); markitdown follows the news_store lazy-import graceful-degrade pattern, `.venv`-only, inside a NEW companion-only `scripts/news_fetch.py` fetch ladder (D3); flow = ONE-SHOT URL→extract (user rejected preview-then-run) mitigated by standardizer + verifier gate + the early converted-text event filling the textarea for trim + re-run (D3); compliance = a docs/news-live.md posture note in T5 (URL fetch local/dev-only; fetched text never committed/redistributed; public/US-federal posture for fixture promotions) — no non-negotiable change (D4).
- [OPEN · planning, gated at Phase-38 T1 CHECKPOINT] Phase-38 QWEN-VS-CLAUDE DIVERGENCE ADJUDICATION — the Half-A evaluation reports a Qwen-vs-Claude INTER-RATER AGREEMENT + the actual divergences (which entities/flags one rater caught and the other missed) over the committed DOJ/OFAC articles. The Claude reference is a SECOND rater, NOT ground truth; the user adjudicates the divergences at the T1 checkpoint before the goldens lock. NO accuracy/precision/recall number is presented as real; the always-on badge stays. Resolved at the checkpoint (T1), not blocking the capture itself.
- [RESOLVED 2026-06-08 · planning, by Phase-38 D1/D2/D3] Phase-38 CONSOLIDATE-VS-SCALE / EVALUATION SHAPE / OFFLINE-STRIP INTEGRITY — all three resolved at the dev-plan gate: the user chose to CONSOLIDATE the live news subsystem (D1; verify the agent backend + watchlist management) over extending source scale (FINTRAC /intel/ depth or a third jurisdiction, both DEFERRED) — reframed by the NEW FACT that the Qwen model is LIVE on 127.0.0.1:8080 and a planning smoke confirmed the backend end-to-end (4 entities + 5 grounded flags; the gate dropped a 6th ungrounded ellipsis flag), so verification is a CAPTURE-AND-LOCK not a fix; the evaluation is two-layer — deterministic grounding (faithfulness) + a Claude-reference INTER-RATER AGREEMENT (consensus, never ground truth; NO accuracy number; badge stays), mirroring Phase 34/37 (D2); the watchlist VIEW + prune is companion-only and the OFFLINE dist/news stays byte-frozen via the `/*LIVE_START*/…/*LIVE_END*/` strip, build.py never importing the live/store layer (D3). KNOWLEDGE GAPS: none significant (the live backend is confirmed working; `call_llm` is the only stubbed step; the fixture-replay pattern generalizes the existing CANNED stub).
- [OPEN · planning, gated at Phase-37 T1] Phase-37 TYPOLOGY MODEL — single primary typology per indicator vs a multi-label SET vs an honest indicator-level "sector-general" bucket. The T0 risk: many sector-page indicators are genuinely CROSS-CUTTING (structuring serves drug-trafficking AND fraud AND TF), so forcing one typology may merely RELOCATE the catch-all to indicator level. The T1 measure-first probe (section→typology determinism + the cross-cutting fraction over the ~589 unique texts) decides this AT A CHECKPOINT with the user before any tagging; also open at the checkpoint: whether the 3 specific sector pages (casinos/real-estate/VC) fold into per-indicator tagging for consistency, and which closed-vocab EXTENSIONS (if any) are grounded. Do NOT tag before the checkpoint lands.
- [RESOLVED 2026-06-08 · planning, by Phase-37 D1] Phase-37 FINTRAC SOURCE COMPLETENESS (the user's premise) — investigation established the per-sector "suspicious transaction indicators" guidance section IS complete: FINTRAC publishes exactly 11 `indicators-indicateurs/<sector>_mltf-eng` pages, all 11 acquired, 10 derived (crown-agents honestly skipped), within-page extraction complete (derived ≥ source bullets); the STR-reporting (`str-dod`) + sanctions-evasion guidance pages enumerate NO indicators (process guidance, honestly non-derivable). The "only 7 documents under the FINTRAC guidance typology" the user saw = the `fintrac-sector-baselines` catch-all CLUSTER (7 of 10), not missing docs (all 10 render; 3 under casino-gaming/real-estate/VC; all 10 in the Documents lens). The remaining derivable FINTRAC frontier is /intel/ special bulletins (OA001 + sanctions-evasion SB + Russia-linked-ML SB + dual-use advisory) — DEFERRED. Phase 37 fixes the REPRESENTATION (per-indicator typology), not the source set.
- [RESOLVED 2026-06-08 · planning, by Phase-36 D1/D2/D3] Phase-36 STORE LOCATION & SHAPE / WATCHLIST UNION RECONCILIATION / OFFLINE-STRIP INTEGRITY / DUCKDB CONFINEMENT — all four resolved at the dev-plan gate: a NEW `scripts/news_store.py` OWNS DuckDB (D2; schema scans/entities[+disposition]/red_flags, native COPY parquet export); the store file + parquet exports live gitignored (the `data/*/raw/` convention), DuckDB into the existing gitignored uv `.venv`; the union watchlist reconciles book `{role,country,segment}` + scanned `{location,age,profession,context}` to a uniform `{name,type,kind,provenance}` screening row (name scored, provenance for the honest exposure view); the watchlist is fetched LIVE via a companion-only `GET /watchlist` with all client wiring inside `/*LIVE_START*/…/*LIVE_END*/` → stripped offline → dist/news byte-identical, static offline screening book-only. KNOWLEDGE GAPS to verify at impl: DuckDB append/parquet idioms (check the DuckDB docs); ThreadingHTTPServer + a single DuckDB file under threaded `/extract` appends (serialize writes — a lock / single-writer connection).
- [RESOLVED 2026-06-08 · planning] Phase-33 direction → **CORPUS COMPLETENESS (maximal) + FULL TYPOLOGY RE-SEGMENTATION, workflow-driven.** The user reviewed the BUILT corpus and found the SOURCE SET incomplete — a DIFFERENT defect from Phase-28's within-doc extraction completeness (there each doc shipped too few of its OWN flags; here the corpus is missing whole DOCUMENTS): missing FinCEN advisories "especially the latest" + FINTRAC "way way off — so much in the Obligations/Guidance section we didn't pull". Research CONFIRMED two distinct gaps: (1) FinCEN = a discovery-WINDOW miss — the crawler floor is 2020 with no re-crawl since 2026-03, so FIN-2026-A002 (latest, 18 red flags) + the 2018-19 back-catalog (FIN-2019-A006 the original US fentanyl advisory / A005 BEC / A003 CVC / FIN-2018-A003 PEP, 14 red flags) are absent; the superseded 2020 COVID cluster is skipped. (2) FINTRAC = a source-AREA miss — all 10 current FINTRAC docs are /intel/ strategic-intel; ZERO from /guidance-directives/, the 11 sector "ML/TF indicators" pages (Financial entities master + MSB + real estate + securities + life insurance + DPMS + casinos + accountants + BC notaries + Crown agents + virtual currency), 50-100+ indicators each, heavy cross-page shared-spine duplication, HTML not PDF. At the goal gate the user chose MAXIMAL on BOTH axes: FINTRAC = all 11 sector pages (over financial-entities-master-only / master+distinctive-tails) and typology = a full vocab review (over TBML-targeted; add trade-based-money-laundering). ~16 new docs (5 FinCEN + 11 FINTRAC) roughly DOUBLE the corpus (~875 → ~1500+ indicators). The user explicitly called for "a dedicated dynamic workflow." ARCHITECTURE: a NEW `data/fintrac-guidance/` dir + a NEW CORPUS_SOURCES registry entry (the Phase-20 pattern keeps `data/fintrac/` + its 10 records byte-frozen) + a new HTML→md acquisition path; `derive_signals.py` touched ONLY IF the guidance heading doesn't ground under `_RF_HEADER_FINTRAC` (a regression-gated rf_region anchor add — the Phase-21/22/23 precedent; NOT a non-negotiable change either way). HONESTY: new-indicator posture INHERITED-or-INTERVIEWED (inherit from the C/D code via the Phase-28 interview + the cover×data matrix; FLAG non-mapping → a short targeted interview extends `data/capability-taxonomy.json` with GROUNDED posture — zero fabricated); the FINTRAC shared-spine ingested as distinct docs with the overlap DISCLOSED, NEVER cross-source de-duped (Phase-24 gate). KNOWLEDGE GAP (resolved during T2 impl): whether the FINTRAC /guidance-directives/ heading form grounds under the existing `_RF_HEADER_FINTRAC` anchors — if not, a regression-gated anchor add (0-collision verified across all existing mds). ABORT: a guidance heading that would shift any existing rf_region or fail any of the 42 existing records → SKIP that doc (honest non-derivable), never broaden a shifting anchor; a new indicator with no C/D code the user can't resolve → FLAG honestly, never fabricate; the sector pages can't be ingested without cross-source de-dup → ingest as distinct docs + disclose, never claim a de-dup number. NO non-negotiable change; the showcase (index.html + config/** + 3 typology dists), the news stream (news.html, dist/news, data/news/**), and all 42 EXISTING derived records + their source mds stay BYTE-FROZEN.
- [RESOLVED 2026-06-08 · planning] Phase-32 direction → **NEWS-STREAM ELEVATION + REAL GOV-ENFORCEMENT SOURCE SWITCH — elevate the Phase-31 news stream (dist/news) on TWO fronts at once, the six-act showcase AND the entire corpus staying BYTE-FROZEN.** The user reviewed the BUILT dist/news and judged it "very low effort … a poorly staged slide show … you are repeating the same mistakes" — the same staged-render failure called out on the corpus in Phase 27 (a RESULT not a PROCESS); and SEPARATELY found the corpus source set incomplete. The user reframed Phase 32 from the four offered forward lanes (the M8 composition north star / AUSTRAC third jurisdiction / deepen-the-news-stream / corpus overlap-dedup guard) to FIXING THE BUILT ARTIFACTS, said "do both" → split into TWO phases by frozen set (combining would over-scope): Phase 32 = news (this), Phase 33 = corpus completeness + typology re-segmentation (queued, workflow-driven). FRONT (1) REAL-SOURCE SWITCH — replace the SYNTHETIC fictional articles with REAL US-FEDERAL GOV-ENFORCEMENT adverse media (DOJ press releases/indictments · OFAC sanctions designations · Treasury-FinCEN enforcement · SEC litigation releases on ML & sanctions). THE LOAD-BEARING SUB-DECISION: the user asked to switch to REAL online news and to "ignore copyright warnings, non-commercial." PUSHBACK held + reframed — verbatim commercial news = copyright reproduction (non-commercial ≠ a verbatim right) + defamation/privacy of real NAMED persons + it undercuts an AML-compliance demo's own credibility + needs a HANDOFF §4 non-negotiable edit; the CLEAN path that delivers REAL + verbatim + recognizable WITHOUT any of that is US-federal gov-enforcement, PUBLIC DOMAIN under 17 U.S.C. §105 (the corpus's exact basis, extended FinCEN→US-federal in Phase 21 for OFAC), and the MOST AML-relevant adverse media (OFAC designations / DOJ ML indictments are what real adverse-media screening hits on). The user chose "Gov enforcement — verbatim" over [gov-verbatim+paraphrased-commercial / paraphrased-commercial-only / verbatim-commercial-news]. NOT a non-negotiable CHANGE: the existing US-federal verbatim basis applied to a NEW artifact; the COUNTERPARTY BOOK STAYS SYNTHETIC (#4 held — REAL adverse-media entity × SYNTHETIC book, honestly more compelling than fictional-vs-fictional); the always-on illustrative badge stays (the scenario/book/exposure illustrative, the source articles real — the corpus's own real-source-under-illustrative-badge model). Acquisition is build-time/authoring ONLY (WebSearch/WebFetch → committed verbatim md + provenance header: source URL + public-domain basis); the ship artifact stays offline, no fetch/LLM/keys. FRONT (2) PRESENTATION ELEVATION to the corpus's bar — make the screening PROCESS legible (Phase 31 shipped a RESULT, never a PROCESS): port the corpus's streaming "agent reading" Read render (source streams in, each red-flag phrase highlights as the read reaches it, the TYPOLOGY + ENTITY CARDS [name/location/age/profession, each QUOTE-GROUNDED in the real article — DOJ releases routinely carry "X, 45, of Miami, a licensed attorney…"; drop any attribute that can't ground, never fabricate] reveal alongside, counts up from 0); replace the Screen jump-cut with a VISIBLE SCAN PROCESS (each book row scored REAL Jaro-Winkler in a sweep, ranked into exposure, threshold line, near-match surfaced, the common-name trap flagged); adopt the FULL corpus dossier theme (news.html ~17 theme tokens vs corpus ~36 — the styling "doesn't match") + clear per-stage labeling. The runtime stays a pure client-side matcher (normalize → token-sort → Jaro-Winkler, REAL scores, offline). KNOWLEDGE GAPS: none blocking — T1 de-risks the one open feasibility item (real gov-enforcement docs carry groundable name/location/age/profession; DOJ indictment press releases routinely do). HONESTY: entity attributes ground or drop; the scan shows REAL computed scores (no fake progress bar); the book synthetic; counts honest; the near-match + the common-name trap DESIGNED INTO the synthetic book against the REAL entities, not claimed as detection rates; source attribution rendered like the corpus. ABORT: if a chosen real doc can't ground the rich entity attributes, pick another doc or carry fewer attributes — NEVER fabricate; if the scan PROCESS can't be made legible honestly, DEGRADE to a ranked static exposure table + flag it — never fake a score/threshold/progress bar; if build.py can't extend validate_news_data without disturbing existing-target byte-output, surface it. M8 NORTH STAR (still scoped OUT): the adverse-media atom composing with the transaction-signal atom. NO non-negotiable change; the showcase + the ENTIRE corpus + the grounding core derive_signals.py stay BYTE-FROZEN; build.py edited ADDITIVELY (validate_news_data extended for entity attributes).
- [RESOLVED 2026-06-08 · planning] Phase-31 direction → **a SCOPE EXPANSION: open a SECOND atom stream over AML-relevant NEWS — the adverse-media / negative-news stream (entity extraction + adverse-media red-flag identification → fuzzy-match against a SYNTHETIC client/counterparty book → potential exposure → human disposition), as a NEW STANDALONE offline artifact dist/news (from news.html, mirroring how dist/corpus was added).** The user reframed from the three offered Phase-31 lanes (a third jurisdiction [AUSTRAC/UK] / an overlap-near-dup guard at the corpus extraction gate / eyeball-the-built-4-lens-demo) to this expansion. WHY IT EXTENDS THE VISION (not a bolt-on): the demo already opens on "the missed-monitoring / TD anxiety made visual" (HANDOFF §1.1) — TD Bank's $3.09B 2024 penalty was a CDD/adverse-media failure; the thesis is unchanged (an adverse-media hit is an ATOM that composes with the transaction-signal atom); the aml-wiki backs the gap (~80% of investigative intelligence is in UNSTRUCTURED text transaction systems can't touch; entity-resolution fragmentation + the >90% false-positive wall = exactly the fuzzy-match-then-human-disposition beat). TWO SUB-DECISIONS at the goal gate: (a) news source = SYNTHETIC illustrative (fictional entities) over paraphrased-real-adjudicated / hybrid (cleanest, zero defamation/copyright surface, lets the near-match + false-positive trap be designed in so the beat lands honestly under the badge); (b) shape = WALKING SKELETON as a new standalone artifact over folding into corpus / full-M8-now (keeps the showcase + corpus byte-frozen; proves the ONE new muscle — entity extraction → fuzzy-match → exposure + human gate; everything else is corpus-proven). ARCHITECTURE: build-time committed data/news/{articles/*.md, derived/*.json, book.json} — entities + red-flags QUOTE-GROUNDED via normalize-substring + a red_flag translation; the book seeded with a near-match + an exact + a false-positive trap + clean rows; validated at the BUILD BOUNDARY (validate_news_data in build.py, fail-loud) — the grounding core derive_signals.py stays BYTE-FROZEN. RUNTIME: a pure client-side fuzzy matcher (normalize → token-sort → Jaro-Winkler, REAL scores, thresholded) — offline, file://, the only new code. ARC: Select → Read (highlights + entities) → Screen (fuzzy-match → exposure, shows the near-match exact-match misses) → Disposition (keyboard-safe human gate; dismiss the trap) → Close (honest exposure counts). KNOWLEDGE GAPS: none unfilled (AUSTRAC third-jurisdiction feasibility + the adverse-media/entity-resolution domain verified in-session; deferred not chosen). HONESTY: synthetic data + always-on badge; REAL fuzzy scores; honest counts; the near-match/trap teach the mechanism (not detection stats); NO fabricated number. ABORT: if the fuzzy beat can't be made legible even with designed synthetic data, DEGRADE to a static exposure table + flag it; if build.py can't add the news target without disturbing existing-target byte-output, surface it. M8 NORTH STAR (scoped OUT): the adverse-media atom composing with the transaction-signal atom (composite risk). NO non-negotiable change; the showcase + the entire corpus + the grounding core stay BYTE-FROZEN; build.py edited ADDITIVELY.
- [RESOLVED 2026-06-08 · planning] Phase-30 direction → **the DATA-SOURCE LENS (D1–D20) ONLY — surface the data-source axis as an institution coverage-BY-DATA-SOURCE view in the corpus explorer, the symmetric counterpart to the Phase-29 capability lens.** The user first reframed to "data-source lens + OSFI as a new source." A read-only feasibility check DISPOSED OSFI: OSFI Guideline B-8 is principles-based SUPERVISORY guidance that DEFERS to FINTRAC/FATF for the actual indicators — it publishes NO enumerated red-flag list of its own, so it cannot be a red-flag corpus source honestly (deriving from it would reproduce FINTRAC's indicators [cross-doc reproduction, explicitly avoided in Phase 23] or fabricate). OSFI's compliance basis is fine (non-commercial reproduction with attribution — the Crown-copyright family like FINTRAC), but the CONTENT isn't a red-flag corpus. The user then asked for "another Canadian regulator to complete the multi-jurisdiction setup"; a documentation + landscape check found NO clean second Canadian indicator-source exists — FINTRAC is effectively Canada's SOLE publisher of enumerated ML/TF indicators (OSFI, CIRO, AMF all defer to it). "Complete the multi-jurisdiction setup" honestly points to a THIRD jurisdiction (AUSTRAC [CC BY] or UK [Open Government Licence] offered as the best derivable + cleanly-licensed candidates). The user chose **No new source — D-lens only** — the certain, nearly-free win that completes the lens pair (C + D axes) without source-add risk. SCOPE FACT: the backend is ALREADY BUILT (Phase 29) — `data/capability-taxonomy.json` carries the `data_sources` block (20 entries {id,name,desc,posture}; posture 9 y / 4 partial / 7 n), build.py already validates + inlines the `data_source` codes, corpus.html already has `DS_BY`/`DSRC`/`dsNum` helpers — so Phase 30 needs ZERO change to build.py, the taxonomy, or any of the 42 derived records; it is a `corpus.html` mirror of the capability mode onto the D-axis + harness + docs + dist rebuild (3 lite M tasks). The distinct payoff vs the capability lens: 7 of the 20 data sources have posture "n" (not yet available) — exactly the SOURCE_DATA indicators currently buried per-doc — so the lens answers "do we even have the DATA FEED" (access/vendor problem) vs the capability lens's "do we have the detection CAPABILITY" (build problem). DESIGN: a 4th co-equal Select mode (Documents / Typologies / Capabilities / Data sources), NOT a cross-tab inside the capability view. HONESTY = the Phase-24/29 re-projection precedent: per-data-source DEMAND = honest count of corpus indicators; institution POSTURE = the interview answers re-grouped by data source; covered/partial/gap = honest counts over existing status; NO similarity/overlap/lift number; the always-on badge stays. KNOWLEDGE GAPS: none unfilled (OSFI derivability + the Canadian-source landscape verified in-session via web research; the data_source code distribution [all 20 used, posture 9y/4partial/7n] verified directly from the derived records). ABORT: if the D-lens can't say something distinct from the capability lens (genuine redundancy), or any aggregate would need a fabricated/overlap number, CUT the beat (degrade to a cross-tab inside the capability view, or drop); if build.py unexpectedly needs a change, surface it. NO non-negotiable change; build.py + capability-taxonomy.json + all 42 derived records stay BYTE-FROZEN.
- [RESOLVED 2026-06-07 · planning] Phase-29 direction → **the CAPABILITY LENS — surface the Phase-28 C1–C28 capability / D1–D20 data-source taxonomy as an institution coverage-BY-CAPABILITY view in the corpus explorer** (chosen at the goal gate over gate/harness hardening and eyeball-the-built-demo-first). A read-only grounding inspection disposed the two engineering-rigor candidates the Phase-28 debrief left behind: exact-equality dedup already exists at `derive_signals.py:353-361` (only the harder overlap/near-dup case remains, low ROI) and the full-motion streaming harness path already exists at `tests/corpus-explorer.test.mjs:654-685` — while the 28+20 interview taxonomy is entirely UNUSED in the ship artifact. The lens answers coverage PER DETECTION CAPABILITY across the whole corpus (vs PER DOCUMENT today): per-capability DEMAND = honest count of corpus indicators; institution POSTURE (have/partial/gap) = the user's interview answers re-grouped by capability — the executive buy-in view. HONESTY = the Phase-24 re-projection precedent: honest counts only, NO fabricated/similarity/overlap/lift number, the always-on badge stays. FOUNDATIONAL DECISION: PROMOTE the taxonomy from `.dev-wiki/tmp/ph28-taxonomy.json` (code→name) + `ph28-answers.json` (code→y/partial/n) → committed build-validated `data/capability-taxonomy.json` (the Phase-24 typology-map overlay pattern; build.py must NEVER read `.dev-wiki/`), validated at the build boundary (closed vocab + referential integrity against all 42 records + posture ∈ {y,n,partial}, fail-loud). PRE-VERIFIED: all 875 indicators carry both codes, every code used exists in the taxonomy, every used code has a posture — referential integrity holds, the abort precondition is satisfied. Derived records + the grounding core + the showcase + typology-map stay BYTE-FROZEN (records already carry the codes — NO re-derivation). ABORT: a code with no name → surface + fix the taxonomy, never silently drop a code; a beat needing a fabricated/overlap number → cut it. NO non-negotiable change.
- [RESOLVED 2026-06-07 · planning] Phase-27 direction → **ASSESS-ONLY-FIRST: assess the corpus demo's output quality, then replan the fix.** The user reviewed the BUILT Phase-26 corpus artifact and judged it NOT shippable (the Read-advisory extract/translate beat "brutally bad"; the build animation "not in place" unlike the six-act showcase) and floated foregoing the grounding system for full LLM extraction+translation. Inline evidence-backed diagnosis: the brutality is PRESENTATION not grounding (translations already register-quality; grounded flags are real sentences; the left panel dumps RAW markitdown + highlights MISS; the build-log never adds the showcase's `.run` pulse). PUSHBACK held: forego-the-gate risks fabricated verbatim quotes (compliance non-negotiable) + wouldn't fix the raw-article render. Offered keep-gate-fix-presentation-now / assess-only-first / go-fully-LLM-now; the user chose **assess-only-first** (evidence before committing to a build tier). T1 = a READ-ONLY dynamic assessment workflow → a per-doc fix tier (PRESENTATION_ONLY / RE_TRANSLATE / RE_EXTRACT) + an overall build recommendation; build replanned from the findings. OPEN (resolved by T1, not blocking): the exact tier split across the 42 docs (how many need only presentation vs re-translate vs re-extract). The grounding gate STAYS unless the assessment proves faithfulness another way; NO non-negotiable change; NO fabricated numbers; everything ship-side byte-frozen this round (read-only).
- [RESOLVED 2026-06-07 · planning] Phase-26 direction → **elevate the corpus demo to showcase quality (all-in, workflow-driven)**: re-translate all 42 `red_flag`s to the fentanyl-register AML-indicator style + progressive article render + build-log/combination-lift wow (illustrative-badged, reverses Ph18) + doc grouping by source/newest-first + red-flag grouping + a landing page. The register miss = Phase 25 never anchored to fentanyl.json. Open design detail (resolve in T1, not blocking): what "group red flags by typology" means (a doc has one typology → likely group a doc's indicators by section/sub-category and/or the cross-corpus view). Showcase + source mds + corpus-status + typology-map stay byte-frozen; verbatim flag + grounding untouched (only red_flag values change); NO non-negotiable change.
- [RESOLVED 2026-06-07 · planning] Phase-25 direction → **corpus OUTPUT QUALITY: extract → translate (natural AML red flags) + the article-processing page** — the corpus explorer's red flags are bare VERBATIM article extractions (the grounded `flag`), not how an AML program writes red flags, and it lacks the showcase's full-article + highlight→extract beat. Add a per-indicator `red_flag` (natural AML phrasing) BESIDE the verbatim `flag`, re-derived for ALL 42 live docs via the inverted loop, + a per-doc screen rendering the FULL source article (grounded phrases highlighted → translated). User chose FULL RE-DERIVE NOW over exemplar-first / presentation-only, and the corpus extract→translate + full-article cut over bundling the landing page / landing-page-first (landing pages + showcase-full-article-for-all → roadmap). HONESTY: verbatim stays the grounded authority shown BESIDE the red_flag; grounding gate logic BYTE-UNCHANGED; new gate check is SHAPE only (present/non-empty/distinct/bounded) — translation faithfulness is the one neural step, mitigated by show-both + the EFE oracle + per-doc re-check + the always-on badge; paraphrase is the compliance DEFAULT so this ALIGNS with the non-negotiables (NO non-negotiable change). DEGRADE: if T1's EFE proof can't yield honest translations, fall back to presentation-only (verbatim-in-context + existing signal logic) before T2. Source mds + every corpus-status.json + the six-act showcase + index.html + config/** + the 3 typology dists + data/typology-map.json stay byte-frozen.
- [RESOLVED 2026-06-06 · planning] Phase-23 direction → **FINTRAC depth — grow corpus source #4 from 3 derived Operational Alerts to the full anchorable set of FINTRAC strategic-intelligence products carrying enumerated ML/TF indicators (Operational Alerts AND Operational Briefs, incl. the real-estate brief)**, deriving everything that anchors + grounds (~5-8 new docs → source 3 → ~8-11, the second-deepest source). User chose **FINTRAC depth** over cross-corpus synthesis / presentation at the goal gate (audience = a Canadian bank — weight Canadian-relevant scale per the carried memory), and **OAs + Briefs WITH the regression-gated narrow-anchor widening** over OAs-only-no-gate-touch / a source-scoped rf_region refactor at the scope gate. NO new source, NO non-negotiable change (the FINTRAC Crown-copyright non-commercial reproduction basis was established in Phase 22), NO architecture / build.py / corpus.html STRUCTURAL change — the SELECT menu, doc_type chips, and count line are already data-driven, so new docs flow through the merge automatically (only the harness's hardcoded counts + the docs' counts update). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core `normalize`/`check_record` stays BYTE-UNTOUCHED — only the rf_region relevance anchors widen. THE ONE REAL RISK — heterogeneous heading forms: the 3 derived OAs head their lists "Money laundering indicators" (ML/TF-qualified → anchors under the existing narrow `_RF_HEADER_FINTRAC`), but the real-estate Operational BRIEF was CONFIRMED this session to head its ~40-indicator list with bare "TABLE OF INDICATORS", which the deliberately-narrow ML/TF-qualified FINTRAC anchor will NOT match → T2 carries a REGRESSION-GATED anchor widening per new heading form discovered (e.g. "TABLE OF INDICATORS"; possibly an inverted "Indicators of ML/TF" form), each NARROW + SPECIFIC + verified 0-collision across all 36 FinCEN+OFAC mds, every existing rf_region BYTE-UNCHANGED, all 35 existing records `--check-derived` clean. Acquisition is HAND-CURATED (FINTRAC serves a PDF at `<page-slug>.pdf` → the existing `_to_pdf_url` direct-download branch handles it with NO tweak, Phase-22 confirmed; pdf_to_md.py provenance already source-aware); EXCLUDE FINTRAC's sector GUIDANCE indicator pages (compliance guidance, not strategic-intel — would blur the corpus identity). KNOWLEDGE GAPS: none unfilled (the FINTRAC catalog, the real-estate brief's "TABLE OF INDICATORS" heading form, and acquisition feasibility all verified this session). ABORT: if a new form shifts ANY FinCEN/OFAC/existing-FINTRAC rf_region or fails ANY of the 35 existing records, SKIP that doc (honest non-derivable) — NEVER broaden a shifting anchor (subtraction test). NEVER reproduce a FINTRAC doc without its required Crown-copyright attribution; NEVER fabricate a BUILD_NOW; the always-on badge + the source-aware verbatim attribution stay; data/fincen/ + data/fincen-alerts/ + data/ofac/ + the showcase + the 3 EXISTING FINTRAC records stay byte-frozen (grow, don't modify)
- [RESOLVED 2026-06-07 · planning] Phase-24 direction → **cross-corpus synthesis — turn the 4-source/2-jurisdiction corpus (42 derived / 46 pubs) from 46 isolated documents into an analytical tool: group documents by TYPOLOGY and show COMBINED coverage across a cross-jurisdiction cluster ("no single advisory covers a typology; the combined corpus does")** (user chose synthesis over navigability-at-scale / durability-CI / more-scale at the goal gate — highest net-new stakeholder value for the Canadian-bank audience, uniquely enabled now that the corpus spans FinCEN + OFAC + FINTRAC, US + Canada). INTEGRATION SHAPE (approved): a group-by-typology view reachable from Select → pick a typology → its cross-jurisdiction cluster + combined coverage → drill into any doc's existing per-doc 5-screen arc (the per-doc arc stays the spine; the lens is ADDITIVE; incidentally eases the 46-doc flat-menu watch-item, a side benefit not a goal). THE LOAD-BEARING HONESTY GATE (ties to the Ph18 precision-lift rejection): NO similarity %, NO overlap score, NO correlation/lift number — ONLY (a) combined coverage as honest UNION arithmetic over the existing per-indicator status (disclosed-illustrative under the always-on badge), (b) honest per-jurisdiction contribution counts, (c) every clustered indicator traceable to its source doc + jurisdiction; if a beat needs a fabricated number → CUT it. THE SUBTRACTION + GATE-LOCATION CHOICE: the typology label is a SEPARATE committed overlay `data/typology-map.json` (doc-id → one closed-vocab typology), NOT 42 derived-record edits → all 4 source dirs + 42 derived records + the grounding core `derive_signals.py` stay BYTE-FROZEN; validate the map at the BUILD BOUNDARY in build.py (closed vocab + referential integrity + total live-doc coverage, fail-loud) — where derived-shape validation already lives, NOT in the grounding gate (a refinement from the originally-proposed derive_signals.py gate, made to keep the grounding core frozen); agent proposes the map, the deterministic build-boundary gate disposes, the user reviews. THE ONE REAL RISK (weakest assumption): that genuine cross-jurisdiction clusters EXIST — T1 (cluster-verify-first) builds the map + tabulates typology → {sources, jurisdictions} and CONFIRMS ≥2-3 typologies span ≥2 jurisdictions BEFORE T2 builds any UI. Strong priors: terrorist financing (FINTRAC TF OA ↔ FinCEN ISIS fin-2025-a001 + Iran fin-2024-a001), ransomware (a FinCEN advisory + its alerts, cross-doc-type). KNOWLEDGE GAPS: cluster breadth beyond TF/ransomware (synthetic-opioids, human-trafficking, fraud) is UNVERIFIED → T1 verifies it. ABORT/DEGRADE: if no genuine cross-jurisdiction cluster (≥2 typologies × ≥2 jurisdictions), degrade to same-jurisdiction cross-doc-type clusters or report back BEFORE T2; if any synthesis beat needs a fabricated metric, CUT the beat — never invent a similarity/overlap/lift number. NO non-negotiable change (the always-on badge + the verbatim US-federal-public-domain + FINTRAC-Crown-copyright bases stay); the per-doc arc unbroken; index.html + config/** + the 3 typology dists + the 4 source dirs + 42 derived records + the showcase stay byte-frozen
- [RESOLVED 2026-06-06 · planning] Phase-22 direction → **add FINTRAC (Canada's FIU) as corpus source #4 — the demo's FIRST cross-jurisdiction source (US Treasury FinCEN + OFAC → +Canada FINTRAC), via the VERBATIM-corpus path** (user chose FINTRAC over the 3 documented next-source directions — "this demo is for a Canadian bank after all" — and verbatim-corpus over paraphrase-corpus / paraphrase-showcase). What's genuinely NEW vs the OFAC source-#3 add: (1) a DIFFERENT compliance basis — Crown-copyright NON-COMMERCIAL reproduction LICENCE (per FINTRAC's Terms & Conditions), explicitly NOT public domain (the US-federal 17 USC §105 basis is no-copyright-at-all); requires FINTRAC's specific attribution (© His Majesty the King in Right of Canada + complete title + author + "a copy of the version available at <URL>"), is NOT commercial redistribution, and all other non-US/non-FINTRAC sources still paraphrase (user signed off "proceed"); (2) a gate vocab widening for FINTRAC's "indicators" heading (vs FinCEN "red flags" / OFAC "risk indicators"), deliberately NARROW (ML/TF-specific `_RF_HEADER_FINTRAC`/`_RF_INTRO_FINTRAC`) to avoid shifting fin-2020-a008's "Financial/Behavioral Indicators" region — verified 0 collisions across all 36 existing mds; (3) SOURCE-AWARE attribution rendering (FINTRAC must NEVER show the US-federal "public domain" string). Acquisition is HAND-CURATED (FINTRAC OAs are HTML pages WITH a PDF version → data/fintrac/index.json lists the OA PDF-version URLs, reusing acquire/pdf_to_md via --source + the OFAC `_to_pdf_url` handling; crawl_fincen.py stays FinCEN-only). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted loop + the gate; the grounding core `normalize`/`check_record` stays byte-untouched. KNOWLEDGE GAPS: none unfilled (compliance basis, OA structure/~30–35 enumerated indicators, and acquisition feasibility all verified this session via the FINTRAC Terms & Conditions + a real OA page). ABORT: if the widening shifts ANY FinCEN/OFAC rf_region or fails ANY of the 32 existing records, REVERT + narrow the anchor; T2 carries a convert-one-first rf_region + 0-baseline-shift CHECKPOINT. NEVER reproduce a FINTRAC doc without its required attribution; NEVER fabricate a BUILD_NOW; the always-on badge + the verbatim source attribution stay; data/fincen/ + data/fincen-alerts/ + data/ofac/ + the showcase stay byte-frozen (prove source #4 via the MERGE)
- [RESOLVED 2026-06-06 · planning] Phase-21 direction → **add OFAC as corpus source #3 WITH a regression-gated rf_region anchor widening** (user chose "widen the gate" over a small-clean-source / holding OFAC). Q1 (the load-bearing risk — do OFAC docs ground under the EXISTING anchors?): the red-flag-TEMPLATE OFAC docs already ground UNCHANGED (Sham Transactions rf_region (82,236), triage clean·26), but most OFAC docs use "Risk Indicators" (VC guidance), "Deceptive Practices" (maritime), or defer to a co-issued FinCEN advisory (ransomware → no list). rf_region is hard-coupled to the literal "red flag(s)" phrase (4 regexes) and `check_record` treats `rf_region==None` as a HARD violation → **widen the anchor set** to also match "Risk Indicators"/"Deceptive Practices"/"Risk Factors" headings (+ the risk-indicator intro), GATED by ZERO FinCEN regression (verified low risk: "risk indicators" 0 FinCEN mds, "deceptive practic" 0, "risk factors" 1; 0 derived records mention them → the new anchors are ~inert for FinCEN, rf_regions stay byte-identical). ABORT: if the widening shifts any FinCEN region or fails any of the 29 records, REVERT + fall back to the small-clean-source (red-flag-template OFAC only). Q2 (acquisition): OFAC's site is a JS SPA with no static listing → **hand-curated** `data/ofac/index.json` from `/media/<id>/download` PDFs; `acquire_fincen.py`/`pdf_to_md.py` reused via `--source data/ofac` + a `_to_pdf_url` direct-download tweak (absolute `/media/.../download` or `/system/files/` URL = zero-hop); crawl_fincen.py stays FinCEN-only; the `corpus_status_records` issuer parameterized (FinCEN / OFAC). Q3 (proof-batch size): ≥3 OFAC docs (the registry mechanics are already proven; OFAC is proving the agency-extension + the gate widening + the non-negotiable change). Q4 (compliance): extend the verbatim non-negotiable FinCEN-only → US-federal (17 USC §105 — FinCEN + OFAC + US-federal/Treasury) in CLAUDE.md + HANDOFF, STILL excluding FINTRAC (Canadian Crown copyright) + any non-US/non-government source; user signed off. OFAC content is sanctions/vessel-oriented → records may be honestly 0-BUILD_NOW (non-FI-observable); NEVER fabricate a BUILD_NOW; the always-on badge + verbatim public-domain attribution stay; the two existing FinCEN sources + the showcase stay byte-frozen (prove source #3 via the MERGE)
- [RESOLVED 2026-06-06 · planning] Phase-20 direction → **scale the corpus beyond FinCEN *advisories* to other FinCEN *publication types* (Alerts first) via a THIN multi-source SOURCES registry — staying entirely inside the verbatim public-domain regime so NO non-negotiable change** (still FinCEN, still 17 USC §105, still verbatim; the quote-grounding gate is reused UNCHANGED). User reframed at the gate ("try FinCEN articles first, other than advisories") — chose OTHER FinCEN publication types over other agencies (OFAC — documented next-source candidate) / a new showcase typology. Architecture: a thin SOURCES registry decouples source-id from storage dir; `build.py`'s `render_corpus` iterates sources, reads each source's `corpus-status.json` + `derived/*.json`, merges by id into one `__CORPUS__`; `derive_signals.py --corpus-status` takes an optional source path. ZERO churn on what works: `data/fincen/` stays the `fincen-advisories` source BYTE-FROZEN (prove multi-source via the MERGE, not a migration — the subtraction test rejects renaming 14 mds + 12 derived + corpus-status); alerts land in a NEW `data/fincen-alerts/`. Proof = ~5 alerts grounded via the inverted loop (LLM extracts, `--check-derived` disposes); the remaining alerts are cheap follow-on. Open sub-question, resolved at impl by the T2 CHECKPOINT: do alert PDFs convert to a groundable red-flag region? — convert ONE alert and confirm `rf_region(md)` is not None before committing the batch; degrade path = pivot to FinCEN Financial Trend Analyses (indicator-heavy) or derive fewer-but-deeper. If the registry can't merge without growing as complex as a per-source migration (subtraction MOVES not shrinks), narrow to advisories-only + a registry stub. NEVER fabricate; the always-on badge + the verbatim public-domain attribution stay
- [RESOLVED 2026-06-06 · planning] Phase-18 direction → **give the corpus explorer (`dist/corpus/`, from `corpus.html`) a dramatic 5-screen arc — Select → Coverage → Build recs/GATE → Signal → Close the loop — mirroring the showcase's Act 3 (human gate) + Act 6 (loop closes), grounded ENTIRELY in existing data, NO fabricated numbers**. User chose the corpus-explorer arc over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate. The payoff is COVERAGE close-the-loop (the existing `coverageIndex()` recompute with picked gaps flipped to covered), NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (the corpus records carry no precision/lift numbers; porting it would fabricate ~12 per-advisory illustrative stats, violating the "never present synthetic numbers as real" non-negotiable). Scope contained to `corpus.html` (+ dist/corpus rebuild + docs): the arc reuses existing fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card) → NO schema/data/build.py change; the gate uses div-toggles (NOT `<input>`) to avoid keyboard-nav collision + preserve determinism; the gate folds into the existing Build-recs screen (subtraction test). Open sub-question, resolved at impl: can interactive selection be made keyboard-safe + deterministic without growing complex? Abort/degrade path = close-the-loop-only (no gate; coverage close over ALL BUILD_NOW — the payoff still lands); if the coverage math can't be honest, keep the close screen informational; NEVER fabricate precision/lift
- [RESOLVED 2026-06-06 · planning] Phase-17 direction → **complete the corpus derivation (7/14 → 12/14) + the REAL subtraction (delete `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack), unblocked by a cheap `rf_region`-bounded triage counter**. User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table (it would leave un-regenerable stale triage). The debrief had the dependency backwards — `extract_red_flags` is off the correctness path but is the SOLE producer of the extraction/flag_count triage the byte-frozen `corpus.html` renders; the ~12-line counter (reuses the existing `rf_region` span) replaces that role, keeps `--corpus-status` regenerable, and is materially smaller. Subtraction-FIRST ordering: delete + counter + reshape (T1), prove the 7/14 still builds clean on the stripped spine, then scale (T2–T4). Open sub-question, resolved at impl: can the `rf_region`-bounded counter preserve corpus-status.json's SHAPE / pass build.py validation without growing as complex as `extract_red_flags`? Abort path = STOP the deletion, keep `extract_red_flags`, narrow to scale-only (subtraction test must SHRINK complexity, not move it). Degrade-path for a LOW/glued advisory that won't ground faithfully = scale-what-passes (the deletion still lands — counter-gated, not scale-gated)
- [RESOLVED 2026-06-06 · planning] Phase-16 direction → **INVERT the extraction architecture** (the LLM extracts candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE — normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority — + a cheap section-cite relevance guard) **+ scale as proof to 7/14** (COVID-EIP fin-2021-a002 + ≥1 previously-LOW/glued advisory). User chose this over pure breadth-scaling / a structure-preserving converter (DISSOLVED). Decisions A (groundedness + section-cite, not pure-groundedness, not a full parser), B (demote `extract_red_flags`, preserve corpus-status SHAPE, derivable false only for the 2 FATF advisories), C (2 records as proof over count) confirmed. Open sub-question, resolved at impl: can `normalize()` ground the escrow STRESS case (header-glued + hyphen-truncated raw L499) without becoming as complex as the extractor it replaces? Abort path = keep quote-grounding for the clean records, leave `extract_red_flags` as-is for the rest, narrow breadth to COVID-EIP only (subtraction test must SHRINK complexity, not move it)
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- [CANDIDATE · future phase, raised 2026-06-08 by the user] **SearXNG-backed authoring search/fetch.** A self-hosted SearXNG meta-search + readability content-extraction capability for the BUILD-TIME authoring pipeline (corpus + news acquisition) — more robust than WebFetch/`curl`, which hit bot-protection on gov sites this phase (DOJ 403; we routed around it via the Wayback Machine). Already prototyped in the sibling `nanaclaw` project (Phase 5: `SEARXNG_URL` service + `web_search`/`web_extract` MCP tools); not currently running here (localhost:8888 refused). Authoring-only — NEVER in the ship artifact (offline non-negotiable). A future-phase candidate, NOT this phase.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus; Phase 24 added `jurisdiction` per source + `load_typology_map`/`validate_typology` (build-boundary typology gate) + merges typology/jurisdiction into `__CORPUS__`; Phase 25 added `_inline_article`/`_strip_provenance` (inlines each live doc's full source-md body into `__CORPUS__`) + a re-declared `red_flag` SHAPE boundary check in `validate_corpus_data` | 2026-06-07 |
| data/typology-map.json | Phase 24 NEW cross-corpus typology OVERLAY (22-term closed vocab + 42-entry doc-id→typology map; jurisdiction from the source registry, not stored). A SEPARATE committed file — the source dirs + derived records stay byte-frozen; validated fail-loud at the build boundary | 2026-06-07 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>`; Phase 25 added an ADDITIVE `red_flag` SHAPE check to `check_record` (grounding normalize/rf_region/flag⊂md BYTE-UNCHANGED) + 3 `--selftest` fixtures | 2026-06-07 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (2.15MB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips). Phase 24 Documents/Typologies toggle + synthesis view; Phase 25 the per-doc "Read advisory" screen + the `red_flag` label through the 6-screen arc. **Phase 27 (shippability)**: `cleanArticle()` markitdown-sanitizes the displayed source (form-feeds / running headers / tab-soup; display-only, source md untouched); `highlightArticle` rewritten to normalize-both-sides + index-map (634/634 = 100% highlight); the Signal build-log ports the Act-4 `.run` working-pulse in a proposal grid + a combination-lift lift-side panel (firestat OMITTED); the progressive read types the WHOLE article (no 1600 cap, length-scaled ~6s) | 2026-06-07 |
| news.html + dist/news/index.html | Phase 31 (M8) NEW — the adverse-media / negative-news stream, a SECOND atom stream + a third standalone ship artifact (~40.6KB). Arc: Select → Read (grounded red-flag + entity highlighting, red_flag translation) → Screen (the client-side fuzzy matcher: normalize → token-sort → Jaro-Winkler, REAL scores, threshold 0.85) → Disposition (keyboard-safe human gate) → Exposure. Reads committed SYNTHETIC `data/news/{articles,derived,book}`; grounded + inlined by `build.py`'s additive `news` target (`validate_news_data`). Harness `tests/news-stream.test.mjs` (38, both motion modes). | 2026-06-08 |
| data/news/ | Phase 31 NEW — SYNTHETIC news data: `articles/*.md` (4 fictional scenarios) + `derived/*.json` (entities + red-flags, normalize-grounded + red_flag translation) + `book.json` (12-row synthetic client/counterparty book, seeded with exacts + near-matches + a common-name false-positive trap). No real data. | 2026-06-08 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored. Phase 25 added a natural `red_flag` per indicator. **Phase 27** tightened 121 over-long verbatim `flag`s to crisp contiguous SUB-SPANS (faithfulness-guarded; 36 long-but-faithful kept whole) across 33 derived records + re-translated fin-2022-a001's 2 red_flags — byte-surgical (flag/red_flag VALUES only); mds + every corpus-status.json + the grounding core byte-frozen | 2026-06-07 |
| tests/corpus-explorer.test.mjs | Zero-dep Node DOM-shim arc harness — **148 assertions** (Phase-25 108 + Phase-26 31 landing/grouping/wow-beats + Phase-27 9: T2 cleaned-panel-no-artifacts on fin-2020-a008, T3 ≥80%-highlight, T4 proposal-grid + 6-step build-log + lift-side panel + firestat-omitted; animation `.run`/`.ok` is post-render classList so asserted via STRUCTURE not `_html`) | 2026-06-07 |

## Session Journal (last 5)

- [2026-06-09] [[2026-06-09-phase-40-live-redflag-extraction-quality|Phase 40 Live red-flag extraction quality — measure-first (M8)]] (lite, 6 tasks, DELIVERED → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — the Phase-38 entity playbook applied to FLAGS. T1 stress corpus 12 commercial (6 cal/6 hold, 9 domains, LOCAL-ONLY) + 7 federal — DISCOVERY: no `extraction: full` frontmatter; filter rebuilt on the committed news_fetch verifier + a measured anti-listing rule. T2 measure + USER CHECKPOINT: blind second-rater agreement federal 0.73 / commercial 0.51–0.54; covQ 0.80–0.86 → residue = RECALL on commercial longreads; clusters C1–C4 adjudicated; the registry-scoring reframe (covQ measures existence, not extraction quality); T5 precision verify DROPPED. T3: SYSTEM_PROMPT 20-family mechanism checklist + granularity contract + committed-record exemplars + [12,240] drift fix; 3 calibration rounds (r3 ROLLED BACK on regression); HOLDOUT eval #1 (budget closed at 1 of 2): covR 0.40→0.55, agreement 0.54→0.62, mechCovR 0.46→0.63, positional decay ELIMINATED (0.64→0.43); federal unregressed (0.73→0.74). T4: shared-gate dup-collapse — news_ground.flag_dup_key (normalized quote+category), live DROP first-survives / build CHECK fail-loud, different-category KEPT; 4 committed records CLEAN, goldens valid without regeneration. T5 SKIPPED-WITH-REASON (trigger didn't fire). T6: 3 new .ph40 federal fixtures (10 total) + docs + smoke-checklist + CLAUDE.md in place. FULL REGATE GREEN: --check all 5/5 ZERO DRIFT (Python-side only, NO client change), news-stream 90 + corpus, all selftests, news_live_test (system + .venv + --live). ESCAPE HATCHES: DISCOVERY (frontmatter filter) + DISCOVERY/USER-ENVIRONMENT (~/.claude/enforce mid-phase → /spec --internal, specs/phase-40-live-redflag-quality.md). Assumption revisit: A1/A3/A4 held, A2 held-with-deviation. SOFT OBS / Phase-41 candidates: semantic instance-dup merge pass · denial-quote micro-rule · CLAUDE.md 279 lines · fold /spec --internal into lite dev-plan · commit the registry-scoring harness.
- [2026-06-09] [[2026-06-09-phase-39-live-news-qol|Phase 39 Live news QOL — streamed extraction progress + one-shot URL acquisition (M8)]] (lite, 5 tasks, DELIVERED → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — `/extract` became an ALWAYS-NDJSON stage stream (HTTP/1.0 body-until-close → per-line write+flush, NO chunked framing, polling fallback unused — D7; `extract(on_progress=None)` kept the Phase-38 replay fixtures green WITHOUT re-capture) + a NEW companion-only `scripts/news_fetch.py` (fetch ladder urllib→curl→markitdown + deterministic standardizer + article-shape VERIFIER + `--selftest` over committed fixtures). The live discovery: justice.gov answered urllib with a 2.5KB Akamai interstitial that converted to 0 chars → D5 THE LADDER RULE (a rung wins ONLY by passing the verifier) + D6 the interstitial two-step (cookie jar + one same-host meta-refresh → the 102KB real article). `/extract` accepts {url} OR {text} (text wins, D8; early `converted` event fills the textarea — the one-shot recovery honoring A1's accept-with-condition); news.html live-region URL input + stage-label/elapsed progress UI, ALL inside the strip → offline dist/news BYTE-IDENTICAL. Measured live: URL → 42.7s end-to-end, 16 entities + 8 flags grounded, 0 dropped (treasury.gov jy2735). Health: news-stream 81→90 + corpus green, `--check all` 5/5 ZERO DRIFT, news_live_test PASS (system + .venv + `--live` real-Qwen smoke), all selftests PASS. Assumption revisit: A1 BIT (the condition proved load-bearing), A2–A4 held.
- [2026-06-08] [[2026-06-08-phase-38-consolidate-live-news|Phase 38 Consolidate the live news subsystem (verify agent backend + watchlist management) (M8)]] (lite, 7 tasks — grew 5→7, DELIVERED + accepted + committed 7df3ce4 + f27f99b + pushed to main) — CONSOLIDATED the Phase-35/36 LIVE news subsystem (companion-served, dev/authoring-time only) over extending source scale (FINTRAC /intel/ vs a third jurisdiction, both DEFERRED). **T1** VERIFIED the live Qwen backend end-to-end across all 4 committed DOJ/OFAC articles (16–40s each; 4 ungrounded flags dropped corpus-wide — the messy-real output the CANNED stub never exercised); captured `<id>.qwen.json`+`<id>.golden.json` per article under `tests/fixtures/news-live/`; Claude-reference agreement (`.dev-wiki/tmp/ph38_agree.py`, consensus, NO accuracy number): 22 consensus subjects / ZERO Qwen misses but +22 noise + 8 alias-dups → user adjudicated ADD an entity-precision filter (new T2). **THE PIVOT (headline):** the deterministic denylist (T2, OFAC 33→14 on the 4 CALIBRATION articles) OVERFIT — T4's stress test on 3 NEW federal articles (DOJ Chinese-CMLO, DOJ transnational-fraud RICO, OFAC Sinaloa) showed 38/69 still noise (announcing officials/prosecutors/agency offices/court districts — an OPEN vocab a denylist can't enumerate; removed 0 on transnational-fraud). The lever is CONTEXT SHAPING: one SUBJECTS-ONLY rule in `serve_news.SYSTEM_PROMPT` (Qwen noise 38→~3, generalizes) → T2 REVISED to trim `news_ground.screen_entities` to its structural rules (alias-dedup/source-line/judicial/moniker) + DROP the denylist. **QWEN-vs-OPUS VERDICT:** same prompt + same pipeline → a well-prompted LOCAL Qwen ≈ Opus (35 vs 29 entities); the gap was INSTRUCTION CLARITY, not capability → NO API/Opus backend needed (keeps offline/no-key). **Keep-biased second-pass verify, WIRED IN on-by-default** (user idea + user choice): the naive forced-choice variant LOST 6 real designated parties (net-negative in AML); the keep-biased + alias-aware-context variant drops 5 residual institutional entities (incl. 3 transnational-fraud agencies nothing else caught) with ZERO subject loss → `serve_news.verify_entities`/`verify_subject` (LIVE-only, fail-OPEN=KEEP, layered ON TOP of `build_record` so the offline replay core is untouched; `--no-verify-entities` disables). **T3** offline recorded-fixture replay (each `.qwen.json` → parse→build→ground→screen → asserts the committed golden, 34 drops reproduced, no model) + an opt-in `--live` smoke OFF the default run. **HALF B (T5/T6)** watchlist view+prune completes the Phase-36 loop: `news_store.prune(name)` un-escalate (retains the audit row) + `POST /watchlist/prune` + a `news.html` live-region panel (escalated surface + provenance + ✕ Prune per row, onclick delegation) — ALL inside `/*LIVE_START*/…/*LIVE_END*/` → stripped offline → `dist/news` BYTE-IDENTICAL. **T7** regate + promoted the 3 stress articles to fixtures (`<id>.article.md` in tests/, NOT in `data/news/articles` so dist/news stays frozen) + docs + CLAUDE.md `## Current state` IN PLACE + commit/push. GATES: `--check all` **5/5 ZERO DRIFT** (offline dists byte-identical) · news-stream **76→81** (+strip-for-view/prune, +source-carries, +behavioral panel render, +empty-state) · `+fixture_replay_test` over 7 fixtures · `+verify_entities_test` (stubbed) · `+prune` route test · `news_ground`/`serve_news`/`news_store`/`derive_signals` `--selftest` PASS · node corpus 239. ESCAPE HATCHES: USER OVERRIDE (T2 + T4 added mid-phase) · DISCOVERY (the denylist overfit, revealed by the stress test → pivot to context shaping, user adjudicated) · USER OVERRIDE (second pass wired in on-by-default). HONEST: Claude/Opus a SECOND rater not ground truth — agreement reported as consensus + divergences, NO accuracy/precision/recall number; the always-on badge stays. NO dist/corpus/showcase change; build.py still never imports the live/store layer (it DOES import news_ground by design). NO non-negotiable change. Review gate: lite self-check. Retro NOT triggered (26 `status: completed` phases; 26 % 5 != 0). SOFT OBS / Phase-39 candidates: a CLAUDE.md trim pass (255 → ≤~200) · the deferred SCALE frontier (FINTRAC /intel/ vs AUSTRAC/UK) · a recorded-fixture for the second-pass verify (currently stub + `--live` only) · a second-pass latency optimization (batch the verify / a smaller verify model) · grow the replay-fixture corpus.
- [2026-06-08] [[2026-06-08-phase-37-per-indicator-typology|Phase 37 Per-indicator typology (corpus Typologies lens)]] (lite, 5 tasks incl. one nominal-L that dissolved, PLANNED + IMPLEMENTED + DELIVERED in one session) — the user invoked /dev-plan and reframed THREE times, sharper each pass ("FINTRAC completeness?" → "the suspicious-transaction-indicators section?" → "why only 7 docs under the FINTRAC guidance typology?"). Investigation DISPROVED the literal premise (the 11 per-sector indicator pages are complete + fully extracted; the STR/sanctions guidance pages enumerate nothing) but found the spirit right: the "7" was the `fintrac-sector-baselines` catch-all CLUSTER (7 of 10 docs), because a doc→one-typology overlay can't place an inherently multi-typology sector page. The user chose the PROPER fix — tag typology PER INDICATOR — over a lite relabel and over adding the /intel/ special-bulletin scale (both deferred). **THE MEASURE-FIRST PAYOFF (the headline):** the T1 probe earned its keep twice — (1) it caught a bug in my own section→typology classifier (`\btf\b` matched the generic "ML/TF" prefix, inflating "typology-clean" to a bogus 95%; corrected: **27% clean / 73% cross-cutting**); (2) the user chose Model C (deterministic floor + bounded neural rescue), and measuring the rescue surface BEFORE building it found **0 genuine rescues** (8 candidates, all generic risk-jurisdiction boilerplate) → **C converged to A**: the whole assignment is DETERMINISTIC (350 corruption/TF by source SECTION heading), no workflow, no neural pass, nothing to agreement-verify (the planned [L] T2 dissolved into a deterministic step). **T1** the measure-first probe + model CHECKPOINT (`.dev-wiki/tmp/ph37_probe.md`, gitignored; no tagging). **T2** assign — resolved deterministically (no neural). **T3** a NEW sparse overlay `data/indicator-typology-map.json` (350 entries, indicator global-id `<doc>/<ind>`→typology) + build.py `load_indicator_typology_map`/`validate_indicator_typology` (closed vocab + referential integrity, fail-loud, both tested) + render-time resolve (overlay-else-inherit-doc) + inject; ALL 56 derived records + `derive_signals.py` BYTE-FROZEN. **T4** corpus.html — `clusters()`/`indsForTypology`/`clusterFor`/`renderSynthesis` rewired to group by INDICATOR typology (a doc appears in every cluster its indicators touch; mirrors the capability lens), `fintrac-sector-baselines`→`cross-cutting-indicators` rename in `data/typology-map.json`; the harness export + S2 coverage assertion updated + 5 new Phase-37 assertions. **T5** regate + rebuild dist/corpus (the new ~4.98MB baseline) + CLAUDE.md `## Current state` IN PLACE (3 overlays now) + smoke-checklist Typologies check. RESULT: `corruption` (293 ind/11 docs) + `terrorist-financing` (169 ind/14 docs) now CROSS-JURISDICTION US+Canada; `cross-cutting-indicators` (746 ind) the honest bucket. GATES: `--check all` **5/5 ZERO DRIFT** (dist/corpus = new baseline; 3 typology dists + dist/news byte-identical) · corpus harness **235→239** · news **76** (frozen) · derive `--selftest` PASS · frozen set byte-clean. Honest union arithmetic; no lift/similarity/cross-regulator dedup; badge intact. ESCAPE HATCH: DISCOVERY (the T1 measurement dissolved the planned neural assign/verify — Model C executed faithfully converged to A; documented in the probe report). NO non-negotiable change. Review gate: lite self-check. SOFT OBS / Phase-38 candidates: the derivable FINTRAC /intel/ frontier (OA001 + sanctions-evasion SB + Russia-linked-ML SB + dual-use advisory — source scale) · a third jurisdiction (AUSTRAC CC BY / UK OGL — would also make `cross-cutting-indicators` cross-jurisdiction) · the live-mode `call_llm` verification gap (still stubbed across 35/36) · a watchlist-management VIEW.
- [2026-06-08] [[2026-06-08-phase-36-persistence-watchlist|Phase 36 Persistence (DuckDB→parquet) + feedback watchlist (M8)]] (lite, 5 tasks incl. one L, DELIVERED → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — wired the Phase-35 M8 live companion to a DuckDB persistence layer + an ESCALATED-ONLY feedback watchlist (the user's gate choice, REFINING D4's "book ∪ all prior-scanned"), all companion/venv-side with ZERO ship-path impact. The loop: a live scan → grounded record → companion PERSISTS to DuckDB (disposition NULL); Screen scores entities vs book ∪ watchlist(escalated); ESCALATE at the Disposition gate posts back → `disposition='escalate'` → joins the watchlist; a later article screens the now-larger surface, a re-mentioned escalated entity hitting WITH provenance — making the human gate CONSEQUENTIAL. **T1** `scripts/news_store.py` (NEW — OWNS DuckDB: scans/entities[+disposition]/red_flags; `append_scan`/`set_disposition`/`watchlist_rows()` escalated-only reconciled to `{name,type,kind,provenance}` book∪escalated/`export_parquet` native COPY/`reconcile_book`; build.py NEVER imports it; writes serialized; DuckDB 1.5.3 → the existing `.venv`). **T2** `scripts/serve_news.py` — persist each grounded `/extract` record (store OPTIONAL, graceful degrade) + `GET /watchlist` + `POST /disposition` + `--export-parquet`/`--no-persist`/`--db`. **T3 (L)** `news.html` live-region overrides inside `/*LIVE_START*/…/*LIVE_END*/` only (matchEntities book∪watchlist + escalate-any-entity Disposition gate + provenance) → `render_news` STRIPS the region → dist/news BYTE-IDENTICAL. **T4** `news_live_test.py` /watchlist + /disposition HTTP routes over a temp DuckDB store (DuckDB-gated skip under system python) + a `news-stream.test.mjs` offline book-only strip assertion + an 8-assertion live-client behavioral block (union-screen compounds + escalate gate renders). **T5** `docs/news-live.md` + smoke-checklist live-persistence walkthrough + CLAUDE.md `## Current state` IN PLACE (maintenance contract, no per-phase bullet) + `.gitignore` (data/news/.live/ + *.duckdb + *.parquet). GATES: `--check all` **5/5 ZERO DRIFT** · corpus **235** + news **67→76** (+9) · derive `--selftest` PASS · serve_news/news_ground/news_store selftests PASS. CAVEAT (carried from Phase 35): the real `call_llm` vs an actual llama-cpp server is STILL UNVERIFIED (stubbed) — first-run smoke is the user's; the grounding gate is the backstop. ALSO this session, two between-phases corpus QOL pacing commits (USER OVERRIDE on the frozen corpus): **7e31dd8** (45s cap → 22s + dwell halved) + **7e7c0b8** (phrase reveal → ~1.5× the text per-char rate) — corpus.html + dist/corpus only, harness 235/0; that pair is the NEW frozen corpus baseline. FROZEN byte-clean: the showcase, the ENTIRE corpus (new baseline), derive_signals.py, news_ground.py. NO non-negotiable change. Review gate: lite self-check. SOFT OBS / Phase-37 candidates: a real-model smoke harness / recorded-fixture integration test (close the stubbed-`call_llm` gap) · a watchlist-management VIEW (it only grows) · persist/seed the committed articles to demo the compounding loop without a live model · a presenter-facing read-speed control (corpus pacing tuned by feel 3×).
(Earlier entries — Phase 35 news live local-model backend ([[2026-06-08-phase-35-news-live-backend|journal]] — the served-by-companion live backbone: serve_news.py + the shared news_ground.py grounding gate + the stripped /*LIVE_*/ region, dist/news byte-identical; the CLAUDE.md 689→208 maintenance contract); Phase 34 C/D-assignment verification ([[2026-06-08-phase-34-cd-assignment-verification|journal]], DELIVERED + accepted + committed 83a79c3 — blind re-assignment over 589 unique texts, agreement C 74.4%/D 77.9%, 243 human dispositions, 213 byte-surgical corrections, consistency 30.5%→2.0%); Phase 33 corpus completeness + re-segmentation ([[2026-06-08-phase-33-corpus-completeness-resegmentation|journal]], committed 823c0c2 — 5th source data/fintrac-guidance, corpus 875→2,251 indicators / 42→56 records); Phase 32 news real-source elevation ([[2026-06-08-phase-32-news-real-source-elevation|journal]] — 4 real DOJ/OFAC articles, news.html rewritten to the corpus bar); Phase 30 data-source lens ([[2026-06-08-phase-30-data-source-lens|journal]] — the D-axis fourth Select mode, harness 190→217); Phase 29 capability-lens ([[2026-06-07-phase-29-capability-lens|journal]] — the C-axis third Select mode, taxonomy promoted to committed data/capability-taxonomy.json); Phase 28 corpus-completeness ([[2026-06-07-phase-28-corpus-completeness|journal]], committed 24e4a08 — re-extraction 634→875 indicators, the 28+20 coverage interview, streaming read, attribution footer).)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN/E-23. Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
