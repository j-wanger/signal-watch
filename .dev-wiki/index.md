# Dev Wiki Index

## By Category

### Phases
- [[phase-43-live-pipeline-robustness|Phase 43: Live pipeline robustness + progressive presentation (live news)]] — active, IMPLEMENTED → READY FOR COMPLETION (ALL 5 tasks [x] 2026-06-10 same-session; full regate GREEN — --check all 5/5 zero drift, news-stream 130→140, corpus 239, news_live_test system+.venv+--live; reviewer 9/10 ACCEPT, 1 HIGH fixed inline; the ~200s failure REPRODUCED + NAMED [primary OUTPUT-BUDGET truncation w/ discarded finish_reason, secondary read-timeout; intrinsic degradation REFUTED] and FIXED [call_llm streams w/ idle-gap 120s + budget 16384 + named in-stream failures; stage-completion LIVE rendering; preflight n_ctx refusal; single-flight 409; disconnect-persists-nothing]; delivery gate pending; journal [[2026-06-10-phase-43-live-pipeline-robustness]]; spec specs/phase-43-live-pipeline-robustness.md nana:approved; planned 2026-06-10; direction = the user's REFRAME at the dev-plan gate off the carried candidates [fuzzy-merge adjudication · negative-news bulk scan · FINTRAC /intel/ · AUSTRAC/UK · hygiene — all deferred again]; assumption gate closed 2026-06-10: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept) — a live test on a REAL investigation note timed out ~200s with NO intermediate feedback: extraction must work regardless of article size/complexity, and the page must show staged progress instead of an opaque wait. Code facts: `call_llm` = hard timeout=180 + max_tokens=4096, NON-streaming (serve_news.py:197-224); `extract()` emits ONE "extracting" stage then blocks; the documented llama-server launch sets NO `--ctx-size`. MEASURE-FIRST (D2): T1 [M] LOCAL stress harness (.dev-wiki/tmp/ph43_stress.py; SYNTHETIC tiered investigation notes [length × entity count × relationship density] + long commercial articles from the negative-news wiki, LOCAL-ONLY) classifies the failure (read-timeout vs finish_reason=length truncation vs context overflow [/props n_ctx] vs intrinsic degradation) + REPRODUCES the ~200s failure · T2 [M] streaming transport INSIDE call_llm (stream:true SSE, per-chunk IDLE-gap timeout replaces the 180s whole-response deadline, token-count progress into the Phase-39 NDJSON stage stream; SAME signature/full-text return → 13 replay fixtures green, NO re-capture; finish_reason=length → honest in-stream truncation error) · T3 [M] stage-completion progressive rendering (D3 — A2' after a reject-by-reframe round 1; LIVE region only: converted text → GROUNDED red flags + provisional entities at grounding-complete [the verify loop is the measured wall-time majority] → entities refine through verify i/N → final record at done; NO partial-JSON parsing, NO visible token stream) · T4 [M] size/complexity handling EARNED by T1 (pre-flight token-vs-n_ctx check w/ honest refusal; sectioned extraction ONLY IF intrinsic degradation measured — no prompt/schema change) · T5 [S] full regate + docs (--ctx-size launch guidance + staged-rendering walkthrough). SCOPE (D4): pure transport — news_ground / SYSTEM_PROMPT / EXTRACT_SCHEMA / store writes FROZEN; offline dist/news byte-identical (`--check all` 5/5); any prompt unfreeze = a surfaced finding, not drift. PRIVACY (D5): the real note NEVER committed; stress notes SYNTHETIC; commercial captures LOCAL-ONLY; fixtures US-federal-only. PRECONDITION: an approved spec via /spec --internal before implementation edits (specs/phase-43-live-pipeline-robustness.md does NOT exist yet). Decision article: articles/decisions/phase-43-live-pipeline-robustness.md. Abort: blocked >3 → ask skip or abort; dist/news can't stay byte-identical → STOP + surface; any gate/prompt/schema/store-write/fixture need → surface, don't drift; stream:true incompatible w/ the json_schema strict grammar → surface at T2 w/ T1 evidence
- [[phase-42-anchor-dossier-network-view|Phase 42: Anchor dossier view + per-scan network visualizer (live news)]] — completed (DELIVERED + accepted + committed ecb2de7 [work] + 0527fed [CLAUDE.md trim] + 2ec4075 [gate flip] + pushed to main 2026-06-10; full regate GREEN: --check all 5/5 zero drift, news-stream 103→130, corpus 239, --live real-Qwen smoke; reviewer 9/10 ACCEPT zero HIGH+; journal [[2026-06-10-phase-42-anchor-dossier-network-view]]; planned 2026-06-10; direction user-picked at the dev-plan gate; assumption gate closed 2026-06-10: A1/A3 accept, A2 don't-know→A2' accept, A4 accept-with-condition, A5 scope constraint) — CONSUME the Phase-41 ER data model instead of extending it (the live-layer-is-a-real-tool trajectory; `news_store.anchor_summary()` was built but UNCONSUMED — the named seam). One INTEGRATED view, ALL new client code inside news.html's `/*LIVE_START*/…/*LIVE_END*/` region: (1) a per-scan NETWORK VISUALIZER at Disposition (replaces the Phase-41 text subject-map) — vanilla SVG, DETERMINISTIC layout (radial init + fixed-iteration relaxation, no Math.random, no vendored lib; A4 CONDITION: SVG is the INITIAL implementation for demo testing, revisit rendering tech past demo scale; ≤~35 nodes); nodes = scan entities (main subjects highlighted), edges = relationships[] w/ labels, edge click → evidence quote; (2) the ANCHOR DOSSIER as node detail — a NEW companion `GET /anchor?name=` route wrapping anchor_summary() → scans touched / properties grouped by kind WITH per-scan provenance / same-kind-multi-value conflicts flagged "conflicting values — both kept" (presentation-only, NEVER auto-resolved — Phase-41 D2 carried) / aliases / relationship edges; honest null/404 + empty state + graceful degrade w/o store; (3) demo seed (A2' after an evidence-refuted round 1) — a committed SYNTHETIC investigation note (clearly labeled; fixture entity + client_number + one deliberately conflicting property) pasted as source_type=investigation-note + a same-article re-scan (the 7 fixture articles are case-DISJOINT, zero cross-article entity overlap); the seeded DuckDB store stays local/gitignored. PURE CONSUMPTION (A5/A1): NO change to news_ground / EXTRACT_SCHEMA / SYSTEM_PROMPT / store writes / replay fixtures (no re-capture); offline dist/news + the 4 committed records + book.json BYTE-FROZEN (`--check all` 5/5). Rider: CLAUDE.md trim 305 → ≤~220 replace-in-place (non-negotiables + honesty constraints verbatim). 6 lite tasks: T1 [S] /anchor route · T2 [M] SVG network visualizer · T3 [M] anchor dossier panel · T4 [S] synthetic-note demo seed · T5 [S] full regate + docs · T6 [S] CLAUDE.md trim. PRECONDITION: an approved spec via /spec --internal before implementation edits. Decision article: articles/decisions/phase-42-anchor-dossier-network-view.md. Abort: blocked >3 → ask skip or abort; dist/news can't stay byte-identical → STOP + surface; any gate/schema/prompt/store-write need → out of the pure-consumption scope, surface it
- [[phase-41-entity-resolution-schema|Phase 41: Entity-resolution schema enrichment (live news)]] — completed (DELIVERED + accepted + committed 1f74cb2 [work] + 62603be [gate flip] + pushed to main 2026-06-10; all 6 tasks [x], full regate GREEN, reviewer 9/10 ACCEPT; spec specs/phase-41-entity-resolution-schema.md nana:approved 2026-06-09; assumption gate closed: A1 reject→A1' accept-with-conditions, A3 reject→A3'a/A3'b accept, A2/A4/A5 accept) — the user's REFRAME at the dev-plan gate (off FINTRAC-/intel/, flag-quality-round-2, AUSTRAC/UK, QOL): enrich the LIVE entity scan schema for proper ENTITY RESOLUTION. Load-bearing reveal (round-1 A1 reject): PRIVATE INVESTIGATION NOTES are a first-class future input — attribute slots designed for that domain (client_number, account_number). TWO-LAYER model: nested per-scan JSON (entities + aliases[] verbatim + properties[] {kind,value} closed kind vocab; record + relationships[] {from,to,label,evidence} ~8-term closed vocab + main_subject; everything grounded-or-stripped by the SHARED gate, labels vocab-checked never correctness-checked) + DuckDB ANCHOR normalization (anchors w/ source_type gov-enforcement/commercial-news/investigation-note + ONE monolithic property association table [detail JSON, evidence, provenance, grounded; confidence RESERVED/NULL] + relationship edges; exact-name cross-scan accumulation, fuzzy merge DEFERRED). Alias handling INVERTS: alias-dedup/moniker DROPs → FOLDs onto the fuller entity; screen matches name ∪ aliases (max score). PRIVACY boundary: private/client data confined to the local live layer (gitignored DuckDB; 127.0.0.1 model), never committed / fixture-promoted (US-federal-only) / shipped. 6 lite tasks: T1 [M] schema+prompt · T2 [M] shared-gate extensions + alias-fold · T3 [M] store anchor redesign · T4 [S] watchlist/screen alias-awareness · T5 [M] live UI (LIVE region only) · T6 [S] regate + .ph41 fixtures + docs. Offline dist/news + 4 committed records + book.json BYTE-FROZEN; replay goldens regenerate deterministically (old captures default-empty). Decision article: articles/decisions/phase-41-entity-resolution-schema.md
- [[phase-40-live-redflag-extraction-quality|Phase 40: Live red-flag extraction quality (measure-first)]] — completed (DELIVERED + accepted + committed ea53adc [work] + 760de35 [gate flip] + pushed to main 2026-06-09; T5 skipped-with-reason; assumption revisit: A1/A3/A4 held, A2 held-with-deviation) — the Phase-38 entity playbook applied to RED FLAGS: measure → context-shape → gate-harden → verify-only-if-residue, with a USER CHECKPOINT between measure and fix. The live companion's flag extraction passes the deterministic faithfulness/grounding gate but completeness/span-quality/translation-register/consistency are UNGUARDED (no structured prompt guidance, no second pass); the user sees inconsistent flags in real live tests; rf_region does NOT transfer to news (no enumerated red-flag list) — hence the blind second-rater design. 6 lite tasks (no L; T5 conditional): T1 [M] stress corpus + baseline capture (~12 commercial articles from the negative-news wiki via deterministic frontmatter filter [extraction: full + source_score floor], calibration/holdout split, text-mode from stored bodies + URL-mode where links resolve, + the 7 committed federal fixture articles as baseline; captured outputs gitignored/LOCAL-ONLY) · T2 [M] MEASURE — deterministic proxies (flag count, span lengths, near-dup rate, grounded-drop rate, register shape) + a BLIND second-rater reference extraction → per-dimension INTER-RATER AGREEMENT (completeness/span/register; consensus, NEVER ground truth; NO accuracy number as real) + divergence clusters + USER CHECKPOINT (adjudicate clusters; call the T5-verify residue + which deterministic checks earn gate status) · T3 [M] context-shape `serve_news.SYSTEM_PROMPT` (few-shot exemplars from COMMITTED gate-passing records [anchor-style-to-the-reference] + aml-wiki money-laundering-red-flags mechanism vocabulary; fix the 12-200→[12,240] prompt/gate drift TO the gate; holdout must improve or hold — the Phase-38 overfit guard; replay fixtures green WITHOUT re-capture) · T4 [M] measurement-EARNED deterministic flag-quality checks (e.g. near-dup collapse, span bounds) in the SHARED `news_ground.py` (companion + build.py's validate_news_data; the 4 committed records must pass — a failure is an adjudicated FINDING, never a silent loosening; goldens regenerate deterministically from pinned qwen.json) · T5 [S, conditional on the T2/T3 checkpoint] batched keep-biased per-flag verify mirroring verify_entities (fail-OPEN=KEEP, LIVE-only, atop deterministic build_record, `--no-verify-flags`, ONE batched call not N) · T6 [S] regate + NEW captured US-federal fixtures under the new prompt + docs/news-live.md (measurement method + agreement-as-consensus note + new gate checks) + smoke-checklist + in-place CLAUDE.md. UNFREEZE: scripts/serve_news.py, scripts/news_ground.py + scripts/build.py (shared-gate checks only), tests/news_live_test.py, tests/fixtures/news-live/**, docs/news-live.md, tests/smoke-checklist.md, CLAUDE.md, .dev-wiki/tmp/** + data/news/.live/** (local-only), .claude/rules/active-phase.md. FROZEN byte-clean: the showcase, the ENTIRE corpus, derive_signals.py, news.html + the OFFLINE dist/news (NO client change — Python-side only, dists byte-identical), news_fetch.py + news_store.py (logic-stable). Fixture promotion US-federal-only; the always-on badge stays; NO non-negotiable change. Decisions D1–D5 (lite, in _CURRENT_STATE). KNOWLEDGE GAPS (resolve at impl): few-shot exemplar budget/granularity for the local Qwen (T3, empirical); no prior art on news red-flag completeness measurement without an enumerated source list (the blind second-rater design is the adopted answer). Abort: an extended gate check failing a committed record → adjudicate, never loosen; holdout degrades → roll the prompt back + report; no meaningful second-rater signal → present honestly at the checkpoint; blocked >3 → ask skip or abort
- [[phase-39-live-news-qol|Phase 39: Live news QOL — streamed extraction progress + one-shot URL acquisition]] — completed (DELIVERED + accepted + committed 3786042 + 3c35902 + pushed to main 2026-06-09; assumption revisit: A1 BIT — the verifier/standardizer condition proved load-bearing on the justice.gov Akamai interstitial; A2–A4 held) — QOL upgrade for the LIVE news subsystem (companion-served, dev/authoring-time only), two halves: (1) PROGRESS — `POST /extract` becomes a chunked NDJSON stream of stage events (received → [fetching → converting] → extracting → grounding → verifying i/N → done {payload}); `serve_news.extract()` gains `on_progress=None` so the Phase-38 replay-fixture seam (call_llm stubbing, deterministic build_record) is untouched; client = fetch+ReadableStream progress UI (stage label + verify i/N + elapsed timer) in the live region; NO token streaming (the per-entity verify loop is the wall-time majority); designed fallback = job-id+polling if stdlib chunked flush fails under ThreadingHTTPServer. (2) URL ACQUISITION (ONE-SHOT — the user rejected preview-then-run) — `/extract` accepts {url} OR {text}; a NEW companion-only `scripts/news_fetch.py` owns acquisition: a multi-method fetch LADDER (urllib browser-like headers → curl subprocess → markitdown convert(url); bot guards expected) + a deterministic STANDARDIZER (strip nav/link-list/image boilerplate, collapse whitespace) + a VERIFIER gate (min prose length/ratio; bot-guard/captcha/paywall/JS-wall detection → honest structured failure suggesting paste); markitdown stays `.venv`-only via lazy import + graceful degrade (the news_store/DuckDB pattern); the converted text streams back in an early event and fills the textarea (trim + re-run recovery). Acquisition PROPOSES, the scripted gate DISPOSES. 5 lite tasks T1–T5 (no L): T1 [M] streamed stage-level progress · T2 [M] news_fetch.py ladder/standardizer/verifier + `--selftest` over committed HTML fixtures · T3 [M] wire {url} into /extract · T4 [M] news.html live-region URL input + progress UI · T5 [S] regate + docs (incl. the URL-mode compliance posture note: URL fetch local/dev-only, fetched text never committed/redistributed, public/US-federal posture for fixture promotions). UNFREEZE: scripts/serve_news.py, scripts/news_fetch.py (NEW), news.html (live region only), dist/news/index.html (byte-identical via the strip), tests/news_live_test.py, tests/news-stream.test.mjs, tests/fixtures/news-fetch/** (NEW), docs/news-live.md, tests/smoke-checklist.md, CLAUDE.md, .claude/rules/active-phase.md. FROZEN byte-clean: the showcase, the ENTIRE corpus, derive_signals.py, news_ground.py + news_store.py (logic-stable), the replay fixtures tests/fixtures/news-live/** (green WITHOUT re-capture), AND the OFFLINE dist/news. build.py NEVER imports the live layer. NO non-negotiable change. KNOWLEDGE GAPS (verify at impl): markitdown 0.1.6 in-memory HTML API; BaseHTTPRequestHandler chunked-flush under ThreadingHTTPServer (T1 carries the polling fallback). Abort: chunked streaming unworkable even after the polling fallback → surface it; a fetch ladder that can't pass the verifier on real targets is an HONEST FAILURE (paste fallback), never loosen the verifier; blocked >3 → ask skip or abort
- [[phase-38-consolidate-live-news|Phase 38: Consolidate the live news subsystem (verify agent backend + watchlist management)]] — completed (DELIVERED + accepted + committed 7df3ce4 + f27f99b + pushed to main 2026-06-08) — CONSOLIDATE the Phase-35/36 LIVE news subsystem (companion-served, dev/authoring-time only) over extending source scale (FINTRAC /intel/ depth or a third jurisdiction, both DEFERRED). NEW FACT: the Qwen model (Qwen3.6-35B-A3B, llama.cpp, OpenAI-compatible) is LIVE on 127.0.0.1:8080; a planning smoke confirmed the backend end-to-end (one committed article → 4 entities + 5 grounded red flags in 17.6s; the deterministic gate DROPPED a 6th ungrounded ellipsis flag) — a CAPTURE-AND-LOCK phase. Two halves, BOTH companion-only; the OFFLINE `dist/news` stays BYTE-FROZEN (the live + watchlist-view code lives in the build-time-STRIPPED `/*LIVE_START*/…/*LIVE_END*/` region; build.py NEVER imports the live/store layer serve_news/news_store — it DOES import news_ground, the SHARED stdlib grounding primitives, by design). HALF A (the centerpiece) — verify + lock the agent backend: `call_llm` is the ONLY step the test suite stubs (a hand-written CANNED dict; everything downstream parse_llm_json → build_record → ground is covered), so run the real Qwen over the committed DOJ/OFAC articles → CAPTURE each raw output as a committed fixture under `tests/fixtures/news-live/` → OFFLINE replay tests (parse→build→ground, dep-free, no model) pin the contract incl. the ungrounded-elision drop + an OPT-IN `--live` smoke OFF the default run (the `.venv`-gated DuckDB pattern). EVALUATION two-layer + honesty-constrained: (1) deterministic GROUNDING = faithfulness (automatic), (2) a CLAUDE-REFERENCE comparison = quality reported as INTER-RATER AGREEMENT (consensus, NEVER Claude-as-ground-truth; the user adjudicates divergences; NO accuracy/precision/recall number; the always-on badge stays — mirrors Phase 34/37; grounding gate ≠ completeness gate ≠ correctness gate). HALF B — the watchlist-management VIEW + prune (completes the Phase-36 loop, currently invisible — the escalated-only watchlist only GROWS, `GET /watchlist` serves it but there is no view/prune UI): a `news_store` prune/un-escalate method + a `serve_news` prune route + a `news.html` LIVE-REGION watchlist panel (escalated surface + provenance + a prune control); stripped offline → dist/news byte-identical. Decisions: D1 DIRECTION = consolidate vs scale (the live backend never exercised against a real model; the watchlist has no view/prune UI); D2 EVALUATION/HONESTY = two-layer grounding + a Claude-reference inter-rater agreement (consensus, no accuracy number); D3 ARCH = the view + prune is companion-only, dist/news byte-frozen via the strip, build.py never imports the live/store layer. T1 [M] verify the live Qwen + capture raw-output fixtures + a Claude-reference agreement+divergence finding (no production change) · T2 [M] offline recorded-fixture replay tests + an opt-in `--live` smoke · T3 [M] news_store prune method + serve_news prune route · T4 [M] news.html live-region watchlist view panel + prune wiring; offline byte-identical · T5 [S] regate + docs + in-place state/CLAUDE.md. UNFREEZE: tests/fixtures/news-live/** (.qwen.json + .golden.json per article; a planning smoke seeded all 4), tests/news_live_test.py, scripts/news_store.py, scripts/serve_news.py, news.html (live region only), dist/news/index.html, tests/news-stream.test.mjs, .dev-wiki/tmp/**, docs/news-live.md, tests/smoke-checklist.md, CLAUDE.md, .claude/rules/active-phase.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the ENTIRE corpus (corpus.html + dist/corpus + all source dirs + every derived record + the 2 overlays), the grounding core derive_signals.py, AND the OFFLINE dist/news (byte-identical via the strip). NO non-negotiable change. 5 lite tasks (no L). KNOWLEDGE GAPS: none significant. Abort: the live backend regresses against the real Qwen → STOP + report (the gate is the backstop; do NOT loosen it); a captured fixture can't drive a deterministic offline replay → re-confirm the recorded-fixture approach; dist/news can't stay byte-identical after the strip → surface it; blocked >3 → ask skip or abort
- [[phase-37-per-indicator-typology|Phase 37: Per-indicator typology (corpus Typologies lens)]] — completed (delivered + accepted + committed 798fe28 + pushed to main 2026-06-08) — the corpus Typologies lens groups each DOC by ONE money-laundering typology (`data/typology-map.json`, 27-term vocab); that collapsed 7 of the 10 derived FINTRAC sector-guidance pages into a catch-all `fintrac-sector-baselines` (a sector page enumerates indicators across MANY typologies). The user (eyeballing the BUILT demo) asked why only "7 documents" show under the FINTRAC guidance typology. Investigation (this session): all 10 RENDER (3 under casino-gaming/real-estate/virtual-currency; all 10 together in the Documents lens); the FINTRAC SOURCE side is COMPLETE (11 per-sector `indicators-indicateurs/*_mltf-eng` pages all acquired, 10 derived, crown-agents honestly skipped; the STR + sanctions-evasion guidance pages enumerate NO indicators — process guidance, non-derivable); the remaining derivable FINTRAC frontier is /intel/ special bulletins (OA001 + sanctions-evasion SB + Russia-linked-ML SB + dual-use advisory) — offered + DEFERRED. The user chose the PROPER fix — tag typology PER INDICATOR so a sector page distributes across the real typology clusters + the catch-all retires — over (a) a lite relabel and (b) adding /intel/ source scale. Decisions: D1 DIRECTION = per-indicator typology (corpus-is-the-primary-demo + ground-judgments-in-a-user-interview); D2 METHOD/ARCH = MEASURE-FIRST (the T0 risk: cross-cutting indicators may relocate the catch-all to indicator level; the T1 probe CHECKPOINTS the model — single primary typology / multi-label set / honest indicator-level "sector-general" bucket — + the deterministic[section-heading]/neural split) + an OVERLAY `data/indicator-typology-map.json` (indicator-id→typology; build-time inherits the doc typology where absent — the ~1,231 single-typology indicators inherit free; only the ~1,020 sector-page indicators, ~589 unique, need neural assignment) so ALL 56 derived records + `derive_signals.py` stay BYTE-FROZEN + AGREEMENT-HONEST verify (assign per UNIQUE text → blind re-assignment → an inter-rater AGREEMENT rate, consensus NEVER "proven correct"; the user adjudicates ambiguous CLUSTERS; the rate is a journal/quality artifact, not a new demo number). T1 [M] measure-first probe + model CHECKPOINT (no tagging) · T2 [L] assign + blind-agreement verify + cluster adjudication → resolved indicator-id→typology (a Workflow candidate) · T3 [M] overlay + build.py validate/inherit/inject (records + core byte-frozen) · T4 [M] rewire the Typologies lens + synthesis to group by indicator typology; retire `fintrac-sector-baselines` · T5 [S] regate + rebuild dist/corpus (new frozen baseline) + docs + in-place state/CLAUDE.md. UNFREEZE: data/indicator-typology-map.json (NEW), scripts/build.py, corpus.html, data/typology-map.json (the 7 catch-all doc entries), dist/corpus/index.html, tests/corpus-explorer.test.mjs, .dev-wiki/tmp/**, docs/**, tests/smoke-checklist.md, CLAUDE.md, .claude/rules/active-phase.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the ENTIRE news stream (news.html + dist/news + data/news/** + scripts/{serve_news,news_store,news_ground}.py), ALL 56 derived records, the grounding core derive_signals.py, data/capability-taxonomy.json. Combined coverage stays honest union arithmetic; no lift/similarity/cross-regulator dedup; the always-on badge stays. NO non-negotiable change. 5 lite tasks (1 L). Abort: the T1 probe shows most indicators are genuinely cross-cutting → adopt the multi-label or sector-general model, do NOT force one-typology; the overlay can't keep the 56 records byte-frozen → re-confirm overlay vs Phase-34-style record edits; T4 > M → split the synthesis sub-task; blocked >3 → ask skip or abort
- [[phase-36-persistence-watchlist|Phase 36: Persistence (DuckDB→parquet) + feedback watchlist (M8)]] — active (planned, direction approved 2026-06-08) — wire the Phase-35 M8 live companion to a DuckDB persistence layer + an ESCALATED-ONLY feedback watchlist, all companion/venv-side with ZERO ship-path impact. The loop (the user's gate choice, REFINING D4's "book ∪ all prior-scanned"): a live scan → grounded record → companion PERSISTS to DuckDB (disposition NULL); the Screen step scores entities against book ∪ watchlist(escalated); ESCALATE at the Disposition gate posts back → DuckDB marks `disposition='escalate'` → that entity joins the watchlist; a later article screens against the now-larger surface (a re-mentioned escalated entity hits with provenance "escalated from <prior article>, <date>"). A domain-honest CURATED watchlist that makes the human Disposition gate CONSEQUENTIAL — closing the "working instance of the loop" arc Phase 35 named. Decisions: D1 DIRECTION/SCOPE = escalated-only (over "all-scanned per D4" — simplest wiring but compounding noise); D2 ARCH = a NEW `scripts/news_store.py` OWNS DuckDB (schema scans/entities[+disposition]/red_flags; native `COPY … (FORMAT parquet)`) — build.py NEVER imports it; new companion-only `GET /watchlist` + `POST /disposition` + a `--export-parquet` flag; all client wiring inside `/*LIVE_START*/…/*LIVE_END*/` → stripped offline → dist/news byte-identical; DuckDB → the existing gitignored uv `.venv`; store + parquet gitignored; the companion degrades gracefully if duckdb missing; D3 HONESTY = invariants carry forward (real scores, synthetic book, always-on badge, nothing ungrounded; escalation provenance shown; escalation may target ANY extracted entity). Watchlist reconciliation: book `{role,country,segment}` + scanned `{location,age,profession,context}` collapse to a uniform `{name,type,kind,provenance}` screening row (name scored; provenance for the honest exposure view). T1 [M] `scripts/news_store.py` (DuckDB store; build.py must NOT import it) · T2 [M] companion wiring (persist on `/extract` + `GET /watchlist` + `POST /disposition` + `--export-parquet`; store optional, graceful-degrade) · T3 [L] news.html live branch (refetch watchlist, screen book ∪ escalated, escalate posts back, provenance shown; offline strip keeps dist/news byte-identical) · T4 [S] tests (store/union/parquet in news_live_test.py, DuckDB-gated + an offline book-only assertion in news-stream.test.mjs) · T5 [S] docs + full regate + in-place state refresh. UNFREEZE: scripts/news_store.py (NEW), scripts/serve_news.py, news.html, dist/news/index.html, tests/news_live_test.py, tests/news-stream.test.mjs, docs/news-live.md, tests/smoke-checklist.md, CLAUDE.md, .claude/rules/active-phase.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the ENTIRE corpus (corpus.html + dist/corpus/index.html — just QOL-updated in commit 7e31dd8, the NEW frozen baseline — + all source dirs + every derived record + data/typology-map.json + data/capability-taxonomy.json), the grounding core derive_signals.py, AND scripts/news_ground.py (the shared grounding primitives stay logic-stable). ZERO ship-path impact (DuckDB venv-only). NO non-negotiable change. 5 lite tasks (1 L). KNOWLEDGE GAPS (verify at impl): DuckDB append/parquet idioms; ThreadingHTTPServer + single-DuckDB-file threaded `/extract` appends (serialize writes). Abort: DuckDB can't confine in the `.venv` → stdlib `sqlite3` + (DuckDB-CLI or skip) for parquet (re-confirm the one-new-dep choice); dist/news can't stay byte-identical after the strip → surface it; T3 > L → split the client-UI sub-task; blocked >3 → ask skip or abort
- [[phase-34-cd-assignment-verification|Phase 34: C/D-assignment verification pass (M7)]] — active (planned, direction approved 2026-06-08) — verify + correct the 1,376 NEW capability(C1-C28)/data_source(D1-D20) assignments Phase 33 added (61% of the corpus, in the 14 new derived records — 4 new FinCEN advisories + 10 FINTRAC-guidance sector pages): the C/D tagging was the ONE neural step in Phase 33, gated only for vocabulary VALIDITY (in-set; ph33_apply.py reported 0 flagged), NEVER CORRECTNESS. Those codes drive status/data/build_rec/build_logic AND the Phase-29 capability + Phase-30 data-source executive lenses — a mis-assignment silently shows a WRONG posture in the primary demo, against the honest-grounding thesis. The grounding gate (check_record/normalize/rf_region) checks FAITHFULNESS of the verbatim flag, never C/D correctness — this audits the dimension the gate never checked (memory: "a grounding gate ≠ a completeness gate; audit recall, not just faithfulness"). MEASURE-FIRST + HUMAN-adjudicated, in four moves: (1) a DETERMINISTIC consistency audit (no LLM, cheap, sizes the problem — same-text-different-code contradictions + within-section outliers); (2) a BLIND neural re-assignment (the measurement) — a workflow re-assigns C/D BLIND to the existing code → an INTER-RATER AGREEMENT rate + a disagreement list, reported as CONSENSUS NEVER "proven correct" (the key honesty guard against a second-LLM pass becoming false confidence); (3) HUMAN adjudication (the gate) — disagreements + contradictions CLUSTERED by (existing→proposed) code-pair/pattern so the user disposes at the cluster level interview-style (the user is the accepted truth source, the Phase-28 model); (4) a BYTE-SURGICAL apply reusing ph33_apply.py's deterministic downstream (a corrected C/D code mechanically regenerates status/data/build_rec/build_logic; the grounded `flag` and `red_flag` stay BYTE-IDENTICAL — only C/D + their consequences move). Decisions: D1 DIRECTION = verify the 1,376 new C/D assignments (over a 3rd jurisdiction + a proper Sector axis at the priority gate; measurement before optimization); D2 METHOD = measure-first + honest-as-agreement (no new demo number; the agreement rate is a quality artifact, the always-on illustrative badge stays the only claim); D3 SCOPE = the 14 new records only (the 42 existing / 875 old stay FROZEN — they had the fuller Phase-28 interview; fold them in only IF T2's rate is ugly). A DATA-correctness phase — corpus.html + build.py are data-driven, corrected codes flow through on rebuild — NO UI feature work. T1 [M] deterministic consistency audit (`.dev-wiki/tmp/ph34_audit.py`, no LLM) · T2 [L] blind re-assignment workflow + inter-rater agreement measure (`.dev-wiki/tmp/ph34_reassign.json`; the 14 records unmodified) · T3 [M] cluster + human disposition (`.dev-wiki/tmp/ph34_dispositions.json`) · T4 [M] byte-surgical apply (only C/D + deterministic downstream) · T5 [M] rebuild + full regate + harness + docs (record the measured agreement rate + correction count; handle declared-but-unused if it arises). UNFREEZE: .dev-wiki/tmp/ph34_{audit.py,reassign.json,dispositions.json}, data/fincen/derived/*.json + data/fintrac-guidance/derived/*.json (the 14 new records ONLY), dist/corpus/index.html, tests/corpus-explorer.test.mjs, tests/news-stream.test.mjs, CLAUDE.md, HANDOFF.md, tests/smoke-checklist.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the entire news stream, data/typology-map.json, data/capability-taxonomy.json (definitions UNCHANGED — only which code an indicator carries moves), the grounding core derive_signals.py, build.py, and the 42 existing records / 875 old indicators (flag + red_flag byte-identical within the 14 new records too). NO non-negotiable change. 5 lite tasks (1 L). Abort: T2's measured agreement rate comes back UGLY → STOP + surface before applying (a wholesale re-derive may be the honest call); a disposition the user can't resolve → FLAG + keep-existing, never fabricate; a correction that strands a C/D code + validate_capability_taxonomy can't accept declared-but-unused → surface it, never delete a taxonomy definition
- [[phase-01-bootstrap|Phase 1: Bootstrap (M0)]] — completed
- [[phase-02-config-driven-refactor|Phase 2: Config-driven refactor (M1)]] — completed
- [[phase-03-multi-typology|Phase 3: Multi-typology (M2)]] — completed (TBML, build-time switch)
- [[phase-04-presenter-polish|Phase 4: Presenter polish (M3)]] — completed (engine-only: nav + reset + reduced-motion)
- [[phase-05-live-pregen-mode|Phase 5: Live / pre-gen mode (M4, optional)]] — skipped (inert under file://)
- [[phase-06-ship|Phase 6: Ship (M5)]] — completed + accepted (doc/verify; compliance + offline file:// hard gate PASS)
- [[phase-07-pipeline-walking-skeleton|Phase 7: Pipeline walking skeleton (M6)]] — completed + accepted (FinCEN EFE FIN-2022-A002 slice; Act 1 verbatim render; Signal Watch rebrand)
- [[phase-08-doc-true-up|Phase 8: Doc true-up + provenance fix (M6 debt)]] — completed + accepted (rebrand docs; FinCEN-verbatim non-negotiable; fentanyl provenance → FINTRAC; dist drift corrected)
- [[phase-09-build-drift-guard|Phase 9: Build-drift guard]] — completed + accepted (in-process `build.py --check` for the zero-drift invariant; wired into smoke-checklist; commit 33db22a)
- [[phase-10-fincen-corpus-crawler|Phase 10: FinCEN corpus crawler (SCALE)]] — completed + accepted (discovery manifest `data/fincen/index.json`; authoring-only `crawl_fincen.py`, pure parser + `--selftest`; bounded batch, not mass-download; commit 0c87c47)
- [[phase-11-automated-derivation|Phase 11: Automated derivation (LLM-drafted signal config)]] — completed + accepted (AUTOMATE; authoring-only `derive_signals.py`, deterministic `--selftest`/`--scaffold` + neural `--draft`; LLM proposes a `.draft.json`, build.py + schema + human gates dispose; variant B over A; review 9/10; commit c37dc39)
- [[phase-12-fincen-corpus-derivation|Phase 12: FinCEN corpus derivation foundation (M7)]] — completed + accepted (backend-only; deterministic spine validated on ALL 14 → 7 CLEAN/3 LOW/4 NEEDS; LLM-backend (this session, no key) derived 2 records; boundary = cover×data matrix + traceability + shape; spine assists-not-automates; review 8/10→fixed; commit 90939b4; demo expansion = Phase 13)
- [[phase-13-corpus-explorer|Phase 13: Corpus explorer (advisory-selection front-end + per-indicator build-rec render) (M7)]] — completed + accepted (the PAYOFF — a NEW standalone ship artifact `dist/corpus/index.html` via `corpus.html`, staged 4-screen flow SELECT→COVERAGE→BUILD RECOMMENDATIONS→SIGNAL SPEC, all 14 advisories with honest status, 2 derived live; showcase byte-frozen; build.py decoupled from derive_signals.py; review 9/10; impl commit 54516d4)
- [[phase-14-scale-corpus-derivation|Phase 14: Scale corpus derivation (3 more CLEAN advisories → 5/14 live) (M7)]] — completed + accepted (PURE AUTHORING — derived fin-2020-a008 trafficking [10 ind/2 BUILD_NOW] + fin-2025-a003 Chinese MLN [17 ind/5 BUILD_NOW] + fin-2025-a002 Iran [16 ind/4 BUILD_NOW·7 ENRICH] via the proven --scaffold-derived→author→--check-derived loop, rebuilt dist/corpus to 5/14 live; zero engine/spine/front-end edits, all byte-frozen; --check all zero drift; findings → Phase-15 extractor footnote-resume fix + fin-2022-a001 esc() bug; 5 lite tasks; impl ce0de90 + gate 410241f)
- [[phase-15-harden-extraction-faithfulness|Phase 15: Harden extraction faithfulness + fix shipped defects (M7)]] — completed + accepted (footnote-resume fix in extract_red_flags [_SECTION_STOP terminals + conditional _FOOTNOTE_STOP + 2 targeted _CITATION signatures] — SURGICAL: fin-2025-a003 recovered its silently-dropped L499 escrow flag 17→18, 0 collateral, EFE 12+12, 7C/3L/4N held; esc() entity sweep [fin-2022-a001 + fin-2024-a002 → raw text, verified in built file]; escrow IND-18 added; regen+rebuild, --check all zero drift. Glued-no-separator DEFERRED — no safe split, stays FLAGGED [re-confirms Phase-12]. byte-frozen held; 5 lite tasks)
- [[phase-16-invert-extraction|Phase 16: Invert extraction (LLM extracts, deterministic gate disposes) + scale as proof (M7)]] — completed + accepted (impl commit bca3612; reviewer ACCEPT 9/10) — SUBTRACTION TEST on the ~130-line accreted extract_red_flags: the LLM extracts candidate red flags, the deterministic layer became a GROUNDEDNESS GATE [normalize(flag) ⊂ normalize(md) replaced src_line∈extractor-output as the traceability authority] + a coarse rf_region() section-cite relevance guard; complexity relocated from brittle section-PARSING [open] to md NORMALIZATION [closed]. ONE normalize() rule absorbed the whole closed artifact set (escrow STRESS case grounds without special-casing); 5 committed records migrated near-free; extract_red_flags DEMOTED to selftest-anchor + triage hint; corpus-status SHAPE preserved [build.py/corpus.html byte-untouched], derivable false only for the 2 FATF advisories. PROOF: ransomware fin-2021-a004 (0 deterministic flags, glued, previously unreachable) → LLM extracted all 12, grounded verbatim, NO converter/splitter [Phase-15 glued-deferral DISSOLVED]; COVID-EIP fin-2021-a002 → 5/14 → 7/14 live. 2 MEDIUM findings fixed inline. SOFT OBS → Phase-17: derive_signals.py GREW 1063→1189 (decision B retained the demoted extractor) — the real subtraction is deleting it. 5 lite tasks [1 L]
- [[phase-17-complete-corpus-derivation|Phase 17: Complete corpus derivation + delete extract_red_flags (the real subtraction) (M7)]] — completed + accepted (impl commit 572cd3b; reviewer ACCEPT 9/10) — THE REAL line-count subtraction + scale to near-completion. T1 (WIDE): deleted extract_red_flags + the whole --scaffold/--draft/--scaffold-derived authoring stack [scaffold_config, write_scaffold, draft_judgment, write_draft, _few_shot, _apply_judgment, _derived_skeleton] + the anthropic dep + the os import → derive_signals.py 1202→600 [−602, −50%, beat the ~700 estimate; def-grep 0; stdlib-only]; the ~14-line _rf_triage(md, region) counter reuses the existing rf_region span [real shrink, not a reintroduced parser]; --selftest gate-only PASS. The inverted loop [LLM extracts → check_record quote-grounds] is the SOLE derivation path. T2–T4: derived the 5 remaining derivable advisories via the inverted loop — fin-2026-a001 [glued health-care, 24 ind/6 BUILD_NOW] · fin-2021-a001 [16] · fin-2024-a001 [9] · fin-2025-a001 [11] · EFE fin-2022-a002 [12 FINANCIAL flags only] — the 2 "LOW" validated clean [the LOW label was the deleted extractor under-counting through footnotes], no degrade → corpus 7/14 → 12/14 live [only the 2 FATF advisories non-derivable]. T5 dist/corpus rebuilt 109,948→180,119 B, --check all 4-artifact ZERO DRIFT. T6 docs. DISCOVERY: corpus_status_records uses _rf_triage UNIFORMLY for all 14 [simpler than the planned per-live read]. 1 reviewer MEDIUM [latent triage-overcount footgun for a FUTURE not-yet-derived glued advisory — harmless in the shipped 12/14, disclosed; NOT fixed inline → Phase-18+ candidate]. index.html/corpus.html/build.py/config/** + 3 typology dists BYTE-FROZEN. 6 lite tasks [1 L]
- [[phase-18-corpus-explorer-arc|Phase 18: Corpus explorer arc — human gate + close-the-loop coverage payoff (M7)]] — completed + accepted (impl commit 6d654a4 + gate 2604202; reviewer ACCEPT, no MEDIUM/HIGH) — gave the corpus explorer the dramatic arc the six-act showcase has, grounded ENTIRELY in existing data, NO fabricated numbers. Flat 4-screen analytical flow → 5-screen arc: Select → Coverage → Build recs/GATE → Signal → Close the loop, mirroring the showcase's Act 3 [human gate] + Act 6 [loop closes]. THE GATE: BUILD_NOW recommendation rows became SELECTABLE div-toggles [NOT <input> — Space/arrow nav + determinism, the showcase .selrow pattern], FOLDED into the existing Build-recs screen [subtraction test], default all-BUILD_NOW-selected, reset on pick(), non-BUILD_NOW read-only. Signal: spec cards for selected ∩ BUILD_NOW with build_logic + two honest empty states. Close the loop [new renderClose]: the coverage index animates BEFORE → AFTER via the existing coverageIndex() with picked gaps flipped covered on a COPIED indicator set [showcase Act-6 model]; reduced-motion lands after directly; 0 BUILD_NOW / 0 picked → honest flat-hold + note. Payoff = COVERAGE close-the-loop, NOT precision combination-lift [EXPLICITLY REJECTED — the records carry no precision/lift numbers; porting it would fabricate ~12 per-advisory stats]. corpus.html-only unfreeze [+ dist/corpus rebuild + docs], NO schema/data/build.py change. 15/15 headless assertions, --check all 4-artifact zero drift, index.html/build.py/config/data/3-typology dists byte-frozen. 5 lite tasks [1 L]. SOFT OBS → Phase 19: the headless DOM-shim is ad-hoc/uncommitted → a tests/ harness
- [[phase-19-durability-closeout|Phase 19: Durability closeout — commit corpus-explorer test harness + pin _rf_triage (M7)]] — completed + accepted (impl commit ab0739a; exit criteria GREEN) — a DURABILITY CLOSEOUT at the end of the completed M0–M7 roadmap [demo at Definition of Done]: the one remaining piece with real non-fabrication value was durability. Committed tests/corpus-explorer.test.mjs — a ZERO-DEP Node vm+DOM-shim runner [NEVER jsdom — file:// offline ethos] that reads the committed dist/corpus/index.html [doubling as a build-output smoke test] and drives the 5-screen arc, 28 assertions pass [plan estimated ~15]. PINNED + DISCLOSED _rf_triage [Ph17 reviewer MEDIUM] via a bidirectional glued/separated --selftest assertion + a docstring note, NO output change [an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today]. README + CLAUDE + smoke-checklist all name the harness command. --check all 4-artifact ZERO DRIFT, frozen set git-diff-empty. 3 lite tasks
- [[phase-20-multi-source-spine|Phase 20: Multi-source spine, proven with FinCEN Alerts (M7)]] — completed + accepted (impl commit c9de677; reviewer 9/10 ACCEPT, no CRITICAL/HIGH) — scaled the corpus beyond FinCEN ADVISORIES to other FinCEN PUBLICATION TYPES [Alerts first] via a THIN multi-source SOURCES registry, staying entirely inside the verbatim public-domain regime so NO non-negotiable change [still FinCEN, still 17 USC §105, still verbatim]. The quote-grounding gate [check_record/rf_region/normalize] is source-agnostic, reused UNCHANGED. T1 [L]: a thin CORPUS_SOURCES registry decoupling source-id from storage dir — render_corpus iterates sources, reads each source's corpus-status.json + derived/*.json, merges by id into one __CORPUS__ [Advisory/Alert tag, per-source shape validation]; derive_signals.py source_dir params. data/fincen/ stays the `fincen-advisories` source BYTE-FROZEN [multi-source via the MERGE, not a migration]; alerts land in a NEW data/fincen-alerts/. T2: crawl_fincen.py parse_alerts + --alerts [zero-hop alert PDFs], acquire/pdf_to_md --source; ALL 19 alerts acquired+converted. T3: 6 alerts derived via the inverted loop [72 ind / 19 BUILD_NOW]; 2 honestly non-derivable. T4–T6: unified menu + Advisory/Alert chips, harness 28→40, --check all 4-artifact zero drift, docs. Post-phase FOLLOW-ON: the 11 remaining derivable alerts derived → corpus 29/33 [12 advisories + 17 alerts]. 6 lite tasks [1 L]. RETRO Dim 3 = the recurring user reframe toward scale/architecture over the showcase-debt true-up
- [[phase-21-ofac-corpus-source|Phase 21: OFAC as corpus source #3 (gate widened for OFAC vocab; US-federal public-domain) (M7)]] — completed + accepted (impl commit 68ee1dc + gate f61f92c; reviewer 9/10 ACCEPT, one MEDIUM comment-inaccuracy FIXED) — added OFAC [US Treasury] as the THIRD corpus source WITH a regression-gated widening of the rf_region anchors so OFAC's heading vocabulary derives [user chose "widen the gate" over a small-clean-source / holding OFAC]. EVIDENCE [planning probe]: the red-flag-TEMPLATE OFAC docs already ground UNCHANGED [Sham Transactions rf_region (82,236), triage clean·26], but most OFAC docs use "Risk Indicators" [VC guidance], "Deceptive Practices" [maritime], or defer to a co-issued FinCEN advisory [ransomware → no list]. rf_region is hard-coupled to the literal "red flag(s)" phrase [4 regexes] + check_record treats rf_region==None as a HARD violation → widen the anchor set to also match "Risk Indicators"/"Deceptive Practices"/"Risk Factors" headings [+ the risk-indicator intro], GATED by ZERO FinCEN regression [verified low: "risk indicators" 0 FinCEN mds, "deceptive practic" 0, "risk factors" 1; 0 derived records mention them → ~inert for FinCEN]. T1 [L]: WIDEN the rf_region anchors + parameterize the corpus_status_records issuer [FinCEN / OFAC] — regression-gated [29 FinCEN records --check-derived clean, --selftest + a new bidirectional OFAC-heading fixture, every FinCEN md's rf_region byte-unchanged]. T2 [M]: OFAC acquisition HAND-CURATED [OFAC's site is a JS SPA, no static crawl] — data/ofac/index.json from /media/<id>/download PDFs, acquire/pdf_to_md reused via --source + a _to_pdf_url direct-download tweak, convert-one-first checkpoint; crawl_fincen.py stays FinCEN-only. T3 [M]: derive the OFAC batch via the inverted loop [--check-derived clean; honest 0-BUILD_NOW allowed where content is non-FI-observable; NEVER fabricate]. T4 [S]: build.py register OFAC source #3 [doc_type "OFAC"] + corpus.html 3-type-aware count line [the doc_type chip is already data-driven]. T5 [S]: regen + rebuild + drift guard + extend the harness for an OFAC record walking the arc. T6 [S]: docs + the compliance non-negotiable extension FinCEN-only → US-federal [17 USC §105, FINTRAC still excluded]. FROZEN: index.html, config/**, 3 typology dists, AND the two existing FinCEN sources data/fincen/** + data/fincen-alerts/** [prove source #3 via the MERGE]. ABORT: if the widening shifts any FinCEN region or fails any record, REVERT + fall back to the small-clean-source. NEVER fabricate a BUILD_NOW; the always-on badge + the verbatim public-domain attribution stay. 6 lite tasks [1 L]. User signed off on the US-federal verbatim extension + "widen the gate for OFAC"
- [[phase-30-data-source-lens|Phase 30: Data-source lens — institution coverage-by-data-source view over the corpus (M7)]] — active (planned, direction approved 2026-06-08) — surface the D1–D20 data-source axis as a DATA-SOURCE LENS in the corpus explorer, the symmetric counterpart to the Phase-29 capability lens: answer coverage PER DATA SOURCE across the whole corpus — "do we even have the DATA FEED?" (an access/vendor problem) vs the capability lens's "do we have the detection CAPABILITY?" (a build problem). Distinct payoff: 7 of 20 data sources have posture "n" (not yet available) — exactly the SOURCE_DATA indicators buried per-doc, now corpus-wide legible. Decisions: (1) D-LENS ONLY [chosen at the goal gate] — the user first reframed to "+OSFI as a new source", but a read-only feasibility check disposed OSFI [Guideline B-8 is principles-based supervisory guidance that DEFERS to FINTRAC/FATF — no enumerated red-flag list, can't be a red-flag corpus source honestly], and a landscape check found NO clean second Canadian indicator-source exists [FINTRAC is Canada's sole enumerated-indicator publisher; OSFI/CIRO/AMF defer to it]; completing the multi-jurisdiction setup honestly points to a THIRD jurisdiction [AUSTRAC/UK offered], but the user chose the certain nearly-free D-lens win; (2) NO new committed artifact + NO build.py change — the Phase-29 taxonomy already carries the `data_sources` block [20 entries {id,name,desc,posture}; posture 9 y / 4 partial / 7 n], build.py already validates + inlines the `data_source` codes, corpus.html already has `DS_BY`/`DSRC`/`dsNum` helpers → the whole feature is a corpus.html mirror of the capability mode onto the D-axis + harness + docs + dist rebuild. HONESTY = the Phase-24/29 re-projection precedent: per-data-source DEMAND = honest count; POSTURE = the interview answers re-grouped; NO similarity/overlap/lift number; the always-on badge stays. T1 [M] the Data-sources Select mode [4th toggle; `dsAgg`/`indsForDS`; per-source card posture + demand + covered/partial/gap, gap-sorted, group line omitted] · T2 [M] drill-through `renderDataSource` [indicators pooled across all docs grouped by source doc + a coverage gauge + the capabilities those indicators implement] + Back via `fromDataSource` · T3 [M] harness + regate + docs + dist rebuild. UNFREEZE: corpus.html, dist/corpus, tests/**, CLAUDE.md, HANDOFF.md, README.md. FROZEN byte-clean: the showcase [index.html + config/** + the 3 typology dists], every source md, every corpus-status.json, data/typology-map.json, data/capability-taxonomy.json, scripts/build.py, the grounding core derive_signals.py, AND all 42 derived records [the data_sources axis was already inlined/validated in Phase 29 — NO data/build change]. NO non-negotiable change. 3 lite tasks. Abort: if the D-lens can't say something distinct from the capability lens, or any aggregate would need a fabricated/overlap number, CUT the beat [degrade to a cross-tab, or drop]; if build.py unexpectedly needs a change, surface it
- [[phase-33-corpus-completeness-resegmentation|Phase 33: Corpus completeness + full typology re-segmentation (workflow-driven) (M7)]] — active (planned, direction approved 2026-06-08) — close the corpus SOURCE-completeness gap (a DIFFERENT defect from Phase-28's within-doc extraction completeness: there each doc shipped too few of its OWN flags; here the corpus is missing whole DOCUMENTS) + re-segment the typology axis across the doubled corpus, via a DEDICATED derivation Workflow. Research-confirmed gaps: FinCEN = a discovery-WINDOW miss (crawler floor 2020, no re-crawl since 2026-03) → FIN-2026-A002 (latest, 18 red flags) + the 2018-19 back-catalog (FIN-2019-A006 the original US fentanyl advisory / A005 BEC / A003 CVC / FIN-2018-A003 PEP, 14 red flags); skip the superseded 2020 COVID cluster. FINTRAC = a source-AREA miss (all 10 current docs are /intel/ strategic-intel; ZERO from /guidance-directives/ — the 11 sector "ML/TF indicators" pages: Financial entities master + MSB + real estate + securities + life insurance + DPMS + casinos + accountants + BC notaries + Crown agents + virtual currency; 50-100+ indicators each, HTML not PDF, heavy shared-spine duplication). The user chose MAXIMAL on both axes at the goal gate (FINTRAC all 11 pages; a full typology vocab review, add trade-based-money-laundering) — consistent with the prioritizes-scale-over-showcase-polish posture. ~16 new docs (5 FinCEN + 11 FINTRAC) roughly DOUBLE the corpus (~875 → ~1500+ indicators). ARCHITECTURE: new FINTRAC guidance → a NEW `data/fintrac-guidance/` dir + a NEW CORPUS_SOURCES registry entry (the Phase-20 multi-source pattern keeps `data/fintrac/` + its 10 records byte-frozen) + a new HTML→md acquisition path; `derive_signals.py` touched ONLY IF the guidance heading doesn't ground under `_RF_HEADER_FINTRAC` (a regression-gated rf_region anchor add — Phase-21/22/23 precedent). HONESTY: new-indicator posture INHERITED-or-INTERVIEWED (inherit from the C/D code via the Phase-28 interview + the cover×data matrix; FLAG non-mapping → a short targeted interview extends `data/capability-taxonomy.json` with GROUNDED posture — zero fabricated); the FINTRAC shared-spine ingested as distinct docs, the overlap DISCLOSED, NEVER cross-source de-duped (the Phase-24 gate). T1 [M] acquire + convert ~16 new docs (re-crawl FinCEN widened window; an HTML→md path for the 11 guidance pages) · T2 [L] the dedicated derivation Workflow (one extract→adversarial-verify agent per new doc, full Phase-28 treatment + completeness-critic + C/D code assignment; honest dedup; FLAG non-mapping; conditional regression-gated anchor add) + regenerate corpus-status.json · T3 [M] capability-posture grounding for the new indicators (inherit + a short targeted interview for the flagged) · T4 [M] full typology re-segmentation (22-term review; add trade-based-money-laundering; resolve the sector-page handling; validate_typology disposes) · T5 [M] add the `fintrac-guidance` CORPUS_SOURCES entry (additive) + corpus.html legibility + rebuild dist/corpus · T6 [M] harness + docs + regate. UNFREEZE (additive): scripts/{crawl_fincen,acquire_fincen,pdf_to_md,build,derive_signals}.py, data/fincen/*.md + index.json + derived/*.json (NEW) + corpus-status.json, data/fintrac-guidance/** (NEW dir), data/capability-taxonomy.json, data/typology-map.json, corpus.html, dist/corpus/index.html, tests/corpus-explorer.test.mjs, CLAUDE.md, HANDOFF.md, README.md, tests/smoke-checklist.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the news stream (news.html, dist/news, data/news/**), all 42 EXISTING derived records + their source mds. NO non-negotiable change (both compliance bases established). 6 lite tasks (1 L). Abort: a guidance heading that shifts any existing rf_region or fails any of the 42 existing records → SKIP that doc (honest non-derivable), never broaden a shifting anchor; a new indicator with no C/D code the user can't resolve → FLAG honestly, never fabricate; the sector pages that need cross-source de-dup → ingest distinct + disclose, never claim a de-dup number
- [[phase-32-news-real-source-elevation|Phase 32: News stream — real gov-enforcement adverse-media source + presentation elevation (M8)]] — completed + accepted (direction approved 2026-06-08; delivered + committed afb24f4 + pushed to main) — elevated the Phase-31 news stream (dist/news) on TWO fronts at once, the six-act showcase AND the entire corpus staying BYTE-FROZEN; the user judged the BUILT dist/news "very low effort … a poorly staged slide show … repeating the same mistakes" (the Phase-27 corpus failure — a RESULT not a PROCESS). (1) REAL-SOURCE SWITCH — replace the SYNTHETIC fictional articles with REAL US-federal gov-enforcement adverse media (DOJ press releases/indictments · OFAC sanctions designations · Treasury-FinCEN enforcement · SEC litigation releases on ML & sanctions), public domain under 17 U.S.C. §105 (the corpus's exact verbatim basis, Phase 21 — NOT a non-negotiable change; the COUNTERPARTY BOOK STAYS SYNTHETIC, #4 held; REAL adverse-media entity × SYNTHETIC book). The user's "real online news, ignore copyright / non-commercial" reframed under pushback to the clean public-domain gov-enforcement path (the MOST AML-relevant adverse media, lowest defamation surface). (2) PRESENTATION ELEVATION to the corpus's bar — make the screening PROCESS legible: port the corpus streaming "agent reading" Read render (source streams in, each red-flag phrase highlights as the read reaches it, the TYPOLOGY + ENTITY CARDS [name/location/age/profession, each grounded] reveal alongside, counts up from 0) + a visible scan PROCESS on Screen (each book row scored REAL Jaro-Winkler in a sweep, ranked, threshold line, near-match surfaced, the common-name trap flagged) + the full corpus dossier theme. Acquisition is build-time/authoring ONLY (WebSearch/WebFetch → committed verbatim md + provenance header); the runtime stays a pure client-side matcher (offline, REAL scores). HONESTY: entity attributes ground or drop, the scan shows REAL scores (no fake progress bar), the book synthetic, the always-on badge stays. T1 [M] acquire ~4–6 real docs + re-derive grounded entity attributes + reseed the book · T2 [M] full dossier theme + per-stage labeling · T3 [L] streaming Read render + entity cards · T4 [M] the visible scan PROCESS on Screen · T5 [M] harness + dist + docs + regate. UNFREEZE (additive only): data/news/**, news.html, dist/news/index.html, scripts/build.py (validate_news_data extended for entity attributes), tests/news-stream.test.mjs, CLAUDE.md, HANDOFF.md, README.md, tests/smoke-checklist.md. FROZEN byte-clean: the showcase (index.html + config/** + the 3 typology dists), the ENTIRE corpus (corpus.html, dist/corpus, all 4 source dirs, every corpus-status.json, all 42 derived records, data/typology-map.json, data/capability-taxonomy.json), and the grounding core derive_signals.py; build.py edited ADDITIVELY (existing dist outputs byte-identical). The M8 north star (compose with the transaction signal) stays scoped OUT. 5 lite tasks. Abort: a real doc that can't ground rich attributes → pick another / carry fewer, never fabricate; the scan that can't be made legible honestly → degrade to a ranked static table, never fake a score/threshold/progress bar; if build.py can't extend validate_news_data without disturbing existing-target byte-output, surface it. Queued: Phase 33 = corpus completeness + typology re-segmentation (workflow-driven; different frozen set)
- [[phase-31-adverse-media-stream|Phase 31: M8 walking skeleton — the adverse-media / negative-news stream (a second atom stream)]] — completed + accepted (committed/pushed to main). A SECOND atom stream as a NEW standalone artifact `dist/news/` (from news.html, mirroring dist/corpus): synthetic news → grounded entity + red-flag extraction → a client-side fuzzy match (normalize → token-sort → Jaro-Winkler, REAL scores) against a synthetic client/counterparty book → potential exposure → a human disposition gate. Surfaces the near-matches an exact-name screen misses + the common-name false-positive trap (dismissable). build.py edited ADDITIVELY (a new `news` target + `validate_news_data`); showcase + entire corpus + grounding core byte-frozen. Reviewer SHIP (all 4 findings folded in). news harness 38, corpus 217 (no regression), --check all 5/5 zero drift. The compose-with-the-transaction-signal payoff is the M8 north star, scoped OUT.
- [[phase-29-capability-lens|Phase 29: Capability lens — institution coverage-by-capability view over the corpus (M7)]] — completed + accepted (direction approved 2026-06-07; delivered + committed/pushed to main 029f33c) — surface the Phase-28 C1–C28 capability / D1–D20 data-source taxonomy as a CAPABILITY LENS in the corpus explorer: answer coverage PER DETECTION CAPABILITY across the whole corpus (vs PER DOCUMENT today), cross-referenced against the institution's interview posture — the executive buy-in view and the realized payoff of the Phase-28 28+20 interview (the per-indicator codes ship in every derived record but are UNUSED in the artifact). A read-only grounding inspection disposed the two Ph28-debrief rigor candidates as mostly already-done (dedup at derive_signals.py:353-361; streaming harness at tests/corpus-explorer.test.mjs:654-685). Decisions: (1) capability-lens UI chosen at the goal gate [over gate/harness hardening, eyeball-first]; (2) PROMOTE the taxonomy from `.dev-wiki/tmp/ph28-{taxonomy,answers}.json` → a committed build-validated `data/capability-taxonomy.json` (code → {name, posture}; the Phase-24 typology-map overlay pattern; build.py never reads `.dev-wiki/`), validated at the build boundary [closed vocab + referential integrity against all 42 records + posture ∈ {y,n,partial}, fail-loud] [over two separate files]. HONESTY = the Phase-24 re-projection precedent: per-capability DEMAND = honest count of corpus indicators; institution POSTURE = the interview answers re-grouped by capability; NO fabricated/similarity/overlap/lift number; the always-on badge stays. T1 [M] commit the taxonomy + thread load_capability_taxonomy/validate_capability_taxonomy through build.py + inline codes/taxonomy into __CORPUS__ · T2 [M] the Capabilities Select mode [demand count + posture + coverage breakdown, gap-sorted] · T3 [M] drill-through into the per-doc arc + Back · T4 [M] harness + regate + docs. UNFREEZE: data/capability-taxonomy.json [NEW], scripts/build.py, corpus.html, dist/corpus, tests/**, CLAUDE.md, HANDOFF.md. FROZEN byte-clean: the showcase [index.html + config/** + the 3 typology dists], every source md, every corpus-status.json, data/typology-map.json, the grounding core derive_signals.py, AND every derived data/*/derived/*.json record [they already carry the codes — NO re-derivation]. PRE-VERIFIED: all 875 indicators carry both codes, every code used exists in the taxonomy, every used code has a posture. NO non-negotiable change. 4 lite tasks. Abort: a code with no name → surface + fix the taxonomy, never silently drop a code; a beat needing a fabricated/overlap number → cut it
- [[phase-26-corpus-elevation|Phase 26: Elevate the corpus demo to showcase quality (register re-translation + progressive render + build/lift wow + grouping/sort + landing) (M7)]] — completed + accepted (commits 7a35147/44c24a6/83be590/337b8aa; reviewer 10/10 ACCEPT) — workflow-driven, all-in elevation of the corpus demo to the six-act showcase's bar. Reviewing the BUILT Phase-25 artifact the user named five quality gaps at once: the `red_flag` translations read like PROSE not the showcase's crisp typology-named AML INDICATORS [Phase 25 never anchored to `config/typologies/fentanyl.json` — the register miss]; the per-doc article screen renders STATIC [no progressive "agent reading" reveal]; the Signal screen "doesn't wow" [no build-log / combination-lift]; docs aren't grouped [want by SOURCE, newest-first] + red flags aren't grouped; no LANDING page. Decisions: (1) ALL-IN elevation in ONE workflow-driven phase [over a substance-first / experience-first cut]; (2) the REGISTER bar — short typology-named mechanism-naming noun phrases [the fentanyl.json style], SHOW-BOTH [the verbatim `flag` stays beside the translation as grounded evidence], only the `red_flag` VALUES change [grounding + the SHAPE gate byte-unchanged]; (3) WOW-NUMBERS HONESTY [user-approved, REVERSES the Phase-18 no-fabricated-lift call] — the build-log animates the REAL `build_logic`, the combination-lift figures are a GENERIC illustrative TEMPLATE with a loud "illustrative · pending calibration" tag under the always-on badge [NEVER 42 fabricated per-doc findings, never presented as real; the user supplies real figures later]. T1 [M] register rubric [distilled from fentanyl/elder] + workflow design briefs + re-translate EFE + 2 exemplars as the register PROOF/checkpoint [resemble the showcase oracle, else degrade] · T2 [L] the re-translation workflow over all 42 live docs [only the `red_flag` VALUES change; grounding + the SHAPE gate byte-unchanged, each re-checked] · T3 [M] the progressive ("agent reading") article render + doc grouping by source/newest-first + red-flag grouping · T4 [L] the build-log + combination-lift WOW beats, illustrative-badged · T5 [M] the story-driven LANDING page · T6 [S] rebuild dist/corpus + drift guard + extend the harness + docs [CLAUDE, README, a register-rubric note]. UNFREEZE: the 42 `data/{fincen,fincen-alerts,ofac,fintrac}/derived/*.json` [the `red_flag` VALUES only], corpus.html, build.py [ONLY if a wow beat needs it], dist/corpus, tests/**, CLAUDE.md, README.md, a register-rubric note. FROZEN: the six-act showcase [index.html + config/typologies/** + the 3 typology dists], every source md, every corpus-status.json, data/typology-map.json, the grounding core derive_signals.py + the authoring scripts [the verbatim `flag` + grounding stay untouched]. NO non-negotiable change. 6 lite tasks [two L]. Abort: if T1's register PROOF fails, REPORT before T2; if a wow beat needs a fabricated per-doc finding, CUT it to the generic illustrative-badged template
- [[phase-25-corpus-translation|Phase 25: Corpus output quality — extract → translate (natural AML red flags) + the article-processing page (M7)]] — completed + accepted (impl commit d3fdadf + review cd0e1e5; reviewer 9/10 ACCEPT, two LOW fixed inline) — brought the corpus explorer to the six-act showcase's two-layer red-flag model: keep the grounded VERBATIM `flag` extraction (the evidence), ADD a natural-AML `red_flag` TRANSLATION beside it + a per-doc "Read advisory" screen (`renderArticle`) rendering the FULL source article (grounded phrases highlighted → translated). Re-derived all 42 live docs via the inverted loop (one subagent per doc, self-gated then independently re-checked; 42/42 `--check-derived` clean). HONESTY: the verbatim stays the grounded authority shown BESIDE the red_flag, the grounding gate logic BYTE-UNCHANGED, the new gate check is SHAPE only (present/non-empty/distinct/12–240 chars) in BOTH `check_record` + `validate_corpus_data`; translation faithfulness is the one accepted neural step, mitigated by show-both + the EFE oracle (T1 PASS) + per-doc re-check + the always-on badge; paraphrase is the compliance DEFAULT → NO non-negotiable change. The full article is inlined at the BUILD boundary (`_inline_article`/`_strip_provenance`), keeping `render_one` + the 3 typology dists byte-frozen. The per-doc arc grew 5→6 screens (Select → Read advisory → Coverage → Build recs → Signal → Close); `red_flag` threaded as the indicator label with the verbatim kept as the traceable subline. T1 (M) gate+build+EFE proof · T2 (L) re-derive 41 · T3 (M) article screen + thread red_flag · T4 (S) rebuild + drift + harness 98→108 · T5 (S) docs. `--check all` 4/4 ZERO DRIFT, `--selftest` PASS (grounding core byte-unchanged), dist/corpus 635KB→2.19MB (under the 2.5MB watch line); frozen set byte-clean (every source md + corpus-status.json, index.html, config/**, the 3 typology dists, data/typology-map.json, the showcase). Two LOW fixed inline (OFAC maritime red_flag heading-faithfulness; build-boundary MIN_RED_FLAG_CHARS=12 parity). 5 lite tasks (1 L). User REFRAMED to an output-QUALITY ask (away from grow-Canada / a 3rd jurisdiction / durability-CI) — full re-derive now over exemplar-first / presentation-only + corpus extract→translate + full article over bundling the landing page (landing pages → roadmap)
- [[phase-23-fintrac-depth|Phase 23: FINTRAC depth — grow source #4 with the remaining anchorable strategic-intel products (Operational Alerts + Operational Briefs) (M7)]] — active (planned, direction approved 2026-06-06) — grow the existing FINTRAC corpus source #4 [data/fintrac/, doc_type "FINTRAC"] from 3 derived Operational Alerts to the full anchorable set of FINTRAC strategic-intelligence products carrying enumerated ML/TF indicators [OAs AND Operational Briefs, incl. the real-estate brief]. "Canadian depth" for the audience [a Canadian bank]; honest yield ~5-8 → FINTRAC source 3 → ~8-11 [second-deepest source]. NO new source, NO non-negotiable change [the FINTRAC Crown-copyright non-commercial reproduction basis was established Phase 22], NO architecture / build.py / corpus.html STRUCTURAL edit [the menu, doc_type chips, count line are data-driven — new docs flow through the merge]. Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core normalize/check_record stays BYTE-UNTOUCHED — only the rf_region relevance anchors widen. THE ONE REAL RISK = heterogeneous heading forms: the 3 derived OAs head their lists "Money laundering indicators" [ML/TF-qualified → anchors under the existing narrow _RF_HEADER_FINTRAC], but the real-estate Operational BRIEF was CONFIRMED this session to head its ~40-indicator list with bare "TABLE OF INDICATORS", which the narrow ML/TF-qualified anchor will NOT match → a REGRESSION-GATED anchor widening per new heading form, each NARROW + SPECIFIC + verified 0-collision across all 36 FinCEN+OFAC mds, every existing rf_region BYTE-UNCHANGED, all 35 existing records --check-derived clean [chosen OVER a source-scoped rf_region refactor, deferred]. EXCLUDE FINTRAC's sector GUIDANCE indicator pages [compliance guidance, not strategic-intel]. T1 [M]: acquire + convert the candidate batch [human-trafficking oai-hts-2021, OCSE exploitation, romance fraud, illegal wildlife trade, casino/Project-Athena, real-estate real] + per-doc rf_region triage + capture the baseline. T2 [L]: the GATE change — a narrow specific 0-collision anchor per new heading form, regression-gated [35 records clean, --selftest + new fixtures, every prior md byte-unchanged; NO-OP if no new form]. T3 [M]: derive the anchoring batch via the inverted loop [--check-derived clean; FINTRAC Crown-copyright non-commercial provenance; honest BUILD_NOW only where FI-observable; never fabricate]. T4 [S]: regen + rebuild + drift guard + harness counts + a FINTRAC Brief walking the arc. T5 [S]: docs [counts + Operational-Brief inclusion; NO non-negotiable change]. FROZEN: index.html, config/**, the 3 typology dists, scripts/build.py + corpus.html [no structural edit], AND the THREE OTHER sources data/fincen/** + data/fincen-alerts/** + data/ofac/**, AND the 3 EXISTING data/fintrac/ records [grow, don't modify]. ABORT: if a new heading form shifts ANY existing rf_region or fails ANY of the 35 records, SKIP that doc [honest non-derivable] — do NOT broaden a shifting anchor. 5 lite tasks [1 L]. User chose FINTRAC depth at the goal gate [audience = a Canadian bank] + OAs + Briefs WITH the regression-gated widening at the scope gate
- [[phase-22-fintrac-corpus-source|Phase 22: FINTRAC as corpus source #4 (first cross-jurisdiction source; gate widened for FINTRAC "indicators" vocab; Crown-copyright non-commercial reproduction) (M7)]] — completed + accepted (impl commit 6765d26 + gate 7f6bc22; reviewer 9/10 ACCEPT, one MEDIUM config/schema.md stale claim FIXED inline) — added FINTRAC [Canada's FIU] as the FOURTH corpus source — the demo's FIRST move beyond US-federal [US Treasury FinCEN + OFAC → +Canada FINTRAC]. User chose FINTRAC over the 3 documented next-source directions ["this demo is for a Canadian bank after all"] + the VERBATIM-corpus path over paraphrase-corpus/paraphrase-showcase. Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core normalize/check_record stays BYTE-UNTOUCHED. What's genuinely NEW vs the OFAC source-#3 add: (1) FIRST cross-jurisdiction source — a DIFFERENT compliance basis [Crown-copyright NON-COMMERCIAL reproduction LICENCE per FINTRAC's Terms & Conditions, explicitly NOT public domain — the US-federal 17 USC §105 basis is no-copyright-at-all; requires FINTRAC attribution (© His Majesty the King in Right of Canada + complete title + author + "a copy of the version available at <URL>"), NOT commercial redistribution, other non-US/non-FINTRAC still paraphrase]; (2) a gate vocab widening for FINTRAC's "indicators" heading [vs FinCEN "red flags" / OFAC "risk indicators"], deliberately NARROW [ML/TF-specific _RF_HEADER_FINTRAC/_RF_INTRO_FINTRAC] to avoid shifting fin-2020-a008's "Financial/Behavioral Indicators" region — verified 0 collisions across all 36 existing mds; (3) SOURCE-AWARE attribution rendering [FINTRAC must NEVER show the US-federal "public domain" string]. T1 [L]: WIDEN the rf_region anchors [narrow FINTRAC "indicators"] + parameterize the issuer [FINTRAC] — regression-gated [all 32 existing records --check-derived clean, --selftest + new bidirectional FINTRAC fixtures, every FinCEN+OFAC md's rf_region byte-unchanged]. T2 [M]: FINTRAC acquisition HAND-CURATED [OAs are HTML pages WITH a PDF version] — data/fintrac/index.json lists the OA PDF-version URLs, acquire/pdf_to_md reused via --source + the OFAC _to_pdf_url handling, convert-one-first + 0-baseline-shift checkpoint; crawl_fincen.py stays FinCEN-only. T3 [M]: derive the FINTRAC batch via the inverted loop [--check-derived clean; FINTRAC Crown-copyright non-commercial provenance, NOT "public domain"; NEVER fabricate]. T4 [S]: build.py register FINTRAC source #4 [doc_type "FINTRAC"] + corpus.html 4-type count line + SOURCE-AWARE FINTRAC attribution. T5 [S]: regen + rebuild + drift guard + extend the harness for a FINTRAC record walking the arc. T6 [S]: docs + the compliance non-negotiable extension [add FINTRAC as a Crown-copyright non-commercial reproduction licence, NOT public domain]. FROZEN: index.html, config/**, 3 typology dists, AND the THREE existing sources data/fincen/** + data/fincen-alerts/** + data/ofac/** [prove source #4 via the MERGE]. ABORT: if the widening shifts ANY FinCEN/OFAC rf_region or fails ANY of the 32 existing records, REVERT + narrow the anchor. NEVER reproduce a FINTRAC doc without its required attribution; NEVER fabricate a BUILD_NOW; the always-on badge + the verbatim source attribution stay. 6 lite tasks [1 L]. User signed off on the Crown-copyright non-commercial reproduction extension + the approach ("proceed")

### Decisions
- [[decisions/phase-43-live-pipeline-robustness|Phase 43 direction: live pipeline robustness + progressive presentation (live news)]] — finalized (confidence: high, 2026-06-10) — the user's reframe at the dev-plan gate (a real-note live test timed out ~200s with no feedback); measure-first failure classification (D2), stage-completion progressive rendering — no token stream (D3/A2'), pure-transport scope w/ the call_llm fixture seam preserved (D4), privacy: real note never committed, stress material synthetic/local-only (D5)
- [[decisions/phase-42-anchor-dossier-network-view|Phase 42 direction: anchor dossier view + per-scan network visualizer (live news)]] — finalized (confidence: high, 2026-06-10) — consume the Phase-41 ER data model (graph as the navigation surface, dossier as node detail); deterministic vanilla-SVG layout (A4 condition: initial implementation for demo testing); synthetic-note demo seed (A2' — fixture articles are case-disjoint); pure read-side consumption scope (A5/A1)
- [[decisions/phase-41-entity-resolution-schema|Phase 41 direction: entity-resolution schema enrichment (live news)]] — finalized (confidence: high, 2026-06-09) — the user's reframe at the dev-plan gate; two-layer data model (nested grounded-or-stripped JSON + DuckDB anchors), alias FOLDs, closed property/relation vocabs, main_subject, privacy boundary for private investigation notes

### Journal
- [[2026-06-10-phase-43-live-pipeline-robustness|Phase 43: Live pipeline robustness + progressive presentation (live news) — implemented, ready for completion]] — ALL 5 tasks [x] same-session; the user's ~200s opaque failure reproduced + named (OUTPUT-BUDGET primary, read-timeout secondary; intrinsic degradation refuted) and fixed (streaming call_llm w/ idle-gap timeout + named in-stream failures; stage-completion LIVE rendering — grounded reveal 18s live; preflight n_ctx refusal + single-flight 409 + disconnect-persists-nothing); 13/13 replay green zero re-capture; --check all 5/5 zero drift; reviewer 9/10 ACCEPT (1 HIGH fixed inline); delivery gate pending.
- [[2026-06-10-phase-42-anchor-dossier-network-view|Phase 42: Anchor dossier view + per-scan network visualizer (live news) — implemented, ready for completion]] — ALL 6 tasks [x], full regate GREEN (news-stream 103→130, --check all 5/5 zero drift, --live real-Qwen smoke), reviewer 9/10 ACCEPT; PURE CONSUMPTION held (gate/schema/store-writes/fixtures untouched, reviewer git-diff-verified); GET /anchor route + deterministic SVG graph (evidence closed-until-clicked) + anchor dossier ("both kept" conflicts) + the SYNTHETIC-note accumulation demo confirmed LIVE (2 scans, client_number, phone conflict both-kept); CLAUDE.md trim 319→220; delivery gate pending.
- [[2026-06-10-phase-41-entity-resolution-schema|Phase 41: Entity-resolution schema enrichment (live news) — implemented, ready for completion]] — ALL 6 tasks [x], full regate GREEN (node 103, fixtures 10→13 w/ 3 .ph41 pairs, --check all 5/5, --live smoke), reviewer 9/10 ACCEPT; D5 user-ruled r2 prompt-regression fix (red_flags FIRST in EXTRACT_SCHEMA order — strict-grammar generation order is load-bearing); anchor data model live (anchor_summary the unconsumed next-phase seam); delivery gate pending.
- [[2026-06-08-phase-31-adverse-media-stream|Phase 31: M8 walking skeleton — the adverse-media / negative-news stream (a second atom stream)]] — DELIVERED + accepted (committed/pushed to main). Planned + implemented in one session; the user reframed to a SECOND atom stream over unstructured news. A new standalone artifact dist/news (synthetic news → grounded entity + red-flag extraction → client-side Jaro-Winkler fuzzy match vs a synthetic book → exposure → human disposition). build.py additive; reviewer SHIP (4 findings folded); news harness 38, corpus 217, --check all 5/5 zero drift, frozen set byte-clean. North star (compose with the transaction signal) scoped OUT.
- [[2026-06-07-phase-28-corpus-completeness|Phase 28: Corpus COMPLETENESS + grounded-coverage interview + streaming render + branding/compliance (M7)]] — COMPLETE + accepted + committed 24e4a08. The assessment-missed core defect (incomplete extraction) fixed: 634→903 complete (dedup →875) + grounded coverage from a 28+20 user interview (not fabricated) + 28 templated build_logic + a FULL-MOTION STREAMING "agent reading" render + FINTRAC attribution relocated to a per-doc footer (non-negotiable HELD) + "AML Corpus Explorer" branding + de-piped tables; harness 148→165, --check all 4/4 zero drift, grounding core byte-unchanged
- [[2026-06-07-phase-27-corpus-shippability|Phase 27: Make the corpus demo shippable — assess → presentation fixes → faithfulness-guarded re-extraction (M7)]] — assess-only-first disposed the framing (PRESENTATION not the grounding system); cleanArticle + normalize-both-sides highlight (100%) + Act-4 build-log/lift-side beats + whole-article progressive read + a 72-agent re-extraction tightening 121 over-long flags to crisp grounded sub-spans (transitive grounding; 36 long-but-faithful kept whole) + fin-2022-a001 re-translate; --check all 4/4, harness 139→148, gate byte-unchanged, SHIPPABLE
- [[2026-06-07-phase-25-corpus-translation|2026-06-07 · Phase 25 Corpus output quality — extract → translate (natural AML red flags) + the article-processing screen (M7)]]
- [[2026-06-07-phase-24-cross-corpus-synthesis|2026-06-07 · Phase 24 Cross-corpus synthesis — a typology lens over the multi-source corpus (M7)]]
- [[2026-06-06-phase-19-durability-closeout|2026-06-06 · Phase 19 Durability closeout — corpus-explorer test harness + pin _rf_triage (M7)]]
- [[2026-06-05-phase-12-fincen-corpus-derivation|2026-06-05 · Phase 12 FinCEN corpus derivation foundation (M7)]]
- [[2026-06-05-phase-11-automated-derivation|2026-06-05 · Phase 11 Automated derivation (LLM-drafted signal config)]]
- [[2026-06-05-phase-10-fincen-corpus-crawler|2026-06-05 · Phase 10 FinCEN corpus crawler (SCALE)]]
- [[2026-06-05-phase-09-build-drift-guard|2026-06-05 · Phase 9 build-drift guard]]
- [[2026-06-04-phase-08-doc-true-up|2026-06-04 · Phase 8 doc true-up + provenance fix]]
- [[2026-06-04-m6-pipeline-walking-skeleton|2026-06-04 · M6 pipeline walking skeleton]]
- [[2026-06-04-m5-ship|2026-06-04 · M5 ship]]
- [[2026-06-04-m3-presenter-polish|2026-06-04 · M3 presenter polish]]
- [[2026-06-04-m2-multi-typology|2026-06-04 · M2 multi-typology (TBML)]]
- [[2026-06-04-m1-config-driven-refactor|2026-06-04 · M1 config-driven refactor]]

## By Hierarchy

- Milestones M0 → M5 map 1:1 to Phases 1 → 6 (see HANDOFF.md §8); M6 spans Phase 7 (pipeline slice) + Phase 8 (doc/provenance true-up) + Phase 9 (build-drift guard, HARDEN) + Phase 10 (corpus crawler, SCALE) + Phase 11 (automated derivation, AUTOMATE)
- M0–M3 done; M5 (ship) done + accepted; **M4 (live/pre-gen) skipped** by decision. **M6 (Signal Watch ingestion pipeline) — Phases 7–11 ALL completed + accepted** — the vision arc is complete: Phase 11 (AUTOMATE) automated the manual article→signal derivation, boundary-preserving (LLM proposes, build.py + schema + 2 human gates dispose). **M7 (corpus-backed demo)** — Phase 12 (completed + accepted): deterministic spine validated across all 14 FinCEN advisories (7 CLEAN/3 LOW/4 NEEDS) + LLM-backend derivation (no key) proven on a 2-advisory slice; spine assists-not-automates. **Phase 13 (completed + accepted) — the PAYOFF**: a NEW standalone corpus-explorer ship artifact (`dist/corpus/index.html` via `corpus.html`) renders the derived records as a staged 4-screen flow (select an advisory → coverage → build recommendations → signal spec); showcase byte-frozen, build.py decoupled from the authoring layer; shipped 2/14 derived live. **Phase 14 (completed + accepted) — SCALE**: pure authoring filled the explorer's live menu 2/14 → 5/14 by deriving 3 more CLEAN advisories (trafficking · Chinese MLN · Iran) via the proven --scaffold-derived→author→--check-derived loop + a dist rebuild; zero engine/spine/front-end edits (all byte-frozen). **Phase 15 (completed + accepted) — HARDEN**: fixed the 2 defects Phase 14 surfaced (extractor footnote-resume — a CLEAN advisory stops silently dropping a flag, surgical fin-2025-a003 17→18 with 0 collateral; the fin-2022-a001 esc() double-escape sweep) scoped by measurement; glued-no-separator splitting deferred (no safe split, stays FLAGGED). **Phase 16 (completed + accepted) — INVERT**: subtraction test on the accreted extract_red_flags — the LLM extracts candidate red flags, the deterministic layer became a groundedness gate (normalized `flag` ∈ normalized md replaces src_line as the traceability authority) + a section-cite relevance guard; extract_red_flags demoted to selftest-anchor; corpus-status SHAPE preserved; scaled to 7/14 incl. ransomware (previously-unreachable glued advisory; converter option dissolved). **Phase 17 (completed + accepted) — THE REAL SUBTRACTION + SCALE-TO-COMPLETE**: deleted extract_red_flags + the whole scaffold/draft authoring stack + anthropic/os (derive_signals.py 1202→600, stdlib-only; the inverted loop the SOLE derivation path); the ~14-line _rf_triage counter reuses the rf_region span; scaled 7/14 → 12/14 live (only the 2 FATF advisories non-derivable). **Phase 18 (completed + accepted) — THE ARC**: gave the corpus explorer the dramatic arc the showcase has — a 5-screen arc (Select → Coverage → Build recs/GATE → Signal → Close the loop) mirroring Act 3 (human gate, div-toggle BUILD_NOW selection) + Act 6 (loop closes, COVERAGE before→after via coverageIndex(), NOT precision-lift — that was rejected to avoid fabricated stats); corpus.html-only scope, NO schema/data/build.py change. **Phase 19 (completed + accepted) — DURABILITY**: committed tests/corpus-explorer.test.mjs (a ZERO-DEP Node vm+DOM-shim arc harness, 28 assertions, reads the committed dist) + PINNED + DISCLOSED _rf_triage (no output change); demo at Definition of Done. **Phase 20 (active — planned, direction approved) — MULTI-SOURCE SPINE**: scale the corpus beyond FinCEN advisories to other FinCEN publication types (Alerts first) via a thin SOURCES registry (decouples source-id from storage dir; build.py merges per-source corpus-status.json + derived/*.json by id; data/fincen/ byte-frozen, data/fincen-alerts/ source #2); the quote-grounding gate reused UNCHANGED; still FinCEN, still verbatim — no non-negotiable change; OFAC the documented next-source candidate. M6 (Phases 7–11) + M7 backend (Phases 12–17) + the arc (18) + durability (19) complete; Phase 20 generalizes to multiple sources.

## Living Documents

- `_CURRENT_STATE.md` — project state, active phase, next action
- `_ARCHITECTURE.md` — structural snapshot
- `tasks.md` — task tracking
- `schema.md` — wiki schema · `config.md` — ceremony (lite)

## Knowledge Wiki

- **aml-wiki** (registered central store, `/Users/jwang/private-knowledge/aml-wiki`) — AML
  domain reference. Linked via gitignored `wiki/` symlink; auto-scoped by CWD. Query: `/wiki-query`.

## Recent

- [2026-06-10] Phase 43 (Live pipeline robustness + progressive presentation, live news) IMPLEMENTED → READY FOR COMPLETION (delivery gate pending) — T1 stress harness reproduced + named the ~200s failure (primary OUTPUT-BUDGET ≈400–450 tokens/entity w/ discarded finish_reason → opaque parse error; secondary read-timeout; n_ctx=262144 overflow unreachable; intrinsic degradation REFUTED); call_llm STREAMS (SSE, idle-gap 120s, MAX_GEN_TOKENS 16384, length/stall named in-stream; 13/13 replay zero re-capture; live XL probe 193s); stage-completion LIVE rendering (grounded flags FINAL + provisional chips through verify + token counter; grounded reveal 18s); preflight n_ctx honest refusal (FAIL-OPEN) + /extract single-flight 409 + disconnect-persists-NOTHING; sectioned extraction NOT earned; --check all 5/5 zero drift, news-stream 130→140; reviewer 9/10 ACCEPT (1 HIGH extract_lock leak fixed inline); A1/A2'/A3/A4 ALL held; pure transport held (gate/prompt/schema/store-writes/fixtures untouched).
- [2026-06-10] Phase 43 (Live pipeline robustness + progressive presentation, live news) PLANNED, direction = the user's REFRAME at the dev-plan gate (assumption gate closed: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept [stage-completion rendering, NOT a token stream]) — a real-note live test timed out ~200s with no intermediate feedback; T1 measure-first stress harness reproduces + classifies the failure (timeout vs truncation vs n_ctx overflow vs intrinsic degradation) before any fix; T2 stream:true SSE inside call_llm w/ idle-gap timeout + token-count progress (SAME signature — 13 replay fixtures green, no re-capture); T3 corpus-demo-style staged reveal in the LIVE region (grounded flags + provisional entities at grounding-complete, refined through verify); T4 T1-earned pre-flight n_ctx check w/ honest refusal; T5 regate + --ctx-size docs. Pure transport (gate/prompt/schema/store-writes frozen); offline dist/news byte-identical; real note never committed. 5 lite tasks T1–T5. First step: /spec --internal (enforce hooks).
- [2026-06-10] Phase 42 (Anchor dossier view + per-scan network visualizer, live news) DELIVERED + accepted + committed ecb2de7 [work] + 0527fed [CLAUDE.md trim, own commit] + 2ec4075 [gate flip] + pushed to main.
- [2026-06-10] Phase 42 (Anchor dossier view + per-scan network visualizer, live news) IMPLEMENTED → READY FOR COMPLETION (delivery gate pending) — GET /anchor read route (400/503/404/200) wrapping the previously-unconsumed anchor_summary(); deterministic SVG network visualizer at Disposition (liveGraphLayout radial + 24-iteration relaxation; esc() everywhere; edge evidence CLOSED until clicked); anchor dossier panel (source-type provenance, properties by kind, "both kept" conflict flags, honest 404/store-off/empty, watchlist wdoss); committed SYNTHETIC note docs/demo-investigation-note.md + the accumulation demo confirmed LIVE (real Qwen: article 72.1s/14 entities + note 19.5s/4 → 2 scans, client_number C-2024-0117, phone conflict both-kept); pure consumption HELD (gate/schema/store-writes/fixtures untouched); CLAUDE.md trim 319→220 (protected sections diff-clean); news-stream 103→130, --check all 5/5 zero drift; reviewer 9/10 ACCEPT zero HIGH+; A2 bit-then-held-as-A2', A4 held w/ condition.
- [2026-06-10] Phase 42 (Anchor dossier view + per-scan network visualizer, live news) PLANNED, direction user-picked at the dev-plan gate (assumption gate closed: A1/A3 accept, A2 don't-know→A2' accept, A4 accept-with-condition [SVG = the INITIAL implementation for demo testing], A5 scope constraint) — CONSUME the Phase-41 ER model: per-scan vanilla-SVG network graph (deterministic layout, no Math.random) replaces the text subject-map at Disposition; node/watchlist click → anchor dossier via a NEW GET /anchor route wrapping the unconsumed anchor_summary() (properties by kind w/ provenance, "both kept" conflict flags, aliases, edges); a committed SYNTHETIC investigation-note demo seed (fixture articles are case-disjoint — zero cross-article entity overlap); PURE CONSUMPTION (no gate/schema/prompt/store-write/fixture change), offline dist/news byte-frozen; rider: CLAUDE.md trim 305→≤~220. 6 lite tasks T1–T6. First step: /spec --internal (enforce hooks).
- [2026-06-10] Phase 41 (Entity-resolution schema enrichment, live news) IMPLEMENTED → READY FOR COMPLETION (delivery gate pending) — aliases[] KEPT (DROP→FOLD inversion) + closed-vocab properties[] + relationships[] w/ main_subject, grounded-or-stripped by the shared gate (4 committed records pass CLEAN); DuckDB ANCHOR design (anchors + source_type / monolithic property table w/ NULL confidence / relationship edges, exact-name accumulation, legacy migration proven); watchlist/screen alias-aware (class-aware max-pair); live UI subject map + identity cards (strip intact); 3 .ph41 US-federal fixtures + FIXTURE_META privacy allowlist; the prompt-regression gate fired and was cleared by the user-ruled r2 (red_flags first in schema order, 24→25 kept flags w/ enrichment intact); --check all 5/5 zero drift; reviewer 9/10 ACCEPT zero HIGH+.
- [2026-06-09] Phase 41 (Entity-resolution schema enrichment, live news) PLANNED, direction approved (user REFRAME; assumption gate A1'/A3'a/A3'b revised-accept, A2/A4/A5 accept) — aliases KEPT (DROP→FOLD inversion) + closed-vocab properties (incl. client_number/account_number — private investigation notes a first-class future input) + relationships w/ main_subject, grounded-or-stripped by the shared gate; DuckDB anchor normalization w/ source_type + monolithic property table (confidence RESERVED) + relationship edges; exact-name cross-scan accumulation; screen name ∪ aliases; offline artifacts byte-frozen; privacy boundary local-live-layer-only. First step: /spec --internal (enforce hooks).
- [2026-06-08] Phase 36 (Persistence (DuckDB→parquet) + feedback watchlist, M8) PLANNED, direction approved — wire the Phase-35 M8 live companion to a DuckDB persistence layer + an ESCALATED-ONLY feedback watchlist, companion/venv-side with ZERO ship-path impact. The loop (the user's gate choice, REFINING D4's "book ∪ all prior-scanned"): a live scan → grounded record → companion PERSISTS to DuckDB (disposition NULL); Screen scores entities against book ∪ watchlist(escalated); ESCALATE at the Disposition gate posts back → DuckDB marks `disposition='escalate'` → the entity joins the watchlist; a later article screens against the now-larger surface (a re-mentioned escalated entity hits with provenance "escalated from <prior article>, <date>"). A domain-honest CURATED watchlist that makes the human gate CONSEQUENTIAL. ARCH: a NEW `scripts/news_store.py` OWNS DuckDB (schema scans/entities[+disposition]/red_flags; native COPY parquet) — build.py NEVER imports it; new companion-only `GET /watchlist` + `POST /disposition` + a `--export-parquet` flag; all client wiring inside `/*LIVE_START*/…/*LIVE_END*/` → stripped offline → dist/news byte-identical; DuckDB → the existing gitignored uv `.venv`; store + parquet gitignored; the companion degrades gracefully if duckdb missing. HONESTY invariants carry forward (real scores, synthetic book, always-on badge, nothing ungrounded; escalation provenance shown; escalation may target ANY extracted entity). Watchlist reconciliation: book `{role,country,segment}` + scanned `{location,age,profession,context}` collapse to a uniform `{name,type,kind,provenance}` row. ZERO new ship dep (DuckDB venv-only). FROZEN byte-clean: the showcase, the ENTIRE corpus (corpus.html + dist/corpus QOL-updated in 7e31dd8 = the new baseline), derive_signals.py, AND scripts/news_ground.py (logic-stable). 5 lite tasks (1 L = T3 the news.html live branch). NO non-negotiable change. Abort: DuckDB can't confine in the `.venv` → stdlib `sqlite3` + (DuckDB-CLI or skip) for parquet; dist/news can't stay byte-identical after the strip → surface it; T3 > L → split the client-UI sub-task
- [2026-06-08] Phase 32 (News stream: real gov-enforcement adverse-media source + presentation elevation, M8) PLANNED, direction approved — elevate the Phase-31 news stream (dist/news) on TWO fronts at once, the six-act showcase + the entire corpus staying BYTE-FROZEN; the user judged the BUILT dist/news "very low effort … a poorly staged slide show … repeating the same mistakes" (the Phase-27 corpus failure — a RESULT not a PROCESS). (1) REAL-SOURCE SWITCH — replace the SYNTHETIC fictional articles with REAL US-federal gov-enforcement adverse media (DOJ/OFAC/Treasury-FinCEN/SEC), public domain under 17 U.S.C. §105 (the corpus's exact verbatim basis, Phase 21 — NOT a non-negotiable change; the book stays SYNTHETIC, #4 held; REAL entity × SYNTHETIC book); the user's "real online news, ignore copyright" reframed under pushback to the clean public-domain gov-enforcement path. (2) PRESENTATION ELEVATION to the corpus's bar — the streaming "agent reading" Read render + entity cards (name/location/age/profession, each grounded) + a visible scan PROCESS on Screen + the full corpus dossier theme. T1 (M) acquire ~4–6 real docs + re-derive grounded entity attributes + reseed the book · T2 (M) full dossier theme + per-stage labeling · T3 (L) streaming Read render + entity cards · T4 (M) the visible scan PROCESS on Screen · T5 (M) harness + dist + docs + regate. Acquisition is build-time/authoring ONLY; the runtime stays a pure client-side matcher (offline, REAL scores). build.py edited ADDITIVELY (validate_news_data extended for entity attributes); the showcase + the ENTIRE corpus + the grounding core derive_signals.py stay BYTE-FROZEN. HONESTY: entity attributes ground or drop; the scan shows REAL scores (no fake progress bar); the book synthetic; the always-on badge stays. The user said "do both" → split by frozen set: Phase 32 = news (this), Phase 33 = corpus completeness + typology re-segmentation (QUEUED, workflow-driven). 5 lite tasks. Abort: a real doc that can't ground rich attributes → pick another / carry fewer, never fabricate; the scan that can't be made legible honestly → degrade to a ranked static table, never fake a score/threshold/progress bar. M8 north star (compose with the transaction signal) scoped OUT
- [2026-06-06] Phase 23 (FINTRAC depth — grow source #4 with the remaining anchorable strategic-intel products: Operational Alerts + Operational Briefs, M7) PLANNED, direction approved — grow the existing FINTRAC corpus source #4 from 3 derived Operational Alerts to the full anchorable set of FINTRAC strategic-intelligence products carrying enumerated ML/TF indicators (OAs AND Operational Briefs, incl. the real-estate brief). "Canadian depth" for the audience (a Canadian bank): FINTRAC source 3 → ~8-11 (second-deepest source). NO new source, NO non-negotiable change (the FINTRAC Crown-copyright non-commercial reproduction basis was established Phase 22), NO architecture / build.py / corpus.html STRUCTURAL edit (the menu, doc_type chips, count line are data-driven). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted loop + the quote-grounding gate; grounding core normalize/check_record BYTE-UNTOUCHED. THE ONE REAL RISK = heterogeneous heading forms — the real-estate Operational Brief heads its ~40-indicator list with bare "TABLE OF INDICATORS" (vs the OAs' ML/TF-qualified "Money laundering indicators"), which the narrow FINTRAC anchor won't match → a REGRESSION-GATED narrow-anchor widening per new heading form (0-collision across all 36 FinCEN+OFAC mds, every existing rf_region byte-unchanged, all 35 existing records clean; chosen OVER a source-scoped rf_region refactor, deferred). T1 (M) acquire + convert the candidate batch + per-doc rf_region triage + capture the baseline · T2 (L) the GATE change (narrow specific 0-collision anchor per new form; NO-OP if no new form) · T3 (M) derive the anchoring batch via the inverted loop · T4 (S) regen + rebuild + drift guard + harness counts + a FINTRAC Brief walking the arc · T5 (S) docs. EXCLUDE FINTRAC's sector GUIDANCE indicator pages. FROZEN: index.html, config/**, 3 typology dists, build.py + corpus.html, the THREE OTHER sources + the 3 EXISTING FINTRAC records. Abort: if a new form shifts ANY existing rf_region or fails ANY of the 35 records, SKIP that doc (honest non-derivable) — never broaden a shifting anchor. 5 lite tasks (1 L). User chose FINTRAC depth at the goal gate (audience = a Canadian bank) + OAs + Briefs WITH the regression-gated widening at the scope gate
- [2026-06-06] Phase 19 (Durability closeout — commit corpus-explorer test harness + pin _rf_triage, M7) PLANNED, direction approved — a durability closeout at the end of the completed M0–M7 roadmap (demo at Definition of Done): no net-new feature value remains without fabrication risk (combination-lift rejected Ph18) and no scale frontier inside FinCEN (12/14 derived; the 2 FATF have no red-flag list), so the one remaining piece with real non-fabrication value is durability — a committed test harness protecting the corpus.html/build.py investment, flagged a soft observation three phases running. T1 (M): commit a ZERO-DEP Node DOM-shim harness (tests/corpus-explorer.test.mjs) that reads the committed dist/corpus/index.html, drives the 5-screen arc, asserts the ~15 Ph18 invariants — NEVER jsdom (file:// offline ethos + the dep-free --selftest/--check idiom; loading the committed dist doubles it as a build-output smoke test). T2 (S): PIN + DISCLOSE the _rf_triage footgun (Ph17 reviewer MEDIUM) — a glued-fixture --selftest assertion + a one-line "coarse hint, not authoritative for glued" comment, NO output change (an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today). T3 (S): wire the harness command into README + CLAUDE + smoke-checklist. Two carried candidates resolved in planning: the anthropic pin is DEAD (requirements-authoring.txt does not exist — Ph17 deleted it), the tests/ gap is REAL. UNFREEZE: tests/**, scripts/derive_signals.py (comment + selftest fixture ONLY), README.md, CLAUDE.md, tests/smoke-checklist.md; FROZEN: index.html, corpus.html, scripts/build.py, config/**, data/fincen/**, ALL dist/**. NO behavioral change — corpus-status.json + dist/** byte-identical; --check all zero drift. 3 lite tasks. Abort: DEGRADE to a leaner assertion set if the shim can't drive the arc — NEVER pull jsdom into a file:// offline demo. User chose durability closeout over call-it-done / showcase-debt-true-up / new-stakeholder-ask
- [2026-06-06] Phase 17 (Complete corpus derivation + delete extract_red_flags — the real subtraction, M7) PLANNED, direction approved — finish the Phase-16 inversion with the REAL line-count subtraction + scale to completion, sequenced subtraction-FIRST. (A) Delete extract_red_flags + the now-dead --scaffold/--draft/--scaffold-derived authoring stack (scaffold_config, write_scaffold, draft_judgment, write_draft, _few_shot, _apply_judgment, _derived_skeleton) the inverted loop replaced; add a ~12-line rf_region-bounded triage counter (reuses the existing rf_region span, counts candidate lines inside it) so the deletion is clean + --corpus-status stays regenerable for a future advisory ("corpus may grow later"). derive_signals.py ~1202 → ~700, leaving normalize + rf_region + check_record + counter + --corpus-status + --selftest (gate-only). The debrief named "delete extract_red_flags" as the top candidate but had the dependency backwards — extract_red_flags is off the correctness path (the LLM extracts, the gate disposes) yet is the SOLE producer of the extraction/flag_count triage the byte-frozen corpus.html renders for not-yet-derived advisories (corpus.html:281-284, build.py:373); the counter replaces that role and is materially smaller than the 130-line section-finder (subtraction SHRINKS, doesn't move). (B) Scale on the stripped spine: derive the 5 remaining derivable advisories (fin-2026-a001 glued, fin-2021-a001, fin-2024-a001 LOW, fin-2025-a001 LOW, fin-2022-a002 EFE) via the inverted loop → 7/14 → 12/14 live (only the 2 FATF advisories non-derivable); validate-first, degrade gracefully (skip + leave not-yet-derived; the counter handles its chip). FROZEN/byte-untouched: index.html, corpus.html, config/**, scripts/build.py, 3 typology dists (counter preserves corpus-status.json SHAPE). 6 lite tasks (1 L = the subtraction). User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table (un-regenerable stale triage). Abort: if the counter can't preserve corpus-status SHAPE / pass build.py validation without growing as complex as extract_red_flags (subtraction MOVES not shrinks), STOP the deletion, keep extract_red_flags, narrow to scale-only; a LOW/glued advisory that won't ground faithfully degrades to scale-what-passes (deletion still lands — counter-gated, not scale-gated)
- [2026-06-06] Phase 16 (Invert extraction — LLM extracts, deterministic gate disposes — + scale as proof, M7) PLANNED, direction approved — SUBTRACTION TEST on the ~130-line accreted extract_red_flags machinery (_SECTION_STOP/_FOOTNOTE_STOP/_CITATION; still only 7/14 clean, and the spine already only ASSISTS not AUTOMATES — the LLM stitches the page-broken/header-glued flags the regex can't). INVERT: the LLM (this session) EXTRACTS candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE — normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority — + a cheap section-cite relevance guard. "Deterministic validators at boundaries over neural judges at the end" — LLM at the start (generative), validator at the boundary; complexity relocates from brittle section-PARSING (open) to md NORMALIZATION (closed: running headers, form-feeds, hyphen-breaks, smart quotes, whitespace). matrix-consistency + BUILD_NOW⇒logic + shape checks UNCHANGED; the 5 committed records migrate nearly free (each `flag` already carries the verbatim span). extract_red_flags DEMOTED to selftest-anchor + scaffold hint; corpus-status keeps its SHAPE (build.py/corpus.html byte-untouched), derivable false only for the 2 FATF advisories. Scale as PROOF over count — 2 new records incl. ≥1 previously-LOW/glued advisory (COVID-EIP fin-2021-a002 + ISIS fin-2025-a001 OR ransomware fin-2021-a004) → 5/14 → 7/14 live; pymupdf4llm converter option DISSOLVED. The named trade: groundedness proves a quote is REAL TEXT, not a GENUINE red flag — the section-cite check + the 2 human gates dispose on residual relevance. 5 lite tasks (1 L = the gate + normalizer). User chose invert-architecture over pure breadth-scaling / a structure-preserving converter. Abort: if the normalizer can't ground the escrow/glued cases without becoming as complex as the extractor it replaces (subtraction test FAILS), keep quote-grounding for the clean records + narrow breadth to COVID-EIP only
- [2026-06-05] Phase 15 (Harden extraction faithfulness + fix shipped defects, M7) DELIVERED + accepted — scoped by MEASUREMENT (the LOW/NEEDS advisories fail for 3 distinct reasons, not one). Footnote-resume fix in extract_red_flags (3 iterations): _SECTION_STOP terminals always break + a conditional _FOOTNOTE_STOP (mid-list page-boundary footnote run transient when another section follows → skip+resume to the next anchor; terminal for a last section → break) + 2 targeted _CITATION signatures (case-docket + no-day paren-date) to kill 2 footnote-tail leaks. SURGICAL: fin-2025-a003 recovered its silently-dropped L499 escrow flag (17→18), 0 collateral — all 13 other advisories byte-identical, EFE 12+12, 7C/3L/4N held. esc() entity sweep (fin-2022-a001 + fin-2024-a002 `&gt;=`/`&lt;=` → raw text, verified in the built file); escrow IND-18 added; manifest regen + dist/corpus rebuild + --check all zero drift. GLUED-NO-SEPARATOR SPLITTING DEFERRED — no safe deterministic split (re-confirms Phase-12); needs a better converter, not a splitter. The abort rule was hit (first attempt over-captured) but resolved by the bounded next_boundary rule, not aborted. 5 lite tasks; byte-frozen held. User chose harden-spine over scaling / showcase debt / wow beat
- [2026-06-05] Phase 14 (Scale corpus derivation — 3 more CLEAN advisories → 5/14 live, M7) DELIVERED + accepted — PURE AUTHORING (zero engineering): derived 3 more records via the proven --scaffold-derived→author→--check-derived loop, rebuilt dist/corpus → live menu 2/14 → 5/14. fin-2020-a008 human trafficking (10 ind, pruned 1 noise line, 2 BUILD_NOW) · fin-2025-a003 Chinese MLN (17 ind, clean, 5 BUILD_NOW — most buildable) · fin-2025-a002 Iran (16 ind, validate-first passed, 4 BUILD_NOW / 7 BUILD_ENRICH — enrichment-hungry contrast). Matrix-merge authoring (verbatim flags + src_line preserved, build_rec auto-derived). Verified: 3× --check-derived · --check all 4-artifact zero drift · headless render assertions · node --check · --selftest 12+12; index.html/corpus.html/config/**/scripts/** + 3 typology dists byte-untouched. FINDINGS → Phase-15: extractor missed fin-2025-a003 L499 (page-break-glued flag); pre-existing fin-2022-a001 esc() double-escape render bug. User chose scale-derivation over spine-robustness / corpus-wow-beat / showcase-debt
- [2026-06-05] Phase 13 (Corpus explorer — advisory-selection front-end + per-indicator build-rec render, M7) PLANNED, direction approved — the PAYOFF: a NEW standalone ship artifact `dist/corpus/index.html` (built from `corpus.html`), a FinCEN CORPUS EXPLORER where a stakeholder picks 1 of 14 advisories and watches the loop derive coverage → per-indicator build recommendations → signal. STAGED 4-screen flow (SELECT → COVERAGE → BUILD RECOMMENDATIONS → SIGNAL SPEC); all 14 advisories shown with honest status (2 derived live). Showcase (index.html + config/** + 3 typology dists) byte-frozen; corpus.html owns its own theme CSS; build.py reads committed data (corpus-status.json + derived/*.json), never imports derive_signals.py. 5 lite tasks (1 L = corpus.html). User chose standalone over fold-into-index.html, staged over dashboard, all-14-honest over only-2-derived
- [2026-06-05] Phase 12 (FinCEN corpus derivation foundation, M7) DELIVERED — backend for a singular corpus-backed FinCEN demo (user picks 1 of 14 advisories). Committed the 14-advisory corpus md; `extract_red_flags` rewritten as a corpus-wide section-FINDER (Tier-1 + Tier-2 fallback + filters); `--corpus` → 7 CLEAN · 3 LOW · 4 NEEDS (2 NEEDS = FATF advisories, correct). Deterministic checks (cover×data matrix + traceability + build_logic shape). LLM backend = THIS session (no key) derived 2 records (kleptocracy + PRC precursors), boundary holds. Review 8/10→fixed. Spine ASSISTS, doesn't AUTOMATE. EFE 12+12; engine untouched. 5 lite tasks + 2 user refinement passes; demo expansion = Phase 13
- [2026-06-05] Phase 11 (Automated derivation — LLM-drafted signal config, AUTOMATE) completed + accepted — authoring-only `derive_signals.py` automates the manual Phase-7 article→signal derivation (deterministic `--selftest`/`--scaffold` + neural `--draft`, lazy `anthropic`, env-keyed); LLM proposes a gitignored `.draft.json`, build.py + schema + 2 human gates dispose; Anthropic structured-output shape verified vs the claude-api reference; review gate 9/10 accept (2 MEDIUM `--draft` fixes folded in); 5 lite tasks; commit c37dc39. M6 vision arc (7–11) complete
- [2026-06-05] Phase 10 (FinCEN corpus crawler — SCALE) completed + accepted — discovery manifest (`data/fincen/index.json`, 14 advisories) over mass-download; authoring-only `crawl_fincen.py` (pure `parse_index` + `--selftest`); acquire REGISTRY→manifest + `resolve_pdf` detail-page hop; 4 lite tasks; commit 0c87c47 (user chose SCALE over the elder true-up)
- [2026-06-05] Phase 9 (build-drift guard) completed + accepted — in-process `build.py --check` for the M5 zero-drift invariant (broke silently in Phase 7); commit 33db22a
- [2026-06-04] Phase 8 (doc true-up + provenance fix) completed + accepted — rebrand docs to Signal Watch; FinCEN-verbatim non-negotiable; fentanyl provenance → FINTRAC; M6 doc staleness folded in; Phase 7 dist drift corrected; commit 042d732
- [2026-06-04] M6 (pipeline walking skeleton) completed + accepted — FinCEN EFE FIN-2022-A002 slice; Act 1 verbatim render; Signal Watch rebrand; commit 8459dd9
- [2026-06-04] M5 ship — completed + accepted; compliance + offline file:// hard gate PASS; project shipped
- [2026-06-04] M3 presenter polish — engine-only keyboard nav + reset + reduced-motion; both dist rebuilt
- [2026-06-04] dev wiki bootstrapped (retrofit from HANDOFF.md milestone plan); aml-wiki linked
