# Active Phase Context

Phase: 45 — Corpus demo presentation polish (pre-presentation day). Lite. 6/6 tasks [x] same-session — READY FOR COMPLETION; delivery gate pending. dist/corpus presents to bank stakeholders 2026-06-11.

Objective (DELIVERED): T2 the headline — the fake 18→64→83 LIFT template + "pending calibration" tag DELETED; the beat renders R2 REAL inventory counts (covered indicators in the committed signal's typology × contributing regulators, client-side from __CORPUS__; NO performance claim) — completes the Phase-18 honesty arc. T1 live-risk fixes (stagger cap · gate copy reframe · zero-build-now dead-end). T3 FINTRAC attribution MULTI-DOC on the two quoting lens drills (adversarial-pass upside; synthesis silent per Phase-28). T4 ranked copy pass. T5 walkthrough FROZEN (2 items fixed: display-only fixEncoding mojibake repair [records/md BYTE-FROZEN; byte-surgical repair DEFERRED] + landing hook reshape, 2 rounds). T6 full regate.

Scope (held): corpus.html DISPLAY LAYER ONLY · dist/corpus/** (rebuilt — frozen baseline MOVED by design) · tests/corpus-explorer.test.mjs (239→273) · tests/smoke-checklist.md (presenter/demo-path notes) · CLAUDE.md (honesty paragraph in place) · .dev-wiki/* · specs/. No scripts/, data/, or module changes.

Key constraints HELD: grounding core + derived data + build pipeline FROZEN; showcase index.html + 3 typology dists + news artifacts byte-identical (Act-5 lift divergence gate-accepted, A2); NO new fabricated/similarity/overlap/lift number; always-on badge stays; NO non-negotiable change.

Exit criteria: ALL MET — three story HIGHs in; no fabricated lift number remains (R2 honest counts, independent-recomputation-pinned); FINTRAC attribution fires on FINTRAC-bearing drills / empty US-only; copy MEDIUMs done; walkthrough run, no open HIGH, notes in smoke-checklist; --check all 5/5 + corpus 273 + news-stream 150 + all selftests green. Assumption revisit: A1–A5 ALL held (A4 with the multi-doc upside).

Next action: DELIVERY GATE — present the post-implementation report; on acceptance commit to main (phase-work commit, then the gate-log flip in its OWN post-commit-verify commit) and push; then present 2026-06-11 (pre-present: --check all → node suites → smoke-checklist walk).

Abort rule (stood down — phase complete): blocked >3 attempts → ask skip or abort.

Gates:
- [x] Direction confirmed by user (assumption gate closed 2026-06-10: A1 don't-know→defended with worked real-value examples→accepted, A2–A5 accept)
- [ ] Delivery accepted
