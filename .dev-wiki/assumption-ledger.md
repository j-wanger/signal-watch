# Assumption Ledger

Append-only. One block per phase direction gate (dev-plan Step 13). `revisit-status:` filled at debrief.

## Phase 39 — Live news QOL: streamed extraction progress + one-shot URL acquisition (2026-06-09)

all_accept: false (A1 rejected in round 1 → revised → accepted with condition)

- A1 [HIGH] URL flow shape. Round 1 (preview-then-run): REJECT — user wants one-shot. Round 2 (one-shot /extract {url}, converted text fills textarea as passive recovery): ACCEPT WITH CONDITION — acquisition must go through a multi-method fetch ladder (bot guards expected) + a scripted format STANDARDIZER + VERIFIER before extraction. revisit-status: bit (the accept-with-condition standardizer/verifier turned out LOAD-BEARING — justice.gov answered urllib with a 2.5KB Akamai interstitial converting to 0 chars; a fetch-error-only ladder failed; D5 verifier-advances-the-ladder + D6 interstitial two-step were the fix — the condition validated the design)
- A2 [HIGH] Progress = real stage-level chunked-NDJSON events over POST /extract (fetch+ReadableStream client); token streaming + client-only spinner rejected; job-id+polling is the verified-at-impl fallback if stdlib chunked flush misbehaves. ACCEPT. revisit-status: held (HTTP/1.0 body-until-close per-line write+flush streamed cleanly; the polling fallback was never needed)
- A3 [MED] Verify-latency cause (per-entity second pass, N sequential model calls) stays OUT of scope — feedback this phase, batching stays deferred. ACCEPT. revisit-status: held (latency untouched by design; progress made the 42.7s wait legible, not shorter; batching stays the deferred candidate)
- A4 [MED] Invariants carry forward: all client code in /*LIVE_START*/…/*LIVE_END*/ → offline dist/news byte-identical; markitdown .venv-only lazy import + graceful degrade; build.py never imports the live layer; extract()/call_llm fixture seam preserved (on_progress defaults no-op, no fixture re-capture). ACCEPT. revisit-status: held (replay fixtures green WITHOUT re-capture; dist/news byte-identical; --check all 5/5 zero drift)

## Phase 40 — Live red-flag extraction quality (measure-first) (2026-06-09)

all_accept: false (A3 + A4 rejected in round 1 → revised → accepted)

- A1 [HIGH] The flag-quality gap is INSTRUCTION CLARITY, not Qwen capability — few-shot exemplars from committed gate-passing records + mechanism vocabulary generalize beyond calibration (holdout split guards the Phase-38 overfit trap). If false: fix shifts to the verify layer or an honest local-model-ceiling finding. ACCEPT. revisit-status: held (prompt-shaping closed the gap on HOLDOUT — covR 0.40→0.55, mechCovR 0.46→0.63, positional decay eliminated 0.64→0.43 second-half miss; though the recall/precision frontier took 3 calibration rounds, r3 rolled back on measured regression)
- A2 [HIGH] The negative-news wiki's 2,313 raw articles are usable stress material via deterministic frontmatter filter (extraction: full + source_score floor); stored bodies → text-mode runs independent of URL liveness; local-only/never committed; fixture promotion stays US-federal-only. ACCEPT. revisit-status: held (with deviation: the corpus WAS usable stress material, but `extraction: full` does NOT exist — 2,256/2,314 files carry no extraction field; the deterministic filter was rebuilt on the committed news_fetch standardize→verify_article gate + a measured `continue reading < 3` anti-listing rule; selection stayed deterministic, manifest frozen local-only)
- A3 [MED] Measurement design. Round 1 (deterministic proxies + qualitative cluster adjudication only): REJECT — user wants the quantitative second-rater design. Round 2 (proxies PLUS blind second-rater reference extraction → INTER-RATER AGREEMENT per dimension [completeness/span/register], divergence clusters human-adjudicated, agreement = consensus never ground truth, T4-verify trigger = measured agreement delta): ACCEPT. revisit-status: held (the blind second-rater design produced the decisive signal — the residue-is-RECALL call, the covQ-undercredits finding via blind validity judging of unmatched flags, and the user's registry-scoring reframe all came out of it)
- A4 [MED] Gate posture. Round 1 (news_ground.py FROZEN, checks stay measurement-only): REJECT — user expects measurement-earned deterministic flag-quality checks to SHIP into the gate. Round 2 (news_ground.py UNFREEZES: near-dup collapse / span-quality checks land as shared gate rules for companion + build.py; the 4 committed news records must pass the extended gate — failures are adjudicated findings, never silent loosening; replay goldens regenerate DETERMINISTICALLY from pinned qwen.json captures, no model re-capture; full regate incl. --check all; dists byte-identical): ACCEPT. revisit-status: held (the gate unfroze cleanly — flag_dup_key dup-collapse landed shared [live DROP / build CHECK]; the 4 committed records passed the extended gate with NO adjudication needed; replay goldens valid WITHOUT regeneration; --check all 5/5 zero drift)

## Phase 41 — Entity-resolution schema enrichment (live news) (2026-06-09)

all_accept: false (direction itself was a user REFRAME off the offered candidates; A1 rejected in round 1 → revised → accepted with conditions; A3 rejected in round 1 → split A3'a/A3'b → both accepted)

- A1 [HIGH] Attribute-slot basis. Round 1 (census-earned slots from the public federal corpus): REJECT — the real input domain includes PRIVATE investigation notes carrying address/phone/email + client_number/account_number; slots are designed for that domain, not census-limited. Round 2 (A1' two-layer model: nested per-scan extraction JSON [grounded-or-stripped] + DuckDB ANCHOR design — entity anchor table + property association table [entity×kind×value edge w/ evidence + provenance + grounded status] + relationship edge table; confidence column reserved, NOT model-populated until a measured basis): ACCEPT WITH CONDITIONS — (1) anchors need an entity SOURCE TYPE (document types differ in significance: gov-enforcement / commercial-news / investigation-note); (2) monolithic-vs-per-property-subtables is a real scaling decision — monolithic+kind chosen now, revisit on measured divergence. revisit-status: held (both conditions implemented — scans.source_type stamps anchors + watchlist provenance; ONE monolithic entity_properties table w/ reserved detail-JSON landed, no kind-schema divergence at this scale; confidence stayed NULL; the non-destructive both-kept conflict rule fixture-proven)
- A5 [HIGH] Privacy boundary (NEW, surfaced by the A1 reject): private/client data is first-class INPUT but confined to the local live layer — gitignored DuckDB store + live session only; never committed, never fixture-promoted (fixtures stay US-federal public-domain only), never in ship artifacts; local 127.0.0.1 model means notes never leave the machine; the "no real customer data ever" non-negotiable governs everything committed/shipped. ACCEPT. revisit-status: held + STRENGTHENED (the boundary became a CHECK, not a convention — fixture_replay_test asserts every fixture base id ∈ the US-federal FIXTURE_META allowlist; nothing private/commercial committed; pinned captures byte-clean)
- A2 [HIGH] Relationships are honestly gateable: each edge {from, to, label, evidence} = verbatim-grounded evidence quote + referential integrity to extracted entities + label from a small closed vocab (~8 relation types); the LABEL stays a neural judgment (vocab-checked, never correctness-checked — the C/D-code honest split); main subject = the entity the enforcement action targets, multi-subject honest. ACCEPT. revisit-status: held (13 gate-passed evidence edges across the 3 .ph41 federal captures; DRQ3 distribution owner-or-controller-of 9 / employee-or-agent-of 3 / co-conspirator 1 / other 0 — the vocab covers; honest multi-subject exercised, tgr 7 mains)
- A4 [MED] Alias inversion: the current alias-dedup DROP (token-subset names) + moniker DROP (@handles) become FOLDs — kept as aliases attached to the fuller entity; fixture-pinned drop behavior changes deterministically; screening matches name ∪ aliases. ACCEPT. revisit-status: held (folds land w/ folded_into audit + reconcile_refs remap; orphan handles still drop; UNEXPECTED upside — the model's own aliases[] extraction attached @monalisa7→Zhdanova where structural adjacency could not: the prompt does ER work the deterministic layer can't; class-aware screen rules [single-token/@-handle exact-only] kept the common-name trap dismissable)
- A3 [MED] Scope. Round 1 (bundled live-only/offline-frozen/merge-deferred): REJECT — ambiguous bundle. Round 2 split: A3'a offline demo artifacts byte-frozen this phase (dist/news + 4 committed derived records + book.json; enrichment renders in the live region only): ACCEPT. A3'b cross-scan ACCUMULATION in scope via the anchor table (exact-normalized name → same anchor, properties accumulate with provenance); fuzzy cross-scan MERGE adjudication DEFERRED: ACCEPT. revisit-status: held (offline dist/news + 4 committed records + book.json byte-frozen, --check all 5/5; exact-name accumulation proven 4-scans→1-anchor; merge stayed out; LATE BITE adjacent to A3's silent sibling: the enriched ONE-CALL prompt regressed the flag layer ~12.5% — caught by the SPEC's prompt-regression constraint, user-ruled, fixed by r2 flags-first schema ordering 24→25)

## Phase 42 — Anchor dossier view + per-scan network visualizer (live news) (2026-06-10)

all_accept: false (A2 don't-know in round 1 → evidence check REFUTED the cross-article-overlap seeding claim → revised A2' accepted; A4 accepted with condition)

- A1 [HIGH] LIVE-only rendering: the network visualizer + anchor dossier render in the companion LIVE region only; offline dist/news + the 4 committed records stay byte-frozen (committed records carry no relationships[] — an offline graph would force enrichment + regate + dist unfreeze, a different phase). ACCEPT. revisit-status: held — offline dist/news + the 4 committed records + book.json byte-frozen (`--check all` 5/5 zero drift); all new client code LIVE-region-only, the offline strip intact (harness-asserted).
- A2 [HIGH] Demo payoff seeding. Round 1 (documented re-scan of the 3 .ph41 articles, "which share entities"): DON'T-KNOW — evidence check REFUTED the claim (the 7 fixture articles are disjoint enforcement cases, zero cross-article entity overlap). Round 2 (A2': seeding is a small deliverable — same-article re-scan for accumulation/provenance PLUS a committed SYNTHETIC investigation note referencing a fixture entity [client_number + one deliberately conflicting property] pasted as source_type=investigation-note; note committed clearly-labeled synthetic per the book.json precedent, the resulting store stays local/gitignored): ACCEPT. revisit-status: bit (round 1) then held as A2' — the don't-know was the right position: the evidence check REFUTED the cross-article-overlap claim PRE-implementation; the revised synthetic-note seed CONFIRMED live at the T4 spec checkpoint (real Qwen: 2 scans w/ source-type provenance, client_number C-2024-0117, phone conflict flagged both-kept).
- A3 [MED] Integrated shape: the per-scan graph is the NAVIGATION surface (rendered at Disposition, replacing the Phase-41 text subject map); node click — or a watchlist row — opens that entity's anchor dossier (GET /anchor → anchor_summary) as the detail view; conflicts surfaced presentation-only (same kind, >1 distinct value → flagged, both kept, never auto-resolved — Phase-41 D2). ACCEPT. revisit-status: held — graph-as-navigation shipped at Disposition; node→dossier AND watchlist→dossier (wdoss) both wired; conflicts rendered "both kept", presentation-only.
- A4 [MED] Rendering tech: hand-rolled vanilla SVG, DETERMINISTIC layout (radial initial placement + fixed-iteration relaxation, no Math.random, no vendored lib; node counts ≤ ~35). ACCEPT WITH CONDITION — SVG is the INITIAL implementation for demo testing; revisit rendering tech (richer interaction / vendoring) if the live tool grows past demo scale. revisit-status: held (condition recorded) — SVG-initial shipped, deterministic layout harness-pinned; reviewer noted the min-gap pass can wrap a dense outer ring's last node past the first (harmless ≤35 nodes) — fold into the rendering-tech revisit if the tool grows.
- A5 [scope constraint, agent-verified] Pure consumption phase: no change to news_ground (shared gate), EXTRACT_SCHEMA/SYSTEM_PROMPT, store WRITES, or replay fixtures (no re-capture); investigation-note source_type already exists (Phase 41); anchor_summary already built. Recorded as a contract constraint, not a positioned assumption. revisit-status: held — pure consumption verified by the reviewer via git diff: news_ground.py / build.py / store writes / replay fixtures untouched; no re-capture.

## Phase 43 — Live pipeline robustness + progressive presentation (2026-06-10)

all_accept: false (direction itself was a user REFRAME off the offered candidates — a live test on a REAL investigation note timed out ~200s with no intermediate feedback; A2 rejected-by-reframe in round 1 → revised A2' accepted)

- A1 [HIGH] The live failure is LIMITS-class — the 180s call_llm read timeout, max_tokens=4096 truncation, or context overflow from the default llama-server launch (docs set no --ctx-size) — and a synthetic stress corpus at size/complexity tiers can reproduce it; NOT intrinsic model degradation on complex notes. If false: streaming/limit fixes yield a slow bad extraction and sectioned extraction becomes the main fix. ACCEPT (T1 measures + classifies before T2–T4 commit). revisit-status: held — T1 reproduced BOTH limits-class causes (OUTPUT-BUDGET primary at ~10+ entities / 4096 tokens, READ-TIMEOUT secondary at >180s once the budget was raised); intrinsic degradation REFUTED (planted recall perfect at every tier, head+tail); context overflow unreachable at the probed n_ctx=262144.
- A2 [HIGH] Progressive rendering shape. Round 1 (token-stream partial-JSON parse renders red flags mid-generation): REJECT-BY-REFRAME — user doesn't want visible token streaming/agent-thinking; the article content is static, a corpus-demo-style progressive reveal is the desired effect. Round 2 (A2': STAGE-COMPLETION-driven rendering — converted text → grounded red flags + provisional entities reveal as soon as grounding completes [early in wall-time; the verify loop is the measured majority] → entities refine live through verify i/N → final record; internal stream:true in call_llm serves ONLY the timeout fix + an elapsed/token progress counter, no partial-JSON parsing, no visible token stream): ACCEPT. revisit-status: held — stage-completion rendering shipped exactly as revised (grounded flags FINAL + provisional chips refined through verify + token counter; no token stream, no partial-JSON parse); verified live end-to-end (demo note: grounded at 18s, verdicts streaming).
- A3 [MED] Showing mid-pipeline content before the verify pass completes is acceptable when honestly staged — red flags render GROUNDED (the gate has already disposed by then); entities render provisional and refine as verify drops non-subjects; the final record remains the only persisted output. ACCEPT. revisit-status: held — red flags render post-gate (grounded) at the grounding-complete event; entities provisional until verified; the final record is the only persisted output; the 'persisting' probe event guarantees disconnect-before-done persists nothing.
- A4 [MED] Pure-transport scope: streaming stays INSIDE call_llm (same signature, full-text return → the 13 replay fixtures green, NO re-capture); news_ground / SYSTEM_PROMPT / EXTRACT_SCHEMA / store writes FROZEN; all client changes in the LIVE region; offline dist/news byte-identical (--check all 5/5). Sectioned extraction, IF earned at T4 by measurement, feeds section text without prompt/schema change — any prompt unfreeze is a surfaced finding, not silent drift. ACCEPT. revisit-status: held — pure transport verified by the reviewer via git diff (news_ground/EXTRACT_SCHEMA/SYSTEM_PROMPT/store writes/fixtures/news_fetch/build.py untouched); 13/13 replay green ZERO re-capture; offline dist/news byte-identical (--check all 5/5); sectioned extraction not earned, not built — no prompt/schema unfreeze needed.

## Phase 44 — Live extraction quality: targeted harness, classified fixes, processing page (2026-06-10)

all_accept: false (direction itself was a user REFRAME off the offered candidates — live extraction quality: missed high-risk-country wire flags + wrong alias assignments + a fresh processing page + "do we need a better specific harness?"; A1 accepted WITH conditions [sample sentences only + speed surfaced as a concern → quality-gated optimization task added]; A3 don't-know in round 1 → defended with the Phase-40/41 seam precedent → accepted)

- A1 [HIGH] The two reported failures REPRODUCE on constructible material — synthetic high-risk-country wire notes + local commercial articles, seeded with the user's failing examples. If false: we tune against a proxy and may fix the wrong thing (the Phase-40 r3 regression precedent). ACCEPT WITH CONDITIONS — (1) the user can provide SAMPLE SENTENCES only (not full notes): the harness embeds them into synthetic note scaffolds; kept local/gitignored, never committed; (2) processing SPEED surfaced as an additional concern ("don't know if we can optimize without losing quality") → resolved as a quality-gated optimization task: T1 measures per-stage wall-time, optimization lands ONLY where the harness proves quality holds. revisit-status: held — the failures REPRODUCED on constructible material (synthetic wire notes: 4/5 planted wire facts dropped on wrapped prose; the user's SEEDED STR sample sentences confirmed the gate-drop class on the real register, 3/5 dropped → 0 after the fix); the speed condition resolved as the T4 MEASURED skip (the hotspot IS model generation, 92–98% of wall on note-register input — no quality-preserving lever at a fixed model; slot-parallel verify + harness-gated smaller-model eval named as future levers).
- A2 [HIGH] The failures are CLASSIFIABLE by the harness into actionable causes — flags: missed-at-generation vs dropped-at-gate (grounding drop / dup-collapse) vs registry blind spot (the SYSTEM_PROMPT registry already names high-risk-jurisdiction as a family); aliases: model-generated bad alias vs deterministic fold misparent (token-subset or _adjacent_parent moniker). The fix follows the class; if ambiguous, eval rounds burn. ACCEPT. revisit-status: held — classification was DECISIVE: flag misses = GATE-DROP (raw-substring grounding broken by hard line-wraps + `*`-strip asymmetry + title-line quotes), NOT model recall (5/5 on single-line bullets); alias misassignment = predominantly DETERMINISTIC fold misparent (rule (a) proven order-dependent + type-blind), not model aliases; each fix followed its class exactly (T2 locate_span requote; T3 ambiguity-refusal + type-match); the constructed P1c wrong-owner model alias stayed a MEASURED residual (the harness alias_suspects dimension watches it; zero live evidence).
- A3 [MED] The fixes stay within KNOWN seams — prompt iteration through the existing prompt-regression gate (red_flags-FIRST schema order preserved, never-reduce guard); news_ground/fold changes through the known regate procedure (4 committed records + 13 replay goldens regenerate deterministically, NO re-capture); a structural EXTRACT_SCHEMA change surfaces as a finding first. Round 1: DON'T-KNOW → defended with evidence (Phase 40 rewrote SYSTEM_PROMPT, Phase 41 changed EXTRACT_SCHEMA + news_ground — both zero re-capture, regate triple green both times). Round 2: ACCEPT. revisit-status: held — the locate_span wrap-tolerant requote + the fold ambiguity/type rules landed through the KNOWN seams: 13/13 replay fixtures green ZERO re-capture (the goldens were not even regenerated — no pinned capture had wrap-broken quotes; the gate change is purely additive on the live path); the 4 committed records pass; no EXTRACT_SCHEMA change was needed (no finding to surface).
- A4 [MED] The processing-page UX + hygiene trim are mechanically bounded — the fresh page is a LIVE-region-only screen (run-extraction navigates there; the Phase-43 staged rendering moves onto it; offline dist/news byte-identical via the strip); the hygiene trim is lossless archival (closed tasks.md blocks + over-cap state files → articles/journals with pointers). ACCEPT. revisit-status: held — the processing page shipped LIVE-region-only (in-page #liveproc takeover; offline dist/news byte-identical, --check all 5/5 zero drift); the hygiene trim was lossless (reviewer-verified: 22+2=24 baseline blocker entries accounted for; 37 tasks.md phase blocks archived VERBATIM with pointer indexes).

## Phase 45 — Corpus demo presentation polish (pre-presentation day) (2026-06-10)

all_accept: false (A1 don't-know in round 1 — "give an example for both in text" → defended with worked examples carrying REAL corpus values (fin-2024-a002: 28 covered synthetic-opioids indicators × 2 regulators) → R2 accepted in round 2)

- A1 [HIGH] Lift-beat replacement: "concept-focused" still admits REAL numbers — R2 renders honest INVENTORY counts (covered indicators in the committed signal's typology × contributing regulators, computed client-side at render, same honesty class as every lens) as the composition search space; NO performance/lift claim of any kind; the "Illustrative · pending calibration" disclaimer is REMOVED because nothing on the screen is fabricated. Small/single-regulator typologies degrade honestly (e.g. elder: "5 covered indicators published by FinCEN"). Round 1: DON'T-KNOW → worked R1 and R2 examples written with real values. Round 2: ACCEPT (R2; R1 zero-numbers ladder remains the copy-only fallback if R2 reads wrong at the T5 walkthrough). revisit-status: held — R2 confirmed by the user at the T5 walkthrough (the worked-examples defense was the right call); honest small-N degradation copy carried the edge cases and the corpus-wide fallback was never needed on real docs; R1 stayed designed-but-unused; the harness pins the counts by INDEPENDENT RECOMPUTATION from the committed data files (which killed a real bare-vs-doc-qualified overlay-key bug on first contact).
- A2 [HIGH] Showcase divergence: index.html Act-5 keeps its illustrative lift figures (byte-frozen, protected by the non-negotiable wow-beat clause + corpus-is-the-primary-demo); the corpus-only removal creates a deliberate cross-artifact divergence. ACCEPT. revisit-status: held — showcase index.html + the 3 typology dists byte-identical in `--check all` 5/5; Act-5 untouched; the divergence stands as gate-accepted (a future true-up candidate only if the showcase is ever presented again).
- A3 [MED] Human-gate fix = copy + stagecraft, no interaction redesign tonight: pre-selection behavior stays; lead copy reframes to "the agent has PROPOSED all N — deselect to dispose"; the presenter visibly deselects 1–2 live (also tames the 30-card Signal screen). ACCEPT. revisit-status: held — copy-only landed (no interaction change); the deselect stagecraft is scripted in tests/smoke-checklist.md; the interactive second gate stays a named DEFERRED item.
- A4 [MED] FINTRAC attribution gap (the user's compliance call): verbatim FINTRAC excerpts on the Capabilities/Data-sources lens screens get the SAME relocated footer-attribution mechanism extended to those views (Phase-28 lineage) — verbatim-with-attribution held, not suppressed. ACCEPT. revisit-status: held with an UPSIDE — the spec's adversarial-constraints pass surfaced that the licence attributes PER REPRODUCED WORK, so the extension landed MULTI-DOC (the footer lists every contributing FINTRAC doc, re-derived per render; C1 lists 18 docs) — licence-complete where the planned single-doc footer would not have been; synthesis drill stays silent (titles/counts only, the Phase-28 precedent); harness asserts US-only-empty.
- A5 [MED] Breadth: GLOBAL polish (all HIGHs + ranked MEDIUMs artifact-wide) + curated demo-path notes in smoke-checklist — not a single hardened route; demo docs not yet named by the user → a route recommendation (one FinCEN advisory + one FINTRAC OA) lands at the T5 walkthrough. ACCEPT. revisit-status: held — global polish + the curated route notes landed in smoke-checklist (recommended route, deselect stagecraft, zero-build-now docs to avoid, fonts cache warm); the user's walkthrough feedback (2 items + 2 refinement rounds) was absorbed within the day and FROZEN with overflow explicitly deferred.

## Phase 46 — Corpus live derivation mode: local-model agentic derivation through the frozen gate (2026-06-10)

all_accept: true (warned + restated per gate protocol — the direction itself was a user REFRAME off all five carried DEFERRED candidates, the 5th reframe at 6 consecutive gates: "live mode with local model for the corpus demo as well, ideally with a better harness integration like opencode" + a pasted opencode/local-model practitioner report; the reframe carried the user's intent so positions landed accept)

- A1 [HIGH] The reading: Phase 46 = a live CORPUS DERIVATION mode (advisory md/URL → local-model agent derives indicators + C/D tags → the existing frozen derive_signals.py check_record gate disposes → only gate-green output renders into the corpus arc), companion-served dev/authoring-time like news live; opencode's role is the local-model agent RUNTIME for that derivation loop, NOT general dev tooling (maker/checker for building signal-watch itself). If false: the whole task list is wrong. ACCEPT. revisit-status: held — the phase shipped exactly as read: serve_corpus.py companion + corpus.html /*LIVE_*/ region, the FROZEN check_record gate disposing (live E2E 17 kept / 0 dropped, independent re-gate 0 violations); opencode was evaluated strictly as the derivation-loop runtime at T1, never as dev tooling (it stays installed for dev use, outside the phase).
- A2 [HIGH] Probe-gated harness: opencode adoption is decided by the T1 measure-first probe, not upfront — it must beat the direct serve_news-pattern pipeline on the real derivation task (gate pass rate, iteration count, wall time, tool-call reliability); if local-model tool-calling is unreliable, the phase still ships live corpus mode on the proven pattern. If adopted blind: the report's own caveats (version churn, headless permission bugs, quant-sensitive tool calls) become the schedule. ACCEPT. revisit-status: held — the probe gate WORKED as designed: opencode matched quality exactly (identical 17/17 indicators, both gate-green FIRST shot, 0 violations, 0 tool-call failures) at 3.1× wall (255.1s vs 82.6s); its differentiator (iterate-on-gate-failure) NEVER ENGAGED, so it did not earn its complexity → direct pipeline + ONE violation-guided retry chosen at the user checkpoint (the loop's one real idea folded in deterministically). Note: the retry path has no live exercise yet — both probe docs passed first-shot.
- A3 [MED] Model + material: probe with the EXISTING llama-server Qwen serving first (running at 127.0.0.1:8080, news-proven); held-out derivation material = a FINTRAC /intel/ frontier doc (composes with the deferred C1 candidate — its licence/heading-form sub-questions travel with it); model swap (Qwen3-Coder-Flash / GLM-4.7-Flash class per the user's report) only on measured tool-call-class failure. If false (model shopping first): scope explodes before anything is measured. ACCEPT. revisit-status: held — the swap clause never fired: the existing llama-server model measured as Qwen3.6-35B-A3B-UD-Q4_K_XL (agentic-tuned MoE class, --jinja confirmed via chat_template_caps, n_ctx 65536/slot) and carried both harnesses; held-out FINTRAC-2024-OA001 (tax-evasion real estate OA) parsed CLEAN under the EXISTING rf_region anchors (region (220,376)) — no anchor extension; the doc stayed LOCAL-ONLY, its committed-extension sub-questions still travel with the deferred candidate.
- A4 [MED] Boundaries: offline dist/corpus stays BYTE-IDENTICAL via the news-style /*LIVE_*/ build strip (live mode optional/isolated/off-by-default, scripted fallback — the non-negotiable); live-derived records are DISPLAY/PROPOSE-only — committing any new derived record to data/ remains a separate human-reviewed act under the existing licence rules; the 2026-06-11 presentation outranks the phase — nothing presentation-touching moves before it (--check all 5/5 enforced before any commit). ACCEPT. revisit-status: held — dist/corpus BYTE-IDENTICAL through the live-region addition (--check all 5/5 zero drift; the render_corpus strip works); live output DISPLAY/PROPOSE-only (session-only, labeled UNREVIEWED, nothing persisted — no store exists); nothing presentation-touching moved before 2026-06-11; no new record committed to data/.

## Phase 47 — Demo-to-program design: the regulatorily defensible agentic AML program (2026-06-12)

all_accept: false (the direction itself was a user REFRAME at the dev-plan gate — the 6th in 7 gates: demo presented 2026-06-11 was "a huge success" → design how the demo's pipeline patterns become a REAL efficient/effective/regulatorily-defensible program, ~95% agentic / 5% high-judgment human work; A1 REJECTED-BY-REFRAME in round 1 → revised A1′ accepted in round 2 — the user's reframe BECAME the design spine)

- A1 [HIGH] Round 1 (the demo's quote-grounding gate class does NOT transfer to no-source-text workloads; the spine = a gate taxonomy deriving a different gate class per workload): REJECT-BY-REFRAME — the user: "there's some level of source text to ground to anywhere… transaction monitoring will be built on those signals, the quotes then are the signals and the underlying data, transactional and non-transactional… SAR/STR narratives need to be grounded to the guidance, policies, signals, and data." Round 2 (A1′: grounding is UNIVERSAL as the architecture principle; what varies per workload is the grounding SUBSTRATE — advisory text [substring verifier] → signals + transactional/non-transactional data [referential/lineage verifiers] → guidance/policies/signals/data for SAR-STR narratives [citation verifiers]; grounding CHAINS, defensibility = the audit walk down the chain, nothing ungrounded survives a gate; each workload's substrate + verifier NAMED in the blueprint, never assumed): ACCEPT. revisit-status: held — the spine shipped AS REFRAMED: blueprint §2 states the universal grounding principle, the 5-workload × 4-cell substrate/verifier table is all-cells-filled (RED 5/0 → GREEN 5/5, deterministic table check), grounding-chains/audit-walk written as the defensibility argument; no workload turned up without a nameable substrate (the FINDING escape clause never fired).
- A2 [HIGH] Repo role: this repo remains the VISION LAB + design source-of-truth — Phase 47 produces the blueprint + the 4th demo-idiom artifact (gate console); the real program build is a future/elsewhere effort; ship artifacts + committed data/dist FROZEN. ACCEPT. revisit-status: held — design artifacts only (blueprint + spec + console vision artifact); no program build started; existing 3 ship artifacts + dists BYTE-IDENTICAL through the phase (--check all 6/6, reviewer re-verified).
- A3 [MED] Audience/register: no live bank engagement/procurement dictates the deliverable's register or deadline — the blueprint is engineering-first; a stakeholder digest derives in a later phase. ACCEPT. revisit-status: held — no external clock surfaced during the phase; the blueprint stayed engineering-register; no digest produced.
- A4 [MED] Honesty applied to the headline: "95/5" enters the blueprint as a design DIRECTION (automate-by-default, judgment-by-design), never a stated target ratio; any ratio carries its future measurement definition (decision-volume share per gate class) — the Phase-45 fake-lift deletion pattern applied forward. ACCEPT. revisit-status: held — blueprint §10 explicitly REFUSES a ratio target on design grounds; the T3 adversarial grep (no sentence states 95% as a target) ran green; the only numbers in the blueprint are real measured ones (3.1×, 5.5×).
- A5 [MED] Charter breadth: the 5% human charter includes a MANDATED-ACCOUNTABILITY class (SAR sign-off, board/model-governance attestations, three-lines-of-defense roles — compulsory under SR 11-7 Pillar 3 regardless of judgment-need) alongside the art-like judgment work; an interesting-only charter under-counts the human layer and leaves an examiner-visible defensibility hole. ACCEPT. revisit-status: held — the dual-stream charter (§5) names both judgment AND mandated-accountability work w/ 4 invariants; Class A sits in the 4-class gate taxonomy; the SAR/STR workload row carries it.

## Phase 48 — Brownfield history + LFCM: blueprint extension, triage-elicitation loop, synthetic-history probe (2026-06-12)

all_accept: false (A1 accepted WITH a condition; the direction itself is the user's own planning input — the blueprint's greenfield gap [TM/investigation/filing history unaddressed] + the LFCM idea [10^3–10^4 grounded signals assisting FC-risk judgment] + the mini-triage elicitation loop idea raised at the Step-9 questions)

- A1 [HIGH — the T0 weakest assumption] The triage-elicitation loop is designed so its value does NOT depend on analyst-judgment convergence: gap discovery (need-more-info wired to the C/D coverage model) is the value floor, calibration stats the upside, agreement itself a first-class measured dimension (divergence routes to adjudication — the console pattern). If false, the loop's design center moves. ACCEPT WITH CONDITION — the scenarios are sourced from REAL institutional history (alerts/cases/filings replayed as mini-triage scenarios): historical decisioning is ground truth about DECISIONS, never about correctness; and the loop's discovery outputs explicitly include PROCESS INCONSISTENCIES and POLICY GAPS alongside signal/data gaps. revisit-status: held — the loop designed gap-discovery-first in §14 (need-more-info wired to the C/D coverage model = the value floor); the CONDITION honored: history-sourced scenarios + process-inconsistency/policy-gap discovery written into the section; convergence stays an at-adoption measured open question (the revisit hook).
- A2 [HIGH] LFCM's regulatorily survivable architecture = a grounded signal LIBRARY + a small composition layer — the library IS the model inventory (per-signal lifecycle, tiered validation); "LFCM" stays the program-level name, never a single Tier-1 mega-model as the validation unit. If false, the LFCM section is wrong. ACCEPT. revisit-status: held — §13 shipped library-not-monolith: the grounded signal library IS the model inventory (per-signal lifecycle, tiered validation); LFCM kept as the program-level name; 5 named failure modes incl. the monolith trap.
- A3 [MED] History enters the blueprint as THREE distinct roles — derivation substrate / §6 probe baseline (legacy TM = the A/B comparator) / outcome-feedback embryo — under "history is evidence, never ground truth" (it encodes the legacy program's blind spots), with §8's deferred rows RE-DISPOSITIONED (vision-lab-deferred vs adopter-available-with-caveats), not duplicated. ACCEPT. revisit-status: held — §12 carries the three named roles each with substrate+verifier + the doctrine line; §8 rows re-dispositioned IN PLACE (vision-lab-deferred vs adopter-available-with-caveats), not duplicated; adversarial greps green.
- A4 [MED — silent infrastructure class] The probe executes as a fully SYNTHETIC, advisory-SHAPED legacy rulebook derived through the EXISTING FROZEN gate (zero gate changes; the shape caveat stated honestly in the writeup — real rulebooks may need the regression-gated anchor-extension path) + a stdlib stats script over synthetic alert/disposition history (incl. the disposition-inconsistency rate, every number with its measurement definition); outputs committed OUTSIDE every build.py-read path; all 4 dists byte-frozen (--check all in exit criteria); the two stale blueprint counts (§3 "five workloads", §12 "three ship artifacts") fixed in scope. ACCEPT. revisit-status: held — probe gate-green 12/12 FIRST shot through the byte-unchanged gate (git diff --quiet derive_signals.py held; zero check_record violations); outputs outside every build.py-read path; all 4 dists 6/6 zero drift; both stale counts fixed in scope + the reviewer's third stale count ("five implementations") caught and fixed post-review.

## Phase 49 — Triage-loop embryo made demo-able: §14 as the 5th ship artifact (2026-06-12)

all_accept: false (A2 took a don't-know round 1 → defended with the Phase-48 ledger verbatim + the console-cases curation precedent → accepted round 2; the direction itself = the user picked the offered §14-embryo candidate at the Step-9 direction question — the first non-reframe pick in 3 gates)

- A1 [HIGH] The triage-loop embryo is DEMO-FIRST — an artifact to present (bank-audience class), not an internal instrument; the loop's defining beat (analyst judgment vs the institution's historical decision, decisions-not-correctness) needs replayed history, which only the synthetic probe data carries. If false, the right build is instrument-first (extend the 213-case console over real divergences) and the synthetic scenario stream is the wrong deliverable. Instrument-first explicitly noted as the natural follow-on (sequencing, not dismissal). ACCEPT. revisit-status: held — the demo-first artifact was built and is presentable (full Queue→Evidence→Disposition→Reveal→Ledger arc, dist/triage single-file offline); instrument-first stays the noted natural follow-on.
- A2 [HIGH — silent infrastructure class] A NEW committed dataset data/triage/scenarios.json (deterministically curated from data/probe-history + US-federal-only corpus indicators) may be a build-read input for the NEW triage target — the console-cases layering: curate script reads probe-history at AUTHORING time only; build.py reads only the committed, build-boundary-validated scenarios.json; probe-history itself stays outside every build path; nothing enters __CORPUS__ (the Phase-48 A4 object untouched). DON'T-KNOW round 1 → defended (Phase-48 A4 verbatim scopes the probe's OWN files + __CORPUS__; data/console/cases.json precedent "validation never reads git"; §14 itself prescribes history replayed as scenarios) → ACCEPT round 2. revisit-status: held — the console-cases layering implemented exactly: curate script reads probe-history at AUTHORING time only; build.py reads only the committed build-boundary-validated scenarios.json (probe-history in build.py comments ONLY — claim-shaped code-grep verified); nothing entered __CORPUS__.
- A3 [HIGH — the T0 weakest assumption] Authored synthetic evidence panels can be made BELIEVABLE within the ~16-scenario + ~4-control ceiling: the 44 alert stubs carry no judgable fact pattern, so panel authoring IS the bulk of the phase; mitigated structurally (D7: template-derived skeletons from rule logic + stub fields; one panel per fact pattern shared BY REFERENCE across divergent-disposition pairs — the process-inconsistency beat build-validated, never an authored coincidence). If false, scenarios read thin, the disposition gate feels arbitrary, the beat dies. ACCEPT. revisit-status: held (structural) — the D7 mitigation built as designed (template-derived skeletons, ONE panel per fact pattern shared BY REFERENCE across the divergent pair, the 16+4 ceiling held without the split valve); believability itself is adjudicated by the user at the delivery gate (smoke-checklist "read 4 panels, one per stratum" row + a verbatim scenario in the delivery report).
- A4 [MED — either/or] Ship-class discipline is the right cost: a FIFTH ship artifact (triage.html → dist/triage, build target wired into all/--check, dedicated node harness, smoke-checklist row) on the console precedent — an interactive artifact presented without a harness is the riskier alternative; the non-ship docs/ shape (booth/blueprint-report precedent) was surfaced and declined. ACCEPT — 5th ship artifact. revisit-status: held — the 5th ship artifact landed with full discipline: `triage` build target wired into all/--check (7 targets, zero drift), 4-class tamper validation fail-loud, dedicated 93-assertion harness, smoke-checklist row.

## Phase 50 — AML program build kickoff: data-substrate pillar spun out (2026-06-12)

> Direction = a major user REFRAME at the dev-plan gate: from incremental signal-watch phases to BUILDING THE REAL AML PROGRAM (blueprint §3 design-stage workloads → real systems), **spun out one repo per pillar** (real-system-class). Pillar 1 = the data substrate → new repo `/Users/jwang/aml-substrate` (design in its DESIGN.md). signal-watch stays the program-architecture home. Pre-gate framing forks: first-build = data substrate (the keystone with no embryo); housing = real-system-class spin-out per pillar; engine = hybrid (own data model + Canadian/EMT/FINTRAC; reuse AMLSim plumbing; learn AMLgentex calibration); phase-1 shape = foundation-first. Interview (Jake's institutional knowledge) + 3 parallel research agents (generators · real schemas · distributions/topology/benchmarks) grounded the design.

all_accept: false (A3 rejected → revised to small-first; A2 = user override of the offered review-first mitigation)

- A1 [HIGH — T0 weakest] Foundation-first delivers an expert-credible phase-1 artifact even with no laundering in it yet, AND the faithful substrate won't need rework when emergence lands in phase 2. ACCEPT — mitigation: data model built EMERGENCE-READY (criminal-agent + transitive-label hooks in the schema from day 1). revisit-status: held — emergence-ready hooks (latent_role/illicit_income/laundering_label/illicit_flow) built INERT in P1; 54 tests confirm phase-2 emergence needs no schema migration. A1 mitigation proven.
- A2 [HIGH] Researched Canadian schemas/distributions are faithful enough to build phase 1 on directly (several flagged "representative, not canonical" — EMT field names, account-role legal rights, the σ values as design choices). Offered mitigation = a Jake-review checkpoint on the spec before generation; USER OVERRODE → build on research directly, corrections surface during build. ACCEPT (build directly). revisit-status: held (P1) — built directly on the researched schemas/distributions; channel medians landed on the Canadian anchors (EMT $400/card $46/cheque $400/retail-wire $8k), no fidelity blocker surfaced. Several fields remain "representative not canonical" (EMT names, account-role legal rights) — to be confirmed as the data is used.
- A3 [HIGH] 1M-client scale tractable scripted in phase 1. REJECT → parameterized SMALL-FIRST: build + validate at 10k–100k, scale the same engine to 1M later; scale is a parameter, never assumed. revisit-status: held — small-first VALIDATED: perf probe linear ~58us/client / ~5.5us/txn, stable 1k→200k, ~161s projection to 1M clients (1 month). Scale is a CLI parameter; byte-identical deterministic regen.
- A4 [MED] AMLSim flow/topology plumbing bends to the fresh Canadian/EMT model (hybrid saves real work vs collapsing to fresh). ACCEPT, eyes open — re-evaluate after a reuse spike. revisit-status: DEFERRED to phase 2 — the AMLSim flow/topology reuse is exercised by the EMERGENCE engine (phase 2), not the foundation. P1's data model + population + background engine are genuinely fresh (AMLSim has no foundation layer to reuse), so the hybrid-vs-fresh question is untouched and still open for the laundering flow layer.

## Phase 51 — Corpus redundancy measurement (the §13 fm-1 frontier, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's first deliverable (post-triple-null: composition's surviving value includes redundancy-management, real on the corpus). The OBVIOUS approach (cluster by semantics, report an overlap fraction) violates the LOAD-BEARING no-similarity/no-dedup non-negotiable. User gated the HYBRID (over deterministic-only [in-constraint but marginal vs the existing synthesis lens] and pivot-to-C/D-correctness): a NON-SHIP artifact = deterministic label co-occurrence UPPER BOUND + a sampled consensus-class semantic-equivalence estimate.

all_accept: false (the offered redundancy framing was rejected on honesty-constraint grounds → re-scoped to the hybrid non-ship measure-not-claim design before the gate)

- A0 [HIGH — the load-bearing honesty assumption] The hybrid stays IN-CONSTRAINT: the ship corpus (corpus.html/dist/corpus/all derived) stays byte-frozen + read-only; the deterministic part is honest union/co-occurrence arithmetic over EXISTING committed labels (the class CLAUDE.md:204 allows); the semantic-equivalence number is SAMPLED + blind + consensus-class + illustrative (the Phase-34 inter-rater doctrine) in a NON-ship artifact — NOT a re-open of "the ship artifact computes no similarity," NOT a validated dedup/lift. ACCEPT. If false (any similarity/dedup/lift figure lands on a ship artifact, or the semantic number is presented as ground truth) → STOP, it's the overclaim the whole thesis forbids. revisit-status: HELD — stayed in-constraint end to end: `--check all` 7/7 zero drift (ship corpus byte-frozen), the T2 number is single-rater/consensus-class/illustrative with consensus deferred, framed candidate/ceiling/estimate, the only dedup mentions are disclaimers; data/corpus-redundancy/ is non-corpus (build never reads it).
- A1 [HIGH] Typology resolution (per-indicator override in indicator-typology-map.json else doc-level inherit from typology-map.json) is deterministic and gives meaningful per-indicator coverage for the (typology×capability) cell. ACCEPT. If false (the overlay is too sparse — fintrac-guidance is 1,283 of 2,251 and the per-indicator map is sparse, so the axis collapses to doc-level inherit) → fall back to (capability × regulator) co-occurrence and report the coarser axis honestly (named in the abort rule). revisit-status: HELD — 0 unmapped (350 per-indicator override + 1901 doc-level inherit); the (typology × capability) axis was viable, the fallback was not needed.
- A2 [HIGH] A (typology × capability) cell is a meaningful candidate-redundancy proxy — co-occurrence in a cell across regulators ≈ an honest UPPER BOUND on redundancy. ACCEPT. If false (the cell is too coarse — many distinct indicators share a cell) → the upper bound is uninformatively loose and the sampled semantic estimate (A3) carries the real signal; report the bound as "loose ceiling," lean on the sample. revisit-status: BIT (as designed) — the cell IS a loose ceiling (0.325 co-occurrence vs ~1.4% strict genuine equivalence); the gap is exactly the finding (shared label = distinct facets), and T2 absorbed it. Reported as a ceiling refined by the sample, not as redundancy-as-fact.
- A3 [MED] A blind semantic-equivalence sample over cross-regulator co-occurring pairs yields a stable, defensible consensus equivalence rate. ACCEPT. If false (the sample is noisy / raters diverge widely) → report the deterministic upper bound ONLY and surface the divergence as the finding (consensus failure is itself honest). revisit-status: HELD (single-rater) — the sample produced a clear, integrity-verified distribution (1 equiv / 15 partial / 8 distinct), decisive enough to ground the finding; consensus (≥2 raters) + a larger sample stay deferred-with-owner as planned.
- A4 [LOW] The right home is a NON-ship analysis artifact in signal-watch (the blueprint-report.html / probe-history.md precedent — non-ship analysis already lives here), not a new sibling measurement repo. ACCEPT. If false (the measurement workstream wants its own repo/owner) → lift the script + report into a sibling, reading signal-watch's frozen corpus read-only. revisit-status: HELD — the blueprint-report.html / probe-history.md non-ship-analysis precedent fit cleanly; build.py never reads data/corpus-redundancy/; --check all unaffected.

## Phase 52 — C/D-tag reliability measurement (the unguarded dimension, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's SECOND deliverable (the sibling of Phase 51): the C/D dimension is the one the grounding gate explicitly never checks (CLAUDE.md: "a grounding gate ≠ a completeness gate ≠ a correctness gate"; working-knowledge: "that dimension is unguarded"). User gated the STRATIFIED frame (over 213-divergences-only and random-only): a NON-SHIP analysis = a blind same-model re-rate over a RANDOM corpus stratum + the 213 Phase-34 divergences as a labeled HARD stratum, reported as consensus / self-consistency, never validated correctness.

all_accept: true (the load-bearing set A0/A1/A2 accepted as stated; warn-tracked. A0 reshapes the random headline from "reliability/correctness" to "reproducibility/self-consistency"; A1 forces the blind-by-construction dump so the orchestrator never hand-rates; A2 fixes the illustrative-first n=24/seed-0 posture with consensus + larger-N deferred-with-owner; A3 is the standing non-ship byte-frozen abort rule.)

- A0 [HIGH — the load-bearing honesty assumption; T0 weakest] A same-model blind re-rate of C/D measures REPRODUCIBILITY / self-consistency, NOT validated correctness — the rater shares the original extractor's systematic biases, so agreement is necessary-not-sufficient for correctness. ACCEPT. If false (we headline the random number as "the C/D dimension is X% correct/reliable") → overclaim, the measure-not-claim violation Phase 51's A0 forbids. Mitigation: label the random number self-consistency; the divergence stratum (a real two-pass disagreement) is the closer-to-independent number; genuine independent-rater consensus (different model family / human) + larger N deferred-with-owner. revisit-status: HELD (with a post-delivery tightening) — the report labels the random number REPRODUCIBILITY / self-consistency throughout (never "correct"), the finding leans on the divergence stratum, independence is deferred-with-owner; honesty grep clean. NOTE: a post-delivery adversarial review (of the aml-wiki capture) found the report's FIRST finding sentence ("the gap = adjacent-code scatter, NOT error") OVERCLAIMED — the random + divergence strata are different samples/tasks/metrics, NOT subtractable, so the defensible-vs-error share is unmeasured; the level is also inflated by the forced-choice format and at n=24 the two rates are statistically indistinguishable. Tightened in docs/cd-correctness-report.md to "two separate non-subtractable measurements; defensible scatter plausibly one driver, share unmeasured" + a single-rater / no-chance-correction / overlapping-Wilson-CI caveat. A0 still HELD (no number was ever presented as correctness), but the gap-as-decomposition was the exact spot it nearly slipped — the catch validates the measure-not-claim discipline.
- A1 [HIGH] Blindness is preserved BY CONSTRUCTION — the rater (a subagent) never sees the committed code (random stratum) or which option is the Phase-34 correction (divergence stratum, order fixed by seed); the script dumps blind samples, the orchestrator does NOT hand-rate. ACCEPT. If false (blindness leaks — the rater sees the answer) → the agreement number is meaningless. Mitigation: the dump format excludes committed codes; divergence options presented neutrally with seed-fixed order; judgments committed as a replayed fixture (the Phase-51 / Phase-34 pattern). revisit-status: HELD — both blind subagents ran tool_uses: 0 (no repo file read at all); the dumps excluded committed codes (random) / which-is-correction (seed-fixed sha256 order, divergence); --verify-fixtures VERIFIED both against the seeded samples.
- A2 [MED] n=24 per stratum, seed 0 is an adequate ILLUSTRATIVE first instance (chosen-not-derived, the Phase-51 T2 posture) — the deliverable is the reproducible machinery + an honest first instance, not a final number. ACCEPT. If false (too small / too noisy to ground a finding) → report the distribution as illustrative + surface the noise; larger N + consensus already deferred-with-owner. revisit-status: HELD (noise noted) — n=24 produced a decisive-enough first instance (62.5%/axis · 70.8% uphold); the random both-axes 0.417 is the noisiest cell at this N and is labeled illustrative; larger N + consensus stay deferred-with-owner as planned.
- A3 [LOW] NON-ship guard + abort rule: the ship corpus (corpus.html / dist/corpus / all derived / the overlays) stays byte-frozen + read-only; build.py NEVER imports cd_correctness.py; data/cd-correctness/ is non-corpus; `--check all` 7/7 zero drift. ACCEPT. If false (any ship-artifact drift) → STOP and surface (the standing abort rule), never re-baseline. revisit-status: HELD — --check all 7/7 ZERO drift; build.py has no cd_correctness / cd-correctness reference; data/cd-correctness/ is non-corpus (build.py never reads it).

## Phase 53 — C/D disagreement decomposition (error vs defensible-neighbor, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's THIRD deliverable (finishes Phase 52, not a new dimension): the Phase-52 report's own closing paragraph names the gap — the ~1/3 of random-stratum mismatches "whose error-vs-defensible composition was NOT adjudicated," and "a clean decomposition would require running the forced-pairwise adjudication on the SAME random-stratum mismatches." Phase 53 IS that decomposition: enlarge the random sample, adjudicate its mismatches forced-pairwise (blind code vs committed, neutral order), + add chance-corrected kappa (closes the report's named "not chance-corrected" gap). NON-ship, read-only, corpus byte-frozen (the Phase-51/52 envelope). Chosen at the gate over: an independent-rater panel + Krippendorff's alpha (needs a genuinely non-Claude rater — the scarce ingredient, subagents are all Claude-family; deferred-with-owner) · the heading->capability determinism probe (a new dimension, Phase-37 thread; deferred) · rooting into the aml-casework audit-walk (higher-value but un-drivable from a signal-watch-rooted session).

all_accept: true (direction confirmed + the load-bearing weakest assumption A0 ACCEPTED at the gate; warn-tracked. A0 reshapes the decomposition headline from a clean split to a BOUND [error = lower bound / scatter = upper bound] because the adjudicator is same-family; A1 fixes the decomposition as the report's named missing measurement, not a new claim; A2 sets the larger-N illustrative-first posture with even-larger-N + consensus deferred; A3 is the standing non-ship byte-frozen abort rule.)

- A0 [HIGH — the load-bearing honesty assumption; T0 weakest] A same-Claude-family fresh blind rater adjudicating the random-stratum mismatches (its-class blind code vs the committed code, neutral seed-fixed order) is independent-ENOUGH to license the decomposition ONLY as a BOUND — it shares the biases that generated the neighbouring code, so it tends to rate its own scatter "both-defensible" -> the measured error-share is a LOWER bound and the defensible-scatter-share an UPPER bound. ACCEPT. If false (the split is presented as a clean/exact decomposition, or as genuine independence) -> overclaim, the measure-not-claim violation Phase 51 A0 / Phase 52 A0 forbid. Mitigation: report error as lower-bound / scatter as upper-bound throughout; genuine independence (a different model family / human) stays deferred-with-owner (the panel+alpha framing was the gate alternative, not chosen). revisit-status: BIT (mechanism differed — the conclusion held, the prediction did not). The BOUND held (error = LOWER bound 23/62=37%; committed-questionable = UPPER bound 38/62=61%), but the PREDICTED mechanism — same-family adjudicator inflates "both-defensible" by rating its own scatter generously — did NOT fire (both-defensible 1/62=1.6%). The actual confound is CONTEXT ASYMMETRY: both blind raters saw flag+red_flag only while the committed code had full document context, so the two flag-only raters side with each other against committed. A0's measure-not-claim spirit HELD (no number presented as correctness; the 61% explicitly framed as an unmeasured error+confound mix, not a committed-error rate) — but the bound lives in a different place than written. Follow-up reframed: an independent rater must be CONTEXT-MATCHED.
- A1 [HIGH] Decomposition validity: adjudicating the random-stratum MISMATCHES as forced-pairwise (blind vs committed, neutral order) is a meaningful decomposition of the ~1/3 disagreement into genuine-error / defensible-neighbor / committed-questionable -- the EXACT measurement the Phase-52 report names as missing ("the forced-pairwise adjudication on the same random-stratum mismatches"). ACCEPT. If false (the forced-pairwise cannot isolate error from scatter -- the adjudicator can't tell the codes apart on the flag+red_flag alone) -> the neither/escalate rate spikes and THAT is the honest finding (the dimension is un-adjudicable, not soft-but-adjudicable). Mitigation: neither/escalate is a first-class outcome (it was 0 in the Phase-52 divergence stratum -- a spike here is itself a result). revisit-status: HELD — the decomposition was a meaningful, valid measurement: it REFUTED the Phase-52 "plausibly scatter" guess (both-defensible 1/62=1.6%) and was adjudicable (neither/escalate 0/62 — no spike, the dimension is not un-adjudicable). The forced-pairwise DID isolate a sharp preference; the surprise was the DIRECTION confound (A0), not an inability to adjudicate.
- A2 [MED] Larger-N feasibility: a modestly enlarged random sample (n~72-96, seed-fixed, corpus-proportional from the 2,251) yields enough mismatches (~25-40 at the Phase-52 ~58% any-axis disagreement) to decompose with reportable (if wide) CIs, and Cohen's kappa (blind vs committed over the 28-C / 20-D vocab) is computable. ACCEPT. If false (too few mismatches / sparse-marginal kappa undefined or uselessly noisy) -> report the distribution as illustrative + surface the noise; even-larger N deferred-with-owner (the deliverable stays "reproducible machinery + honest first instance," the Phase-51/52 posture). revisit-status: HELD — n=96 yielded 62 mismatches (31 C + 31 D, ample); Cohen's kappa computed cleanly (C 0.658, D 0.646, ≈ raw 0.677 → genuine self-consistency, not chance); Wilson CIs reported. Minor deviation (non-material): the first blind rater dropped 1 of 96 items (duplicate-flag fintrac-guid-casinos/IND-17); a fresh single-item blind rater filled it (SendMessage unavailable to continue the original) — rebuilt fixture gids == the seeded sample, integrity verified.
- A3 [LOW] NON-ship guard + abort rule: the ship corpus (corpus.html / dist/corpus / all derived / the overlays) stays byte-frozen + read-only; build.py NEVER imports cd_correctness.py; data/cd-correctness/ is non-corpus; `--check all` 7/7 zero drift; the report stays a NON-ship doc (the blueprint-report.html / probe-history.md / corpus-redundancy-report.md class). ACCEPT. If false (any ship-artifact drift) -> STOP and surface (the standing abort rule), never re-baseline. revisit-status: HELD — `--check all` 7/7 zero drift (ship corpus + all 5 dists byte-identical); build.py has no cd_correctness/cd-correctness reference; data/cd-correctness/ is non-corpus; the report stays a NON-ship doc. No abort triggered.

## Phase 54 — C/D tag control: the measured-not-gated control made real (SR-11-7 ongoing monitoring) (2026-06-16)

> Direction = the user's REFRAME at the dev-plan direction question (chose "prototype the tag control" OVER a 4th point-measurement [the context-matched-rater close-out] OR the heading→capability determinism probe) + a rigor ESCALATION at the gate (chose "execute the independent effectiveness challenge ONCE this phase" over merely scheduling it — a control whose key activity has never run is also prose). Phase 54 makes the blueprint's §4–§5 "measured-not-gated" control class CONCRETE + EXECUTABLE for the C/D tag (the canonical unguarded/judgmental dimension) — closing the named-OPEN Phase-47 T2/T3 blocker ("the production-grade SR-11-7-class control DESIGN at scale — cadence, sampling, ownership, where it sits in the 5% charter — is not yet written"). The three measurement phases (51/52/53) were the instrument + standing baseline; the control is what they were FOR. It also EXECUTES the Phase-52/53 deferred follow-up once: a context-matched + genuinely-independent (local Qwen, a different model family) rater → the clean cross-family reliability number + Krippendorff's α that 52/53 left deferred. NON-ship, read-only, ship corpus byte-frozen (the Phase-51/52/53 envelope). SR-11-7 Pillar-2 (ongoing monitoring) + OSFI E-23 vocab from the aml-wiki, not guessed.

all_accept: true (warned + restated per gate protocol — the direction is the user's chosen reframe + a deliberate rigor escalation; positions landed accept. A0 reshapes the "execute once" deliverable's RISK into a probe-gate + named fallback [availability confirmed up; competence is T1's probe]; A1 fixes "context-matched" as the apples-to-apples definition that makes the new number a reliability number not a context-gap re-measurement; A2 isolates the model dependency at the build boundary so the replay core stays pure + deterministic; A3 the standing non-ship / no-blueprint-edit constraint.)

- A0 [HIGH — the T0 weakest assumption] The local different-family model (Qwen3.6-35B on 127.0.0.1:8080, confirmed UP this session) is available AND competent enough at context-matched closed-vocab C/D tagging to serve as the genuine independent rater the "execute once" deliverable needs. ACCEPT (probe-gated + named fallback). If false (the model is unavailable mid-phase, or cannot reliably emit an in-vocab C/D from the context-matched prompt — Qwen≈Opus on EXTRACTION per working-knowledge, but closed-vocab classification is a DIFFERENT task, unverified) → the clean cross-family number blocks. Mitigation: T1 `--probe` (the Phase-46 probe-gate pattern) verifies competence on a few items BEFORE the full run; on failure → fall back to an Opus-context-matched rater (removes the Phase-53 CONTEXT-asymmetry confound but not the same-family one — still a strictly cleaner number than Phase 53) and the independence leg reverts to DESIGNED-IN / scheduled-with-owner, surfaced at the checkpoint (the user adjudicates). The phase cannot hard-block. revisit-status: HELD (positive surprise) — the local Qwen3.6-35B was available AND competent: the T1 8-item probe was user-adjudicated COMPETENT (8/8 in-vocab, sane/adjudicable picks, hit the named C8/C14 neighbour, flagged a plausible committed mis-tag) → ran the full n=96 AS-IS; the named Opus-context-matched fallback was NOT needed, and the independence leg ran for real (not just scheduled). Closed-vocab classification proved a solvable task for the cross-family model.
- A1 [HIGH] "Context-matched" = giving the rater the SAME inputs the committed assignment had — the source-doc region for the indicator (gid = doc_id/ind_id → data/<source>/<doc_id>.md) + the 28+20 capability/data-source interview posture + the closed vocab — NOT the Phase-53 flag+red_flag-only frame. So Qwen-context-matched-vs-committed is an apples-to-apples RELIABILITY comparison, not a re-measurement of the Phase-53 context gap. ACCEPT. If false (the doc region + posture do not reconstruct the context the inverted-extraction loop actually used → a residual context gap survives) → the "clean number" still mixes reliability with a smaller context gap. Mitigation: document precisely what "matched" feeds; the cross-rater (Opus-blind-flag-only vs Qwen-context-matched) + Krippendorff-α still BOUND the family/context effects; the number is reported as consensus + context-matched, never as validated correctness. revisit-status: HELD — context-matched = source-doc region + the 28+20 posture + the closed vocab, so Qwen-cm-vs-committed is an apples-to-apples reliability number (C 0.604 / D 0.646 ≈ the same-family self-consistency 0.677; Wilson CIs overlap) — NOT a re-measurement of the Phase-53 context gap. Reported as consensus + context-matched + κ + Krippendorff-α + Wilson, never as validated correctness. CAVEAT (documented, not a bite): "context-matching resolves the Phase-53 confound" is an INFERENCE from the aggregate ~0.6 ≈ self-consistency, NOT a re-run of the forced-pairwise decomposition with the Qwen rater — it bounds the confound's DIRECTION, not its exact share (a Qwen forced-pairwise decomposition would convert the bound to a measured share — deferred-with-owner).
- A2 [MED] The deterministic replay core (cd_correctness.py) stays PURE stdlib / no-network — a SEPARATE dev-time companion (scripts/cd_rate_independent.py) is the ONLY code that calls the local model + produces the independent fixture; cd_correctness.py reads fixtures only; build.py imports NEITHER. ACCEPT. If false (model-calling/network code leaks into the replay core or the build boundary) → the measurement stops being deterministically replayable AND the build→authoring-layer boundary is violated. Mitigation: the companion is the news/corpus live-mode pattern (companion calls 127.0.0.1, fixture captured-then-replayed); cd_correctness.py + build.py stay model-free; --check all proves the boundary. revisit-status: HELD — cd_rate_independent.py is the ONLY model/network code (urllib → 127.0.0.1, produces the fixture); cd_correctness.py stayed pure stdlib / deterministic replay-from-fixtures; build.py imports NEITHER (T5 grep clean); --check all 7/7 zero drift proves the build boundary held.
- A3 [MED] The control ships as a STANDALONE non-ship doc (docs/cd-tag-control.md) + the executable harness; NO edit to docs/program-blueprint.md (it CITES §4–§5, the cd-correctness-report-cites-§13 pattern) — dodging the known blueprint-report.html hand-sync drift (the Phase-49 precedent); ship corpus byte-frozen, --check all 7/7; the independent fixture stores JUDGMENTS over committed indicators (no new external content → no fixture-promotion-allowlist concern); 127.0.0.1 model only, nothing leaves the machine. ACCEPT (the standing non-ship abort rule + the Phase-49 no-blueprint-edit precedent). If false (blueprint edited, or any ship-artifact drift) → blueprint-report.html hand-sync drift / STOP-and-surface. revisit-status: HELD — docs/cd-tag-control.md ships as a standalone non-ship doc citing §4–§5; `git diff --quiet docs/program-blueprint.md` clean (NO blueprint edit); --check all 7/7 zero drift (ship corpus + all 5 dists byte-identical); the independent fixture stores JUDGMENTS over committed indicators (no new external content → no allowlist concern); 127.0.0.1 model only. No abort triggered; the change set contains ZERO ship artifacts.

## Phase 55 — Cross-pillar end-to-end bridge: aggregation + verification (the 3-pillar full demo connected) (2026-06-17)

> Direction = the user's ask at the dev-plan gate: plan the bridges that connect the 3-pillar full demo, where the actual wiring may execute in sibling sessions but the AGGREGATION + FINAL DELIVERY sit in signal-watch. Shape question answered "verifier harness + walkthrough" (Option A: PROVE the join with a re-runnable cross-repo check, not narrate it) + A3-reject re-scoped to "launcher + live pillar-status" (a new dist/index.html front door is the one ship-surface addition). signal-watch delivers the SPINE this session (ratified serialization contract + the e2e_chain_check harness fixture-proven + the two sibling briefs + the launcher + the walkthrough); the --real verification (and the final delivery gate) is GATED on the two sibling sessions executing the persist (#1) + consume-real-bundle (#2) bridges. Grounded against sibling HEADs aml-substrate@bafc67d + aml-casework@0316580 (code-verified this session).

all_accept: false (A2 don't-know round 1 → DEFENDED by code [three-way C4↔IND-11↔C4 alignment verified] → accept; A3 reject round 1 [pure-non-ship wrong, a ship change is intended] → RE-SCOPED to the launcher + pillar-status → accept on the revised set. A0/A1 accept.)

- A0 [HIGH — the T0 weakest assumption] The §5a evidence serialization schema can be RATIFIED NOW — from integration-contract §2 + casework's hand-mirrored contract.py at current HEADs — accurately enough that the fixture-driven e2e_chain_check written here matches what aml-substrate actually emits once the persist bridge lands. ACCEPT. If false (the real §5a emission diverges from the ratified schema) → e2e_chain_check --real fails at first contact and sibling brief #1 needs a revision round. Mitigation: the harness IS the divergence detector — brief #1's acceptance criterion is "e2e_chain_check --real passes its substrate-side checks," so any hand-mirror drift surfaces at the join at the cheapest point; ratification reconciles contract §2 ⟷ casework contract.py at current HEADs before brief #1 ships; the synthetic fixture is built FROM the ratified schema. revisit-status: HELD (spine) — ratified §2 from casework's actual validate_bundle + the briefs; the harness IS the divergence detector. The review gate confirmed the id-mint rule is BYTE-IDENTICAL across the contract, the harness, and both sibling briefs (ran all three as code → identical hashes). The REAL-emission match is the gated BEAT-2 test (the casework brief's A0 names it as load-bearing) — not yet bitten because beat 2 hasn't run. **UPDATE 2026-06-17 (beat 2 ran — A0 did NOT bite):** the real emission (aml-substrate@df23bba `CASE-P-0010361`) matched byte-for-byte (5/5 alert_ids + dossier_id; all 5 flags byte-grounded in the SOURCE regulator md), adversarially confirmed by an independent join re-derivation (no harness); the full `e2e_chain_check --real` chain is CONNECTED. The hand-mirrored schema held on first real contact — the load-bearing risk resolved clean.
- A1 [HIGH] The end-to-end join can be verified by READING committed/vendored sibling outputs (the serialized evidence json + casework's signed-SAR json) with NO import of sibling code — the file-contract / vendored-pin pattern casework already uses for the frozen corpus. ACCEPT. If false (verification needs to import aml_substrate / aml_casework or run a sibling engine) → signal-watch couples to sibling code, violating the one-repo-per-pillar doctrine. Mitigation: the harness reads serialized files only; the casework corpus vendored-pin precedent (reads signal-watch@a75a136 derived/*.json as data) proves the read-only file-contract seam works; --selftest pins it offline. revisit-status: HELD — e2e_chain_check.py imports NO sibling code (review-gate grep-confirmed; the only import is signal-watch's own derive_signals.normalize); reads committed json only; --selftest pins it offline; build.py never imports the harness.
- A2 [MED] The demonstrable vertical slice is a C4-STRUCTURING case — the substrate can emit one, casework registers C4, and the corpus pin (IND-11) grounds it — so the first real wired case lands in the EXISTING grounding path with no corpus-pin widening. ACCEPT (don't-know round 1 → DEFENDED by code). If false → sibling brief #2 needs a corpus-pin-widening / capability-registration prerequisite first. Evidence (verified this session): aml-substrate monitor/detectors/structuring.py StructuringDetector capability="C4" is docstring-grounded in fin-2026-alert001:IND-11; that same pinned record (casework fixtures/corpus/fincen-alerts/derived/fin-2026-alert001.json) has id=IND-11 → capability=C4; casework grounding_replay.py registers "C4":_assert_c4_structuring (+ fail-closed default). A clean three-way alignment on the existing pin. revisit-status: HELD — code-verified three-way C4↔fin-2026-alert001:IND-11↔C4 alignment (don't-know→DEFENDED-by-code at the gate); the review gate confirmed IND-11→C4 is unique in the corpus. No corpus-pin widening, no new capability registration needed for the slice.
- A3 [MED] The ONE ship-surface addition is the launcher — a new self-contained offline dist/index.html (8th build target) front door linking the 5 existing artifacts + a cross-pillar panel rendering data/pillar-status.json (each bridge's state, regenerated from the harness output, inlined at build like every other artifact's config); the 5 EXISTING artifacts + their dists stay byte-frozen; --check all extends to 8/8; the console/triage are NOT re-opened (Option C declined). ACCEPT (reject round 1 of pure-non-ship → re-scoped). If false (a 6th-artifact creep, or any of the 5 existing dists drift, or the console is touched) → STOP-and-surface per the standing abort rule. Mitigation: the launcher follows the ship doctrine (single offline file, always-on "Illustrative data & outputs" badge, no fetch/LLM, config inlined at build); build.py gains a launcher target; the freeze guard re-asserts the 5 existing dists byte-identical at 8/8. revisit-status: HELD — --check all 8/8 zero drift, the 5 existing dists byte-identical, only dist/index.html added; console/triage untouched; the obsolete M1 stale-dist/index.html deletion in build.py was removed (DISCOVERY — it would have deleted the launcher). LATE BITE (adjacent, caught+fixed by the review gate): --real on the synthetic data/e2e/ fixtures would have flipped the committed pillar-status to a FALSE green launcher (an honesty bite next to "the launcher reads as a claim") — fixed e95df82 (--real now refuses data/e2e/; signoff required; illustrative enforced on the signed SAR). SECOND late bite (beat 2, caught+fixed): a verification-workflow agent overstepped "do not modify" + committed e5a3138 — an incomplete all-green flip (stale launcher + stale grounding HEADs → --check all 1/8 drift); caught by the adversarial honesty/regression lens, undone (reset --soft, unpushed), redone clean in 10867ff (launcher rebuilt all-green, HEADs corrected df23bba/85602c1). Process learning: workflow agents keep git via Bash even under read-only prompts.

## Phase 56 — Chain workbench: the HTML analyst UI (companion-served live consume + verify over a pre-baked case library) (2026-06-17)

> Direction = the user wants an HTML frontend as the UI for the substrate→casework→verify chain, "living in signal-watch." Reframed (the user's "detection doesn't need runtime gen" + the scope/gate choices) from "orchestrator that runs the pipeline" to an ANALYST CASE-WORKBENCH: detection pre-baked/upstream (evidence bundles vendored like the corpus pin); the casework consume (6 verifiers + a LIVE neural SAR draft) + the cross-pillar verify run LIVE per case, stage-streamed. Dev-time companion (serve_chain.py + chain.html), NEVER a ship artifact (offline dists byte-frozen). A3 (defer live neural) REJECTED → live drafting pulled into v1 → the casework consume CLI is the one sibling prerequisite. Grounded against aml-substrate@df23bba + aml-casework@85602c1.

all_accept: false (A3 reject round 1 [defer-neural] → live drafting pulled into v1 → the revised set A1'/A4/A5 accepted; A0/A2 held round 1).

- A0 [HIGH] Detection is pre-baked/upstream — the substrate evidence BUNDLES are vendored (the case library); the cross-pillar grounding/verify is the LIVE, re-runnable proof ("the gate is the oracle"). ACCEPT. If false (the demo needs live detection/gen to be credible) → a live-gen hot path (slow, gen-frozen). Mitigation: frame as "detection runs upstream/batch as in a real system; this is the analyst's runtime loop"; the bundle is the analyst-facing input, exactly like a real alert. revisit-status: held (spine) — the vendored bundle IS the analyst input + the LIVE re-runnable verify is the proof: serve_chain --selftest streams a stubbed run → e2e_chain_check --real CONNECTED on the real vendored CASE-P-0010361; gate-is-oracle stands.
- A2 [MED] The case library = committed substrate evidence bundles (synthetic/illustrative), vendored into signal-watch like the corpus pin; CASE-P-0010361 + room for more. (The SAR is LIVE-drafted, not vendored — a cached SAR is the keyless fallback.) ACCEPT. If false (bundles not vendorable) → runtime gen. Mitigation: all synthetic, no real data; the committed CASE-P-0010361 proves vendorability. revisit-status: held — CASE-P-0010361 vendored byte-identical (sha1 bac574d…, illustrative:true) + a manifest; validate_chain_cases reuses check_substrate (schema + §2 id-mint + corpus grounding) + manifest↔bundle integrity; --check all 8/8 (not a build target). Vendorability proven; room for more cases by design.
- A4 [HIGH — the T0 weakest] casework can expose a thin per-case consume CLI cheaply — a ~15-line wrapper over its EXISTING functions (load_real_bundle / generate_narrative / ClaudeDrafter / record_signoff / emit_signed_sar), authored via a brief + run in a casework-rooted session. v1's live drafting is GATED on it. ACCEPT (defensible — the 5 functions exist; only the entrypoint is missing). If false (casework needs deeper changes) → v1 blocks on a bigger sibling change. Mitigation: the brief specs the thin wrapper; the companion's --selftest STUBS the subprocess so the signal-watch spine is provable offline before the CLI lands. revisit-status: HELD at beat 2 (verified 2026-06-17, aml-casework@f0542b7 Phase 7) — the CLI WAS cheap: `python -m aml_casework.ingest <bundle> --out <signed> --drafter stub|claude` is a thin wrapper over the existing functions, plus the one predicted addition — a small SRC-resident `drafter_stub.py` (the brief-A0 bundle-derived stub: one inculpatory claim per signal_id, prose names only the grounded account_id + generic lowercase typology phrases → grounds by construction). brief-A0 (can a GENERATED narrative satisfy narrative_grounding) RESOLVED YES, the recommended design. `--drafter claude` is fail-soft. signal-watch verified the chain CONNECTED via `serve_chain` driving the real CLI (drafter stub here; neural path key-gated + fail-soft, covered by casework @integration). The early wrinkle (FixedDraftStub test-only) was the only deviation from "~15-line wrapper", and it was the predicted one.
- A1' [HIGH] The companion (serve_chain.py) subprocesses casework's consume CLI + signal-watch's OWN e2e_chain_check — subprocess + file handoff, NO imports of either sibling; a DEV-TIME companion, never a ship artifact (offline dists byte-identical, --check all 8/8). ACCEPT. If false (import / dist drift) → coupling / ship regression. Mitigation: subprocess-only (the news/corpus live-mode pattern extended to drive a sibling CLI); chain.html is NOT a build target; the freeze guard holds 8/8. revisit-status: held — serve_chain.py subprocesses casework's CLI + signal-watch's OWN e2e_chain_check; grep clean (no import aml_substrate/aml_casework); chain.html not a build target, not in dist/; --check all 8/8 zero drift; pillar-status.json snapshot+restored so a workbench run never drifts the launcher.
- A5 [MED] The neural draft uses a SERVER-SIDE ANTHROPIC_API_KEY (companion env, never the browser), with the deterministic stub drafter as the no-key fallback — the demo runs keyless (deterministic SAR) AND with a key (live neural). ACCEPT. If false (key in frontend / can't run keyless) → non-negotiable violation. Mitigation: the key stays in the companion process; --drafter stub|claude; stub when no key (the "Copilot is NOT a web backend" + local-companion pattern). revisit-status: held (design+spine) — drafter_for_env() returns claude iff ANTHROPIC_API_KEY else stub (server-side only, never inlined into the page); selftest proves the key→stub selection both ways; the brief specs a fail-soft claude→stub fallback. The actual neural draft is beat 2, but the keyless-runs + key-server-side invariants are in place and tested.

## Phase 57 — Live neural SAR/STR draft + pluggable drafter backends (claude · OpenAI /v1 · opencode) (2026-06-18)

> Direction = the user picked the "live neural draft" frontier at the dev-plan direction question + reframed to add OpenAI-standard backend endpoint support (enabling local models + opencode). On the follow-up, "opencode support" = DRIVE DRAFTING THROUGH opencode (its agent loop), not a model behind a raw /v1 endpoint — so A2 was REFRAMED to the bigger interpretation and beat 2 carries TWO casework adapters (thin /v1 + the meatier opencode-agent-loop). The user then accepted all on the gate. Sibling state code-verified: aml-casework@2381d71 (two past the held pin f0542b7) — Phase 7 consume CLI + Phase 8 OAuth-wired ClaudeDrafter; the drafter is a pluggable Protocol; no OpenAI/opencode adapter exists. Two-beat, two-repo (P55/P56 rhythm): beat 1 = the signal-watch spine + live-claude (drivable now); beat 2 = the openai+opencode adapters (briefs here, live runs gated on casework). Grounded against aml-substrate@df23bba + aml-casework@2381d71.

all_accept: true (warned + restated per gate protocol — A2 was reframed at Q1 [opencode = a real agent-loop integration, not a /v1 endpoint], which DISPLACED the original weakest assumption: the new T0-weakest is A0 [opencode-feasibility], not the /v1-adapter cost. The user accepted all AFTER seeing the opencode-reframe option, so the position is: accept the bigger scope, proceed. How each accepted assumption shapes the approach: A0 is the load-bearing risk carried into implementation, mitigated by the backend-agnostic spine [selftest stubs every backend] + the /v1 adapter as the simpler local-model fallback; A1 makes the /v1 adapter the cheap companion; A2 makes the live-claude beat drivable here NOW [no sibling change]; A3 keeps all creds server-side [the non-negotiable]; A4 splits the spine [now] from the live local/opencode runs [gated].)

- A0 [HIGH — T0 weakest, NEW after the A2 reframe] Driving drafting THROUGH opencode is feasible + boundable from casework — via `opencode serve` (OpenAPI 3.1 + SSE, async POST→poll), a local model wired through opencode's @ai-sdk/openai-compatible provider (model key == llama-server --alias), avoiding the known bare-`opencode run` headless-permission bug. ACCEPT. If false (opencode serve can't be driven headless from a subprocess / SSE consumption is fragile / the agent loop won't reliably emit a verifier-groundable narrative) → beat 2's opencode leg blocks; fall back to the /v1 adapter (local models direct, no agent loop) which still delivers "local model support." Mitigation: the brief specs the serve+SSE pattern from the working-knowledge Phase-46 practitioner notes (the exact pattern is documented); serve_chain's selftest STUBS the subprocess so the signal-watch spine is provable without opencode; the /v1 adapter is the simpler fallback. revisit-status: DEFERRED (beat 2, not yet bitten) — the opencode adapter is GATED on a casework session (brief authored: aml-casework/docs/opencode-drafter-PLAN-BRIEF.md); the headless-serve+SSE feasibility risk is carried into that session, untested here. The mitigation is in place + proven: the signal-watch spine is backend-agnostic (serve_chain --selftest stubs opencode offline → CONNECTED) and the /v1 adapter is the named fallback (briefed). A0 bites (or holds) when the casework opencode adapter runs.
- A1 [HIGH] The /v1 OpenAI-standard adapter is cheap — a thin Drafter-Protocol adapter mirroring drafter_claude.py (a urllib/openai-SDK POST to OPENAI_BASE_URL/chat/completions; base_url+model+key via env) + one `openai` branch in ingest._generate_with_drafter (fail-soft like claude). ACCEPT (defended by code: the Drafter Protocol is the explicit pluggable seam, ClaudeDrafter is "deliberately thin", drafter_stub proves a 2nd adapter slots in). If false (the boundary bakes in anthropic specifics) → beat 2 balloons. Mitigation: the brief specs the thin adapter; serve_chain's selftest stubs the subprocess so the spine + the live-claude beat are provable before drafter_openai lands. revisit-status: DEFERRED (beat 2, not yet bitten) — the /v1 adapter is GATED on a casework session (brief: aml-casework/docs/openai-drafter-PLAN-BRIEF.md). The cheapness is strongly pre-supported by code (the Drafter Protocol is the explicit seam; ClaudeDrafter is "deliberately thin"; drafter_stub proves a 2nd adapter slots in) — but "cheap" is assessed when the casework session builds it. The one identified refactor (extract the prompt-shaping to drafter_prompts.py) is the only non-trivial part, and it is mechanical + test-preserving.
- A2 [HIGH] The live neural CLAUDE draft is drivable from signal-watch NOW (no sibling change) — casework@2381d71 already has `--drafter claude` + an OAuth-aware ClaudeDrafter (ANTHROPIC_AUTH_TOKEN + the oauth-2025-04-20 beta header), so serve_chain driving `--drafter claude` with server-side OAuth env produces a real neural SAR/STR draft that the 6 verifiers gate. ACCEPT (code-verified the CLI + OAuth wiring exist). If false (OAuth needs interactive login / doesn't work headless in a subprocess / rate-limited) → beat-1's LIVE run degrades to the documented fail-soft stub + the live run is scheduled. Mitigation: fail-soft is already built (claude→stub); the workbench renders drafter_effective honestly; the keyless path still demonstrates the full chain. revisit-status: PARTIAL BITE (plumbing HELD, live exercise DEFERRED) — code-verified casework@2381d71 has `--drafter claude` + the OAuth-aware ClaudeDrafter (ANTHROPIC_AUTH_TOKEN + the oauth-2025-04-20 beta header), and serve_chain drives it; the REAL chain CONNECTED against casework@2381d71 (via the STUB drafter). BUT the live NEURAL claude run was NOT exercised: no ANTHROPIC_AUTH_TOKEN/API_KEY in this env, so the documented fail-soft fired (drafter_effective=stub). The "if false" (OAuth not headless / rate-limited) is therefore UNTESTED here; the live neural path is covered by casework @integration and is one creds-set away. So: the drivable-NOW plumbing held; the live neural exercise is deferred to a creds-present session (no overclaim — the delivery reports the chain connected via stub, not via a neural draft).
- A3 [HIGH] Boundary held — serve_chain selects the drafter by NAME + passes creds/endpoints (anthropic OAuth / OPENAI_BASE_URL+key / opencode serve URL+model) via SERVER-SIDE env to the casework subprocess (no creds in chain.html); subprocess + file handoff only (no sibling import); chain.html stays a non-build-target dev companion; offline dists byte-frozen, `--check all` 8/8. ACCEPT (the Phase-56 A1'/A5 design extended). If false (creds reach the browser / a sibling import / dist drift) → non-negotiable violation §4.5 / ship regression. Mitigation: drafter_for_env extended server-side; the selftest asserts name→env mapping + no creds in the rendered page; the freeze guard holds 8/8. revisit-status: HELD — serve_chain selects backend by NAME + resolves creds/endpoints server-side; the selftest asserts NO secret/endpoint leaks into the served config/page (verified with a planted-secret env); no sibling import (grep clean); chain.html not a build target; --check all 8/8 (only dist/index.html moved with the re-grounded pin, 7 dists byte-identical); a direct secret scan of dist/index.html came back clean (the dist-wide grep "hits" were the `risk-`/`task-` false positive in the frozen corpus/console/triage). No abort triggered.
- A4 [MED] Two-beat delivery — beat 1 (signal-watch spine: N-backend selection + workbench picker + live-draft rendering + gate-verdict + selftest-stubbed; the LIVE claude neural run actually executed or honest fail-soft) is deliverable now; beat 2 (the LIVE openai + opencode runs) is gated on the casework drafter_openai/drafter_opencode adapters landing. ACCEPT (the P55/P56 rhythm; the spine is provable offline before the siblings land). If false → restructure. Standing non-ship abort rule. revisit-status: HELD — beat 1 (the signal-watch spine: N-backend selection + picker + live-draft + gate-verdict + comparison + the re-grounded pin) delivered + selftest-proven this session; the live openai/opencode runs (beat 2) are gated on the casework adapters exactly as planned. The two-beat shape held cleanly (same P55/P56 rhythm). Minor note: the live-CLAUDE run, planned as a beat-1 payoff, degraded to the documented fail-soft (no creds — see A2) — so beat 1 delivered the spine + the stub-verified chain, with the live neural exercise carried with A2; the two-beat structure itself was correct.

## Phase 58 — Corpus→substrate signal-coverage map (design the detection layer FROM the corpus) (2026-06-18)

> Direction = the user's REFRAME at the dev-plan gate: the synthetic-data + detection layer (Pillar 1) was built BOTTOM-UP (6 detectors, "chosen-not-measured" thresholds from individual advisory flags) with NO systematic join to the corpus catalog (2,251 indicators / 523 buildable = status==gap AND data==available). The corpus is the program's signal source-of-truth; the detection layer should be designed AGAINST it, targeting 200+ grounded corpus signals to demonstrate signal/atom monitoring + narrative generation at scale. Grounding (code-verified this session): aml-substrate@df23bba [Phase 14 — the persist seam is BUILT (`--emit-evidence`); 6 detectors C2/C3/C4/C5/C6/C15; the synthetic data is already RICH (6 channels wire/EMT/AFT/cash/cheque/card + full KYC/CDD + ownership graph); the top-5 corpus D-codes (D8 20% / D1 17% / D3 13% / D13 6% / D2 6% = 62%) all modeled → the gap is detector breadth + view exposure + behavioral coverage, NOT data fields]; aml-casework@2381d71 [grounding_replay scales by CAPABILITY (5 registered: C2/C3/C4/C5/C15), one capability grounds many corpus signals]. Deliverable = the executable coverage MAP + the doctrine-safe design + prioritized sibling build briefs (signal-watch's architecture role; the 200+ build runs in sibling-rooted sessions). NON-ship; the 8 ship dists byte-frozen, --check all 8/8.

all_accept: true (warned + restated per gate protocol — all three gate questions answered at the recommended option. The weakest assumption A0 [doctrine reconcilability] was surfaced FIRST and accepted explicitly [the user picked "detectors top-down, data bottom-up"]. How each accepted assumption shapes the approach: A0 sets the abort-line for T3 [any brief that stamps behavior/labels is out of bounds — the corpus drives detectors + observable-exposure ONLY]; A1 keeps the deliverable the map+briefs HERE, the build sibling-rooted; A2 makes "200+ signals" = 200+ grounded corpus indicators backed by ~15-20 capability detectors [capability-scaled, not 200 detector implementations]; A3 makes reachability MEASURED against pinned real emitted data with behavioral reachability honestly flagged reasoned-not-measured; A4 keeps it non-ship + re-grounds the cross-pillar artifact with HEADs pinned inline.)

- A0 [HIGH — T0 weakest] Doctrine reconcilability: the corpus can drive DETECTOR + observable-exposure design (top-down detectors authored to ground corpus signals) WITHOUT violating aml-substrate's emergence doctrine — data generation + labels stay bottom-up/emergent; the map MEASURES behavioral-coverage gaps, it never stamps them. ACCEPT (user picked "detectors top-down, data bottom-up"). If false (corpus-driven detector design reads as label/behavior injection) → "design data to support signals" is a doctrine violation, the sibling rejects the briefs, and the phase reframes to the SCOREBOARD framing ("measure what the existing emergent data already supports; build detectors only for those"). Mitigation: the map only classifies/measures; the T3 briefs author detectors + view-exposure, never behavior; the abort-line is explicit. revisit-status: HELD — the map only classified/measured; both briefs carry an explicit no-stamping doctrine constraint (detectors + observable-exposure ONLY); nothing stamped behavior or labels. The map SURFACED that only 2/523 buildable signals are out-of-reach (the substrate's emergent data is rich), reinforcing the boundary rather than straining it.
- A1 [HIGH] The deliverable is the executable coverage MAP + the doctrine-safe design + prioritized build briefs, authored HERE (signal-watch owns the corpus + integration contract); the actual 200+ detector build runs in aml-substrate/aml-casework-rooted sessions (their dev-* lifecycle hooks bind there). ACCEPT (user picked "Measured map + briefs"). If false (the user wants the build here) → re-root to aml-substrate. Mitigation: the P55/P56/P57 pattern — signal-watch delivers spine+contract+briefs, siblings build. revisit-status: HELD — delivered the executable map + both sibling briefs from signal-watch; the briefs live in the sibling trees (untracked there) for sibling-rooted sessions; no build attempted here.
- A2 [HIGH] "200+ signals" = 200+ GROUNDED CORPUS INDICATORS backed by ~15-20 CAPABILITY detectors, NOT 200 detector implementations — casework's grounding_replay scales by capability (28 C-codes), and one capability detector grounds many corpus indicators (523 buildable ÷ 28 capabilities). ACCEPT (implied by the Q1 "Measured map + briefs" choice over "literal 200 detectors"). If false (literal 200 detectors) → infeasible + contradicts the capability-scaled architecture + the triple-null redundancy finding. Mitigation: the map counts grounded-signals-per-capability; the briefs rank capabilities by signals-unlocked. revisit-status: HELD + SHARPENED — measured: 93 reachable-now via ONE capability (C15); the path to 200+ is capability-scaled (C7 +62, then the view-exposure capabilities); confirmed NOT 200 detector implementations.
- A3 [MED] Reachability is MEASURED against a pinned snapshot of real emitted data (the substrate observable-schema field list + the already-vendored CASE-P-0010361 emitted sample + the detector/assertion registries), with honest TIERING: observable-surface reachability = measured (D-code field exists in the schema AND populated in the emitted sample); behavioral-emergence reachability = REASONED from DESIGN.md and flagged behavior_confirmed:false (to confirm in a substrate session). ACCEPT (user picked "Pinned real data + honest tiering"). If false (schema declarations only) → "reachable" overcounts declared-but-unpopulated fields. Mitigation: the map reads the vendored emission, not just the schema; the behavioral tier is honestly separated. revisit-status: HELD — observable reachability MEASURED against the real emission (D17 modeled-inactive because counterparty_country is 0/71; the selftest tamper-test proves the classifier READS the emission, not a hardcode); behavioral tier reasoned + flagged behavior_confirmed:false. ONE judgment surfaced + NAMED as the most-likely-to-move claim (C7→emerges, justified by the measured single-feature separability, vs the grounding agent's literal data-only read).
- A4 [MED] NON-ship — the 8 ship dists stay byte-frozen (--check all 8/8); build.py never imports the new script (scripts/signal_coverage_map.py is dev-time only, the corpus_redundancy/cd_correctness measure-first pattern); the cross-pillar artifacts (the map + the doc) re-grounded against the sibling HEADs, pinned inline (the 2026-06-16 re-ground-before-commit process rule). ACCEPT. If false (ship-surface touch / dist drift) → STOP-and-surface per the standing abort rule. revisit-status: HELD — --check all 8/8 zero drift; build.py never imports the script; the HEADs pinned inline (substrate-pin.json + the doc + contract §8); ZERO ship artifacts in the change set.

## Phase 59 — Consume the substrate Phase 15 build into the coverage map (re-ground · re-freeze · document the exposure≠reachable-now coupling) (2026-06-18)

> Direction = aml-substrate Phase 15 (@5875241, DELIVERED) shipped the SUBSTRATE HALF of Phase 58's brief — a label-blind PartyView (exposes D8 KYC + D12 pep) + 4 SCREENING_DETECTORS (C7/C14/C8/C26). substrate-pin.json was re-grounded in the working tree (uncommitted) but coverage.json was left un-frozen → `signal_coverage_map.py --check` fails. The user chose **direction A** at the dev-plan gate (re-freeze + document the coupling, tiers unchanged) over re-tiering with a `blocked_on` field (C) / holding-and-reverting (B). The user then chose **"verify first, then re-confirm"** → A0/A1 were code-verified against the real sibling source BEFORE any freeze, both HELD, the user re-confirmed proceed. Grounded against aml-substrate@5875241 + aml-casework@4ac9523 + signal-watch-corpus@472b44e.

all_accept: true (warned + restated; verification-gated — A0/A1 were checkpoint-verified clean against the real sibling source BEFORE the freeze, per the user's "verify first" position; A2/A3 held by evidence. How each shapes the approach: A0 holds the map's grounding [pin faithful to the real sibling HEAD]; A1 keeps direction A valid [the 242→needs-behavior / 62-C7→needs-detector movement is INTENDED classifier semantics, so prose-documenting + keeping tiers is the honest call]; A2 sets the honest headline [reachable-now genuinely can't move on the substrate half alone — the 3-way AND]; A3 keeps it non-ship.)

- A0 [HIGH — T0 weakest] The uncommitted substrate-pin.json faithfully matches aml-substrate@5875241. ACCEPT (VERIFIED before freeze, the user's "verify first" position). If false → re-freeze a map grounded on a wrong pin → the whole classification is off. Result: code-verified at HEAD 5875241 (clean tree) — PartyView's 16 fields match `monitor/detectors/views.py` exactly (the 4 label/PII fields latent_role/illicit_income/illicit_income_type/sin correctly omitted); the 4 detectors (C7 BusinessActivityAnomaly→fin-2023-alert001:IND-06 · C14 KycIntegrity→fin-2026-alert001:IND-04 · C8 IncomeMismatch→fin-2023-alert001:IND-07 · C26 ScamVulnerable→fin-2020-alert001:IND-01) class names + capabilities + signal_ids + SCREENING_DETECTORS membership all match; D8/D12 exposed via PartyView (cdd_level/source_of_funds, pep_tier). revisit-status: HELD (verified clean before freeze).
- A1 [HIGH] The classifier's post-landing tiering — 242 KYC/pep → needs-behavior (via the `modeled-inactive` branch: D8/D12 exposed:true but emission_probe:null → not active in the txn-only emission), 62 C7 → needs-detector (via the `emerges` branch: has_detector:true but has_casework_assertion:false → not is_live) — is INTENDED classifier semantics, not a pin-edit bug. ACCEPT (VERIFIED). If false → keeping tiers + prose bakes in a wrong number, defeating direction A. Result: read `classify()` (lines 146-161) — the movement is the deliberate multi-way AND; the per-signal `data_source_class` + `behavior_confirmed:false` carry the real reason. The caveat documented in prose per direction A: the tier NAMES are now stale/composite for the touched signals — needs-behavior=296 = ~242 exposed-but-unmeasurable-on-the-txn-emission (an EMISSION-SAMPLE limitation, cleared by a party-bearing bundle, NOT emergence work) + ~54 genuine emergence gaps; needs-detector=62 (all C7) = detector-EXISTS-blocked-on-the-casework-assertion. revisit-status: HELD.
- A2 [HIGH] reachable-now genuinely can't move on the substrate half alone — the 3-way AND (has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges") leaves it at 93; the rise needs a party-bearing emission (substrate) + the 4 paired grounding_replay assertions (casework). ACCEPT (held by evidence across 3 repos: the emission is txn-only [substrate docs: "No +62/+128 claimed"]; aml-casework@4ac9523 [Phase 9 = drafter backends] has NO C7/C8/C14/C26 grounding_replay assertions). revisit-status: HELD.
- A3 [MED] NON-ship — re-freezing coverage.json touches no ship dist (coverage-map isn't a build target); `--check all` stays 8/8; build.py never imports the script. ACCEPT. revisit-status: HELD.

## Phase 60 — Consume the landed sibling halves (substrate P16-18 + casework P10-12): the C7 reachable-now rise + the real e2e chain (2026-06-19)

> Direction = a cross-pillar review (code-verified this session) found BOTH siblings moved 3 phases past the Phase-59 pins: aml-substrate@9c75c03 [Phase 18 — §13 fourth-null closure (P16) · party-bearing bundle + `--screen` (P17) · emits the real C8 party-bearing v0.2 evidence bundle, sample CASE-P-0000251.json (P18)]; aml-casework@c6d8401 [Phase 12 — C7 assertion (P10) · C8 assertion (P11) · C14 party-leaf assertion (P12); C26 deliberately UNREGISTERED, honest null]. Phase 59 A2 said reachable-now rises only when a party-bearing emission (substrate) + the paired grounding_replay assertions (casework) BOTH land — they now have. The verified consequence: the 3-way AND (has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges") moves ONLY C7 (the one screening cap with behavior_emergence=="emerges") → ALL 78 of its buildable signals become reachable-now (62 direct + 16 proxy — `is_reachable` is evaluated FIRST, regardless of data_source_class; needs-detector 62→0 exhausted, plus 16 C7 leave needs-behavior(15)/needs-view-exposure(1)) → reachable-now 93→171 (+78, the first REAL rise — the C7 win banked for a combined consume; matches the 2026-06-18 transient review + the banked memory). C8/C14/C26 do NOT move (behavior_emergence data-only/absent). The user chose the **combined scope** (coverage-map consume + the real e2e chain on substrate's C8 bundle → casework SAR) over the recommended coverage-only focus — a deliberate scale-up batching the full landed sibling work — and **"verify first, then freeze"** on the C7 +78. Grounded against aml-substrate@9c75c03 + aml-casework@c6d8401 + signal-watch-corpus@472b44e.

all_accept: true (no reject/don't-know; warned + restated. NOTE: the user up-scoped Q1 to the COMBINED scope [coverage map + e2e chain] over my recommended coverage-only — a scale-up, not an assumption rejection. How each accepted assumption shapes the approach: A0 makes the C7 +78 a VERIFY-FIRST gate [re-ground → diff → confirm BEFORE freeze, the Phase-59 A0 pattern the user re-chose] — STOP-and-surface if the delta is anything but a clean C7-only +78; A1 sets the honest headline [the binding gap for screening caps is emergent behavior, NOT the cross-pillar wiring that just completed — substrate emergence-engine work, bottom-up, or a permanent null for C26]; A2 keeps the e2e chain honest-either-way [CONNECTED proves the spine on real C8, or a contract-drift GAP is a real surfaced finding, never papered over]; A3 keeps it non-ship + re-grounds both cross-pillar artifacts against the sibling current HEADs, pinned inline.)

- A0 [HIGH — T0 weakest] Clean C7-only +78: casework@c6d8401's C7 grounding_replay assertion (`_screen_c7_peer_anomaly`, Phase 10) is green/registered, and nothing in substrate P16-18 shifted C7's detector or its emission measurement → re-grounding the pin (C7 has_casework_assertion false→true) makes `is_reachable(C7)` true → ALL 78 C7 buildable signals become reachable-now (62 direct + 16 proxy) → reachable-now 93→171, needs-detector 62→0, needs-view-exposure 70→69, needs-behavior 296→281, with C8/C14/C26 unmoved. ACCEPT (VERIFY-FIRST per the user's position — re-ground → run map → diff tier counts → confirm BEFORE freezing coverage.json). NOTE: the +78 (not +62) corrects a plan-stage undercount — `is_reachable` fires before the data_source_class fall-through, so the 16 C7 signals in needs-behavior/needs-view-exposure ALSO flip (proxy mode), not just the 62 needs-detector. If false (delta ≠ clean C7-only +78, or the assertion isn't actually green at c6d8401) → STOP-and-surface, do NOT freeze; the value proposition or the pin needs rework. revisit-status: HELD-WITH-CORRECTION — verify-first VALIDATED itself: a plan-stage undercount (+62, counting only the 62 needs-detector C7) was caught by re-reading classify() (is_reachable precedes the data_source_class branch) → corrected to +78 BEFORE the freeze; the empirical run confirmed EXACTLY reachable-now 93→171 (62 direct + 16 proxy), needs-detector 62→0, C8/C14/C26 unmoved (clean C7-only). casework's C7 assertion confirmed registered + dispatched at c6d8401.
- A1 [HIGH] C8/C14/C26 do NOT move despite the now-landed casework C8/C14 assertions + substrate's party-bearing emission — the binding gap is the 3rd leg, behavior_emergence (data-only for C8/C14, absent for C26), which is NEITHER emission work NOR assertion work. The honest finding to document: emergent behavior is the screening-capability bottleneck (substrate emergence-engine work, bottom-up, or a permanent honest null for C26) — Phase 59's "both halves" framing sharpened to "only the cap whose behavior genuinely emerges reaches reachable-now". ACCEPT (held by evidence — pin values code-verified this session: C8=data-only, C14=data-only, C26=absent). revisit-status: HELD — measured 0 movement for C8/C14/C26 despite their landed assertions + substrate's party-bearing emission; `C8/D1 → needs-behavior` pinned as the selftest regression anchor proving the assertion alone doesn't move a data-only cap. The headline finding: the binding gap for screening caps is emergent behavior, not wiring.
- A2 [HIGH] The real e2e chain is producible + honest-either-way: substrate's real C8 party-bearing bundle (committed sibling fixture CASE-P-0000251.json) + a casework-produced signed SAR (run casework's DETERMINISTIC pipeline as a TOOL → output JSON; file-contract, NO sibling import — not driving casework's dev-lifecycle) run through e2e_chain_check.py --real; the outcome is recorded honestly — CONNECTED (the spine proven on a REAL C8 chain, not the synthetic C4 fixture) or GAP-FOUND (e.g. casework not yet adopting substrate's v0.2 contract → a real cross-pillar finding surfaced). ACCEPT. If false (casework can't be run from a signal-watch session / the bundle can't be obtained) → fall back to documenting chain-readiness + handing the real --real run to a casework-rooted session, recorded honestly. revisit-status: HELD — STRONGER than expected: the chain CONNECTED, not GAP. casework consumed substrate's real C8 v0.2 bundle (CASE-P-0000251) on FIRST contact → signed SAR, zero blocking violations (all 6 Class-G verifiers passed, no contract-version gap); e2e_chain_check --real CONNECTED (C4 too). The fallback was never needed.
- A3 [MED] NON-ship — re-freezing coverage.json + re-grounding e2e_chain_check.py (GROUNDING_HEADS) + the docs/briefs touch no ship dist; `--check all` stays 8/8; build.py never imports signal_coverage_map.py or e2e_chain_check.py; both cross-pillar artifacts re-grounded against the sibling CURRENT HEADs, pinned inline (the 2026-06-16 re-ground-before-commit rule). ACCEPT. If false (ship-surface touch / dist drift) → STOP-and-surface per the standing abort rule. revisit-status: BIT — the e2e half (combined scope) cascaded into the launcher SHIP dist: e2e_chain_check regenerates data/pillar-status.json (grounding_heads), which the launcher dist/index.html embeds → re-grounding the e2e artifact INHERENTLY re-grounds the launcher. The abort rule fired (STOP-and-surface); the user chose Option-A (the sanctioned launcher re-ground — the Phase-55/57 pattern: the launcher IS the cross-pillar status front door). Resolution: 7/8 dists byte-identical, only dist/index.html moved (grounding_heads, 2 lines); coverage map + everything else held non-ship; --check all 8/8. The "zero ship artifacts" framing was wrong for the COMBINED scope (correct for coverage-map-only). SOFT OBS: e2e --selftest also writes pillar-status.json ("bridges pending") → silently clobbers the committed --real state; restored via --real; a guard is a future candidate.

## Phase 61 — Blueprint review against what's been implemented (+ batched cross-pillar re-ground; deep blueprint true-up) (2026-06-20)

> Direction = the user invoked `/dev-plan "blueprint review against whats been implemented"` under ultracode. docs/program-blueprint.md is a Phase-47/48 DESIGN snapshot whose central §3 built-vs-design-stage partition is materially stale: the program SPLIT into pillars (P50) and built aml-substrate P1–20 (@34400e2 — detectors C2–C15 + screening C7/C8/C14 + composition bench + emergence nulls) + aml-casework P1–12 (@c6d8401 — 6 Class-G verifiers + real neural narrator + grounding_replay + signed SAR) + signal-watch's coverage map / e2e chain / C/D control / corpus-redundancy. The four §3 "(design-stage)" rows (txn monitoring · case investigation · SAR/STR narrative · LFCM assist) now carry substantial committed SIBLING implementation. The user chose **C — report + deep blueprint revision** (audit doc + §3 markers + §8/§11/§13 prose), **batched the cross-pillar re-ground** (coverage map pinned @P18 + e2e pins → substrate@P20/casework@P12, verify-first), and **re-sync blueprint-report.html revised sections**. Re-opens the FROZEN-set blueprint (USER OVERRIDE, logged). Grounded against aml-substrate@34400e2 + aml-casework@c6d8401 + signal-watch-corpus@472b44e (HEADs verified LIVE this session via git log, not from loaded facts — the cross-pillar-review process rule).

all_accept: true (no reject/don't-know; warned + restated per gate protocol. NOTE: A1 up-scoped to the BATCHED cross-pillar re-ground over my recommended report-only review boundary — a deliberate scale-up [the Phase-60 batching instinct], not an assumption rejection. How each accepted assumption shapes the approach: A0 makes every blueprint edit VERIFY-FIRST [no marker/prose flip without adversarially code-verifying the named artifact at its pinned HEAD] + adds a THIRD honest status where binary built/design-stage would overclaim a synthetic pillar build; A1 batches the coverage-map + e2e re-ground as a verify-first NO-OP-EXPECTED consume [substrate P19/P20 added no screening detector + C14 was already not-moving] — non-zero tier movement → STOP-and-surface, do NOT silently re-freeze; A2 makes re-opening the blueprint a sanctioned deliberate act carrying the cross-repo re-ground coupling forever; A3 re-syncs the report HTML's revised sections; A4 keeps it non-ship EXCEPT the sanctioned launcher re-ground the e2e cascade forces [the Phase-60 Option-A pattern].)

- A0 [HIGH — T0 weakest] Verify-first gates every blueprint edit: a §3 marker flip or an §8/§11/§13 prose "now built" claim is written ONLY after adversarial code-verification of the named sibling/local artifact at its pinned HEAD; where the binary built/design-stage would overclaim a synthetic, pillar/probe-scale build, the marker becomes an honest THIRD status ("pillar-build / synthetic-scale", caveated) instead of "(built)". ACCEPT. If false (verification is sloppy / the third status is skipped) → the revision launders "synthetic pillar build" into "built workload", the exact dishonesty the blueprint exists to prevent (the doc's own standard: "(design-stage) rows do not exist"). Mitigation: the audit workflow's verify stage is default-skeptical and code-checks each named artifact; a row that verifies as partial gets "partial/pillar-scale" with the caveat, never "(built)"; the report enumerates the evidence per status. revisit-status: HELD (the spine, T0-weakest — and it BIT in the productive sense): verify-first did real work both directions. (1) The §3 binary was OUTGROWN — the audit (96-agent fan-out) found 4 "(design-stage)" rows now carry committed SIBLING code at pillar-build-synthetic scale (real fail-closed code, synthetic data, NOT deployed); without the third status the revision would have either UNDER-claimed (leave them design-stage) or OVERCLAIMED (stamp them "(built)"). The third tier ("pillar-build/synthetic-scale", caveated) carried them honestly; partials stayed "partial/pillar-scale", never "(built)". (2) Verify-first confirmed the ZERO tier movement before re-freeze and confirmed no blueprint marker overclaimed a verified artifact at its pinned HEAD. 8 blueprint-stale + 6 impl-diverged items enumerated with evidence per status.
- A1 [HIGH] The batched cross-pillar re-ground is a VERIFY-FIRST, no-op-EXPECTED consume: re-grounding signal_coverage_map.py's pin + e2e_chain_check.py GROUNDING_HEADS to substrate@34400e2 (P20) / casework@c6d8401 (P12) moves NO tier — substrate P19 (C14 KYC-integrity emergence: honest null closure) confirmed C14 stays not-moving and P20 (composition stress-bench + advisory drift seam) added no screening detector → the diff is zero tier movement (reachable-now stays 171), only the pin HEAD + regrounding_note update; the e2e --real on the vendored C8 bundle stays CONNECTED (casework unchanged @c6d8401). ACCEPT (user batched it in). If false (the diff is non-zero — P19/P20 moved reachability, or e2e finds a GAP from P20's drift seam) → STOP-and-surface, do NOT silently re-freeze; the movement is a finding in its own right (the Phase-59/60 verify-first abort discipline). Mitigation: re-ground → run map → DIFF tier counts → confirm BEFORE re-freeze; e2e outcome recorded honestly (CONNECTED or GAP). revisit-status: HELD — genuinely ZERO tier movement, as predicted. `git diff 9c75c03..34400e2` (substrate P18→P20) touched NO detector/view — only `validate/composition.py` (the stress-bench), a 51-line `monitor/signal_ref.py` referential helper, and 2 measurement probes (kyc_emergence/composition_redundancy); P19's C14 honest-null added no screening detector. coverage.json diff = the single `aml_substrate: 9c75c03→34400e2` pin line; `--check` byte-identical (523 signals, reachable-now 171 held) + `--selftest` green; e2e `--selftest` green; no STOP-and-surface needed.
- A2 [HIGH] Re-opening the blueprint is a sanctioned, deliberate act — it sits in the standing FROZEN set "unless re-opened"; the user's C choice IS the conscious re-open (USER OVERRIDE, logged). It is non-ship (program-blueprint.md + blueprint-report.html are not build targets; --check all stays 8/8). The cross-repo "built" refs + HEAD pins entering the blueprint carry the re-ground-before-commit coupling forever (the coverage-map pattern, accepted). ACCEPT (by construction of the C choice). If false (the blueprint should stay frozen / cross-repo refs are unwanted) → fall back to report-only (option B), blueprint byte-frozen. Mitigation: every edit traces to a verified finding; HEADs pinned inline; "Status: DESIGN" framing + §15 demo-charter non-negotiables preserved. revisit-status: HELD (by construction) — the re-open was the conscious C choice; the blueprint stayed non-build-target (`--check all` 8/8 across the re-open). Three-tier legend + §3 markers + §7/§8/§11/§13 prose trued-up with cross-repo refs + HEAD pins inline; "Status: DESIGN" + the §15 demo-charter non-negotiables preserved verbatim. The cross-repo re-ground coupling now travels with the blueprint forever (accepted).
- A3 [MED] blueprint-report.html (the hand-synced render, non-ship, not a build target) is re-synced for the revised sections + a md↔html consistency check is added, so the rendered report does not contradict its source after the deep revision. ACCEPT (user chose re-sync over defer). If false (full md→html regeneration is too heavy) → scope to the changed sections + a consistency note; full regeneration stays a future refactor candidate. Mitigation: only the revised §3/§8/§11/§13 sections are synced. revisit-status: HELD — blueprint-report.html revised sections re-synced to match the md + a md↔html consistency note added; the HTML opens offline, DESIGN-labeled framing intact; full md→html regeneration stays a future refactor candidate (the hand-sync drift risk is now noted as a consistency-check candidate alongside the NEW review doc).
- A4 [MED] NON-ship EXCEPT the SANCTIONED launcher re-ground: the e2e re-ground regenerates data/pillar-status.json (grounding_heads), which the launcher dist/index.html embeds → re-grounding the e2e artifact INHERENTLY re-grounds the launcher (the Phase-60 Option-A pattern, code-verified there). So: 7/8 ship dists byte-identical + the ONE sanctioned launcher grounding_heads move (≤2 lines); --check all 8/8; build.py never imports signal_coverage_map.py or e2e_chain_check.py; the audit reads committed sibling artifacts via file-contract, NO sibling import. ACCEPT. If false (any OTHER dist drifts / a sibling import / the blueprint becomes a build target) → STOP-and-surface per the standing abort rule. Mitigation: `git diff --stat HEAD -- dist/` must show ONLY dist/index.html; grep-guard for sibling imports. revisit-status: HELD (by construction, as the Phase-60 Option-A pattern predicted) — `git diff --stat HEAD -- dist/` = ONLY dist/index.html (1 line, the sanctioned launcher grounding_heads cascade via data/pillar-status.json); the 7 other dists byte-identical; `--check all` 8/8; `! grep import aml_substrate|aml_casework` clean on both scripts; build.py never imports either. The audit read committed sibling artifacts via file-contract only, NO sibling import.

## Phase 62 — Grounded probe-history consume + sanctioned triage-console unfreeze (2026-06-20)

> Direction (AS STATED AT THE GATE — the §14 unfreeze leg was later STOOD DOWN at the T4 checkpoint; see the RESOLUTION below) = the user invoked `/dev-plan` to consume aml-substrate's P22 "grounded probe-history" END-TO-END into signal-watch. Substrate P22 (loop-closure) projects monitor/compose.Dossier into a grounded `alert-history.json` (conformance-validated against a VENDORED copy of signal-watch's OWN `probe_history_stats.py` @58925a8), which REPLACES the SYNTHETIC Phase-48 probe-history fixture as the curate source for BOTH the §12 non-ship measurement layer (probe_history_stats.py) AND the §14 triage console (data/triage/scenarios.json — a FROZEN ship artifact). Substrate output is itself synthetic (meta.synthetic:true), so "no real data" holds. The C-code→TM-### namespace map becomes SHIP-LOAD-BEARING (the curate source + the silent_rules metric). Only the HISTORY-DERIVED strata (fired-signal + below-the-line) re-ground; the novel + random strata stay as-authored. dist/triage rebuilds + re-freezes at a NEW grounded baseline (the OTHER 7 dists stay byte-frozen) — a sanctioned USER-OVERRIDE ship-artifact unfreeze, the same class as the Phase-60 launcher + Phase-61 blueprint true-up. build.py NEVER imports the substrate (tool-use produces the file, build reads committed data/ only). Honesty split: the alert/entity/cited-evidence side becomes GROUNDED in real substrate detection output, but DISPOSITIONS stay label-blind ILLUSTRATIVE (never ground truth) — metrics framed "grounded detection, illustrative dispositions". Grounded against aml-substrate@<P22 commit, pinned at T1 verify> + aml-casework@c6d8401 (unchanged) + signal-watch-corpus@472b44e.

all_accept: false (A1 REJECTED → REVISED ACCEPT [the consume flows end-to-end into dist/triage, a sanctioned unfreeze, with a built-in CHECKPOINT]; A3 DON'T-KNOW → routed to the DELIVERY-GATE checkpoint [the honesty framing is checked by the user before anything is called "grounded"]. How each shapes the approach: A0 makes T1 a verify-first gate on the substrate's committed P22 HEAD [pause+ask if uncommitted] before any consume — running substrate's `--probe-history` projector is sanctioned tool-use, file-contract output, NOT lifecycle-driving; A1's revision flows the grounded history into dist/triage with a PAUSE-CHECKPOINT if the strata don't map cleanly or build-boundary validation breaks [report, do NOT redesign the console to force the fit]; A3 drafts the grounded-detection/illustrative-dispositions framing explicitly in T3/T4 for the user's delivery-gate check.)

- A0 [HIGH — T0 weakest] The substrate P22 grounded probe-history projector is COMMITTED in the aml-substrate session, and running its committed `--probe-history` is sanctioned tool-use (a file-contract output), not driving the sibling's dev-lifecycle. ACCEPT — T1 verifies `git -C /Users/jwang/aml-substrate` and pins that committed HEAD inline. If false (still uncommitted at T1) → PAUSE and ask the user to commit it in the substrate session BEFORE proceeding; do NOT consume an uncommitted projector (the cross-pillar re-ground rule: code-verify the sibling's CURRENT committed HEAD, never reason from loaded facts; the P21/P22 HEADs in the planning blockers may be stale). Mitigation: T1 is the verify-first gate; the projector output is deterministic (re-run byte-identical) and conformance-validated against signal-watch's own vendored `probe_history_stats.py` @58925a8.
- A1 [HIGH] Consume scope. Round 1 (the grounded history flows END-TO-END into dist/triage — a sanctioned ship-artifact unfreeze): REJECT — an unfreeze of a frozen ship artifact is not a default; it needs an explicit guard. Round 2 (REVISED ACCEPT: the consume DOES flow end-to-end into dist/triage as a sanctioned USER-OVERRIDE unfreeze [the Phase-60 launcher / Phase-61 blueprint precedent], WITH a built-in CHECKPOINT — if the grounded history doesn't map cleanly into the §14 strata OR breaks build-boundary validation, PAUSE and report; do NOT redesign the console to force the fit): ACCEPT. If false (the strata don't map / validation breaks and we force it) → the §14 grammar / decisions-not-correctness / no-real-data invariants are at risk → STOP-and-surface at the T4 checkpoint, fall back to the §12 non-ship measurement consume only (triage stays byte-frozen). Mitigation: T4 reads `curate_triage_scenarios.py` FIRST + confirms strata coupling before regenerating; only the HISTORY-DERIVED strata (fired-signal + below-the-line) re-ground, novel+random byte-stable; the 7 other dists stay byte-frozen; build.py never imports the substrate.
- A2 [HIGH] The C-code→TM-### namespace map is SHIP-LOAD-BEARING and grounded: every C-code in the grounded history resolves to a TM-### rule id (from data/capability-taxonomy.json) or an explicit honest-null; the map drives both the curate source and the §12 silent_rules metric. ACCEPT. If false (a C-code resolves to nothing AND the null isn't surfaced honestly) → the silent_rules metric and the curated scenarios silently mis-attribute capability → the grounded claim is hollow. Mitigation: T2 commits the map closed-vocab-validated against the taxonomy; `--selftest`/assert covers every C-code resolves-or-explicit-null; a missing rule is surfaced, never silently dropped.
- A3 [MED — DON'T-KNOW → DELIVERY-GATE checkpoint] Honesty framing: the alert/entity/cited-evidence side becomes GROUNDED in real substrate detection output, but the DISPOSITIONS stay label-blind ILLUSTRATIVE (never ground truth) — the metrics are framed "grounded detection, illustrative dispositions", never "real disposition rates". DON'T-KNOW at the gate → ROUTED to the delivery-gate checkpoint: the framing is drafted EXPLICITLY in T3 (the §12 measurement surfacing) + T4 (the triage reveal/ledger copy), and the user checks it at the DELIVERY gate before anything is called "grounded". If false (the framing reads as claiming grounded dispositions) → re-word at the delivery checkpoint before commit; the always-on "Illustrative data & outputs" badge stays. Mitigation: no overclaim language in T3/T4; dispositions explicitly labeled illustrative/decisions-not-correctness; the user adjudicates the framing at delivery.

> RESOLUTION (2026-06-21, T1–T6 done): A0 HELD — P22 was committed at T1 (verified `git -C /Users/jwang/aml-substrate` HEAD ae98924; the planning-stage 2e5d0f0/P21 facts WERE stale, exactly as A0 anticipated — code-verify caught it); the projector ran deterministically (sha256 d9d1110e, byte-identical re-run), pinned. **A1's CHECKPOINT FIRED**: reading `curate_triage_scenarios.py` firsthand showed §14 couples to alert-history via only 5 metadata fields on 7/20 scenarios — everything substantive (panels/TM-pairing/divergent-pair/controls) is hand-authored, the substrate's label-blind alerts carry NO fact pattern, and the marquee TM-104 pair is C20 (no substrate detector; the grounded build fires only C2/C3/C5/C15). The §14 unfreeze was therefore STOOD DOWN — the user chose "accept the boundary" → dist/triage stays BYTE-FROZEN, no re-curation, NO redesign-to-force (the A1 fallback exactly: §12-measurement-consume-only). A2 HELD — the map is committed + selftested (inversion-faithful, C15 honest-null surfaced). A3 HELD — the honesty split is explicit (every disposition-derived metric tagged "[over illustrative dispositions]"); an adversarial overclaim audit found NONE. The only ship change is the launcher pin cascade (dist/index.html, 1 line — Phase-60 Option-A); `--check all` 8/8; coverage re-ground to substrate@ae98924 = ZERO tier movement (reachable-now 171). Adversarial 4-lens verification: all HOLD. **all_accept stays false (A1 reject→checkpoint-fired-to-boundary, A3 don't-know→delivery-checked).**

## Phase 63 — Investigator case workbench: clutter → signals → live decisioning + grounded narrative (companion demo) (2026-06-21)

> Direction = the user invoked `/dev-plan` for the capstone SALES-PITCH demo (the artifact tying all three pillars together for a bank stakeholder): a presenter-driven INVESTIGATOR CASE WORKBENCH walking clutter → clarity → live decisioning → grounded narrative over a curated range of synthetic cases. Three beats: (1) a single CLUTTERED investigator page (full KYC profile + accounts + txn summaries/details + relationships) — the information-overload "before"; (2) "turn on signals" — risk-grounded signals surface relevant activity + compose the risk picture (clutter→clarity; value = grounding/clarity/defensibility/corroboration, NO catch-rate claim — the substrate detection-lift triple-null governs); (3) the LIVE finale — decisioning + narrative via real backends (default Claude, configurable), wiring the EXISTING serve_chain→casework consume. Each case carries an ILLUSTRATIVE precedent-confidence badge. Scoped TIGHTLY: the agentic tool-calling (OSINT/counterparty/network-ER) + the precedent-confidence GATING ENGINE are sequenced as FOLLOW-ON phases. Companion-served (extends serve_chain), NOT a build/dist target → lite ceremony. Grounded against signal-watch@1d35f84 (HEAD) + the vendored marquee bundle CASE-P-0010361 (aml-substrate pin) + aml-casework@c6d8401 (the Phase-57 Claude drafter backend, run as a TOOL via the consume CLI — file-contract, NO sibling import).

all_accept: true (no reject/don't-know; warned + restated per gate protocol — an all-accept on a NEW demo-artifact direction, the smell the gate exists to surface; flagged to the user in-conversation. How each accepted assumption shapes the approach: A0 makes the clutter SELF-CONTAINED [marquee case = real vendored bundle for the grounded signals + the live finale; the 2-3 range variants = signal-watch-local synthetic curate, the triage/book.json pattern] → NO sibling substrate phase blocks P63; A1 keeps confidence DISPLAY-ONLY [illustrative badge now; the auto-gating engine is its own follow-on] → P63 stays tight; A2 makes beat 3 WIRING-WITH-A-CHECKPOINT [run the never-live-exercised Phase-57 claude backend once early; real debugging beyond creds = a surfaced finding, not silent absorption]; A3 keeps it COMPANION-ONLY [serve_chain-served page, not a 9th build target → no launcher cascade, lite ceremony holds, --check all stays 8/8].)

- A0 [HIGH — T0 weakest] The investigator "clutter" (rich KYC profile + accounts + relationships for the range VARIANTS) can be authored as signal-watch-LOCAL synthetic curate (the deterministic curate pattern of triage scenarios.json / book.json) — substrate-grounded emission is NOT required; only the ONE marquee LIVE case needs the real vendored bundle (its alerts ground to real advisories + drive the casework finale). ACCEPT (hybrid). If false (the user wants every case substrate-grounded, or the clutter must be real substrate party output) → P63 acquires a cross-pillar dependency (a substrate rich-party multi-case emission phase) + sequencing before P63 can close. Mitigation: the SIGNALS stay grounded (real bundle); the variant clutter-profile is illustrative set-dressing under the always-on badge; curate is deterministic + selftested, never hand-fabricated ad hoc. revisit-status: bit (FLIPPED post-gate — the user rejected the hybrid/local-synth framing and chose option B [substrate-grounded real population]; superseded by A0' below, which VERIFIED feasible @f90bd39 → no precursor phase. The flip is the gate working: the all-accept smell surfaced, the user re-opened it, the stale "needs a precursor" inference was retracted on code-verify)
- A1 [HIGH] Showing an ILLUSTRATIVE per-case precedent-confidence badge ("N similar prior dispositions — chosen, not measured") satisfies the confidence-from-precedent intent for P63; the auto-GATING engine (confidence + sample-size → fewer human gates) is a FOLLOW-ON phase, not P63. ACCEPT. If false (the gating IS the point and must be demoed in P63) → the phase roughly doubles (a similarity function + precedent store + gate policy). Mitigation: the badge is honest-illustrative under the badge; the engine earns its own phase on the LFCM path; if the display alone under-sells at delivery, the engine is the next phase. revisit-status: held (confidence stayed DISPLAY-ONLY — an illustrative precedent-confidence badge anchored to real fired-signal-combo frequency [gate funnel 129 auto-clear / 52 review / 19 human-gate], dispositions label-blind illustrative; the auto-GATING engine remains a deferred follow-on phase as planned)
- A2 [MED] Beat 3 is WIRING the existing live finale, not building it: the Phase-57 Claude drafter backend runs live end-to-end through serve_chain → casework consume (it was BUILT but never exercised live from signal-watch — creds-gated). ACCEPT (wire + checkpoint). If false (it needs real debugging beyond creds) → that's a surfaced finding / its own task, not silently absorbed into "wiring". Mitigation: run it live ONCE early (T4 checkpoint); STOP-and-surface if it's debugging not creds; the deterministic stub fallback keeps the demo robust regardless. revisit-status: bit-productively (the T4 live-finale checkpoint FIRED and did real work — it surfaced a real cross-pillar finding: composed cases fail on a substrate↔casework C3/C15 replay divergence [~28% of composed cases sign, coverage MEASURED at 57/200]. NOT silently absorbed — embraced as the FAIL-CLOSED defensibility climax [the gate refusing → escalate-to-human]; the verifier was NEVER loosened, abort rule held; user accepted the framing)
- A3 [MED] The workbench is a COMPANION extension (a serve_chain-served page), NOT a new offline ship artifact — so lite ceremony holds (the Phase-49 "new ship artifact → STANDARD" precedent does not fire) and no build/dist target is added. ACCEPT. If false (it should be an offline dist/workbench ship target) → ceremony flips to STANDARD + build.py gains a 9th target + --check drift + the launcher cascade. Mitigation: beats 1-2 are model-free (robust even companion-served); the live finale needs the companion anyway; `--check all` must stay 8/8 with ZERO dist drift (companion-only); chain.html (Phase 56) is the precedent for a companion-only, non-build-target page. revisit-status: held (companion-only HELD — workbench.html + serve_workbench.py served by the companion, NOT a 9th build/dist target; lite ceremony held; `--check all` 8/8 with ZERO dist drift confirmed at delivery. NOTE: a SEPARATE mid-phase USER OVERRIDE added a thin pytest wrapper [pyproject.toml package=false + tests/test_selftests.py + uv.lock] — dev-tooling only, no runtime dep, the dep-free paths stay the source of truth)

> REVISION (2026-06-21, same day) — A0 FLIPPED then VERIFIED. After the all-accept close, the user reviewed the curated-4 scope and chose **(B): feed the REAL (synthetic) substrate alert population**, REJECTING A0's hybrid/local-synth/self-contained framing in favor of substrate-grounded grounding. This re-opened the gate. RESOLUTION: code-verified aml-substrate@f90bd39 (clean tree) this session — the earlier "substrate lacks the rich profile → needs a precursor emission phase" inference (from the OLD vendored CASE-P-0010361 bundle) was STALE and is RETRACTED. The substrate's committed contract-v0.2 emission (`PartyView`, since substrate Phase 17) ALREADY emits the rich investigator clutter — risk_rating/cdd_level/pep_tier/sanctions+adverse-media flags/occupation/source_of_funds/source_of_wealth/nationality/residency/NAICS/expected_monthly_volume+count, label-leak-proof by frozen dataclass — at POPULATION scale (`--emit-evidence --emit-screening`, ~500-2,000 deterministic synthetic bundles per 50k-client run; `meta.synthetic:true`). So (B) is feasible NOW with NO substrate precursor phase. Running the emit is sanctioned tool-use (file-contract; build.py NEVER imports the substrate — the Phase-62 probe-history-consume pattern). all_accept now FALSE (A0 reject→verified-revised-accept). REVISED positions (the user's (B) + the two build-parameter answers ARE the gate positions):

- A0' [HIGH — flipped+VERIFIED] The investigator clutter is GROUNDED in the substrate's real (synthetic) v0.2 population emission, NOT local-synth — verified emittable today, no precursor. ACCEPT (verified @f90bd39). If false (emission not as rich/deterministic as verified) → STOP at the T1 emit-checkpoint, fall back to the bounded curated set. Mitigation: T1 runs the emit + inspects output BEFORE building against it; substrate HEAD pinned inline; output is deterministic (run_id=seed{S}-n{C}-m{M}, no clock). revisit-status: held (the substrate emit @f90bd39 produced the rich v0.2 PartyView clutter at population scale exactly as verified; 200-case deterministic slice vendored, no precursor phase needed; T1 emit-checkpoint passed)
- A1' [HIGH] The 3 emission gaps are handled in signal-watch WITHOUT a substrate phase: (1) PII name/DOB omitted-by-design → a clearly-SYNTHETIC display identity laid over the real grounded KYC; (2) no ownership/beneficial-owner graph (substrate recorded gap) → render the REAL emitted txn counterparty edges only [user choice], ownership graph = substrate follow-on; (3) single-account dossier → cross-account display aggregation over subject.account_ids. ACCEPT. If false (a gap is load-bearing for the clutter beat) → surface; the network/ownership richness becomes a substrate follow-on. Mitigation: the risk-relevant profile is REAL; only display identity + the formal ownership network are synthetic/deferred, under the always-on badge. revisit-status: held (all 3 gaps handled in signal-watch: synthetic display identity over real KYC, real txn-counterparty edges only [ownership graph deferred to a substrate follow-on], cross-account aggregation over subject.account_ids — no substrate phase required)
- A2' [MED] Demo population = a BOUNDED deterministic vendored SLICE (~100-300 cases) [user choice], NOT the full run; the 4 named exemplars are tagged WITHIN it; the coverage statistic is computed over the slice; expandable later without rework. ACCEPT. revisit-status: held (bounded slice = 200 cases vendored deterministically; the 4 exemplars tagged within it [mule/fp_trap/thin/ambiguous]; coverage computed over the slice)
- A3' [MED] Honesty split (the Phase-62 pattern): the coverage statistic ("N of M cases ground end-to-end") is REAL/measured over the vendored slice; the per-case precedent-confidence badge is anchored to REAL firing frequency (the 4,966-firing probe-history) but DISPOSITIONS stay label-blind ILLUSTRATIVE (never ground truth); the always-on "Illustrative data & outputs" badge stays; checked at the delivery gate. ACCEPT. revisit-status: held (the honesty split held: coverage 57/200 MEASURED [casework's stub re-derives each signal]; precedent-confidence anchored to real combo firing frequency; dispositions label-blind illustrative "chosen, not measured"; badge always-on — the user checks the framing at the delivery gate)

## Phase 64 — Precedent-confidence gating engine + the elicitation loop (live) (2026-06-21)

> Direction = the user picked the precedent-confidence GATING engine (Phase-63 follow-on #3, "the LFCM elicitation-loop path") at the Step-9 direction question over agentic tool-calling (local) and the sibling-rooted C3/C15 alignment. Recommendation basis: the LFCM critical path, the measuring→controlling pivot (Phase 63 DISPLAYED the confidence + the 129/52/19 funnel; P64 makes the routing a LIVE control), and honestly buildable (the static `_confidence` is PURELY sample-size → confidence is cleanly §12-grounded, the disposition separable + illustrative). Grounded against the committed Phase-63 workbench (aml-substrate@f90bd39 / aml-casework@c6d8401 slice; serve_workbench + curate_workbench_cases + workbench.html read firsthand this session).

all_accept: true (no reject/don't-know; warned + restated per gate protocol — an all-accept on a NEW mechanism direction, the smell the gate exists to surface; flagged to the user in-conversation. How each accepted assumption shapes the approach: A0 [the weakest] makes the live elicitation loop the TARGET with a BUILD-TIME honesty checkpoint — the loop shifts ROUTING via real sample-growth (§12-grounded firing frequency) while the recorded/auto-applied DISPOSITION stays labeled illustrative + no learns-correct claim; if it can't be kept honest at build → scope to display-only batch routing, report don't force (the named fallback). A1 keeps it COMPANION-ONLY — the loop is inherently live-stateful so it's a serve_workbench extension, NOT a 9th ship target (Phase-49 new-ship→standard does not fire); --check all stays 8/8. A2 recomputes the loop from data already in cases.json (n_precedent stored) → records byte-frozen, no substrate re-emit, no sibling import. A3 holds the Phase-62 §12/§14 boundary — route on §12 confidence, dispositions §14 illustrative, NO §14 re-grounding from probe-history. A4 makes the key activity EXECUTED ONCE live over the 200-case slice — the funnel re-derived + the loop shifting one decision — not just designed [the measuring→controlling pivot].)

## Phase 65 — Agentic tool-calling: the investigator evidence-gathering loop (companion) (2026-06-21)

> Direction = the user picked AGENTIC TOOL-CALLING (the one remaining signal-watch-LOCAL Phase-63 follow-on) at the Step-9 direction question, over (B) the sibling-rooted C3/C15 alignment [highest-value but un-drivable from this repo — it lives in aml-substrate/aml-casework] and (C) a consolidation/pitch-polish phase. The Phase-64 gating engine + elicitation loop is DELIVERED + committed (0ee4489). Recommendation basis: the LFCM path (a new grounding modality — tool-evidence extends the grounding chain), the one frontier signal-watch can actually drive locally, and honestly buildable as a companion (the grounding gate is REUSABLE — code-verified). Feasibility code-verified firsthand this session via an Explore scout: NO multi-step agent-loop primitive exists (every companion is single-shot call_llm — this phase authors the first; BUILD-NEW, the hard part); news_ground.locate_span/ground_record are pure stdlib already shared build↔live (REUSABLE — tool-evidence grounds identically: claim ⊂ the tool's returned text); liveGraphLayout is pure JS (REUSABLE) but news_store [DuckDB] is companion-doctrine-bound [reference, don't import]; the plug-in point is clear [SIGNALS → DECIDE in serve_workbench.py + workbench.html]; NO synthetic OSINT corpus exists [the phase authors one — the book.json/scenarios.json committed-synthetic pattern]. Grounded against the committed Phase-64 workbench (signal-watch HEAD 0ee4489; aml-substrate@9d2e65c / aml-casework@81df91c HEADs verified live but NOT consumed — companion-only, no sibling import).

all_accept: true (no reject/don't-know; warned + restated per gate protocol — an all-accept, BUT not a silent one: the direction was chosen at Step 9 against two named alternatives, and the two contestable assumptions [A0 the honesty seam, A2/A4 how-live] were each put to an explicit position [A0 → "accept synthetic tools, grounded-or-stripped"; A2/A4 → "full live loop + execute-once"] while A1/A3 ride established precedent. How each accepted assumption shapes the approach: A0 [the weakest — the honesty seam] makes tool-evidence GROUNDED-OR-STRIPPED against the tool's OWN returned text via the reused news_ground gate — the agent proposes, the deterministic gate disposes, ungrounded findings drop; the consistency-not-correctness limit is bounded by the corpus being COMMITTED synthetic [not model-generated, so the citation-hallucination bypass the scout flagged is low-risk] and is surfaced honestly [badge on, ZERO catch-rate/detection-lift number]; if it can't be kept honest [reads as real OSINT / claims un-groundable to a synthetic source] → fall back to network-ER over the EXISTING committed counterparty edges only [no OSINT lookup, trivially grounded], report don't force [the named A0 fallback]. A1 keeps it COMPANION-ONLY — a serve_workbench.py + workbench.html extension, NOT a 9th ship target [the chain.html/Phase-56 + Phase-64 precedent; Phase-49 new-ship→standard does NOT fire]; --check all stays 8/8 ZERO dist drift, build.py never imports the siblings, the agent runs server-side [browser sends a backend NAME only — Phase-57 §4.5]. A2 makes the agent loop BUILD-NEW but BOUNDED — a fixed 2-3 deterministic tool set over the synthetic corpus, capped iterations [the Phase-47 D3 max-iteration mandate], a deterministic-STUB fallback so the demo runs model-free regardless. A3 makes the tool corpus COMMITTED SYNTHETIC [registry/adverse-media/counterparty hits indexed by entity name], shape-validated at the build boundary [the validate_news_data pattern], NO real-web fetch in the demo path. A4 reuses liveGraphLayout [pure JS] for the evidence network [NOT the DuckDB news_store — companion-doctrine; session-only, persists nothing] AND EXECUTES the loop ONCE live over a marquee exemplar — grounded + dropped-ungrounded evidence captured in the delivery report [the measuring→controlling / execute-once pattern].)
>
> Abort: if the live tool-evidence can't be kept honest over synthetic data [reads as "real OSINT" / a claim can't be grounded to its synthetic source] → fall back to network-ER over the existing committed edges only, report don't force (the A0 fallback). The live agent loop needs real debugging beyond creds → surface as a FINDING, ship the deterministic stub (the demo stays robust). Any new ship target / dist drift / a sibling import in build.py / a validator loosened to force a fit → STOP-and-surface.

- A0 [HIGH — T0 weakest, the honesty seam] Tool-evidence stays HONEST: the agent calls deterministic tools over a COMMITTED SYNTHETIC universe; every proposed finding is GROUNDED-OR-STRIPPED by the reused news_ground gate [claim ⊂ the tool's returned text]; the consistency-not-correctness limit is bounded [committed corpus, not model-generated] + surfaced honestly [badge, ZERO catch-rate number]. ACCEPT (synthetic tools, grounded-or-stripped — the full agentic-tooling demo). If false → fall back to network-ER over the EXISTING committed counterparty edges only [no OSINT lookup], report don't force. revisit-status: HELD (the full synthetic-tools-grounded-or-stripped seam shipped; the gate held LIVE over a local Qwen3.6-35B — 0 fabricated; a post-build adversarial code review found a fragment-entity bypass [a 1-char/partial name riding a grounded quote into the graph] + 2 latent gaps [tab/CR span-bridge, validator sweep], ALL fixed + locked with selftests; the fallback was NOT needed — consistency-not-correctness surfaced everywhere [synthetic-provenance line, authored-not-discovered network, subordinate illustrative synthesis, rejections shown with reason])
- A1 [HIGH] COMPANION-ONLY: a serve_workbench.py + workbench.html extension, NOT a 9th ship target [chain.html/Phase-56 + Phase-64 precedent; Phase-49 new-ship→standard does NOT fire]; --check all 8/8 ZERO dist drift; build.py never imports the siblings; agent server-side, browser sends a NAME only [Phase-57 §4.5]. ACCEPT (by precedent — mechanical). If false → STANDARD ceremony + a build target + the launcher cascade. revisit-status: held (companion-only HELD — serve_workbench + workbench.html extension, NOT a 9th ship target; 8 dists byte-frozen, --check all 8/8 zero drift; build.py imports neither osint_tools nor serve_workbench nor any sibling; the agent runs server-side, browser sends a NAME only — the §4.5 cred-leak selftest over ALL gather NDJSON stages passed)
- A2 [MED] The agent loop is BUILD-NEW [none exists — verified] but BOUNDED: a fixed 2-3 deterministic tool set, capped iterations [Phase-47 D3 max-iter mandate], a deterministic-STUB fallback [the demo runs model-free]. ACCEPT (full live loop). If unreliable beyond creds → surface as a finding, ship the stub. revisit-status: held (the BUILD-NEW hand-rolled loop shipped, BOUNDED — fixed 3-tool set, max_iters cap + a no-progress guard, deterministic-STUB fallback; the LIVE loop was RELIABLE beyond creds, not unreliable — it fired the registry→sanctions chain reproducibly [a first live run that didn't chain surfaced a real fix: the planner couldn't SEE the discovered entity from {tool,n_records} history → threaded discovered-entity context → chain fired]; the stub remains the model-free demo path)
- A3 [MED] The tool corpus is COMMITTED SYNTHETIC [registry/adverse-media/counterparty by entity name; the book.json/scenarios.json pattern], shape-validated at the build boundary [the validate_news_data pattern]; NO real-web fetch in the demo path. ACCEPT (by precedent). If false → re-scope. revisit-status: held (committed synthetic `data/osint/corpus.json` with a head-of-file disclaimer; `validate_osint_corpus` enforces closed kinds + globally-unique ids + non-empty text + the disclaimer + a banned-metric-token sweep [post-build-hardened to also sweep entity/linked_entities/outlet/jurisdiction, since news_normalize strips '%']; no real-web fetch; run fail-loud at companion load + selftest, NEVER in build.py [data/osint is no ship input])
- A4 [MED] Reuse liveGraphLayout [pure JS] for the evidence network [NOT the DuckDB news_store — companion-doctrine; session-only, persists nothing]; EXECUTE the loop ONCE live over a marquee case [grounded + dropped evidence captured as delivery evidence — the measuring→controlling / execute-once pattern]. ACCEPT (execute-once). If false → drop the graph / drop the live run. revisit-status: held (reused `liveGraphLayout` [pure JS, ported verbatim], NOT the DuckDB `news_store`; the loop is session-only / persists-nothing [selftest asserts cases.json + corpus.json byte-identical after a gather]; EXECUTED LIVE once over the mule — the registry→sanctions chain fired vs a local Qwen3.6-35B, 2 grounded/0 dropped/0 fabricated, transcript saved; the graph renders the grounded edges)

## Phase 66 — Workbench richness: richer slice + OSINT corpus depth + the substrate BO-graph emission handoff brief (2026-06-21)

> Direction = the user, after a two-repo richness recon (signal-watch consume side + aml-substrate generation side, code-verified @9d2e65c), chose to do the TWO signal-watch-LOCAL richness wins now AND bundle the aml-substrate beneficial-owner/ownership-graph EMISSION handoff brief into the SAME phase so the repos stay coherent. Recon findings that shape it: the substrate is single-signal-separable (composition architecturally subsumed by network linkage — P16) so MORE cases/typologies/detectors do NOT add detection difficulty, only visible volume; the richness that compounds is the NETWORK (the generated-but-unprojected RelationshipEdge BO/director graph — the recon's ranked #1 lever); the workbench slice is a deterministic substrate emit (`--clients 40000 --seed 0 …`, re-runnable as tool-use — VERIFIED this session, gen unchanged from the f90bd39 pin). Two design positions taken at the gate (AskUserQuestion): (slice) MODEST re-vendor ~320, OSINT depth primary; (coherence) YES — the OSINT ownership records MIRROR the substrate RelationshipEdge schema so the local win doubles as the BO-graph brief's rendering prototype.

all_accept: true (no reject/don't-know; warned + restated per gate protocol — A1 [coherence] + A2 [slice scope] each positioned explicitly via the gate questions, A0/A3/A4 ride established precedent. How each accepted assumption shapes the approach: A0 [T0 weakest — the GOVERNOR] makes every richness claim demo-VISIBLE [more cases/profiles/network] with ZERO catch-rate/detection-difficulty/lift number — the GATHER findings stay consistency-not-correctness and the substrate single-signal-separable finding is DOCUMENTED, not hidden; if a framing reads as "harder to detect / better catch" → re-word, never claim detection lift. A1 makes the deepened OSINT corpus's ownership records MIRROR the substrate RelationshipEdge schema [BENEFICIAL_OWNER/DIRECTOR_OF/ownership_pct] → the local win previews the emitted BO graph + the repos stay coherent at the data-contract level [the chosen position]. A2 makes the slice a DETERMINISTIC substrate re-emit + re-curate with wider caps → ~320 cases [more 4+-cap exemplars, wider combo spread], re-measured coverage, re-vendored + re-pinned [tool-use; build.py never imports the substrate; OSINT depth is the primary win, the slice secondary volume — the chosen position]. A3 makes the BO-graph emission a PLAN-BRIEF authored HERE [docs/], executed in aml-substrate [the Phase-55–58 sibling-brief pattern] — signal-watch defines the contract [RelationshipEdge → related_parties[] emission + a PartyGraphView + a non-tautological C14 BO-disclosure detector], NOT built here. A4 keeps it COMPANION-ONLY [workbench/osint are NOT ship targets; --check all 8/8 ZERO dist drift; build.py never imports the siblings; the honesty seam + always-on badge unchanged].)
>
> Abort: if any richness framing reads as a detection-difficulty/catch-rate claim → re-word (the A0 governor; the substrate is single-signal-separable). The substrate re-emit drifts from the f90bd39-pin population [gen changed] → re-pin honestly + accept the new deterministic slice [do NOT force the old slice]. The OSINT schema-mirror can't match the substrate RelationshipEdge cleanly → the brief documents the delta, the OSINT shape stays a valid synthetic stand-in. Any new ship target / dist drift / a sibling import in build.py / a validator loosened to force a fit → STOP-and-surface.

- A0 [HIGH — T0 weakest, the GOVERNOR] Every richness claim is demo-VISIBLE [more cases / deeper profiles / richer network], NEVER a detection-difficulty / catch-rate / lift claim; the GATHER findings stay consistency-not-correctness; the substrate single-signal-separable finding [composition subsumed by network linkage — P16] is DOCUMENTED, not hidden. ACCEPT. If false [a framing reads as "harder to detect / better catch"] → re-word, never claim detection lift. revisit-status: HELD (every richness framing stayed demo-VISIBLE — docs/case-workbench.md's *Workbench richness* section + the cases.json slice_rule both end "...not detection difficulty — single-signal-separable"; an adversarial governor sweep found ZERO catch-rate/%/lift in the gather UI or the corpus; the ONE adversary finding — a model-authored SYNTHESIS prose could carry a "%" beside the record-sourced edge — was CLOSED by sweeping the synthesis for banned tokens like a record field + a test; the governor held)
- A1 [HIGH — chosen at the gate] The deepened OSINT corpus's ownership records MIRROR the substrate RelationshipEdge schema [BENEFICIAL_OWNER/DIRECTOR_OF/ownership_pct] → the local win doubles as the BO-graph brief's rendering prototype + the repos stay coherent at the data-contract level. ACCEPT (mirror the substrate schema). If false [the substrate schema differs materially] → the brief documents the delta; the OSINT shape stays a valid synthetic stand-in. revisit-status: HELD (registry records carry `relationships:[{src,dst,label∈RelationshipLabel,ownership_pct}]` mirroring schema/graph.py:16-24 — verified live; CRUCIALLY the gate READS label/pct/DIRECTION from the record [record-sourced], NOT the model: the live execute-once caught Qwen fabricating an ownership pct [51 vs 75] + flipping direction → fixed; the BO-graph brief's consume-side mapping shows the emitted shape maps 1:1 onto what GATHER already renders)
- A2 [MED — chosen at the gate] The slice is a DETERMINISTIC substrate re-emit [tool-use, `--clients … --seed 0`, re-runnable — verified] + re-curate with wider caps → ~320-case slice [more 4+-cap exemplars, wider combo spread], re-measured coverage, re-vendored + re-pinned; OSINT depth is the PRIMARY win, the slice secondary volume. ACCEPT (modest ~320). If the re-emit drifts from the pin's population → re-pin honestly + accept the new deterministic slice. revisit-status: held (the re-emit was deterministic + reproduced the f90bd39-gen population — `git -C aml-substrate diff f90bd39..HEAD -- src/` is EMPTY, so the pin stays honest [no drift, no re-pin needed]; a combo-coverage pass [curate] pushed the slice to 294 cases / ALL 23 population combos surfaced [was 14] / 85 4+-cap exemplars / coverage re-measured 99/294 / funnel 189/66/39; OSINT depth was the primary win as planned)
- A3 [MED] The BO-graph emission is a PLAN-BRIEF authored HERE [docs/], executed in aml-substrate [the Phase-55–58 sibling-brief pattern]; signal-watch defines the contract [RelationshipEdge → related_parties[] bundle emission + a PartyGraphView projection + a non-tautological C14 BO-disclosure detector], NOT built here. ACCEPT. revisit-status: held (docs/substrate-bo-graph-emission-PLAN-BRIEF.md authored — the 3-part ask [PartyGraphView · related_parties[] bundle emission v0.2→v0.3 · the non-tautological C14] + the contract mapping + the governor; pinned @9d2e65c; sibling-executed, NOT built here)
- A4 [MED] COMPANION-ONLY: workbench.html / data/osint / data/workbench are NOT ship targets [build.py never imports the siblings or osint_tools; the 8 dists byte-frozen, --check all 8/8 ZERO dist drift]; the honesty seam [consistency-not-correctness, the synthetic-provenance line, the banned-token sweep, the always-on badge] is UNCHANGED. ACCEPT (by precedent). If false → STOP-and-surface. revisit-status: held (companion-only HELD — data/workbench re-vendored + data/osint deepened are companion data, NOT ship inputs; build.py imports no sibling/osint_tools/serve_workbench [grep clean]; 8 ship dists byte-frozen, --check all 8/8 ZERO drift; the honesty seam was EXTENDED not changed [the synthesis banned-token sweep + record-sourced ownership added; consistency-not-correctness + the synthetic-provenance line + the badge held]. NOTE: the abbreviation-period fix RELAXED the sentence-bridge guard to terminator+ws+CAPITAL — adversary-verified NO committed record is exploitable, documented as a latent defense-in-depth limitation [a future lowercase-led 2-sentence record could stitch])

## Phase 67 — Make the LIVE investigator workbench shippable from a bare clone (vendor aml-casework + make setup) (2026-06-22)
Direction gate accepted 2026-06-22 (all_accept:true; A0 + A2 positioned at the gate, A1/A3/A4 by precedent). The workbench's LIVE DECIDE finale depended on the sibling `../aml-casework` (no remote, local-only) — un-shippable from a clone. VENDOR a copy of casework into signal-watch + a `make setup`; the companion still subprocesses it (DISTRIBUTION, not import-coupling).
- A0 [HIGH — T0 weakest] `uv sync` from the vendored casework pyproject/uv.lock builds a WORKING venv on a fresh device AND the DECIDE finale then runs end-to-end with the stub drafter OFFLINE (no model, no ../aml-casework). ACCEPT — verify-first: T1 PROVES it in an isolated /tmp clone (no siblings) before T2/T3. If false [hidden casework coupling: a local-path dep / editable sibling install / an OS lib / a second-sibling subprocess at the SAR-consume path] → STOP-and-surface, escalate to a pre-built wheel (or Docker). revisit-status: HELD/PROVEN (verified in a no-siblings /tmp isolation clone: `make setup` built vendor/aml-casework/.venv via uv sync [no sibling] AND `python -m aml_casework.ingest <signable bundle> --drafter stub` signed a SAR OFFLINE [signed:true, 0 blocking violations, no model]. The guard EARNED ITS KEEP — it surfaced a hidden coupling: casework's `corpus_grounding` verifier grounds against a pinned signal-watch corpus snapshot it expects under `fixtures/corpus/`; vendoring src alone left every SAR corpus-gated → vendored `fixtures/corpus/` too, then signs with NO env. No wheel escalation needed.)
- A1 [HIGH] Vendoring is a DISTRIBUTION choice, NOT import-coupling: the companion serve_workbench/serve_chain still invoke casework via the EXISTING subprocess/file-handoff; build.py NEVER imports casework; vendor/ is outside build.py's world; the 5 offline ship artifacts + 8 dists stay BYTE-FROZEN (`--check all` 8/8). ACCEPT. If false [a vendored import sneaks into build.py, or a dist drifts] → STOP-and-surface. revisit-status: HELD (build.py imports no casework [grep clean — `aml_casework`/`vendor/` absent]; serve_workbench/serve_chain still subprocess the vendored CLI over the file-handoff; `vendor/` is outside build.py's world; `--check all` 8/8 ZERO dist drift, the 5 offline ship artifacts byte-frozen. Distribution, not coupling — held.)
- A2 [MED — chosen at the gate] Mechanism = a plain COPY of casework's runtime (`src/aml_casework` + pyproject + uv.lock + README/license; NOT tests/.venv/.dev-wiki) into `vendor/aml-casework/` via `scripts/vendor_casework.sh` + a `VENDORED_AT` pin (casework commit + date) — NOT git-subtree, NOT a wheel (the wheel is the A0 fallback only). ACCEPT (copy + refresh + pin). If the copy can't build standalone → the A0 wheel fallback. revisit-status: HELD (`scripts/vendor_casework.sh` rsyncs src/aml_casework + pyproject + uv.lock + README + [the A0-required] fixtures/corpus, with __pycache__/.venv/.dev-wiki excludes; `VENDORED_AT` pins casework@81df91c + date; `make vendor-refresh` re-syncs. Plain copy — no subtree, no wheel needed [the copy built standalone, so the wheel fallback was not taken].)
- A3 [MED] The live/neural path may REQUIRE Python ≥3.11 + uv (casework's floor; signal-watch's is 3.10) — acceptable: the 5 OFFLINE ship artifacts stay zero-dep (browser only) and the stub workbench stays stdlib-3.10; "shippable live" = clone + make setup, NOT open-a-file. ACCEPT (by precedent). revisit-status: held (`make setup` = uv sync in vendor/aml-casework [needs Python ≥3.11 + uv + network ONCE to fetch casework's deps; offline thereafter]; the 5 OFFLINE ship artifacts stay zero-dep [browser only], the stub workbench stdlib-3.10 — confirmed in the isolation clone. The README/CLAUDE/docs state the 3.11+uv live-tier prereq.)
- A4 [MED] Vendoring casework's source is COMPLIANCE-CLEAN (casework is synthetic-only — no real customer/transaction data; both repos are the user's) and the live-mode non-negotiable holds (optional, off by default; the stub-drafter scripted fallback; GATED if vendor/ absent). ACCEPT (by precedent). If false [real data in the vendored source] → STOP-and-surface. revisit-status: held (the vendored content is casework's CODE [15 .py modules] + signal-watch's OWN public-domain FinCEN-alert corpus snapshot [fixtures/corpus/, 5 derived json] — synthetic/public, no real customer/transaction data; the venv is gitignored [built by make setup], the src committed; live mode optional/off-by-default with the stub fallback; GATED if vendor/ absent. The stub DECIDE made NO network call [isolation clone, all model env unset, signed offline].)
