# Active Phase Context

Phase: 26 - Elevate the corpus demo to showcase quality — register re-translation + progressive render + build/lift wow beats + grouping/sort + landing page (M7) — PLANNED 2026-06-07; direction approved; WORKFLOW-DRIVEN; large (6 tasks, 2 L) — likely spans sessions.

Objective: Phase 25 shipped the corpus's two-layer model but the output is still weak — the `red_flag` translations read like prose, not the showcase's crisp typology-named AML INDICATORS (I never anchored to `config/typologies/fentanyl.json`); the article screen renders STATIC (no progressive "agent reading" reveal); the Signal screen is static (no build-log / combination-lift WOW); docs aren't grouped (want: by SOURCE, newest-first) and red flags aren't grouped; no LANDING page. Phase 26 elevates all of it to showcase quality, all-in, workflow-driven.

THE REGISTER (the bar): short typology-named noun phrases that name the laundering MECHANISM (fentanyl.json style: "Rapid pass-through / flow-through account", "Inbound e-transfer fan-in from unrelated senders") — drop the "An older customer…" throat-clearing, use the AML lexicon (fan-in/flow-through/pass-through/structuring/layering/nominee/funnel/mule/front-company/MCC-mismatch). The verbatim `flag` stays beside it as grounded evidence (show-both holds).

Tasks (lite, riskiest-first):
- T1 (M) author the register rubric (from fentanyl/elder) + the WORKFLOW DESIGN stage (parallel beat-design proposals → briefs for progressive-render, build/lift wow, grouping/sort, landing) + re-translate EFE+2 exemplars as the register PROOF/checkpoint (reads-like-indicator + still grounds + --check-derived clean, else report).
- T2 (L) the RE-TRANSLATION WORKFLOW — pipeline over the remaining ~39 docs: translate to the register → adversarial verify (reads-like-indicator / grounded / distinct-bounded) → fix/loop. All 42 --check-derived clean.
- T3 (M) corpus.html: progressive article rendering (port the showcase `streamAdvisory` typing/reveal beat) + Select grouping (by source, newest-first) + red-flag grouping (per the brief — likely by section/sub-category).
- T4 (L) the build-log + combination-lift WOW beats ported into the corpus arc — build-log reads the REAL `build_logic`; the lift figures are a GENERIC illustrative template (NOT 42 fabricated per-doc findings) with a loud "illustrative · pending calibration" tag under the always-on badge.
- T5 (M) the story-driven LANDING page (entry before Select; the showcase too if cheap).
- T6 (S) rebuild + drift guard + extend the harness (progressive render, grouping/sort, wow beats, landing) + docs.

Scope (UNFREEZE): all 4 sources' `derived/*.json` (re-translate the `red_flag` field ONLY — verbatim `flag` + grounding UNTOUCHED), `corpus.html`, `scripts/build.py` (only if a beat needs it — wow numbers are a corpus.html template, NOT data), `dist/corpus/index.html`, `tests/**`, `CLAUDE.md`, `README.md`, + a register-rubric/design-brief note. FROZEN byte-untouched: `index.html` + `config/typologies/**` + the 3 typology dists (the showcase), every source MD, every `corpus-status.json`, `data/typology-map.json`, the authoring + grounding scripts (`derive_signals.py` grounding core — only red_flag VALUES change, not the gate), `scripts/{acquire,crawl,pdf_to_md}.py`.

Key constraints:
- REGISTER FIRST (the credibility miss): the red_flag must read like a showcase AML indicator (terse, mechanism-named), NOT prose. The verbatim `flag` stays the grounded authority shown BESIDE it (show-both); the SHAPE gate (present/non-empty/distinct/12–240) is unchanged; grounding is unchanged (only red_flag VALUES change). NO over-interpretation — the indicator names what the verbatim states, no added meaning.
- WOW-NUMBERS HONESTY (reverses Phase 18, user-approved): build-log animates the REAL build_logic; the combination-lift figures are a GENERIC illustrative template, loud "illustrative · pending calibration", under the always-on badge — NEVER presented as real, NEVER 42 hand-fabricated distinct findings. Nothing is presented as real.
- PER-DOC ARC + SYNTHESIS preserved; the showcase (index.html + config + 3 dists) byte-FROZEN; NO non-negotiable change (the badge + the verbatim US-federal-public-domain + FINTRAC-Crown-copyright bases stay).

Exit criteria: all 42 red_flags re-translated to the register (reads-like-indicator, --check-derived clean, verbatim+grounding untouched) · the Read-advisory screen progressively renders · Select grouped by source + newest-first + red flags grouped · build-log + combination-lift wow beats ported (illustrative-badged, honest) · a story-driven landing page · `--check all` zero drift · the harness extended + green · the showcase + source mds + corpus-status + typology-map byte-frozen · CLAUDE + README updated · NO non-negotiable change.

Abort/degrade: if the register can't be hit while staying grounded/distinct (T1 checkpoint) → report before T2. If a wow beat needs a fabricated per-doc finding to land → CUT it to the generic illustrative template (never fabricate). If 8+ tasks / two L's overrun a session → checkpoint + resume. Blocked >3 attempts → ask the user: skip or abort.

Gates:
- [x] Direction confirmed by user (all-in elevation, workflow-driven, illustrative-badged wow numbers; the register rubric + the honesty safeguard signed off; 2026-06-07)
- [ ] Delivery accepted (post-implementation report)
