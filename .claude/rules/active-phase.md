# Active Phase Context

**Phase 75 — *Consume the substrate v0.5 entity-resolution emission: the spine's real-data memory lever*** (signal-watch-local, STANDARD — user override) — DELIVERED 2026-06-25; READY FOR COMPLETION (T1–T6 all [x]). **No active signal-watch-local phase now — run `/dev-plan` for the next.**

## Status
**COMPLETE — T1–T6 ALL [x] (STANDARD).** The orchestrator flips the delivery gate after the commit verifies. CONSUMED aml-substrate's just-shipped ADDITIVE emission — named-identity **v0.4** (`display_name`/`counterparty_name`) + entity-resolution **v0.5** (party-level `identifiers[]`, `RelationshipEdge.strength`, `resolution_edges[]`) — into the companion-only path (`curate_workbench_cases`→`entity_spine`→`workbench`), grounding the Phase-74 memory short-circuit in REAL substrate data. **THE T1 MEASURE-FIRST GATE WAS THE KEYSTONE — it caught a MECHANISM error, not the planned zero-overlap down-scope.** The zero-branch did NOT fire (229 entity_refs re-surface cross-case in the probe), but the gate caught that the PLANNED strong-merge of substrate `identifiers[]` is WRONG: substrate's shared strong ids are a DELIBERATE collision noise floor + controller-cluster SHARES between DISTINCT entities, and its `resolution_edges` over-merge distinct people. The reliable same-entity key is `entity_ref`. **User's CHECKPOINT pick "Entity_ref memory + SHARES adjudication"** (A3 REVISED — bit in the strong-merge sense): key cross-case memory on `entity_ref`; demote substrate email/phone to weak candidate-SHARES; ADJUDICATE resolution_edges (refuse to merge distinct entity_refs — the over-merge traps become a demo beat; on-ramps the deferred Class-J merge-adjudication console). NEW `scripts/measure_xcase_overlap.py`. Re-curated a **376-case v0.5 slice** + a disclosed cross-case CO-REFERENCE selection pass (0→**36 co-references**); committed slice **66 over-merge-refused**. **THE A1 FILE-BAR GUARD HELD** — `evidence_requirements.py` BYTE-UNCHANGED; inject-a-prior-cleared → byte-identical verdict. A `/memory` route + the "Persistent entity intelligence" panel (live-verified). The 3 sibling briefs re-grounded to live HEADs (substrate fc98b09, casework 4a858e6); the casework re-vendor SKIPPED (no-op). STANDARD adversarial review (8 agents): 0 must-fix · 2 should-fix + 1 nit ALL FIXED · 1 refuted; honesty/A1/boundary all PRAISE. Companion-only; build.py imports nothing new; `--check all` 8/8 byte-frozen; `node tests/workbench.test.mjs` 151→159; `node tests/news-stream.test.mjs` 150 + `news_live_test` PASS (M8 not regressed); `uv run pytest` 22.

## Phase 76+ candidates
Render the over-merge-refused EXAMPLES as a SHARES network on the `/memory` panel (the adjudication is a number now; the network view makes the "refuses to over-merge" beat visceral) · the merge-adjudication Class-J console (the natural next consume — substrate emits the candidate SHARES, the over-merge gap is the unbuilt console named in the standards) · execute the still-open sibling briefs (substrate exogenous-disposition-label · C1/C7/TF · casework `cleared`/graded-resolution — sibling-rooted) · converge `news_store` onto the shared spine core (the deferred Phase-74 A1 question) · probabilistic/Splink ER + graph/Kuzu + medallion/DuckLake (named-deferred).

## Gates
- [x] spec — generated this phase (STANDARD ceremony; `specs/phase-75-consume-substrate-v05-er-emission.md`)
- [x] Direction confirmed by user (2026-06-25; FULL per-planned + measure-first ACCEPTED; all-accept with the A1 measurement precondition — not silent; ledger Phase-75)
- [x] Delivery accepted (post-implementation report 2026-06-25; user "Accept"; committed daef922)

Plan [[phases/phase-75-consume-substrate-v05-er-emission]]; ledger Phase-75.
