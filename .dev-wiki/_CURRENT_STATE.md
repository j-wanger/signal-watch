# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-10 by /dev-debrief (Phase 45 — Corpus demo presentation polish — DELIVERED + accepted + committed 324734e + gate-flip 7e9fa23 + pushed to main; presents to bank stakeholders 2026-06-11. Full regate GREEN: --check all 5/5 [the frozen dist/corpus baseline MOVED by design] · corpus 239→273 · news-stream 150 · all selftests; reviewer 9/10 ACCEPT zero HIGH; A1–A5 ALL held. POST-PHASE one-off: booth.html draw-in loop [USER OVERRIDE, journal 2026-06-10-booth-draw-in-loop] committed same day.)

## Recommended Next Action

**Present 2026-06-11.** Phase 45 is DELIVERED + committed + pushed (324734e/7e9fa23). Pre-present sequence on the presentation machine: `python3 scripts/build.py --check all` (5/5) → `node tests/corpus-explorer.test.mjs` + `node tests/news-stream.test.mjs` → walk tests/smoke-checklist.md "Presenter notes + demo path" (recommended route fin-2024-a002 → Typologies → Capabilities; deselect stagecraft at the gate; avoid fin-2021-a004 / fin-2023-alert003; warm the Google-Fonts cache). Booth monitor: open booth.html directly (standalone one-off, 32.8s loop, FINTRAC opioid OA paraphrased + attributed — journal 2026-06-10-booth-draw-in-loop; if reused beyond the event, promote to a phase: build target + timeline harness). After the presentation: run /dev-plan for Phase 46 against the DEFERRED candidates below + audience feedback.

**DEFERRED candidates (offered, not chosen — for a future /dev-plan; their open sub-questions travel with them):** the derivable FINTRAC /intel/ frontier (OA001 + sanctions-evasion SB + Russia-linked-ML SB + dual-use advisory; open: heading-form coverage under the existing rf_region anchor set vs the deferred source-scoped refactor; same-source depth vs a new CORPUS_SOURCES entry; doc-level vs per-indicator typology mapping) · a third jurisdiction (AUSTRAC CC BY / UK OGL; open: a THIRD verbatim licence basis needs the user's compliance call; which regulator has an enumerated-indicator derivation surface) · fuzzy-merge adjudication for entity anchors (open: JS fuzzy-scorer port-vs-move; REVERSIBLE merge-edges never destructive re-anchor [Phase-41 D2/D3]; identifier-layered matching via properties[]; whether same-name-different-person anchor SPLIT travels with merge) · negative-news bulk scan (open: wall-time budget [2,313 articles × ~60s ≈ 38h → verify posture / subset selection]; the single-flight 409 means a batch driver serializes or calls extract() directly; deliverable shape undefined; commercial captures LOCAL-ONLY) · Phase-40 residue: a batched semantic instance-dup MERGE pass · a denial-quote prompt micro-rule · sectioned extraction IF positional decay re-measures on >30K-char material · a structural pre-H1 head-matter strip in news_fetch · a recorded-fixture for the second-pass verify + replay-corpus growth. [The committed registry-scoring harness + verify-latency optimization + living-doc hygiene candidates were ABSORBED into Phase 44 (T1/T4/T6).] Phase-44 residuals (journal soft obs): alias-ownership verify extension (P1c, measured-not-gated) · alias RECALL attribution · slot-parallel per-entity verify · smaller-model eval via the harness --check · nested-parents unique-MAXIMAL-parent fold rule · CLAUDE.md trim (245 vs ~200; own-commit precedent). Phase-45 residuals (tasks.md T5 dispositions + journal soft obs): byte-surgical mojibake repair of the fincen-alerts derived records (~2.2K occurrences in authored coverage fields — the permanent fix; the fixEncoding display repair becomes the safety net) · FINTRAC month-only date display · publications/documents naming unification · interactive second gate · R3 composition graph (post-demo lift upgrade) · showcase Act-5 illustrative-lift true-up if the showcase is ever presented again (gate-accepted divergence, A2).

## Active Phase

**[[phase-45|Phase 45: Corpus demo presentation polish (pre-presentation day)]]** (status: DELIVERED — 6/6 tasks [x] same-session; delivery gate ACCEPTED 2026-06-10; committed 324734e + gate-flip 7e9fa23 + pushed to main; presents 2026-06-11). Phase 44 (Live extraction quality) DELIVERED + accepted + committed 0835bff + 5c8c014 + pushed to main 2026-06-10.

Entry criteria: MET (Phase 44 delivered + accepted + committed + pushed; 0 open prior tasks. Direction = presentation polish driven by two independent review agents [story coherence + inconsistency sweep — findings in articles/decisions/phase-45-corpus-presentation-polish.md]: three story HIGH live-risk fixes; lift-beat refocus to R2 real composition-search-space inventory counts [the fake 18→64→83 template + "pending calibration" tag DELETED — completes the Phase-18 honesty arc]; FINTRAC footer attribution extended to the two lens views; ranked copy-coherence pass; user walkthrough + FREEZE checkpoint; full regate. Assumption gate closed 2026-06-10, all_accept: false — A1 [HIGH] R2 honest inventory counts, no performance claim, R1 zero-numbers fallback copy-only (don't-know round 1 → defended with worked real-value examples → accepted) · A2 [HIGH] showcase Act-5 untouched, divergence accepted · A3 [MED] gate copy reframe + stagecraft, no interaction redesign · A4 [MED] extend the Phase-28 footer mechanism · A5 [MED] global polish + curated demo-path notes; ledger block in assumption-ledger.md; decision article articles/decisions/phase-45-corpus-presentation-polish.md.)
Exit criteria: the three story HIGH fixes in; NO fabricated lift number remains in corpus.html/dist/corpus + CLAUDE.md honesty paragraph rewritten in place; FINTRAC attribution fires on FINTRAC-bearing lens drills, empty on US-only; copy MEDIUMs done; user walkthrough run, feedback dispositioned (no open HIGH), presenter/demo-path notes in tests/smoke-checklist.md; dist/corpus rebuilt (frozen baseline moves) with --check all 5/5 + both node suites green; showcase + news artifacts byte-identical; the always-on badge stays; NO non-negotiable change; presentation-ready by 2026-06-11.

Progress: 100% — 6/6 tasks [x] same-session, DELIVERED (T1 live-risk fixes · T2 lift R2 refocus · T3 FINTRAC lens attribution · T4 ranked copy pass · T5 walkthrough/FREEZE [2 feedback items fixed, 2 refinement rounds] · T6 full regate). The planning knowledge gaps resolved at T5: demo route recommended (fin-2024-a002), feedback absorbed within the day.

## Active Phase Contract

Phase: 45 - Corpus demo presentation polish (pre-presentation day)
Tasks: 6 (see tasks.md) — T1 live-risk fixes (cap .brecrow stagger Math.min(k*90,1500); human-gate lead copy reframe "the agent has PROPOSED all N pre-committed — deselect to dispose"; zero-build-now lift empty state ports the renderSignal two-message split; "Backtest on population" → "Queue backtest on population") · T2 lift beat R2 refocus (delete the LIFT const + % bars/counters + .liftbar/.fill CSS + the .illus tag; render real composition-search-space counts from __CORPUS__ — covered indicators in the committed signal's typology across contributing regulators, honest single-regulator/small-N copy; promotion-gate sentence into the side panel; tests P26-5 lift block replaced in the SAME commit; CLAUDE.md honesty constraints rewritten IN PLACE) · T3 FINTRAC attribution on lens views (updateAttribution fires on capability/data-source drills with FINTRAC verbatim on screen; empty on US-only) · T4 ranked copy-coherence pass (MEDIUMs first: atom vocabulary seeded on landing, all 5 source families named, "5 regulators/sources" honest split, "Advisories"→"Documents" chrome, "gap → covered" pill, typoLabel title-case, FINTRAC pseudo reference codes display-only; LOWs time-permitting) · T5 user walkthrough + feedback intake (FREEZE checkpoint; dispositions ranked + time-boxed, overflow explicitly deferred; presenter/demo-path notes into smoke-checklist incl. recommended route, deselect stagecraft, E-23/Model-Validation second-gate narration, zero-build-now docs to avoid, Google-Fonts cache warm) · T6 full regate + docs (rebuild dist/corpus — the frozen baseline MOVES; --check all 5/5; both node suites; CLAUDE.md current-state true in place).
Transition: continue (lite). UNFREEZE: `corpus.html`, `dist/corpus/**` (rebuild — frozen baseline moves), `tests/corpus-explorer.test.mjs`, `tests/smoke-checklist.md`, CLAUDE.md, `.dev-wiki/*`, `.claude/rules/active-phase.md`.
FROZEN: the grounding core (`derive_signals.py`, `news_ground.py`), ALL derived data (source dirs + derived records + the 3 overlays), the build pipeline (`scripts/build.py`), the showcase `index.html` + config/** + the 3 typology dists BYTE-IDENTICAL (its Act-5 lift template STAYS — the cross-artifact divergence is accepted at the gate, A2), the ENTIRE news stream (news.html + dist/news + data/news/** + the live companion scripts) byte-identical. HONESTY: NO new fabricated/similarity/overlap/lift number (Phase-18/24 rejections + honesty-over-demo-drama stand); the R2 counts are honest INVENTORY arithmetic, the same class as the lenses; the always-on badge stays; NO non-negotiable change.
Abort: blocked >3 attempts on a task → mark [blocked: …] + ask the user: skip or abort (one-day horizon — ask EARLY). R2 counts can't render honestly / would imply a performance claim → fall back to the R1 zero-numbers ladder, never invent a number. Showcase or news dist drift → STOP and surface it. Feedback exceeding the day → disposition ranked + time-boxed at T5, overflow explicitly deferred.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| D1 (Phase 45 DIRECTION) = pre-presentation polish of dist/corpus (presents to bank stakeholders 2026-06-11), driven by two independent review agents (story coherence + inconsistency sweep; findings in articles/decisions/phase-45-corpus-presentation-polish.md): three story HIGH live-risk fixes + lift refocus + lens attribution + ranked copy pass + walkthrough/FREEZE + full regate. | medium | 2026-06-10 |
| D2 (Phase 45 LIFT — A1 [HIGH], don't-know→defended w/ worked real-value examples→accepted) = R2 "real composition search space" — DELETE the fake 18→64→83 LIFT template + the "Illustrative · pending calibration" tag; render honest INVENTORY counts (covered indicators in the committed signal's typology × contributing regulators, client-side from __CORPUS__, the lenses' honesty class); NO performance claim; honest small-N/single-regulator degradation; R1 zero-numbers ladder = the copy-only fallback. Completes the Phase-18 honesty arc. | medium | 2026-06-10 |
| D3 (Phase 45 SHOWCASE — A2 [HIGH]) = showcase index.html Act-5 untouched (byte-frozen, non-negotiable-protected); the corpus-vs-showcase lift divergence is DELIBERATE and accepted at the gate. | high | 2026-06-10 |
| D4 (Phase 45 GATE+ATTRIBUTION — A3/A4 [MED]) = human gate fixed by COPY reframe ("the agent has PROPOSED all N — deselect to dispose") + presenter stagecraft, NO interaction redesign; FINTRAC attribution = EXTEND the Phase-28 relocated-footer mechanism to the Capabilities/Data-sources lens views (the user's compliance call, not suppression). | medium | 2026-06-10 |
| D5 (Phase 45 ROUTE+FREEZE — A5 [MED]) = GLOBAL polish + curated demo-path notes in tests/smoke-checklist.md (user hasn't named demo docs — route recommendation at T5); T5 = the user-walkthrough FREEZE checkpoint, feedback dispositioned ranked + time-boxed, overflow explicitly deferred. | medium | 2026-06-10 |
| D1 (Phase 44 DIRECTION, user REFRAME at the dev-plan gate) = live extraction quality — red-flag recall (high-risk-country wire misses) + alias-assignment precision + a fresh processing page + the user's harness hypothesis as the T1 frame; processing SPEED surfaced at the gate → resolved as quality-gated T4. Carried candidates deferred again (fuzzy-merge, bulk scan, FINTRAC /intel/, AUSTRAC/UK); hygiene BUNDLED as T6. | high | 2026-06-10 |
| D2 (Phase 44 MEASURE+CLASSIFY — A1/A2) = committed targeted harness (promoted from the Phase-40 scratch) + alias-OWNERSHIP scoring (currently unmeasured: the gate checks an alias is verbatim, never WHOSE it is) + synthetic wire notes embedding the user's sample sentences (local-only); BOTH failures REPRODUCED + CLASSIFIED before any fix; per-stage wall-time measured. | high | 2026-06-10 |
| D3 (Phase 44 SEAMS — A3, don't-know→defended→accept) = fixes through proven seams — the prompt-regression gate (red_flags FIRST, never-reduce, holdout eval) + the news_ground regate procedure (4 records + 13 goldens deterministic regeneration, NO re-capture); a structural EXTRACT_SCHEMA change = a surfaced FINDING, never silent. | high | 2026-06-10 |
| D4 (Phase 44 SPEED — A1 condition) = quality-gated optimization — T4 optimizes the T1-proven hotspot ONLY where harness quality scores hold (no reduction on any dimension); honest report if no optimization preserves quality. (Outcome: honest skip — generation is 92–98% of wall.) | high | 2026-06-10 |
| D5 (Phase 44 UI+HYGIENE — A4) = processing page LIVE-region-only (offline dist/news byte-identical); hygiene trim = lossless archival with pointers. PRIVACY carried (Phase-40 D3 / 41 A5 / 43 D5 lineage): samples + commercial material local-only; fixtures US-federal-only. | high | 2026-06-10 |

(Earlier decision rows — Phase 43 and back — trimmed to the ~10-row cap at the Phase-45 plan, 2026-06-10: the full record lives in `articles/decisions/*`, the phase journals, and the archived tasks.md phase headers. Load-bearing carry-overs already live in `.claude/rules/working-knowledge.md` + the non-negotiables.)

## Blockers and Open Questions

- [RESOLVED 2026-06-10 · planning, by the Phase-45 gate A1–A5] The five Phase-45 planning questions all closed at the assumption gate (all_accept: false — A1 don't-know round 1 → defended with worked real-value examples → accepted): LIFT SHAPE → R2 honest real-inventory counts (no performance claim, disclaimer removed, R1 zero-numbers copy-only fallback) [A1]; SHOWCASE CONSISTENCY → Act-5 byte-frozen, deliberate cross-artifact divergence accepted [A2]; INCONSISTENCIES INVENTORY → already measured by the two pre-gate review agents (findings ranked in articles/decisions/phase-45-corpus-presentation-polish.md; the gate fix is COPY + stagecraft, no interaction redesign [A3]); DELIVERY-REVIEW ARTIFACT → corpus.html copy edits + presenter/demo-path notes into tests/smoke-checklist.md [A4 attribution extension + A5]; FEEDBACK ABSORPTION → T5 user-walkthrough FREEZE checkpoint, dispositions ranked + time-boxed, overflow explicitly deferred [A5]. Decision article approved; ledger block in assumption-ledger.md.
- [RESOLVED 2026-06-10 · planning, by Phase-44 D1–D5] Phase-44 DIRECTION + the conditional IF-… sub-questions — the user REFRAMED off the offered candidates at the dev-plan gate (the 4th reframe in 5 gates): Phase 44 = LIVE EXTRACTION QUALITY — red-flag RECALL (obvious high-risk-country wire flags missed in limited real testing) + alias-assignment PRECISION (entities assigned aliases clearly not theirs) + a FRESH dedicated processing page + the user's own harness hypothesis accepted as the T1 frame + SPEED resolved as quality-gated T4. Measure-first/classification-first (D2: committed targeted harness w/ alias-OWNERSHIP scoring + synthetic wire notes embedding the user's sample sentences, local-only; both failures reproduced + classified before any fix); fixes through proven seams (D3: prompt-regression gate + the news_ground regate procedure; structural EXTRACT_SCHEMA change = a surfaced finding); quality-gated speed (D4); LIVE-region-only UI + lossless hygiene (D5). The IF-(hygiene) and IF-(verify-latency) sub-questions were ABSORBED into Phase 44 (T6/T4); the IF-(FINTRAC /intel/)/(third jurisdiction)/(fuzzy-merge)/(bulk scan) sub-questions travel with their candidates in the DEFERRED list above. Assumption gate closed 2026-06-10: A1 accept-with-conditions (sample sentences; speed → quality-gated T4), A2 accept, A3 don't-know→defended→accept, A4 accept; ledger block in assumption-ledger.md; decision article articles/decisions/phase-44-live-extraction-quality.md. KNOWLEDGE GAPS carried to impl: whether the user's wire-miss material reproduces (T1's job); the actual failure classes (T1); whether batched verify preserves quality (T4).
- [RESOLVED 2026-06-10 · planning, by Phase-43 D1–D5] Phase-43 DIRECTION + the conditional IF-(a)…(e) sub-questions — the user REFRAMED off all five carried candidates at the dev-plan gate (none picked; all stay in the DEFERRED list above, their open sub-questions traveling with them): Phase 43 = LIVE PIPELINE ROBUSTNESS + PROGRESSIVE PRESENTATION — a live test on a REAL investigation note timed out ~200s with NO intermediate feedback; extraction must work regardless of article size/complexity, and the page must show staged progress instead of an opaque wait. Measure-first (D2: the stress harness classifies read-timeout vs finish_reason=length truncation vs context overflow [/props n_ctx] vs intrinsic degradation; the ~200s failure class must be reproduced); rendering = stage-completion-driven, corpus-demo-style (D3 — A2' after a reject-by-reframe round 1: NO partial-JSON parsing, NO visible token stream; internal stream:true serves only the timeout fix + a token counter); scope = pure transport (D4: gate/prompt/schema/store-writes/fixtures FROZEN, call_llm signature preserved — the fixture seam); privacy (D5: the real note never committed, stress notes synthetic, commercial captures local-only, fixtures US-federal-only). Assumption gate closed 2026-06-10: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept; ledger row in assumption-ledger.md; decision article articles/decisions/phase-43-live-pipeline-robustness.md. KNOWLEDGE GAPS carried to impl: the server's actual n_ctx (measured at T1); whether stream:true composes with the json_schema strict grammar in the user's llama-cpp build (verified at T2); the actual failure class (T1's whole job).
- [RESOLVED 2026-06-10 · planning, by Phase-42 D1–D5] Phase-42 DIRECTION + the conditional IF-(a)…(f) sub-questions — the user picked (a) anchor dossier view + a per-scan network visualizer, with the (c) CLAUDE.md trim bundled as T6. The IF-(a) sub-question resolved IN the design: a surfaced CONFLICT = same anchor + same kind + different value (the both-kept rows per Phase-41 D2; presentation-only flag, never auto-resolve); the view = a `GET /anchor` companion route + a news.html LIVE-region panel (the watchlist-panel pattern, NOT a separate page), the per-scan graph as the navigation surface. The IF-(b)/(d)/(e)/(f) candidates stay in the DEFERRED list above (their open sub-questions — Python-side fuzzy scorer port-vs-move + reversible merge mechanics; rf_region anchor coverage for the /intel/ SBs; the third licence basis; bulk-scan wall-time/deliverable — travel with them). Assumption gate closed 2026-06-10: A1/A3 accept, A2 don't-know→A2' accept (evidence check: the 7 fixture articles are case-disjoint, zero cross-article entity overlap → a committed SYNTHETIC investigation-note seed replaces any re-scan-different-articles script), A4 accept-with-condition (SVG is the INITIAL implementation for demo testing), A5 scope constraint (pure consumption); ledger block in assumption-ledger.md; decision article articles/decisions/phase-42-anchor-dossier-network-view.md.
- [OPEN · planning, gated at Phase-38 T1 CHECKPOINT] Phase-38 QWEN-VS-CLAUDE DIVERGENCE ADJUDICATION — the Half-A evaluation reports a Qwen-vs-Claude INTER-RATER AGREEMENT + the actual divergences (which entities/flags one rater caught and the other missed) over the committed DOJ/OFAC articles. The Claude reference is a SECOND rater, NOT ground truth; the user adjudicates the divergences at the T1 checkpoint before the goldens lock. NO accuracy/precision/recall number is presented as real; the always-on badge stays. Resolved at the checkpoint (T1), not blocking the capture itself.
- [OPEN · planning, gated at Phase-37 T1] Phase-37 TYPOLOGY MODEL — single primary typology per indicator vs a multi-label SET vs an honest indicator-level "sector-general" bucket. The T0 risk: many sector-page indicators are genuinely CROSS-CUTTING (structuring serves drug-trafficking AND fraud AND TF), so forcing one typology may merely RELOCATE the catch-all to indicator level. The T1 measure-first probe (section→typology determinism + the cross-cutting fraction over the ~589 unique texts) decides this AT A CHECKPOINT with the user before any tagging; also open at the checkpoint: whether the 3 specific sector pages (casinos/real-estate/VC) fold into per-indicator tagging for consistency, and which closed-vocab EXTENSIONS (if any) are grounded. Do NOT tag before the checkpoint lands.
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- [CANDIDATE · future phase, raised 2026-06-08 by the user] **SearXNG-backed authoring search/fetch.** A self-hosted SearXNG meta-search + readability content-extraction capability for the BUILD-TIME authoring pipeline (corpus + news acquisition) — more robust than WebFetch/`curl`, which hit bot-protection on gov sites this phase (DOJ 403; we routed around it via the Wayback Machine). Already prototyped in the sibling `nanaclaw` project (Phase 5: `SEARXNG_URL` service + `web_search`/`web_extract` MCP tools); not currently running here (localhost:8888 refused). Authoring-only — NEVER in the ship artifact (offline non-negotiable). A future-phase candidate, NOT this phase.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)
- (Resolved pre-Phase-42 planning entries — ~20, each multi-KB — archived VERBATIM to [articles/state-archive-resolved-blockers.md](articles/state-archive-resolved-blockers.md) by the Phase-44 T6 hygiene trim; each is also in its decision article + journal.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus; Phase 24 added `jurisdiction` per source + `load_typology_map`/`validate_typology` (build-boundary typology gate) + merges typology/jurisdiction into `__CORPUS__`; Phase 25 added `_inline_article`/`_strip_provenance` (inlines each live doc's full source-md body into `__CORPUS__`) + a re-declared `red_flag` SHAPE boundary check in `validate_corpus_data` | 2026-06-07 |
| data/typology-map.json | Phase 24 NEW cross-corpus typology OVERLAY (22-term closed vocab + 42-entry doc-id→typology map; jurisdiction from the source registry, not stored). A SEPARATE committed file — the source dirs + derived records stay byte-frozen; validated fail-loud at the build boundary | 2026-06-07 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>`; Phase 25 added an ADDITIVE `red_flag` SHAPE check to `check_record` (grounding normalize/rf_region/flag⊂md BYTE-UNCHANGED) + 3 `--selftest` fixtures | 2026-06-07 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (2.15MB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips). Phase 24 Documents/Typologies toggle + synthesis view; Phase 25 the per-doc "Read advisory" screen + the `red_flag` label through the 6-screen arc. **Phase 27 (shippability)**: `cleanArticle()` markitdown-sanitizes the displayed source (form-feeds / running headers / tab-soup; display-only, source md untouched); `highlightArticle` rewritten to normalize-both-sides + index-map (634/634 = 100% highlight); the Signal build-log ports the Act-4 `.run` working-pulse in a proposal grid + a combination-lift lift-side panel (firestat OMITTED); the progressive read types the WHOLE article (no 1600 cap, length-scaled ~6s) | 2026-06-07 |
| news.html + dist/news/index.html | M8 adverse-media stream (third ship artifact). Arc: Select → Read → Screen (real Jaro-Winkler, 0.85) → Disposition (human gate; deterministic SVG subject network + anchor dossier in LIVE mode) → Exposure. OFFLINE ship = static book only; ALL live client code in `/*LIVE_START*/…/*LIVE_END*/`, build-stripped (dist/news byte-identical). Phase 43 added the stage-completion LIVE preview (grounded flags FINAL + provisional chips + token counter — never a token stream); Phase 44 moved it onto a DEDICATED PROCESSING PAGE (in-page takeover, presenter keys guarded, Esc arm→abandon) and made grounding WRAP-TOLERANT (locate_span requote) + folds ambiguity-refusing/type-matched. Harness `tests/news-stream.test.mjs` (150, both motion modes + live overrides + strip assertion). | 2026-06-10 |
| scripts/{serve_news,news_ground,news_store,news_fetch}.py | The LIVE news companion (dev/authoring-time only; offline ship unaffected): serve_news = localhost page + llama-cpp proxy + NDJSON stage-stream `/extract` (Phase 43: call_llm STREAMS SSE w/ idle-gap 120s timeout, MAX_GEN_TOKENS 16384, failures NAMED in-stream, preflight_size vs /props n_ctx FAIL-OPEN, single-flight 409, disconnect-persists-nothing) + `/watchlist[/prune]` + `/anchor`; news_ground = the SHARED grounded-or-stripped gate (the one allowed build→companion import; vocab single-authority); news_store = gitignored DuckDB ANCHOR store (.venv-only; build.py never imports it); news_fetch = URL fetch ladder + standardizer + article-shape verifier. Replay: 13 US-federal fixtures (`tests/fixtures/news-live/`, FIXTURE_META allowlist), goldens deterministic from pinned captures; `tests/news_live_test.py` (system + .venv + --live). | 2026-06-10 |
| data/news/ | REAL US-federal gov-enforcement articles (DOJ/OFAC, verbatim 17 USC §105) `articles/*.md` + grounded `derived/*.json` + SYNTHETIC `book.json` (exact + near-matches + a common-name trap; non-negotiable #4). Phase 42 added the committed SYNTHETIC demo seed `docs/demo-investigation-note.md`. | 2026-06-10 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored. Phase 25 added a natural `red_flag` per indicator. **Phase 27** tightened 121 over-long verbatim `flag`s to crisp contiguous SUB-SPANS (faithfulness-guarded; 36 long-but-faithful kept whole) across 33 derived records + re-translated fin-2022-a001's 2 red_flags — byte-surgical (flag/red_flag VALUES only); mds + every corpus-status.json + the grounding core byte-frozen | 2026-06-07 |
| tests/corpus-explorer.test.mjs + tests/news-stream.test.mjs | Zero-dep Node DOM-shim harnesses — corpus 239 assertions, news-stream 150 (Phase 44: +10 processing-page/key-guard). Per-phase growth history: the journal entries. |

## Session Journal (last 5)

- [2026-06-10] [[2026-06-10-booth-draw-in-loop|Booth draw-in loop — booth.html (post-phase one-off)]] (USER OVERRIDE — no open phase; user chose oneshot over a phase) — standalone 32.8s three-act looping animation for the booth monitor (read/extract → 12 atoms w/ REAL committed signal names → combinations re-wiring 3→3→4→5 atoms, coverage arc labeled illustrative). Content iterated synthetic → real latest FinCEN FIN-2026-A002 VERBATIM (US public-domain, byte-verified) → FINTRAC synthetic-opioids OA (Jan 2025; the domain topic, 68 indicators). COMPLIANCE CALL (named, reversible): FINTRAC verbatim licence is NON-COMMERCIAL-only and a booth is marketing → indicators PARAPHRASED + attributed (the fentanyl-showcase precedent; anti-verbatim 8-word-window check mechanical; chips byte-match the committed red_flag translations). Pacing 1.3× + a deliberate ~3s ACT-1 hold (user); halo ellipse FITTED to the atom bounding box (user: lineup). Verified: 2 full loop cycles simulated under a node vm shim. NOT a ship artifact — outside build.py/tests; promote to a phase if reused beyond the event.
- [2026-06-10] [[2026-06-10-phase-45-corpus-presentation-polish|Phase 45 Corpus demo presentation polish (pre-presentation day) (M7)]] (lite, 6 tasks, ALL [x] same-session → DELIVERED + accepted + committed 324734e/7e9fa23 + pushed; dist/corpus presents to bank stakeholders 2026-06-11) — T2 the headline: the fake 18→64→83 LIFT template + the "Illustrative · pending calibration" tag DELETED; the beat renders R2 REAL inventory counts (covered indicators in the committed signal's typology × contributing regulators, client-side from __CORPUS__, honest corpus-wide fallback; NO performance claim — disclaimer removed, nothing left to disclaim; completes the Phase-18 honesty arc; R1 zero-numbers fallback designed, never needed). T1 live-risk fixes (stagger cap · human-gate copy reframe "agent has PROPOSED all N — deselect to dispose" · zero-build-now dead-end · "Queue backtest"). T3 FINTRAC Crown-copyright attribution EXTENDED to the two QUOTING lens drills, MULTI-DOC (per reproduced work; re-derives per render; synthesis drill silent — Phase-28 precedent; the spec's adversarial pass surfaced the multi-doc requirement). T4 ranked copy pass. T5 walkthrough + FREEZE: feedback #1 "non-ASCII" = TWO classes (fincen-alerts AUTHORED-field mojibake ~2.2K + PUA bullets in 9 article mds) → LOAD-TIME DISPLAY-ONLY fixEncoding (records/md BYTE-FROZEN; byte-surgical repair DEFERRED); #2 landing hook reshaped onto "effective and regulatorily defensible" (2 rounds; "extensive human review" beat restored). T6 regate GREEN: --check all 5/5 (corpus baseline MOVED by design) · corpus 239→273 (+34 incl. the FULL-CONTENT mojibake sweep + R2 independent-recomputation counts — the anti-tautology pattern killed a real bare-vs-doc-qualified overlay-key bug) · news-stream 150 · all selftests. corpus.html display layer ONLY (no scripts/data/module changes). A1–A5 ALL held (A4 with the multi-doc upside).
- [2026-06-10] [[2026-06-10-phase-44-live-extraction-quality|Phase 44 Live extraction quality: targeted harness, classified fixes, processing page (live news) (M8)]] (lite, 7 tasks, ALL [x] same-session → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — T1 CLASSIFICATION VERDICT (the headline): the user's "missed high-risk-country wire flags" = GATE-DROP, NOT model recall (ground_record's raw-substring check failed on model quotes crossing hard line-wraps — 4/5 planted wire facts dropped on wrapped prose, the user's seeded STR sample 3/5; model recall 5/5 on single-line bullets; sibling paths: `*`-strip asymmetry + title-line quotes); alias misassignment = predominantly DETERMINISTIC fold misparent (rule (a) ORDER-DEPENDENT + TYPE-BLIND); residual P1c wrong-owner model alias = measured-not-gated. T2 news_ground.locate_span wrap-tolerant locator that REQUOTES flags/evidence to the body's exact bytes (raw substring BY CONSTRUCTION; the STR note 3 drops→0; 13/13 replay green WITHOUT golden regeneration). T3 fold AMBIGUITY-REFUSAL + TYPE-MATCH (order-independence pinned; Phase-41 upsides @monalisa7→Chirkinyan/Rossi→George Rossi unregressed; nested-parents recall cost known-honest). T1 HARNESS committed: tests/news_quality_harness.py + quality-baseline.json (17 fixtures; kept_flags/mech_families/entity_count/alias_suspects/type_blind_folds; --check non-zero on regression, --freeze = conscious re-baseline) — THE gate for future prompt/gate/model changes. T4 HONEST SKIP (hotspot = model generation 92–98% of wall on notes; levers named: slot-parallel verify + smaller-model eval). T5 in-page processing takeover #liveproc (presenter keys guarded; Esc arm→abandon via AbortController; 409/stream-error/abort NAMED; news-stream 140→150). T6 hygiene archival (tasks.md 755→61 lines, _CURRENT_STATE 111→89, _ARCHITECTURE 163→94; verbatim archives + pointers, reviewer-verified lossless; NEW convention: tasks.md active-phase-only). T7 regate GREEN (--check all 5/5 · corpus 239 · all selftests · news_live_test system+.venv+--live · ZERO re-capture). REVIEW GATE 9/10 ACCEPT zero HIGH+ (2 staleness MEDIUMs → this debrief; CLAUDE.md trim carried). Assumption revisit: A1–A4 ALL held.
- [2026-06-10] [[2026-06-10-phase-43-live-pipeline-robustness|Phase 43 Live pipeline robustness + progressive presentation (live news) (M8)]] (lite, 5 tasks, ALL [x] same-session → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — the user's reframe (a REAL-note live test timed out ~200s w/ no feedback) measured, fixed, made visible under the approved spec (specs/phase-43-live-pipeline-robustness.md). T1 stress harness (LOCAL ph43_stress.py, tiered synthetic notes w/ head/tail planted entities): primary failure = OUTPUT-BUDGET (≈400–450 tokens/entity; 4096 truncates at ~10+ entities; DISCARDED finish_reason → opaque parse error ~103s — the user's class), secondary READ-TIMEOUT (XL 191s/XXL 321s); overflow unreachable (n_ctx=262144); INTRINSIC DEGRADATION REFUTED. T2 call_llm STREAMS (SSE, idle-gap 120s, MAX_GEN_TOKENS 16384, length/stall NAMED in-stream; SAME signature → 13/13 replay zero re-capture; live XL probe 193s). T3 stage-completion LIVE rendering (grounded slim-record event, flags FINAL + provisional chips through verify, token counter; grounded reveal 18s live; NO token stream). T4 preflight_size vs /props n_ctx (honest refusal + --ctx-size remedy, FAIL-OPEN) + /extract single-flight 409 (slots=4 — ghost jobs split throughput) + disconnect-persists-NOTHING ('persisting' probe; store row-count 0 proven); re-run ALL 5 tiers PASS; sectioned extraction NOT earned. T5 regate GREEN (--check all 5/5, news-stream 130→140, corpus 239, all selftests, news_live_test system+.venv+--live) + docs/smoke/CLAUDE.md (225 lines). NO escape hatches; pure transport reviewer git-diff-verified. REVIEW GATE: 9/10 ACCEPT — 1 HIGH (extract_lock leak → try/finally) fixed inline + 3 MEDIUM fixed/resolved + stall-message suggestion adopted. Assumption revisit: A1/A2'/A3/A4 ALL held. RETRO (30 completed phases): hygiene debt + user-reframe pattern high-signal; rollbacks measured-by-design. SOFT OBS / Phase-44 candidates: batched verify (again the wall-time majority — Phase 39 A3 lineage) · graduate the stress harness to a committed perf harness · slots-divide-context docs note · living-doc hygiene (tasks.md 415KB chokes Read tooling — the strongest candidate).
- [2026-06-10] [[2026-06-10-phase-42-anchor-dossier-network-view|Phase 42 Anchor dossier view + per-scan network visualizer (live news) (M8)]] (lite, 6 tasks, ALL [x] → READY FOR COMPLETION — delivery gate handled by the delivery flow after commit) — PURE CONSUMPTION of the Phase-41 ER model under the approved spec (specs/phase-42-anchor-dossier-network-view.md). T1 GET /anchor read route (400/503/404/200; name-keyed — the merge-robust seam; anchor_route_test: 2 same-article scans → ONE anchor w/ per-scan alias provenance; persist-off 503). T2 deterministic SVG network visualizer at Disposition (liveGraphLayout: radial init + 24-iteration relaxation, self-edge skip, missing-endpoint synthesis; esc() everywhere; edge evidence CLOSED until clicked — spec-pinned, replacing the Phase-41 always-visible rows). T3 anchor dossier panel (scans w/ source-type provenance · properties by kind · conflicts "both kept" presentation-only · honest 404/store-off/empty · watchlist wdoss). T4 committed SYNTHETIC note docs/demo-investigation-note.md (TWO phone values = deterministic in-note conflict; the TGR article yields NO properties[]) — the SPEC CHECKPOINT ran LIVE (real Qwen: article 72.1s/14 entities + note 19.5s/4 → dossier 2 scans, client_number C-2024-0117, phone conflict both-kept, alias+edges accumulated — A2' confirmed). T5 full regate GREEN; T6 CLAUDE.md trim 319→220 (protected sections DIFF-CLEAN; two-commit plan at delivery). HEALTH: news-stream 103→130 (+27); +anchor_route_test; --check all 5/5 ZERO DRIFT; --live real-Qwen smoke RAN. NO escape hatches; gate/schema/store-writes/fixtures untouched (reviewer git-diff-verified). REVIEW GATE: 9/10 ACCEPT zero HIGH+ (3 MEDIUMs: 2 staleness → this debrief, 1 stale scope ref fixed inline). Assumption revisit: A1/A3/A5 held, A2 bit-then-held-as-A2', A4 held w/ condition. SOFT OBS / Phase-43 candidates: fuzzy-merge adjudication (properties[] makes identifier-layered matching actionable) · negative-news bulk scan · FINTRAC /intel/ + AUSTRAC/UK · living-doc hygiene (state 158 / architecture 161 over cap) · dossier edge display-dedup + layout wrap-around (cosmetic) · anchor_summary now CONSUMED (prior "unconsumed seam" notes stale).
(Earlier entries — Phase 41 entity-resolution schema enrichment ([[2026-06-10-phase-41-entity-resolution-schema|journal]] — aliases/properties/relationships enrichment through the shared gate; store ANCHOR redesign; class-aware alias screening; the prompt-regression gate FIRED → red_flags-FIRST schema-order fix; reviewer 9/10 ACCEPT); Phase 40 live red-flag extraction quality ([[2026-06-09-phase-40-live-redflag-extraction-quality|journal]] — the Phase-38 entity playbook applied to FLAGS: blind second-rater measurement + USER CHECKPOINT [residue = RECALL on commercial longreads], SYSTEM_PROMPT mechanism-checklist prompt-shaping w/ HOLDOUT eval [covR 0.40→0.55, positional decay eliminated; r3 rolled back on regression], shared-gate flag_dup_key dup-collapse, 3 new .ph40 federal fixtures); Phase 39 live news QOL ([[2026-06-09-phase-39-live-news-qol|journal]] — always-NDJSON `/extract` stage stream + companion-only news_fetch.py URL ladder/standardizer/verifier; A1 BIT: the accept-with-condition verifier proved load-bearing on the justice.gov Akamai interstitial); Phase 38 consolidate-live-news ([[2026-06-08-phase-38-consolidate-live-news|journal]] — replay-fixture capture, the denylist→subjects-only-prompt PIVOT, keep-biased second-pass verify on-by-default, watchlist view+prune; Qwen ≈ Opus once well-prompted); Phase 37 per-indicator typology ([[2026-06-08-phase-37-per-indicator-typology|journal]] — the Typologies lens regrouped by INDICATOR typology via the sparse data/indicator-typology-map.json overlay; the T1 measure-first probe dissolved the planned neural pass — Model C converged to A, 350 deterministic section-heading assignments; corpus 235→239); Phase 36 persistence + feedback watchlist ([[2026-06-08-phase-36-persistence-watchlist|journal]] — DuckDB store scripts/news_store.py + the escalated-only watchlist loop, companion/.venv-only, dist/news byte-identical); Phase 35 news live local-model backend ([[2026-06-08-phase-35-news-live-backend|journal]] — the served-by-companion live backbone: serve_news.py + the shared news_ground.py grounding gate + the stripped /*LIVE_*/ region, dist/news byte-identical; the CLAUDE.md 689→208 maintenance contract); Phase 34 C/D-assignment verification ([[2026-06-08-phase-34-cd-assignment-verification|journal]], DELIVERED + accepted + committed 83a79c3 — blind re-assignment over 589 unique texts, agreement C 74.4%/D 77.9%, 243 human dispositions, 213 byte-surgical corrections, consistency 30.5%→2.0%); Phase 33 corpus completeness + re-segmentation ([[2026-06-08-phase-33-corpus-completeness-resegmentation|journal]], committed 823c0c2 — 5th source data/fintrac-guidance, corpus 875→2,251 indicators / 42→56 records); Phase 32 news real-source elevation ([[2026-06-08-phase-32-news-real-source-elevation|journal]] — 4 real DOJ/OFAC articles, news.html rewritten to the corpus bar); Phase 30 data-source lens ([[2026-06-08-phase-30-data-source-lens|journal]] — the D-axis fourth Select mode, harness 190→217); Phase 29 capability-lens ([[2026-06-07-phase-29-capability-lens|journal]] — the C-axis third Select mode, taxonomy promoted to committed data/capability-taxonomy.json); Phase 28 corpus-completeness ([[2026-06-07-phase-28-corpus-completeness|journal]], committed 24e4a08 — re-extraction 634→875 indicators, the 28+20 coverage interview, streaming read, attribution footer).)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN/E-23. Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
