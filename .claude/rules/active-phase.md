# Active Phase Context

**Phase 74 — *The persistent entity intelligence spine: consumer slice + standards/schemas + sibling handoff briefs*** (signal-watch-local, STANDARD — user override) — DELIVERED 2026-06-25; READY FOR COMPLETION (T1–T7 all [x]). **No active signal-watch-local phase now — run `/dev-plan` for the next.**

## Status
**COMPLETE — T1–T7 ALL [x] (STANDARD).** The orchestrator flips the delivery gate after the commit verifies. Stood up the companion-only **persistent entity intelligence spine** — the LFCM "memory" lever separating same-signal cases (Northgate-files / Lakeshore-clears). STANDARDS-FIRST → consumer slice → 3 sibling briefs. NEW pillar-neutral `scripts/entity_spine.py` (observations / append-only bitemporal `resolution_links` / `persistent_entities`; deterministic strong/weak/REJECT linkage [name-only REJECTED; 2+ distinct strong → refuse-as-ambiguous]; reversible `retract_link` + cascade-invalidation; event-driven stale-prior; the directional no-news-import firewall) + `scripts/resolution_scorer.py` (pairwise/B-cubed over synthetic `data/entity-spine/true_entities.json` behind a resolver-input firewall — a 1:1 surrogate REJECTED). **THE A1 FILE-BAR GUARD HELD** — `evidence_requirements.py` BYTE-UNCHANGED; injecting a prior `cleared` → byte-identical verdict; confidence/priors ride a SEPARATE grade-gated read path (`_cf_read_manifest`: strong/weak ADMITS, reject/empty QUARANTINES). The MEMORY demo is a MEASURED number — CASE-C Vesna Maric (independent prior-STR PSR-0001) shrinks gather targets 2→1 + pre-names the predicate; genuine gitignored DuckDB write-then-read-back; stale-prior fires after a split. **A5 paid off — both siblings DRIFTED** (substrate a3fb02b not f15c241; casework cfd989f not bf15535, the `cleared` path still UNBUILT) → 3 briefs re-grounded to live HEADs. STANDARD review gate → ship/ship, all findings PRAISE, zero bugs, 8 invariants HELD. Companion-only; build.py imports neither; `--check all` 8/8 byte-frozen; `node tests/workbench.test.mjs` 151; `node tests/news-stream.test.mjs` 150 + `news_live_test` PASS (M8 not regressed); `uv run pytest` 20→22.

## Phase 75+ candidates
Render the memory short-circuit in `workbench.html` (proven by a number, not yet rendered) · converge `news_store` onto the shared spine core (the deferred A1 question) · probabilistic/Splink ER + the merge-adjudication Class-J console · the 3 sibling emission briefs ready for sibling execution · graph/Kuzu projection over the spine.

## Gates
- [x] spec — generated this phase (STANDARD ceremony; `specs/phase-74-entity-intelligence-spine.md`)
- [x] Direction confirmed by user (2026-06-25; NOT all-accept — A1/A5 don't-know→DOWN-SCOPED, A2/A3/A4 accept)
- [x] Delivery accepted (post-implementation report 2026-06-25; user "Accept"; committed e18eba9)

Plan [[phases/phase-74-entity-intelligence-spine]]; ledger Phase-74.
