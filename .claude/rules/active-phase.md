# Active Phase Context

Phase: 32 — News stream: real gov-enforcement adverse-media source + presentation elevation (M8). DELIVERED → READY FOR COMPLETION (all 5 tasks [x]; exit criteria met; delivery gate pending the commit).

Objective: elevate the Phase-31 news stream (`dist/news`) on TWO fronts, the showcase + the entire corpus BYTE-FROZEN. (1) REAL-SOURCE SWITCH — 4 SYNTHETIC fictional articles → 4 REAL US-federal gov-enforcement docs (DOJ Ravenell/Mullings/Goltsev + OFAC TGR Group, verbatim-excerpted under 17 U.S.C. §105 public domain — the corpus's exact basis; NOT a non-negotiable change); the COUNTERPARTY BOOK STAYS SYNTHETIC (#4 held). (2) PRESENTATION ELEVATION to the corpus's bar — full dossier theme + step rail + a streaming "agent reading" Read + entity cards (name/location/age/profession, grounded) + a visible scan PROCESS. The M8 north star (compose the adverse-media atom with the transaction-signal atom) stays scoped OUT.

Outcome: `news.html`/`dist/news` rewritten to the corpus bar (~40.6KB→~70KB); 14 entities + grounded attributes + 29 grounded red-flags; `book.json` reseeded synthetic (1 exact ORG true-positive + 5 near-matches + a common-name trap, scores verified with the real matcher); `validate_news_data` extended ADDITIVELY to quote-ground the attributes (build.py never imports the authoring layer; existing dists byte-identical); Wayback-Machine build-time acquisition (DOJ bot-blocks WebFetch/curl; authoring-only). Harness 38→65 (both motion modes); `--check all` 5/5 ZERO DRIFT; `--selftest` PASS; 42/42 `--check-derived` clean; fail-loud proven; build deterministic. Frozen set byte-clean (git-confirmed); NO non-negotiable change.

Next: Phase 33 (QUEUED) — corpus completeness + typology re-segmentation (workflow-driven).

Gates:
- [x] Direction confirmed by user (news elevation + real gov-enforcement source switch; approved 2026-06-08)
- [x] Delivery accepted (post-implementation report 2026-06-08 — news 65/65, --check all 5/5 zero drift, frozen set byte-clean; unified reviewer 9/10 accept; committed afb24f4 + pushed to main)
