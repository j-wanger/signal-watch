# Dev Wiki Log

[2026-06-04T19:15:46] INIT -- dev wiki bootstrapped, 6 phase articles (retrofit from HANDOFF.md M0-M5), M0 completed / M1 active, ceremony: lite, git: yes
[2026-06-04T19:15:46] PLAN -- Phase 2 (M1) planned, 6 tasks, 3 decisions (minimal structure: engine template + JSON config + stdlib build inliner)
[2026-06-04T19:15:46] IMPL -- Phase 2 (M1) all 6 tasks done; dist/index.html verified byte-identical to baseline; awaiting delivery acceptance + /dev-debrief
[2026-06-04T19:15:46] DEBRIEF -- Phase 2 (M1) completed + accepted; journal 2026-06-04-m1-config-driven-refactor; next: /dev-plan M2
[2026-06-04T19:15:46] PLAN -- Phase 3 (M2) planned, 4 tasks, 3 decisions (TBML typology, build-time switch, build-boundary validation)
[2026-06-04T19:15:46] IMPL -- Phase 3 (M2) all 4 tasks done; TBML added as config-only, engine untouched (zero index.html diff); fentanyl regression byte-identical
[2026-06-04T19:15:46] DEBRIEF -- Phase 3 (M2) completed + accepted; journal 2026-06-04-m2-multi-typology; next: /dev-plan M3
[2026-06-04] PLAN -- Phase 4 (M3) planned, 3 tasks (pure-engine: nav+reset+keys, prefers-reduced-motion, rebuild+Chrome pass); speaker notes deferred; engine edits intentional
[2026-06-04] IMPL -- Phase 4 (M3) all 3 tasks done; engine-only (config/+build.py byte-identical); both dist rebuilt + self-contained; gates verified on both dist x both motion modes; real Chrome 149 renders
[2026-06-04] DEBRIEF -- Phase 4 (M3) completed + accepted; journal 2026-06-04-m3-presenter-polish; next: /dev-plan M5 ship
[2026-06-04] PLAN -- Phase 6 (M5 ship) planned, 3 tasks, 4 decisions; M4 (live/pre-gen) skipped (inert under file://); doc/verify only — parameterize smoke-checklist + refresh README + compliance/offline hard gate; ask-slide/rename/Playwright deferred
[2026-06-04] IMPL -- Phase 6 (M5 ship) all 3 tasks done; doc/verify only (index.html+config/+scripts/ byte-identical); compliance + offline file:// HARD GATE PASS (zero drift, badge both dist, self-contained, advisories paraphrased+attributed, no secrets/PII)
[2026-06-04] DEBRIEF -- Phase 6 (M5 ship) completed + accepted; journal 2026-06-04-m5-ship; project meets HANDOFF §1.2 definition of shipped; M4 + ask-slide + new typologies are optional config-driven follow-ups
[2026-06-04T20:56:40] PLAN -- Phase 7 (M6 pipeline walking skeleton) planned, 5 tasks, 8 decisions; thin vertical slice on FinCEN EFE FIN-2022-A002 (acquire PDF → convert PDF→MD persist → hand-derive one schema-valid signal → render verbatim in Act 1's SOURCE DOCUMENT panel); authoring-time vs ship-artifact split (converter authoring-only, ship stays single-file/offline/no-fetch); FinCEN-only verbatim exception (17 USC §105); Signal Engine→Signal Watch rebrand; T2 converter quality is the de-risk gate
[2026-06-04T01:45:00] DEBRIEF -- Phase 7 (M6 pipeline walking skeleton) all 5 tasks done (not yet committed); 0 decisions captured (lite ceremony skips decision articles), 12 in substance; journal 2026-06-04-m6-pipeline-walking-skeleton; codebase snapshot created; _ARCHITECTURE.md refreshed (realized authoring pipeline: acquire_fincen.py + pdf_to_md.py/markitdown + advisory_full text_file→inline); 3 follow-up phases discovered (doc true-up + provenance fix, FinCEN corpus crawler, automate derivation); retro check ran (5 completed phases) — no systemic issues; delivery gate UNCHECKED (commit handled interactively)
[2026-06-05T02:51:15Z] PLAN -- Phase 8 (doc true-up + provenance fix) planned, 4 tasks, 3 decisions (provenance=remove+attribute-FINTRAC, H1 wording, scope correction). Direction approved.
[2026-06-05T03:07:45Z] DEBRIEF -- Phase 8 (doc true-up + provenance fix) full debrief; phase COMPLETED+accepted (commit 042d732); journal 2026-06-04-phase-08-doc-true-up; 3 decisions; DISCOVERY: Phase 7 dist drift corrected.
[2026-06-05T08:53:42] PLAN -- Phase 9 (build-drift guard) planned, 3 tasks, 4 decisions (HARDEN-before-SCALE direction, in-process build.py --check vs git-diff one-liner, keep committing dist, defer pre-commit/CI). Direction approved. Lite — decisions in _CURRENT_STATE only.
[2026-06-05T09:06:48] DEBRIEF -- Phase 9 (build-drift guard) full debrief, lite; all 3 tasks [x], exit criteria MET in working tree → READY FOR COMPLETION (NOT auto-completed; orchestrator runs delivery gate + commit). 0 decision articles (lite — 4 decisions live in _CURRENT_STATE). journal 2026-06-05-phase-09-build-drift-guard; status snapshot created; _ARCHITECTURE.md refreshed (build.py render_one/check_one/resolve_targets + --check guard). 1 task discovered (Phase 10 candidate: elder presentation-values true-up). retro not triggered (7 completed, 7%5!=0). active-phase.md written with delivery gate UNCHECKED.
[2026-06-05T09:31:39] PLAN -- Phase 10 (FinCEN corpus crawler — SCALE) planned, 4 tasks, 4 decisions (SCALE direction over elder true-up; discovery-manifest not mass-download; pure parser + --selftest determinism split; repo-hygiene rails). Direction approved. Lite — decisions in _CURRENT_STATE. Authoring-only: crawl_fincen.py generates data/fincen/index.json; acquire_fincen.py reads it (EFE fallback). ABORT rail: index not deterministically scrapeable → hand-curated manifest.
[2026-06-05T09:31:39] DEBRIEF -- Phase 10 (FinCEN corpus crawler — SCALE) full debrief, lite; all 4 tasks [x], exit criteria MET in working tree → READY FOR COMPLETION (delivery gate + commit pending user). 0 decision articles (lite — decisions in _CURRENT_STATE). journal 2026-06-05-phase-10-fincen-corpus-crawler. New authoring-only crawl_fincen.py (pure parse_index + --selftest/--write/--list/--fetch) → data/fincen/index.json (14 advisories); acquire_fincen.py REGISTRY→manifest + resolve_pdf detail-page hop + EFE direct-PDF override. T1 DISCOVERY: listing yields detail-page URLs not PDF URLs. Widened pipe proven live on 2-advisory batch (deleted per user call). engine untouched, --check all zero drift. 4 soft observations (elder true-up carried, AUTOMATE fork, fin-2024-a002 fentanyl re-point, manifest --fetch cadence). retro not triggered (8 completed, 8%5!=0). active-phase.md delivery gate UNCHECKED.
[2026-06-05T10:14:32Z] PLAN -- Phase 11 (Automated derivation — LLM-drafted signal config, AUTOMATE) planned, 5 tasks, 5 decisions (lite — decisions in _CURRENT_STATE). Direction approved: AUTOMATE → variant B (LLM-drafted signal definition NOW); user overrode the planner's finish-the-elder-true-up-first + deterministic-only (variant A) recommendations. One authoring-only scripts/derive_signals.py, two layers: deterministic (extract_red_flags + scaffold_config, stdlib, --selftest/--scaffold, offline, anthropic LAZY) + neural (--draft, ANTHROPIC_API_KEY from env, anthropic in the gitignored authoring venv). Boundary preserved (reconciles the override with Phase-9 anti-neural-judge-at-boundary): LLM PROPOSES a .draft.json; build.py + schema + the two human gates DISPOSE. T3 (single L) consults the claude-api reference for the model id + SDK + structured-output. Abort: variant A deterministic-scaffolder fallback if the LLM can't hit schema-valid output. Elder true-up + fentanyl re-point + manifest --fetch cadence deprioritized to Future (not dropped).
[2026-06-05] DEBRIEF -- Phase 11 (Automated derivation — LLM-drafted signal config, AUTOMATE) full debrief, lite; all 5 tasks [x], exit criteria MET in working tree → DELIVERED, user-accepted at the gate (commit + gate-mark follow). 0 decision articles (lite — 5 decisions in _CURRENT_STATE). journal 2026-06-05-phase-11-automated-derivation; _ARCHITECTURE refreshed (derive_signals.py two-layer module + anthropic authoring dep + automated `derive` pipeline step). Size-gated review gate (5 tasks ≥4): 9/10 ACCEPT — the Anthropic structured-output shape (claude-opus-4-8 + output_config.format json_schema) verified vs the claude-api reference, not guessed; 2 MEDIUM --draft-path fixes folded in pre-commit (adaptive thinking + effort:high; graceful refusal/max_tokens handling). Boundary preserved (LLM proposes a gitignored .draft.json; build.py + schema + 2 human gates dispose; build.py rejects the bare skeleton, accepts a filled draft). engine untouched, --check all zero drift, no sk-ant, anthropic lazy. retro not triggered (9 prior completed, 9%5!=0). 5 soft observations (--draft live-path hardening DONE this phase; run --draft on a NEW advisory w/ a real key; elder true-up; fentanyl re-point to fin-2024-a002; manifest --fetch cadence). M6 vision arc (Phases 7–11) complete. Committed c37dc39 (impl) + 7c76971 (gate), pushed to origin/main.
[2026-06-05] PLAN -- Phase 12 (FinCEN corpus derivation foundation, M7) planned, 5 tasks, 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved BACKEND-ONLY. Build the backend for a SINGULAR corpus-backed FinCEN demo (eventual: user picks 1 of 14 advisories, watches the loop derive coverage → build recommendations → signal): deterministic spine validated on ALL 14 (generalize extract_red_flags beyond EFE's exact phrasing — corpus heterogeneous, fin-2020-a009/fin-2021-a003 have 0 red-flag grep hits — + a --corpus report + FLAG non-conformers) + LLM-backend derivation proven on a 2–3 slice (THIS session as backend, NO API key — Phase-11 T4 substitution). Boundary preserved: LLM proposes build-rec + build-logic; deterministic spine disposes (schema/shape + build-rec consistency vs cover×data + traceability to a red-flag md line). 14 corpus md ALREADY on disk (gitignored) + network blocked in-session → T1 commits the existing corpus (no acquisition). Engine index.html + build.py UNTOUCHED; demo expansion (advisory-selection UI + build-rec render) = Phase 13. Abort: section-FINDER that flags non-conformers if formats too heterogeneous. User chose backend-only over a minimal selectable demo view.
[2026-06-05] DEBRIEF -- Phase 12 (FinCEN corpus derivation foundation, M7) full debrief, lite; all 5 tasks [x] + 2 user-requested post-completion refinement passes (widening + artifact filters), exit criteria MET in working tree → DELIVERED, user-accepted at the gate (commit + gate-mark follow). 0 decision articles (lite — decisions in _CURRENT_STATE). journal 2026-06-05-phase-12-fincen-corpus-derivation; _ARCHITECTURE refreshed (corpus committed + derived/ records + the section-FINDER/--corpus/checks/--scaffold-derived/--check-derived modes + the assists-not-automates note). Size-gated review gate (5 tasks ≥4) dispatched. Deterministic spine: extract_red_flags rewritten as a corpus-wide section-FINDER (Tier-1 + Tier-2 fallback + intro-noise/header-block/citation filters); --corpus → 7 CLEAN · 3 LOW · 4 NEEDS (2 NEEDS = FATF jurisdiction advisories, correctly flagged). Checks build_rec_category (cover×data matrix) + check_record (consistency + traceability + BUILD_NOW⇒logic). LLM backend = THIS session (no key) derived 2 records (fin-2022-a001 kleptocracy, fin-2024-a002 PRC precursors), each --check-derived clean; boundary holds (tampered record rejected). NOTED in CLAUDE+README: the spine ASSISTS but does not AUTOMATE — complete records need LLM-backend authoring. EFE still 12+12; index.html/build.py/schema untouched; --check all zero drift; anthropic lazy. RETRO triggered (10 completed, 10%5==0): healthy self-correction pattern (EFE 12+12 regressions caught+fixed twice during generalization; extraction artifacts surfaced via user review + filtered; threshold recalibrated 500→600 with corpus data not first-guess) — no systemic issue; meta-lesson: markitdown-converted real-world corpora need EMPIRICAL calibration, not first-guess constants. 6 soft observations (Phase 13 demo expansion; glued-list splitting; exclude FATF advisories; scale derivation to 5 more CLEAN; residual artifacts; EFE-as-derived validation). active-phase.md delivery gate UNCHECKED (flips at the gate-mark commit, D3).
[2026-06-06T02:04:23Z] PLAN -- Phase 13 (Corpus explorer — advisory-selection front-end + per-indicator build-rec render, M7) planned, 5 tasks, 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved: a NEW standalone ship artifact dist/corpus/index.html (built from corpus.html) — a FinCEN CORPUS EXPLORER where a stakeholder picks 1 of 14 advisories and watches the loop derive coverage → per-indicator build recommendations → signal. STAGED 4-screen flow (SELECT [14 advisories, honest status chips — DERIVED live/CLEAN/LOW/NEEDS "not yet derived"/FATF non-derivable, 2 derived clickable] → COVERAGE [gauge covered=1/partial=0.5/gap=0 + indicator list] → BUILD RECOMMENDATIONS [the centerpiece — per-indicator cover×data→build_rec matrix, BUILD_NOW-first, src_line-traceable] → SIGNAL SPEC [BUILD_NOW cards from build_logic]). Showcase byte-frozen (index.html + config/** + 3 typology dists untouched); corpus.html owns its own copy of the theme CSS (no shared include). build.py stays DECOUPLED — render/build/check_corpus + special "corpus" target + a corpus-data boundary validator (build_rec ∈ enum; BUILD_NOW ⇒ full build_logic shape), reads committed corpus-status.json + derived/*.json, NEVER imports derive_signals.py. derive_signals.py --corpus-status emits the committed 14-entry corpus-status.json. No fabricated lift/stats; always-on "Illustrative data & outputs" badge. Tasks: T1 --corpus-status manifest (M) · T2 corpus.html standalone 4-screen template (L) · T3 build.py corpus path (M) · T4 build + verify (S) · T5 docs (S). User chose standalone over fold-into-index.html, staged 4-screen over a dense dashboard, all-14-honest-status over only-2-derived or derive-5-more-first. Abort: re-implement the renderable component standalone in corpus.html if rendering would force an engine edit — never touch index.html.

[2026-06-05T22:30:52] DEBRIEF -- 0 decision articles (lite; 4 decisions in journal), 1 journal (phase-13-corpus-explorer), tasks updated (5 completed), state refreshed, phase-13 snapshot, active-phase.md rewritten (delivery gate unchecked). Phase 13 READY FOR COMPLETION.
[2026-06-06T17:42:48Z] PLAN -- Phase 19 (Durability closeout — commit corpus-explorer test harness + pin _rf_triage, M7) planned, 3 tasks, 3 decisions (lite — decisions in _CURRENT_STATE). Direction approved: durability closeout over call-it-done / showcase-debt-true-up / new-stakeholder-ask. At the end of the completed M0–M7 roadmap (demo at Definition of Done): no net-new feature value remains without fabrication risk (combination-lift needs a precision source the records don't carry — rejected Ph18) and no scale frontier inside FinCEN (12/14 derived; the 2 FATF have no red-flag list); the one remaining piece with real non-fabrication value is durability. T1 (M): commit tests/corpus-explorer.test.mjs — a ZERO-DEP Node DOM-shim runner that reads the committed dist/corpus/index.html, drives the 5-screen arc, asserts the ~15 Ph18 invariants (NEVER jsdom — file:// offline ethos + the dep-free --selftest/--check idiom; loading the committed dist doubles it as a build-output smoke test). T2 (S): PIN + DISCLOSE _rf_triage (Ph17 reviewer MEDIUM) — a glued-fixture --selftest assertion + a one-line "coarse hint, not authoritative for glued" comment, NO output change (an accurate glued counter would reintroduce the Phase-17-deleted parser — anti-subtraction; harmless today). T3 (S): wire the harness command into README + CLAUDE + smoke-checklist. Two carried candidates resolved in planning: the anthropic pin is DEAD (requirements-authoring.txt does not exist — Ph17 deleted it), the tests/ gap is REAL (only fixtures/ + smoke-checklist.md; the DOM-shim was ad-hoc/uncommitted — soft observation 3 phases running). UNFREEZE: tests/**, scripts/derive_signals.py (comment + selftest fixture ONLY), README.md, CLAUDE.md, tests/smoke-checklist.md. FROZEN: index.html, corpus.html, scripts/build.py, config/**, data/fincen/** (corpus-status.json + derived/*.json), ALL dist/** (harness only READS dist/corpus). NO behavioral change — corpus-status.json + dist/** byte-identical; --check all zero drift. Abort: DEGRADE to a leaner assertion set (boot + each screen renders + coverage-math unit, skip event-driven toggles) if the shim can't drive the arc — NEVER pull jsdom into a file:// offline demo.

[2026-06-05] PLAN -- Phase 14 (Scale corpus derivation — 3 more CLEAN advisories → 5/14 live, M7) planned, 5 tasks, 2 decisions (lite — decisions in _CURRENT_STATE). Direction approved: raise the corpus explorer's live menu from 2/14 → 5/14 by deriving 3 more CLEAN FinCEN advisories — fin-2020-a008 human trafficking (11), fin-2025-a003 Chinese money-laundering networks (17), fin-2025-a002 Iran illicit finance (16). PURE AUTHORING, no engineering: the spine tools (--scaffold-derived/--check-derived/build_rec_category) + the front-end (corpus.html, build.py corpus) already exist from Phases 12–13; build.py makes an advisory live simply by the presence of data/fincen/derived/<id>.json, so this phase touches ONLY new derived/*.json + a dist/corpus rebuild + docs. Per advisory: --scaffold-derived → author per-indicator status/data → build_rec (cover×data matrix) + rationale + build_logic for BUILD_NOW gaps → prune residual noise → --check-derived disposes (matrix-consistent + traceable + BUILD_NOW⇒full definition). LLM backend = THIS session (no key). EFE fin-2022-a002 excluded (already the showcase elder typology); COVID EIP fin-2021-a002 deferred (dated; T3 swap target if Iran extracts unfaithfully). Tasks: T1 derive fin-2020-a008 (M) · T2 derive fin-2025-a003 (M) · T3 derive fin-2025-a002 — validate-scaffold-first + swap path (M) · T4 regen --corpus-status + build.py corpus + verify the 3 render + --check all zero drift (S) · T5 docs bump 2/14→5/14 (S). UNTOUCHED/byte-frozen: index.html, corpus.html, config/**, scripts/**, 3 typology dists. User chose scale-derivation over spine-robustness / corpus-wow-beat / showcase-debt; 3 advisories, EFE/COVID out. Abort: if ≥2 of 3 extract too noisily/unfaithfully for a demo-quality traceable record, narrow (quality over count) + raise a glued-list-splitting Phase-15 item — don't ship filler.

[2026-06-05] DEBRIEF -- Phase 15 (Harden extraction faithfulness + fix shipped defects, M7) DELIVERED + accepted; full debrief (sig 13). 5/5 tasks [x]; 0 decision articles (lite; 2 decisions in _CURRENT_STATE); 1 journal (2026-06-05-phase-15-harden-extraction-faithfulness). Footnote-resume fix in extract_red_flags (3 measured iterations: _SECTION_STOP terminals + conditional _FOOTNOTE_STOP + 2 targeted _CITATION signatures) — SURGICAL: fin-2025-a003 recovered its silently-dropped L499 escrow flag 17→18, 0 collateral [all 13 other advisories byte-identical, EFE 12+12, 7C/3L/4N]. esc() entity sweep (fin-2022-a001 + fin-2024-a002 → raw text); escrow IND-18 added; manifest regen + dist/corpus rebuild + --check all zero drift. Glued-no-separator DEFERRED (no safe split, re-confirms Phase-12; needs a better converter). Health: extract_red_flags refined, no new deps, --selftest 12+12, byte-frozen held. No escape hatches; no architectural change. Abort rule hit (first attempt over-captured) but resolved by the bounded next_boundary rule, not aborted. Gate: direction approved + delivery accepted (gate-flip commit follows impl, gate-state follows git-state). Next: /dev-plan Phase 16.

[2026-06-05] PLAN -- Phase 15 (Harden extraction faithfulness + fix shipped defects, M7) planned, 5 tasks, 2 decisions (lite — decisions in _CURRENT_STATE). Direction approved: fix the 2 concrete defects Phase 14 surfaced, scoped by MEASUREMENT. (1) Footnote-resume fix in extract_red_flags — a mid-list page-boundary footnote run no longer hard-ends a section [_SECTION_STOP], so a CLEAN advisory stops silently dropping a real flag (fin-2025-a003 L499 escrow flag); guarded by --selftest EFE 12+12. (2) Pre-escaped-entity sweep across derived/*.json — fin-2022-a001's `&gt;= 2` → raw text (corpus.html esc() is the sole escaper). Then add the recovered escrow flag as fin-2025-a003's 18th --check-derived-clean indicator, regen --corpus-status, rebuild dist/corpus, --check all zero drift. GLUED-NO-SEPARATOR SPLITTING DEFERRED — measurement showed the glued LOW/NEEDS advisories (ransomware fin-2021-a004, health-care fin-2026-a001) have no safe deterministic split (markitdown dropped bullets AND blank lines); they stay correctly FLAGGED (re-confirms the Phase-12 flag-don't-force decision). Tasks: T1 footnote-resume fix (M) · T2 entity sweep (S) · T3 escrow 18th indicator (S) · T4 regen+rebuild+drift-guard (S) · T5 docs (S). UNTOUCHED/byte-frozen: index.html, corpus.html, config/**, 3 typology dists. User chose harden-spine + fix-defects over scaling further / showcase debt / wow beat; approved deferring glued-splitting. Abort: if footnote-resume can't capture L499 without regressing EFE or swallowing footnote prose, revert + keep the miss FLAGGED (documented).

[2026-06-05] DEBRIEF -- Phase 14 (Scale corpus derivation, M7) DELIVERED + accepted; full debrief (sig 13). 5/5 tasks [x]; 0 decision articles (lite; 2 decisions in _CURRENT_STATE); 1 journal (2026-06-05-phase-14-scale-corpus-derivation). Live menu 2/14 → 5/14: 3 new --check-derived-clean records [fin-2020-a008 trafficking 10ind/2 BUILD_NOW · fin-2025-a003 Chinese MLN 17ind/5 BUILD_NOW · fin-2025-a002 Iran 16ind/4 BUILD_NOW·7 ENRICH]. PURE AUTHORING — zero engine/spine/front-end edits; --check all 4-artifact zero drift; --selftest 12+12; index.html/corpus.html/config/**/scripts/** + 3 typology dists byte-untouched. FINDINGS (Phase-15): extractor missed fin-2025-a003 L499 (page-break-glued flag, same class as the 3 LOW); pre-existing fin-2022-a001 `&gt;=` esc() double-escape render bug (store RAW text). No escape hatches; no architectural change. Gate: direction approved + delivery accepted (gate-flip commit follows impl commit, gate-state follows git-state). Next: /dev-plan Phase 15.
[2026-06-06T04:18:00Z] PLAN -- Phase 16 (Invert extraction — LLM extracts, deterministic gate disposes — + scale as proof, M7) planned, 5 tasks, 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved 2026-06-06: INVERT the extraction architecture. Subtraction test on the ~130-line accreted extract_red_flags machinery (_SECTION_STOP/_FOOTNOTE_STOP/_CITATION; still only 7/14 clean) — the LLM (this session) EXTRACTS candidate red flags, the deterministic layer becomes a GROUNDEDNESS GATE: normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority, + a cheap section-cite relevance guard. "Deterministic validators at boundaries over neural judges at the end" — LLM at the start (generative), validator at the boundary; complexity relocates from brittle section-PARSING (open) to md NORMALIZATION (closed: running headers, form-feeds, hyphen-breaks, smart quotes, whitespace). matrix-consistency (build_rec_category) + BUILD_NOW⇒logic + shape checks UNCHANGED. Decisions: A relevance trade = groundedness + section-cite, not pure-groundedness/not a full parser; B extract_red_flags DEMOTED not deleted (selftest-anchor + scaffold hint, corpus-status keeps its SHAPE so build.py/corpus.html byte-untouched, derivable false only for the 2 FATF advisories, CLEAN/LOW/NEEDS an informational hint); C breadth = 2 records as PROOF over count (COVID-EIP fin-2021-a002 + ≥1 previously-LOW/glued advisory: ISIS fin-2025-a001 OR ransomware fin-2021-a004 → 7/14 live), pymupdf4llm converter option DISSOLVED. Tasks: T1 groundedness gate + md normalizer (L) · T2 migrate the 5 committed records (M) · T3 demote extractor + shift corpus-status semantics SHAPE-preserved (M) · T4 scale as proof — 2 new records incl. ≥1 previously-unreachable (M) · T5 rebuild dist/corpus 7/14 + drift guard + docs (S). UNTOUCHED/byte-frozen: index.html, corpus.html, config/**, 3 typology dists. User chose invert-architecture over pure breadth-scaling / a structure-preserving converter; named the fear that we still rely on LLM authoring → make the deterministic layer a GATE. Abort: if the normalizer can't ground the escrow/glued cases without becoming as complex as the extractor it replaces (complexity MOVES not shrinks — subtraction test FAILS), keep quote-grounding for the clean records, leave extract_red_flags as-is, narrow breadth to COVID-EIP only.
[2026-06-06T01:02:57] DEBRIEF -- 0 decision articles (lite; 4 decisions recorded in _CURRENT_STATE during planning), 1 journal (phase-16-invert-extraction), tasks updated (5 completed, 2 discovered Phase-17 candidates), state refreshed, snapshot created, active-phase.md delivery gate UNCHECKED (awaiting commit)

[2026-06-06T14:50:44Z] PLAN -- Phase 17 (Complete corpus derivation + delete extract_red_flags — the real subtraction, M7) planned, 6 tasks, 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved 2026-06-06: finish the Phase-16 inversion with the REAL line-count subtraction + scale to completion, sequenced subtraction-FIRST. (A) Delete extract_red_flags + the now-dead --scaffold/--draft/--scaffold-derived authoring stack (scaffold_config, write_scaffold, draft_judgment, write_draft, _few_shot, _apply_judgment, _derived_skeleton) the inverted loop replaced; add a ~12-line rf_region-bounded triage counter (reuses the existing rf_region span, counts candidate lines) so the deletion is clean + --corpus-status stays regenerable. derive_signals.py ~1202 -> ~700, leaving normalize + rf_region + check_record + counter + --corpus-status + --selftest (gate-only). The debrief had the dependency backwards: extract_red_flags is off the correctness path but is the SOLE producer of the triage the byte-frozen corpus.html renders. (B) Scale on the stripped spine: derive the 5 remaining derivable advisories (fin-2026-a001 glued, fin-2021-a001, fin-2024-a001 LOW, fin-2025-a001 LOW, fin-2022-a002 EFE) via the inverted loop -> corpus 7/14 -> 12/14 live (only the 2 FATF advisories non-derivable). Tasks: T1 THE SUBTRACTION (L) - delete + counter + gate-only selftest - T2 scale batch 1 fin-2026-a001 + fin-2021-a001 (M) - T3 scale batch 2 LOW validate-first fin-2024-a001 + fin-2025-a001 (M) - T4 EFE corpus record fin-2022-a002 -> 12/14 (S) - T5 regen + rebuild + headless verify (S) - T6 docs (S). FROZEN/byte-untouched: index.html, corpus.html, config/**, scripts/build.py, 3 typology dists (counter preserves corpus-status.json SHAPE). User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table. Abort: if the counter can not preserve corpus-status SHAPE without growing as complex as extract_red_flags (subtraction MOVES not shrinks), STOP the deletion, keep extract_red_flags, narrow to scale-only. Degrade a LOW/glued advisory that won not ground to scale-what-passes (deletion still lands - counter-gated).
[2026-06-06T15:30:00Z] DEBRIEF -- Phase 17 (Complete corpus derivation + delete extract_red_flags — the real subtraction, M7) DELIVERED + reviewer ACCEPT 9/10; full debrief (lite ceremony). 6/6 tasks [x]; 0 decision articles (lite — 4 decisions in the journal); 1 journal (2026-06-06-phase-17-complete-corpus-derivation). THE SUBTRACTION: deleted extract_red_flags + the whole --scaffold/--draft/--scaffold-derived authoring stack (scaffold_config, write_scaffold, draft_judgment, write_draft, _few_shot, _apply_judgment, _derived_skeleton) + the anthropic dep + the os import → derive_signals.py 1202→600 (−602, −50%; def-grep 0; stdlib-only); the ~14-line _rf_triage(md, region) counter reuses the rf_region span (real shrink, not a reintroduced parser); --selftest gate-only PASS. SCALE: 5 new gate-passing records via the inverted loop [fin-2026-a001 glued health-care 24ind/6 BUILD_NOW · fin-2021-a001 COVID 16ind · fin-2024-a001 Iran-terror 9ind · fin-2025-a001 ISIS 11ind · EFE fin-2022-a002 12 FINANCIAL flags only] → corpus 7/14 → 12/14 live (only the 2 FATF advisories non-derivable). dist/corpus 109,948→~181 KB; --check all 4-artifact ZERO DRIFT; built __CORPUS__ valid JSON (14 advisories, 12 derived, render-ready). Reviewer 1 MEDIUM = latent triage-overcount footgun for a FUTURE not-yet-derived glued advisory (harmless in the shipped 12/14, disclosed; folded to soft obs, NOT fixed inline). _ARCHITECTURE.md updated (structural change: deletion + stdlib-only). DISCOVERY: corpus_status_records uses _rf_triage UNIFORMLY for all 14 (simpler than the planned per-live read). DEBRIEF FINDING: requirements-authoring.txt still pins anthropic though derive_signals.py no longer imports it (stale pin — Phase-18 cleanup). RETRO (Phases 1-15, 15%5==0): high-signal Dim 3 = recurring user override toward scale/architecture over the showcase-debt true-up; no systemic failure, only 1 high-signal dim → no improvement phase. State refreshed (138→105, all Ph16/17 decisions + OPEN blockers preserved); snapshot refreshed to Phase 17; active-phase.md delivery gate UNCHECKED (awaiting commit — gate-state follows git-state). Gate: direction approved; delivery accepted flips at the gate commit. Next: commit + flip the gate, then /dev-plan Phase 18.
[2026-06-06T16:40:00Z] PLAN -- Phase 18 (Corpus explorer arc — human gate + close-the-loop coverage payoff, M7) planned, 5 tasks, 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved 2026-06-06: give the corpus explorer (dist/corpus/, from corpus.html) the dramatic arc the six-act showcase has — a 5-screen arc (Select -> Coverage -> Build recs/GATE -> Signal -> Close the loop), mirroring the showcase's Act 3 (human gate) + Act 6 (loop closes), grounded ENTIRELY in existing data, NO fabricated numbers. (3) Build recs -> THE GATE: BUILD_NOW recommendation rows become SELECTABLE (the "agent proposes, human disposes" thesis), folded INTO the existing Build-recs screen (subtraction test), default all-BUILD_NOW-selected, non-BUILD_NOW read-only, div-toggles NOT <input> (preserve Space/arrow keyboard nav + determinism — the showcase .selrow pattern). (4) Signal: spec cards for selected ∩ BUILD_NOW with build_logic; honest labeled empty state. (5) Close the loop [NEW]: the coverage index animates BEFORE -> AFTER via the existing coverageIndex() with picked gap indicators flipped to covered (identical model to showcase Act 6); reduced-motion jumps to after; 0 BUILD_NOW / 0 picked -> honest flat-hold + note (never a fake rise). Decisions: (1) Phase 18 = the corpus-explorer arc (chosen over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate — the explorer is where the project's earned identity lives [real provenance, 12/14 derived] + is the one artifact missing the arc; net-new buy-in, not polish; chosen at the END of the completed M0–M7 roadmap after a UX-research review confirmed the agentic spine was already sound). (2) The payoff is COVERAGE close-the-loop, NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (the records carry no precision/lift numbers; porting it would FABRICATE ~12 per-advisory illustrative stats, violating "never present synthetic numbers as real"); coverage is already computed [coverageIndex()] + already disclosed illustrative -> the honest, zero-new-fabrication payoff. (3) Scope contained to corpus.html (+ dist/corpus rebuild + docs); NO schema/data/build.py change [the arc reuses existing fields: status->coverage, build_rec->BUILD_NOW, build_logic->spec card]; the gate uses div-toggles not <input> to avoid keyboard-nav collision + preserve determinism. (4) The gate FOLDS into the existing Build-recs screen (subtraction test — don't add a screen when an existing one carries the interaction); arc stays 5 screens, default all-BUILD_NOW-selected so the default presenter path walks a complete arc. Tasks: T1 the gate — BUILD_NOW rows selectable div-toggles + per-advisory selection Set, default all-selected, reset on pick(), non-BUILD_NOW read-only (M) · T2 Signal reflects the gate — renderSignal filters to selected ∩ BUILD_NOW with build_logic, honest empty state (S) · T3 close-the-loop screen — 5th screen, STEPS->5 + renderClose, coverage before->after from the picks, reduced-motion branch, honest 0-BUILD_NOW flat-hold (L) · T4 rebuild dist/corpus + headless verify + drift guard (S) · T5 docs README + CLAUDE (S). FROZEN/byte-untouched: index.html, scripts/build.py, config/**, data/fincen/** (corpus-status.json + derived/*.json), 3 typology dists. User chose the corpus-explorer arc + coverage payoff + corpus.html-only scope + div-toggle gate folded into Build-recs. Abort: DEGRADE to close-the-loop-only (no gate; coverage close over ALL BUILD_NOW — the payoff still lands) if interactive selection can't be made keyboard-safe/deterministic without growing complex; keep the close screen informational if the math can't be honest; NEVER fabricate precision/lift; blocked >3 attempts -> ask user: skip or abort.
[2026-06-06T17:10:00Z] DEBRIEF -- Phase 18 (Corpus explorer arc — human gate + close-the-loop coverage payoff, M7) DELIVERED + accepted + COMMITTED + PUSHED; full debrief (lite ceremony). 5/5 tasks [x]; 0 decision articles (lite — 4 decisions in _CURRENT_STATE/journal); 1 journal (2026-06-06-phase-18-corpus-explorer-arc). THE ARC: corpus explorer flat 4-screen -> 5-screen dramatic ARC (Select -> Coverage -> Build recs/GATE -> Signal -> Close the loop), mirroring showcase Act 3 (human gate) + Act 6 (loop closes). T1 the gate: BUILD_NOW rec rows -> selectable div-toggles (NOT <input>, preserves Space/arrow nav + determinism — the .selrow pattern) + per-advisory `selected` Set, default all-selected, reset on pick(), non-BUILD_NOW read-only, folded into the Build-recs screen (subtraction test). T2 Signal reflects the picks: selected ∩ BUILD_NOW w/ build_logic, two honest empty states (nothing-buildable vs deselected-all). T3 close-the-loop 5th screen: new renderClose (STEPS 4->5, DETAIL 3->4), coverage gauge animates before->after via the existing coverageIndex() with picked gaps flipped covered on a COPIED indicator set (Object.assign — never mutates a.indicators), reduced-motion lands after directly, 0-picked/0-BUILD_NOW honest flat-hold (never a fake rise). THE HONESTY CALL: COVERAGE close-the-loop, NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (records carry no precision/lift numbers; porting the showcase 22%->81% beat would fabricate ~12 per-advisory illustrative stats, violating "never present synthetic numbers as real"). CONTAINED scope: corpus.html-only unfreeze (+ dist/corpus rebuild + docs), NO schema/data/build.py change (the arc reuses existing fields). T4-T5: dist/corpus rebuilt 109,948->180,119-class, 15/15 headless DOM-shim assertions, node --check valid, --check all 4-artifact ZERO DRIFT, frozen set git-diff-empty in 6d654a4 (index.html/build.py/config/data/3-typology dists byte-untouched); README + CLAUDE document the 5-screen arc + the coverage-not-precision honesty stance. _ARCHITECTURE.md already carried the 4->5-screen contract from plan time (timestamp bumped to Phase 18). Review gate (lite + 5 tasks >= 4): lightweight post-hoc, ACCEPT — clean diff, reuses existing helpers, no fabricated numbers, frozen set held; 1 LOW (animated close path headless-untested, reuses proven animVal); no MEDIUM/HIGH, nothing blocking. RETRO not triggered (17 completed phases; 17%5 != 0). SOFT OBS: 0-BUILD_NOW close path unexercised by the 12 live advisories (all have >=1 BUILD_NOW; verified via deselect-all) · animated close path headless-untested · headless DOM-shim ad-hoc/uncommitted -> tests/ harness is a Phase-19 candidate · vm.runInNewContext doesn't auto-expose const/let (epilogue-bind to globalThis). Escape hatches: USER OVERRIDE (corpus-explorer arc via a UX-research-report detour that was a validation pass) + committed direct to main (matches the repo's per-phase dev-wiki lifecycle, user said "commit"/"push"). Clean phase — abort/degrade path never needed, no blocked tasks. Health: no formal test framework (ad-hoc headless assertions + node --check + build.py --check all); no regressions (3 typology dists + showcase byte-identical). Gate: direction approved + delivery accepted (impl 6d654a4 + gate 2604202 on origin/main; gate-state follows git-state — KEPT [x] this run). State refreshed; snapshot 2026-06-06 already existed; index.md rebuild skipped (lite). Next: /dev-plan Phase 19, or call it done (demo at Definition of Done).
[2026-06-06T18:00:30Z] DEBRIEF -- 0 decision articles (lite; 3 decisions in _CURRENT_STATE), 1 journal, tasks updated (3 completed), state refreshed, active-phase.md updated (delivery gate UNCHECKED — awaiting commit)

[2026-06-06T23:06:39Z] PLAN -- Phase 20 (Multi-source spine, proven with FinCEN Alerts, M7) planned, 6 tasks, 3 decisions (lite — decisions in _CURRENT_STATE). Direction approved 2026-06-06: scale the corpus beyond FinCEN ADVISORIES to other FinCEN PUBLICATION TYPES (Alerts first) via a THIN multi-source SOURCES registry — staying entirely inside the verbatim public-domain regime so NO non-negotiable change (still FinCEN, still 17 USC §105, still verbatim; the "FinCEN-only verbatim" rail untouched). The quote-grounding gate (check_record/rf_region/normalize) is source-agnostic, reused UNCHANGED. Architecture: a thin SOURCES registry decouples source-id from storage dir; build.py render_corpus iterates sources, reads each source's corpus-status.json + derived/*.json, merges by id into one __CORPUS__ (per-source shape validation at the boundary, tagging Advisory/Alert); derive_signals.py --corpus-status takes an optional source path. ZERO churn: data/fincen/ stays the `fincen-advisories` source BYTE-FROZEN (no renaming 14 mds + 12 derived + corpus-status — prove multi-source via the MERGE, not a migration); alerts land in a NEW data/fincen-alerts/. Proof: retarget acquisition to the FinCEN alerts hub (direct zero-hop /system/files/...FinCEN Alert*.pdf links) -> ~5 alert mds, derive via the inverted loop (LLM extracts, --check-derived disposes), skip un-groundable alerts honestly (rf_region None -> non-derivable, like the 2 FATF advisories). Decisions: (1) scale via OTHER FinCEN publication types — Alerts first — over other agencies (OFAC, documented next-source candidate) / a new showcase typology (user reframed at the gate: "try FinCEN articles first, other than advisories"); (2) thin SOURCES registry with data/fincen/ byte-frozen, ready for {fincen-advisories, fincen-alerts, ...ofac} and no more (NOT a plugin framework); (3) proof batch ~5 alerts grounded cleanly, remaining alerts cheap follow-on, T2 carries a convert-one-first rf_region CHECKPOINT. Tasks: T1 (L) the SOURCES registry in build.py + derive_signals.py — gate UNCHANGED, data/fincen/ byte-frozen · T2 (M) retarget acquisition to the alerts hub + acquire/convert the batch, with the rf_region checkpoint · T3 (M) derive ~5 alerts via the inverted loop, skip un-groundable honestly · T4 (S) corpus.html unified menu + honest type chips · T5 (S) regen per-source status + rebuild + drift guard + extend the harness · T6 (S) docs. FROZEN/byte-untouched: index.html, config/**, 3 typology dists, data/fincen/**. User chose scale via FinCEN publication types + a thin registry + data/fincen/ byte-frozen + a ~5-alert proof batch. Abort: T2 CHECKPOINT — convert ONE alert, confirm rf_region not None before the batch; degrade = pivot to FinCEN Financial Trend Analyses or derive fewer-but-deeper; if the registry can't merge without a per-source migration (subtraction MOVES not shrinks), narrow to advisories-only + a registry stub. NEVER fabricate; the always-on badge + the verbatim public-domain attribution stay.
[2026-06-06T19:59:41] DEBRIEF -- 0 decision articles (lite), 1 journal, tasks updated, state refreshed, phase-20 status->completed, active-phase updated (delivery gate pending commit), retro triggered
[2026-06-07T00:57:46Z] PLAN -- Phase 21 (OFAC as corpus source #3, gate widened for OFAC vocab; US-federal public-domain, M7) planned, 6 tasks [1 L], 4 decisions (lite — decisions in _CURRENT_STATE). Direction approved 2026-06-06: add OFAC (US Treasury) as the THIRD corpus source WITH a regression-gated widening of the rf_region anchors so OFAC's heading vocabulary derives (user chose "widen the gate" over a small-clean-source / holding OFAC). EVIDENCE (planning probe): the red-flag-TEMPLATE OFAC docs already ground UNCHANGED (Sham Transactions rf_region (82,236), triage clean·26, "Red Flags: Indicia of Sham Transactions"), but most OFAC docs use "Risk Indicators" (VC guidance), "Deceptive Practices" (maritime), or defer to a co-issued FinCEN advisory (ransomware → no list). rf_region is hard-coupled to the literal "red flag(s)" phrase (4 regexes) + check_record treats rf_region==None as a HARD violation → widen the anchor set to also match "Risk Indicators"/"Deceptive Practices"/"Risk Factors" headings (+ the risk-indicator intro), GATED by ZERO FinCEN regression (verified low: "risk indicators" 0 FinCEN mds, "deceptive practic" 0, "risk factors" 1; 0 derived records mention them → the new anchors are ~inert for FinCEN, rf_regions stay byte-identical). COMPLIANCE: extend the verbatim public-domain non-negotiable FinCEN-only → US-federal (17 USC §105 — FinCEN + OFAC + US-federal/Treasury), STILL excluding FINTRAC (Canadian Crown copyright) + any non-US/non-government source; user signed off. ACQUISITION: OFAC's site is a JS SPA with no static listing → hand-curated data/ofac/index.json from /media/<id>/download PDFs (crawl_fincen.py stays FinCEN-only); acquire_fincen.py/pdf_to_md.py reused via --source data/ofac + a _to_pdf_url direct-download tweak; the corpus_status_records issuer parameterized (FinCEN / OFAC). OFAC records may be honestly 0-BUILD_NOW (sanctions/vessel content is non-FI-observable) — never fabricate. Source #3 = a CORPUS_SOURCES registry entry + a new data dir (the Phase-20 registry was purpose-built for this). Decisions: (1) Phase 21 = OFAC source #3 WITH the regression-gated anchor widening; (2) compliance extension FinCEN-only → US-federal (FINTRAC excluded); (3) the widening GATED by ZERO FinCEN regression (29 records clean, --selftest + a new bidirectional OFAC-heading fixture, FinCEN rf_regions byte-unchanged); (4) hand-curated OFAC acquisition + parameterized issuer + honest 0-BUILD_NOW allowed. Tasks: T1 (L) WIDEN the rf_region anchors + parameterize the issuer (the gate change, regression-gated) · T2 (M) OFAC acquisition (hand-curated index.json, _to_pdf_url tweak, convert-one-first checkpoint) · T3 (M) derive the OFAC batch via the inverted loop (--check-derived clean; honest 0-BUILD_NOW) · T4 (S) build.py registry + corpus.html 3-type menu · T5 (S) regen + rebuild + drift guard + extend the harness · T6 (S) docs + the compliance non-negotiable extension. FROZEN/byte-untouched: index.html, config/**, the 3 typology dists, AND the two existing FinCEN sources data/fincen/** + data/fincen-alerts/** (prove source #3 via the MERGE, not a migration). Abort: if the widening shifts ANY FinCEN rf_region or fails ANY of the 29 records (complexity contaminates the core — subtraction test fails), REVERT the widening + fall back to the small-clean-source (red-flag-template OFAC only, no gate change); T2 carries a convert-one-first rf_region CHECKPOINT; NEVER fabricate a BUILD_NOW; blocked >3 attempts → ask user: skip or abort.
[2026-06-07T01:31:21Z] DEBRIEF -- 3 decisions (lite, in _CURRENT_STATE), 1 journal (phase-21-ofac-corpus-source), tasks updated (6/6 [x]), state refreshed, _ARCHITECTURE updated (OFAC source #3 + gate widening), active-phase.md refreshed (delivery gate UNCHECKED)

[2026-06-07T01:58:38Z] PLAN -- Phase 22 (FINTRAC as corpus source #4, first cross-jurisdiction source; gate widened for FINTRAC "indicators" vocab; Crown-copyright non-commercial reproduction, M7) planned, 6 tasks [1 L], 4 decisions (lite -- decisions in _CURRENT_STATE). Direction approved 2026-06-06: add FINTRAC (Canada FIU) as the FOURTH corpus source -- the demo's FIRST move beyond US-federal (US Treasury FinCEN + OFAC -> +Canada FINTRAC). User chose FINTRAC over the 3 documented next-source directions ("this demo is for a Canadian bank after all") + the VERBATIM-corpus path over paraphrase-corpus/paraphrase-showcase. Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core normalize/check_record stays BYTE-UNTOUCHED. What is genuinely NEW vs OFAC (source #3): (1) FIRST cross-jurisdiction source -- a DIFFERENT compliance basis (Crown-copyright NON-COMMERCIAL reproduction LICENCE per FINTRAC Terms & Conditions, explicitly NOT public domain -- the US-federal 17 USC 105 basis is no-copyright-at-all); requires FINTRAC attribution (c His Majesty the King in Right of Canada + complete title + author + "a copy of the version available at <URL>"), NOT commercial redistribution, other non-US/non-FINTRAC still paraphrase (user signed off "proceed"); (2) a gate vocab widening for FINTRAC "indicators" heading (vs FinCEN "red flags" / OFAC "risk indicators"), deliberately NARROW (ML/TF-specific _RF_HEADER_FINTRAC/_RF_INTRO_FINTRAC) to avoid shifting fin-2020-a008 "Financial/Behavioral Indicators" region -- verified 0 collisions across all 36 existing mds; (3) SOURCE-AWARE attribution rendering (FINTRAC must NEVER show the US-federal "public domain" string). Acquisition HAND-CURATED (FINTRAC OAs are HTML pages WITH a PDF version -> data/fintrac/index.json lists the OA PDF-version URLs, reuse acquire/pdf_to_md via --source + the OFAC _to_pdf_url handling; crawl_fincen.py stays FinCEN-only). Decisions: (1) Phase 22 = FINTRAC source #4 via the verbatim-corpus path; (2) NARROW FINTRAC "indicators" anchor widening regression-gated to 0 FinCEN/OFAC shift; (3) compliance extension adds FINTRAC as a Crown-copyright non-commercial reproduction licence (NOT public domain); (4) hand-curated acquisition + source-aware attribution + proof batch ~3-4 OAs spanning ML+TF vocab. Tasks: T1 (L) WIDEN the rf_region anchors (narrow FINTRAC "indicators") + parameterize the issuer (the gate change, regression-gated) * T2 (M) FINTRAC acquisition (hand-curated index.json, OFAC _to_pdf_url reuse, convert-one-first + 0-baseline-shift checkpoint) * T3 (M) derive the FINTRAC batch via the inverted loop (--check-derived clean; FINTRAC Crown-copyright non-commercial provenance, NOT "public domain"; never fabricate) * T4 (S) build.py source #4 + corpus.html 4-type menu + SOURCE-AWARE FINTRAC attribution * T5 (S) regen + rebuild + drift guard + extend the harness * T6 (S) docs + the compliance non-negotiable extension. FROZEN/byte-untouched: index.html, config/**, the 3 typology dists, AND the THREE existing sources data/fincen/** + data/fincen-alerts/** + data/ofac/** (prove source #4 via the MERGE, not a migration). Abort: if the widening shifts ANY FinCEN/OFAC rf_region or fails ANY of the 32 existing records (complexity contaminates the core -- subtraction test fails), REVERT the widening + narrow the anchor; T2 carries a convert-one-first rf_region + 0-baseline-shift CHECKPOINT; NEVER reproduce a FINTRAC doc without its required attribution; NEVER fabricate a BUILD_NOW; blocked >3 attempts -> ask user: skip or abort.
[2026-06-07T02:48:12Z] DEBRIEF -- 0 decision articles (lite), 1 journal (phase-22-fintrac-corpus-source), tasks verified [x], state + architecture + active-phase refreshed; Phase 22 READY FOR COMPLETION (reviewer 9/10 ACCEPT, awaiting commit)

[2026-06-07T03:10:00Z] PLAN -- Phase 23 (FINTRAC depth -- grow source #4 with the remaining anchorable strategic-intel products: Operational Alerts + Operational Briefs, M7) planned, 5 tasks [1 L], 3 decisions (lite -- decisions in _CURRENT_STATE). Direction approved 2026-06-06: grow the existing FINTRAC corpus source #4 (data/fintrac/, doc_type "FINTRAC") from 3 derived Operational Alerts to the full anchorable set of FINTRAC strategic-intelligence products carrying enumerated ML/TF indicators -- OAs AND Operational Briefs (incl. the real-estate brief). "Canadian depth" for the audience (a Canadian bank). NO new source, NO non-negotiable change (the FINTRAC Crown-copyright non-commercial reproduction basis was established Phase 22), NO architecture / build.py / corpus.html STRUCTURAL edit (the menu, doc_type chips, count line are data-driven -- new docs flow through the merge; only harness counts + doc counts update). Reuse the Phase-20 CORPUS_SOURCES registry + the inverted-loop derivation + the quote-grounding gate; the grounding core normalize/check_record stays BYTE-UNTOUCHED -- only the rf_region relevance anchors widen. THE ONE REAL RISK = heterogeneous heading forms: the 3 derived OAs head their lists "Money laundering indicators" (ML/TF-qualified -> anchors under the existing narrow _RF_HEADER_FINTRAC), but the real-estate Operational BRIEF was CONFIRMED this session to head its ~40-indicator list with bare "TABLE OF INDICATORS", which the deliberately-narrow ML/TF-qualified FINTRAC anchor will NOT match -> a REGRESSION-GATED anchor widening per new heading form, each NARROW + SPECIFIC + verified 0-collision across all 36 FinCEN+OFAC mds, every existing rf_region BYTE-UNCHANGED, all 35 existing records --check-derived clean. Honest yield ~5-8 -> FINTRAC source 3 -> ~8-11 (second-deepest source). EXCLUDE FINTRAC's sector GUIDANCE indicator pages (compliance guidance, not strategic-intel -- would blur the corpus identity). Decisions: (1) Phase 23 = FINTRAC depth (OAs + Briefs), user chose FINTRAC depth over cross-corpus synthesis / presentation at the goal gate + OAs+Briefs WITH the regression-gated widening over OAs-only / a source-scoped refactor at the scope gate; (2) gate approach = NARROW-GLOBAL-ANCHOR widening (the proven Phase-21/22 pattern), regression-gated to ZERO FinCEN/OFAC/existing-FINTRAC shift, NOT the source-scoped rf_region refactor (DEFERRED); ABORT = skip-the-doc if a form shifts any existing region, never broaden a shifting anchor; (3) acquisition HAND-CURATED (no FINTRAC crawler; FINTRAC serves <slug>.pdf -> the existing _to_pdf_url branch handles it with NO tweak, Phase-22 confirmed; pdf_to_md provenance already source-aware). Tasks: T1 (M) acquire + convert the candidate batch (human-trafficking oai-hts-2021, OCSE exploitation, romance fraud, illegal wildlife trade, casino/Project-Athena, real-estate real) + per-doc rf_region triage + capture the baseline * T2 (L) the GATE change -- a narrow specific 0-collision anchor per new heading form ("TABLE OF INDICATORS" / an inverted "Indicators of ML/TF" form), regression-gated (35 records clean, --selftest + new fixtures, every prior md byte-unchanged; NO-OP if no new form) * T3 (M) derive the anchoring batch via the inverted loop (--check-derived clean; FINTRAC Crown-copyright non-commercial provenance; honest BUILD_NOW only where FI-observable; never fabricate) * T4 (S) regen + rebuild + drift guard + harness counts + a FINTRAC Brief walking the arc * T5 (S) docs (counts + Operational-Brief inclusion; NO non-negotiable change). FROZEN/byte-untouched: index.html, config/**, the 3 typology dists, scripts/build.py + corpus.html (no structural edit), AND the THREE OTHER sources data/fincen/** + data/fincen-alerts/** + data/ofac/**, AND the 3 EXISTING data/fintrac/ records (grow, don't modify). Abort: if a new heading form shifts ANY existing rf_region or fails ANY of the 35 records, SKIP that doc (honest non-derivable) -- do NOT broaden a shifting anchor (subtraction test); NEVER reproduce a FINTRAC doc without its required Crown-copyright attribution; NEVER fabricate a BUILD_NOW; blocked >3 attempts -> ask user: skip or abort.
[2026-06-07T04:06:52Z] DEBRIEF -- 4 decisions (lite: in _CURRENT_STATE), 1 journal, tasks updated (5 completed), state refreshed; Phase 23 READY FOR COMPLETION; retro TRIGGERED (20 phases); active-phase delivery gate pending commit
