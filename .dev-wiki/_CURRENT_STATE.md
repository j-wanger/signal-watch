# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-27 by /dev-plan (Phase 80 PLANNED) — **Phase 80 PLANNED 2026-06-27 (signal-watch-local, STANDARD; direction gate "Consume P34: merge + workbench" + one-sided fallback "Abort merge → companion + brief").** Consume aml-substrate's one unconsumed emission (Phase 34 @1f5901e — seam-5 sanctions-screening realism: dead `sanctions_flag` made LIVE under `--anchored` via a label-blind real-OFAC-watchlist NAME COLLISION + a revived non-tautological C14). TWO consumes + a queue: (1) merge console — an OFAC watchlist name-collision case class (MEASURE-FIRST gated); (2) workbench §12 — a non-tautological sanctions-driven C14 leg; (3) the substrate P35 brief + cross-pillar true-ups. Phases 32/33 already consumed in the Phase-79 slice; Phase 34's sanctions layer is the delta. A1: `evidence_requirements.py` BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate; 8 non-merge dists byte-frozen; `dist/merge` the ONE sanctioned (gated) re-freeze. OFAC ships clean under 17 USC §105, framed STRICTLY as the false-positive trap. Per-phase history → git log + `.dev-wiki/` journal + index.md (NOT appended here — the maintenance contract). [Phase 79 DELIVERED + accepted 2026-06-27, impl 031a33a / close cd44c6a → journal + index.md.]

## Recommended Next Action

**Phase 80 DELIVERED 2026-06-27 — pending delivery acceptance + the delivery-flow commit (commit + push to main, then flip the delivery gate).** All 5 tasks [x]; the measure-first gate cleared GREEN (no abort — the Phase-34 `--anchored` sanctions emit is genuinely two-sided: 24 candidates, 11 uphold / 13 reject, from the Phase-32 fragment overlay co-occurring with sanctions). BOTH tracks landed: (MERGE) `dist/merge` re-frozen with the OFAC name-collision class as a 3rd scored population (29 anchored + 24 sanctions + 13 synthetic); (WORKBENCH) the §12 sanctions-driven C14 leg lights KYC-A1 + casework SIGNS it (disposition `file`). Exit criteria met: `--check all` 9/9 (8 byte-frozen + `dist/merge` re-frozen), `evidence_requirements.py` BYTE-UNCHANGED (A1), build.py firewall clean, validate↔curate EXACT parity (8-tamper parity-on-broken verified), `uv run pytest` 27, `node tests/merge-console.test.mjs` 88/0, `node tests/workbench.test.mjs` 169/0, curate selftest (29/24/13, 10 broken fixtures). Adversarial review (3 dims — honesty/firewall/drift, opus reviewers running the commands): ZERO findings. After acceptance + commit, run `/dev-plan` for the next phase (the frontier is now sibling-rooted: substrate P35+ `docs/substrate-p35-determination-signals-PLAN-BRIEF.md`).

**After Phase 80, the frontier is fully sibling-rooted** (per `docs/cross-pillar-build-order.md`): substrate P35+ (TF slice / broader C7 / org-name sanctions / Stage-2/3 open data — NOT yet built, briefed in T5); the naive-resolver over-merge quadrant; casework's CI-promotion criterion; the Lakeshore IND-05/IND-02 C3-grounding faithfulness fix. Decisions: [[decisions/phase-80-merge-plus-workbench-consume]] · [[decisions/phase-80-sanctions-oracle-measure-first]]; plan [[phases/phase-80-consume-substrate-sanctions-screening]]; ledger Phase-80; spec `specs/phase-80-consume-substrate-sanctions-screening.md`.

## Active Phase

**[[phases/phase-80-consume-substrate-sanctions-screening|Phase 80 — Consume substrate Phase 34: OFAC name-collision merge case class + non-tautological C14 §12 leg]]** (status: DELIVERED + accepted 2026-06-27)

Entry criteria: MET — Phase 79 DELIVERED + accepted 2026-06-27 (impl 031a33a / close cd44c6a; all 9 dists settled, `dist/merge` at 29 substrate-scored + 13 synthetic-scored). Both sibling HEADs code-verified LIVE this planning session: aml-substrate f2da3e4/c099259→**1f5901e** (Phase 34 — seam-5 sanctions-screening realism; Phases 32/33 already consumed in the Phase-79 slice), aml-casework **076fb8e** (Phase 19 — at the Phase-79 vendor pin, nothing new to consume; one drift correction: its txn-less C14 party-leaf is no longer fails-closed).
Exit criteria: T1 capture committed + no-substrate replayable + the two-sidedness decision documented. IF two-sided: `--check all` 9/9 (8 byte-frozen + `dist/merge` re-frozen with the sanctions-name-collision basis); `node tests/merge-console.test.mjs` green incl. the new basis; validate↔curate parity; honesty word-ban held. IF one-sided/flaky: `dist/merge` BYTE-FROZEN + a substrate emit-two-sidedness brief. A sanctions-driven C14 case in the workbench; the §12 kyc leg lights; `node tests/workbench.test.mjs` green; gather/workbench harnesses pass; `git diff --quiet scripts/evidence_requirements.py` (A1). The substrate P35 brief exists; cross-pillar-build-order trued up; the casework C14-fails-closed drift corrected; FOLLOW-ON markers reconciled; `uv run pytest` green.

Progress: 0% (PLANNED 2026-06-27, STANDARD — 5 tasks [T1 L, T2–T4 M, T5 S]; the one L = T1, the measure-first abort gate; T2/T3 gated on T1 two-sided, T4/T5 run regardless).

Phase 80 consumes aml-substrate's one genuinely-unconsumed emission — **Phase 34 @1f5901e** (seam-5 sanctions-screening realism: the dead `sanctions_flag` made LIVE under `--anchored` via a label-blind real-OFAC-watchlist NAME COLLISION + a revived non-tautological C14 KYC-integrity branch). **(1) MERGE CONSOLE (measure-first gated):** an OFAC watchlist name-collision case class — a synthetic party's real-frequency name collides with a real public-domain OFAC entry → *same latent entity (uphold the link) vs common-name false positive (reject)* — the canonical sanctions-screening entity-resolution decision, landing in the one gate with an oracle. T1 measures two-sidedness BEFORE any dist touch; ONE-SIDED/flaky → ABORT the merge track (T2/T3) to workbench-only + a substrate emit-two-sidedness brief. **(2) WORKBENCH §12 (always, companion-only):** a non-tautological sanctions-driven C14 lights the kyc determination leg (additive + provenance-tagged to the v0.5 slice; the C14→kyc path already exists from Phase 72). **(3) QUEUE + TRUE-UPS:** the substrate P35 handoff brief (TF/C7/org-sanctions/Stage-2-3 — NOT yet built) + cross-pillar-build-order + the casework C14-fails-closed drift + the stale FOLLOW-ON markers. **THE FIREWALL:** build.py imports no spine/scorer/sibling/curate; the 8 non-merge dists byte-frozen; `dist/merge` the ONE sanctioned (gated) re-freeze; validate↔curate parity. **THE A1 GUARD:** `evidence_requirements.py` BYTE-UNCHANGED. **COMPLIANCE:** OFAC ships clean under 17 USC §105 (US-federal public domain — covers OFAC), framed STRICTLY as the false-positive trap (synthetic party ≠ sanctioned entity); badge always-on.

[[phases/phase-79-consume-sibling-emissions|Phase 79]] is **COMPLETE** (Lakeshore `cleared` co-sign via fan-in C3 + the merge supersede — 29 substrate-scored + 13 synthetic-scored, split by oracle provenance). [[phases/phase-78-consume-disposition-validation-control|Phase 78]] is **COMPLETE** (the determination-validation harness [the "circularity exit"] + the §12 discovery-feed control). [[phases/phase-76-merge-adjudication-console|Phase 76]] is **COMPLETE** (the 6th SHIP artifact `merge.html`→`dist/merge/`; `--check all` 9 targets). [[phases/phase-73-rich-investigation-case-live-workbench|Phase 73]] is **COMPLETE** (the matched Northgate/Lakeshore pair; the affirmative-`cleared` clear path). [[phases/phase-69-evidence-sufficiency-filing-control|Phase 69]] is **COMPLETE** (`scripts/evidence_requirements.py` — the byte-frozen engine Phase 80 must not touch).

## Active Phase Contract

Phase: 80 — Consume substrate Phase 34: OFAC name-collision merge case class + non-tautological C14 §12 leg. signal-watch-local **ACTIVE — PLANNED 2026-06-27**, STANDARD (cross-pillar + a conditional ship-dist touch; the one L = T1, the measure-first abort gate). Transition: continue. Abort: if blocked >3 attempts, ask user: skip or abort. Objective: consume aml-substrate's one unconsumed emission (Phase 34 @1f5901e — seam-5 sanctions-screening realism) — (1) an OFAC watchlist name-collision MERGE case class (measure-first gated); (2) a non-tautological sanctions-driven C14 §12 leg in the workbench; (3) the substrate P35 brief + cross-pillar true-ups. Direction = the user's "Consume P34: merge + workbench" + the one-sided fallback "Abort merge → companion + brief" (AskUserQuestion 2026-06-27). **A1 [accept]:** `evidence_requirements.py` BYTE-UNCHANGED. **The firewall [accept]:** build.py imports no spine/scorer/sibling/curate; the merge `oracle` rides a post-disposition block (`assert_no_*_leak`); validate↔curate EXACT parity (Phase-76). **The measure-first gate [T1, the abort rule]:** the anchored sanctions emit won't reproduce after bounded attempts / the oracle is one-sided/tautological → STOP the merge track (T2/T3) to workbench-only + a substrate emit-two-sidedness brief; T4 + T5 run regardless. **COMPLIANCE:** OFAC ships clean under 17 USC §105 (covers OFAC), framed STRICTLY as the false-positive trap (synthetic party ≠ sanctioned entity). **HONESTY:** the synthetic-substrate-anchored qualifier; NO catch-rate/lift/precision/recall; the always-on badge.
FROZEN (signal-watch, held this phase): the 8 non-merge ship dists byte-identical (`--check all`); `dist/merge` the ONE sanctioned re-freeze, GATED on T1's two-sided result (untouched on abort). Phase 80 adds `tests/fixtures/merge-sanctions-oracle/**` + extends `resolution_scorer.py`/`curate_merge_cases.py` (the sanctions oracle); conditionally re-freezes `data/merge/cases.json` + `dist/merge/**` + `merge.html` + `tests/merge-console.test.mjs` + build.py `validate_merge_cases`; extends `curate_workbench_cases.py`/`serve_workbench.py` + `data/workbench/**` + `tests/workbench.test.mjs` (the C14 leg, additive); authors `docs/substrate-p35-*-PLAN-BRIEF.md` + trues up `docs/cross-pillar-build-order.md` + `CLAUDE.md` + reconciles the `.dev-wiki/tasks.md` FOLLOW-ON markers. `build.py` imports NO spine/scorer/sibling/curate (a grep guard); `evidence_requirements.py` BYTE-UNCHANGED (`git diff --quiet`); validate↔curate firewall parity (Phase-76) holds; the workbench/chain/news/merge arcs must not regress.
Program context — Phase 50 — AML program build: the data-substrate pillar (spun out). The real build lives in /Users/jwang/aml-substrate (Python) + /Users/jwang/aml-casework; signal-watch is the program-ARCHITECTURE home (docs/program-blueprint.md) + this lifecycle record + the demo artifacts. aml-substrate/aml-casework facts drift between reviews — both sibling HEADs were code-verified LIVE this planning session (substrate **1f5901e**/Phase 34, casework **076fb8e**/Phase 19), file:line, never reasoned from loaded facts.
Abort: any UNSANCTIONED ship dist drift (the 8 non-merge dists, or `dist/merge` before its T1/T2 gate passes) / a build.py spine-or-scorer-or-sibling-or-curate import / an `evidence_requirements.py` change / a real OFAC name framed as a real sanctions catch (not the false-positive trap) / a confusion number presented as a catch-rate or lift or precision or recall → STOP-and-surface. Merge track (T1): the anchored sanctions emit won't reproduce after bounded attempts / one-sided / tautological → STOP the merge track (T2/T3) to workbench-only + a substrate emit-two-sidedness brief; deliver T4 (the workbench C14 leg) + T5 (the P35 brief + true-ups) + the honest non-result.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| [[decisions/phase-80-merge-plus-workbench-consume]] Phase 80 — consume substrate Phase 34's sanctions-screening realism as a FULL bundle (merge + workbench): an OFAC watchlist name-collision MERGE case class + a non-tautological sanctions-driven C14 §12 workbench leg + the substrate P35 brief. Direction gate "Consume P34: merge + workbench" (AskUserQuestion 2026-06-27). Rejected: workbench-only (the merge sanctions-collision is the audience-recognizable payoff in the one gate with an oracle); bank-and-queue (the last ripe local consume — take it now, queue the P35 frontier). | medium | 2026-06-27 |
| [[decisions/phase-80-sanctions-oracle-measure-first]] Phase 80 — the merge consume is MEASURE-FIRST: T1 assesses two-sidedness of the Phase-34 `--anchored` sanctions oracle BEFORE any `dist/merge` touch; ONE-SIDED (all common-name false positives, no true watchlist matches — the "label-blind" risk) → ABORT the merge track to workbench-only + a substrate emit-two-sidedness brief (the Phase-77 abort rule; user-positioned 2026-06-27). Rejected: ship a one-sided "oracle" framed explicitly (a one-sided oracle is a label, not a measurement — the Phase-77 trap). | medium | 2026-06-27 |
| [[decisions/phase-79-merge-supersede-substrate-scored]] Phase 79 — the merge console SUPERSEDED the consensus-66 with 29 substrate-anchored SCORED cases (the user's "supersedes" call). Substrate data was never production-real (synthetic-substrate); it merely LACKED an independent oracle until the Phase-32/33 `--anchored` fork minted a non-circular `GT-<hash>` truth where `entity_ref ≠ cluster`. Both populations now scored, split by oracle PROVENANCE (not consensus-vs-scored). Rejected: keep the 66 as a 3rd consensus population (an independent oracle now exists). | high | 2026-06-27 |
| [[decisions/phase-79-lakeshore-honest-cosign]] Phase 79 — Lakeshore co-signs `cleared` via window-compression (8 catering credits → a ≤7-day window; only dates tighten, the fan-in is REAL) + an IND-02-grounded `case-b.bundle.json` (case.json keeps IND-05 for matched-pair symmetry, disclosed). The matched pair now BOTH route through casework. A1 held. | high | 2026-06-27 |
| [[decisions/phase-79-floor-plus-gated-upside-bundle]] Phase 79 — floor + gated-upside bundle for the two unblocked sibling consumes; the Lakeshore floor guarantees phase value even if the merge track aborts. Direction gate "Bundle, gated upside" (AskUserQuestion 2026-06-27). Rejected: (a) Lakeshore-only; (b) commit to the dist/merge re-freeze up front. (Outcome: both tracks landed.) | high | 2026-06-27 |
| [[decisions/phase-79-merge-measure-first-before-dist]] Phase 79 — the merge real-data consume is measure-first companion-only before any dist re-freeze; gate the `dist/merge` re-freeze on a clean two-sided result, the Phase-77 abort rule governing. (Outcome: the gate cleared GREEN — the emit reproduced clean, the ReplayError was orthogonal `--monitor`/`--emit-evidence`; T4 ran.) | high | 2026-06-27 |
| [[decisions/phase-78-bundle-only-non-circular-validation]] Phase 78 — score the BUNDLE-ONLY signal structure (mechanism + ≥2 legs), not the human-gated verdict — the non-circular validation frame. The human-gate inputs HELD OUT + named as the boundary, NEVER derived from the oracle basis; the oracle never enters an engine input (`assert_no_oracle_leak`). A per-class confusion structure, not an accuracy. | high | 2026-06-26 |
| [[decisions/phase-78-measure-then-control-discovery-feed]] Phase 78 — pivot the measurement into a CONTROL: the §12 discovery feed over the disagreement cases. The *missed* (§12 gap) + *over-flag* (defensive exposure) cells surface as a live analyst gather/build queue in serve_workbench; presentation-only (the Phase-74 priors-are-provenance precedent); `evidence_requirements.py` BYTE-UNCHANGED. | high | 2026-06-26 |

Older decisions (Phase 73–77 — invert-pillars, the entity spine, the merge console, the three-consumes split) live in `articles/decisions/` + index.md.

## Blockers and Open Questions

- [RESOLVED 2026-06-27 · planning, by the Phase-80 gate + the 2 decisions] The Phase-80 direction (how to proceed after Phase 79 — "double check sibling work to see where things are") closed at the gate. Both sibling HEADs code-verified LIVE this session ([[cross-pillar-review-verify-sibling-repo]]): aml-substrate advanced to **1f5901e** (Phase 34 — seam-5 sanctions-screening realism; Phases 32/33 already consumed in the Phase-79 slice, verified by the "Anna Vega"/"Anna eVga" fragment pairs in `data/merge/cases.json`); aml-casework sits at **076fb8e** (Phase 19 — the Phase-79 vendor pin, nothing new). Phase 34's sanctions layer is the ONE unconsumed emission; everything else is substrate P35+ (TF/C1[a measured null]/broader-C7/org-sanctions/Stage-2-3 — NOT yet built). DECISION: a full consume (merge + workbench) — the OFAC watchlist name-collision MERGE case class (MEASURE-FIRST gated; one-sided → abort to workbench-only + brief, user-positioned) + the non-tautological sanctions-driven C14 §12 workbench leg + the substrate P35 brief. Decision articles [[decisions/phase-80-merge-plus-workbench-consume]] / [[decisions/phase-80-sanctions-oracle-measure-first]] (medium, source plan); plan [[phases/phase-80-consume-substrate-sanctions-screening]]; ledger Phase-80; spec `specs/phase-80-consume-substrate-sanctions-screening.md`. DRIFT corrected: casework's txn-less C14 party-leaf is NO LONGER fails-closed (completes via the resolving party leaf, test reaches `signed`) — CLAUDE.md's Phase-72 "kyc signing is the txn-contract frontier" note is partially stale (T5 corrects it). (raised + resolved 2026-06-27)
- [RESOLVED 2026-06-27 · planning, by the Phase-79 gate + the 2 decisions] The Phase-79 scope questions (which consume(s) / sequence · the merge-oracle honesty re-check · the dist boundary · re-vendor staleness · ceremony) closed at the direction gate ("Bundle, gated upside" via AskUserQuestion; all_accept warned-not-silent). Both Phase-77-deferred blocks verified RESOLVED sibling-side this session (file:line, [[cross-pillar-review-verify-sibling-repo]]): casework b3546d4→**076fb8e** (Phase 19 `_c3_fan_in`); substrate f2da3e4→**c099259** (Phase 33; the Phase-32 anchored fork @**31cb439** mints `entity_ref ≠ cluster` fragments — the circular-oracle cure). DECISION: a floor + gated-upside bundle — the Lakeshore fan-in floor committed (companion-only); the merge real-data oracle measure-first companion-only, the `dist/merge` re-freeze GATED on a clean two-sided result (the live `--anchored --emit-eval-oracles` run CRASHED today → measure-first de-risks both the emit and the dist boundary; the Phase-77 abort rule governs). Decision articles [[decisions/phase-79-floor-plus-gated-upside-bundle]] / [[decisions/phase-79-merge-measure-first-before-dist]] (medium, source plan); plan [[phases/phase-79-consume-sibling-emissions]]; ledger Phase-79; spec `specs/phase-79-consume-sibling-emissions.md`. (raised + resolved 2026-06-27)
- [RESOLVED 2026-06-26 · planning, by the Phase-78 gate A1–A5] The Phase-78 direction (consume substrate Phase 31's CLI-wired disposition oracle) closed all-accept: (Q1) bundle-only non-circular score frame; (Q2) measure→control (the §12 discovery feed). T2 measure-first PASSED non-degenerate. Ledger Phase-78. (raised + resolved 2026-06-26)
- [RESOLVED 2026-06-26 · planning, by the Phase-77 gate] Consume the three sibling emissions — 1 landed (casework `cleared` via a C5 proxy), 2 deferred to named briefs (both NOW resolved sibling-side and consumed in Phases 78–79). Ledger Phase-77. (raised + resolved 2026-06-26)
- (Older resolved-planning blockers Ph71–Ph76 archived to their ledger blocks + decision articles + index.md.)
- [RESOLVED 2026-06-16 · Phase 54] CONTROL STORY for the measured-not-gated dimensions — the production-grade SR-11-7-class control DESIGN (cadence, sampling, ownership, where it sits in the 5% charter), named at Phase-47 T2/T3 but not written, is now **written + executable for the C/D tag**: `docs/cd-tag-control.md` (SR-11-7 Pillar-2 + OSFI E-23 grounded; scope/risk-tier/baseline/cadence/trip-wires/independent-challenge/ownership/escalation/audit-walk) + `cd_correctness.py --control-check`/`--control-freeze` (PASS on the frozen corpus, BREACH on injected drift). The first concrete instance; the reusable program-wide control PATTERN (other judgmental dims) + a human-rater panel stay deferred-with-owner. (raised 2026-06-12; resolved 2026-06-16)
- [OPEN · resolves at Phase-47 T3] HONESTY→EFFECTIVENESS measurement layer — the validation story (no-ground-truth doctrine, drift/ongoing-monitoring per SR 11-7 Pillar 2, what survives of the always-illustrative posture) gets its EXPLICIT designed-now vs deferred-with-owner disposition in T3; the committed quality-harness baseline (--check/--freeze) is the named embryo. (raised 2026-06-12)
- [RESOLVED 2026-06-10 · planning, by the Phase-46 gate D1–D4] The four Phase-46 planning questions closed at the dev-plan gate: TIMING + DIRECTION → the user REFRAMED off all 5 DEFERRED candidates (the 5th reframe in 6 gates) to **corpus live derivation mode** ("live mode with local model for the corpus demo as well, ideally with a better harness integration like opencode" + a pasted practitioner report) — a feedback-robust back-end/live-layer direction; post-presentation audience feedback surfaces against the active phase before T2 if it lands. The IF-(third-jurisdiction) AUSTRAC/UK derivation-surface research gap and the IF-(residuals) mojibake-repair measurement (fresh 2026-06-10: 161 derived + 501 md marker chars, below the carried ~2.2K; repair needs a conscious unfreeze) travel with their still-deferred candidates in the DEFERRED list above. Assumption gate closed 2026-06-10, all_accept: TRUE (A1–A4); decision article articles/decisions/phase-46-corpus-live-derivation.md; ledger block in assumption-ledger.md. KNOWLEDGE GAPS carried to impl: whether the FINTRAC /intel/ frontier doc parses under the existing rf_region anchors (T1 exercises it; extension REGRESSION-GATED); whether opencode + the existing Qwen can drive a reliable tool-calling agent loop at all (T1's whole job); full-advisory derivation wall-time at local-model speed (T1 measures — may force a short-doc demo shape or per-indicator staged reveal).
- [RESOLVED 2026-06-10 · planning, by the Phase-45 gate A1–A5] The five Phase-45 planning questions all closed at the assumption gate (all_accept: false — A1 don't-know round 1 → defended with worked real-value examples → accepted): LIFT SHAPE → R2 honest real-inventory counts (no performance claim, disclaimer removed, R1 zero-numbers copy-only fallback) [A1]; SHOWCASE CONSISTENCY → Act-5 byte-frozen, deliberate cross-artifact divergence accepted [A2]; INCONSISTENCIES INVENTORY → already measured by the two pre-gate review agents (findings ranked in articles/decisions/phase-45-corpus-presentation-polish.md; the gate fix is COPY + stagecraft, no interaction redesign [A3]); DELIVERY-REVIEW ARTIFACT → corpus.html copy edits + presenter/demo-path notes into tests/smoke-checklist.md [A4 attribution extension + A5]; FEEDBACK ABSORPTION → T5 user-walkthrough FREEZE checkpoint, dispositions ranked + time-boxed, overflow explicitly deferred [A5]. Decision article approved; ledger block in assumption-ledger.md.
- [RESOLVED 2026-06-10 · planning, by Phase-44 D1–D5] Phase-44 DIRECTION + the conditional IF-… sub-questions — the user REFRAMED off the offered candidates at the dev-plan gate (the 4th reframe in 5 gates): Phase 44 = LIVE EXTRACTION QUALITY — red-flag RECALL (obvious high-risk-country wire flags missed in limited real testing) + alias-assignment PRECISION (entities assigned aliases clearly not theirs) + a FRESH dedicated processing page + the user's own harness hypothesis accepted as the T1 frame + SPEED resolved as quality-gated T4. Measure-first/classification-first (D2: committed targeted harness w/ alias-OWNERSHIP scoring + synthetic wire notes embedding the user's sample sentences, local-only; both failures reproduced + classified before any fix); fixes through proven seams (D3: prompt-regression gate + the news_ground regate procedure; structural EXTRACT_SCHEMA change = a surfaced finding); quality-gated speed (D4); LIVE-region-only UI + lossless hygiene (D5). The IF-(hygiene) and IF-(verify-latency) sub-questions were ABSORBED into Phase 44 (T6/T4); the IF-(FINTRAC /intel/)/(third jurisdiction)/(fuzzy-merge)/(bulk scan) sub-questions travel with their candidates in the DEFERRED list above. Assumption gate closed 2026-06-10: A1 accept-with-conditions (sample sentences; speed → quality-gated T4), A2 accept, A3 don't-know→defended→accept, A4 accept; ledger block in assumption-ledger.md; decision article articles/decisions/phase-44-live-extraction-quality.md. KNOWLEDGE GAPS carried to impl: whether the user's wire-miss material reproduces (T1's job); the actual failure classes (T1); whether batched verify preserves quality (T4).
- [RESOLVED 2026-06-10 · planning, by Phase-43 D1–D5] Phase-43 DIRECTION + the conditional IF-(a)…(e) sub-questions — the user REFRAMED off all five carried candidates at the dev-plan gate (none picked; all stay in the DEFERRED list above, their open sub-questions traveling with them): Phase 43 = LIVE PIPELINE ROBUSTNESS + PROGRESSIVE PRESENTATION — a live test on a REAL investigation note timed out ~200s with NO intermediate feedback; extraction must work regardless of article size/complexity, and the page must show staged progress instead of an opaque wait. Measure-first (D2: the stress harness classifies read-timeout vs finish_reason=length truncation vs context overflow [/props n_ctx] vs intrinsic degradation; the ~200s failure class must be reproduced); rendering = stage-completion-driven, corpus-demo-style (D3 — A2' after a reject-by-reframe round 1: NO partial-JSON parsing, NO visible token stream; internal stream:true serves only the timeout fix + a token counter); scope = pure transport (D4: gate/prompt/schema/store-writes/fixtures FROZEN, call_llm signature preserved — the fixture seam); privacy (D5: the real note never committed, stress notes synthetic, commercial captures local-only, fixtures US-federal-only). Assumption gate closed 2026-06-10: A1/A3/A4 accept, A2 reject-by-reframe→A2' accept; ledger row in assumption-ledger.md; decision article articles/decisions/phase-43-live-pipeline-robustness.md. KNOWLEDGE GAPS carried to impl: the server's actual n_ctx (measured at T1); whether stream:true composes with the json_schema strict grammar in the user's llama-cpp build (verified at T2); the actual failure class (T1's whole job).
- [RESOLVED 2026-06-10 · planning, by Phase-42 D1–D5] Phase-42 DIRECTION + the conditional IF-(a)…(f) sub-questions — the user picked (a) anchor dossier view + a per-scan network visualizer, with the (c) CLAUDE.md trim bundled as T6. The IF-(a) sub-question resolved IN the design: a surfaced CONFLICT = same anchor + same kind + different value (the both-kept rows per Phase-41 D2; presentation-only flag, never auto-resolve); the view = a `GET /anchor` companion route + a news.html LIVE-region panel (the watchlist-panel pattern, NOT a separate page), the per-scan graph as the navigation surface. The IF-(b)/(d)/(e)/(f) candidates stay in the DEFERRED list above (their open sub-questions — Python-side fuzzy scorer port-vs-move + reversible merge mechanics; rf_region anchor coverage for the /intel/ SBs; the third licence basis; bulk-scan wall-time/deliverable — travel with them). Assumption gate closed 2026-06-10: A1/A3 accept, A2 don't-know→A2' accept (evidence check: the 7 fixture articles are case-disjoint, zero cross-article entity overlap → a committed SYNTHETIC investigation-note seed replaces any re-scan-different-articles script), A4 accept-with-condition (SVG is the INITIAL implementation for demo testing), A5 scope constraint (pure consumption); ledger block in assumption-ledger.md; decision article articles/decisions/phase-42-anchor-dossier-network-view.md.
- [OPEN · planning, gated at Phase-38 T1 CHECKPOINT] Phase-38 QWEN-VS-CLAUDE DIVERGENCE ADJUDICATION — the Half-A evaluation reports a Qwen-vs-Claude INTER-RATER AGREEMENT + the actual divergences (which entities/flags one rater caught and the other missed) over the committed DOJ/OFAC articles. The Claude reference is a SECOND rater, NOT ground truth; the user adjudicates the divergences at the T1 checkpoint before the goldens lock. NO accuracy/precision/recall number is presented as real; the always-on badge stays. Resolved at the checkpoint (T1), not blocking the capture itself.
- [OPEN · planning, gated at Phase-37 T1] Phase-37 TYPOLOGY MODEL — single primary typology per indicator vs a multi-label SET vs an honest indicator-level "sector-general" bucket. The T0 risk: many sector-page indicators are genuinely CROSS-CUTTING (structuring serves drug-trafficking AND fraud AND TF), so forcing one typology may merely RELOCATE the catch-all to indicator level. The T1 measure-first probe (section→typology determinism + the cross-cutting fraction over the ~589 unique texts) decides this AT A CHECKPOINT with the user before any tagging; also open at the checkpoint: whether the 3 specific sector pages (casinos/real-estate/VC) fold into per-indicator tagging for consistency, and which closed-vocab EXTENSIONS (if any) are grounded. Do NOT tag before the checkpoint lands.
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- [CANDIDATE · future phase, raised 2026-06-08 by the user] **SearXNG-backed authoring search/fetch.** A self-hosted SearXNG meta-search + readability content-extraction capability for the BUILD-TIME authoring pipeline (corpus + news acquisition) — more robust than WebFetch/`curl`, which hit bot-protection on gov sites this phase (DOJ 403; we routed around it via the Wayback Machine). Already prototyped in the sibling `nanaclaw` project (Phase 5: `SEARXNG_URL` service + `web_search`/`web_extract` MCP tools); not currently running here (localhost:8888 refused). Authoring-only — NEVER in the ship artifact (offline non-negotiable). A future-phase candidate, NOT this phase.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)
- (Resolved pre-Phase-42 planning entries — ~20, each multi-KB — archived VERBATIM to [articles/state-archive-resolved-blockers.md](articles/state-archive-resolved-blockers.md) by the Phase-44 T6 hygiene trim; each is also in its decision article + journal.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html + config/{schema.md, typologies/*.json} | Six-act showcase engine (`__CONFIG__` injection, M3 keyboard nav) + content-model contract + 3 hand-curated typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary; Phase 20 `CORPUS_SOURCES` registry merges per-source manifests + derived → dist/<id> + dist/corpus; Phase 24 added `jurisdiction` per source + `load_typology_map`/`validate_typology` (build-boundary typology gate) + merges typology/jurisdiction into `__CORPUS__`; Phase 25 added `_inline_article`/`_strip_provenance` (inlines each live doc's full source-md body into `__CORPUS__`) + a re-declared `red_flag` SHAPE boundary check in `validate_corpus_data`; **Phase 47** added `load_console_cases`/`validate_console_cases` + the `console` target wired into `all`/`--check`; **Phase 49** added `TRIAGE_*` + `load/validate/render/build/check_triage` (4 tamper + 2 drift classes fail-loud; probe-history NEVER read) + the `triage` target; **Phase 76** added `MERGE_*` + `load_merge_cases` + a STANDALONE `validate_merge_cases` (closed adjudication vocab + referential integrity + the resolver-input firewall `MERGE_TRUTH_LEAK_KEYS` + the consensus/scored honesty split) + `render/build/check_merge` + the `merge` target — **`all`/`--check` now 9 artifacts** (the 8 existing byte-frozen except the sanctioned launcher cascade); build.py imports NO spine/scorer/sibling/curate | 2026-06-25 |
| data/typology-map.json | Phase 24 NEW cross-corpus typology OVERLAY (22-term closed vocab + 42-entry doc-id→typology map; jurisdiction from the source registry, not stored). A SEPARATE committed file — the source dirs + derived records stay byte-frozen; validated fail-loud at the build boundary | 2026-06-07 |
| scripts/{derive_signals,crawl_fincen,acquire_fincen,pdf_to_md}.py | Authoring pipeline (stdlib) — quote-grounding gate (source-AGNOSTIC, reused unchanged) + `--corpus[-status] [dir]`/`--check-derived`/`--selftest`; Ph20 generalized: `--alerts` + `parse_alerts` (zero-hop) + `--source <dir>`; Phase 25 added an ADDITIVE `red_flag` SHAPE check to `check_record` (grounding normalize/rf_region/flag⊂md BYTE-UNCHANGED) + 3 `--selftest` fixtures | 2026-06-07 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (2.15MB; 46 publications, 42 derived live = 12 adv + 17 alert + 3 OFAC + 10 FINTRAC; unified menu, Advisory/Alert/OFAC/FINTRAC chips). Phase 24 Documents/Typologies toggle + synthesis view; Phase 25 the per-doc "Read advisory" screen + the `red_flag` label through the 6-screen arc. **Phase 27 (shippability)**: `cleanArticle()` markitdown-sanitizes the displayed source (form-feeds / running headers / tab-soup; display-only, source md untouched); `highlightArticle` rewritten to normalize-both-sides + index-map (634/634 = 100% highlight); the Signal build-log ports the Act-4 `.run` working-pulse in a proposal grid + a combination-lift lift-side panel (firestat OMITTED); the progressive read types the WHOLE article (no 1600 cap, length-scaled ~6s) | 2026-06-07 |
| news.html + dist/news/index.html | M8 adverse-media stream (third ship artifact). Arc: Select → Read → Screen (real Jaro-Winkler, 0.85) → Disposition (human gate; deterministic SVG subject network + anchor dossier in LIVE mode) → Exposure. OFFLINE ship = static book only; ALL live client code in `/*LIVE_START*/…/*LIVE_END*/`, build-stripped (dist/news byte-identical). Phase 43 added the stage-completion LIVE preview (grounded flags FINAL + provisional chips + token counter — never a token stream); Phase 44 moved it onto a DEDICATED PROCESSING PAGE (in-page takeover, presenter keys guarded, Esc arm→abandon) and made grounding WRAP-TOLERANT (locate_span requote) + folds ambiguity-refusing/type-matched. Harness `tests/news-stream.test.mjs` (150, both motion modes + live overrides + strip assertion). | 2026-06-10 |
| scripts/{serve_news,news_ground,news_store,news_fetch}.py | The LIVE news companion (dev/authoring-time only; offline ship unaffected): serve_news = localhost page + llama-cpp proxy + NDJSON stage-stream `/extract` (Phase 43: call_llm STREAMS SSE w/ idle-gap 120s timeout, MAX_GEN_TOKENS 16384, failures NAMED in-stream, preflight_size vs /props n_ctx FAIL-OPEN, single-flight 409, disconnect-persists-nothing) + `/watchlist[/prune]` + `/anchor`; news_ground = the SHARED grounded-or-stripped gate (the one allowed build→companion import; vocab single-authority); news_store = gitignored DuckDB ANCHOR store (.venv-only; build.py never imports it); news_fetch = URL fetch ladder + standardizer + article-shape verifier. Replay: 13 US-federal fixtures (`tests/fixtures/news-live/`, FIXTURE_META allowlist), goldens deterministic from pinned captures; `tests/news_live_test.py` (system + .venv + --live). | 2026-06-10 |
| data/news/ | REAL US-federal gov-enforcement articles (DOJ/OFAC, verbatim 17 USC §105) `articles/*.md` + grounded `derived/*.json` + SYNTHETIC `book.json` (exact + near-matches + a common-name trap; non-negotiable #4). Phase 42 added the committed SYNTHETIC demo seed `docs/demo-investigation-note.md`. | 2026-06-10 |
| data/fincen/ + data/fincen-alerts/ + data/ofac/ + data/fintrac/ | Source #1 `fincen-advisories` (14 md + 12 derived) + #2 `fincen-alerts` (19 md + 17 derived) + #3 `ofac-advisories` (3 md + 3 derived, US-federal 17 §105) + #4 `fintrac` (10 md + 10 derived, Ph22/23 — Crown-copyright non-commercial); each {*.md, derived/*.json, corpus-status.json}; raw/ gitignored. Phase 25 added a natural `red_flag` per indicator. **Phase 27** tightened 121 over-long verbatim `flag`s to crisp contiguous SUB-SPANS (faithfulness-guarded; 36 long-but-faithful kept whole) across 33 derived records + re-translated fin-2022-a001's 2 red_flags — byte-surgical (flag/red_flag VALUES only); mds + every corpus-status.json + the grounding core byte-frozen | 2026-06-07 |
| docs/program-blueprint.md + specs/phase-47-*.md + specs/phase-48-*.md | M9 design deliverables: the program blueprint §1–§15 (Phase 47 §1–§12: universal grounding spine, 6-workload substrate/verifier table [6th row Phase 48], G/M/J/A gate taxonomy, dual-stream human charter, probe-rule agentification, E-23×SR-11-7 control mapping, honesty disposition table, §11 capability roadmap; Phase 48 §12–§15: history-utilization [3 roles w/ substrate+verifier, "history is evidence, never ground truth"], LFCM library-not-monolith, continuous adjudication loop [params chosen-not-measured], probe citation) + the two spec contracts | 2026-06-12 |
| scripts/corpus_redundancy.py + data/corpus-redundancy/equiv-sample.json + docs/corpus-redundancy-report.md | **Phase 51 (2026-06-16)** — the measurement workstream's first MEASURED result (the §13 fm-1 frontier). NON-ship, stdlib, read-only over the frozen corpus. T1 deterministic cross-regulator co-occurrence UPPER BOUND (≤0.325, over the corpus's own typology/capability/agency labels) + T2 sampled consensus-class semantic-equivalence (24 single-rater blind judgments, integrity-verified; est genuine ~1.4% strict / ~11.5% band) + the non-ship report. `--selftest` GREEN; `--check all` 7/7 zero drift. FINDING: the corpus's no-dedup stance empirically vindicated | 2026-06-16 |
| docs/blueprint-sales-pitch.md + docs/governance-throughput-model.md | Program GTM + sustainability artifacts (2026-06-16, post 4-angle adversarial critique). Pitch v2: structural-disease→architectural-cure, honesty weaponized, the "what we do not claim yet" list (composition detection-lift now RETIRED per the triple-null). Throughput model: the G/M/J/A taxonomy AS the cost model (~2 FTE@5k / ~4–5@10k, conditional on three measurable params) — answers the per-signal-as-a-model lifecycle-burden concern with arithmetic, not assertion. NON-ship, honesty-disciplined | 2026-06-16 |
| docs/pillar-integration-contract.md | Pillar 1→Pillar 2 cross-pillar data contract (v0.1 DRAFT, 2026-06-16; references the FROZEN blueprint, not an edit). Grounded at aml-substrate HEAD 0daa3cc: the reference-by-path grounding spine (Alert→Dossier→STRRecord, replay-verified), the PROPOSED serialized record schemas + minted ids, the grounded NETWORK-STRUCTURE signal fix (composition's 2nd axis), honesty caveats (case-grouping oracle-only, derived ids, regenerate), the persist-in-Pillar-1 seam, what Pillar 2 owns (narrative + completeness flip + the verifiers), §7 open ratification questions | 2026-06-16 |
| data/probe-history/ + scripts/probe_history_stats.py + docs/probe-history.md + docs/blueprint-report.html | Phase 48 NON-ship artifacts: fully SYNTHETIC advisory-shaped legacy rulebook (12 rules) + 44-alert disposition history + derived record 12/12 gate-green through the BYTE-UNCHANGED gate (outside every build.py-read path); stdlib stats script (9 definition-carrying metrics, no selftest yet); shape-caveat writeup; 87.8KB self-contained offline DESIGN-labeled blueprint report (booth precedent — no build.py target, system-flow + grounding-chain SVG centerpieces) | 2026-06-12 |
| console.html + dist/console/index.html + data/console/cases.json + scripts/curate_console_cases.py | The 4th ship artifact (Phase 47): gate console — 213 REAL C/D adjudication cases (Phase-34 correction, byte-identity verified; 212 FINTRAC attributed, 1 US null) through Queue → Evidence → Disposition (graded + required rationale) → Record reveal (213/213 build-verified) → session-only Ledger; curate script = deterministic regeneration-only | 2026-06-12 |
| triage.html + dist/triage/index.html + data/triage/scenarios.json + scripts/curate_triage_scenarios.py | The 5th ship artifact (Phase 49, NEW — blueprint §14's continuous adjudication loop made demo-able): 20 SYNTHETIC scenarios (16 + 4 controls, 4 strata 10/3/3/4, panels shared BY REFERENCE, 6 labeled second-rater seeds, rule text embedded) deterministically curated from data/probe-history at AUTHORING time only; arc Queue → Evidence → Disposition (§14 grammar, rationale REQUIRED) → Reveal (decisions-not-correctness) → Discovery ledger (render-computed agreement w/ measurement definitions; JSON export; persists nothing); 96,686 B offline | 2026-06-12 |
| merge.html + dist/merge/index.html + data/merge/cases.json + scripts/curate_merge_cases.py | The **6th ship artifact (Phase 76 — the blueprint's Class-J merge-adjudication console)**: the human gate over entity-resolution candidate links. **Phase 79 SUPERSEDED the consensus-66 with 29 substrate-anchored SCORED cases:** BOTH populations now scored, split by oracle PROVENANCE (substrate-scored via the non-circular `GT-<hash>` anchored oracle, `entity_ref ≠ cluster`; synthetic-scored via `true_entities`) — the consensus-no-oracle framing retired (substrate data was never production-real; it lacked an oracle until the anchored fork). The substrate population scores the DEMOTED spine → refuses all 29 → the oracle discriminates (13 fragmentation-gap uphold / 16 correct-rejection reject; constant-verdict limitation stated in-console); only the synthetic-13 spans all four quadrants. Arc Queue (by basis) → Evidence (NEUTRAL — no truth pre-adjudication) → Adjudication (rationale REQUIRED) → Verdict (latent-truth oracle + match indicator + synthetic-substrate-anchored qualifier) → Ledger. `curate_merge_cases.py` (`enumerate_substrate_scored`) = companion authoring tool (reuses entity_spine + resolution_scorer; NOT imported by build.py); `validate_merge_cases` mirrors it in EXACT parity + a masking firewall; ~91 KB offline. dist/merge RE-FROZEN. | 2026-06-27 |
| tests/corpus-explorer.test.mjs + tests/news-stream.test.mjs + tests/gate-console.test.mjs + tests/triage-console.test.mjs + tests/merge-console.test.mjs | Zero-dep Node DOM-shim harnesses — corpus 303, news-stream 150, gate-console 68, triage-console 93, merge-console 73 (Phase 76, NEW). Per-phase growth history: the journal entries. |
| workbench.html + scripts/{serve_workbench,serve_chain,curate_workbench_cases,evidence_requirements,osint_tools}.py + data/{workbench,casefile,osint}/ | The COMPANION investigator workbench (NOT a ship/dist target; build.py imports none of it). The live arc CLUTTER→SIGNALS→GATHER→DETERMINATION→DECIDE over a vendored substrate slice (`data/workbench/`, **376 cases @ substrate v0.5 fc98b09**). **Phase 77:** `serve_workbench`/`serve_chain` gained `--disposition` (file\|cleared) plumbing on the casework DECIDE subprocess + `cleared_demo_consume` + `CLEARED_DEMO_BUNDLE` (a casework-gated `cleared` check in `--selftest`); NEW companion data `data/casefile/cleared-demo.bundle.json` = a SYNTHETIC C5 cash-placement bundle (exculpatory:true txn + grounded exculpatory claim, NO crime_type/inculpatory, grounded on vendored `fin-2023-alert001:IND-08`) → casework SIGNS `cleared` end-to-end (the casework file/determination bar BYTE-UNCHANGED; `evidence_requirements.py` byte-unchanged). The north-star Lakeshore CASE-B fails-closed at casework's fan-OUT-only C3. **Phase 75:** `curate_workbench_cases.py` SUBSTRATE_HEAD f15c241→fc98b09 (v0.4/v0.5 additive read; `validate_v05_bundle` wired into `validate()`; a disclosed cross-case CO-REFERENCE selection pass → 36 real co-references; casework gets a relabeled v0.3 VIEW of the v0.5 bundle); `serve_workbench.py` gained `_observe_substrate_party` + `substrate_memory` + a `/memory` route; `workbench.html` gained the "Persistent entity intelligence" panel (36 co-references + 66 over-merge-refused + the casefile short-circuit). **Phase 79:** casework re-vendored b3546d4→**076fb8e** (Phase 19 `_c3_fan_in`); Lakeshore CASE-B now SIGNS `cleared` end-to-end via fan-in C3 (NEW `data/casefile/case-b.bundle.json` — window-compressed ≤7d, IND-02-grounded + `serve_workbench.lakeshore_cosign_consume`) → the matched pair BOTH route through casework; `evidence_requirements.py` BYTE-UNCHANGED (A1). **Phase 73:** `data/casefile/{case.json,schema.md}` — the AUTHORED north-star matched FILE/DISMISS pair (Northgate files / Lakeshore clears; the artifact-is-the-spec, substrate/casework parked); `evidence_requirements.py` gained the affirmative-`cleared` verdict + a `read`-from-file evidence source + predicate-from-register (**the file/determination bar BYTE-UNCHANGED — the A1 guard**); `serve_workbench.py` gained the casefile path (the pair LEADS the queue; verdicts are LIVE-ENGINE OUTPUT, the authored `expected_*` a regression oracle via a fixture-drift bridge); `workbench.html` gained `showcaseSurface` (3 graphs). **Phase 74:** `serve_workbench.py` gained the grade-gated read path (`_cf_read_manifest` + per-decision `read_manifest`; strong/weak ADMITS, reject/empty QUARANTINES — excluded, never down-weighted), the genuine gitignored DuckDB write seam + `casefile_memory` (the re-surfacing CASE-C measured short-circuit + the stale-prior guard); `data/casefile/case.json` gained the `resurfacing` block; `evidence_requirements.py` BYTE-UNCHANGED (the file bar held). Tests: `tests/workbench.test.mjs` (151) + `tests/fixtures/casefile/*`. | 2026-06-25 |
| scripts/{entity_spine,resolution_scorer,measure_xcase_overlap}.py + data/entity-spine/ | **Phase 74 NEW + Phase 75** — the COMPANION persistent entity intelligence spine (NOT a ship/dist target; build.py imports none; the M8 `news_store` byte-untouched). `entity_spine.py` = the pillar-neutral spine: observations / append-only bitemporal `resolution_links` / `persistent_entities`; deterministic strong/weak/REJECT linkage (name-only REJECTED; 2+ distinct strong → refuse-as-ambiguous); reversible `retract_link` (version bump + cascade-invalidation "re-decision required", audit row preserved); conflicting-values-both-kept; event-driven stale-prior; fail-closed-to-weakest; the directional no-news-import firewall. **Phase 75 added** `entity_ref` to `STRONG_KINDS` (the declared-identity strong key — substrate email/phone DEMOTED to weak candidate-SHARES, never a merge key) + a pillar-neutral `entities_in_multiple_records` cross-case query (deterministic stable sort). `resolution_scorer.py` = pairwise P/R + B-cubed over synthetic `data/entity-spine/true_entities.json` (committed) behind a schema-boundary resolver-input firewall (no cluster field; a 1:1 surrogate REJECTED), every number synthetic-only-qualified; cites aml-substrate `resolve/measure.py`. **`measure_xcase_overlap.py` (Phase 75 NEW)** = the measure-first gate — re-emits substrate v0.5 (seed 0) and measures entity_ref re-surfacing (real co-reference) vs shared-strong-id across distinct entity_refs (the over-merge trap); `--selftest`. **Phase 79 NEW** `data/entity-spine/substrate-anchored-slice.json` = the committed no-substrate-replayable merge oracle capture (32 candidate-relevant obs + the non-circular `GT-<hash>` cluster truth; emails masked to example.test, email/phone demoted to weak) — `resolution_scorer` scores it (demoted spine, 29 candidate pairs, two-sided 13/16) → feeds `curate_merge_cases.enumerate_substrate_scored`. `data/entity-spine/store/` gitignored. All `--selftest` under .venv (in PY_SELFTESTS). | 2026-06-25 |

## Session Journal (last 5)

- [2026-06-27] [[2026-06-27-phase-79-consume-sibling-emissions|Phase 79 — Consume sibling emissions: Lakeshore fan-in C3 floor + the merge real-data oracle (gated → cleared GREEN, supersede) (standard, planned+delivered same session)]] — consumed the two Phase-77-deferred sibling emissions as a **floor + gated-upside** bundle; BOTH tracks landed. **FLOOR (T1–T2):** re-vendored casework `b3546d4→076fb8e` (Phase 19 `_c3_fan_in`; funnel 202/111/63 byte-stable); Lakeshore CASE-B SIGNS `cleared` end-to-end via fan-in C3 — the honest fix = re-time CASE-B's 8 catering credits into a 6d2h window (casework's `_FANOUT_WINDOW=7`; same 8 distinct clients/amounts/memos, only dates tighten) + author `data/casefile/case-b.bundle.json` grounded on the FAITHFUL `fin-2023-alert001:IND-02` (case.json keeps IND-05 for matched-pair symmetry, DISCLOSED) + `serve_workbench.lakeshore_cosign_consume` → the matched pair (Northgate files / Lakeshore clears) BOTH route through casework. **GATED UPSIDE (T3–T4):** the measure-first abort gate cleared GREEN — the `--anchored --emit-eval-oracles` emit reproduced CLEAN (the planning-stage ReplayError was `--monitor`/`--emit-evidence`, orthogonal); the `GT-<hash>` oracle is NON-circular (`entity_ref ≠ cluster`; 257 refs → 233 clusters; 17 fragment clusters / 31 latent should-merge — Phase-77 had ZERO); scored the demoted spine → 29 candidate pairs, two-sided (13 uphold / 16 reject); distilled the committed no-substrate capture `data/entity-spine/substrate-anchored-slice.json`. At the user's **"supersedes"** call, `data/merge/cases.json` REPLACED the consensus-66 with 29 substrate-anchored SCORED cases (BOTH populations scored, split by oracle PROVENANCE; the consensus-no-oracle framing retired — substrate data was never production-real, it lacked an oracle until the anchored fork); `merge.html` + `validate_merge_cases` (EXACT parity) + `merge-console.test.mjs` (73→74) updated; `dist/merge` RE-FROZEN (90,831 B). **T5:** the 3 briefs annotated (casework-c3-fan-in CLOSED · substrate-emit-cli-wiring RESOLVED · open-data-fork PARTLY-LANDED Stage-1) + cross-pillar-build-order + CLAUDE.md trued up. **Adversarial review (3 dims): 1 MUST-FIX — mid-flight stale `cases.json` (old 66 committed while dist/merge had new 29; `--check all` PASSED but `--check merge` FAILED) → RESOLVED (regenerated 29+13); 2 should-fix ADDRESSED (demoted-spine constant-verdict framing explicit; full manifest surfaced); 1 self-refuted.** The T2 floor had its own 3-dim review (0 must-fix / 3 disclosures fixed). Companion-only EXCEPT the sanctioned `dist/merge` re-freeze; `--check all` 9/9; `evidence_requirements.py` BYTE-UNCHANGED (A1); build.py firewall clean; merge 74/0, workbench 169/0, curate broken-fixtures 9, `uv run pytest` 26. **STALENESS LESSON:** a "wrote …" log is not proof a file persisted — verify the committed data file's CONTENT + git status + the specific `--check <target>`; commit the curate INPUT (the slice JSON) with the output. Decisions: [[phase-79-merge-supersede-substrate-scored]] · [[phase-79-lakeshore-honest-cosign]] · [[phase-79-floor-plus-gated-upside-bundle]] · [[phase-79-merge-measure-first-before-dist]]. `data/entity-spine/substrate-anchored-slice.json` (NEW) · `data/casefile/case-b.bundle.json` (NEW) · `scripts/curate_merge_cases.py` · `data/merge/cases.json` · `merge.html` · `scripts/build.py` · `dist/merge/index.html` · `tests/merge-console.test.mjs` · `vendor/aml-casework/**` (→076fb8e) · `scripts/serve_workbench.py` · `tests/fixtures/casefile/CASE-B.detail.json` · `docs/{casework-c3-fan-in,substrate-emit-cli-wiring,substrate-open-reference-data-fork}-PLAN-BRIEF.md` · `docs/cross-pillar-build-order.md` · `CLAUDE.md`.
- [2026-06-26] [[2026-06-26-phase-78-consume-disposition-validation|Phase 78 — Consume the disposition oracle: the determination-validation harness (circularity exit) + the §12 discovery feed (standard, planned+delivered same session)]] — CONSUMED substrate Phase 31's now-CLI-wired `--emit-eval-oracles` (substrate f2da3e4→**9677a37**) — the exact handoff Phase-77 A2 deferred to — into the determination-VALIDATION HARNESS (`scripts/determination_validation_harness.py`, the "circularity exit") + a §12 DISCOVERY-FEED control in serve_workbench. Companion-only: `evidence_requirements.py` BYTE-UNCHANGED (A1), all 9 dists byte-frozen, build.py imports nothing new; `uv run pytest` 24→26, workbench.mjs 159→169. **The non-circular frame:** score the BUNDLE-ONLY signal structure (mechanism + ≥legs from fired caps) vs substrate's EXOGENOUS `intended_disposition` oracle (file|clear, authored BLIND to evaluate_sufficiency); human-gate inputs HELD OUT, never from the oracle basis (`assert_no_oracle_leak` allow-list + signature guard); the feed is presentation-only (priors-are-provenance). Population = SCREENING-slice flagged customers MERGED per-customer with the MONITORING bundle by case_id (Phase-71 "a case = a customer"). **The measure-first result (T2 gate PASSED non-degenerate; 6935 cases, 121 file/6814 clear):** ML signals discriminate (file-ready on 50 of 121 oracle-file vs 593 of 6087 oracle-clear) but MISS 71 of 121 file (the §12 gap); the KYC bar is a PURE structural over-flag (all 727 C14-pure cases oracle-clear yet all file-ready — a source-of-funds gap alone is not laundering). The exogenous oracle revealed a real engine weakness it didn't know about itself. **Method:** an Understand workflow (3 readers) mapped the surfaces BEFORE writing (surfaced the oracle-keys-to-SCREENING-slice finding); the 40k emit pre-warmed in the background. **Adversarial review (4 dims → 14 agents): 0 must-fix · 3 confirmed → all fixed · 3 refuted · 10 praise** — fixed: the deliverable doc presented banned rate/% tokens while the governor only swept JSON → rewrote counts-only + extended `_assert_doc_clean` to sweep the doc (reworded disclaimers to "no rate/score/multiplier" — the bare words trip the regex, 3rd recurrence); the degenerate criterion pooled rates across crime_types (Simpson's) → per-crime-type with n>0 guards + a selftest; --freeze wrote before the honesty assert → assert-first. Praise: firewall airtight, oracle genuinely independent ("a real circularity exit, not a repeat of merge-66"). Escape hatches: DISCOVERY (added the `[x] spec` gate to active-phase.md the enforce-spec hook requires). Decisions: [[phase-78-bundle-only-non-circular-validation]] · [[phase-78-measure-then-control-discovery-feed]]. `scripts/determination_validation_harness.py` (NEW) · `tests/fixtures/determination-validation/{capture,baseline}.json` (NEW) · `scripts/serve_workbench.py` (`/discovery` + `discovery_feed`) · `workbench.html` (`discoveryPanelHTML`) · `tests/{workbench.test.mjs,test_selftests.py}` · `docs/{determination-validation,cross-pillar-build-order}.md` · `CLAUDE.md`. NO dist change. Named-deferred: merge real-66 (circular → open-data fork) · Lakeshore `cleared` (casework C3 fan-in).
- [2026-06-26] [[2026-06-26-phase-77-consume-sibling-emissions|Phase 77 — Consume the three sibling emissions: harness deferred · casework cleared via C5 proxy · real-66 merge scoring reverted to consensus (standard, planned+delivered same session)]] — set out to consume three sibling emissions code-verified live (substrate fc98b09→**f2da3e4**; casework bf15535→**b3546d4**); as delivered ONE consume landed + TWO deferred against discovered sibling state, each routed to a NAMED brief. Companion-only — all 9 ship dists BYTE-FROZEN; build.py imports no spine/scorer/sibling/curate. **T1 (the circularity exit) → DEFERRED (DISCOVERY):** verified @f2da3e4 by code that substrate's `emit_true_entities`/`emit_intended_disposition` are tested-but-UNWIRED into the CLI (run only in substrate's tests; only the `--identity` parquet is CLI-reachable) → `docs/substrate-emit-cli-wiring-PLAN-BRIEF.md`. **T2 → DONE:** re-vendored casework bf15535→b3546d4 (`VENDORED_AT`); gate funnel IDENTICAL (202/111/63) — no signing regression. **T3 → DONE via a C5 PROXY (USER OVERRIDE option b):** casework's `cleared` consumed end-to-end on a casework-replayable C5 cash-placement proxy (`data/casefile/cleared-demo.bundle.json`, exculpatory:true txn + grounded exculpatory claim, NO crime_type/inculpatory, grounded on vendored `fin-2023-alert001:IND-08`; `serve_workbench.cleared_demo_consume` → signed + disposition==`cleared` + blocking_violations==[]; adversarially-reviewed HONEST). The north-star Lakeshore CASE-B fails-closed at casework's `grounding_replay` (casework C3 = fan-OUT ≥5 outflows; Lakeshore's C3 is fan-IN → 0 cited outflows → correctly refused; **A3 abort held — no fan-out fabricated**) → `docs/casework-c3-fan-in-PLAN-BRIEF.md`. **T4 → ATTEMPTED → DEFERRED at the abort rule:** re-emitted the slice with `--identity` (pin f2da3e4) and mapped the 66 real SHARES — the CHECKPOINT fired worse than "all distinct": every cluster is content-addressed `ENT-<entity_ref>` (a 1:1 RELABEL of the SAME field the spine keys its merge/refuse on; all 441 slice persons; ZERO cross-entity_ref merges; 66/66 DISTINCT) → scoring is CIRCULAR (true-by-construction). Per the abort rule + the user's call ("revert real to consensus, defer"), REVERTED curate/build/cases/test to the Phase-76 CONSENSUS state; the circular parquet capture NOT committed; the genuinely-two-sided synthetic-13 oracle untouched. **T5 → N/A (voided by T4):** merge.html/cases.json/test reverted to Phase-76 (`merge-console.test.mjs` 73/0); `build.py --check merge` byte-stable; dist/merge byte-identical to its Phase-76 ship. **T6 → DONE:** `--check all` 9/9 byte-frozen (ZERO dist drift) · `uv run pytest` 24/24 · arcs (merge 73 / gate 68 / triage 93 / news 150 / corpus 303) green · build.py imports no spine/scorer/sibling/curate · CLAUDE.md + `docs/cross-pillar-build-order.md` + the consumed briefs trued up. The **open-data fork** (the unblocking move for the two-sided real merge oracle + realistic collisions — a substrate realism phase) PARKED as `docs/substrate-open-reference-data-fork-PLAN-BRIEF.md`. Escape hatches: DISCOVERY (T1) · USER OVERRIDE (T3 C5 proxy) · USER OVERRIDE + ABORT-RULE (T4 revert-to-consensus). Decisions: [[phase-77-consume-3-true-entities-one-sided]] · [[phase-77-casework-cleared-lakeshore-translation]] · [[phase-77-exogenous-disposition-harness-circularity-exit]] · [[phase-77-three-consumes-one-phase-fork-parked]]. `vendor/aml-casework/**` (→b3546d4) · `scripts/{serve_workbench,serve_chain}.py` (`--disposition` plumbing + `cleared_demo_consume`) · `data/casefile/cleared-demo.bundle.json` (NEW) · `docs/{cross-pillar-build-order,substrate-emit-cli-wiring-PLAN-BRIEF,casework-c3-fan-in-PLAN-BRIEF,substrate-open-reference-data-fork-PLAN-BRIEF}.md` · `CLAUDE.md`. NO dist change.
- [2026-06-25] [[2026-06-25-phase-76-merge-adjudication-console|Phase 76 — The merge-adjudication Class-J console (dist/merge): the scored merge gate (standard, planned+delivered same session)]] — built the **6th SHIP artifact** `merge.html`→`dist/merge/index.html`, the blueprint's **Class-J merge-adjudication console** (sibling to the gate + triage consoles), the human gate over entity-resolution candidate links. Consumes Phase 75's over-merge finding: the spine resolves what it can (entity_ref) + refuses the ambiguous; the human adjudicates the **66 candidate SHARES**. **THE NOVELTY:** the ONE console with a measurable correctness ORACLE (`true_entities`) — unlike the consensus-only gate console + the label-blind §14 triage, the Verdict SHOWS (where the oracle exists) whether the adjudication matched truth. **THE HONESTY SEAM (consensus-vs-scored split):** 66 REAL substrate candidate SHARES = consensus-not-ground-truth, NO oracle (no fabricated truth on real data); only 13 SYNTHETIC cases are scored vs `true_entities`, synthetic-only-qualified; the Verdict + ledger visibly SPLIT them; real-substrate scoring is a NAMED sibling handoff (fc98b09), never faked. **T1 (the A1 CHECKPOINT) PASSED:** the synthetic oracle EXPANDED 8→25 obs / 5→17 clusters (13 candidates, 9 genuinely ambiguous, all 4 quadrants / 3 bases — the SAME weak-address basis carries OPPOSITE truths, so a basis-only rule cannot decide; resolver pairwise P=0.50/R=0.33 = the gate's reason to exist); `resolution_scorer` gained `candidate_pairs()` + `KLASS_*`. **T2** `scripts/curate_merge_cases.py` (companion authoring tool, reuses entity_spine + resolution_scorer, NOT imported by build.py) → committed `data/merge/cases.json` (66 real + 13 synthetic; the resolver-input firewall translated — pre-disposition evidence carries NO truth, the latent truth rides ONLY each scored case's `oracle` block; real emails domain-MASKED to example.test). **T3** `merge.html` + the `merge` build target (STANDALONE validate_merge_cases) + the launcher merge card (the ONE sanctioned existing-dist change → dist/index.html); `dist/merge` byte-frozen; **`--check all` now 9 targets**, build.py imports no spine/scorer/sibling/curate. **T4** `tests/merge-console.test.mjs` (73). **T5** `docs/substrate-true-entities-emission-PLAN-BRIEF.md` (re-grounded fc98b09). **T6** `uv run pytest` 22→24; CLAUDE.md "Six ship artifacts" (413→447). **STANDARD adversarial review (28 findings): 2 should-fix [the build-boundary validator was WEAKER than curate's firewall — the `note` leak-key + the real basis/verdict shape; BOTH FIXED → validators now in EXACT parity] · 2 refuted · 24 praise.** Decisions: [[phase-76-merge-console-as-sixth-ship-artifact]] · [[phase-76-consensus-vs-scored-honesty-split]] · [[phase-76-build-py-firewall-curation-companion-side]] · [[phase-76-ship-all-66-candidate-shares]] · [[phase-76-validator-parity-build-mirrors-curate]]. `merge.html` (NEW) · `scripts/curate_merge_cases.py` (NEW) · `data/merge/cases.json` (NEW) · `dist/merge/index.html` (NEW) · `scripts/build.py` (the `merge` target) · `scripts/resolution_scorer.py` (oracle expanded) · `data/entity-spine/true_entities.json` · `launcher.html` + `dist/index.html` · `tests/{merge-console.test.mjs (NEW),test_selftests.py}` · `docs/substrate-true-entities-emission-PLAN-BRIEF.md` (NEW) · `CLAUDE.md`.
- [2026-06-25] [[2026-06-25-phase-75-consume-substrate-v05-er-emission|Phase 75 — Consume the substrate v0.5 entity-resolution emission: the spine's real-data memory lever (standard, planned+delivered same session)]] — CONSUMED aml-substrate's just-shipped ADDITIVE emission (named-identity **v0.4** `display_name`/`counterparty_name` + entity-resolution **v0.5** party-level `identifiers[]` `{kind,value,normalized,strength}` / `RelationshipEdge.strength` / `resolution_edges[]`) into the companion-only path (`curate_workbench_cases`→`entity_spine`→`workbench`), grounding the Phase-74 memory short-circuit in REAL substrate data. **THE T1 MEASURE-FIRST GATE WAS THE KEYSTONE — it caught a MECHANISM error, not the planned zero-overlap down-scope.** Its zero-branch did NOT fire (229 entity_refs re-surface cross-case in the 3k probe — the memory signal is real), but it caught that the PLANNED strong-merge of substrate `identifiers[]` is WRONG: substrate's shared strong ids are a DELIBERATE collision noise floor + controller-cluster SHARES between DISTINCT entities (`gen/identity.py`: "robust to over-merging on noise"), and its `resolution_edges` (`status:"resolved"`) OVER-MERGE distinct people (verified: "Chloe Ali" → two distinct people). The reliable same-entity key is `entity_ref`. **At the CHECKPOINT the user picked "Entity_ref memory + SHARES adjudication"** (A3 REVISED — `bit` in the strong-merge sense): key cross-case memory on `entity_ref`; demote substrate email/phone to weak candidate-SHARES; ADJUDICATE resolution_edges (REFUSE to merge distinct entity_refs — the over-merge traps become a demo beat; on-ramps the deferred Class-J merge-adjudication console). **NEW** `scripts/measure_xcase_overlap.py`. **T2** re-curated a 376-case v0.5 slice (SUBSTRATE_HEAD f15c241→fc98b09) + a disclosed cross-case CO-REFERENCE selection pass (`DEFAULT_COREF_ENTITIES_CAP=15`; the default slice had 0 → 0→36); `validate_v05_bundle` wired into production `validate()`; casework gets a relabeled v0.3 VIEW (it rejects `"0.5"` but tolerates additive fields); a latent venv-path bug fixed inline. **T3** `entity_ref`→`STRONG_KINDS` + `entities_in_multiple_records` (stable sort) + `_observe_substrate_party` → committed slice **36 co-references + 66 over-merge-refused**; **THE A1 FILE-BAR GUARD HELD** (`evidence_requirements.py` BYTE-UNCHANGED; inject-a-prior-cleared → byte-identical verdict). **T4** a `/memory` route + the "Persistent entity intelligence" panel; live-verified; `workbench.test.mjs` 151→159. **T5** the 3 sibling briefs re-grounded to live HEADs (substrate fc98b09, casework 4a858e6); the casework re-vendor SKIPPED (no-op). **T6** `--check all` 8/8 byte-frozen + build.py imports nothing new; news + news_live + gather + `uv run pytest` 22 green (M8 not regressed); CLAUDE.md trued up. STANDARD adversarial review (8 agents): 0 must-fix · 2 should-fix + 1 nit ALL FIXED (non-deterministic `/memory` examples → stable sort; `validate_v05_bundle` dead on the boundary → wired in; stale comment de-staled) · 1 refuted; honesty/A1/boundary all PRAISE. Decisions: [[phase-75-entity-ref-memory-shares-adjudication]] · [[phase-75-cross-case-coreference-selection-pass]] · [[phase-75-casework-v03-view-of-v05-bundles]] · [[phase-75-skip-casework-re-vendor]]. `scripts/measure_xcase_overlap.py` (NEW) · `scripts/{entity_spine,serve_workbench,curate_workbench_cases}.py` · `workbench.html` · `tests/workbench.test.mjs` · `data/workbench/**` (376 cases) · `docs/{substrate-graded-counterparty-identifiers,substrate-exogenous-disposition-label,casework-confidence-graded-resolution}-PLAN-BRIEF.md` · `CLAUDE.md`.
## Cross-References

- HANDOFF.md · CLAUDE.md · README.md · docs/program-blueprint.md (M9 design source-of-truth, §1–§15)
- docs/{case-workbench,evidence-driven-filing}.md (the companion workbench + the evidence-sufficiency control) ·
  docs/rich-case-target-contract.md (Phase 73 — the cross-pillar handoff: what substrate must EMIT + casework must SIGN/REFUSE/CLEAR; committed ef4bf83)
- **Phase 74 standards** (the cross-repo entity-intelligence contract): docs/{resolution-link-schema, identity-grade-grammar, confidence-as-provenance-contract, true-entities-scorer-contract}.md — one shared strong/weak/reject grade vocabulary
- **Sibling emission briefs** (RE-GROUNDED to verified live HEADs; sibling-executed): docs/{substrate-graded-counterparty-identifiers (aml-substrate@fc98b09 — LARGELY BUILT), substrate-exogenous-disposition-label (aml-substrate@fc98b09 — NOT BUILT), casework-confidence-graded-resolution (aml-casework@4a858e6 — NOT BUILT, graded-resolution still outstanding; `cleared` now BUILT @b3546d4 + CONSUMED via a C5 proxy), **substrate-true-entities-emission (Phase 76 — aml-substrate@fc98b09; emits slice-aligned `true_entities` so the merge console can SCORE the real 66)**}-PLAN-BRIEF.md
- **Phase 77/79 sibling handoff briefs** (Phase 79 ANNOTATED all three with resolving commits): docs/{**casework-c3-fan-in ✅ CLOSED** (`ed93a0d`/076fb8e — fan-IN C3 built + CONSUMED: Lakeshore CASE-B now co-signs `cleared`), **substrate-emit-cli-wiring ✅ RESOLVED** (`9677a37` + the `--anchored` fork — the disposition + anchored `entity_ref ≠ cluster` oracles are CLI-reachable), **substrate-open-reference-data-fork ◐ PARTLY LANDED** (Stage-1 `f9e63e7`; Stages 2/3 sanctions/FATF anchors + paid-landmine routing OPEN — the next merge-oracle realism frontier)}-PLAN-BRIEF.md · docs/cross-pillar-build-order.md (trued-up to casework 076fb8e/Phase 19, substrate c099259/Phase 32-33 + the Phase-79 consume note)
- docs/blueprint-report.html (Phase 48 — self-contained offline NON-ship blueprint report, DESIGN-labeled) ·
  docs/probe-history.md (Phase 48 — synthetic-history probe writeup + shape caveat)
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN, SR 11-7 + OSFI E-23 (the E-23 article NEW at Phase 47, primary-source).
  Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
