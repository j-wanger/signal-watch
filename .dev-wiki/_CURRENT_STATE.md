# Project: Signal Watch — AML Vision Demo

> Last updated: 2026-06-06 by /dev-plan (Phase 18 — corpus explorer arc: human gate + close-the-loop coverage payoff)

## Recommended Next Action

**Phase 18 DELIVERED + accepted — the corpus explorer now ships the 5-screen arc** (Select → Coverage →
**Build recs/GATE** → **Signal** → **Close the loop**). The Build-recs screen is the human gate (BUILD_NOW rows
are selectable div-toggles, default all-selected, "agent proposes, human disposes"); Signal reflects the picks
(two honest empty states); the NEW Close-the-loop screen animates the coverage index before→after as the
committed gaps flip gap→covered (showcase Act-6 model), with a reduced-motion branch + an honest flat-hold for
0-picked/0-BUILD_NOW (never a fake rise). NO fabricated numbers — coverage is the payoff; precision
combination-lift was REJECTED (records carry no precision figures → would fabricate ~12 per-advisory stats).
Unfroze `corpus.html` ONLY; `index.html`/`build.py`/`config/**`/`data/fincen/**` + the 3 typology dists
byte-frozen. Verified: 15/15 headless assertions, `node --check` valid, `--check all` 4-artifact zero drift.
Impl commit 6d654a4; gate commit follows.

**NEXT:** the M0–M7 roadmap + this arc are complete and the demo is at Definition of Done — **run /dev-plan for
Phase 19**, or call it done. Optionally `/dev-debrief` for the full Phase-18 journal/retro capture.

**Phase 19 candidates (carried):** tighten the coarse `_rf_triage` counter (Phase-17 reviewer MEDIUM) · drop the
stale `anthropic` pin from `requirements-authoring.txt` · FATF non-derivable labeling polish · the recurring
showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred Ph10/11/15/16) · corpus
combination-lift wow beat (only with an honest, non-fabricated lift source) · manifest `--fetch` cadence. The 3
hand-curated typologies stay the byte-frozen showcase.

## Active Phase

**[[phase-18-corpus-explorer-arc|Phase 18: Corpus explorer arc — human gate + close-the-loop coverage payoff]]** (status: completed — DELIVERED + accepted 2026-06-06)

Entry criteria: MET (Phase 17 complete + accepted; impl commit 572cd3b — corpus 12/14 live, the real subtraction
landed). Direction approved corpus-explorer arc over call-it-done / engineering-hygiene / showcase-true-up /
deepening-the-showcase-gate.
Exit criteria: the explorer ships a 5-screen arc (Select → Coverage → Build recs/gate → Signal → Close the loop) ·
BUILD_NOW recs selectable (div-toggle human gate, default all-selected, keyboard-safe, non-BUILD_NOW read-only) ·
Signal reflects the picks (selected ∩ BUILD_NOW with build_logic; honest empty state) · close-the-loop animates
coverage before→after from the picks with a reduced-motion branch + an honest flat-hold for 0-BUILD_NOW ·
dist/corpus rebuilt · `--check all` shows index.html/build.py/config/data/3-typology-dists byte-frozen ·
`node --check` valid · README + CLAUDE document the arc + the coverage-not-precision honesty stance.

Progress: 100% — DELIVERED + accepted; all 5 tasks [x]; impl commit 6d654a4.

## Active Phase Contract

Phase: 18 - Corpus explorer arc — human gate + close-the-loop coverage payoff
Tasks: 5 (see tasks.md) — T1 the gate (BUILD_NOW rows → selectable div-toggles, per-advisory selection Set,
default all-selected, reset on pick) · T2 Signal reflects the gate (spec cards for selected ∩ BUILD_NOW, honest
empty state) · T3 close-the-loop screen (5th screen, coverage before→after from the picks, reduced-motion branch,
honest 0-BUILD_NOW flat-hold) · T4 rebuild dist/corpus + headless verify + drift guard · T5 docs.
Transition: continue (lite). The unfreeze is `corpus.html` ONLY (+ `dist/corpus` rebuild + README/CLAUDE). FROZEN
byte-untouched: `index.html`, `scripts/build.py`, `config/**`, `data/fincen/**`, the 3 typology dists. The arc
consumes only existing data fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card) → NO
schema/data/build.py change.
Abort: DEGRADE to close-the-loop-only (no gate; coverage close over ALL BUILD_NOW) if interactive selection can't
be made keyboard-safe/deterministic without growing complex; keep the close-the-loop screen informational rather
than ship a misleading rise if the math can't be honest; NEVER fabricate precision/lift. If blocked >3 attempts,
ask user: skip or abort.

## Recent Decisions

| Decision | Confidence | Date |
|----------|------------|------|
| Phase 18 = give the corpus explorer a dramatic arc (human gate + close-the-loop coverage payoff), mirroring the showcase's Act 3 + Act 6. User chose this over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate. Rationale: the corpus explorer is where the project's earned identity lives (real provenance, 12/14 derived) and is the one artifact missing the dramatic arc the showcase has — net-new buy-in value, not polish. Chosen at the end of the completed M0–M7 roadmap after a UX-research review confirmed the agentic spine was already sound | high | 2026-06-06 |
| The payoff is COVERAGE close-the-loop, NOT precision combination-lift. Precision-lift (the showcase's "alone it's noise 22% → together 81%" beat) was EXPLICITLY REJECTED: the corpus derived records carry no precision/lift numbers, so porting it would FABRICATE ~12 per-advisory illustrative precision stats — violating the "never present synthetic numbers as real" non-negotiable and undercutting the corpus side's whole real-provenance credibility. Coverage is already computed from existing statuses (`coverageIndex()`) and already disclosed illustrative → the honest, zero-new-fabrication payoff | high | 2026-06-06 |
| Scope contained to `corpus.html` (+ dist/corpus rebuild + docs); NO schema/data/build.py change because the arc reuses existing fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card). The human gate uses div-toggles (NOT `<input>`) to avoid keyboard-nav collision (Space/arrows) and preserve determinism — the same pattern the showcase's `.selrow` uses | high | 2026-06-06 |
| The gate FOLDS INTO the existing Build-recs screen (BUILD_NOW rows become selectable) rather than adding a separate gate screen — subtraction test: don't add a screen when an existing one carries the interaction. Arc is 5 screens, defaulting all-BUILD_NOW-selected so the default presenter path still walks a complete arc | medium-high | 2026-06-06 |
(Earlier Phase 16–17 decisions — see `[[2026-06-06-phase-17-complete-corpus-derivation|Ph17]]`, `[[2026-06-06-phase-16-invert-extraction|Ph16]]` journals. Load-bearing carry-overs: Ph17 the REAL subtraction — `extract_red_flags` + the whole `--scaffold`/`--draft`/`--scaffold-derived` authoring stack + `anthropic`/`os` DELETED (1202→600, stdlib-only); the inverted loop is the SOLE derivation path; `_rf_triage` counter reuses the `rf_region` span; corpus 12/14 live (only the 2 FATF non-derivable). Ph16 INVERTED the extraction boundary — the LLM EXTRACTS, the deterministic layer GATES (normalize(flag) ⊂ normalize(md) = the traceability authority, replacing src_line∈extractor) + a coarse `rf_region()` section-cite relevance guard + the cover×data matrix + BUILD_NOW⇒full-build_logic; corpus-status SHAPE preserved; `derivable: false` = the 2 FATF advisories.)
(Earlier Phase 12–15 decisions — see `[[2026-06-05-phase-15-harden-extraction-faithfulness|Ph15]]`, `[[2026-06-05-phase-14-scale-corpus-derivation|Ph14]]`, `[[2026-06-05-phase-13-corpus-explorer|Ph13]]`, `[[2026-06-05-phase-12-fincen-corpus-derivation|Ph12]]` journals. Load-bearing carry-overs: Ph15 harden-FAITHFULNESS (footnote-resume; glued-no-separator stays FLAGGED, no safe deterministic split; derived records store RAW text, `corpus.html` esc() the sole escaper); Ph14 PURE-AUTHORING scaling (build.py makes an advisory live by the presence of `derived/<id>.json`); Ph13 standalone STAGED 4-screen corpus explorer (`dist/corpus/` via `corpus.html`, all-14-honest status, six-act showcase byte-frozen); Ph12 SINGULAR corpus-backed demo (user picks 1 of 14; per-advisory `derived/*.json` the analytical artifact, the 3 typologies stay the showcase), build.py DECOUPLED from authoring (reads committed corpus-status.json + derived/*.json, never imports derive_signals.py), LLM backend = THIS session not `--draft`, validate at the build boundary.)

(Earlier Phase 7–11 decisions: see `[[phase-11-automated-derivation]]`, `[[phase-10-fincen-corpus-crawler]]`, `[[phase-08-doc-true-up]]` + journal. Load-bearing carry-overs: Phase 11 AUTOMATE → variant B (LLM-drafted signal NOW), boundary-preserving (LLM proposes; build.py + schema + 2 human gates dispose); authoring-time vs ship-artifact split (build-time tools; ship file stays single-file/offline/no-fetch, HANDOFF §4/§4.5); validate at the build boundary (deterministic validator, fail loud); FinCEN-only verbatim public-domain exception (17 USC §105, NOT FINTRAC); lite ceremony.)

## Blockers and Open Questions

- [RESOLVED 2026-06-06 · planning] Phase-18 direction → **give the corpus explorer (`dist/corpus/`, from `corpus.html`) a dramatic 5-screen arc — Select → Coverage → Build recs/GATE → Signal → Close the loop — mirroring the showcase's Act 3 (human gate) + Act 6 (loop closes), grounded ENTIRELY in existing data, NO fabricated numbers**. User chose the corpus-explorer arc over call-it-done / engineering-hygiene / showcase-true-up / deepening-the-showcase-gate. The payoff is COVERAGE close-the-loop (the existing `coverageIndex()` recompute with picked gaps flipped to covered), NOT precision combination-lift — precision-lift EXPLICITLY REJECTED (the corpus records carry no precision/lift numbers; porting it would fabricate ~12 per-advisory illustrative stats, violating the "never present synthetic numbers as real" non-negotiable). Scope contained to `corpus.html` (+ dist/corpus rebuild + docs): the arc reuses existing fields (status → coverage, build_rec → BUILD_NOW, build_logic → spec card) → NO schema/data/build.py change; the gate uses div-toggles (NOT `<input>`) to avoid keyboard-nav collision + preserve determinism; the gate folds into the existing Build-recs screen (subtraction test). Open sub-question, resolved at impl: can interactive selection be made keyboard-safe + deterministic without growing complex? Abort/degrade path = close-the-loop-only (no gate; coverage close over ALL BUILD_NOW — the payoff still lands); if the coverage math can't be honest, keep the close screen informational; NEVER fabricate precision/lift
- [RESOLVED 2026-06-06 · planning] Phase-17 direction → **complete the corpus derivation (7/14 → 12/14) + the REAL subtraction (delete `extract_red_flags` + the dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack), unblocked by a cheap `rf_region`-bounded triage counter**. User chose scale-to-complete + delete over scale-only / lean-freeze-and-delete; "corpus may grow later" took the lean freeze off the table (it would leave un-regenerable stale triage). The debrief had the dependency backwards — `extract_red_flags` is off the correctness path but is the SOLE producer of the extraction/flag_count triage the byte-frozen `corpus.html` renders; the ~12-line counter (reuses the existing `rf_region` span) replaces that role, keeps `--corpus-status` regenerable, and is materially smaller. Subtraction-FIRST ordering: delete + counter + reshape (T1), prove the 7/14 still builds clean on the stripped spine, then scale (T2–T4). Open sub-question, resolved at impl: can the `rf_region`-bounded counter preserve corpus-status.json's SHAPE / pass build.py validation without growing as complex as `extract_red_flags`? Abort path = STOP the deletion, keep `extract_red_flags`, narrow to scale-only (subtraction test must SHRINK complexity, not move it). Degrade-path for a LOW/glued advisory that won't ground faithfully = scale-what-passes (the deletion still lands — counter-gated, not scale-gated)
- [RESOLVED 2026-06-06 · planning] Phase-16 direction → **INVERT the extraction architecture** (the LLM extracts candidate red flags; the deterministic layer becomes a GROUNDEDNESS GATE — normalized `flag` ∈ normalized md replaces src_line∈extractor-output as the traceability authority — + a cheap section-cite relevance guard) **+ scale as proof to 7/14** (COVID-EIP fin-2021-a002 + ≥1 previously-LOW/glued advisory). User chose this over pure breadth-scaling / a structure-preserving converter (DISSOLVED). Decisions A (groundedness + section-cite, not pure-groundedness, not a full parser), B (demote `extract_red_flags`, preserve corpus-status SHAPE, derivable false only for the 2 FATF advisories), C (2 records as proof over count) confirmed. Open sub-question, resolved at impl: can `normalize()` ground the escrow STRESS case (header-glued + hyphen-truncated raw L499) without becoming as complex as the extractor it replaces? Abort path = keep quote-grounding for the clean records, leave `extract_red_flags` as-is for the rest, narrow breadth to COVID-EIP only (subtraction test must SHRINK complexity, not move it)
- [OPEN] M6 converter choice — markitdown (MIT) evaluated first for license-cleanliness; pymupdf4llm (AGPL, authoring-only) is the quality fallback. NONE installed in env. Resolved by the T2 CHECKPOINT against the real EFE PDF; the converter is authoring-only (scripts/), never in the ship file. (NOTE Phase-16/17: the converter question DISSOLVED for the glued advisories — the inverted loop reads them like a human; markitdown stays the committed converter for the corpus md.)
- [DEFERRED] Carried follow-ups: pre-commit/CI `--check` enforcement (Phase-9 lite scope) · closing "ask" slide (touches the six-act arc + schema) · the showcase-debt true-up (elder presentation-values + fentanyl verbatim re-point — deferred across Ph10/11/15/16, see the Phase-17 journal Retro Dim 3) · corpus combination-lift wow beat · manifest `--fetch` cadence.
- (Resolved planning blockers Ph11–Ph13 — Ph13 standalone corpus-explorer artifact (staged 4-screen, all-14-honest, showcase byte-frozen); Ph11 AUTOMATE variant B (LLM-drafted signal, user overrode the elder-true-up-first + variant-A recs) — see their journals.)
- (Resolved blockers Ph8–M6 — anchor advisory EFE FIN-2022-A002, product name "Signal Watch", `advisory_full` renders in Act-1's SOURCE DOCUMENT panel, single-file ship, scripted mode, claude-api model-id gap filled, Phase-8 doc true-up — see `[[2026-06-04-phase-08-doc-true-up|Ph8]]` + earlier journals.)

## Key Artifacts

| Path | Purpose | Last Modified |
|------|---------|---------------|
| index.html | Generic engine template (`__CONFIG__` injection point); M3 added keyboard nav + reset + reduced-motion | 2026-06-04 |
| config/schema.md | Content-model contract | 2026-06-04 |
| config/typologies/*.json | 3 hand-curated showcase typologies (fentanyl, trade-based, elder) | 2026-06-04 |
| scripts/build.py | Validates config + corpus data at boundary, inlines → dist/<id> + dist/corpus | 2026-06-06 |
| scripts/derive_signals.py | Corpus derivation spine — quote-grounding gate + `_rf_triage` + `--corpus-status`/`--check-derived`/`--selftest` (1202→600, stdlib-only) | 2026-06-06 |
| corpus.html + dist/corpus/index.html | Standalone corpus-explorer template + built ship file (12/14 derived live) | 2026-06-06 |
| data/fincen/{*.md, derived/*.json, corpus-status.json} | 14-advisory corpus md + 12 derived records + the status manifest | 2026-06-06 |

## Session Journal (last 5)

- [2026-06-06] [[2026-06-06-phase-17-complete-corpus-derivation|Phase 17 Complete corpus derivation — delete extract_red_flags (the real subtraction) + scale to 12/14 (M7)]] (lite, 6 tasks, DELIVERED — READY FOR COMPLETION at the gate; reviewer ACCEPT 9/10) — did the REAL line-count subtraction Phase 16 deferred, then scaled to near-completion. **T1 the subtraction (WIDE):** deleted `extract_red_flags` AND the whole now-dead `--scaffold`/`--draft`/`--scaffold-derived` authoring stack (`scaffold_config`, `write_scaffold`, `draft_judgment`, `write_draft`, `_few_shot`, `_apply_judgment`, `_derived_skeleton`) + the `anthropic` dependency + the `os` import → `derive_signals.py` **1202 → 600 lines (−602, −50%)**, beating the ~700 estimate; def-grep 0; stdlib-only. The extractor's sole surviving job became a ~14-line `_rf_triage(md, region)` counter reusing the existing `rf_region` span (real shrink, not a reintroduced parser); `--selftest` is now gate-only (hardcoded verbatim EFE fixtures L507/L509). The inverted loop (LLM extracts → `check_record`/`--check-derived` gate disposes by quote-grounding) is the SOLE derivation path. **T2–T4 scale:** derived the 5 remaining derivable advisories — fin-2026-a001 (glued health-care, 24 ind, 6 BUILD_NOW; the LLM reached a glued advisory the deleted extractor sized as ~6 blocks, no splitter) · fin-2021-a001 (COVID, 16; footnote-heavy → contiguous grounded spans, page-break continuations dropped not bridged) · fin-2024-a001 (Iran-terror, 9; "NPO84" footnote marker kept verbatim so the span grounds) · fin-2025-a001 (ISIS, 11) · EFE fin-2022-a002 (12 FINANCIAL flags only — the 12 BEHAVIORAL are teller-witnessed SAR-narrative, out of scope; the showcase elder typology presents them in full). The two "LOW" advisories validated clean (the LOW label was the deleted extractor under-counting through interleaved footnotes). Corpus **7/14 → 12/14 live**; only the 2 FATF advisories non-derivable. **T5–T6:** dist/corpus rebuilt 109,948→180,119 B; `--check all` 4-artifact ZERO DRIFT; headless brace-match confirmed the built `__CORPUS__` valid JSON (14 advisories, 12 derived, render-ready through all 4 screens); README + CLAUDE document the deleted stack + the counter + 12/14. DISCOVERY (refines plan): `corpus_status_records` uses `_rf_triage` UNIFORMLY for all 14 (corpus.html reads flag_count only for the not-yet-derived chip) — simpler than the planned per-live-record read. Reviewer 1 MEDIUM = latent triage-overcount footgun for a FUTURE not-yet-derived glued advisory (harmless in the shipped 12/14, disclosed; folded to soft obs, NOT fixed inline). index.html/corpus.html/build.py/config/** + 3 typology dists byte-frozen; `--selftest` gate-only PASS. RETRO (Phases 1–15) fired (15 % 5 == 0): high-signal Dim 3 = the recurring user override toward scale/architecture over the showcase-debt true-up.
- [2026-06-06] [[2026-06-06-phase-16-invert-extraction|Phase 16 Invert extraction (LLM extracts, deterministic gate disposes) + scale to 7/14 (M7)]] (lite, 5 tasks, DELIVERED + accepted; reviewer ACCEPT 9/10) — INVERTED the extraction boundary per the user's reframe (subtraction test on the ~130-line accreted extractor; "make the deterministic layer a GATE, not a neural-overridden extractor"). Traceability authority moved from `src_line ∈ extract_red_flags(md)` → QUOTE-GROUNDING (`normalize(flag) ⊂ normalize(md)`). **T1** the subtraction won decisively: ONE rule `normalize(text)=re.sub(r'[^a-z0-9]+','',text.lower()).replace('fincenadvisory','')` absorbs the WHOLE closed FinCEN-md artifact set (line wraps, word-wrap hyphens, smart quotes, footnote digits, AND the page-break running header → droppable token); the header-glued escrow STRESS case (fin-2025-a003 L499) grounds WITHOUT special-casing; `check_record` rewired to grounding + a coarse `rf_region()` relevance guard; ~3-line normalizer + ~12-line region REPLACE the src_line traceability that depended on the ~130-line extractor — correctness complexity SHRANK. **T2** all 5 committed records migrated NEAR-FREE (gate-pass unedited; only a raw-text provenance swap). **T3** `extract_red_flags` DEMOTED to EFE selftest-anchor + triage hint; `--corpus-status` SHAPE preserved, `derivable` rebased on `rf_region(md) is not None` (false only for the 2 FATF advisories; the 2 glued advisories correctly flip false→true). **T4** THE PROOF: ransomware **fin-2021-a004** (0 deterministic flags, glued-no-separator, previously unreachable) → LLM extracted all 12, every one grounds verbatim, NO converter/splitter (Phase-15 glued-deferral DISSOLVED); COVID-EIP **fin-2021-a002** consolidated 7 sub-clause blocks → 3 ind; 7/7 derived → corpus **5/14 → 7/14 live**. **T5** dist/corpus rebuilt (95,188→109,937 B), `--check all` 4-artifact ZERO DRIFT, README + CLAUDE document the inverted architecture + the honesty shift. 2 MEDIUM reviewer findings fixed inline (`_MIN_FLAG_NCHARS=24` floor + 2 regression-pin selftest cases). DISCOVERY (refines T1): broadened `_RF_REGION_END` so ransomware's "Reminder of Regulatory Obligations" terminates its region — no regression. index.html/corpus.html/config/** + 3 typology dists byte-frozen; `--selftest` EFE 12+12. SOFT OBS (Phase-17): the file GREW 1063→1189 (decision B retained the demoted extractor) — the real subtraction is deleting it.
- [2026-06-05] [[2026-06-05-phase-15-harden-extraction-faithfulness|Phase 15 Harden extraction faithfulness + fix shipped defects (M7)]] (lite, 5 tasks, DELIVERED + accepted) — fixed the 2 defects Phase 14 surfaced, scoped by MEASUREMENT (the LOW/NEEDS advisories fail for 3 distinct reasons, not one). **T1 footnote-resume fix** in `extract_red_flags` (3 measured iterations): split stop logic into `_SECTION_STOP` (terminals, always break) + a conditional `_FOOTNOTE_STOP` (a mid-list page-boundary footnote run is transient when another section follows → skip + resume to the next anchor; terminal for a last section → break) + 2 targeted `_CITATION` signatures (case-docket + no-day paren-date) to kill 2 footnote-tail leaks. SURGICAL: `fin-2025-a003` recovered its silently-dropped L499 escrow flag (17→18), **0 collateral** — all 13 other advisories byte-identical, EFE 12+12, summary 7C/3L/4N. **T2 esc() entity sweep** (`html.unescape` over fin-2022-a001 + fin-2024-a002 `&gt;=`/`&lt;=` → raw text; verified end-to-end in the built file). **T3** escrow IND-18 added to fin-2025-a003 (18 ind, check-clean). **T4** manifest regen + dist/corpus rebuild + `--check all` zero drift. **T5** docs (footnote-resume + glued-deferral + RAW-text/esc() convention). GLUED-NO-SEPARATOR splitting DEFERRED — no safe deterministic split (re-confirms Phase-12); needs a better converter, not a splitter. index.html/corpus.html/config/** + 3 typology dists byte-untouched. The abort rule was hit (first attempt over-captured) but resolved by the bounded next_boundary rule, not aborted. Impl commit + gate commit landed + pushed.
(Earlier entries — Phase 14 scale corpus derivation (2/14→5/14, pure authoring), Phase 13 corpus explorer THE PAYOFF (`dist/corpus/` staged 4-screen, the artifact Phase 18 now gives an arc), Phase 12 corpus-derivation foundation, Phase 11 automated derivation, Phase 9 build-drift guard, Phase 8 doc true-up, M6 pipeline walking skeleton, M5 ship, M3/M2/M1 — see `index.md` journal list + their journal articles. Trimmed to last 3 per the _CURRENT_STATE size budget overflow rule.)

## Cross-References

- HANDOFF.md · CLAUDE.md · README.md
- Knowledge wiki: **aml-wiki** (registered central store) — domain reference for typologies,
  indicators, FINTRAC/FinCEN/E-23. Linked via gitignored `wiki/` symlink; query with `/wiki-query`.
