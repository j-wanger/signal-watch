# Tasks

> ACTIVE PHASE ONLY. Closed phase blocks live VERBATIM in [articles/tasks-archive-pre-phase-44.md](articles/tasks-archive-pre-phase-44.md) (split out 2026-06-10 by the Phase-44 T6 hygiene trim; per-phase narrative also in the journal + articles/decisions/*). Archived blocks:
> - Phase 48: Brownfield history + LFCM — blueprint extension, triage-elicitation loop, synthetic-history probe + HTML blueprint report — M9
> - Phase 47: Demo-to-program design — the regulatorily defensible agentic AML program (blueprint + gate console) — M9
> - Phase 46: Corpus live derivation mode — local-model agentic derivation through the frozen gate (M7×M8 convergence)
> - Phase 45: Corpus demo presentation polish (pre-presentation day) — M7
> - Phase 44: Live extraction quality: targeted harness, classified fixes, processing page (live news) — M8
> - Phase 43: Live pipeline robustness + progressive presentation (live news) — M8
> - Phase 42: Anchor dossier view + per-scan network visualizer (live news) — M8
> - Phase 41: Entity-resolution schema enrichment (live news) — M8
> - Phase 40: Live red-flag extraction quality (measure-first) — M8
> - Phase 39: Live news QOL — streamed extraction progress + one-shot URL acquisition
> - Phase 38: Consolidate the live news subsystem (verify agent backend + watchlist management)
> - Phase 37: Per-indicator typology (corpus Typologies lens)
> - Phase 36: Persistence (DuckDB→parquet) + feedback watchlist — M8
> - Phase 35: News live local-model backend — M8
> - Phase 34: C/D-assignment verification pass — M7
> - Phase 33: Corpus completeness + full typology re-segmentation — M7
> - Phase 32: News stream — real gov-enforcement adverse-media source + presentation elevation — M8
> - Phase 31: M8 walking skeleton — the adverse-media / negative-news stream (a second atom stream) — M8
> - Phase 30: Data-source lens — surface the D1–D20 data-source axis as an institution coverage-by-data-source view — M7
> - Phase 29: Capability lens — surface the C1–C28 capability / D1–D20 data-source taxonomy as an institution coverage-by-capability view — M7
> - Phase 28: Corpus COMPLETENESS + grounded-coverage interview + methodical render + branding/compliance — M7
> - Phase 27: Make the corpus demo shippable — assess corpus output quality, then replan the fix — M7
> - Phase 26: Elevate the corpus demo to showcase quality — register re-translation + progressive render + build/lift wow + grouping/sort + landing page — M7
> - Phase 25: Corpus output quality — extract → translate (natural AML red flags) + the article-processing page — M7
> - Phase 24: Cross-corpus synthesis — a typology lens over the multi-source corpus — M7
> - Phase 23: FINTRAC depth — grow source #4 with the remaining anchorable strategic-intel products (Operational Alerts + Operational Briefs) — M7
> - Phase 22: FINTRAC as corpus source #4 (first cross-jurisdiction source; gate widened for FINTRAC "indicators" vocab; Crown-copyright non-commercial reproduction) — M7
> - Phase 21: OFAC as corpus source #3 (gate widened for OFAC vocab; US-federal public-domain) — M7
> - Phase 20: Multi-source spine, proven with FinCEN Alerts — M7
> - Phase 19: Durability closeout — commit corpus-explorer test harness + pin _rf_triage — M7
> - Phase 18: Corpus explorer arc — human gate + close-the-loop coverage payoff — M7
> - Phase 17: Complete corpus derivation + delete extract_red_flags (the real subtraction) — M7
> - Phase 16: Invert extraction (LLM extracts, deterministic gate disposes) + scale as proof — M7
> - Phase 15: Harden extraction faithfulness + fix shipped defects — M7
> - Phase 14: Scale corpus derivation (3 more CLEAN advisories → 5/14 live) — M7
> - Phase 13: Corpus explorer (advisory-selection front-end + per-indicator build-rec render) — M7 — THE PAYOFF
> - Phase 12: FinCEN corpus derivation foundation (deterministic spine all-14 + LLM proof slice) — COMPLETED + accepted (impl commit 90939b4)
> - Phase 11: Automated derivation (LLM-drafted signal config) — AUTOMATE — COMPLETED + accepted (impl commit c37dc39)
> - Phase 10: FinCEN corpus crawler (SCALE) — COMPLETED + accepted (commit 0c87c47)
> - Phase 9: Build-drift guard (zero-drift invariant) — COMPLETED + accepted
> - Phase 8: Doc true-up + provenance fix (M6 debt) — COMPLETED + accepted
> - Phase 7: Pipeline walking skeleton (M6) — COMPLETED + accepted

<!-- phase:phase-82-consume-sibling-northstar-evidence-at-scale -->
<!-- gate-log:phase-82 direction=approved delivery=accepted -->

<!-- status: PLANNED 2026-06-29 (STANDARD). Consume four sibling emissions so the GENERATED 376-case slice carries north-star-quality determinations AT SCALE — the same engine that decides the 2 hand-authored cases (Northgate FILES / Lakeshore CLEARS, Phase 73) now decides the slice end-to-end. substrate f7fbdb0→294d3e5 (P38 a9a088a org-fragment / P39 1483c84 predicate-reference / P40 978c8fe affirmative-mitigation); casework 076fb8e→04cc335 (P20 a059fc5 C15/C4 reconcile). Four consumes, three answering signal-watch's own briefs: (1) §12 FILE-at-scale (P39 predicate, the keystone — closes the measured 0-of-376 named_predicate gap; A1-preserving DATA, the engine already exposes the param at evidence_requirements.py line 310-312); (2) §12 CLEAR-at-scale (P40 mitigation, the clear-side mirror); (3) casework signing (P20 re-vendor, ~55 fail-close→sign); (4) merge-org class (P38 — un-aborts Phase 81's org track as a 4th SCORED population). Every gated build runs a RIGOROUS-engine measure FIRST (determine() with/without — never a coverage proxy, the Phase-81 C17 lesson); each track independently gated with an honest fallback (§12 degenerate → observable + brief; merge one-sided → abort, dist/merge byte-frozen; emit won't reproduce → emit-stability brief). A1: evidence_requirements.py BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate/casework; 8 non-merge dists byte-frozen; dist/merge the ONE conditional re-freeze. Real OFAC org names ship clean under 17 USC §105, false-positive trap; NO CC-BY-NC bytes in the repo. -->

## Phase 82 — Consume sibling emissions: north-star evidence AT SCALE (substrate P39 predicate + P40 mitigation §12 loops) + merge org-collision class (P38) + casework P20 signing re-vendor

> Phase 82 — direction gate 2026-06-29 (all_accept: true, NOT silent — A1 the T0 weakest ACCEPTED-AS-FALSIFIABLE = the T2b/c rigorous measure-first gate; AskUserQuestion — scope "Both clusters (full batch)" · §12 "measure-first-with-fallback, rule frozen" · merge "measure two-sidedness on our own path, one-sided → abort" · casework "re-vendor + funnel re-measure"; ledger Phase-82 appended). Sibling HEADs code-verified LIVE this session (3 background investigators, file:line, [[cross-pillar-review-verify-sibling-repo]]): aml-substrate @**294d3e5** (Phase 40 — P38 `a9a088a` org-fragment + P39 `1483c84` predicate-reference + P40 `978c8fe` affirmative-mitigation); aml-casework @**04cc335** (Phase 21 — P20 `a059fc5` C15/C4 reconcile + P21 `7398ddc` drift-hardening). All four CLI-wired/non-circular/two-sided where needed (none the Phase-77 unwired-or-circular trap). STANDARD ceremony (four independently-gated consumes; the one L = T3, the merge-org class). Ledger: assumption-ledger.md Phase 82. The A1 guard: `evidence_requirements.py` BYTE-UNCHANGED — P39/P40 enter as bundle DATA the engine ALREADY reads (the named_predicate_risk + mitigation_established params exist at line 310-312); build.py imports no spine/scorer/sibling/curate/casework; the 8 non-merge dists byte-frozen; `dist/merge` the ONE conditional re-freeze (gated on T2a two-sided). Compliance: real OFAC org names ship clean under 17 USC §105 (covers OFAC), false-positive trap (the synthetic org is NEVER the sanctioned entity); no CC-BY-NC bytes in the repo. Spec `specs/phase-82-consume-sibling-northstar-evidence-at-scale.md`.

- [x] T1 (M, DEPENDENCY) Foundation: re-verify sibling HEADs + emit the enriched substrate slice + re-pin | RED: a check asserting the scratch bundles carry `reference.named_predicate_risk` + `mitigation_evidence` AND `true_entities` carries ≥1 O-FRAG org cluster (`entity_ref ≠ cluster`) — fails before the emit. GREEN: re-verify substrate HEAD contains P38/39/40 (file:line, not loaded facts); run `--anchored --emit-screening --emit-eval-oracles` at a known-good param set (route around the Phase-81 ReplayError on the `--emit-evidence` path per the Phase-79 pattern); capture the enriched bundles + the org-fragment oracle to scratch; commit no-substrate replay captures. REFACTOR: record the param set + the substrate SHA in a capture manifest. VERIFY: the scratch emit reproduces (bundles carry the two new field families; true_entities has ≥1 O-FRAG cluster `entity_ref ≠ cluster`; param set + SHA recorded) OR the emit-stability abort is documented (escape hatch — note in the commit; the §12 tracks T4/T5 then degrade, the merge track T3 uses the clean `--emit-eval-oracles` path). | scope: docs/**, data/entity-spine/**, tests/fixtures/** | success: the scratch emit reproduces (bundles carry reference.named_predicate_risk + mitigation_evidence; true_entities has ≥1 O-FRAG cluster entity_ref≠cluster; param set + SHA recorded) OR the emit-stability abort documented
- [x] T2 (M) Measure-first gate: the four rigorous non-ship deltas | RED: the measure harness asserts each delta is COMPUTED via the rigorous engine (`determine()` / `resolution_scorer`), NOT a coverage proxy (the Phase-81 C17 lesson). GREEN: (a) merge-org two-sidedness — distill the org slice, score uphold/reject through `resolution_scorer`/`curate_merge_cases` on OUR path; (b) §12 predicate delta — `determine()` WITH/WITHOUT the predicate over the slice, count cases moved to FILE; (c) §12 mitigation delta — WITH/WITHOUT, count cases moved to CLEAR; (d) casework funnel delta — re-vendor in scratch, re-measure the signing funnel (moves must be fail-close→sign, not sign→fail-close). REFACTOR: record the four deltas + each gate decision (build/degrade/abort) in the journal. VERIFY: all four deltas recorded as non-ship numbers; each gate decision documented; `git diff --quiet scripts/evidence_requirements.py` (the measure READS, never edits). | scope: scripts/distill_sanctions_slice.py, scripts/resolution_scorer.py, scripts/curate_merge_cases.py, scripts/curate_workbench_cases.py, scripts/serve_workbench.py, data/entity-spine/** | success: all four deltas recorded as non-ship numbers && each gate decision documented && git diff --quiet scripts/evidence_requirements.py
- [x] T3 (L) (gated on T2a two-sided) Merge-org class build | RED: a `merge-console.test.mjs` case asserting the org basis renders + scores two-sided + the post-disposition `oracle` never leaks pre-adjudication — fails before curate. GREEN: IF two-sided → curate the org population into `data/merge/cases.json` (additive — no population-count ripple), hold validate↔curate EXACT parity (the Phase-76 lesson), rebuild `dist/merge`, extend the test + fixtures; IF one-sided → write the abort brief, `dist/merge` BYTE-FROZEN (escape hatch — note in the commit). REFACTOR: dedup the org curate path against the Phase-80 person path. VERIFY: ADVERSARIAL — verify the committed `cases.json` CONTENT + `git status` + `--check merge` (the Phase-79 staleness lesson, not the "wrote…" log). | scope: scripts/curate_merge_cases.py, scripts/distill_sanctions_slice.py, scripts/resolution_scorer.py, data/merge/cases.json, merge.html, dist/merge/**, scripts/build.py, tests/merge-console.test.mjs, tests/fixtures/merge-sanctions-org-oracle/**, docs/substrate-org-fragment-emit-PLAN-BRIEF.md | success: IF two-sided → node tests/merge-console.test.mjs (incl. org basis + firewall) && python3 scripts/build.py --check merge (re-frozen) && validate↔curate parity && honesty word-ban held; IF one-sided → git diff --quiet dist/merge && a substrate org brief updated
- [x] T4 (M) (gated on T2b non-degenerate; A1-frozen) §12 predicate file-at-scale consume | RED: a workbench determination regression asserting a slice case reaches the FILE bar WITH the predicate, WITHHELD WITHOUT — fails before consume. GREEN: IF non-degenerate → consume the predicate-reference into `curate_workbench_cases`/`serve_workbench` so slice cases carry `named_predicate_risk` from the grounded register (the file-at-scale beat); IF degenerate → render the predicate grounding as an observable + a brief (route to substrate asks #3/#4). REFACTOR: the same-predicate dedup lives in the consume layer, never the engine (if it forces an engine edit → STOP-and-surface). VERIFY: `git diff --quiet scripts/evidence_requirements.py` (A1) && the with/without determination-bar regression proves the rule frozen && `node tests/workbench.test.mjs`. NOT scripts/evidence_requirements.py. | scope: scripts/curate_workbench_cases.py, scripts/serve_workbench.py, data/workbench/**, data/casefile/**, workbench.html, tests/workbench.test.mjs | success: git diff --quiet scripts/evidence_requirements.py && the with/without determination-bar regression passes && node tests/workbench.test.mjs
- [x] T5 (M) (gated on T2c non-degenerate; A1-frozen) §12 mitigation clear-at-scale consume | RED: a regression asserting a slice case affirmatively CLEARS WITH `mitigation_evidence`, WITHHELD WITHOUT. GREEN: IF non-degenerate → consume `mitigation_evidence` + exculpatory legs so generated cases CLEAR (interacting with the §12 discovery-feed over-flag cells — affirmative clear, not clear-by-absence); IF degenerate → observable + note. REFACTOR: share the consume path with T4 (one evidence-ingest surface). VERIFY: `git diff --quiet scripts/evidence_requirements.py` (A1) && the cleared-path with/without regression proves the rule frozen && `node tests/workbench.test.mjs`. NOT scripts/evidence_requirements.py. | scope: scripts/curate_workbench_cases.py, scripts/serve_workbench.py, data/workbench/**, data/casefile/**, workbench.html, tests/workbench.test.mjs | success: git diff --quiet scripts/evidence_requirements.py && the cleared-path with/without regression passes && node tests/workbench.test.mjs
- [x] T6 (M) Casework re-vendor + funnel land | RED: casework selftests under the vendored copy + a funnel-direction assertion (moves are fail-close→sign, NOT sign→fail-close). GREEN: re-vendor 076fb8e→04cc335 (`scripts/vendor_casework.sh`), update `VENDORED_AT`, re-curate workbench cases, re-measure the signing funnel + record the new counts. REFACTOR: confirm the contract stays v0.3-compatible (curate hands the v0.3 view). VERIFY: casework selftests green under the vendored copy && the funnel re-measured + counts recorded && build.py imports no casework (grep) && the 8 non-merge dists byte-frozen (`git diff --quiet`). | scope: vendor/aml-casework/**, scripts/curate_workbench_cases.py, data/workbench/** | success: casework selftests green under the vendored copy && the funnel re-measured + counts recorded && build.py imports no casework (grep) && the 8 non-merge dists byte-frozen
- [x] T7 (S) Brief true-ups + cross-pillar + re-pin | RED: a grep for stale pins (f7fbdb0 / 076fb8e) in the briefs + the honesty word-ban grep. GREEN: re-pin substrate to 294d3e5 + casework to 04cc335 in the briefs/build-order; update the CONSUMED brief banners (org-fragment-emit, northstar-evidence #1/#2, casework-northstar-signing P20); true up `cross-pillar-build-order`; name the still-open frontier (substrate asks #3/#4 second-leg/ownership-edges, the C20 jurisdiction leg, the casework C17-sign gap). REFACTOR: honesty governor sweep across the touched docs. VERIFY: no stale pin in the consumed briefs (grep) && the banners updated && no catch-rate/lift/precision/recall wording (grep) && the open frontier named. | scope: docs/*-PLAN-BRIEF.md, docs/cross-pillar-build-order.md | success: no stale pin in the consumed briefs (grep) && banners updated && no catch-rate/lift/precision/recall wording (grep) && the open frontier named
- [x] T8 (M) Verify + CLAUDE.md + close | RED: the pre-present sequence (`--check all` → `node tests/*` → `pytest`). GREEN: `python3 scripts/build.py --check all` 9/9; `node tests/*.test.mjs` green; `uv run pytest` green; walk `tests/smoke-checklist.md`; update CLAUDE.md `## Current state` IN PLACE (the merge populations, the workbench §12 funnel/coverage, the new evidence consume, the re-pins). REFACTOR: CLAUDE.md ≤~200 lines (facts replaced in place, no per-phase bullet); active-phase gates. VERIFY: `python3 scripts/build.py --check all` 9/9 && `node tests/*.test.mjs` all green && `uv run pytest` green && CLAUDE.md current-state trued in place. | scope: CLAUDE.md, verification only | success: python3 scripts/build.py --check all 9/9 && node tests/*.test.mjs all green && uv run pytest green && CLAUDE.md current-state trued in place

> Phase 82 — abort: any UNSANCTIONED ship dist drift (the 8 non-merge dists, or `dist/merge` before its T2a gate passes) / a build.py spine-scorer-sibling-curate-casework import / an `evidence_requirements.py` change (incl. one forced by a §12 dedup — surface it, don't silently touch) / a real OFAC org name framed as a real sanctions catch (not the false-positive trap) / any CC-BY-NC dataset committed / a cohort or confusion count presented as a catch-rate/precision/lift/recall → STOP-and-surface. Measure-first: T2a one-sided/tautological → STOP the merge org track (T3 does NOT run) to a brief, `dist/merge` byte-frozen; T2b/c degenerate (DELTA≈0) → the §12 consume degrades to a rendered observable + a brief; the substrate `--emit-evidence` re-emit won't reproduce after bounded attempts → STOP T4/T5, route to a substrate emit-stability brief, keep the §12 loop at the current committed slice. The validate↔curate firewall parity (Phase-76) holds; the synthetic-substrate-anchored qualifier + the false-positive-trap framing govern any OFAC content.

<!-- phase:phase-81-consume-substrate-sanctions-arc -->
<!-- gate-log:phase-81 direction=approved delivery=accepted -->

<!-- status: PLANNED 2026-06-28 (STANDARD). Consume aml-substrate Phases 35–37 (the sanctions arc) @f7fbdb0 — 3 phases ahead of the Phase-34 pin 1f5901e. THREE consumes + a plan: (1) MERGE — an OFAC ORG-name collision case class (synthetic org's real-frequency legal name collides with a real public-domain OFAC SDN entity; the person-class's sibling), MEASURE-FIRST gated (T1a: two-sided non-circular GT-<hash> oracle? one-sided → ABORT merge org track to a brief, user-positioned); (2) WORKBENCH §12 — the substrate-P36 C17 exposure-via-ownership leg consumed as a NEW EVIDENCE atom (profile data + companion assembly; rule + evidence_requirements.py byte-frozen — "evidence-advance, rule frozen", user-positioned) so a customer with a sanctioned beneficial owner + a DISTINCT ML mechanism reaches the §12 bar via ≥2 INDEPENDENT legs, MEASURE-FIRST gated (T1b: non-degenerate cohort + moves ≥1 case to the bar? else ship a rendered observable + a brief); + render substrate-P37's geo observable (no leg); (3) PLAN-ONLY — docs/open-sanctions-data-fork-PLAN-BRIEF.md (per-source license matrix + the non-commercial boundary; OpenSanctions CC-BY-NC = no-ship, OFAC PD / UK OGL = ship; user-positioned). + reconcile the stale substrate-P35 brief (TF + broader-C7 substrate-CUT; org-name DONE) + cross-pillar-build-order true-up + re-pin substrate f7fbdb0. A1: evidence_requirements.py BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate; 8 non-merge dists byte-frozen; dist/merge the ONE sanctioned (gated) re-freeze. OFAC ORG names ship clean under 17 USC §105, false-positive trap (synthetic org ≠ sanctioned entity); NO CC-BY-NC bytes in the repo. -->

## Phase 81 — Consume substrate Phases 35–37: the sanctions arc (OFAC org name-collision merge class + the C17 exposure-via-ownership §12 leg + geo) + open-sanctions data-fork brief

> Phase 81 — direction gate 2026-06-28 (all_accept: true with A1+A2's measure-first gates user-positioned; AskUserQuestion — Q1 SCOPE "All three (+ P37 geo)" · Q2 OPEN-SANCTIONS "Plan-only brief + license matrix" · Q3 BAR-INVARIANT "Evidence-advance, rule frozen" · Q4 MEASURE-FIRST "Accept both abort fallbacks"; no unresolved reject/don't-know; ledger Phase-81 appended). Sibling HEAD code-verified LIVE this session (file:line, [[cross-pillar-review-verify-sibling-repo]]): aml-substrate @**f7fbdb0** (Phase 37 — P35 org-name OFAC `4f49e53` + P36 exposure-via-ownership C17 `1651b1e` + P37 geo `5b5cf32`; substrate CUT the old TF + broader-C7 asks); aml-casework unchanged @**076fb8e** (Phase-79 pin — does NOT ground C17, a named SIGN-gap handoff). STANDARD ceremony (cross-pillar consume of 3 phases + a conditional ship-dist touch + a plan; the one L = T4, the exposure leg). Ledger: assumption-ledger.md Phase 81. The A1 guard: `evidence_requirements.py` BYTE-UNCHANGED (the C17 leg is a profile-DATA atom + companion assembly — the engine derives legs from capabilities, never sees provenance); build.py imports no spine/scorer/sibling/curate; the 8 non-merge dists byte-frozen; `dist/merge` the ONE sanctioned re-freeze (gated on T1a two-sided). Compliance: real OFAC ORG names ship clean under 17 USC §105 (covers OFAC), false-positive trap (the synthetic org is NEVER the sanctioned entity); OpenSanctions CC-BY-NC stays PLAN-ONLY (no CC-BY-NC bytes in the repo). Spec `specs/phase-81-consume-substrate-sanctions-arc.md`.

- [x] T1 (M) Measure-first DUAL GATE — capture the substrate P35 org-collision slice + the P36 exposure-leg cohort | DONE 2026-06-28. **T1a → ABORT (structural one-sided):** substrate fragments PERSONS but NOT orgs (354 multi-record GT clusters, 100% person-only) → every org-name collision is between DISTINCT orgs → all-reject, 0 uphold, unfixable by scale/seed (verified on the f7fbdb0 12k/3m/seed0 emit; 10 flagged orgs, 3 sanctions-touching candidates, all correct-rejection). Merge org track (T2/T3) does NOT run; dist/merge BYTE-FROZEN; routed to a substrate org-fragment-emit brief (T5). **T1b → NON-DEGENERATE but DEFENSIVE-ONLY:** the C17 exposure leg fires on 13 customers with a sanctioned beneficial owner (14 org-BO + 4 person-BO occurrences); 9 reach file-ready (mechanism C8 + exposure + ≥1 other leg). BUT all 13 are oracle-CLEAR (zero overlap with the 38 oracle-file; substrate's sanctions_flag is label-blind, corr≈0 by design) → the leg is a DEFENSIVE-EXPOSURE basis, not a latent-laundering discriminator (it expands the Phase-78 over-flag cell). User pick 2026-06-28: **ship the leg framed as defensive-exposure** (A1-clean determination atom; the §12 discovery feed classifies all 13 as over-flag). | RED: two assertions (structural, replayable with NO substrate). T1a: scoring the substrate @f7fbdb0 `--anchored --emit-eval-oracles` ORG slice produces a NON-tautological TWO-SIDED structure — some org-name collisions are TRUE latent-entity matches (uphold the link) + some are common-name false positives (reject), split by the opaque GT-`<hash>` cluster (`entity_ref ≠ cluster`); `assert_no_*_leak` holds; the uphold/reject split reported. T1b: the C17 exposure cohort fires NON-degenerately over the `--anchored` slice AND (with the same-OFAC-hit dedup applied) ≥1 case reaches the determination bar BECAUSE of the exposure leg (a sanctioned BO + a DISTINCT ML mechanism = ≥2 independent legs). GREEN: drive substrate @f7fbdb0 `--anchored --emit-eval-oracles` (the org GT- oracle) + `--anchored` evidence/screening bundles (the exposure case material: customers whose `related_parties[]` carry a `sanctions_flag`) as TOOL-USE (subprocess, the curate/Phase-78-harness pattern; build.py never imports it; route around any param-set ReplayError per Phase-79); distill committed no-substrate-replayable captures (tests/fixtures/merge-sanctions-org-oracle/** + the candidate-relevant obs into data/entity-spine/**; org emails/PII masked); score the org collisions; measure the exposure cohort + the moves-to-bar count. REFACTOR: reuse resolution_scorer + the Phase-79/80 anchored-slice distillation (distill_sanctions_slice pattern). VERIFY: both captures replay non-tautological/non-degenerate with no substrate; firewall holds. DUAL ABORT GATE (escape hatch — document in the commit if taken): T1a one-sided/tautological → STOP the merge org track (T2/T3 do NOT run), `dist/merge` byte-frozen, write a substrate org-emit-two-sidedness brief; T1b degenerate → the C17 leg degrades to a rendered observable + a brief (honest null); proceed to whatever survives + T5 + T6. | scope: scripts/resolution_scorer.py, scripts/curate_merge_cases.py, scripts/distill_sanctions_slice.py, tests/fixtures/merge-sanctions-org-oracle/**, data/entity-spine/** | success: both committed captures replay (org two-sided + exposure cohort/moves-to-bar) with no substrate && the firewall assertions pass — with the two gate decisions recorded (or a documented abort + the corresponding brief)
- [x] T2 (M) (gated on T1a two-sided) Curate the OFAC ORG name-collision merge case class | ABORTED 2026-06-28 — T1a is STRUCTURALLY one-sided (substrate fragments persons not orgs); the merge org track does NOT run. dist/merge BYTE-FROZEN, no cases.json/curate change. The honest non-result → docs/substrate-org-fragment-emit-PLAN-BRIEF.md (T5). | RED: curate_merge_cases `--selftest` + build.py `validate_merge_cases` assert a new `sanctions-org-collision` basis sourced from the T1a slice — two-sided (uphold + reject), closed adjudication vocab, referential integrity, the masking firewall (no real PII beyond public-domain OFAC org names; the synthetic org ≠ sanctioned entity in evidence), the post-disposition `oracle` never leaking into pre-adjudication evidence. GREEN: extend curate_merge_cases.py with the org basis (sourced from the T1a capture); regenerate `data/merge/cases.json`; mirror `validate_merge_cases` in build.py in EXACT parity (the Phase-76 lesson). REFACTOR: reuse the Phase-80 sanctions-collision enumeration shape. VERIFY: `python3 scripts/curate_merge_cases.py --selftest` (incl. the new org basis + two-sided assertion + broken fixtures) && validate↔curate parity. NOTE: gated — skipped entirely if T1a takes its abort path. | scope: scripts/curate_merge_cases.py, data/merge/cases.json, scripts/build.py | success: python3 scripts/curate_merge_cases.py --selftest && python3 scripts/build.py --check merge
- [x] T3 (M) (gated on T2) Re-freeze dist/merge with the org-collision class + update the test | ABORTED 2026-06-28 — gated on T2 (aborted). dist/merge BYTE-FROZEN; no merge.html / test / dist change this phase. | RED: merge-console.test.mjs asserts the `sanctions-org-collision` basis renders in Queue (grouped by basis) + the Verdict shows the latent-truth oracle + the false-positive-trap framing (synthetic org ≠ sanctioned entity); the honesty word-ban holds (no catch-rate/lift/precision/recall). GREEN: rebuild merge (`build.py merge`); re-freeze `dist/merge`; render the org basis + framing in merge.html. REFACTOR: none. VERIFY: `python3 scripts/build.py --check all` → the 8 non-merge dists byte-frozen + `dist/merge` re-frozen to the new bytes; `node tests/merge-console.test.mjs` green; the honesty governor holds; `git diff --quiet scripts/evidence_requirements.py`; ADVERSARIAL: verify the committed `cases.json` CONTENT + `git status` + `--check merge` (the Phase-79 staleness lesson — not the "wrote…" log). NOTE: gated — skipped if T1a aborts. | scope: merge.html, dist/merge/**, tests/merge-console.test.mjs, scripts/build.py | success: python3 scripts/build.py --check all && node tests/merge-console.test.mjs
- [x] T4 (L) (the exposure leg gated on T1b non-degenerate) Workbench §12: the C17 exposure-via-ownership leg as a new EVIDENCE atom + P37 geo render | DONE 2026-06-28 — **T1b DEGENERATE (corrected measurement error):** the C17 leg moves 0 cases to the ML bar — the 13 sanctioned-BO cohort lacks an ML MECHANISM (C2/C3/C5; C8→ML-A3 leg, C14→kyc), so a leg can't satisfy mechanism+2legs (verified WITH-C17 delta=0). → shipped **OBSERVABLE-ONLY** (user pick): a `/sanctions-c17-exposure` route + workbench panel (`sanctionsC17PanelHTML`) + `serve_workbench.sanctions_c17_exposure_consume` over `data/casefile/sanctions-c17-exposure-demo.bundle.json` (a masked f7fbdb0 case — subject CLEAN, sanctioned BO "Samantha Jung" = a label-blind OFAC false positive) surface the exposure + P37 `counterparty_country` (AE; no leg); the LIVE engine SHOWS the case does NOT reach the bar (verdict needs_more_info). `evidence_requirements.py` + the profile BYTE-UNCHANGED (no atom that never fires). workbench.test.mjs 167→178; serve_workbench --selftest green (funnel 202/111/63 + coverage 128/376 byte-stable). | RED: a serve_workbench/workbench-harness DETERMINATION-BAR REGRESSION asserting (a) a customer with a sanctioned beneficial owner + a DISTINCT ML mechanism reaches the determination bar via ≥2 INDEPENDENT legs WITH the C17 exposure leg, and is WITHHELD WITHOUT it (the leg does real work, the RULE is not weakened); (b) the same-OFAC-hit dedup — a C17 exposure leg + a C14 escalation tracing to the SAME sanctioned party count as ONE independent leg, not two; (c) `git diff --quiet scripts/evidence_requirements.py` (the leg is a profile-DATA atom + companion assembly). GREEN: add the C17 leg atom to `data/workbench/evidence-requirements.json` (a new leg-kind evidence atom mapped to the money_laundering crime_type); assemble it + the same-hit dedup in serve_workbench from each case's `related_parties[]`+`sanctions_flag` (substrate's exposure detector reads, doesn't emit — signal-watch COMPUTES the leg); render the §12 sanctioned-BO-exposure beat in workbench.html; render substrate-P37's richer `counterparty_country` observable (22 countries / FATF tail) with NO leg (C20 named as a future brief item). REFACTOR: reuse the curate_workbench_cases bundle-reading + the existing leg-counting (no engine edit). VERIFY: `python3 scripts/serve_workbench.py --selftest` (the bar regression + dedup) && `node tests/workbench.test.mjs` && `python3 tests/gather_quality_harness.py --check` && `git diff --quiet scripts/evidence_requirements.py` (A1). NOTE: if T1b degenerate, the leg ships as a rendered observable only (no §12-advance claim) + a brief; the geo render + the workbench test still land. | scope: data/workbench/evidence-requirements.json, scripts/curate_workbench_cases.py, scripts/serve_workbench.py, workbench.html, tests/workbench.test.mjs | success: python3 scripts/serve_workbench.py --selftest && node tests/workbench.test.mjs && git diff --quiet scripts/evidence_requirements.py
- [x] T5 (S) Author the open-sanctions data-fork brief + reconcile the stale substrate-P35 brief + cross-pillar true-ups | DONE 2026-06-28 — authored `docs/open-sanctions-data-fork-PLAN-BRIEF.md` (the ship-compliance per-source license matrix: OFAC PD / FinCEN PD / UK HMT OGL / Canada → ship; EU/UN conditional; OpenSanctions CC-BY-NC → NO-SHIP — the demo's buy-in purpose is plausibly commercial) + `docs/substrate-org-fragment-emit-PLAN-BRIEF.md` (the T1a abort handoff: substrate must fragment ORGS) + `docs/substrate-exposure-signal-PLAN-BRIEF.md` (the T1b degenerate handoff: a discriminating exposure signal). Reconciled the stale substrate-P35 brief (TF + broader-C7 substrate-CUT with cut-rationale; org-name built-but-one-sided; Phase-36/37 newly consumed). Trued up cross-pillar-build-order to f7fbdb0 + the casework-C17-SIGN gap. Honesty governor swept clean (disclaimers reworded to "no rate, score, or multiplier is claimed"). | RED: a grep asserting (a) no open-sanctions data-fork brief exists yet, (b) the substrate-P35 brief still names TF + broader-C7 as live asks (now substrate-CUT) + org-name as a gap (now DONE), (c) cross-pillar-build-order shows the stale Phase-34 pin (not Phase-37 @f7fbdb0), (d) CLAUDE.md/briefs carry no casework-C17-SIGN-gap note. GREEN: author `docs/open-sanctions-data-fork-PLAN-BRIEF.md` (the per-source license/compliance matrix — OFAC PD/17 USC §105 → ship; UK HMT OGL → ship+attribution; EU/UN conditional; OpenSanctions CC-BY-NC → no-ship; the non-commercial boundary documented + what substrate should emit, Stage-2/3; cross-references substrate-open-reference-data-fork-PLAN-BRIEF.md); reconcile the stale substrate-P35 brief (TF/C7 substrate-CUT with the cut-rationale; org-name DONE); true up cross-pillar-build-order to substrate-P37 HEAD + this consume; note the casework-C17-SIGN gap (a named handoff). REFACTOR: none. VERIFY: the open-sanctions brief exists + the license matrix present; the P35 brief reconciled; build-order trued up; the new brief swept clean by the honesty governor (no catch-rate/lift/precision/recall — the Phase-78 doc-gap lesson); `uv run pytest` green. | scope: docs/*-PLAN-BRIEF.md, docs/cross-pillar-build-order.md, CLAUDE.md | success: a docs/open-sanctions-data-fork-PLAN-BRIEF.md with the license matrix && the substrate-P35 brief reconciled (TF/C7 cut, org-name done) && cross-pillar-build-order trued up to f7fbdb0
- [x] T6 (S) Full verification + true-up | DONE 2026-06-28 — `--check all` 9/9 byte-frozen (ZERO dist drift — workbench is companion-only, no dist touched; `dist/merge` untouched per the T1a abort); A1 held (`evidence_requirements.py` + `data/workbench/evidence-requirements.json` byte-unchanged); build.py firewall clean (no spine/scorer/serve_workbench/sibling/curate import); workbench 178 / merge 88 / gate 68 / triage 93 / news 150; serve_workbench/entity_spine/resolution_scorer/curate_merge_cases --selftest PASS; gather_quality_harness + news_live + determination-validation harness PASS; `uv run pytest` 27; honesty doc-sweep clean. CLAUDE.md ## Current state trued up (the Phase-81 sanctions-arc consume; observable-only C17; substrate f7fbdb0 pin distinct from the fc98b09 slice). | RED: n/a. GREEN: CLAUDE.md ## Current state trued up (the substrate P35–37 sanctions-arc consume — the merge org-collision class [if T1a landed] + the workbench C17 exposure leg [if T1b landed] + the P37 geo observable + the open-sanctions plan-brief + the substrate re-pin f7fbdb0; replace-in-place, no per-phase narrative); the merge/workbench durable snapshots + the test list updated. VERIFY (tiered): `python3 scripts/build.py --check all` (9 targets; the 8 non-merge dists byte-frozen + dist/merge re-frozen IF the org track landed) && grep-guard build.py imports no spine/scorer/serve_workbench/sibling/curate && `git diff --quiet scripts/evidence_requirements.py` (A1) && node tests/{merge-console,workbench,gate-console,triage-console,news-stream}.test.mjs && python3 scripts/curate_merge_cases.py --selftest && python3 scripts/serve_workbench.py --selftest && python3 scripts/entity_spine.py --selftest && python3 scripts/resolution_scorer.py --selftest && python3 tests/gather_quality_harness.py --check && python3 tests/news_live_test.py && uv run pytest. | scope: CLAUDE.md, scripts/**, tests/** | success: --check all green (only dist/merge re-frozen if the org track landed; 8 non-merge byte-frozen) && no spine/scorer/sibling/curate import in build.py && evidence_requirements.py byte-unchanged && all arcs + selftests + harnesses + pytest green && CLAUDE.md trued up

> Phase 81 — abort: any UNSANCTIONED ship dist drift (the 8 non-merge dists, or `dist/merge` before its T1a gate passes) / a build.py spine-scorer-sibling-curate import / an `evidence_requirements.py` change (incl. one forced by the C17 dedup — surface it, don't silently touch) / a real OFAC name framed as a real sanctions catch (not the false-positive trap) / any CC-BY-NC dataset committed to the repo / a cohort or confusion count presented as a catch-rate/precision/lift/recall → STOP-and-surface. Measure-first: T1a one-sided/tautological → STOP the merge org track (T2/T3 do NOT run) to a brief, `dist/merge` byte-frozen; T1b degenerate → the C17 leg degrades to a rendered observable + a brief (honest null). The open-sanctions brief (T5) + P37 geo render + the verification (T6) run regardless. The validate↔curate firewall parity (Phase-76) holds; the synthetic-substrate-anchored qualifier + the false-positive-trap framing govern any OFAC content.

> Phase 81 — ALL T1-T6 [x] (T2/T3 ABORTED — the merge org track is structurally one-sided). STANDARD ceremony. Direction gate 2026-06-28 (Q1 "All three (+ P37 geo)" · Q2 "Plan-only brief + license matrix" · Q3 "Evidence-advance, rule frozen" · Q4 "Accept both abort fallbacks"). **Both planned consumes hit their measure-first branches — the phase delivered honest non-results + an observable beat + briefs, NOT the two scoped "wins".** **T1a:** the OFAC ORG-name collision is STRUCTURALLY one-sided (substrate fragments persons not orgs — 354 multi-record GT clusters, 0 org; all-reject, unfixable by scale/seed) → the merge org track (T2/T3) ABORTED, `dist/merge` BYTE-FROZEN → `docs/substrate-org-fragment-emit-PLAN-BRIEF.md`. **T1b:** the C17 exposure leg is DEGENERATE as a determination advance (13 sanctioned-BO customers, ALL oracle-clear [label-blind, corr≈0]; a C17 leg moves 0 to the bar — the cohort lacks an ML mechanism; a planning-stage measurement error [a loose proxy counting ≥2 related parties as a leg] caught by the T4 surface-map workflow's rigorous data-path read + corrected with the user) → shipped **OBSERVABLE-ONLY** (user pick): the `/sanctions-c17-exposure` route + panel + the masked demo bundle surface the exposure and the live engine SHOWS it does NOT reach the bar; P37 geo rendered as an observable; the open-sanctions ship-compliance license matrix + 2 substrate handoff briefs authored; the P35 brief reconciled; cross-pillar-build-order + CLAUDE.md trued up. **Adversarial review (4 dims → 10 opus agents, the reviewers RAN the commands): 0 must-fix · 2 should-fix FIXED** (incomplete email masking in the demo bundle's `resolution_edges` → ALL @-strings masked to example.test; T4/T5/T6 [x] + this closure block) **· 14 praise** (the T1b degeneracy + the org-fragment numbers reproduce EXACTLY against the live engine; A1/firewall/dist held; compliance airtight; substrate-pin coherence right; the refuted findings — the "detection lift" negation [the project's gate-accepted convention] + the T1a-capture criterion [waived by the documented abort branch] — correctly dismissed). A1: `evidence_requirements.py` + the profile BYTE-UNCHANGED; build.py imports nothing new; all 9 dists byte-frozen (`dist/merge` untouched); `uv run pytest` 27; workbench 178/0. Companion + docs + dev-wiki only (NO dist change). Delivery gate: pending user acceptance.

<!-- phase:phase-80-consume-substrate-sanctions-screening -->
<!-- gate-log:phase-80 direction=approved delivery=pending -->

<!-- status: PLANNED 2026-06-27 (STANDARD). Consume aml-substrate Phase 34 @1f5901e (seam-5 sanctions-screening realism: dead sanctions_flag made LIVE under --anchored via a label-blind real-OFAC-watchlist NAME COLLISION + the revived non-tautological C14 KYC-integrity branch). Both consumes: (1) merge console — an OFAC watchlist name-collision case class (synthetic party's real-frequency name collides with a real public-domain OFAC entry → same latent entity vs common-name false positive), MEASURE-FIRST gated (T1: two-sided non-circular GT-<hash> oracle? one-sided → ABORT merge to workbench-only + brief, user-positioned); (2) workbench §12 — a non-tautological sanctions-driven C14 leg lights the kyc determination (additive to the v0.5 slice). + the substrate P35 brief + cross-pillar true-ups. A1: evidence_requirements.py byte-unchanged; build.py imports no spine/scorer/sibling/curate; 8 non-merge dists byte-frozen; dist/merge the ONE sanctioned (gated) re-freeze. OFAC ships clean under 17 USC §105, framed strictly as the false-positive trap (synthetic party ≠ sanctioned entity). -->

## Phase 80 — Consume substrate Phase 34: OFAC name-collision merge case class + non-tautological C14 §12 leg

> Phase 80 — direction gate 2026-06-27 (all_accept: true with A1's measure-first gate user-positioned; AskUserQuestion — "Consume P34: merge + workbench" + the one-sided fallback "Abort merge → companion + brief"; no unresolved reject/don't-know; ledger Phase-80 appended). Both sibling HEADs code-verified LIVE this session (file:line, [[cross-pillar-review-verify-sibling-repo]]): aml-substrate @**1f5901e** (Phase 34 — seam-5 sanctions screening; Phases 32/33 already consumed in the Phase-79 slice), aml-casework @**076fb8e** (Phase 19 — at the Phase-79 vendor pin, nothing new). STANDARD ceremony (cross-pillar + a conditional ship-dist touch; the one L = T1, the measure-first gate). Ledger: assumption-ledger.md Phase 80. The A1 guard: `evidence_requirements.py` BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate; the 8 non-merge dists byte-frozen; `dist/merge` the ONE sanctioned re-freeze (gated on T1 two-sided). Compliance: real OFAC names ship clean under 17 USC §105 (US-federal public domain — covers OFAC), framed STRICTLY as the false-positive trap (the synthetic party is NEVER the sanctioned entity). Spec `specs/phase-80-consume-substrate-sanctions-screening.md`.

- [x] T1 (L) Measure-first: capture the Phase-34 `--anchored` sanctions emit + assess two-sidedness — THE ABORT GATE | RED: an assertion (structural, replayable with NO substrate) that scoring the Phase-34 `--anchored --emit-eval-oracles` sanctions slice produces a NON-tautological structure — the watchlist-name collisions split TWO-SIDED (some TRUE latent-entity matches [uphold the link] + some common-name false positives [reject], distinguished by the opaque GT-`<hash>` cluster, `entity_ref ≠ cluster`); `assert_no_*_leak` holds; the same/distinct split over the population reported. GREEN: drive substrate @1f5901e `--anchored --emit-eval-oracles` (the merge GT- oracle) + `--anchored` evidence/screening bundles (the C14-from-sanctions case material) as TOOL-USE (subprocess, the curate_workbench/Phase-78-harness pattern; build.py never imports it; route around any param-set ReplayError per Phase-79); distill a committed no-substrate-replayable capture (tests/fixtures/merge-sanctions-oracle/** + the candidate-relevant obs into data/entity-spine/**; emails/PII masked); score the collisions; record the two-sidedness count. REFACTOR: reuse resolution_scorer + the Phase-79 anchored-slice distillation. VERIFY: the capture replays a non-tautological two-sided result with no substrate; firewall holds. ABORT GATE (escape hatch — document in the commit if taken): emit won't reproduce after bounded attempts OR the oracle is one-sided/tautological → STOP the merge track (T2/T3 do NOT run), write the honest non-result + a substrate emit-two-sidedness brief; proceed to T4 (workbench) + T5. | scope: scripts/resolution_scorer.py, scripts/curate_merge_cases.py, tests/fixtures/merge-sanctions-oracle/**, data/entity-spine/** | success: the committed capture replays a non-tautological two-sided result with no substrate && the firewall assertions pass — OR a documented abort + a substrate emit-two-sidedness brief authored
- [x] T2 (M) (gated on T1 two-sided) Curate the OFAC name-collision merge case class | RED: curate_merge_cases `--selftest` + build.py `validate_merge_cases` assert a new `sanctions-name-collision` basis sourced from the T1 slice — two-sided (uphold + reject), closed adjudication vocab, referential integrity, the masking firewall (no real PII beyond public-domain OFAC names; the synthetic party ≠ sanctioned entity in evidence), the post-disposition oracle never leaking into pre-adjudication evidence. GREEN: extend curate_merge_cases.py with the basis (sourced from the T1 capture); regenerate `data/merge/cases.json`; mirror `validate_merge_cases` in build.py in EXACT parity (the Phase-76 lesson). REFACTOR: reuse `enumerate_substrate_scored` shape. VERIFY: `python3 scripts/curate_merge_cases.py --selftest` (incl. the new basis + two-sided assertion + broken fixtures) && validate↔curate parity. NOTE: gated — skipped entirely if T1 takes its abort path. | scope: scripts/curate_merge_cases.py, data/merge/cases.json, scripts/build.py | success: python3 scripts/curate_merge_cases.py --selftest && python3 scripts/build.py --check merge
- [x] T3 (M) (gated on T2) Re-freeze dist/merge with the sanctions case class + update the test | RED: merge-console.test.mjs asserts the `sanctions-name-collision` basis renders in Queue (grouped by basis) + the Verdict shows the latent-truth oracle + the false-positive-trap framing (synthetic party ≠ sanctioned entity); the honesty word-ban holds (no catch-rate/lift/precision/recall). GREEN: rebuild merge (`build.py merge`); re-freeze `dist/merge`; render the basis + framing in merge.html. REFACTOR: none. VERIFY: `python3 scripts/build.py --check all` → the 8 non-merge dists byte-frozen + `dist/merge` re-frozen to the new bytes; `node tests/merge-console.test.mjs` green; the honesty governor holds; `git diff --quiet scripts/evidence_requirements.py`; ADVERSARIAL: verify the committed `cases.json` CONTENT + `git status` + `--check merge` (the Phase-79 staleness lesson — not the "wrote…" log). NOTE: gated — skipped if T1 aborts. | scope: merge.html, dist/merge/**, tests/merge-console.test.mjs, scripts/build.py | success: python3 scripts/build.py --check all && node tests/merge-console.test.mjs
- [x] T4 (M) Workbench §12: a non-tautological sanctions-driven C14 leg (companion-only; runs regardless of the merge abort) | RED: a serve_workbench/workbench-harness selftest asserting ≥1 case whose C14 fires on a sanctions-FLAGGED under-classified party (NOT the EDD≔HIGH tautology) lands in the workbench, and the §12 determination loop lights its kyc leg from that real signal. GREEN: curate the sanctions-driven C14 case material from the T1 `--anchored` Phase-34 bundles into the workbench (ADDITIVE + provenance-tagged to the existing v0.5 slice — not a population swap); wire it through the existing C14→kyc determination path (Phase 72). REFACTOR: reuse curate_workbench_cases bundle-reading. VERIFY: `python3 scripts/serve_workbench.py --selftest` (the new C14 case + the kyc leg lights) && `node tests/workbench.test.mjs` && `python3 tests/gather_quality_harness.py --check` && `git diff --quiet scripts/evidence_requirements.py` (A1). | scope: scripts/curate_workbench_cases.py, scripts/serve_workbench.py, data/workbench/**, tests/workbench.test.mjs | success: python3 scripts/serve_workbench.py --selftest && node tests/workbench.test.mjs && git diff --quiet scripts/evidence_requirements.py
- [x] T5 (S) Author the substrate P35 brief + cross-pillar true-ups + drift corrections | RED: a grep asserting (a) no substrate-P35 brief exists yet, (b) cross-pillar-build-order shows the stale Phase-32/33 pin (not Phase-34 @1f5901e), (c) CLAUDE.md still reads the casework txn-less C14 party-leaf as fails-closed, (d) the stale FOLLOW-ON markers (Phase-64 gating engine / Phase-65 agentic gather already built) sit unreconciled in tasks.md. GREEN: author `docs/substrate-p35-determination-signals-PLAN-BRIEF.md` (TF slice / broader C7 / org-name sanctions / Stage-2-3 open data — the now-confirmed P35+ frontier, pinned @1f5901e); true up cross-pillar-build-order to the Phase-34 HEAD + this consume; correct the casework C14-fails-closed drift in CLAUDE.md (it completes via the resolving party leaf — test reaches `signed`); reconcile the 4 stale FOLLOW-ON markers. REFACTOR: none. VERIFY: the P35 brief exists; build-order trued up; the C14 drift corrected; `git diff --stat` shows no dist/** change beyond T3's (gated) re-freeze; `uv run pytest` green. | scope: docs/*-PLAN-BRIEF.md, docs/cross-pillar-build-order.md, CLAUDE.md, .dev-wiki/tasks.md | success: a substrate-p35 brief committed && the casework C14-fails-closed drift corrected in CLAUDE.md && git diff --stat shows no unsanctioned dist/** change

> Phase 80 — abort: any UNSANCTIONED ship dist drift (the 8 non-merge dists, or `dist/merge` before its T1/T2 gate passes) / a build.py spine-scorer-sibling-curate import / an `evidence_requirements.py` change / a real OFAC name framed as a real sanctions catch (not the false-positive trap) / a confusion number presented as a catch-rate/precision/lift/recall → STOP-and-surface. Measure-first (T1, the Phase-77 abort rule): the anchored sanctions emit won't reproduce after bounded attempts / the oracle is one-sided or tautological → STOP the merge track (T2/T3 do NOT run) to workbench-only + a substrate emit-two-sidedness brief; deliver T4 (the workbench C14 leg) + T5 (the P35 brief + true-ups) + the honest non-result. The validate↔curate firewall parity (Phase-76) holds; the synthetic-substrate-anchored qualifier + the false-positive-trap framing govern any OFAC content.

> Phase 80 — ALL T1-T5 [x]. STANDARD ceremony. Direction gate "Consume P34: merge + workbench" + one-sided fallback "Abort merge → companion + brief" (2026-06-27, AskUserQuestion). **The measure-first gate cleared GREEN (no abort):** the Phase-34 `--anchored` sanctions emit yields a genuinely TWO-SIDED non-circular merge oracle (24 sanctions-touching candidates, 11 uphold / 13 reject) — the two-sidedness comes from the Phase-32 fragment overlay running ALONGSIDE sanctions (a flagged record + its same-person fragment that evaded screening = uphold; two strangers sharing a watchlisted name = the common-name false positive = reject), NOT from the label-blind sanctions flag alone (one-sided by construction — no party IS a designated person). BOTH tracks landed: (MERGE) `dist/merge` re-frozen (133,179 B) with the OFAC name-collision case class as a THIRD scored population (29 anchored + 24 sanctions + 13 synthetic = 66 cases; `scripts/distill_sanctions_slice.py` → `data/entity-spine/substrate-sanctions-slice.json`; validate↔curate EXACT parity, 8-tamper parity-on-broken verified incl. the one-sided guard; merge test 88/0). (WORKBENCH) the §12 sanctions-driven C14 leg lights KYC-A1 + casework's Phase-19 party-leaf grounding SIGNS it end-to-end (`sanctions_c14_consume` over `data/casefile/sanctions-c14-demo.bundle.json`, disposition `file`) — the txn-less party-leaf C14 that used to fail-closed; companion-only, the population/funnel/coverage byte-unchanged. + the substrate P35 brief + cross-pillar-build-order true-up + the casework C14-fails-closed drift correction + the FOLLOW-ON reconciliation. A1: `evidence_requirements.py` BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate; 8 non-merge dists byte-frozen + dist/merge re-frozen. `--check all` 9/9; `uv run pytest` 27; merge 88/0; workbench 169/0; curate selftest (29/24/13, 10 broken fixtures). Delivery gate: pending the orchestrator's delivery-flow commit (2026-06-27).

<!-- phase:phase-79-consume-sibling-emissions -->
<!-- gate-log:phase-79 direction=approved delivery=accepted -->
<!-- delivery: ACCEPTED 2026-06-27, committed 031a33a -->

<!-- status: PLANNED 2026-06-27 (STANDARD). Floor + gated-upside bundle for the two Phase-77-deferred consumes, both code-verified RESOLVED sibling-side (casework Phase 19 @076fb8e built _c3_fan_in; substrate Phase 32 @c099259 mints entity_ref≠cluster anchored fragments). FLOOR (committed): Lakeshore CASE-B signs cleared via fan-in C3 (re-vendor b3546d4→076fb8e, no-regression checkpoint, companion-only). GATED UPSIDE: the merge real-data oracle — MEASURE-FIRST companion-only (the live --anchored --emit-eval-oracles run CRASHED today with substrate ReplayError) → gate the dist/merge re-freeze on a clean two-sided non-tautological result; ABORT (Phase-77 rule) → consensus + a substrate emit-stability brief, no dist touch. A1: evidence_requirements.py byte-unchanged; build.py imports no spine/scorer/sibling/curate; 8 non-merge dists byte-frozen; dist/merge the ONE sanctioned (gated) re-freeze. -->

## Phase 79 — Consume sibling emissions: Lakeshore fan-in C3 floor + the merge real-data oracle (gated)

> Phase 79 — direction gate 2026-06-27 (Bundle, gated upside; assumption positions taken via AskUserQuestion — all_accept warned-not-silent; no unresolved reject/don't-know; ledger Phase-79 appended). Direction = the user's "consume sibling repos" (plural) + the floor+gated-upside structure. Both Phase-77-deferred blocks code-verified RESOLVED sibling-side this session (file:line, not the loaded snapshot): casework Phase 19 (ed93a0d, feat/phase-1a-deterministic-verifiers @076fb8e) built `_c3_fan_in`; substrate Phase 32 (31cb439, main @c099259/Phase 33) mints `entity_ref ≠ cluster` anchored fragments under `--anchored` (opaque `GT-<sha1>` cluster ids disjoint from every resolver input; 17 multi-ref clusters / refs 255 > clusters 231 at n=400/seed0). STANDARD ceremony (cross-pillar + a conditional ship-dist touch). Ledger: assumption-ledger.md Phase 79. The A1 guard: `evidence_requirements.py` BYTE-UNCHANGED; build.py imports no spine/scorer/sibling/curate; the 8 non-merge dists byte-frozen; dist/merge the ONE sanctioned re-freeze (gated on T3). Spec `specs/phase-79-consume-sibling-emissions.md`.

- [x] T1 (M) Re-vendor casework to Phase 19 (fan-in C3) + no-regression checkpoint | DONE 2026-06-27. Re-vendored aml-casework `b3546d4→076fb8e` (Phase 19, feat/phase-1a-deterministic-verifiers) via `scripts/vendor_casework.sh`; wheel rebuilt. GREEN: `_c3_fan_in` now in vendored `src/aml_casework/grounding_replay.py` (was absent). VERIFY: `serve_workbench --selftest` PASS — funnel {auto-clear 202, human-gate 111, review 63} + coverage 128/376 held; `evidence_requirements.py` byte-unchanged (A1); change surface only `vendor/aml-casework/**` + planning artifacts (no `data/workbench/**` → funnel byte-stable by construction). The selftest still reporting "Lakeshore CASE-B fails-closed on fan-in C3" is the correct RED for T2 (runtime now supports fan-in; the Lakeshore bundle isn't shaped to it yet). | RED: a check asserting the vendored `grounding_replay` exposes `_c3_fan_in` (absent in the b3546d4 vendor). GREEN: run `scripts/vendor_casework.sh` to sync the vendored copy to sibling 076fb8e; update VENDORED_AT. REFACTOR: none. VERIFY: the gate funnel (202/111/63) holds; `git diff --quiet scripts/evidence_requirements.py`; `serve_workbench --selftest` + `serve_chain --selftest` green under the new vendor. | scope: vendor/aml-casework/**, scripts/vendor_casework.sh, scripts/serve_chain.py | success: grep `_c3_fan_in` in vendor/aml-casework/**/grounding_replay.py hits && python3 scripts/serve_workbench.py --selftest && git diff --quiet scripts/evidence_requirements.py
- [x] T2 (M) Lakeshore CASE-B signs `cleared` end-to-end via fan-in C3 | DONE 2026-06-27. Lakeshore CASE-B SIGNS `cleared` end-to-end via casework's Phase-19 fan-in C3 (`signed:true, disposition:cleared, blocking:[]` — verified live against the vendored casework). The HONEST fix (user-confirmed "compress to ≤7d"): re-timed CASE-B's 8 catering credits into a 6d2h window (04-02..04-08, same 8 distinct clients/amounts/memos — only dates tighten; the fan-in pattern is REAL, not fabricated); authored `data/casefile/case-b.bundle.json` (casework-contract translation mirroring casework's verified fan-in fixture, grounded on the FAITHFUL `fin-2023-alert001:IND-02` multi-originator-deposit indicator + a documented exculpatory settlement + neutral/exculpatory claims, NO crime_type/inculpatory); added `lakeshore_cosign_consume` + selftest assertion to serve_workbench (mirroring `cleared_demo_consume`, selftest-proven not UI-coupled — keeps `casefile_determination` pinned/venv-free); regenerated the drifted `CASE-B.detail.json` (7 timestamps only). FAITHFULNESS NOTE (disclosed in bundle provenance + flagged to user): case.json's AL-LS-C3 keeps `IND-05` (social-media-solicitation) for matched-pair signal symmetry with CASE-A; the bundle grounds on the faithful IND-02 — a candidate future fix needing both cases re-grounded. VERIFY: serve_workbench --selftest PASS (Lakeshore co-sign + matched pair Northgate-files/Lakeshore-clears + drift bridge); `--check all` 9/9 byte-frozen (0 dist touched); node workbench.test.mjs 169/0; uv run pytest 26; `git diff --quiet scripts/evidence_requirements.py` (A1). | RED: a serve_workbench/serve_chain selftest asserting Lakeshore CASE-B (shaped to casework's fan-in C3 contract — ≥N distinct inbound CREDIT originators from Lakeshore's REAL network + neutral/exculpatory source-of-funds) passes `--disposition cleared` → casework SIGNS (currently fails-closed). GREEN: shape the data/casefile Lakeshore CASE-B casework bundle + wire the cleared-disposition path. REFACTOR: none. VERIFY: Lakeshore signs cleared; the Northgate FILE half still files (matched pair holds); the live-engine verdict matches the authored `expected_*` (fixture-drift bridge); `git diff --quiet scripts/evidence_requirements.py`. | scope: data/casefile/**, scripts/serve_workbench.py, scripts/serve_chain.py | success: python3 scripts/serve_workbench.py --selftest (Lakeshore cleared + Northgate file + drift bridge) && git diff --quiet scripts/evidence_requirements.py
- [x] T3 (L) Measure-first companion-only: anchored emit reproduction + real-data resolution scoring — THE ABORT GATE | DONE 2026-06-27. **Measure-first gate GREEN (non-degenerate, two-sided — the Phase-77 circular-oracle abort CURED with hard data).** Reproduced substrate's `--anchored --emit-eval-oracles` emit CLEAN (clients 400/months 10/seed 0; no ReplayError — the crash was `--monitor`/`--emit-evidence`, orthogonal to the emit path, per the scout). The oracle is NON-circular: 257 entity_refs → 233 opaque `GT-<hash>` clusters (not the `ENT-<entity_ref>` echo), 17 same-person fragment clusters / 31 latent should-merge pairs (Phase-77 had ZERO). Distilled the committed no-substrate capture `data/entity-spine/substrate-anchored-slice.json` (32 candidate-relevant obs + the GT- oracle; emails masked to example.test; email/phone demoted to weak per Phase-75). Scored via the existing resolution_scorer (demoted spine) → 29 candidate pairs, TWO-SIDED: 13 uphold_merge (real fragments the spine MISSED) + 16 reject_as_shares (noise-floor collisions correctly refused). Replays deterministically with NO substrate; `assert_no_cluster_leak` firewall holds. | RED: an assertion that the scorer over the substrate `--anchored --emit-eval-oracles` `GT-` oracle produces a NON-tautological two-sided structure (`assert_no_cluster_leak` holds; both correct-rejections of the spine's refusals AND genuine should-merge fragment cases present; the same/distinct split over the real population reported). GREEN: pin a known-good substrate param set (route around the n=400/seed0 ReplayError; substrate's tests drive build_dataset directly), drive the emit, score the spine's real-slice candidate SHARES, commit a replayable confusion capture + baseline (tests/fixtures/merge-anchored-oracle/). REFACTOR: none. VERIFY: the measurement replays deterministically with NO substrate run (the Phase-78 harness pattern); firewall + synthetic-substrate-anchored qualifier hold. ABORT GATE (escape hatch — document in the commit if taken): emit won't reproduce after bounded attempts OR result tautological/one-sided → STOP, write the honest non-result + a substrate emit-stability brief; T4 does NOT run. | scope: scripts/resolution_scorer.py, scripts/curate_merge_cases.py, tests/fixtures/merge-anchored-oracle/** | success: the committed capture replays a non-tautological two-sided result with no substrate && the firewall assertions pass — OR a documented abort + a substrate emit-stability brief authored
- [x] T4 (M) (gated on T3 clean) Re-freeze dist/merge with scored real cases | DONE 2026-06-27. **SUPERSEDED the Phase-76/77 consensus-66 with 29 substrate-anchored SCORED cases (the user's "supersedes" call).** curate_merge_cases rewritten (`enumerate_substrate_scored` replaces `enumerate_real_shares`; BOTH populations SCORED, split by oracle PROVENANCE; the consensus-no-oracle framing retired — substrate data was never production-real, it lacked an oracle until the anchored fork). build.py `validate_merge_cases` mirrored in EXACT parity (the Phase-76 lesson) + a masking firewall (no real email domain ships). merge.html rewritten (both populations show the latent-truth oracle; Verdict/ledger split by provenance; substrate cases show "substrate's anchored ground truth"; synthetic-substrate qualifier; honesty governor holds). merge-console.test.mjs updated (74/0). dist/merge RE-FROZEN (90,831 bytes). VERIFY: `--check all` 9/9 (8 non-merge dists byte-frozen, dist/merge re-frozen); curate selftest PASS (29 substrate two-sided / 13 synthetic / 9 broken fixtures); evidence_requirements.py byte-unchanged (A1); build.py firewall clean; uv run pytest 26; resolution_scorer selftest PASS. | RED: merge-console.test.mjs + validate_merge_cases assert the real cases now carry a scored oracle (consensus→scored), the consensus-vs-scored split + synthetic-substrate-anchored qualifier render, the firewall (no truth pre-adjudication) holds. GREEN: curate_merge_cases.py flips the real cases to scored against the `GT-` oracle; rebuild merge; re-freeze dist/merge. REFACTOR: none. VERIFY: `python3 scripts/build.py --check all` → the 8 non-merge dists byte-frozen + dist/merge re-frozen to the new bytes; validate↔curate parity; `node tests/merge-console.test.mjs` green; the honesty-governor word-ban holds; `git diff --quiet scripts/evidence_requirements.py`. NOTE: gated — skipped entirely if T3 takes its abort path. | scope: scripts/curate_merge_cases.py, data/merge/cases.json, scripts/build.py, merge.html, dist/merge/**, tests/merge-console.test.mjs | success: python3 scripts/build.py --check all && node tests/merge-console.test.mjs && python3 scripts/curate_merge_cases.py --selftest
- [x] T5 (S) Docs true-up: close the three resolved briefs + true-up the cross-pillar build order | DONE 2026-06-27. CLAUDE.md merge-console bullet rewritten for the supersede (both populations scored, split by oracle provenance; the consensus framing retired). The 3 handoff briefs annotated with resolving commits: casework-c3-fan-in **✅ CLOSED** (`ed93a0d`), substrate-emit-cli-wiring **✅ RESOLVED** (`9677a37` + the `--anchored` fork), substrate-open-reference-data-fork **◐ PARTLY LANDED** (Stage-1 `f9e63e7`; Stages 2/3 open). cross-pillar-build-order trued-up to the live HEADs (casework `076fb8e`/Phase 19, substrate `c099259`/Phase 32-33) + the Phase-79 consume note. VERIFY: only dist/merge changed (T4's re-freeze), no other dist touched by docs. | RED: a grep asserting the three briefs still read blocked/deferred and cross-pillar-build-order shows stale pins. GREEN: annotate casework-c3-fan-in + substrate-emit-cli-wiring + substrate-open-reference-data-fork (Stage-1) closed/advanced-by-sibling with the resolving commits; true-up cross-pillar-build-order to the live HEADs (substrate c099259/Phase 33, casework 076fb8e/Phase 19); update CLAUDE.md current-state. REFACTOR: none. VERIFY: no dist/** changed; links resolve. | scope: docs/*-PLAN-BRIEF.md, docs/cross-pillar-build-order.md, CLAUDE.md | success: the three briefs annotated closed with the resolving commit && git diff --stat shows no dist/** change

> Phase 79 — abort: any non-merge ship dist drift / a build.py spine-or-scorer-or-sibling-or-curate import / an `evidence_requirements.py` change / a confusion number presented as a catch-rate or lift or precision or recall → STOP-and-surface. Merge track (T3, the Phase-77 abort rule): the anchored emit won't reproduce after bounded attempts / the result is tautological (cluster==entity_ref echo) / one-sided → STOP the merge track to CONSENSUS + a substrate emit-stability brief, ship the Lakeshore floor + the honest non-result; T4 (the dist/merge re-freeze) does NOT run. The validate↔curate firewall parity (Phase-76) holds; the synthetic-substrate-anchored qualifier governs any scored real claim.

> Phase 79 — ALL T1-T5 [x]. STANDARD ceremony. Direction gate "Bundle, gated upside" (2026-06-27, AskUserQuestion). BOTH tracks landed: (FLOOR) Lakeshore CASE-B SIGNS `cleared` end-to-end via casework's Phase-19 fan-in C3 (re-vendor b3546d4→076fb8e; honest window-compression to ≤7d + an IND-02-grounded `case-b.bundle.json`) — the matched north-star pair BOTH route through casework. (GATED UPSIDE) the merge measure-first gate cleared GREEN (the emit reproduced clean — the ReplayError was `--monitor`/`--emit-evidence`, orthogonal; `GT-<hash>` oracle non-circular, `entity_ref ≠ cluster`, two-sided 13 should-merge / 16 correct-reject) → at the user's "supersedes" call, `data/merge/cases.json` REPLACED the consensus-66 with 29 substrate-anchored SCORED cases (both populations scored, split by oracle PROVENANCE); `dist/merge` RE-FROZEN (90,831 B). Adversarial review (3 dims): 1 MUST-FIX (mid-flight stale `cases.json` — old 66 committed while dist/merge had new 29; `--check merge` FAILED) RESOLVED (regenerated → 29+13); 2 should-fix ADDRESSED (demoted-spine constant-verdict framing explicit; full manifest surfaced); 1 self-refuted. T2 floor had its own 3-dim review (0 must-fix / 3 disclosures fixed). Companion-only EXCEPT the sanctioned `dist/merge` re-freeze; `--check all` 9/9 (8 byte-frozen + dist/merge re-frozen); `evidence_requirements.py` BYTE-UNCHANGED (A1); build.py firewall clean; merge test 74/0, workbench 169/0, curate broken-fixtures 9, `uv run pytest` 26. STALENESS LESSON: a "wrote …" log is not proof a file persisted — verify the committed data file's CONTENT + git status + the specific `--check <target>`; commit the curate INPUT (the slice JSON) with the output. Delivery gate: pending the orchestrator's delivery-flow commit (2026-06-27).

<!-- phase:phase-78-consume-disposition-validation-control -->
<!-- gate-log:phase-78 direction=approved delivery=accepted -->
<!-- status: PLANNED 2026-06-26 (STANDARD). Consume substrate Phase 31's now-CLI-reachable eval/intended_disposition.json (--emit-eval-oracles, verified live) → the determination-validation harness (Phase-77 A2, the circularity exit) + a §12 discovery-feed control. Companion-only; evidence_requirements.py + all 9 dists byte-frozen; build.py imports nothing new. Bundle-only non-circular frame; the oracle never enters an engine input; T2 is the measure-first gate (degenerate → down-scope T3). Substrate pin 9677a37. -->

## Phase 78 — Consume the disposition oracle: the determination-validation harness (the circularity exit) + the §12 discovery-feed control

> Phase 78 — direction gate 2026-06-26 (all-accept; both Step-13 decisions positioned). Direction = the user's "consume substrate" + two picks: (Q1) SCORE FRAME = bundle-only structure (mechanism + ≥2 legs); hold the human-gate inputs (named_predicate_risk, mitigation) OUT and name them as the boundary — the non-circular frame, NEVER derive them from the oracle basis. (Q2) DELIVERABLE = measure → control: pivot the confusion structure into a live §12 discovery feed over the disagreement cases in serve_workbench (the measuring→controlling pivot). Verified this session: substrate advanced f2da3e4→9677a37 (Phase 31 emit-cli-wiring), `--emit-eval-oracles` emits eval/intended_disposition.json across the boundary (two-sided file|clear, 11/807 at 5k clients); the merge real-66 stays CIRCULAR/CONSENSUS (substrate Phase-31 commit confirms all-singleton) and Lakeshore cleared needs casework C3 fan-in — neither in scope. STANDARD ceremony (cross-pillar + the circular-oracle adjacency: non-circularity must be PROVEN). Ledger: assumption-ledger.md Phase 78. The A1 guard: evidence_requirements.evaluate_sufficiency BYTE-UNCHANGED; build.py imports no harness/serve_workbench/substrate; 9 dists byte-frozen.

- [x] T1 (M) The boundary capture: join the substrate emit ↔ the disposition oracle (`--freeze`) | DONE 2026-06-26. `scripts/determination_validation_harness.py` (companion-only; build.py never imports it) `--freeze --emit-dir <out>` joins the disposition oracle ↔ screening+monitoring bundles by case_id, merges caps per customer (the Phase-71 "a case = a customer" frame), distills → committed `tests/fixtures/determination-validation/capture.json` (6935 cases, 2.0MB, deterministic seed 0). Map-driven correction: the oracle is keyed to the SCREENING slice (C8/C14); a customer may also carry a MONITORING bundle (C2/C3/C5/C15) — merged so the engine sees mechanism+legs. `--selftest` green (schema/join/firewall/classify). Self-review fixed: `_caps_by_case` now fail-loud (no silent bundle skip). | RED: a `--selftest` (structural, NO substrate run) asserting the distilled capture schema (per case: case_id, the crime_type-driving capabilities/atoms, the bundle-derived mechanism+leg structure, the oracle {intended_disposition, intended_basis}) + join completeness (every oracle case_id has a bundle; orphans NAMED, not silently dropped) + no full-bundle bloat (only harness-needed fields stored) + the firewall (no oracle field in the engine-input slice). GREEN: `scripts/determination_validation_harness.py --freeze` runs substrate @9677a37 with `--emit-evidence --emit-screening --emit-eval-oracles` as TOOL-USE (subprocess, the curate_workbench_cases pattern; build.py never imports it), joins evidence bundles ↔ eval/intended_disposition.json by case_id, distills → committed `tests/fixtures/determination-validation/capture.json` (deterministic, seed 0, no clock). REFACTOR: reuse curate's bundle-reading helpers where present; no new substrate import path. VERIFY: python3 scripts/determination_validation_harness.py --selftest && grep-guard build.py imports the harness nowhere. | scope: scripts/determination_validation_harness.py, tests/fixtures/determination-validation/** | success: capture.json committed + --selftest green (schema + join + firewall); build.py imports it nowhere
- [x] T2 (M) The `--check` measurement: the non-circular confusion structure + firewall + baseline — THE MEASURE-FIRST GATE | DONE 2026-06-26. `--check` replays the committed capture (NO substrate) → RE-RUNS the engine per case (bundle-only: mechanism + ≥2 legs, human-gate inputs HELD OUT) → the per-class confusion vs committed baseline.json; `assert_no_oracle_leak` (allow-list, rejects a renamed surrogate) + the signature guard run inside summarize; honesty governor reuses osint_tools._BANNED (caught its own disclaimer → reworded). **MEASURE-FIRST CHECKPOINT PASSED (non-degenerate).** Matrix (6935 cases, 121 file/6814 clear): file_ready×file=50 · over-flag=1320 · MISSED=71 · not_ready×clear=5494. ML discriminates (41% of file vs 9.7% of clear file-ready) but misses 59% of file (§12 gap); the KYC bar is PURE OVER-FLAG (727/727 C14-pure cases oracle-clear AND all file-ready). → proceed to T3 (NOT down-scoped). | RED: extend --selftest to assert (a) the bundle-only sufficiency structure (mechanism present? ≥2 legs?) is computed via evidence_requirements with NO oracle input — `assert_no_oracle_leak` (mirrors resolution_scorer.assert_no_cluster_leak); (b) each case classifies into {signal-file-ready, not-ready} × {oracle file, clear}; (c) the committed baseline.json matches; (d) a synthetic degenerate-input case is reported, not crashed; (e) the honesty word-ban (no catch-rate/precision/lift/recall — reuse osint_tools._BANNED). GREEN: `--check` derives the bundle-only structure per captured case, classifies vs the oracle, computes the confusion structure + per-basis breakdown with each cell DEFINED at render (missed = oracle-file/not-ready = §12 gap; over-flag = oracle-clear/file-ready = defensive exposure), asserts vs baseline.json (`--freeze` re-baselines). The held-out human-gate inputs are NAMED as the boundary, never read from the oracle. REFACTOR: the firewall guard mirrors the scorer. CHECKPOINT (the falsifiable measure-first gate): inspect the matrix; if degenerate (signal-file-ready ≈ 0, or non-discriminating across the oracle classes) → STOP + REPORT, down-scope T3 to an honest-degeneracy report. VERIFY: python3 scripts/determination_validation_harness.py --check && --selftest. | scope: scripts/determination_validation_harness.py, tests/fixtures/determination-validation/baseline.json | success: --check passes vs baseline; firewall + degenerate-handling + word-ban asserted; the matrix recorded with definitions; synthetic-only qualified
- [x] T3 (L) The §12 discovery-feed control in serve_workbench (GATED on T2's matrix) | DONE 2026-06-26 (gate PASSED non-degenerate → built, not down-scoped). serve_workbench: a read-only `/discovery` route + `discovery_feed()` (reads the committed harness baseline.json; PRESENTATION-ONLY — the oracle never reaches determine/evaluate_sufficiency; honest empty-state when unbuilt). workbench.html: `discoveryPanelHTML`/`loadDiscovery` + the panel div + boot wiring + exports — renders the §12 build queue (missed, each with the engine's own missing[] gap) + the over-flag exposure + the KYC structural-over-flag finding (727) + the no-silent-cap disclosure; reuses esc()/memctl classes. serve_workbench --selftest extended (feed available + cells sum to n_cases + sample≤total + the FIREWALL: determine_case byte-identical across a discovery_feed call) PASS; workbench.test.mjs 159→169 (+10: header/counts/kyc-finding/missing-annotation/cap-disclosure/empty/2×XSS/honesty-governor) green. A1 guard HELD (evidence_requirements.py byte-unchanged); no dist touched. | RED: extend tests/workbench.test.mjs (+ serve_workbench --selftest) to assert the discovery feed renders the two disagreement cells — missed (oracle-file, signals not file-ready) + over-flag (oracle-clear, signals file-ready) — each annotated by the engine's `missing[]` gap; the oracle is PRESENTATION-only (absent from any determine/evaluate_sufficiency input path — the Phase-74 priors-are-provenance guard); the synthetic-only qualifier + the honesty word-ban; XSS-escape (esc() the sole escaper). GREEN: a read-only `/discovery` route (persists nothing) + a "§12 discovery feed" panel reading the harness output; serve_workbench surfaces the disagreement cases as a gather/build queue. REFACTOR: reuse the memoryPanel/graph idioms; no new lib. (If T2 degenerate → this becomes the honest-degeneracy report variant.) VERIFY: node tests/workbench.test.mjs && python3 scripts/serve_workbench.py --selftest && git diff --quiet scripts/evidence_requirements.py. | scope: scripts/serve_workbench.py, workbench.html, tests/workbench.test.mjs | success: the feed renders the two cells with gap annotations; oracle absent from the engine path; evidence_requirements.py byte-unchanged
- [x] T4 (S) Regression + honesty sweep + docs/true-up | DONE 2026-06-26. `docs/determination-validation.md` authored (the circularity-exit harness + the §12 feed + the matrix + run cmds + the named-deferred consumes); CLAUDE.md ## Current state trued up (the harness test entry + the §12 discovery feed in the workbench bullet); `docs/cross-pillar-build-order.md` trued up (substrate f2da3e4→9677a37 Phase 31; Track-C′ item 2 → DONE). Harness added to the `uv run pytest` umbrella (PY_SELFTESTS --selftest + a dedicated --check fn). VERIFY ALL GREEN: `--check all` 9/9 byte-frozen (ZERO dist drift); build.py imports no harness/serve_workbench/substrate/spine/scorer (grep clean); `evidence_requirements.py` BYTE-UNCHANGED (A1); `determination_validation_harness.py --check` PASS; `uv run pytest` 24→26. | RED: n/a. GREEN: doc `docs/determination-validation.md`; CLAUDE.md ## Current state + `docs/cross-pillar-build-order.md` trued up (Phase 78 consumed the disposition oracle; substrate pin 9677a37; the §12 discovery control; the merge real-66 + Lakeshore consumes stay named-deferred); add the harness to the `uv run pytest` umbrella. VERIFY (tiered): python3 scripts/build.py --check all (9/9 byte-frozen) && grep-guard build.py imports no determination_validation_harness/serve_workbench/substrate && python3 scripts/determination_validation_harness.py --check && node tests/workbench.test.mjs && uv run pytest && git diff --quiet scripts/evidence_requirements.py. | scope: docs/**, CLAUDE.md, tests/**, scripts/** | success: --check all 9/9; pytest green; engine byte-unchanged; boundary grep-clean; docs trued up

> Phase 78 — abort: any ship dist drift / a build.py harness-or-serve_workbench-or-substrate import / an evidence_requirements.py change / the oracle label reaching an engine input (firewall leak) / a confusion number presented as a catch-rate or precision or lift / a dist requiring a live substrate read → STOP-and-surface. Measure-first (T2): degenerate matrix → down-scope T3 to an honest-degeneracy report, never fabricate a feed over a non-result.

> Phase 78 — ALL T1-T4 [x]. STANDARD ceremony. Direction gate all-accept (2026-06-26; A1 accept-as-falsifiable = the T2 measure-first gate, which PASSED non-degenerate). CONSUMED substrate Phase 31's CLI-wired `eval/intended_disposition.json` → the determination-validation harness (the "circularity exit" Phase-77 A2 deferred) + the §12 discovery-feed control. The matrix (6935 cases): ML signals discriminate (file-ready on 50 of 121 oracle-file vs 593 of 6087 oracle-clear) but miss 71 of 121 file (§12 gap); the KYC bar is a pure structural over-flag (727/727 C14-pure cases oracle-clear yet all file-ready). Adversarial review (4 dims → adversarial verify; 14 agents): 0 must-fix, 3 distinct should-fix/nit CONFIRMED + FIXED [the deliverable doc presented banned rate/%/lift tokens while the governor only swept JSON → rewrote counts-only + extended `_assert_doc_clean` to sweep the doc (+ the harness qualifier register); the degenerate criterion pooled rates across crime_types (Simpson's-paradox) → per-crime-type with n>0 guards + a selftest; --freeze wrote before the honesty assert → assert-first], 3 refuted, 10 praise (firewall airtight, oracle genuinely independent = a real circularity exit, A1/boundary/determinism hold). Companion-only — `--check all` 9/9 byte-frozen; build.py imports no harness/serve_workbench/substrate; `evidence_requirements.py` BYTE-UNCHANGED; `uv run pytest` 26. The merge real-66 (circular) + Lakeshore `cleared` (casework C3 fan-in) stay named-deferred. Delivery gate: accepted by the user 2026-06-26.

<!-- phase:phase-77-consume-sibling-emissions -->
<!-- gate-log:phase-77 direction=approved delivery=accepted -->
<!-- status: DELIVERED 2026-06-26 (STANDARD; user-accepted at the delivery gate this session — gate-log flips to accepted post-commit, gate-state follows git-state). All 6 tasks [x]. CONSUMED the three sibling emissions code-verified live (substrate fc98b09→f2da3e4; casework bf15535→b3546d4) — as delivered ONE consume landed + TWO deferred against discovered sibling state, each routed to a NAMED brief, NOT left open. Companion-only: all 9 ship dists BYTE-FROZEN; build.py imports no spine/scorer/sibling/curate. OUTCOMES: (T1) exogenous-disposition VALIDATION HARNESS DEFERRED (DISCOVERY — substrate's emit_true_entities/emit_intended_disposition are tested-but-UNWIRED into the CLI; only the `--identity` parquet is CLI-reachable) → `docs/substrate-emit-cli-wiring-PLAN-BRIEF.md`. (T2) re-vendored casework bf15535→b3546d4 (VENDORED_AT); gate funnel IDENTICAL (202/111/63) — no signing regression. (T3) casework `cleared` CONSUMED end-to-end via a casework-REPLAYABLE C5 PROXY (`data/casefile/cleared-demo.bundle.json`, grounded exculpatory, adversarially-reviewed honest; USER OVERRIDE option b) — the north-star Lakeshore CASE-B fails-closed at casework's fan-OUT-only C3 (Lakeshore's C3 is fan-IN; A3 abort held, no fan-out fabricated) → `docs/casework-c3-fan-in-PLAN-BRIEF.md`. (T4) real-66 merge scoring ATTEMPTED → DEFERRED at the abort rule: the CLI-reachable `--identity` parquet emits clusters content-addressed `ENT-<entity_ref>` (1:1 relabel of the spine's decision key; 66/66 DISTINCT, ZERO cross-entity_ref merges) → scoring is CIRCULAR/true-by-construction → REVERTED real-66 to the Phase-76 CONSENSUS state (user: "revert real to consensus, defer"); the circular capture NOT committed; the two-sided synthetic-13 untouched. (T5) N/A — voided by T4; merge.html/cases.json/test reverted to Phase-76 (merge-console.test.mjs 73/0); dist/merge byte-identical to its Phase-76 ship. (T6) full verify + true-up: `--check all` 9/9 byte-frozen (ZERO dist drift); `uv run pytest` 24/24; arcs (merge 73 / gate 68 / triage 93 / news 150 / corpus 303) green; build.py imports no spine/scorer/sibling/curate; CLAUDE.md + cross-pillar-build-order + the consumed briefs trued up. The open-data fork (the unblocking move for the two-sided real merge oracle + realistic collisions) PARKED as `docs/substrate-open-reference-data-fork-PLAN-BRIEF.md`, NOT a signal-watch task. Spec `specs/phase-77-consume-sibling-emissions.md`; ledger Phase-77; plan [[phases/phase-77-consume-sibling-emissions]]. Grounded against signal-watch HEAD (Phase-76 committed) / aml-substrate@f2da3e4 / aml-casework@b3546d4. -->

## Phase 77 — Consume the three sibling emissions: exogenous-disposition validation harness · casework `cleared` → Lakeshore DECIDE · true_entities → merge real-scoring

Objective: CONSUME the NEW sibling work code-verified live this session — substrate advanced fc98b09→**f2da3e4** (Phase 29 slice-aligned `true_entities` `identity/true_entities.json` + Phase 30 `exogenous-disposition-label` `eval/intended_disposition.json`, both additive/firewalled/eval-only) and casework advanced 4a858e6→**b3546d4** (Phase 17 advisory CI reconciliation lane + Phase 18 the `cleared` affirmative-dismissal disposition). Three independent consumes ordered by strategic depth: (1) the exogenous-disposition VALIDATION HARNESS — the "circularity exit", validating signal-watch's determination engine against an independent oracle it didn't produce (companion-only; the engine stays byte-unchanged); (2) casework `cleared` → the Lakeshore DECIDE signs a documented dismissal (re-vendor + a Lakeshore→casework-contract translation; the casework file bar byte-unchanged); (3) `true_entities` → score the merge console's REAL 66 (ONE-SIDED, framed honestly — substrate's content-addressed clusters make every real SHARES truth 'distinct'/reject, so it scores the human adjudicator's reject-vs-overmerge calls, NOT the spine; the two-sided real oracle awaits the open-data fork). **STANDARD ceremony** (one L = T4). AUTHORING-TIME consume (substrate emissions re-emitted from a pinned slice, pin substrate f2da3e4, like `curate_workbench_cases`; the dist reads only committed data). The open-data fork is PARKED as a substrate handoff brief (`docs/substrate-open-reference-data-fork-PLAN-BRIEF.md`), NOT a signal-watch task. Companion-only except the ONE dist touch (`dist/merge` re-freeze for consume #3); build.py imports no spine/scorer/sibling/curate; honesty governor holds (synthetic-only qualifiers, no catch-rate/lift/precision, always-on badge); the resolver-input firewall holds (the real-66 oracle rides the revealed `oracle` block, never the pre-disposition evidence). Direction gate 2026-06-26 (all-accept; A1 accept-with-shaping); spec `specs/phase-77-consume-sibling-emissions.md` (the orchestrator authors it); ledger Phase-77; plan [[phases/phase-77-consume-sibling-emissions]].

- [x] T1 (S, DONE 2026-06-26) — DEFER the validation harness + author the substrate-CLI-wiring brief (DISCOVERY → Option 1): VERIFIED @f2da3e4 that substrate's `emit_true_entities`/`emit_intended_disposition` are tested-but-UNWIRED into the CLI (called only in substrate tests; the documented emit produces NEITHER `identity/true_entities.json` NOR `eval/intended_disposition.json` — only `identity/true_entities.parquet` via `--identity` is CLI-reachable). The exogenous-disposition harness therefore has NO tool-use-boundary path to its oracle → DEFER (don't build against an unreachable emission; don't library-import substrate). GREEN: author `docs/substrate-emit-cli-wiring-PLAN-BRIEF.md` — wire both `emit_*` JSON emissions into the substrate CLI (a flag), pin f2da3e4; the harness consumes in a follow-on. | scope: docs/*PLAN-BRIEF.md | success: the brief exists, pins f2da3e4, names both unwired `emit_*` functions + the CLI-wiring ask + the firewall contract | size: S

- [x] T2 (M, DONE 2026-06-26) — Re-vendor casework bf15535→b3546d4 + verify no signing regression: test (RED) assert the vendored pin is NOT yet b3546d4 + capture the existing workbench/chain DECIDE signings as a baseline; implement (GREEN) `scripts/vendor_casework.sh /Users/jwang/aml-casework` → VENDORED_AT b3546d4, re-run serve_workbench/serve_chain selftests; none (REFACTOR). | scope: vendor/aml-casework/**, scripts/vendor_casework.sh | success: `grep b3546d4 vendor/aml-casework/VENDORED_AT` AND `python3 scripts/serve_workbench.py --selftest` + `python3 scripts/serve_chain.py --selftest` green (existing DECIDE signings unchanged — no regression) | size: M

- [x] T3 (M, depends T2; DONE 2026-06-26 via a C5 proxy — Lakeshore itself fails-closed) — Lakeshore DECIDE → casework `cleared` signed dismissal: test (RED) serve_workbench/serve_chain selftest asserts the Lakeshore case (`data/casefile/case.json` CASE-B), translated to a casework-contract bundle (contract_version 0.1; an `exculpatory:true` txn + a neutral mechanism claim + an exculpatory mitigation claim), passed through the DECIDE subprocess with `--disposition cleared`, produces a signed record with disposition==`cleared`, signed==true, blocking_violations==[]; the casework file/determination bar is byte-unchanged (cleared is a separate branch); the exculpatory evidence is GROUNDED in the Lakeshore case's affirmative mitigation (NOT fabricated); implement (GREEN) add the Lakeshore→casework-contract translation + the cleared DECIDE path in serve_workbench/serve_chain; reuse the existing subprocess file-handoff (REFACTOR). | scope: scripts/serve_workbench.py, scripts/serve_chain.py, data/casefile/** | success: `python3 scripts/serve_workbench.py --selftest` shows the Lakeshore DECIDE signs `cleared` (grounded exculpatory, no fabrication; file bar byte-unchanged) | size: M — DONE 2026-06-26 (USER OVERRIDE option b): casework's `cleared` IS consumed end-to-end, but on a casework-REPLAYABLE C5 cash-placement proxy (`data/casefile/cleared-demo.bundle.json`, grounded on vendored `fin-2023-alert001:IND-08`; `serve_workbench.cleared_demo_consume` → signed+disposition==cleared+blocking_violations==[]). The north-star Lakeshore CASE-B does NOT co-sign — it fails-closed at casework's `grounding_replay` (casework C3 is fan-OUT ≥5 outflows; Lakeshore's C3 is fan-IN multi-originator → 0 cited outflows → correctly refused). A3 abort HELD: no fan-out pattern fabricated to force a sign. The gap → `docs/casework-c3-fan-in-PLAN-BRIEF.md` (pin b3546d4). `serve_workbench --selftest`: "CW-4 cleared consume: casework SIGNS `cleared` (C5 replayable; Lakeshore CASE-B fails-closed on fan-in C3)".

- [x] T4 (L, 2026-06-26 — ATTEMPTED → DEFERRED at the abort rule [oracle CIRCULAR]; real-66 stay CONSENSUS) — true_entities consume via the CLI-reachable PARQUET → the real 66 scored (TWO-SIDED where it exists; A1 reframe): RED — `curate_merge_cases.py --selftest` asserts the real 66 carry an `oracle` sourced from the substrate `identity/true_entities.parquet` (party_id→cluster_id; the REAL gen/identity ground truth — genuine same-person merges, NOT the content-addressed one-sided JSON): same_entity = (cluster_a == cluster_b) per pair → genuine fragments the spine missed = uphold_merge, noise-floor collisions = reject_as_shares; a substrate-population synthetic-only qualifier; the resolver-input firewall held (no truth in evidence a/b); `validate_merge_cases` (build.py) accepts a REAL case WITH a substrate-parquet oracle (relax "real → no oracle" → "real → a substrate-parquet oracle OR none"; KEEP the firewall + basis/verdict shape). GREEN — re-emit the slice population with `--identity` (pin f2da3e4; party-id-aligned to the committed slice — gen byte-identical fc98b09→f2da3e4); capture a committed slice-aligned `data/entity-spine/slice-true-entities.json` (the slice's party_ids→clusters); curate reads it + attaches the oracle. REFACTOR — reuse the oracle-block shape. CHECKPOINT: report how many of the 66 are same vs distinct — ALL distinct → honestly still one-sided (report; revert the framing), mixed → two-sided (the parquet pivot delivered). | scope: scripts/curate_merge_cases.py, scripts/build.py, data/merge/cases.json, data/entity-spine/** | success: curate --selftest green (the 66 carry a substrate-parquet oracle; firewall held; deterministic) AND the same/distinct split reported AND `python3 scripts/build.py merge` validates the new shape | size: L — OUTCOME 2026-06-26: the CHECKPOINT fired worse than "all distinct" — the captured `--identity` parquet emits clusters **1:1 with `entity_ref` (`ENT-<entity_ref>`, all 441 slice persons; ZERO cross-entity_ref merges)**, i.e. the oracle is a relabel of the SAME field the spine keys on → scoring is CIRCULAR (true-by-construction), not "one-sided". Per the abort rule + A1 fallback ("if the oracle can't yield ambiguous scored cases honestly → consensus + defer scoring") + the user's call (2026-06-26: "revert real to consensus, defer"), REVERTED curate/build/cases/merge/test to the Phase-76 CONSENSUS state (real-66 no oracle; the genuinely-two-sided synthetic-13 untouched). `dist/merge` BYTE-FROZEN (no dist touch this phase). The circular capture was NOT committed. Real merge scoring needs `entity_ref ≠ cluster` (a genuine identity layer / open-data real collisions) → `docs/substrate-emit-cli-wiring-PLAN-BRIEF.md` (deeper-gap section) + `docs/substrate-open-reference-data-fork-PLAN-BRIEF.md`.

- [x] T5 (M, 2026-06-26 — N/A: real-66 retained as CONSENSUS, dist/merge byte-frozen) — merge.html scored-real-case framing + merge-console.test.mjs update + dist/merge re-freeze: RED — `tests/merge-console.test.mjs` asserts the real (now scored) cases render the framing (a scored chip; the Verdict shows the substrate-parquet oracle truth [same/distinct] + a match indicator + the substrate-population qualifier; the ledger agreement counts the real scored cases; the copy honestly reflects the T4 same/distinct split — two-sided where genuine fragments exist + names the open-data fork for richer realism); the synthetic 13 still render. GREEN — update merge.html (the real cases now scored; the Verdict/ledger framing; the queue chips), `python3 scripts/build.py merge` → re-freeze dist/merge. REFACTOR — reuse the existing scored-render path. | scope: merge.html, tests/merge-console.test.mjs, dist/merge/** | success: `node tests/merge-console.test.mjs` green (the scored-real-case framing asserted, honest to the T4 split) AND `python3 scripts/build.py --check merge` byte-stable (rebuilt + committed) | size: M — OUTCOME 2026-06-26: VOIDED by the T4 deferral — no scored-real framing to render. merge.html/cases.json/test reverted to Phase-76; `node tests/merge-console.test.mjs` 73/0 (the Phase-76 consensus arc) AND `python3 scripts/build.py --check merge` byte-stable vs HEAD (dist/merge unchanged). The merge console is byte-identical to its Phase-76 ship.

- [x] T6 (S, DONE 2026-06-26) — Full verification + true-up: VERIFY only — `python3 scripts/build.py --check all` (9 targets; only dist/merge changed [re-frozen], the other 8 byte-frozen) AND build.py imports no spine/scorer/sibling/curate (grep) AND `node tests/{merge-console,gate-console,triage-console,workbench,news-stream}.test.mjs` + `python3 scripts/serve_workbench.py --selftest` + `python3 scripts/curate_merge_cases.py --selftest` + `uv run pytest` all green AND CLAUDE.md trued up (the consume — real 66 now scored against the substrate parquet [two-sided where genuine fragments exist]; `cleared` in the Lakeshore DECIDE; pins substrate f2da3e4 + casework b3546d4; the exogenous-disposition harness DEFERRED → `docs/substrate-emit-cli-wiring-PLAN-BRIEF.md`) AND `docs/cross-pillar-build-order.md` + the consumed briefs (true_entities CONSUMED-via-parquet · casework-cleared CONSUMED · exogenous-disposition DEFERRED+brief) trued up. | scope: CLAUDE.md, docs/**, scripts/**, tests/** | success: `python3 scripts/build.py --check all` 9/9 (only dist/merge re-frozen) AND no spine/scorer/sibling import in build.py AND all arcs + harness selftest + pytest green AND CLAUDE.md + build-order + briefs trued up | size: S

> Phase 77 — direction gate 2026-06-26 (all-accept; A1 accept-with-shaping, A2–A5 accept-with-evidence). Direction = the user's "consume the sibling work + plan for the open-source data fork" + two Step-13 picks: consume #3 (true_entities → merge real-scoring) FRAMED ONE-SIDED (it scores the human adjudicator, not the spine; substrate's content-addressed clusters mean every real SHARES truth is 'distinct'/reject — so it catches over-merges but cannot present a should-merge; the two-sided real oracle awaits the open-data fork), and STANDARD ceremony / all three consumes. A1 [T0 weakest] consume #3 is meaningful not circular — ACCEPT-with-shaping (the Verdict/ledger must FRAME the one-sidedness + name the fork; carry a substrate-population qualifier). A2 the exogenous-disposition harness is companion-only + leaves evaluate_sufficiency BYTE-UNCHANGED — ACCEPT-with-evidence (verified @f2da3e4). A3 casework `cleared` needs a re-vendor + a Lakeshore→casework-contract translation; the casework file bar BYTE-UNCHANGED — ACCEPT-with-evidence. A4 boundary/honesty — the ONLY dist touch is `dist/merge` re-freeze; build.py imports no spine/scorer/sibling/curate; synthetic-only qualifiers; no catch-rate/lift; the firewall holds — ACCEPT. A5 authoring-time — substrate emissions re-emitted from a pinned slice (f2da3e4); the dist reads only committed data — ACCEPT.
> Abort: any non-merge dist drift / a sibling-or-spine import in build.py / a loosened validator / a scored number presented as a real catch-rate / the resolver-input firewall leaking / the casework file bar weakened or exculpatory evidence fabricated / the exogenous label fed into the determination engine → STOP-and-surface. If consume #3 can only score the spine (never the human) → defer it to post-fork. The open-data fork stays a NAMED substrate handoff, never built here.

<!-- phase:phase-74-entity-intelligence-spine -->
<!-- gate-log:phase-74 direction=approved delivery=accepted -->
<!-- status: PLANNED 2026-06-25 (STANDARD — user override of the project LITE default; cross-pillar contract/schema work, high blast radius). Direction gate NOT all-accept (two don't-knows DOWN-SCOPED, never a silent pass): A1 [T0 weakest] news_store-separability → DROP the dependency (the spine is a NEW module `scripts/entity_spine.py`, news_store BYTE-UNTOUCHED → the M8 arc is inherently safe); A5 cross-pillar drift → code-verify BOTH siblings' live HEAD BEFORE writing any brief, pin the verified commit. A2/A3/A4 accept. Direction = the user's 3-turn design conversation (all claims code-verified): signal-watch takes the CONSUMER role, AUTHORS the cross-repo standards/schemas HERE, hands off emission briefs to the siblings. The decisioning lever (separating same-signal cases like Northgate/Lakeshore) lives in PERSISTED ENTITY INTELLIGENCE → a thin DETERMINISTIC spine slice now; the full medallion/DuckLake/Splink/Kuzu/probabilistic-ER stack is the TARGET architecture, explicitly OUT of scope. STANDARDS-FIRST (the contract everything depends on), then the consumer slice that proves it, then the sibling briefs. KEY RESOLUTIONS: confidence = a deterministic ordinal GRADE (strong=exact email/phone/identifier · weak=address · reject=name-only), never a probabilistic/model score unless measured vs true_entities; priors are analyst-visible provenance ONLY, never read by the boolean filing engine; confidence rides a SEPARATE grade-gated read path; the file/determination bar in evidence_requirements.py stays BYTE-IDENTICAL (the A1 file-bar guard). GENUINE gitignored DuckDB write seam in serve_workbench (deliberate, companion-firewalled crossing of "persists nothing"); a minimal scorer PROVEN here over a tiny synthetic true_entities. Bitemporal valid-time + decision-time, 1D lenses only, NO 2D cross-product; stale-prior = event-driven supersession. Companion-only — NOT a ship/dist target; build.py imports none of it; the 8 dists stay BYTE-FROZEN. Step-9 user picks: GENUINE persistent store + PROVE a minimal scorer here. -->

## Phase 74 — The persistent entity intelligence spine: consumer slice + standards/schemas + sibling handoff briefs

Objective: stand up a companion-only **persistent entity intelligence spine** that ties records to the same counterparty over time, presents each entity's resolved context (network, source of funds, prior dispositions) consistently on every surfacing, and accumulates prior decisions on the entity — proven by a thin deterministic slice in the live investigator workbench. The decisioning lever this program chases — separating two cases that fire the SAME grounded signals but deserve OPPOSITE outcomes (Northgate-files / Lakeshore-clears, `data/casefile/case.json`) — rests on evidence that lives in the entity's network + source of funds; that evidence must PERSIST on a resolved entity or every alert re-gathers it cold. **STANDARDS-FIRST**: author the cross-repo standards/schemas (resolution-link bitemporal schema · strong/weak/reject identity-grade grammar · confidence-as-provenance contract · the true_entities resolution-correctness SCORER contract) HERE — the contract everything depends on — then a thin DETERMINISTIC consumer slice that PROVES them, then THREE emission briefs handed off to the siblings. **STANDARD ceremony** (user override of the project LITE default — cross-pillar contract/schema work, high blast radius; the one L = T2 the spine module). Direction = the user's 3-turn design conversation (all claims code-verified at planning): `news_store.py` has the right bones (anchor + per-row provenance + conflicting-values-both-kept + reversible prune + confidence-RESERVED) but keys identity on the exact-normalized NAME and is walled into the M8 news pillar; `serve_workbench` reads per-case committed JSON only (zero entity persistence); `evidence_requirements.py`'s file/determination bar is byte-frozen (the A1 guard); aml-substrate is Parquet-only with ground-truth clusters in `true_entities.parquet` + a ground-truth-blind resolver + inert identifier hooks; no medallion/DuckLake/Splink/Kuzu exists anywhere. **KEY RESOLUTIONS (the 4 decisions):** (1) the spine is a NEW module `scripts/entity_spine.py` — `news_store` BYTE-UNTOUCHED, the M8 arc inherently safe, convergence DEFERRED to Phase-75+ (drops the A1 separability don't-know); (2) confidence is a DETERMINISTIC ordinal grade strong/weak/reject from the identifier grammar, never a fabricated score (probabilistic only if measured vs true_entities), fail-closed-to-weakest; (3) priors are analyst-visible PROVENANCE ONLY (never read by the boolean filing engine — the self-confirming-loop guard); confidence on a SEPARATE grade-gated read path that EXCLUDES (never down-weights) low-grade inherited atoms; the file bar stays BYTE-IDENTICAL (mirrors the Phase-73 affirmative-clear separate-path discipline); (4) a GENUINE gitignored DuckDB write seam in serve_workbench (the news_store §4.5-clean precedent) + a minimal scorer PROVEN here over a tiny synthetic true_entities behind a resolver-input firewall (no cluster-id field, no 1:1 surrogate). **THE A1 FILE-BAR GUARD (load-bearing):** if wiring the spine requires ANY change to `evaluate_sufficiency()` or the determination bar → STOP, route confidence/priors AROUND the frozen bar, never through it; injecting a prior `cleared` must yield a byte-identical file/clear verdict for a fixed evidence set. Companion-only — NOT a 9th ship target; build.py imports neither the spine nor the scorer; the 8 dists stay BYTE-FROZEN (`--check all` 8/8); the M8 news pillar must not regress (`news-stream.test.mjs` + `news_live_test.py` green). OUT of scope (DEFERRED as governed enhancements, NAMED in the standards not built): probabilistic/Splink ER, the merge-adjudication Class-J console, graph/Kuzu analytics, the full medallion/DuckLake stack, any 2D (resolution-version × data-snapshot) time-travel, any change to the 8 dists or the filing threshold, and EXECUTING the sibling briefs. Direction gate 2026-06-25 (NOT all-accept — A1/A5 don't-know→DOWN-SCOPED); spec `specs/phase-74-entity-intelligence-spine.md` (STANDARD); ledger Phase-74; plan [[phases/phase-74-entity-intelligence-spine]]. Grounded against signal-watch HEAD (Phase-73 committed f804722) / the live workbench (357 cases) / `evidence_requirements.py` byte-frozen file bar / `news_store.py` / `data/casefile/case.json` (the census-verified strong identifiers).

- [x] T1 (M) — Author the 4 standards/schemas (the cross-repo contract): test (RED) a structural+consistency check fails — the 4 docs (`docs/resolution-link-schema.md`, `docs/identity-grade-grammar.md`, `docs/confidence-as-provenance-contract.md`, `docs/true-entities-scorer-contract.md`) do NOT exist AND the shared grade vocabulary (strong/weak/reject) is not referenced identically across all 4 + the spec; author the 4 docs (GREEN) — the resolution-link schema (bitemporal valid-time + decision-time; append-only supersede-not-overwrite; reversible merge/un-merge; 1D lenses only [production=decision-time frozen / analytics=current-resolution recompute], NO 2D cross-product; stale-prior = event-driven supersession), the strong/weak/reject ordinal identifier grammar (named per-grade criteria + a `basis[]`; name-only = reject = the E-CALDERON exclusion), the confidence-as-provenance contract (priors are analyst-visible provenance, never a signal; the gate keys on the grade; missing/unrecognized fails closed to weakest), the true_entities resolution-correctness SCORER contract (synthetic clusters validate the resolver; production has no ground truth → human-adjudication-rate not accuracy; the resolver-input firewall — no cluster field); cross-link the 4 docs (REFACTOR). **CHECKPOINT after T1:** STOP and report the resolution-link schema + grade vocabulary before building the spine (the contract everything depends on; a wrong grade vocabulary propagates into the gate + the briefs). | scope: docs/resolution-link-schema.md, docs/identity-grade-grammar.md, docs/confidence-as-provenance-contract.md, docs/true-entities-scorer-contract.md | success: `test -f docs/resolution-link-schema.md && test -f docs/identity-grade-grammar.md && test -f docs/confidence-as-provenance-contract.md && test -f docs/true-entities-scorer-contract.md && for d in docs/resolution-link-schema.md docs/identity-grade-grammar.md docs/confidence-as-provenance-contract.md docs/true-entities-scorer-contract.md specs/phase-74-entity-intelligence-spine.md; do grep -q "strong" "$d" && grep -q "weak" "$d" && grep -q "reject" "$d" || { echo "FAIL grade vocab missing in $d"; exit 1; }; done && echo "OK 4 standards + shared grade vocab"` | size: M — DONE 2026-06-25: 4 standards authored (`docs/resolution-link-schema.md` three-layer model + bitemporal link schema + reversible-split-with-cascade + conflicts-both-kept; `docs/identity-grade-grammar.md` ordinal strong/weak/reject + STRONG_KINDS/WEAK_KINDS closed vocab + fail-closed; `docs/confidence-as-provenance-contract.md` exclude-not-downweight + per-decision manifest + the self-confirming-loop guard + bitemporal 1D lenses + stale-prior event-driven + the deferred Splink/Kuzu/console; `docs/true-entities-scorer-contract.md` synthetic-validates + resolver-input firewall [no cluster field / no 1:1 surrogate] + production=adjudication-rate-not-accuracy). Shared strong/weak/reject vocab across the 4 + the spec — success criterion PASS. CONSOLIDATION NOTE: authored as the 4 NAMED docs the plan anchors (not the interim 1-blob draft).

- [x] T2 (L) — Build the pillar-neutral spine module `scripts/entity_spine.py` + `--selftest`: RED — `entity_spine.py --selftest` absent/failing; GREEN — observation → bitemporal resolution link → `persistent_entity`; deterministic strong/weak/REJECT linkage (exact-normalized email/phone/strong-identifier = a link; address = weak; **name-only = REJECT**); append-only supersede-not-overwrite; reversible split WITH cascade-invalidation (inject a disposition grounded ACROSS an edge → split → it flips to "re-decision required", the audit row PRESERVED never deleted); conflicting-values-both-kept; confidence = the ordinal grade; REFACTOR — the core depends on nothing news-specific. THE DIRECTIONAL FIREWALL: `news_store.py` stays BYTE-UNTOUCHED; `entity_spine.py` imports no `news_store`/`serve_news`; the --selftest asserts news disposition vocab (`set_disposition`, the escalation watchlist) is NOT in the core. | scope: scripts/entity_spine.py, tests/ | success: `python3 scripts/entity_spine.py --selftest && ! grep -nE "^(import\|from) .*(news_store\|serve_news)" scripts/entity_spine.py && echo "OK spine selftest + no-news-import firewall"` (the --selftest internally asserts the link/supersede/split-cascade/conflict cases AND that news disposition vocab is core-absent; the `! grep` proves no news import at module top) | size: L — DONE 2026-06-25: `scripts/entity_spine.py` (NEW module, news_store BYTE-UNTOUCHED). Three layers: observations / append-only bitemporal `resolution_links` (link_id/entity_id/obs_id/method/grade/basis/valid_time/decision_time/resolution_version/supersedes/status) / persistent_entities. Deterministic merge: ONE strong-id match → merge (grade strong), 2+ distinct → refuse (ambiguity, new entity), weak-only/name-only → new entity (`edge_grade()` pure pairwise: strong/weak/REJECT). Append-only supersede; reversible `retract_link` BUMPS resolution_version + cascade-marks dispositions whose grounding crossed the edge "re-decision required" (audit row preserved). Conflicting values both-kept. Event-driven stale-prior (prior carries the version decided under; identity changed → stale). Fail-closed grade (unknown/empty basis → reject). `--selftest` PASS under .venv (9 cases incl. the live directional-firewall assertion: news disposition machinery [set_disposition/watchlist_rows/escalated/append_scan/anchor_summary] core-ABSENT + no news in sys.modules); `! grep` no news import. Graceful-degrade (SKIP without duckdb).

- [x] T3 (M) — Minimal scorer + tiny synthetic true_entities + resolver-input firewall + `--selftest`: RED — `resolution_scorer.py --selftest` absent; GREEN — score the resolver vs synthetic clusters (pairwise precision/recall OR cluster-F1/B-cubed; cite aml-substrate `test_resolution_lift.py` as the in-family reference), with the "synthetic-only; production has no ground truth" qualifier mandatory on every number; the cluster id lives ONLY in the scorer's evaluation-only channel; REFACTOR — the firewall is a schema-boundary guard, not a convention. | scope: scripts/resolution_scorer.py, data/entity-spine/true_entities.json, tests/ | success: `python3 scripts/resolution_scorer.py --selftest` passes AND a contract test (in the selftest) asserts no resolver-input field is the cluster id NOR 1:1-correlated with cluster identity (renaming the cluster field does NOT pass) | size: M — DONE 2026-06-25: `scripts/resolution_scorer.py` + `data/entity-spine/true_entities.json` (8 synthetic observations / 5 latent clusters; the `cluster` field is the eval-only channel the resolver never reads). FIREWALL = a schema-boundary ALLOW-LIST (`ALLOWED_INPUT_KEYS`): `resolver_input()` physically strips to the observable surface; `assert_no_cluster_leak()` REJECTS any non-observable key — the selftest proves a 1:1 `cluster_ref` surrogate is rejected (renaming the cluster field does NOT pass — the test is the schema boundary, not the field name). Metrics over the live deterministic resolver: Calder + Maric MERGE (shared strong email), Calderon != Calderón (different emails), the name-only Okafor pair NOT merged (conservative — unknown over wrong) → pairwise P=1.00/R=0.67 (fp=0, the costly false-merge error is zero; 1 recall miss = the name-only pair), B-cubed P=1.00/R=0.875. Every number carries the mandatory `SYNTHETIC_QUALIFIER` ("measured on synthetic clusters; production has no ground truth"). Cites aml-substrate's `resolve/measure.py` (the live sibling B-cubed/pairwise scorer found by the T6 verify) as the in-family reference. `--selftest` PASS under .venv; SKIPs gracefully without duckdb.

- [x] T4 (M) — Wire serve_workbench's grade-gated read path + the per-decision manifest + the self-confirming-loop / file-bar guard: RED — the guard assertion fails (injecting a prior `cleared` is NOT yet proven byte-identical; no grade-gated read path); GREEN — a SEPARATE grade-gated read path (unknown/missing grade AND null `basis[]` → weakest → EXCLUDED from filing inputs, never down-weighted); a per-decision admitted/quarantined-by-low-grade manifest; the matched Northgate/Lakeshore pair re-derives against the LIVE spine matching `expected_*` (the fixture-drift bridge over the spine); REFACTOR — confidence routes AROUND the byte-frozen bar. `evidence_requirements.py` is `--selftest`-only (the file bar BYTE-UNTOUCHED); if wiring requires ANY change to `evaluate_sufficiency()` → STOP-and-surface. | scope: scripts/serve_workbench.py, scripts/evidence_requirements.py, tests/workbench.test.mjs | success: `python3 scripts/evidence_requirements.py --selftest` passes UNCHANGED AND a new assertion proves injecting a prior `cleared` yields a byte-identical file/clear verdict for fixed evidence; `python3 scripts/serve_workbench.py --selftest` re-derives the pair against the live spine matching `expected_*` + asserts the grade-gated manifest excludes a null-grade atom as quarantined-by-low-grade | size: M — DONE 2026-06-25: serve_workbench imports `entity_spine.{EntitySpine,max_grade}`; new `_cf_read_manifest(case)` grade-gates the read-from-file network atom (ML-A4) via the spine grammar (max_grade over each resolved/flagged edge's shared strengths) — strong/weak ADMITS, reject/empty (name-only) QUARANTINES (excluded, never down-weighted); `casefile_determination` returns a per-decision `read_manifest`. Northgate: ML-A4 admitted×2 at grade strong → verdict held (determination); Lakeshore: excluded edge → empty manifest → verdict held (cleared) — the PAIR'S VERDICTS PRESERVED. FILE-BAR GUARD: `evidence_requirements.py` UNTOUCHED (`git diff --stat` empty; `--selftest` PASS unchanged). SELF-CONFIRMING-LOOP GUARD (live): the selftest introspects `evaluate_sufficiency`/`determine` signatures (no prior/disposition/history param — structurally can't read priors) AND proves injecting a prior `cleared` onto the subject in the spine yields a byte-identical determination (json sort_keys equal). Render fixtures regenerated (read_manifest field). `serve_workbench --selftest` PASS (357 cases); `node tests/workbench.test.mjs` 151/0.

- [x] T5 (M) — Genuine persistence seam + the re-surfacing "memory" case + the memory demo + the stale-prior guard: RED — the memory short-circuit assertion fails (no persistent store; no re-surfacing path); GREEN — a genuine gitignored DuckDB write seam in serve_workbench (write a disposition, read it back); author a re-surfacing case (an entity surfaces in a 2nd case after an INDEPENDENT-provenance prior — a prior STR/case record, NOT hand-set to steer the verdict); the second surfacing attaches the prior + grounding chain so the gather targets-to-close MEASURABLY shrink (prior-attached atoms make `present_before` already sufficient — the short-circuit is the measured DROP in targets, not a status flag); the stale-prior guard (event-driven supersession; the prior carries the resolution-version decided under → if identity changed since, surfaced "re-decision required", never trusted silently); REFACTOR — the store is gitignored, 127.0.0.1, never committed. If the only re-surfacing that works requires hand-setting the prior to steer the verdict → STOP (the A2 circularity trap). | scope: scripts/serve_workbench.py, data/casefile/, tests/ | success: `python3 scripts/serve_workbench.py --selftest` asserts the DuckDB write-then-read-back loop, the re-surfacing path SHRINKS targets-to-close (the measured short-circuit, a number not a status flag), AND the stale-prior re-examine path fires "re-decision required" | size: M — DONE 2026-06-25: a `resurfacing` block in `data/casefile/case.json` (CASE-C "Vesna Maric" — a CASE-A inbound counterparty whose email matches the INDEPENDENT prior-STR register PSR-0001 [human-trafficking STR], NOT hand-set to steer; kept OUT of `cases[]` so the matched-pair invariants hold; the cross-reference is a REAL identifier link). serve_workbench gained `SPINE_STORE` (gitignored — `*.duckdb` already ignored), `_accumulate_priors` (observe all case entities + attach the prior-STR register as independent-provenance priors), and `casefile_memory(store_path)`. MEASURED short-circuit (live): the spine resolves Vesna by the shared strong email across CASE-A + PSR-0001 + CASE-C → cold_targets [ML-A4, ML-A5] → memory_targets [ML-A4] = **targets_shrink 1** (the corroboration gather the cold case would chase is pre-closed by the prior STR — a number, not a status flag); predicate **pre-named "human trafficking"** by memory. GENUINE persistence: write-then-read-back across a store REOPEN (`persisted_read_back 1` == n_priors). STALE-PRIOR guard (event-driven): a split bumps resolution_version → the prior reads "re-decision required" (`stale_after_split True`). Confidence/priors ride this SEPARATE path — never the byte-frozen bar. `serve_workbench --selftest` PASS under .venv (the T5 block graceful-SKIPs without duckdb). NOTE: a rich workbench.html render of the memory beat is a polish follow-on (the measured number is the proof; T5 scope = serve_workbench/data/tests).

- [x] T6 (M) — The 3 sibling handoff briefs — VERIFY both siblings' live HEAD FIRST (the A5 resolution), pin commits: RED — the 3 briefs absent; GREEN — (PRECONDITION) code-verify aml-substrate + aml-casework live HEAD, THEN author (a) `docs/substrate-graded-counterparty-identifiers-PLAN-BRIEF.md` (emit graded strong/weak identifiers on counterparty legs — the inert identifier hooks made live), (b) `docs/substrate-exogenous-disposition-label-PLAN-BRIEF.md` (an exogenous disposition label decoupled from the casework sufficiency rule — priors from independent provenance), (c) `docs/casework-confidence-graded-resolution-PLAN-BRIEF.md` (consume the confidence-graded resolution; CW-side ER-verification + the cleared verdict); each pins the VERIFIED sibling commit hash; REFACTOR — each brief references the T1 standards (one shared grade vocabulary, sibling-executed — the Phase-55–58/66 pattern). If a sibling has moved → re-ground the brief to the live shape before writing. | scope: docs/substrate-graded-counterparty-identifiers-PLAN-BRIEF.md, docs/substrate-exogenous-disposition-label-PLAN-BRIEF.md, docs/casework-confidence-graded-resolution-PLAN-BRIEF.md | success: `test -f docs/substrate-graded-counterparty-identifiers-PLAN-BRIEF.md && test -f docs/substrate-exogenous-disposition-label-PLAN-BRIEF.md && test -f docs/casework-confidence-graded-resolution-PLAN-BRIEF.md && for b in docs/substrate-graded-counterparty-identifiers-PLAN-BRIEF.md docs/substrate-exogenous-disposition-label-PLAN-BRIEF.md docs/casework-confidence-graded-resolution-PLAN-BRIEF.md; do grep -qE "[0-9a-f]{7,40}" "$b" || { echo "FAIL no pinned commit in $b"; exit 1; }; done && echo "OK 3 briefs + pinned sibling commits"` | size: M — DONE 2026-06-25 (background subagent; A5 verify-first PAID OFF — both siblings DRIFTED): aml-substrate HEAD **a3fb02b** "close Phase 27" (NOT the assumed f15c241 — its `gen/identity.py` is a FULL identity subsystem [populates email/phone/device_id + SHARES_* edges, `--identity` flag], a ground-truth-blind `resolve/resolver.py` + a B-cubed/pairwise `resolve/measure.py` scorer ALREADY EXIST → the real gap is the counterparty LEG + the `strength` tag [`RelationshipEdge.attrs` is `dict[str,int]`, needs widening], not greenfield ER; `compose.py` genuinely score-deferred → the exogenous-label brief is a real gap). aml-casework HEAD **cfd989f** "close Phase 15" (NOT bf15535 — the `cleared` path NOT built; vocab still `{blocked,needs_more_info,signed}`+`{file,both_defensible}`; `party_ref` strictly fail-closed; no graded resolution → CW-4 still GAP). The 3 briefs (`docs/substrate-graded-counterparty-identifiers-PLAN-BRIEF.md` · `docs/substrate-exogenous-disposition-label-PLAN-BRIEF.md` · `docs/casework-confidence-graded-resolution-PLAN-BRIEF.md`) RE-GROUNDED to live HEADs + a DRIFT note each; each pins its verified hash + references the T1 standards' shared grade vocab; docs-only, sibling-executed; cross-links resolve both ways.

- [x] T7 (S) — True-up + full verification: VERIFY only — `python3 scripts/build.py --check all` 8/8 byte-identical AND `grep -n import scripts/build.py` shows no spine/scorer import (companion-only firewall); `node tests/workbench.test.mjs` + `node tests/news-stream.test.mjs` + `python3 tests/news_live_test.py` + `uv run pytest` all green (the M8 pillar not regressed); CLAUDE.md `## Current state` trued up (replace-in-place, no per-phase bullet; ≤~200 lines). | scope: CLAUDE.md, docs, HANDOFF.md | success: `python3 scripts/build.py --check all && ! grep -nE "import.*(entity_spine\|resolution_scorer)" scripts/build.py && node tests/workbench.test.mjs && node tests/news-stream.test.mjs && python3 tests/news_live_test.py && uv run pytest && echo "OK 8/8 byte-frozen + no spine import + all arcs green"` (the `! grep` proves build.py imports neither the spine nor the scorer — the companion-only firewall) | size: S — DONE 2026-06-25: `build.py --check all` 8/8 ZERO dist drift (companion-only held); build.py imports neither entity_spine nor resolution_scorer (firewall grep clean); `node tests/workbench.test.mjs` 151/0; `node tests/news-stream.test.mjs` 150/0 + `python3 tests/news_live_test.py` PASS (M8 NOT regressed — news_store byte-untouched, the A1 new-module down-scope paid off); `uv run pytest` 20→22 (entity_spine + resolution_scorer added to PY_SELFTESTS, run fully under .venv). CLAUDE.md `## Current state` trued up (Phase-74 entity-spine durable fact + the 2 new --selftest commands, replace-in-place). `data/entity-spine/store/` gitignored.


<!-- phase:phase-73-rich-investigation-case-live-workbench -->
<!-- gate-log:phase-73 direction=approved delivery=accepted -->
<!-- status: PLANNED 2026-06-23 (STANDARD — user override of the project LITE default). Direction gate all_accept:true (warned + restated; the load-bearing risk is A1 the affirmative-clear-vs-file-bar guard, defended by a hard STOP). The user's REFRAME (verified against the live workbench/data/engine): the curated substrate cases are "not it" (all C2/C3, synthetic ids, raw codes, no network) → INVERT the pillar dependency: signal-watch AUTHORS the north-star rich case FIRST (the artifact is the spec), substrate/casework PARKED. Author ONE matched pair — CASE-A Northgate (files) & CASE-B Lakeshore (clears) — firing the SAME grounded signals (identical indicator ids: C2 fin-2023-alert001:IND-03 + C3 fin-2020-alert001:IND-05 + C14 fin-2025-a003:IND-09) but resolving OPPOSITELY on an authored identity/network layer; the determination is COMPUTED BY THE LIVE ENGINE over authored evidence (NOT a precomputed dist — the user chose "extend the companion workbench"). THE THESIS: same grounded signal, opposite outcome — the network + the source of funds is the difference. Companion-only; build.py imports none of it; 8 dists byte-frozen. -->

## Phase 73 — Rich investigation case in the LIVE workbench: the matched FILE/DISMISS pair, rail-aware counterparty network, entity resolution, and the affirmative-clear verdict

Objective: author ONE matched pair of cases in the LIVE companion workbench — **CASE-A "Northgate Hospitality Group Inc."** (files) and **CASE-B "Lakeshore Catering Group Inc."** (clears) — that fire the SAME grounded signals (identical indicator ids: C2 rapid pass-through `fin-2023-alert001:IND-03` + C3 funnel fan-in `fin-2020-alert001:IND-05` + C14 source-not-established `fin-2025-a003:IND-09`) yet resolve OPPOSITELY, driven only by an authored identity/network layer. THE THESIS: *same grounded signal, opposite outcome — the network + the source of funds is the difference.* The determination is **computed by the LIVE ENGINE** over the authored evidence (NOT a precomputed dist): the extended `evaluate_sufficiency` produces `determination`→file for Northgate and a NEW affirmative-`cleared` (distinct from `needs_more_info`) for Lakeshore. The direction is the user's REFRAME, verified against the live workbench/data/engine before planning (memory `reframes-to-output-quality`): the curated substrate cases are "not it" → **INVERT the pillar dependency** — signal-watch AUTHORS the north-star rich case FIRST (the artifact is the spec); aml-substrate + aml-casework are PARKED and become downstream implementers. **STANDARD ceremony** (user override of the project LITE default — two L tasks, all three graphs this phase, unified reviewer dispatch). Honesty seam VERIFIED in-repo at planning: the live engine's verdict vocab is `{determination, needs_more_info}` (evidence_requirements.py:348) and disposition `{cleared, escalated, needs_more_info}` (serve_workbench.py:182); the live txn key is `channel` (WIRE/EMT/AFT/P2P/CARD/CASH/CHEQUE) with counterparty_country 0/25,391 null today (the data model mirrors `channel` + POPULATES country); `boGraphHTML` fed `display_name` not `party_id` fixes the `:695` bug. **THE A1 GUARD (load-bearing):** the new affirmative-`cleared` verdict (mechanism fired + legs absent + affirmative mitigation established) is a SEPARATE clear path requiring positive clean evidence — the file/determination bar (mechanism + ≥2 INDEPENDENT legs + named predicate + no unrebutted mitigation) stays BYTE-IDENTICAL; if clearing Lakeshore requires weakening the bar or fabricating evidence → STOP-and-surface. Companion-only — build.py imports none of it; 8 dists byte-frozen (`--check all` 8/8); the honesty governor holds (demo-visible richness + defensibility, ZERO catch-rate/lift/precision; "N pct" not "%"; structured facts record-sourced — the Phase-66 guard; synthetic-by-construction .test/.example + 555-01XX; always-on badge + the prior-STR synthetic marker). The cross-pillar contract doc (`docs/rich-case-target-contract.md`) is a DEFERRED follow-on. Direction gate 2026-06-23 (all_accept:true, A1 accept-with-guard); ledger Phase-73; plan [[phases/phase-73-rich-investigation-case-live-workbench]]. Grounded against signal-watch HEAD (Phase-72 delivered) / the live workbench (355 cases) / `evidence_requirements.py` verdict vocab verified in-repo.

- [x] T1 (L) — Author the rich-case dataset + schema (the load-bearing 80%): test that `data/casefile/case.json` carries the matched pair with the right shape (RED — the matched-pair invariant: identical signal_ids across the pair; all entities `synthetic_label:true`; no `strength:'strong'` on `kind:'address'`; both expected engine verdicts present), author `data/casefile/{case.json,schema.md}` — CASE-A Northgate (determination/file) + CASE-B Lakeshore (cleared/dismiss); rail-aware transactions (`channel` WIRE/EMT/AFT/P2P + named counterparties + populated `counterparty_country`); `resolution_edges` (email/phone strong, address weak — strong-on-address forbidden); multi-hop named `ownership_edges` (Northgate→1187442-Ontario-Inc→caution-listed-address chain); address-keyed `caution_list`; `prior_str_register` (HT predicate on an INBOUND source party, controller a separate identity); the EXPECTED engine verdict authored as the test oracle (GREEN), schema.md documents the entity/transaction/resolution/ownership/reference/determination shapes (REFACTOR). All synthetic (.test/.example, 555-01XX); the DISMISS leads with affirmative reconciliation + established source. | scope: data/casefile/case.json, data/casefile/schema.md | success: `python3 -c "import json; d=json.load(open('data/casefile/case.json')); a,b=d['cases']; sa={x['grounding']['signal_id'] for x in a['alerts']}; sb={x['grounding']['signal_id'] for x in b['alerts']}; assert sa==sb and 'fin-2023-alert001:IND-03' in sa and 'fin-2020-alert001:IND-05' in sa; assert all(e.get('synthetic_label') for c in d['cases'] for e in c['entities']); assert not any(i.get('strength')=='strong' and i.get('kind')=='address' for c in d['cases'] for e in c['entities'] for i in e.get('identifiers',[])); assert a['determination']['presentation_label']=='file' and b['determination']['presentation_label']=='documented_dismissal'; print('OK pair+identical-signal_ids+synthetic+no-strong-address+both-verdicts')"` | size: L — DONE 2026-06-23: `data/casefile/{case.json,schema.md}` authored + verified. The matched pair (CASE-A Northgate→file / CASE-B Lakeshore→cleared) shares identical signal_ids (C3 fin-2020-alert001:IND-05 + C2 fin-2023-alert001:IND-03 + C14 fin-2025-a003:IND-09 — real grounded refs, verbatim flags embedded). Rail-aware txns (EMT/AFT/WIRE/CASH/CARD) with named counterparties + populated country; resolution_edges (cross-institution Calder resolve [strong email]; inbound-source Vesna-Maric prior-STR flag; excluded John-Calderon near-match [no shared id]); multi-hop ownership chain 1187442-Ontario-Inc→44-Holloway caution-listed address; the source-of-funds CONTRAST AXIS (A null+mitigation-not-established, B established+mitigation-established) is what flips ML-A7. All synthetic (.test/.example, 555-01XX). GREEN: the spec criterion + deeper invariants (referential integrity; resolved-edge shared-on-both-entities; prior-STR/caution-list genuinely ground; honesty: synthetic domains/555 phones/zero bare %) ALL PASS. Money-flow: Northgate $46.3k in / $42k structured drain (5×8400) out, ~0 retention; Lakeshore $43.5k in / $41.3k out.
- [x] T2 (M) — Extend the live engine + the affirmative-clear verdict (the file bar UNCHANGED): RED — add selftest cases asserting a Lakeshore-shape case (no-legs + established mitigation) → `cleared`, a Northgate-shape case → `determination`, a non-affirmative no-legs case → still `needs_more_info`, AND the existing file-bar cases unchanged; GREEN — extend `scripts/evidence_requirements.py` + `data/workbench/evidence-requirements.json` with read-from-file evidence (`via:read`), caution_list/prior_str gather kinds (`via:gathered`), predicate read-from-register, and the AFFIRMATIVE-CLEAR verdict (mechanism + legs-absent + affirmative mitigation established → `cleared`, distinct from `needs_more_info`); REFACTOR — the new clear path is a separate branch that widens nothing on the file side. THE FILE/DETERMINATION BAR (mechanism + ≥2 INDEPENDENT legs + named predicate + no unrebutted mitigation) STAYS BYTE-IDENTICAL. If clearing Lakeshore needs the bar weakened or evidence faked → STOP-and-surface. | scope: scripts/evidence_requirements.py, data/workbench/evidence-requirements.json | success: `python3 scripts/evidence_requirements.py --selftest` (the new RED cases pass: Lakeshore-shape→cleared; Northgate-shape→determination; a non-affirmative no-legs case still needs_more_info; the file bar's existing cases unchanged) | size: M — DONE 2026-06-23: ADDITIVE engine change, the file bar BYTE-UNCHANGED (the clear branch sits AFTER `sufficient = not missing`; the determination `missing`-computation is untouched). `present_atoms` gained `read=()` (investigator-read atoms, e.g. ML-A4 network from the file); `evaluate_sufficiency`/`determine` gained `mitigation_established` + the affirmative-`cleared` branch (mechanism present + 0 legs + mitigation affirmatively established + no named predicate → `cleared`, distinct from `needs_more_info`); the ML profile gained a validated `affirmative_clear` block (honesty-swept). RED→GREEN proven in --selftest: Lakeshore-shape→cleared; same shape w/o established mitigation→needs_more_info; predicate-present or leg-present→needs_more_info (clear is earned, conservative); the full ML bar still→determination (file bar invariant); kyc has no clear block→unavailable; the read source lights ML-A4; banned-token in affirmative_clear rejected. REGRESSION CLEAN: evidence_requirements --selftest PASS · serve_workbench --selftest PASS (355 cases) · serve_chain --selftest PASS · workbench.test.mjs 127/127 · `--check all` 8/8 byte-frozen.
- [x] T3 (M) — Wire serve_workbench to load + serve + COMPUTE the determination over the showcase pair: RED — extend `scripts/serve_workbench.py --selftest` to assert the loaded casefile pair COMPUTES `determination` (Northgate) and the affirmative-`cleared` (Lakeshore) via the engine (not authored-frozen strings) and surfaces at the top of the queue; GREEN — load `data/casefile/case.json` in serve_workbench + curate_workbench_cases, compute the verdict per case through the extended engine, place the pair at the queue head; REFACTOR — the casefile pair rides the existing case payload shape. | scope: scripts/serve_workbench.py, scripts/curate_workbench_cases.py | success: `python3 scripts/serve_workbench.py --selftest` (the two computed verdicts: Northgate→determination/file, Lakeshore→cleared/documented_dismissal; both engine OUTPUT over the authored evidence; the pair at the top of the queue) | size: M — DONE 2026-06-23: added a SEPARATE casefile path in serve_workbench (`load_casefile`/`casefile_determination`/`casefile_list`/`casefile_detail`) that DERIVES the engine inputs from the EVIDENCE (caps from alerts; ML-A7 lit/suppressed by `kyc.source_of_funds` read from the file; ML-A4 from a resolved/flagged resolution_edge [via:read]; ML-A5 from a caution-list address hit on the ownership chain OR a prior-STR match on an inbound source [via:gathered]; the predicate READ from the matched prior_str record; mitigation_established/rebutted from the source-of-funds finding) and runs the LIVE engine — the verdict is engine OUTPUT, the authored `expected_*` is a regression ORACLE only (`expectation_match`). `list_cases`/`case_detail`/`determine_case` DISPATCH the authored ids to the casefile path; the pair LEADS the queue (357=355+2; funnel/coverage scoped to population). VERIFIED: serve_workbench --selftest PASS (CASE-A→determination/escalated/file w/ predicate human-trafficking + via fired/read/gathered grounded; CASE-B→cleared/documented_dismissal on affirmative mitigation, 0 legs; both match oracle; identical signal sets). REGRESSION CLEAN: evidence_requirements/serve_chain selftests · workbench.test.mjs 127 · `--check all` 8/8 · uv run pytest 20. (curate_workbench_cases untouched — the casefile is a separate authored source, not part of the curated population; scope note.)
- [x] T4 (L) — Render in workbench.html (names-not-codes; all three graphs): RED — the node test (added in T5) asserts the 6-screen arc + the matched-pair fork + the channel-aware counterparty column + the three graphs render; GREEN — render in `workbench.html`: names-not-codes (codes resolve via the inlined taxonomy `nameOf()`, survive only in tooltips/data-attrs); rail-aware counterparty panels; ALL THREE graphs (money-flow `txnNetwork`; entity-resolution shared-identifier edges incl. the excluded near-match; named-BO via `boGraphHTML` fed `display_name` not `party_id` — fixing the `:695` bug); the caution-list ownership chain; the inbound prior-STR panel (own inline synthetic marker); the file-vs-dismiss fork; "N pct" not "%"; REFACTOR — reuse `liveGraphLayout`/`boGraphHTML` 1:1 (a small local adapter only if a shape diverges). | scope: workbench.html | success: `node tests/casefile.test.mjs` (added T5; asserts names-not-codes — no bare C#/D#/ML-A# in rendered text — AND the three graphs render AND the always-on "Illustrative data & outputs" badge present) | size: L — DONE 2026-06-23: `showcaseSurface(detail)` branch in `paintSurface` (on `DETAIL.showcase`) renders the full rich investigation — a scoped `<style>`, NAMES-NOT-CODES (capName + the atom labels; bare codes only in title tooltips), the rail-aware counterparty ledger (channel-shaped identity), ALL THREE graphs reusing `liveGraphLayout` (money-flow `scMoneyFlowGraph` · entity-resolution `scResolutionGraph` w/ resolved/flagged/excluded edges · named-BO `scBOGraph` fed `display_name` — the `:695` party_id bug avoided), the caution-list ownership chain + the inbound prior-STR panel (own synthetic marker), the determination walk with per-leg `via` tags (fired/read/gathered), and the file STR / clear clearance-record outcome (snake_case humanized). `qcardHTML` distinguishes the pair (★ north-star + file/clear chip + capability NAMES). NOTE: extended the existing `tests/workbench.test.mjs` harness (the T5 description's allowed alternative), not a new casefile.test.mjs. Rendered both cases to standalone previews + screenshotted (sent to the user) — reads as a real investigation. VERIFIED via T5.
- [x] T5 (M) — Tests + drift guard: RED→GREEN — extend the workbench `.mjs` test (`tests/casefile.test.mjs` or the `tests/workbench.test.mjs` arc) covering the live verdicts (file/escalated vs cleared/documented_dismissal), the 3 graphs, ER resolve / excluded-near-match, names-not-codes (no bare code in rendered text), "N pct" not "%", the badge always-on, the prior-STR synthetic marker, XSS-escape via esc, keyboard guards, both motion modes; add the python selftests to the pre-present sequence + a smoke-checklist row; REFACTOR — the suite is the regression gate. `python3 scripts/build.py --check all` 8/8 byte-frozen (companion-only — no dist drift). | scope: tests/casefile.test.mjs, tests/workbench.test.mjs, tests/smoke-checklist.md | success: `node tests/casefile.test.mjs && python3 scripts/evidence_requirements.py --selftest && python3 scripts/serve_workbench.py --selftest && python3 scripts/build.py --check all` (8/8 byte-frozen) | size: M — DONE 2026-06-23: committed `tests/fixtures/casefile/{CASE-A,CASE-B}.detail.json` + `queue.json` (the COMPUTED engine output — render-replay fixtures, like the news goldens; the live computation is verified by serve_workbench --selftest). Added 23 showcase assertions to `tests/workbench.test.mjs` (124→**150**): the file/clear fork (computed), all 3 graphs render, names-not-codes (no bare C#/D#/ML-A#/KYC-A# in stripped rendered text), the caution chain + inbound prior-STR + synthetic marker, per-leg via provenance, "N pct" not "%", CASE-B self-containment (no Northgate/James-Calder reference), the queue north-star distinction, XSS-escape. FULL REGRESSION GREEN: `node tests/workbench.test.mjs` 150/0 · `evidence_requirements --selftest` · `serve_workbench --selftest` · `curate_workbench_cases --selftest` · `python3 scripts/build.py --check all` 8/8 byte-frozen (companion-only — workbench.html/data/casefile not a dist) · `uv run pytest` 20. smoke-checklist.md gets a Phase-73 row.

> Phase 73 — direction gate 2026-06-23 (all_accept:true — warned + restated; the load-bearing risk is A1, defended by a hard STOP not a silent pass). The user REFRAMED off the curated-substrate-case direction: the cases are "not it" → INVERT the pillar dependency (signal-watch authors the north-star rich case first; substrate/casework parked). The user OVERRODE two of the 4 gate questions: ship target = EXTEND THE COMPANION WORKBENCH (the live sufficiency engine actually RUNS over the rich data — not a precomputed dist; directly fixes "the workbench is terrible"); ceremony = STANDARD (two L accepted, unified reviewer). The two AML-correctness calls accepted as recommended: the DISMISS leads with affirmative business reconciliation + established source of funds (clearing a funnel alert on "no negative hit" is AML-wrong); the prior-HT-STR sits on an INBOUND funds source, the outbound controller is a DIFFERENT identity (corroboration belongs on the source; de-concentrate the synthetic identity). A0 [boundary] companion-only / build.py imports no sibling/companion / 8 dists byte-frozen — ACCEPT. A1 [T0 weakest, HONESTY+ENGINE] the live engine can express an honest affirmative-`cleared` WITHOUT moving the file bar — ACCEPT-with-GUARD (T2 adds a SEPARATE clear path requiring positive clean evidence; the file bar stays byte-identical). A2 [accept] verdicts are engine OUTPUT over authored evidence, not authored-frozen strings. A3 [accept] the three graphs reuse the existing layout engines (`liveGraphLayout`/`boGraphHTML` fed display_name). A4 [accept] the honesty governor (demo-visible richness + defensibility, ZERO catch-rate/lift; structured facts record-sourced; synthetic-by-construction; badge + prior-STR synthetic marker). A5 [accept] STANDARD ceremony (user override). A6 [accept] the cross-pillar contract doc is a DEFERRED follow-on; substrate/casework parked.
> Abort: any new ship target / dist drift / a sibling-or-companion import in build.py / a validator loosened to force a fit → STOP-and-surface. If clearing Lakeshore requires WEAKENING the file/determination bar or FABRICATING evidence the authored data doesn't carry (A1 false) → STOP-and-surface, do not loosen the gate, do not fake. If the live engine cannot be made to COMPUTE the two verdicts over the authored evidence without authored-frozen strings (A2 false) → STOP, surface the live-engine path as blocked. If any render/doc reads as a catch-rate / detection-lift / precision improvement → re-word (the honesty governor). If a structured fact (ownership pct, predicate, identifier) would render from the model rather than the record (the Phase-66 lesson) → record-source it, never render the fabrication.

<!-- phase:phase-72-consume-c14-kyc-sign-path -->
<!-- gate-log:phase-72 direction=approved delivery=accepted -->
<!-- status: PLANNED 2026-06-23 (LITE). Direction gate NOT all-accept — A2 don't-know → T1 probe (measure-first, the merge×firewall collision); A1/A3/A4/A5/A6 accept. CONSUME the kyc/C14 sign-path Phase 71 deferred: re-pin substrate 443e4a6→f15c241 (emits C14) + re-vendor casework 157554b→bf15535 (signs C14 + fixes a stale _kyc_defect drift) + re-curate → kyc cases reach KYC-A1 (C14 ALONE licenses it: mechanism_required:1, additional_legs_required:0) and SIGN end-to-end. Scope=BOTH: close the kyc slice AND verify the existing ~107 ML-determined cases now actually SIGN (the determine→SIGN gap). Companion-only; build.py imports none of it; 8 dists byte-frozen. Still deferred: C1 (principled measured null), C7 (screening-only), TF (no live path in any pillar). -->

## Phase 72 — Consume the C14 kyc sign-path: re-pin substrate + re-vendor casework, close the §12 kyc determine→sign half

Objective: CONSUME the kyc/C14 sign-path Phase 71 deferred as sibling-rooted — now BUILT on both sibling sides (CODE-VERIFIED LIVE this session, not from loaded pins). **aml-substrate@f15c241** (Phase 26) flipped C14 `KycIntegrityDetector` into `SCREENING_EMISSION_DETECTORS` (txn-less party-leaf via `Alert.party_ref`; person-scoped; fires on `elevated_obligation and source_of_funds is None`; the "C8-ONLY" suppression LIFTED). **aml-casework@bf15535** (Phase 14) broadened `_screen_c14_kyc_integrity()` to the full `elevated_obligation` predicate so C14 cases SIGN, and fixes a stale `_kyc_defect` drift the vendored copy (157554b) still carries. Phase 72 is the matching CONSUME: re-pin substrate `443e4a6→f15c241` (`curate_workbench_cases.SUBSTRATE_HEAD`), re-vendor casework `157554b→bf15535` (`scripts/vendor_casework.sh`), re-curate → kyc cases appear, reach KYC-A1 (the committed `evidence-requirements.json` `kyc_integrity` profile needs `mechanism_required:1, additional_legs_required:0` → **C14 ALONE licenses a kyc determination**), and SIGN end-to-end. **Scope=BOTH** (user's pick): close the kyc slice AND verify the existing ~107 ML-determined cases now actually SIGN (the determine→SIGN gap). Measure-first keystone (T1 STOP+REPORT, mirroring Phase 71). Companion-only — build.py imports none of it; 8 dists byte-frozen; the honesty governor holds (zero precision/lift; structured facts record-sourced; illustrative badge). STILL DEFERRED (NOT consumable): C1 (a PRINCIPLED measured null — substrate refuses it as a C8/C6 double-count, will never be built), C7 (screening-only/deferred — C8 carries ML-A3), TF (no crime_type / population / emission / verifier in ANY of the 3 pillars). Direction gate 2026-06-23; ledger Phase-72; plan [[phases/phase-72-consume-c14-kyc-sign-path]]. Grounded against signal-watch HEAD 175678f / the Phase-71 workbench (342 cases, 81/342 §12-closed) / aml-substrate@f15c241 / aml-casework@bf15535.

- [x] T1 (M) — Re-pin + re-vendor + re-curate PROBE (KEYSTONE, STOP+REPORT, nothing committed; A2 measure-first). In a SCRATCH measurement, re-pin substrate@f15c241 + re-vendor casework@bf15535, re-curate, and MEASURE: kyc-case count, how many reach KYC-A1, how many SIGN; the existing ML sign count before/after (no-regression check); whether the per-customer MERGE (`_merge_bundles`) × the dual-map firewall suppresses kyc closure (the A2 weakest-assumption check — folding a C14 party-leaf into a customer with ML caps may reclassify it money_laundering → C14→ML-A7 single leg → kyc suppressed). STOP+REPORT to the user before T2 commits anything. | scope: scripts/curate_workbench_cases.py, scripts/vendor_casework.sh, vendor/aml-casework/**, scripts/evidence_requirements.py | success: a written measurement report (kyc count / KYC-A1 reach / kyc sign count / ML sign before+after / merge-suppression verdict) produced AND the user gate passed before T2 — DONE 2026-06-23 (probe `.dev-wiki/tmp/ph72_probe.py`, scratch emit substrate@f15c241). FINDINGS: 727 C14-PURE customers classify kyc + ALL determine from KYC-A1 (C14 alone); 926 C14-MIXED fold into ML (the dual-map is CORRECT, not a bug — C14→ML-A7 corroborates the ML case). A2 RESOLVED: the merge does NOT suppress kyc closure (727 pure cases close it); T3 firewall is a measured NO-OP. A4 CLEAN: re-vendor preserves ML signings (0 regressions). kyc SIGNING is the honest FRONTIER — txn-bearing C14 signs @bf15535 (fails @stale 157554b), purely txn-less party-leaf fails-CLOSED at casework's no-transactions contract. STOP+REPORT delivered. | size: M
- [x] T2 (L) — Commit the pins (DEPENDENCY; T1-resolved). Re-pin substrate (`curate_workbench_cases.SUBSTRATE_HEAD` 443e4a6→f15c241) + re-vendor casework (`vendor_casework.sh` → `VENDORED_AT` bf15535) + re-curate the population. | scope: scripts/curate_workbench_cases.py, scripts/vendor_casework.sh, vendor/aml-casework/**, data/workbench/** | success: curate re-derives AND `node tests/workbench.test.mjs` green AND `python3 scripts/build.py --check all` 8/8 byte-frozen AND `uv run pytest` green — DONE 2026-06-23: SUBSTRATE_HEAD 443e4a6→f15c241; re-vendored casework→bf15535 (VENDORED_AT); casework_pin now READ from VENDORED_AT (not hardcoded — the Phase-71 stale-pin lesson); _measure_grounding gives an honest contract-rejection e2e_note; re-curated 342→355, coverage 107/342→128/355, funnel 183/110/62. ALSO fixed the re-curate-exposed demo coupling: the gather/finale demo resolves from the OSINT corpus (`gather_demo_case_id`), not the volatile mule exemplar. workbench.test.mjs 127 · --check all 8/8 · uv run pytest 20. | size: L
- [x] T3 (M, gated on T1) — Curate firewall: keep C14-pure customers classifiable as `kyc_integrity` through the per-customer MERGE (the fix the T1 probe specifies; SKIP if the probe shows none needed). | scope: scripts/curate_workbench_cases.py | success: kyc cases reach KYC-A1 and classify as `kyc_integrity` (NOT money_laundering) AND the curate selftest re-derives dynamically (no hardcoded re-baseline) AND no validator loosened — NO-OP (measured) — T1 proved the dual-map is CORRECT: C14-pure (727) classify kyc cleanly, C14-mixed (926) correctly classify ML (the SoF gap corroborates the ML case). No firewall added (forcing mixed cases to kyc would be dishonest). Curate selftest dynamic; no validator loosened. | size: M
- [x] T4 (M) — kyc determination + SIGN render path in the workbench; verify the existing ML cases SIGN (scope=BOTH). Surface kyc cases (KYC-A1 + the signed STR; reuse `boGraph` for KYC-A2 if C15 co-present). | scope: scripts/serve_workbench.py, workbench.html, tests/workbench.test.mjs | success: `tests/workbench.test.mjs` covers the kyc determine+sign path + an ML-sign assertion AND both motion modes AND XSS-escaped AND companion-only (no ship-dist drift) — DONE 2026-06-23: serve_workbench --selftest asserts the §12 KYC closure (a C14-pure case determines from KYC-A1, C14 alone; txn-bearing kyc signs, txn-less fails-closed at casework's txn contract); determination verdict made crime-type-HONEST (kyc states its own basis, not the ML legs/mitigation checklist); workbench.html surfaces the honest e2e_note for the won't-sign frontier; 3 new kyc tests (124→127). ML signings verified (126/349 in the new slice, 0 regressions). | size: M
- [x] T5 (S) — Docs + pin bumps + boundary/honesty sweep. Update docs/case-workbench.md, docs/evidence-driven-filing.md, the coherence brief; bump `VENDORED_AT` + `SUBSTRATE_HEAD` references; CLAUDE.md current-state in-place (≤~200 lines). | scope: docs/**, CLAUDE.md, scripts/curate_workbench_cases.py | success: docs reflect the new pins AND `python3 scripts/build.py --check all` 8/8 AND `grep` confirms build.py imports no companion layer AND CLAUDE.md maintained ≤~200 lines — DONE 2026-06-23: CLAUDE.md current-state replace-in-place; docs/case-workbench.md + docs/evidence-driven-filing.md +Phase-72 KYC sections; the COHERENCE brief + the 2 kyc PLAN-BRIEFs + smoke-checklist status-noted (EMIT/GROUND/CONSUME done, the txn-less frontier named); pins bumped. REGATE: --check all 8/8 · workbench 127 / corpus / gate 68 / triage 93 / news 150 · serve_workbench/curate/gather-harness selftests · uv run pytest 20 · honesty greps clean · build.py imports none of the companion layer. | size: S

> Phase 72 — direction gate 2026-06-23 (NOT all-accept; A2 down-scoped to the T1 measure-first probe). The user picked "Yes — kyc-consume" over deferring/pivoting: the two halves of the kyc/C14 sign-path Phase 71 deferred as sibling-rooted are now BUILT (code-verified live — substrate@f15c241 emits C14; casework@bf15535 grounds+signs it + fixes a stale `_kyc_defect` the vendored 157554b carries). A1 [direction] the kyc-consume is the right Phase 72 over the carried alternatives (roll sufficiency into the triage/gate consoles; gather robustness) — ACCEPT. A2 [T0 weakest — don't-know → DEFENDED-DOWN-SCOPE] the per-customer merge × dual-map firewall may suppress kyc closure → T1 measures, STOP+REPORT before T2 commits a curate firewall; if kyc survives the merge cleanly no firewall is needed; if suppressed, T3 keeps C14-pure customers separable (never pad/fabricate); if the slice yields zero kyc-pure customers → STOP-and-surface. A3 [accept] kyc determines on C14 ALONE (`additional_legs_required:0`, verified in the committed profile). A4 [accept] the re-vendor preserves the existing ML signings/funnel (broadened C14 grounding is additive); T1 measures regression. A5 [accept] boundary/honesty holds — companion-only, build.py imports none of it, 8 dists byte-frozen, zero precision/lift, structured facts record-sourced, illustrative badge. A6 [accept] TF stays OUT — no live path in any of the 3 pillars.
> Abort: any new ship target / dist drift / a sibling-or-companion import in build.py / a validator loosened to force a fit → STOP-and-surface. T1 KEYSTONE: if the merge suppresses kyc and no honest curate firewall recovers it → STOP-and-surface (report, do not force). If the re-vendor regresses the existing ML signings → STOP-and-surface (do not commit T2). If the slice yields zero kyc-pure customers → STOP-and-surface. If a live run shows a model-authored structured fact (the Phase-66 lesson) → record-source / sweep / fail-closed, never render the fabrication.

<!-- phase:phase-71-substrate-v03-slice-determination-loop -->
<!-- gate-log:phase-71 direction=approved delivery=accepted -->
<!-- status: DELIVERED + accepted 2026-06-23 (LITE; committed d8f667b, pushed to main; casework sibling 157554b). The §12 loop closes from REAL signals (0→81/342 via the per-customer merge); related_parties[] renders; casework accepts v0.3. kyc/C14/C1/C7 deferred (substrate non-emits C14). PLANNED 2026-06-23 (LITE). Direction gate NOT all-accept — A1/A2 don't-know → T1 probe (measure-first); A0/A3/A4/A5/A6 accept. Adopt aml-substrate's just-shipped Phase-25 v0.3 slice (related_parties[] — the real BO graph) + close the §12 determination loop in the workbench (≥2-leg from REAL signals, NOT gathered corroboration; pure signal-watch, NOT casework). The signed DECIDE finale is a separate later beat; the casework v0.3 bump here only keeps it signing on v0.3 cases. -->

## Phase 71 — Adopt the substrate v0.3 slice; close the §12 determination loop in the workbench

Objective: adopt aml-substrate's just-shipped Phase-25 **v0.3** slice (`related_parties[]` on the evidence bundle — the real BO graph) and CLOSE the §12 determination loop in the investigator workbench: a case reaches the **≥2-leg determination bar from REAL signals** (not gathered corroboration), the real `related_parties[]` renders in the network view, and the `kyc_integrity` profile exercises end-to-end. §12 closure = the DETERMINATION beat (`evidence_requirements.determine` over the fired capabilities) — pure signal-watch; it does NOT route through casework. The signed DECIDE finale (subprocesses to vendored casework) is a SEPARATE later beat; the casework v0.3 bump here only keeps it signing on v0.3 cases (deeper new-detector verifiers DEFERRED to a casework phase — the C3/C15 pattern). CROSS-PILLAR CODE-VERIFIED (the brief's prior pin b53855c was STALE): aml-substrate@443e4a6 is v0.3 (`evidence.py:49 CONTRACT_VERSION="0.3"`, related_parties emitted); aml-casework@021fb80 (live sibling AND the vendored copy) is `KNOWN_CONTRACT_VERSIONS=("0.1","0.2")` → REJECTS v0.3 (`contract.py:48/391`); signal-watch@9a7637a bundles are 0.2 + the network view renders from the synthetic OSINT corpus, not related_parties[]. The gating dependency is casework and it is CHEAP (validate_bundle gates only on the version whitelist + tolerates unknown extra fields → a one-line bump + re-vendor should preserve the ~99/294 signings; MEASURED in T1). Companion-only / LITE; 8 dists byte-frozen; build.py imports no casework/substrate/osint_tools; the agent runs server-side (§4.5); vendoring is distribution-not-coupling; structured ownership facts read from the BUNDLE not the model (the Phase-66 guard); ZERO catch-rate/precision/lift (the single-signal-separable governor). Measure-first, mirroring the substrate's own P25 verifier-first/STOP+REPORT shape. Direction gate 2026-06-23; ledger Phase-71; plan [[phases/phase-71-substrate-v03-slice-determination-loop]]. Grounded against signal-watch HEAD 9a7637a / the Phase-70 workbench / aml-substrate@443e4a6 / aml-casework@021fb80.

- [x] T1 (M) — Re-vendor + bump PROBE (KEYSTONE, STOP+REPORT, nothing committed; A1/A2 measure-first). DONE 2026-06-23 (emitted v0.3 seed-0/40k → 22,924 cases; probe `.dev-wiki/tmp/ph71_probe.py`, sibling temp-bumped+reverted). FINDINGS: **(d) ✓** contract_version 0.3 on ALL; related_parties[] on ALL (2,571 non-empty). **(c) ✗→DEFER** C14 fired in 0 cases — the substrate DELIBERATELY non-emits C14 (`cli.py:92` "C8-ONLY (C26/C14 deliberate non-emission)") → kyc unreachable by the consume path; DEFERRED per user gate. **A1 HOLDS** (contract bump clean): v0.2 99/294 → v0.3 88/294 = 11 regressions, ALL corpus-pin (`fin-2025-a003.json` missing from casework's vendored corpus snapshot `signal-watch@a75a136`), **0 from contract/SoF/C14** (SoF inert: no C14 alert → its only SoF-reading path never fires; related_parties ignored). The 11 fix via a corpus re-vendor (T2). **A2 RECOVERABLE via MERGE**: 0 from deduped bundles → **366** (capability legs) / **667** (with related_parties→ML-A4) under a PER-CUSTOMER MERGE (dedup-keep-richer drops the C8 screening leg); closure leg pair = **C8 (ML-A3) + C15/related_parties (ML-A4)**, NOT the brief's C1/C7/C14. USER GATE: merge YES, kyc DEFER. | scope: scratch only (nothing committed) | success: STOP+REPORT delivered; A1/A2 resolved with numbers | size: M
- [x] T2 (L) — Re-vendor for real + casework v0.3 bump + the consume-side MERGE (DEPENDENCY; T1-resolved). DONE 2026-06-23: (i) casework `KNOWN_CONTRACT_VERSIONS` += "0.3" + the `fin-2025-a003` corpus file + PIN.md COMMITTED in the sibling (157554b, 289 casework tests green) → re-vendored (VENDORED_AT 021fb80→157554b) so the vendored corpus carries `fin-2025-a003` (fixes T1's 11 corpus-pin regressions). LATE NUANCE: the gap was SOURCE-COVERAGE (the snapshot only covered fincen-alerts/, never the fincen advisory series), not pin-staleness. (ii) `_merge_bundles` per-customer MERGE added to `curate_workbench_cases` (replaced dedup-keep-richer) → C8 (ML-A3) + C15 (ML-A4) co-occur; merged cases casework can't ground fail-close honestly (the determination still closes — the §12 bar is the determination beat, not the signature). (iii) re-vendored+merged v0.3 population committed: 294→342 cases, coverage 99/294→107/342, funnel 189/66/39→181/79/82; **§12: 0→81 cases reach ≥2-leg from real signals**. Escape hatch DEPENDENCY (committed the local-only casework sibling — no remote, reversible). | scope: vendor/aml-casework/**, data/workbench/**, ../aml-casework (version bump + corpus re-snapshot), scripts/{vendor_casework.sh,curate_workbench_cases.py} | success: `data/workbench/bundles/*.json` carry `contract_version` 0.3 + `related_parties[]` + merged caps AND the vendored casework accepts 0.3 + has `fin-2025-a003` AND ≥1 case carries C8+C15 (≥2 legs) AND `serve_workbench --selftest` finale signs (no v0.3-contract regression; corpus-pin fixed) AND `curate --selftest` passes vs the new slice | size: L
- [x] T3 (M) — Render related_parties[] in the network view. DONE 2026-06-23: `boGraphHTML` renders the bundle's `related_parties[]` as a labelled ownership-weighted SVG network on the case page (reuses `liveGraphLayout`, fixed to carry `ownership_pct`); "N pct" never "%"; declared-not-gathered framing; owner KYC posture (sanctions/PEP/risk) on edge rows; XSS-escaped. 7 new tests; 124 green. Wire the workbench network/SVG view to render the bundle's `related_parties[]` (the real BO graph) for cases that carry it, REUSING the existing render (today only the synthetic OSINT corpus feeds it). Ownership facts read from the BUNDLE, never the model (the Phase-66 guard); "N pct" not "N%"; demo-visible richness, never a catch-rate/lift claim. | scope: workbench.html, scripts/serve_workbench.py | success: `node tests/workbench.test.mjs` asserts a case's `related_parties[]` renders as the labelled ownership-weighted network AND no %/catch-rate/precision/lift vocabulary AND XSS-escaped | size: M
- [x] T4 (M) — Close the §12 determination loop (REAL signals; ML only — kyc DEFERRED per T1). DONE 2026-06-23: no present_atoms change needed (C15→ML-A4, C8→ML-A3 already light from real caps — lighting ML-A4 from mere related_parties-presence would over-fire dishonestly, so related_parties stays the ML-A4 EVIDENCE/render). serve_workbench --selftest now asserts ≥1 case (CASE-P-0000008) reaches the ≥2-leg ML determination from REAL signals + named-risk + mitigation → "determination" WITHOUT gather; withholds without the human inputs; signal_brief still names the deferred C1/C14 gaps. Old "no case reaches ≥2-leg" thesis updated. Wire the determination beat so ML-A4 lights from the bundle's `related_parties[]` (the real network leg, not just C15/gathered) + ML-A3 from C8 — a case reaches ≥2-leg from REAL signals (not gathered corroboration). kyc_integrity is DEFERRED (T1: C14 deliberately non-emitted by the substrate → not consumable here; becomes a §12 substrate-emission brief item via `signal_brief`, NOT a deliverable this phase). | scope: scripts/evidence_requirements.py, scripts/serve_workbench.py | success: `serve_workbench --selftest` asserts ≥1 case reaches the ≥2-leg ML determination bar from REAL signals (C8 ML-A3 + C15/related_parties ML-A4) — the §12 ML closure; the deferred kyc/C1/C7 gaps surface honestly via `signal_brief` | size: M
- [x] T5 (S) — Re-baseline + tests + docs + full regate. DONE 2026-06-23: selftests are dynamic (curate/serve re-derived 342/107 + funnel 181/79/82, no hardcoded re-baseline needed). Docs updated: case-workbench.md (+Phase-71 section, 107/342, deferred-queue status), evidence-driven-filing.md (+§12-closes section), the consolidated brief (STATUS header), CLAUDE.md Current-state (replace-in-place), smoke-checklist.md (numbers + BO graph + §12 rows). HANDOFF untouched (milestone-level, not per-phase — Phase 67-70 precedent). REGATE: build --check all 8/8 zero drift · workbench.test.mjs 124 · serve/curate selftests · evidence_requirements · gather_quality_harness · news/gate/triage/corpus arcs · uv run pytest 20 — ALL green. Honesty: rendered output carries no %/catch-rate/lift. Re-baseline the committed selftests to the new slice (gate funnel / coverage); update `docs/case-workbench.md` + `docs/evidence-driven-filing.md` + the brief's consume-side note + CLAUDE.md Current-state (replace-in-place, the maintenance contract) + HANDOFF §8 + `tests/smoke-checklist.md`; honesty-governor sweep. (CLAUDE.md/HANDOFF edits happen in THIS task by the implementing orchestrator.) | scope: docs/**, CLAUDE.md, HANDOFF.md, tests/smoke-checklist.md, tests/*.mjs baselines | success: `python3 scripts/build.py --check all` (8/8 zero drift) && `node tests/workbench.test.mjs` && `python3 scripts/serve_workbench.py --selftest` && `python3 scripts/curate_workbench_cases.py --selftest` && `uv run pytest` && honesty greps clean (no catch-rate/precision/recall/lift/% in workbench.html) | size: S

<!-- phase:phase-70-gather-quality-substrate-handoff -->
<!-- gate-log:phase-70 direction=approved delivery=accepted -->
<!-- status: COMPLETE — T1–T5 ALL [x]; delivery accepted by the user 2026-06-22 (LITE). The orchestrator flips the delivery gate after the commit verifies. NOT all-accept — A1 down-scoped (measurement-first baseline), A4 consolidated (one brief absorbs/supersedes the BO-graph brief). The named Phase-69 follow-on RESOLVED: live gather coverage 0.5→1.0, ML-A5 closes, the determination becomes reachable. -->

## Phase 70 — Gather extraction quality (measured) + the consolidated §12 substrate handoff

Objective: measure the live GATHER's extraction recall against the deterministic StubPlanner reference (consistency-not-correctness, the Phase-65 honesty class — NOT "tune the model") and fix the diagnosed under-extraction surface; then consolidate the proliferating sibling-substrate asks into ONE pinned §12 handoff brief. Two halves over one spine (the §12 discovery loop). ROOT CAUSE code-verified (osint_tools.py:430-447): `findings()` passes the model only `{id,text}` per record — not the record's DECLARED entity/officers/relationship names it must copy exactly — with an open-ended "if nothing relevant return []" prompt that conflates an ownership-tie with a direct sanctions hit (the model called the sanctions tool in Phase 69 but under-produced findings from it). Companion-only / LITE; 8 dists byte-frozen; build.py imports no casework/substrate/osint_tools; the agent runs server-side (§4.5). Direction gate 2026-06-22; ledger Phase-70; plan [[phases/phase-70-gather-quality-substrate-handoff]]. Grounded against signal-watch HEAD fbd2291 / the Phase-69 workbench.

- [x] T1 (L) — Gather coverage metric + stub-reference selftest + live BASELINE (measurement-first; A1 down-scope diagnosis). Add a coverage metric (grounded findings vs records returned; targeted-atom closure) to the gather result + NDJSON stream; assert the StubPlanner achieves full targeted-atom closure on the demo case(s) in `serve_workbench --selftest`. Run the gather LIVE once as a baseline; record grounded/dropped/coverage and DIAGNOSE which surface under-extracts (prompt vs payload vs leg-mapping). | scope: scripts/osint_tools.py, scripts/serve_workbench.py, tests/workbench.test.mjs | success: the coverage metric is present in the gather result + stream AND `python3 scripts/serve_workbench.py --selftest` asserts the stub achieves full targeted-atom closure on the demo case AND the live baseline coverage + the diagnosed surface are recorded in docs/evidence-driven-filing.md | size: L
- [x] T2 — Fix the diagnosed under-extraction surface (expected: `findings()` payload-enrich [surface the record's declared entity/officers/relationship names] + an exhaustive-per-record prompt + ownership/direct-hit disambiguation). Structured facts stay record-sourced (no model-authored label/pct); the honesty sweep + grounding gate unchanged. | scope: scripts/osint_tools.py | success: `python3 scripts/osint_tools.py --selftest` green (every gate-DROP case + the record-sourced ownership assertion still hold) AND the fix targets the T1-diagnosed surface | size: M
- [x] T3 — Re-measure live + pin replay fixture + coverage-regression (A3). Re-run the gather LIVE once after the fix; confirm the corroboration finding now grounds + closes its leg; pin ONE live capture as a deterministic replay fixture; add a coverage-regression assertion vs the stub reference (the news_quality_harness pattern, minimal — replayable with NO model; `--freeze` = the conscious re-baseline). | scope: tests/fixtures/, scripts/osint_tools.py, tests/workbench.test.mjs | success: the pinned capture replays deterministically (no model) AND the coverage-regression assertion is green vs the stub reference AND live before/after coverage recorded in docs/evidence-driven-filing.md | size: M
- [x] T4 — Consolidated aml-substrate handoff PLAN-BRIEF (A4 consolidate) + the derived gap inventory. Deterministically aggregate the non-gatherable atoms (signal_brief across the demo/population cases → ML-A3/A6/A7) + the kyc/TF-unexercised finding; author ONE consolidated brief that absorbs/supersedes docs/substrate-bo-graph-emission-PLAN-BRIEF.md and adds the determination-signal detector asks (C1/C7/C8/C14 + SoF/anticipated/income + party/UBO-at-source + a kyc/TF case slice). Code-verify the substrate's current state for an honest pin. | scope: docs/substrate-*-PLAN-BRIEF.md, scripts/evidence_requirements.py, docs/evidence-driven-filing.md | success: one consolidated handoff brief pinned to a code-verified substrate commit (acceptance-seam + contract-version pattern) AND the BO-graph brief folded/superseded AND the gap inventory derived-not-hand-listed (a selftest assertion if a helper is added) | size: M
- [x] T5 — True-up + verify. CLAUDE.md + docs/case-workbench.md / docs/evidence-driven-filing.md (the Phase-70+ sequence) + tests/smoke-checklist.md row; drift guard + selftests + pytest. | scope: CLAUDE.md, docs/, tests/smoke-checklist.md | success: `python3 scripts/build.py --check all` 8/8 ZERO drift AND build.py imports no casework/substrate/osint_tools AND `uv run pytest` green AND node tests/*.mjs + serve_workbench/osint_tools/evidence_requirements/news_ground selftests green AND CLAUDE.md + docs trued up | size: S

> Phase 70 — direction gate 2026-06-22 (NOT all-accept; A1/A4 each moved the scope). The user chose the gather-quality + consolidated-handoff direction (the named Phase-69 follow-on). A0 [boundary, Phase-67/68 precedent] companion-only / build.py imports no casework/substrate/osint_tools / 8 dists byte-frozen / agent server-side (§4.5) — ACCEPT. A1 [T0 weakest, don't-know → DOWN-SCOPE] the live baseline is a measurement-FIRST task that diagnoses the under-extraction surface before any fix is committed (measurement gates the fix). A2 the StubPlanner-as-reference frame is honest (consistency-not-correctness; ZERO catch-rate/precision/lift number — the single-signal-separable governor) — ACCEPT. A3 re-measure live once + pin ONE capture as a deterministic replay fixture + a coverage-regression assertion vs the stub (the news_quality_harness pattern) — ACCEPT. A4 [reject one-brief-per-gap → CONSOLIDATE] ONE consolidated substrate handoff brief that absorbs/supersedes the BO-graph brief.
> Abort: any new ship target / dist drift / a sibling-or-companion import in build.py / a validator loosened to force a fit → STOP-and-surface. If the live execute-once shows a fabricated structured fact (the Phase-66 lesson) → record-source / sweep / fail-closed, never render the fabrication. If gather coverage can't be measured honestly vs the stub reference → STOP-and-surface, do not claim an unmeasured improvement.

<!-- phase:phase-69-evidence-sufficiency-filing-control -->
<!-- gate-log:phase-69 direction=approved delivery=accepted -->
<!-- status: COMPLETE — T1–T6 ALL [x]; delivery accepted by the user 2026-06-22 (LITE). The orchestrator flips the delivery gate after the commit verifies. NOT all-accept — A3 resolved-local, A4 reconciled all-typology (A4 bit-partial: population all-ML, kyc/TF profiles unexercised). -->

## Phase 69 — Evidence-sufficiency filing control: requirement profile → complete-or-honestly-gapped assembly → differentiated determination

Objective: fold the user's two product-coherence defects (lazy/incomplete filing + defensive-not-effective decisioning) into ONE companion-only control — evidence-sufficiency — over a shared per-typology evidence-requirement profile. Completeness is the MEASUREMENT; the decision is the SUFFICIENCY VERDICT (sufficient → a differentiated determination; insufficient → needs-more-info NAMING a data/signal gap to build in aml-substrate — the §12 loop). Supersedes the Phase-64 frequency-route (precedent/frequency → CONTEXT only); respects the substrate label-blind wall (the profile is chosen-not-measured); runs across the multi-typology 294-case population (all 3 crime_types). Companion-only / LITE; 8 dists byte-frozen; build.py imports no casework/substrate/osint_tools. Direction gate accepted 2026-06-22; ledger Phase-69; plan [[phases/phase-69-evidence-sufficiency-filing-control]].

- [x] T1 — Evidence-requirement profile — DONE. Authored `data/workbench/evidence-requirements.json` (ML + kyc_integrity; per crime_type: required STR elements + determination-licensing atoms; chosen-not-measured disclaimer; grounded in the SAR/FinCEN-2025 wiki + the capability taxonomy) + `scripts/evidence_requirements.py` (loader + `validate_requirements` [fail-loud; mirrors casework STR_REQUIRED_ELEMENTS/CRIME_TYPES embedded; C*/D* range-checked; honesty sweep] + the pure `evaluate_sufficiency` determination control) wired into `serve_workbench --selftest`. DOMAIN REFINEMENTS from the gate (user Q1/Q3): TF DROPPED this phase (no capability maps to it — "all typologies" = all-with-cases); ML bar STRICTER = mechanism + ≥2 legs + NAMED predicate risk + NO unrebutted mitigation; atoms enriched with A6 anticipated-activity + A7 source-of-funds (the mitigation atoms) + A4 network. | scope: data/workbench/evidence-requirements.json, scripts/evidence_requirements.py, scripts/serve_workbench.py | success: `python3 scripts/evidence_requirements.py --selftest` (profile validates; 10 tampers rejected; ML stricter bar + kyc bar correct) && `python3 scripts/serve_workbench.py --selftest` (profile consumable; ML bar withholds one leg short) && build.py does not import it (boundary).
- [x] T2 — Completeness assessment + honest-gap render — DONE. Added the shared case-assessment to `evidence_requirements.py` (`crime_type_for_capabilities`, `present_atoms`, `assess_completeness`) — the COMPLETENESS measurement (required STR elements vs casework's completeness dict + the determination ATOMS the case carries vs honest gaps). Wired into `serve_chain.run_case` payload (`completeness`) — REALITY NOTE: the structured STR render lives in the CHAIN workbench (chain.html/serve_chain), NOT serve_workbench (the artifact-writer scope said serve_workbench; the signed STR is in chain). `chain.html`: `strRecordHTML` now renders the previously-DROPPED fields (aliases/beneficial_ownership/IP/VC/EMT/DOB/counterparty_country/account_action) as honest-NULL; new `completenessHTML` panel renders STR-element + atom present/gap (the mule = 3/7 atoms, mechanism✓✓ + A4, one leg short — the GATHER payoff). | scope: scripts/evidence_requirements.py, scripts/serve_chain.py, chain.html, tests/chain.test.mjs | success: `python3 scripts/serve_chain.py --selftest` (completeness rides the payload; mule = ML, mechanism present, ≥1 honest gap) && `python3 scripts/evidence_requirements.py --selftest` (assess_completeness) && `node tests/chain.test.mjs` 55/0 (dropped fields render honest-NULL; panel reports 6/6 STR + 3/7 atoms; ML-A5 renders as gap; esc() sole escaper).
- [x] T3 — Requirement-targeted GATHER — DONE. `evidence_requirements`: added `gather_signal` to ML-A4 (ownership) / ML-A5 (corroboration) / KYC-A2 (ownership) + `GATHER_KIND_TO_SIGNAL` (registry→ownership, sanctions/adverse→corroboration) + `gathered_signals()` + `gather_targets()` + validator tolerance; shared cached `requirements()` (serve_chain + serve_workbench both use it). `serve_workbench.run_gather`: emits a `requirement` stage NAMING the unmet closeable atoms BEFORE gather, and the `done.requirement` reports {targets, closed, still_open, present_before/after} — VERIFIED: the mule (mechanism + ML-A4 from C15) TARGETS ML-A5 and CLOSES it from the record-sourced sanctions+adverse findings → 2 legs → determination reachable. OSINT authoring: the demonstrated subject (Zane Zhao) is ALREADY covered by Phase-66's network (registry→Crescent Dunes→sanctions + adverse) → no redundant entries; the still-OPEN path is demonstrable on the many uncovered subjects (gather honestly closes nothing). FINDING (material, for delivery + A4 revisit): the case-workbench population is ALL money_laundering (C14/C7 absent → kyc_integrity has no case, like TF); ZERO cases reach the ≥2-leg bar from signals alone (181 mechanism+0-leg, 103 mechanism+1-leg); source_of_funds/expected_monthly_* are None in the data → A6/A7 are honest gaps → §12 material. | scope: scripts/evidence_requirements.py, scripts/serve_workbench.py, scripts/serve_chain.py, data/workbench/evidence-requirements.json | success: `serve_workbench --selftest` (gather TARGETS + CLOSES ML-A5 record-sourced) && `evidence_requirements --selftest` (gather mapping) && `news_ground --selftest` + `osint_tools --selftest` (validate_osint_corpus) green; chain 55/0 + workbench 106/0 no regression.
- [x] T4 (L) — Differentiated determination: sufficiency SUPERSEDES frequency — DONE. `evidence_requirements.determine()` = the full verdict (completeness + sufficiency). `serve_workbench.determine_case()` + `POST /determine`: the disposition is licensed by evidence-SUFFICIENCY (mechanism + ≥2 legs + named risk + no unrebutted mitigation), n_precedent DEMOTED to `frequency_context` (the contrast, never the trigger); named_risk + mitigation_rebutted are the HUMAN elicitation inputs. `workbench.html`: the DETERMINATION beat (between GATHER and DECIDE) renders the frequency contrast + the atom assessment + the elicitation form (name-the-risk input + mitigation checkbox) + the verdict (determination green / needs-more-info amber with the NAMED gaps); folds in GATHER's gathered_signals. THESIS PROVEN in the selftest: the mule's frequency gate would clear, but signals-alone is needs-more-info (1 leg short); gather closes corroboration + named risk + mitigation → DETERMINATION; auto-clear cases are NOT auto-determinations (zero cases reach the ≥2-leg bar from signals alone). crime_type derived from capabilities — population is all-ML (C14/C7 absent → kyc_integrity unexercised, honest, like TF; FINDING surfaced for A4 revisit). | scope: scripts/evidence_requirements.py, scripts/serve_workbench.py, workbench.html, tests/workbench.test.mjs | success: `node tests/workbench.test.mjs` 115/0 (+9: frequency demoted to context; sufficient vs insufficient route DIFFERENTLY; insufficient NAMES the gap; XSS-escaped) && `serve_workbench --selftest` (the determination contrast + the auto-clear-≠-determination thesis) && `evidence_requirements --selftest` (determine() verdict matrix); chain 55/0 no regression; build.py imports no determine_case.
- [x] T5 — Named gap → substrate-signal brief + design doc — DONE. `evidence_requirements.signal_brief()` = the §12 feedback: the unmet, NON-gatherable atoms (no gather_signal) → a build-in-substrate brief naming each capability (C*) + data sources (D*) the determination needs but the program lacks; rides `determine()`'s output. `workbench.html`: the determination panel renders the brief ("Signals to build (§12 discovery loop) …") under an insufficient verdict. `docs/evidence-driven-filing.md`: the full spine (profile → completeness → targeted gather → differentiated determination → §12 feedback) + the chosen-not-measured/label-blind honesty seam + the honest findings (all-ML population; zero ≥2-leg cases; A6/A7 always-null) + the Phase-70+ sequence (console rollout + substrate names/UBO-at-source + the SoF/anticipated/income capabilities the briefs name). | scope: scripts/evidence_requirements.py, workbench.html, docs/evidence-driven-filing.md, tests/workbench.test.mjs | success: docs/evidence-driven-filing.md exists (spine + deferred half) && the brief renders for an insufficient case (`node tests/workbench.test.mjs` 116/0 incl. the §12-brief assertion) && `evidence_requirements --selftest` (signal_brief = {ML-A3,ML-A6,ML-A7} for the mule).
- [x] T6 — Run live once + boundary verify + vocab/docs — DONE. LIVE-ONCE executed (local Qwen at 127.0.0.1:8080, 2 runs): the gather is record-sourced — **ZERO structured-fact fabrications** (the Phase-66 ownership-pct guard held); when the live model under-extracted the corroboration finding (called screen_sanctions/adverse on Crescent Dunes but grounded only the registry edges), the determination WITHHELD honestly (needs_more_info — never fabricates sufficiency); the deterministic stub is the reliable model-free demo. BOUNDARY: `--check all` 8/8 ZERO drift; build.py imports no casework/substrate/evidence_requirements/osint_tools/serve_*; `uv run pytest` 19/19. DOCS: CLAUDE.md (the DETERMINATION beat woven into the workbench arc, replace-in-place), docs/case-workbench.md (the determination section), docs/chain-workbench.md (the completeness render), docs/evidence-driven-filing.md (the full design), tests/smoke-checklist.md (the Phase-69 walk). ADVERSARIAL REVIEW (6-dimension workflow, 19 agents): 1/13 confirmed — C5 was a GHOST capability (mapped to ML, cited by no atom) → FIXED (C5 added to ML-A1 "Placement/layering mechanism"; a no-ghost regression guard added); 12/13 refuted (honesty seam / boundary / faithfulness / XSS / cross-field consistency all HELD). | scope: tests/smoke-checklist.md, docs/case-workbench.md, docs/chain-workbench.md, docs/evidence-driven-filing.md, CLAUDE.md, data/workbench/evidence-requirements.json, scripts/evidence_requirements.py | success: `--check all` 8/8 byte-frozen && build.py imports nothing && `uv run pytest` 19/19 && the live-once counts recorded (0 fabrications, honest withhold).

> Phase 69 — direction gate 2026-06-22 (NOT all-accept; A3/A4 each moved the scope): the user REFRAMED the consolidate-vs-switch question into a PRODUCT-COHERENCE critique of the case→STR pipeline — (1) lazy/incomplete filing (chain.html drops most of the casework-emitted subject block) and (2) defensive filing keyed on combo-FREQUENCY rather than evidence-sufficiency. The two defects fold into ONE evidence-sufficiency control over a shared per-typology requirement profile. A0 [boundary, Phase-67/68 precedent] companion-only, build.py imports no casework/substrate/osint_tools, 8 dists byte-frozen, agent server-side (§4.5) — ACCEPT. A1 [T0 weakest] ONE control, single vertical slice — ACCEPT (fallback: reorder around the decision surfaces). A2 the profile is deterministic/chosen-not-measured, label-blind-safe — ACCEPT. A3 [don't-know → RESOLVED-LOCAL] fully signal-watch-local (synthetic display.name keys OSINT; UBO record-sourced); real names/UBO at source = the deferred §12 brief. A4 [reject one-typology → RECONCILED] the profile covers all 3 crime_types + the control runs across the multi-typology population, WITHOUT a signed STR per typology (the signed-STR completeness render stays on the one ML chain case CASE-P-0010361). The Phase-64 frequency-route is SUPERSEDED (precedent/frequency → context). Grounded against the committed Phase-68 workbench (signal-watch HEAD 268f84b). Plan [[phases/phase-69-evidence-sufficiency-filing-control]]; ledger Phase-69 block.
> Abort: any new ship target / dist drift / a sibling-or-companion import in build.py / a validator loosened to force a fit → STOP-and-surface. If the requirement profile can't be authored honestly as chosen-not-measured → re-word, never claim detection-lift. If the live execute-once shows a fabricated structured fact (the Phase-66 lesson) → record-source / sweep / fail-closed, never render the fabrication.

<!-- phase:phase-68-restr-vendor-render -->
<!-- gate-log:phase-68 direction=approved delivery=pending -->
<!-- status: PLANNED + BLOCKED on aml-casework Phase 13 shipping (signal-watch re-vendors casework's NEW commit; implementation is casework-side first) -->

## Phase 68 — Re-vendor the FINTRAC-STR-rich casework + render the structured STR

Objective: after aml-casework Phase 13 ships the FINTRAC-STR-rich drafter, RE-VENDOR it into signal-watch + render the structured FINTRAC STR record + the rich grounded Details-of-suspicion narrative in the workbench DECIDE view (currently a thin narrative blob), and true up SAR->STR vocab — boundary held, the 8 offline dists byte-frozen. PLANNED + BLOCKED on aml-casework Phase 13 (cannot implement until casework commits Phase 13). Direction gate accepted 2026-06-22; ledger Phase-68; plan [[phases/phase-68-restr-vendor-render]].

- [x] Re-vendor aml-casework Phase 13 (run scripts/vendor_casework.sh) + update VENDORED_AT pin | scope: vendor/aml-casework/**, scripts/vendor_casework.sh, vendor/aml-casework/VENDORED_AT | success: vendor reflects casework HEAD@Phase13 (021fb80; venv rebuilt); VENDORED_AT pins 021fb80 + date — VERIFIED: serve_chain CASE-P signs a SIGNED STR carrying all 5 structured blocks + the rich narrative
- [x] SAR->STR vocab true-up across the workbench UI/docs | scope: chain.html, workbench.html, docs/case-workbench.md, docs/chain-workbench.md, CLAUDE.md, README.md | success: "Signed STR" in the chain/case workbench surfaces + docs; no stray filer-facing "signed SAR"
- [x] Render the structured FINTRAC STR record + rich narrative in the workbench DECIDE view | scope: chain.html (the LIVE signed-STR view — the case workbench fails-closed by design), workbench.html | success: node tests/chain.test.mjs covers the structured-blocks render (offence + structured aggregate total) + honest-NULL display; the rich narrative flows through automatically via the re-vendor
- [x] Verify boundary + drift | scope: tests/ | success: build.py --check all 8/8 byte-frozen (chain/workbench.html companion-only); build.py imports no casework (grep clean); serve_chain + serve_workbench --selftest + all 7 .mjs arcs green

<!-- phase:phase-67-shippable-live-workbench -->
<!-- gate-log:phase-67 direction=approved delivery=accepted -->
<!-- status: T1–T4 ALL [x] — DELIVERED + accepted 2026-06-22 (A0 proven in isolation: clone → make setup → signed SAR offline, no sibling) -->

## Phase 67 — Make the LIVE investigator workbench shippable from a bare clone (vendor aml-casework + make setup)

Objective: a recipient who `git clone`s signal-watch runs the FULL live workbench — incl. the DECIDE signed-SAR finale — with NO sibling repo, via `make setup` (model-you-provide for the neural tier). VENDOR a copy of aml-casework into signal-watch + a setup target; the companion still subprocesses it (DISTRIBUTION, not import-coupling). Direction gate accepted 2026-06-22 (all_accept:true); ledger Phase-67; plan [[phases/phase-67-shippable-live-workbench]].

- [x] T1 (the A0 guard) Vendor casework + PROVE the venv builds + an offline stub DECIDE finale runs in TRUE ISOLATION. `scripts/vendor_casework.sh` rsyncs casework's runtime (`src/aml_casework` + `pyproject.toml` + `uv.lock` + README/license; EXCLUDE tests/.venv/.dev-wiki/__pycache__) from `../aml-casework` → `vendor/aml-casework/`; write `vendor/aml-casework/VENDORED_AT` (casework commit 81df91c + local-WIP note + date); `.gitignore` the vendored venv; add a `Makefile` `setup` target (`uv venv` + `uv sync` in `vendor/aml-casework/`). | scope: vendor/aml-casework/**, scripts/vendor_casework.sh, Makefile, .gitignore | success: in an ISOLATED fresh clone (`/tmp`, NO sibling repos), `make setup` builds the casework venv AND a scripted DECIDE consume produces a signed SAR with the STUB drafter — no model, no `../aml-casework`. IF `uv sync` won't build standalone → STOP-and-surface, escalate to a pre-built wheel (the A0 fallback).
- [x] T2 Wire the companion default to the vendored path (keep the subprocess boundary). `serve_workbench.py` + `serve_chain.py`: default `AML_CASEWORK_DIR` → `vendor/aml-casework` when present, else `../aml-casework`, else GATED; `AML_CASEWORK_PYTHON` → the vendored venv's python when present. NO import of casework. | scope: scripts/serve_workbench.py, scripts/serve_chain.py | success: `serve_workbench.py --selftest` + `serve_chain.py --selftest` green; with `vendor/` present + NO env set the DECIDE finale resolves to the vendored casework; with `vendor/` absent + no sibling → named GATED; `build.py --check all` 8/8 ZERO dist drift; `grep` confirms build.py imports no casework.
- [x] T3 Docs + the pin/boundary record. README + CLAUDE.md run docs: the live workbench is `git clone && make setup && python3 scripts/serve_workbench.py` (vendored casework, NO sibling); the live-tier prereq (Python ≥3.11 + uv; the offline ship artifacts stay zero-dep); the GATED fallback if `vendor/` is deleted; the vendoring-is-distribution-not-coupling note. Update `docs/case-workbench.md`. | scope: README.md, CLAUDE.md, docs/case-workbench.md, vendor/aml-casework/VENDORED_AT | success: docs state the clone+make-setup live-workbench path + the 3.11/uv prereq + the GATED fallback (grep-able); `VENDORED_AT` records the casework commit.
- [x] T4 Full regression + the shippability e2e + smoke. Re-run `uv run pytest` + `node tests/*.test.mjs` + all `--selftest`s + `build.py --check all` (8/8 ZERO dist drift, boundary held); re-run the T1 isolation e2e (clone → make setup → offline stub DECIDE → signed SAR) as the shippability proof; add a `tests/smoke-checklist.md` entry for the vendored live-workbench run path. | scope: tests/smoke-checklist.md | success: all suites green; `--check all` 8/8 ZERO drift; the isolation e2e produces a signed SAR offline with no siblings; smoke-checklist updated.

<!-- phase:phase-64-precedent-confidence-gating-engine -->
<!-- gate-log:phase-64 direction=approved delivery=pending -->

## Phase 64 — Precedent-confidence gating engine + the elicitation loop (live)

Turn the Phase-63 workbench's STATIC precedent-confidence bucketer into a LIVE, parameterized gating
CONTROL with a feedback loop — the Phase-63 follow-on #3, "the LFCM elicitation-loop path" (the
measuring→controlling pivot). Today `_confidence(combo, n_precedent)` in `curate_workbench_cases.py` is
a static bucketer baked at curate time (sample-size → {high/auto-clear, medium/review, low/human-gate};
thresholds 500/50 + cleared-% 88/62/28 HARDCODED; the gate frozen into `data/workbench/cases.json`; the
129/52/19 funnel a static count). `serve_workbench.py` serves cases + the live finale but does NO live
routing. The delta: (1) lift the hardcoded knobs into a visible `gating_policy` object ("chosen, not
measured") + a pure `route(confidence, sample_size, policy) → {auto-decide|review|human-gate}`; (2) the
decision APPLIED not displayed (auto-clear cases get an illustrative disposition applied automatically;
human-gate cases escalate — the control EXECUTES); (3) the ELICITATION LOOP (the LFCM core): a human
adjudicates a gated case → that disposition becomes session precedent → the combo's sample grows →
confidence recomputes → the next similar case may re-route toward auto (blueprint §14's continuous
adjudication loop made live; session-only, persists nothing); (4) EXECUTE ONCE live over the real
200-case slice (re-derive the funnel + demonstrate the loop shifting one routing decision).
THE HONESTY SEAM (load-bearing): the loop shifts ROUTING via REAL sample-growth (the §12-grounded firing
frequency — genuine); the recorded/auto-applied DISPOSITION stays labeled illustrative. It demonstrates
WHERE human judgment gets spent + how precedent concentrates it — NEVER that the auto-dispositions are
correct. Holds the Phase-62 §12/§14 boundary: route on §12 measurement, do NOT re-ground §14 from
probe-history. LITE; companion-only (serve_workbench extension, NOT a 9th ship target); the loop is
session-only / persists nothing; records byte-frozen / recompute from cases.json; `--check all` 8/8
ZERO dist drift; always-on "Illustrative data & outputs" badge; ZERO catch-rate/detection-lift number.
Plan: articles/phases/phase-64-precedent-confidence-gating-engine.md. Ledger: Phase-64 (direction gate
2026-06-21, all_accept:true — warned + restated per gate protocol [an all-accept on a NEW mechanism
direction]). Grounded against the committed Phase-63 workbench (aml-substrate@f90bd39 / aml-casework@c6d8401).

- [x] T1 Gating policy + live routing engine: lift the static 500/50 + cleared-% into an explicit `gating_policy`; add a pure `route(confidence, sample_size, policy)`; serve the live routing decision per case. | scope: scripts/serve_workbench.py, scripts/curate_workbench_cases.py | success: `python3 scripts/serve_workbench.py --selftest` passes — `route()` reproduces the committed 129/52/19 funnel from the policy DEFAULTS over the committed cases + the monotonicity invariant (a larger sample never yields a STRICTER gate); `! grep -E 'import (aml_substrate|aml_casework)' scripts/serve_workbench.py scripts/curate_workbench_cases.py` clean | size: M
- [x] T2 The elicitation loop (session-only): a /adjudicate path — record a human disposition → grow the combo's session precedent → recompute confidence → return the re-routed decision; in-memory only, persists nothing; the disposition labeled illustrative. | scope: scripts/serve_workbench.py | success: `serve_workbench.py --selftest` covers the loop (a human-gate case, after adjudications of its combo, re-routes toward auto) + a persists-nothing assertion (no disk write across an /adjudicate cycle) | size: M
- [x] T3 (L) The gating panel UI: visible policy knobs ("chosen, not measured"), per-case live routing decision, the human-gate adjudication action + the loop visualization (sample → confidence → re-route), the funnel re-derived live, always-on badge. | scope: workbench.html | success: `node tests/workbench.test.mjs` gating arc green — routing render + adjudication loop + the live-re-derived funnel + badge + both motion modes + XSS-escape | size: L
- [x] T4 Execute-once + regression + drift guard: run the engine live ONCE over the 200-case slice (the funnel re-derived live + the loop shifts one decision, captured as evidence in the delivery report) + add the gating cases to the suites. | scope: tests/workbench.test.mjs, tests/test_selftests.py | success: `uv run pytest` green (gating selftests added) + `node tests/workbench.test.mjs` green + `python3 scripts/build.py --check all` 8/8 ZERO dist drift + `! grep -E 'import (aml_substrate|aml_casework)' scripts/build.py` | size: M
- [x] T5 Docs + honesty boundary + smoke: the gating-engine section (the policy knobs · the elicitation loop · the §12-routing/§14-illustrative-disposition seam · the executed-once finding) + a smoke-checklist gating entry. | scope: docs/case-workbench.md, tests/smoke-checklist.md | success: `docs/case-workbench.md` has the gating section + the §12/§14 boundary statement (`grep -q` for both); `tests/smoke-checklist.md` has the gating entry | size: S

> Phase 64 — direction gate 2026-06-21 (all_accept:true): the user picked the precedent-confidence GATING
> engine (Phase-63 follow-on #3, "the LFCM elicitation-loop path") at the Step-9 direction question over
> agentic tool-calling (local) + the sibling-rooted C3/C15 alignment. A0 [T0 weakest — the live
> elicitation loop is the TARGET, with a build-time honesty checkpoint: the loop shifts ROUTING via real
> sample-growth while the recorded/auto-applied DISPOSITION stays labeled illustrative + no learns-correct
> claim] · A1 companion-only (the loop is live-stateful → a serve_workbench extension, NOT a 9th ship
> target) · A2 recompute from data already in cases.json (n_precedent stored) → records byte-frozen, no
> substrate re-emit, no sibling import · A3 the Phase-62 §12/§14 boundary (route on §12 confidence,
> dispositions §14 illustrative, NO §14 re-grounding from probe-history) · A4 the engine EXECUTED ONCE
> live over the 200-case slice (the measuring→controlling pivot). Grounded against the committed Phase-63
> workbench (aml-substrate@f90bd39 / aml-casework@c6d8401).
> Abort: if the live loop can't be kept honest over illustrative dispositions (reads as "learns correct
> answers") → scope to display-only batch routing, report don't force (the A0 fallback). Any new ship
> target / dist drift / a sibling import in build.py / a validator loosened to force a fit → STOP-and-surface.

<!-- phase:phase-66-workbench-richness -->
<!-- gate-log:phase-66 direction=approved delivery=accepted -->
<!-- status: T1–T4 ALL [x] — DELIVERED + accepted 2026-06-22 (live execute-once + adversarial pass, 3 honesty flaws caught & fixed) -->

## Phase 66 — Workbench richness: richer slice + OSINT corpus depth + the substrate BO-graph emission handoff brief

Two signal-watch-LOCAL richness wins now + the durable network lever as a sibling brief, so the repos stay
coherent. Recon (signal-watch consume side + aml-substrate generation side, code-verified @9d2e65c) found: the
substrate is single-signal-separable (composition subsumed by network linkage — P16) so MORE cases/typologies/
detectors add visible VOLUME, not detection difficulty; the richness that compounds is the NETWORK (the
generated-but-unprojected RelationshipEdge BO/director graph — the ranked #1 lever); the workbench slice is a
deterministic substrate emit (`--clients 40000 --seed 0 …`, re-runnable as TOOL-USE, gen unchanged from the
f90bd39 pin — VERIFIED). THE GOVERNOR (load-bearing): every richness claim is demo-VISIBLE, ZERO
catch-rate/detection-difficulty/lift number; GATHER stays consistency-not-correctness; single-signal-separable
documented not hidden. Companion-only (workbench/osint NOT ship targets); the honesty seam + always-on badge
UNCHANGED; `--check all` 8/8 ZERO dist drift; build.py NEVER imports aml_substrate/aml_casework (the substrate
emit + casework coverage-measure run as subprocess TOOL-USE, file-contract). LITE. Plan:
articles/phases/phase-66-workbench-richness.md. Ledger: Phase-66 (direction gate 2026-06-21, all_accept:true —
A1 coherence + A2 slice-scope positioned at the gate, A0/A3/A4 by precedent). Grounded against the committed
Phase-65 workbench (signal-watch HEAD 761a446; aml-substrate@9d2e65c / aml-casework@81df91c).

- [x] T1 Richer vendored slice — DONE: re-ran the deterministic substrate emit (verified src/ byte-identical f90bd39..HEAD → same population, pin holds) + raised the curate caps (rich 60→120 / noise 120→180) + ADDED a combo-coverage pass (guarantee ≥1 of every population combo) → re-vendored **294 cases (was 200), all 23 population combos surfaced (was 14)**, 85 4+-cap rich cases (was 60), coverage re-MEASURED **99/294** (was 57/200), gate funnel **189/66/39** (was 129/52/19); exemplars re-tagged (mule Zane Zhao + ambiguous General Trading Co. STABLE; fp_trap→Jin Xu, thin→Khadija Quirke). `curate --selftest` PASS; bundles clean (294, stale deleted); serve_workbench --selftest PASS over the new slice; build.py imports no sibling. Honest: visible VOLUME + full combo spread, NOT detection difficulty. | scope: scripts/curate_workbench_cases.py, data/workbench/** | size: M
- [x] T2 (PRIMARY) Deepen the OSINT corpus + MIRROR the substrate ownership schema — DONE: `data/osint/corpus.json` 9→24 records across 14 subjects (named counterparties / jurisdictions / sanctions / adverse-media); the registry ownership records carry `relationships:[{src,dst,label∈{BENEFICIAL_OWNER/DIRECTOR_OF/OFFICER_OF/CONTROLS/OWNS},ownership_pct}]` MIRRORING the substrate RelationshipEdge (verified schema/graph.py:16-24). `osint_tools.py`: REL_LABELS + the gate vocab/range-checks the relationship (drop-to-None on invalid — the link-granularity lesson), `build_graph` draws DIRECTION-aware src→dst ownership edges, jurisdiction threaded, the stub screens EACH discovered affiliate (so a 2nd-hop sanctioned affiliate is found). Two ownership→sanctions chains fire (Zane Zhao→Crescent Dunes; Management Trading Co.→Silk Road Freight), cross-subject links (Priya Ueda↔Retail Services). workbench.html renders the ownership label + `ownership_pct` (as "N pct", NOT "N%" — the no-% rule). ALSO fixed a latent gate bug: the sentence-bridge guard rejected abbreviation periods ("Co. discloses") → refined to terminator+ws+CAPITAL (a real boundary), org-name records ground while the adversary's stitch still DROPs. `osint_tools --selftest` + `serve_workbench --selftest` green; `node tests/workbench.test.mjs` 103/0 (+3 ownership, the no-% re-check passes "100 pct"); GATHER yields findings on 7 subjects; import-clean. The honesty seam UNCHANGED. | scope: data/osint/corpus.json, scripts/osint_tools.py, workbench.html, tests/workbench.test.mjs | size: M
- [x] T3 The aml-substrate BO-graph EMISSION handoff brief — DONE: `docs/substrate-bo-graph-emission-PLAN-BRIEF.md` (the Phase-55–58 sibling-brief pattern) — the 3-part ask (a `PartyGraphView` projecting the generated-but-unprojected `RelationshipEdge` BENEFICIAL_OWNER/DIRECTOR_OF edges · `related_parties[]` bundle emission with `ownership_pct`, contract v0.2→v0.3 · a NON-tautological C14 BO-disclosure detector) + the contract mapping showing the emitted shape maps 1:1 onto what the Phase-66 OSINT corpus already renders (so the workbench needs no rework when the real graph lands) + the single-signal-separable governor. Pinned @9d2e65c; grep confirms all named sections. Sibling-executed, NOT built here. | scope: docs/substrate-bo-graph-emission-PLAN-BRIEF.md | size: M
- [x] T4 Suites + drift + docs + the governor — DONE: EXECUTED the loop LIVE once over the deepened corpus (mule + General Trading Co. vs Qwen3.6-35B) — which SURFACED a real honesty flaw (the model fabricated an ownership pct [51 vs the record's 75] + flipped the direction) → fixed by RECORD-SOURCING the ownership (gate reads label/pct/direction from the record, not the model; the live re-run then showed the correct 75 pct / Mara→General direction, 0 fabricated). `uv run pytest` 18/18 · `node tests/workbench.test.mjs` 103/0 · `curate`/`osint_tools`/`serve_workbench --selftest` green · `build.py --check all` 8/8 ZERO dist drift · build.py imports no sibling/companion. `docs/case-workbench.md` gained the *Workbench richness* section + the single-signal-separable GOVERNOR (visible-richness-not-detection-difficulty) + the slice numbers updated (294/99/189-66-39); `tests/smoke-checklist.md` gained the Phase-66 entries. | scope: tests/workbench.test.mjs, tests/smoke-checklist.md, docs/case-workbench.md | size: S

> Phase 66 — direction gate 2026-06-21 (all_accept:true): after a two-repo richness recon the user chose the
> TWO signal-watch-LOCAL wins (richer slice + deeper OSINT corpus) + the aml-substrate BO-graph emission brief
> bundled into ONE phase for cross-repo coherence. A0 [T0 weakest — the GOVERNOR]: demo-VISIBLE richness only,
> ZERO detection-difficulty claim (single-signal-separable) → ACCEPT · A1 [chosen]: the OSINT ownership records
> MIRROR the substrate RelationshipEdge schema → ACCEPT (the local win previews the emitted BO graph) · A2
> [chosen]: a deterministic ~320 re-emit + re-curate, OSINT primary → ACCEPT · A3: the BO-graph emission is a
> sibling-executed brief → ACCEPT · A4: companion-only, honesty seam unchanged → ACCEPT.
> Abort: any richness framing reading as detection-difficulty → re-word (the A0 governor). The re-emit drifts
> from the pin population → re-pin honestly. The OSINT schema-mirror can't match RelationshipEdge cleanly → the
> brief documents the delta. Any new ship target / dist drift / a sibling import in build.py / a validator
> loosened to force a fit → STOP-and-surface.

<!-- phase:phase-65-agentic-tool-calling -->
<!-- gate-log:phase-65 direction=approved delivery=accepted -->
<!-- status: T1–T5 ALL [x] — DELIVERED + accepted 2026-06-21 (two adversarial passes, all confirmed findings fixed; live chain fired) -->

## Phase 65 — Agentic tool-calling: the investigator evidence-gathering loop (companion)

Build signal-watch's FIRST multi-step tool-calling agent loop (verified: none exists — every companion is single-shot
`call_llm`). Add a GATHER beat to the workbench arc (between SIGNALS and DECIDE): on a selected investigator case, a
companion agent loop (backend by NAME — Phase-57 §4.5; deterministic-STUB fallback so the demo runs model-free)
proposes a tool → calls a deterministic tool over a COMMITTED SYNTHETIC evidence universe (registry / adverse-media /
counterparty hits indexed by entity name) → each proposed finding is GROUNDED-OR-STRIPPED by the REUSED `news_ground`
gate (the finding's claim must be a substring of the tool's OWN returned text; ungrounded findings DROP) → grounded
tool-evidence extends the case's grounding chain + feeds a `liveGraphLayout` network view. EXECUTE the loop ONCE live
over a marquee exemplar (grounded + dropped evidence captured as delivery evidence — the measuring→controlling /
execute-once pattern). THE HONESTY SEAM (load-bearing, A0): tools query COMMITTED SYNTHETIC data, the agent proposes /
the deterministic gate disposes, the consistency-not-correctness limit is surfaced honestly (badge on; ZERO
catch-rate/detection-lift number) — it shows tool-evidence EXTENDING the grounding chain the same way every other
evidence modality does ("grounding is universal, the substrate varies"), NEVER that the synthetic findings are true.
LITE; companion-only (a `serve_workbench.py` + `workbench.html` extension, NOT a 9th ship target — the chain.html /
Phase-56 + Phase-64 precedent); the loop is session-only / persists nothing (reuse `liveGraphLayout`, do NOT import the
DuckDB `news_store`); 8 ship dists byte-frozen; `--check all` 8/8 ZERO dist drift; build.py never imports the siblings.
Plan: articles/phases/phase-65-agentic-tool-calling.md. Ledger: Phase-65 (direction gate 2026-06-21, all_accept:true —
A0/A2/A4 positioned explicitly, A1/A3 by precedent; warned + restated per gate protocol). Grounded against the
committed Phase-64 workbench (signal-watch HEAD 0ee4489; aml-substrate@9d2e65c / aml-casework@81df91c HEADs verified
live but NOT consumed — companion-only, no sibling import).

- [x] T1 Synthetic evidence corpus + deterministic tool layer + shape validator — DONE: `data/osint/corpus.json` (committed synthetic registry/adverse-media/sanctions, head-of-file disclaimer) + `scripts/osint_tools.py` (the new companion module — stdlib + news_ground only): `validate_osint_corpus` (closed kinds, globally-unique ids, non-empty text, banned-metric-token sweep, note-present) + 3 deterministic tools (`run_tool` exact-normname) + `build_index`. Validated fail-loud at serve_workbench load. `serve_workbench.py --selftest` + `osint_tools.py --selftest` green; `! grep -E 'import (aml_substrate|aml_casework|news_store)'` clean. (Adversary-hardened: validator rejects banned-token text + duplicate ids + missing note.) | scope: data/osint/**, scripts/osint_tools.py, scripts/serve_workbench.py | size: M
- [x] T2 (L) The multi-step tool-calling agent loop + the grounding seam — DONE: `osint_tools.gather()` — signal-watch's FIRST hand-rolled propose/parse agent loop (StubPlanner offline / LivePlanner over `call_openai`); `gate_finding` is the FULL grounding conjunction (record_id binds to the immediately-preceding tool call · `locate_span` + normalized-quote floor ≥12 + sentence-bridge guard + requote-to-located + synthesis distinct + entity/link subject-or-grounded — every adversary-found bypass closed); capped + no-progress guard; fail-CLOSED transport; `/gather` NDJSON endpoint in serve_workbench (single-flight). `serve_workbench.py --selftest` covers the stubbed loop end-to-end (grounded KEPT + planted-ungrounded DROPPED + chain reaches sanctions + cap + persists-nothing on cases.json AND corpus.json + the §4.5 cred-leak over ALL NDJSON stages). | scope: scripts/osint_tools.py, scripts/serve_workbench.py | size: L
- [x] T3 The GATHER beat UI (Beat 2.5) — DONE: the gather panel inserted after the audit-walk, before decide; "Gather evidence (synthetic OSINT corpus)" button + an always-visible beat-local SYNTHETIC-provenance string; per-tool stage-completion reveal (never a token stream); each grounded finding shows the GROUNDED quote as EVIDENCE beside a SUBORDINATE illustrative-synthesis (the .illus precedent, labeled "illustrative reading — not verified"); the gate's rejections render WITH reason in a rejected style; the network reuses `liveGraphLayout` (ported verbatim, pure JS) labeled "authored over synthetic records, not discovered", edge-click reveals the grounded evidence; honest-empty payoff for the fp-trap. `node tests/workbench.test.mjs` 100/100 (+17 gather; XSS-escape, NO %/lift vocab re-checked over the gather HTML, both motion modes). | scope: workbench.html, tests/workbench.test.mjs | size: M
- [x] T4 Execute-once + regression + drift guard — DONE: the loop EXECUTED LIVE ONCE over the mule (CASE-P-0002174 "Zane Zhao") against the local Qwen3.6-35B (/v1 @127.0.0.1:8080). The CHAIN fired live: lookup_registry(Zane Zhao) → discovered Crescent Dunes Trading FZE → screen_sanctions(Crescent Dunes) → the OFAC hit → screen_adverse_media(Crescent Dunes) → honest empty. **2 grounded, 0 dropped, 0 FABRICATED** — the grounding gate held live. A design fix surfaced from the FIRST live run (the model couldn't chain because the planner passed only {tool,n_records} history — it couldn't see the discovered entity): threaded discovered-entity context into LivePlanner; re-ran; the chain fired (transcript `.dev-wiki/tmp/ph65-execute-once-live.json`). `osint_tools.py` added to PY_SELFTESTS. `uv run pytest` 18 passed; `node tests/workbench.test.mjs` 100/0; `build.py --check all` 8/8 ZERO dist drift; build.py imports no sibling/companion. | scope: tests/workbench.test.mjs, tests/test_selftests.py, scripts/osint_tools.py | size: M
- [x] T5 Docs + honesty boundary + smoke — DONE: `docs/case-workbench.md` gained "The agentic evidence-gathering loop (Phase 65)" (the chained discovery · the CONSISTENCY-not-correctness honesty seam · the closed bypasses · tools/transport · the executed-once live finding) + the deferred-list update + a "Built in Phase 65" footer; `tests/smoke-checklist.md` gained the Phase-65 GATHER section (4 selftest/test/isolation checks + the optional live walk). Greps confirm both. | scope: docs/case-workbench.md, tests/smoke-checklist.md | size: S

> Phase 65 — direction gate 2026-06-21 (all_accept:true): the user picked AGENTIC TOOL-CALLING (the one remaining
> signal-watch-LOCAL Phase-63 follow-on) at the Step-9 direction question over (B) the sibling-rooted C3/C15 alignment
> [highest-value but un-drivable from this repo] and (C) a consolidation/pitch-polish phase. A0 [T0 weakest — the
> honesty seam]: tools query COMMITTED SYNTHETIC data + every finding GROUNDED-OR-STRIPPED via the reused news_ground
> gate; the consistency-not-correctness limit is bounded (committed corpus, not model-generated) + surfaced honestly →
> ACCEPT (synthetic tools, grounded-or-stripped) · A1 companion-only (serve_workbench + workbench.html, NOT a 9th ship
> target) → ACCEPT by precedent · A2 the agent loop is BUILD-NEW but BOUNDED (fixed tools, capped iterations, stub
> fallback) → ACCEPT (full live loop) · A3 the tool corpus is committed synthetic, shape-validated → ACCEPT by
> precedent · A4 reuse liveGraphLayout (NOT news_store) + EXECUTE the loop ONCE live → ACCEPT (execute-once).
> Abort: if the live tool-evidence can't be kept honest over synthetic data (reads as "real OSINT" / a claim can't be
> grounded to its synthetic source) → fall back to network-ER over the existing committed edges only, report don't
> force (the A0 fallback). The live loop needs real debugging beyond creds → surface as a FINDING, ship the stub. Any
> new ship target / dist drift / a sibling import in build.py / a validator loosened to force a fit → STOP-and-surface.

<!-- phase:phase-63-investigator-case-workbench -->
<!-- gate-log:phase-63 direction=approved delivery=accepted -->
<!-- status: T1–T5 ALL [x] — DELIVERED + accepted 2026-06-21 (committed a3fde1e; gate-state follows git-state) -->

## Phase 63 — Investigator Case Workbench (capstone sales-pitch demo)

> RE-SCOPED 2026-06-21 (post direction-gate): the gate originally closed all-accept on a CURATED-4 / local-synthetic
> / self-contained set; the user then chose option (B) — feed the REAL (synthetic) substrate alert POPULATION. This
> was VERIFIED feasible this session: aml-substrate@f90bd39 already emits the rich investigator clutter (contract v0.2
> PartyView: risk_rating/cdd_level/pep_tier/sanctions+adverse-media flags/occupation/source_of_funds/source_of_wealth/
> nationality/residency/NAICS/expected_monthly_volume+count, label-leak-proof) at POPULATION scale (`--emergence
> --monitor --emit-evidence --emit-screening`, ~500–2,000 deterministic synthetic bundles per 50k-client run) → NO
> substrate precursor phase is needed. Two build parameters chosen: a BOUNDED slice (~100–300 cases), and REAL txn
> counterparty edges only (the formal ownership/beneficial-owner graph is a substrate follow-on).

A companion-served INVESTIGATOR CASE WORKBENCH (served by `serve_chain.py`; NOT a build/dist target → lite ceremony
holds; the chain.html / Phase-56 precedent) over a REAL (synthetic) substrate alert POPULATION. The substrate emit is
run as TOOL-USE (file-contract; build.py NEVER imports aml_substrate/aml_casework — the Phase-62 probe-history-consume
pattern); a deterministic BOUNDED SLICE (~100–300 cases) is vendored into `data/workbench/` pinned to
aml-substrate@f90bd39 (`meta.synthetic:true` → "no real data" holds). THREE beats: (1) CLUTTER — a QUEUE of the real
population + a per-case dense investigator page (the real PartyView KYC profile + cross-account aggregation over
subject.account_ids + txn summaries/details + REAL counterparty edges); offline/model-free; a clearly-SYNTHETIC
display identity is laid over the real grounded KYC (the substrate omits name/DOB by privacy design). (2) SIGNALS-ON —
a presenter toggle overlays the case's grounded signals + composes the risk picture (grounding/clarity/corroboration;
ZERO catch-rate/precision number — the substrate detection-lift triple-null governs; badge always on). (3) LIVE FINALE
— the workbench "decide" action wires the EXISTING serve_chain → casework consume on a SELECTED real case (default
Claude backend, configurable openai/opencode/stub, deterministic-stub fallback; NDJSON stage reveal, stage-completion
never a token stream). The 4 named exemplars (textbook mule / false-positive trap / thin single-signal /
ambiguous-medium) are tagged WITHIN the real population. An HONEST COVERAGE STATISTIC ("N of M cases ground
end-to-end now; the rest is the visible frontier") is computed over the slice — REAL/measured. Each case shows an
illustrative precedent-confidence badge anchored to REAL probe-history firing frequency (the 4,966-firing history) but
DISPOSITIONS stay label-blind ILLUSTRATIVE (the Phase-62 grounded-detection / illustrative-disposition split).
DEFERRED to follow-on phases: the agentic tool-calling (OSINT / counterparty / network-ER), the precedent-confidence
GATING engine, and the substrate ownership-graph emission (for a richer network view).
ARCHITECTURE INVARIANTS: companion-served, off by default, scripted fallback; NO keys in the frontend (creds
server-side, browser sends a backend NAME only — Phase-57 §4.5); synthetic data only, nothing real leaves; build.py
NEVER imports aml_substrate/aml_casework; `--check all` stays 8/8 with ZERO dist drift (companion-only, no build
target). Honest value framing: clutter→clarity = grounding/defensibility/corroboration, NOT a higher catch rate
(combo-strength = richer converging defensible evidence). LITE; companion-served, NO new build target. Plan:
articles/phases/phase-63-investigator-case-workbench.md. Ledger: Phase-63 (direction gate 2026-06-21, all_accept:true;
RE-SCOPED post-gate to option B — A0 flipped → substrate-grounded real population VERIFIED feasible @f90bd39 · A1'
real txn edges only · A2' bounded vendored slice ~100–300 · A3' coverage REAL / dispositions illustrative). Grounded
against aml-substrate@f90bd39 + aml-casework@c6d8401 + corpus@472b44e.

- [x] T1 Vendor a real substrate population slice + curate exemplars — DONE: 200-case deterministic slice vendored into `data/workbench/` (pinned aml-substrate@f90bd39, `meta.synthetic`); `scripts/curate_workbench_cases.py` validates the bundles, tags the 4 exemplars (mule CASE-P-0002174 / fp_trap CASE-P-0018660 / thin CASE-P-0008468 / ambiguous CASE-O-000008), computes the precedent-confidence badge over REAL firing frequency, and MEASURES the coverage statistic by running casework's stub over all 200 bundles (grounds_e2e per case → 57/200). `--selftest` green. NOTE: companion is a NEW `serve_workbench.py` (reuses serve_chain's consume/verify/audit primitives), not a serve_chain route.
- [x] T2 Cluttered investigator QUEUE + per-case view (Beat 1) — DONE: `workbench.html` renders the 200-case queue + a per-case dense clutter view (real PartyView KYC, cross-account aggregation over subject.account_ids, txn summaries+details, real counterparty edges); companion-served via the new `scripts/serve_workbench.py`; offline/model-free; badge always on.
- [x] T3 "Turn on signals" reveal (Beat 2) — DONE: presenter toggle overlays the grounded signals + composes the risk picture (grounding/clarity/corroboration; ZERO quantitative-lift number); both motion modes; badge on.
- [x] T4 (L) Wire + run the live finale + coverage statistic (Beat 3) — DONE: the "decide" action wires serve_workbench → casework consume on a selected real case (default Claude, configurable, stub fallback; NDJSON stage reveal). The MEASURED coverage statistic (57/200 ground end-to-end; the rest = the visible frontier) is displayed. **The T4 checkpoint FIRED productively** — measured a real cross-pillar finding (composed cases fail on the substrate↔casework C3/C15 replay divergence; ~28% of composed cases sign) → EMBRACED as the FAIL-CLOSED defensibility climax (the gate refuses → escalate-to-human, never loosening). The verifier was never loosened (abort rule held); user accepted the framing.
- [x] T5 Confidence badge + tests + docs + smoke + regate — DONE: per-case precedent-confidence badge (real firing frequency; "chosen, not measured" disposition; gate funnel 129 auto-clear / 52 review / 19 human-gate); `tests/workbench.test.mjs` (61 assertions) green; `docs/case-workbench.md` written (walkthrough + honesty framing + the deferred follow-ons incl. ownership-graph emission); smoke-checklist entry added. `build.py --check all` 8/8 ZERO dist drift. PLUS (USER OVERRIDE, mid-phase): a thin pytest wrapper — `pyproject.toml` (package=false) + `tests/test_selftests.py` (17 parametrized cases) + `uv.lock` → `uv run pytest` runs the whole suite; dep-free paths stay the source of truth.

### Phase 63 — discovered follow-on tasks (deferred to future phases — NOT this phase)
- [ ] (FOLLOW-ON) C3/C15 cross-pillar contract alignment — fan-IN C3 RESOLVED (casework Phase 19 `_c3_fan_in`, consumed Phase 79: Lakeshore CASE-B signs). The C15 shell-threshold divergence + the broader composed-case grounding (~28% of composed cases sign) REMAIN sibling-rooted (a substrate/casework phase).
- [x] (FOLLOW-ON) Agentic tool-calling phase — DONE (Phase 65: GATHER beat over the synthetic OSINT corpus, companion-only; [[phase-65-agentic-gather-built]]).
- [x] (FOLLOW-ON) Precedent-confidence GATING engine — DONE (Phase 64: the live gating + elicitation loop; superseded by Phase 69's evidence-sufficiency control; [[phase-64-gating-engine-built]]).
- [x] (FOLLOW-ON) Substrate ownership/beneficial-owner graph emission — DONE (substrate Phase 25 emitted `related_parties[]`; signal-watch Phase 71 consumes it as the case network `boGraphHTML`). Richer graph/Kuzu still sibling-rooted (named in the standards).
- [ ] (FOLLOW-ON, NEW Phase 80) Substrate P35+ determination-signal frontier — TF case slice · broader C7 · org-name sanctions screening · open-data Stage 2/3 (`docs/substrate-p35-determination-signals-PLAN-BRIEF.md`, pinned @1f5901e). C1/ML-A6 is a documented measured null (not an ask). A substrate sibling-repo phase.

> Phase 63 — direction gate 2026-06-21 (all_accept:true), RE-SCOPED post-gate to option (B): the user chose to feed the REAL (synthetic) substrate alert POPULATION over the curated-4 hybrid. A0 FLIPPED → substrate-grounded real population (A0's "verify feasible" resolved THIS session — aml-substrate@f90bd39 emits the rich v0.2-PartyView investigator clutter at population scale → NO precursor phase). A1' real txn counterparty edges only for the network view (ownership-graph emission deferred to a substrate follow-on). A2' a bounded vendored slice ~100–300 cases (the 4 shapes are exemplars tagged WITHIN the real population). A3' the coverage statistic is REAL/measured; precedent-confidence is anchored to real firing frequency but DISPOSITIONS stay label-blind illustrative (the Phase-62 split). Live finale = wiring + the Phase-57-backend checkpoint; honest value framing = grounding/clarity/defensibility, NOT catch-rate. 3 emission gaps handled in signal-watch (NOT a substrate change): synthetic display identity over real KYC; cross-account display aggregation; the txn-edge network. Grounded against aml-substrate@f90bd39 + aml-casework@c6d8401 + corpus@472b44e.
> Abort: any dist drifts / build.py imports aml_substrate or aml_casework → STOP-and-surface. The substrate emit isn't as rich/deterministic as verified → STOP at the T1 emit-checkpoint, fall back to a bounded curated set (do NOT hand-fabricate at population scale). The Phase-57 live backend needs real debugging beyond creds → surface as a FINDING (do not silently absorb). A keys/token reaches the browser → out of bounds (Phase-57 §4.5).

<!-- phase:phase-62-grounded-probe-history-consume -->
<!-- gate-log:phase-62 direction=approved delivery=accepted -->

## Phase 62 — Grounded probe-history consume (§12) + §14-frozen boundary + P22 pin re-ground

Consume aml-substrate's P22 "grounded probe-history" into signal-watch's §12 non-ship measurement layer
(probe_history_stats.py). Substrate P22 (loop-closure) projects monitor/compose.Dossier into a grounded
`alert-history.json` (conformance-validated against a VENDORED copy of signal-watch's OWN probe_history_stats.py
@58925a8); it REPLACES the SYNTHETIC Phase-48 probe-history fixture as the §12 curate source. Substrate output is
itself synthetic (meta.synthetic:true) → "no real data" holds. The C-code→TM-### namespace map is now
SHIP-LOAD-BEARING (the §12 silent_rules metric). The §14 triage-console unfreeze (planned as a sanctioned
USER-OVERRIDE) was **STOOD DOWN on evidence at the T4 pause-checkpoint** — the substrate's label-blind probe-history
is the RIGHT source for §12 (firing/disposition MEASUREMENT) but the WRONG source for §14 (the triage console needs
adjudicable FACT PATTERNS the substrate doesn't emit; curate couples via only 5 metadata fields on 7/20 scenarios,
and the marquee TM-104 pair is C20 — no substrate detector). The user accepted the boundary → **dist/triage stays
BYTE-FROZEN; NO re-curation**. The ONLY dist change is the launcher pin cascade (dist/index.html, 1 line — the
Phase-60 Option-A pattern); the other 7 dists incl. dist/triage byte-frozen; build.py NEVER imports the substrate
(tool-use produces the file, build reads committed data/ only). Honesty split: the alert/entity/cited-evidence side
becomes GROUNDED in real substrate detection output, but DISPOSITIONS stay label-blind ILLUSTRATIVE (never ground
truth) — every disposition-derived metric tagged "[over illustrative dispositions]".
LITE; the only ship change is the launcher pin cascade (dist/index.html, 1 line) — the other 7 dists byte-frozen.
Plan: articles/phases/phase-62-grounded-probe-history-consume.md. Ledger: Phase-62 (all_accept:false; A0
verify-first = T0 weakest; A1 reject→revised-accept w/ checkpoint — CHECKPOINT FIRED → §14 stood down; A3
don't-know→delivery-checkpoint). Grounded against aml-substrate@ae98924 + aml-casework@c6d8401 + corpus@472b44e.
Gate 2026-06-20 (direction) → delivered + accepted 2026-06-21.

- [x] T1 Produce + pin the grounded probe-history | scope: data/probe-history/**, docs/** | success: VERIFY P22 committed in aml-substrate (`git -C /Users/jwang/aml-substrate` — PAUSE+ASK the user to commit it if still uncommitted, do NOT consume an uncommitted projector); run substrate's COMMITTED `--probe-history` projector (sanctioned tool-use, file-contract output — NOT lifecycle-driving) → a grounded `alert-history.json` under data/probe-history/ (labeled grounded; pin the substrate P22 HEAD inline in a provenance note); grounded alert-history.json EXISTS + is DETERMINISTIC (re-run byte-identical); validates against the probe-history schema
- [x] T2 C-code→TM-### namespace map (ship-load-bearing) | scope: data/probe-history/**, data/**, scripts/** | success: a committed map (substrate capability C-code → signal-watch TM-### rule id), grounded in data/capability-taxonomy.json; closed-vocab validated; EVERY C-code in the grounded history resolves to a TM id OR an explicit honest-null (surfaced, never silently dropped); a `--selftest`/assert covers it
- [x] T3 §12 non-ship measurement consume | scope: scripts/probe_history_stats.py, docs/** | success: re-point probe_history_stats.py at the grounded history (5/6 Role-2 metrics as-is; silent_rules via the T2 map); the stats run PRINTS all 6 Role-2 metrics over the grounded file; the A3 honesty framing (grounded detection / illustrative dispositions) DRAFTED explicitly; NO overclaim language
- [x] T4 §14 triage re-curation → CHECKPOINT FIRED → boundary accepted (§14 stays frozen) | scope: scripts/curate_triage_scenarios.py, data/triage/** | RESOLUTION: read curate firsthand — the §14 scenarios couple to alert-history via only 5 metadata fields on 7/20 scenarios; everything substantive (panels/TM-pairing/divergent-pair/controls) is hand-authored; the substrate's label-blind alerts carry NO fact pattern + the marquee TM-104 pair is C20 (no substrate detector). So the grounded history is the WRONG source for §14 (it needs adjudicable fact patterns) → PAUSED + REPORTED → user chose "accept the boundary, keep §14 frozen" → NO re-curation, dist/triage byte-frozen, documented in docs/probe-history.md + substrate-pin.json + CLAUDE.md. ORIGINAL: READ curate_triage_scenarios.py FIRST; confirm strata coupling; CHECKPOINT — if the grounded history doesn't map cleanly into the §14 strata OR breaks build-boundary validation, PAUSE + REPORT (do NOT redesign the console to force the fit); re-run curate against the grounded probe-history → regenerate data/triage/scenarios.json (HISTORY-derived strata [fired-signal + below-the-line] re-grounded; novel+random strata BYTE-STABLE); the §14 grammar + decisions-not-correctness + no-real-data preserved; curate `--selftest` green; build-boundary validation passes
- [x] T5 Exit verification (§14 frozen → NO rebuild; the sanctioned dist change is the launcher pin cascade, NOT triage) | scope: dist/**, scripts/build.py | success: `build.py --check all` 8/8; `git diff --stat HEAD -- dist/` shows ONLY dist/index.html (1 line — the Phase-60 Option-A launcher grounding_heads cascade; dist/triage + 6 others BYTE-FROZEN); `node tests/triage-console.test.mjs` green (console UNCHANGED); `! grep import aml_substrate` clean in build.py + the 3 cross-pillar scripts; probe_history_stats synthetic byte-identical + --selftest + --grounded green
- [x] T6 Re-ground cross-pillar pins + document | scope: scripts/signal_coverage_map.py, scripts/e2e_chain_check.py, docs/**, .claude/rules/**, HANDOFF.md, CLAUDE.md | success: move the coverage-map / e2e-chain pins to substrate@<P22 commit> (absorb the no-op coverage re-ground); `signal_coverage_map.py --check` byte-identical (reachable-now still 171) + `--selftest` green; `e2e_chain_check.py --selftest` green; document ZERO coverage tier movement + the grounded provenance + the sanctioned-unfreeze rationale (IN-PLACE edits to active-phase.md / HANDOFF / CLAUDE.md current-state — replace facts in place, NO per-phase bullets)

> Phase 62 — direction gate 2026-06-20 (all_accept:false): the user chose to consume substrate P22's grounded probe-history END-TO-END. A0 [T0 weakest] verify-first the substrate P22 committed HEAD (pause+ask if uncommitted) — ACCEPT. A1 the end-to-end dist/triage unfreeze REJECT→REVISED-ACCEPT (sanctioned USER-OVERRIDE, the Phase-60/61 precedent) with a built-in CHECKPOINT (no clean strata map / broken validation → PAUSE+report, do NOT force the fit). A3 honesty framing (grounded detection / illustrative dispositions) DON'T-KNOW → routed to the DELIVERY-gate checkpoint (the user checks it before anything is called "grounded"). A2 the C-code→TM-### map is ship-load-bearing + grounded/null-honest. Grounded against aml-substrate@<P22, pinned at T1> + aml-casework@c6d8401 + corpus@472b44e.
> Abort: any of the OTHER 7 dists drift / a build.py substrate import → STOP and surface (only dist/triage may move — the sanctioned unfreeze). The substrate P22 projector still uncommitted at T1 → PAUSE + ask the user to commit it, do NOT consume an uncommitted projector. The grounded history doesn't map cleanly into the §14 strata / breaks build-boundary validation → PAUSE + report (A1 checkpoint), do NOT redesign the console to force the fit. The honesty framing reads as claiming grounded DISPOSITIONS → re-word at the delivery checkpoint before commit (A3). A C-code resolves to nothing AND the null is not surfaced → out of bounds (A2). A validator looks like it needs loosening → fix the data/design, never the check.

<!-- phase:phase-61-blueprint-implementation-review -->
<!-- gate-log:phase-61 direction=approved delivery=accepted -->

## Phase 61 — Blueprint review against what's been implemented (+ batched cross-pillar re-ground; deep blueprint true-up)

docs/program-blueprint.md is a Phase-47/48 DESIGN snapshot whose §3 built-vs-design-stage partition is materially
STALE: the program split into pillars (P50) and built aml-substrate P1–20 (@34400e2) + aml-casework P1–12
(@c6d8401) + signal-watch's coverage map / e2e chain / C/D control. The four §3 "(design-stage)" rows (txn
monitoring · case investigation · SAR/STR narrative · LFCM assist) now carry substantial committed SIBLING
implementation. The user chose C — report + deep blueprint revision — batched the cross-pillar re-ground (coverage
map @P18 + e2e pins → substrate@P20/casework@P12, verify-first), and re-sync blueprint-report.html revised sections.
Re-opens the FROZEN-set blueprint (USER OVERRIDE). LITE, NON-ship EXCEPT the sanctioned launcher re-ground (the
Phase-60 Option-A cascade). Plan: articles/phases/phase-61-blueprint-implementation-review.md. Ledger: Phase-61
(all_accept:true; A0 verify-first = T0 weakest). Grounded against aml-substrate@34400e2 + aml-casework@c6d8401 +
corpus@472b44e (HEADs verified LIVE via git log). Gate 2026-06-20.

- [x] T1 The audit (fan-out) → the committed reconciliation report | scope: docs/blueprint-implementation-review.md | success: AUDIT §1–§15 against committed artifacts across signal-watch + aml-substrate@34400e2 + aml-casework@c6d8401 (file-contract, NO sibling import) → adversarially VERIFY every "now built" claim at its pinned HEAD → docs/blueprint-implementation-review.md exists with a per-§ status using the THREE-TIER vocab (demo-built [committed signal-watch artifact] / pillar-build-synthetic [committed sibling artifact, synthetic/probe-scale] / design-stage [no implementation]); every built/pillar-built status NAMES the verified artifact + HEAD; both-direction DRIFT enumerated (blueprint-stale-vs-built AND impl-diverged-from-design); a re-grounded next-frontier ranking; HEADs pinned inline; always-on illustrative framing | size: L
- [x] T2 Batched cross-pillar re-ground (verify-first, NO-OP-expected) | scope: data/coverage-map/**, scripts/signal_coverage_map.py (read-only verify + selftest goldens), scripts/e2e_chain_check.py, scripts/e2e/** | success: re-ground signal_coverage_map.py's pin + e2e_chain_check.py GROUNDING_HEADS to substrate@34400e2 / casework@c6d8401; run the map + DIFF tiers — confirm ZERO tier movement (reachable-now stays 171; substrate P19 C14-null confirmed, P20 added no screening detector) BEFORE re-freeze; coverage.json re-frozen → `--check` byte-identical + `--selftest` green (any golden that moves with the pin re-grounded as a regression anchor ONLY, NEVER a loosening); e2e `--selftest` green + a `--real` run recorded honestly (CONNECTED on the vendored C8 bundle, or a documented GAP); `! grep -nE "import aml_substrate|import aml_casework" scripts/signal_coverage_map.py scripts/e2e_chain_check.py`; build.py never imports either. ABORT: any non-zero tier movement / an e2e GAP → STOP-and-surface, do NOT silently re-freeze | size: M
- [x] T3 The verified-gated blueprint deep revision (§3 markers + §8/§11/§13 prose) | scope: docs/program-blueprint.md | success: apply ONLY the edits T1 VERIFIED — a three-tier status legend added; §3 table built/design-stage markers → the three-tier vocab with pinned cross-repo artifact refs; §8 deferred-rows that are now partially built re-dispositioned honestly; §11 roadmap re-sequenced (executed items marked with the sibling phase that did them); §13 LFCM-embryo claims updated (the coverage map / e2e chain / corpus-redundancy / composition-retired findings folded in); every changed line traces to a T1-verified finding; NO overclaim (a partial stays "partial/pillar-scale", never "(built)"); "Status: DESIGN" framing + the §15 demo-charter non-negotiables PRESERVED; HEADs pinned inline; `python3 scripts/build.py --check all` 8/8 (blueprint not a build target) | size: M
- [x] T4 Re-sync blueprint-report.html revised sections + consistency check | scope: docs/blueprint-report.html | success: the revised §3/§8/§11/§13 sections of the HTML render synced to match the md; a md↔html consistency note/check added; the HTML opens offline; DESIGN-labeled framing intact; `python3 scripts/build.py --check all` 8/8 | size: S
- [x] T5 Exit verification | scope: (verification only — no edits) | success: `python3 scripts/build.py --check all` → 8/8; `git diff --stat HEAD -- dist/` shows ONLY dist/index.html (the sanctioned launcher re-ground, ≤2 lines — the Phase-60 Option-A pattern); regression green (`signal_coverage_map.py --check` + `--selftest`; `e2e_chain_check.py --selftest`); `! grep -nE "import aml_substrate|import aml_casework" scripts/signal_coverage_map.py scripts/e2e_chain_check.py`; `git status` shows no unexpected ship-artifact changes (only docs/**, data/coverage-map/**, scripts/e2e*/**, dist/index.html, .dev-wiki/**, .claude/rules/**) | size: S

> Phase 61 — T1–T5 all [x] (DELIVERED 2026-06-20, user accepted delivery + chose "switch to an aml-substrate session" for the next frontier). VERIFIED at delivery (independent of the audit fan-out): `build.py --check all` 8/8 byte-identical; `signal_coverage_map.py --check` byte-identical (523 signals, reachable-now **171** held) + `--selftest` green; `e2e_chain_check.py --selftest` green; `git diff --stat HEAD -- dist/` = ONLY dist/index.html (1 line, the sanctioned launcher grounding_heads cascade — Phase-60 Option-A); `! grep import aml_substrate|aml_casework` clean. **The batched re-ground is genuinely ZERO tier movement**: `git diff 9c75c03..34400e2` (substrate P19→P20) touched NO detector/view — only `validate/composition.py` (the stress-bench), a 51-line `monitor/signal_ref.py` referential helper, and 2 measurement probes (kyc_emergence/composition_redundancy) → no new screening detector → coverage.json diff = the single `aml_substrate: 9c75c03→34400e2` pin line. T1 review (`docs/blueprint-implementation-review.md`, 96-agent fan-out): the §3 binary built/design-stage was OUTGROWN — 4 "(design-stage)" rows (TM/case-investigation/SAR-STR/LFCM-assist) now carry committed SIBLING code at pillar-build-synthetic scale (real fail-closed code, synthetic data, NOT deployed) → the blueprint was UNDER-claiming; 8 blueprint-stale + 6 impl-diverged items enumerated (composition detection-lift RETIRED post-triple-null; §9 row-2/row-3 over-claims refuted; §11 /intel/ + REQUOTE-RETRY over-stated as committed). T3 blueprint true-up landed: three-tier legend + §3 markers + §7/§8/§11/§13 prose, `Status: DESIGN` + the §15 demo-charter PRESERVED, HEADs pinned. T4 blueprint-report.html re-synced. HONEST FRONTIER (T1 §7, confirmed): the coverage-map reachable-now lever is near-CEILING from signal-watch's side (needs-detector exhausted at 0); the next real rise is a SIBLING-rooted aml-substrate emergence/§14-M-layer phase (undrivable from a signal-watch session) → user switched repos.
> Phase 61 — direction gate 2026-06-20 (all_accept:true): C deliverable [report + deep blueprint revision] + A1 batched cross-pillar re-ground + A3 re-sync HTML. A0 [T0 weakest] verify-first gates every blueprint edit + a THIRD honest status (pillar-build/synthetic-scale) where binary built/design-stage overclaims — ACCEPT. A2 re-open sanctioned (USER OVERRIDE), A4 non-ship except the sanctioned launcher cascade — accepted by construction. Grounded against aml-substrate@34400e2 + aml-casework@c6d8401 + corpus@472b44e.
> Abort: any OTHER dist drifts / a non-launcher ship artifact touched / the blueprint becomes a build target → STOP and surface. The batched re-ground diff ≠ ZERO tier movement → STOP, do NOT re-freeze (surface as a finding). A blueprint edit that OVERCLAIMS (a partial stamped "(built)") → out of bounds (A0). Sibling import → out of bounds. A validator looks like it needs loosening → fix the data/design, never the check.

<!-- phase:phase-60-consume-landed-sibling-halves -->
<!-- gate-log:phase-60 direction=approved delivery=accepted -->

## Phase 60 — Consume the landed sibling halves (substrate P16-18 + casework P10-12): the C7 reachable-now rise + the real e2e chain

A cross-pillar review (code-verified this session) found BOTH siblings moved 3 phases past the Phase-59
pins: aml-substrate@9c75c03 [Phase 18 — §13 fourth-null closure · party-bearing bundle + `--screen` · emits
the real C8 party-bearing v0.2 evidence bundle, sample CASE-P-0000251.json]; aml-casework@c6d8401 [Phase 12
— C7 assertion (P10) · C8 assertion (P11) · C14 party-leaf assertion (P12); C26 UNREGISTERED, honest null].
Phase 59 A2 said reachable-now rises only when a party-bearing emission (substrate) + the paired
grounding_replay assertions (casework) BOTH land — they now have. The VERIFIED consequence of the 3-way AND
(has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges"): only C7 moves (the one screening cap
with behavior_emergence=="emerges") → ALL 78 of its buildable signals become reachable-now (62 direct + 16
proxy; needs-detector 62→0 exhausted + 16 leave needs-behavior(15)/needs-view-exposure(1)) → reachable-now
93→171 (+78, the first REAL rise — the C7 win banked for a combined consume). C8/C14/C26 do NOT move
(behavior_emergence data-only/absent — the binding gap is emergent behavior, NOT the wiring that just
completed). The user chose the COMBINED scope (coverage-map consume + the real e2e chain on substrate's C8
bundle → casework SAR) over the recommended coverage-only focus — a deliberate scale-up — and "verify first,
then freeze" on the C7 +78.
LITE, NON-ship; --check all 8/8. Plan: articles/phases/phase-60-consume-landed-sibling-halves.md. Ledger:
Phase-60 (all_accept:true; A0=T0 weakest, verify-first). Grounded against aml-substrate@9c75c03 +
aml-casework@c6d8401 + corpus@472b44e. Gate 2026-06-19.

- [x] T1 Re-ground the coverage pin to the current sibling HEADs + VERIFY the C7 +78 before any freeze | scope: data/coverage-map/substrate-pin.json, scripts/signal_coverage_map.py (read-only verify), tests/ | success: substrate-pin.json re-grounded to aml-substrate@9c75c03 + aml-casework@c6d8401 — code-verified: C7 `has_casework_assertion` false→true (casework P10 `_screen_c7_peer_anomaly` registered + green); C8/C14 `has_casework_assertion` also true now (P11/P12) but `behavior_emergence` stays data-only; C26 stays unasserted/absent; substrate P16-18 added NO new detectors and did NOT shift C7's emission measurement; corpus unchanged @472b44e — the pin's `grounding_heads` + `regrounding_note` record the new HEADs; running `python3 scripts/signal_coverage_map.py` (NO freeze) + diffing tier counts shows EXACTLY reachable-now 93→171 (all 78 C7 buildable signals flip — 62 direct + 16 proxy; needs-detector 62→0, needs-view-exposure 70→69, needs-behavior 296→281; every moved signal is C7) with C8/C14/C26 signals UNMOVED; the diff documented in the phase article BEFORE T2 freezes. ABORT: if the delta is anything other than a clean C7-only +78 (or the C7 assertion isn't actually green at c6d8401) → STOP-and-surface, do NOT proceed to T2 | size: M
- [x] T2 Re-freeze coverage.json + the gate | scope: data/coverage-map/coverage.json, scripts/signal_coverage_map.py, tests/ | success: `python3 scripts/signal_coverage_map.py --freeze` then `--check` byte-identical (523 signals, reachable-now 171); `--selftest` green — any golden that moves with the C7 landing re-grounded to the verified new reality as a REGRESSION ANCHOR for the landing, NEVER a loosening (structural-integrity tests untouched); `! grep -nE "import aml_substrate|import aml_casework" scripts/signal_coverage_map.py`; build.py never imports it; `python3 scripts/build.py --check all` 8/8 | size: M
- [x] T3 Re-ground + run the REAL e2e chain on substrate's C8 bundle → casework SAR | scope: scripts/e2e_chain_check.py, scripts/e2e/** (regenerated fixtures) | success: `GROUNDING_HEADS` re-pinned to aml-substrate@9c75c03 + aml-casework@c6d8401; `--selftest` green (the synthetic-fixture logic still passes); a REAL chain run attempted — substrate's committed C8 party-bearing bundle (aml-substrate/tests/fixtures/evidence/CASE-P-0000251.json) + a casework signed SAR PRODUCED by running casework's DETERMINISTIC pipeline (DeterministicDrafter) as a TOOL (file-contract, NO sibling import) → `python3 scripts/e2e_chain_check.py --real --substrate <c8> --casework <signed>`; the outcome RECORDED honestly in the e2e status — CONNECTED (spine proven on a real C8 chain, not the synthetic C4 fixture) or GAP-FOUND (e.g. casework not yet adopting substrate's v0.2 contract → a real surfaced cross-pillar finding, never papered over); `! grep -nE "import aml_substrate|import aml_casework" scripts/e2e_chain_check.py`. FALLBACK: if casework can't be run from a signal-watch session → document chain-readiness + hand the `--real` run to a casework-rooted session, recorded honestly | size: L
- [x] T4 Document the measured finding + re-ground both sibling briefs | scope: docs/corpus-substrate-coverage.md, docs/pillar-integration-contract.md, aml-substrate/docs/corpus-coverage-build-PLAN-BRIEF.md (sibling), aml-casework/docs/capability-assertions-PLAN-BRIEF.md (sibling) | success: docs/corpus-substrate-coverage.md + contract §8 gain a Phase-60 section — the measured C7 rise (reachable-now 93→171, +78 = 62 direct + 16 proxy, the FIRST real rise), the C8/C14/C26 non-movement finding (the binding gap is `behavior_emergence`, not the now-completed wiring — Phase 59's "both halves" sharpened to "only the cap whose behavior genuinely emerges reaches reachable-now"), and the e2e-chain outcome; HEADs re-pinned inline (substrate@9c75c03, casework@c6d8401, corpus@472b44e); the substrate brief marks the detector/view + party-bearing-emission half DONE → next = emergence-engine work (C8/C14 behavior) or a non-screening detector; the casework brief marks C7/C8/C14 assertions DONE (C26 honest null) → next per its own roadmap; both carry the SHARED acceptance (further reachable-now rises need EMERGENT BEHAVIOR, not more assertions/emissions) + the doctrine constraint (no behavior/label stamping); always-on illustrative framing; `python3 scripts/build.py --check all` 8/8 | size: S
- [x] T5 Exit verification | scope: (verification only — no edits) | success: `python3 scripts/build.py --check all` → 8/8 byte-identical; `git status` shows ZERO ship-artifact/dist changes (only data/coverage-map/**, scripts/e2e_chain_check.py + scripts/e2e/**, docs/**, .dev-wiki/**, .claude/rules/**); regression green: `signal_coverage_map.py --check` + `--selftest` + `e2e_chain_check.py --selftest` | size: S

> Phase 60 — T1–T5 all [x] (DELIVERED 2026-06-19, user accepted via /dev-debrief). VERIFIED MOVEMENT (corpus@472b44e × substrate@9c75c03 × casework@c6d8401): **reachable-now 93→171 (+78, the FIRST real rise)** — needs-detector 62→0, needs-view-exposure 70→69, needs-behavior 296→281. ALL +78 is C7 (62 direct + 16 proxy; `is_reachable` fires before the data_source_class branch). C8/C14/C26 moved 0 — behavior_emergence data-only/absent is the binding 3rd conjunct (NEITHER emission nor assertion work). VERIFY-FIRST caught a plan-stage +62 undercount → corrected to +78 before the freeze. The real C8 chain CONNECTS: casework consumed substrate's C8 v0.2 bundle CASE-P-0000251 → signed SAR, zero blocking violations; e2e_chain_check --real CONNECTED (C4 too). signal_coverage_map.py --check byte-identical + --selftest green (2 goldens re-grounded as landing anchors; C8/D1→needs-behavior now proves an assertion alone doesn't move a data-only cap); no sibling import. DEVIATION (A3 bit → user Option-A): the e2e re-ground cascades into the launcher ship dist via pillar-status.json → only dist/index.html re-grounded (grounding_heads, 2 lines; the 7 other dists byte-identical, the Phase-55/57 pattern). --check all 8/8. Both sibling briefs re-grounded in their own trees (a sibling session commits them). SOFT OBS: e2e --selftest writes pillar-status.json to "bridges pending" → silently clobbers the committed --real state (restore via --real) — a guard is a future-phase candidate.
> Phase 60 — exit: (1) coverage.json re-frozen at the new pin (reachable-now 171), --check byte-identical + --selftest green, no sibling import, build.py clean; (2) e2e_chain_check.py GROUNDING_HEADS re-pinned, --selftest green, --real CONNECTED on the real C8 chain; (3) docs/corpus-substrate-coverage.md §3b + contract §8 carry the +78 rise + the C8/C14/C26 emergence-gap finding + the e2e outcome, HEADs re-pinned; both sibling briefs re-grounded; (4) --check all 8/8, 7/8 dists byte-identical + the sanctioned launcher re-ground.
> Abort (held): no unsanctioned ship drift; verify-first gated the freeze; no behavior/label stamping in the briefs; no sibling import.

<!-- phase:phase-59-consume-substrate-phase-15 -->
<!-- gate-log:phase-59 direction=approved delivery=accepted -->

## Phase 59 — Consume the substrate Phase 15 build into the coverage map (re-ground · re-freeze · document the exposure≠reachable-now coupling)

aml-substrate Phase 15 (@5875241, DELIVERED) shipped the SUBSTRATE HALF of Phase 58's brief — a label-blind
PartyView (exposes D8 KYC + D12 pep) + 4 SCREENING_DETECTORS (C7/C14/C8/C26). `substrate-pin.json` was
re-grounded in the working tree but `coverage.json` was left un-frozen → `signal_coverage_map.py --check`
fails. Direction A (re-freeze + document the coupling, tiers unchanged) over re-tiering (C) / hold-revert (B);
the user chose "verify first, then re-confirm" → A0 (pin↔5875241) + A1 (the 242→needs-behavior /
62-C7→needs-detector tiering is intended classifier semantics) CODE-VERIFIED before the freeze, both HELD,
re-confirmed. The honest finding: the substrate half moved 0 signals into reachable-now (the 3-way AND);
view-exposure 312→70, needs-behavior 54→296 (the 242 are exposed-but-unmeasurable on the txn-only emission,
NOT emergence work); reachable-now stays 93. LITE, NON-ship; --check all 8/8. Plan:
articles/phases/phase-59-consume-substrate-phase-15.md. Ledger: Phase-59 (all_accept:true; A0=T0 weakest,
verified). Grounded against aml-substrate@5875241 + aml-casework@4ac9523 + corpus@472b44e. Gate 2026-06-18.

- [x] T1 Verify the re-grounded pin against substrate@5875241 + re-freeze coverage.json | scope: data/coverage-map/**, scripts/signal_coverage_map.py (read-only verify), tests/ | success: substrate-pin.json code-verified faithful to aml-substrate@5875241 (PartyView 16 fields exact; C7/C14/C8/C26 detector classes + capabilities + signal_ids + SCREENING_DETECTORS membership; D8/D12 exposed) with the HEAD recorded in the pin's regrounding_note; the classifier's 242→needs-behavior (modeled-inactive) / 62-C7→needs-detector (emerges-but-not-is_live) assignment confirmed intended (not a pin-edit bug); `python3 scripts/signal_coverage_map.py --freeze` then `--check` byte-identical; `--selftest` green; `! grep -nE "import aml_substrate|import aml_casework" scripts/signal_coverage_map.py`; build.py never imports it; `python3 scripts/build.py --check all` 8/8 | size: M
- [x] T2 Document the measured movement + the exposure≠reachable-now coupling | scope: docs/corpus-substrate-coverage.md, docs/pillar-integration-contract.md | success: docs/corpus-substrate-coverage.md gains a Phase-15-landing section — the measured tier movement (view-exposure 312→70, needs-behavior 54→296, reachable-now unchanged 93), the 3-way-AND finding (has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges"; the substrate half satisfied only exposure → moved 0 into reachable-now), and the stale-label/composite-needs-behavior caveat (needs-behavior=296 = ~242 exposed-but-unmeasurable-on-the-txn-emission + ~54 genuine emergence gaps; needs-detector=62 = detector-exists-blocked-on-the-casework-assertion); HEADs re-pinned inline (substrate@5875241, casework@4ac9523, corpus@472b44e); the contract §8 subsection cross-references it; always-on illustrative framing; `python3 scripts/build.py --check all` 8/8 | size: S
- [x] T3 Re-ground the two sibling briefs to current HEADs | scope: aml-substrate/docs/corpus-coverage-build-PLAN-BRIEF.md (sibling), aml-casework/docs/capability-assertions-PLAN-BRIEF.md (sibling) | success: the substrate brief marks the detector/view half DONE (C7/C14/C8/C26 + PartyView landed @5875241) + specs the remaining increment = emit a PARTY-BEARING evidence bundle (so the 242 KYC/pep observables become map-measurable); the casework brief re-grounds to @4ac9523 + specs the 4 paired grounding_replay assertions (C7/C8/C14/C26) still needed; both carry the SHARED acceptance (reachable-now rises only when BOTH halves land, re-run `--check`) + the DOCTRINE CONSTRAINT (detectors + observable-exposure ONLY, never behavior/label stamping); authored here, executed in sibling-rooted sessions | size: S

> Phase 59 — T1–T3 all [x] (DELIVERED 2026-06-18, pending the user's delivery-gate acceptance). VERIFIED: substrate-pin.json code-verified faithful to aml-substrate@5875241 (PartyView 16 fields + C7/C14/C8/C26 + D8/D12 exposed) at the verify-first checkpoint, A0/A1 HELD before the freeze; coverage.json re-frozen → `--check` byte-identical + `--selftest` PASSED (2 stale D8-unexposed golden values re-grounded to the verified new reality — D8→modeled-inactive, C13/D8→needs-behavior — now a regression anchor for the landing, NOT a loosening; structural integrity tests untouched). MEASURED movement (corpus@472b44e × substrate@5875241 × casework@4ac9523): needs-view-exposure 312→70, needs-behavior 54→296, **reachable-now UNCHANGED 93** (the 3-way AND — the substrate half satisfied only exposure; the 242 are exposed-but-unmeasurable on the txn-only emission). docs/corpus-substrate-coverage.md §3a + contract §8 carry the finding + the composite-tier caveat; both sibling briefs re-grounded (substrate detector/view half DONE → party-bearing emission next; casework @4ac9523 → 4 paired assertions). --check all 8/8 zero drift; no sibling import; build.py never imports the script; ZERO ship artifacts in the change set. NOTE: the two sibling-brief edits dirty the sibling working trees (substrate brief is tracked; casework brief untracked) — a sibling-rooted session commits them.
> Phase 59 — exit (phase-59): (1) substrate-pin.json verified faithful to @5875241; signal_coverage_map.py --check byte-identical (re-frozen) + --selftest green + no sibling import + build.py clean; (2) docs/corpus-substrate-coverage.md + contract §8 carry the measured movement + the 3-way-AND finding + the composite-tier caveat, HEADs re-pinned; (3) both sibling briefs re-grounded (substrate detector/view half DONE → party-bearing emission next; casework → 4 paired assertions; shared acceptance + doctrine constraint); (4) --check all 8/8, the 8 ship dists byte-identical, ZERO ship artifacts in the change set.
> Abort: any of the 8 offline dists drift / a ship artifact touched → STOP and surface (never re-baseline). A brief that stamps behavior or labels → out of bounds (emergence doctrine). The companion importing sibling code → out of bounds. A validator/selftest looks like it needs loosening → fix the data/design, never the check.

<!-- phase:phase-58-corpus-substrate-coverage-map -->
<!-- gate-log:phase-58 direction=approved delivery=accepted -->

The user's REFRAME at the dev-plan gate: the synthetic-data + detection layer (Pillar 1, aml-substrate)
was built BOTTOM-UP (6 detectors C2/C3/C4/C5/C6/C15, "chosen-not-measured" thresholds from individual
advisory flags) with NO systematic join to the corpus catalog (2,251 indicators / 523 buildable =
status==gap AND data==available). The corpus is the program's signal source-of-truth; the detection layer
should be designed AGAINST it, targeting 200+ GROUNDED corpus signals (backed by ~15-20 CAPABILITY
detectors — grounding_replay is capability-scaled, NOT 200 detector implementations) to demonstrate
signal/atom monitoring + narrative generation at scale. Deliverable = the executable coverage MAP + the
doctrine-safe mapping design + prioritized sibling build briefs (signal-watch's architecture role; the
200+ build runs sibling-rooted, the P55/P56/P57 rhythm). DOCTRINE BOUNDARY (A0): the corpus drives
detectors + observable-exposure TOP-DOWN; data generation + labels stay emergent BOTTOM-UP; the map
MEASURES behavioral gaps, never stamps them. NON-ship; the 8 ship dists byte-frozen, --check all 8/8.
Plan: articles/phases/phase-58-corpus-substrate-coverage-map.md. Ledger: Phase-58 (all_accept:true; A0
doctrine-reconcilability = T0 weakest). Grounded against aml-substrate@df23bba + aml-casework@2381d71.
Gate 2026-06-18.

- [x] T1 The executable corpus→substrate coverage map | scope: scripts/signal_coverage_map.py, data/coverage-map/**, tests/ | success: reads the committed corpus's BUILDABLE indicators (status==gap AND data==available, ~523 across the 5 sources) + a PINNED snapshot data/coverage-map/substrate-pin.json (the aml-substrate@df23bba observable-schema field list + the 6-detector registry C-codes + the aml-casework@2381d71 grounding_replay assertion C-codes, transcribed, HEADs recorded inline) + the already-vendored emitted sample data/chain-cases/CASE-P-0010361/bundle.json; emits a per-signal TIERED reachability classification {reachable-now, needs-detector, needs-view-exposure, needs-behavior, out-of-reach} where observable-surface reachability is MEASURED (the indicator's D-code maps to a schema field that is present AND populated in the emitted sample) and behavioral-emergence reachability is REASONED-and-flagged (behavior_confirmed:false); DETERMINISTIC — `python3 scripts/signal_coverage_map.py --check` re-derives a byte-identical committed data/coverage-map/coverage.json (carries the per-tier counts + grounded-signals-per-capability ranking), `--selftest` validates the classifier on fixtures (a tampered pin / a fabricated reachable-now MUST fail); `! grep -nE "import aml_substrate|import aml_casework" scripts/signal_coverage_map.py`; build.py never imports it; `python3 scripts/build.py --check all` 8/8 | size: L
- [x] T2 The doctrine-safe mapping design + the measured findings writeup | scope: docs/corpus-substrate-coverage.md (NEW), docs/pillar-integration-contract.md | success: docs/corpus-substrate-coverage.md states the corpus→D-code→substrate-field→C-code→detector→casework-assertion MAPPING CONTRACT + the EMERGENCE BOUNDARY explicitly (observable + detector design top-down OK; label + behavior bottom-up only — the map measures behavioral gaps, never stamps them) + reports T1's measured numbers (the 5-tier counts + the itemized path to 200+ grounded signals + the per-capability signals-unlocked ranking); docs/pillar-integration-contract.md gains a "signal-coverage mapping" subsection cross-referencing it; re-grounded against aml-substrate@df23bba + aml-casework@2381d71 (HEADs pinned inline per the 2026-06-16 process rule); always-on illustrative framing; `python3 scripts/build.py --check all` 8/8 (non-ship) | size: M
- [x] T3 Prioritized sibling build briefs (the corpus-driven 200+ build hand-off) | scope: aml-substrate/docs/corpus-coverage-build-PLAN-BRIEF.md (NEW, sibling), aml-casework/docs/capability-assertions-PLAN-BRIEF.md (NEW, sibling) | success: both briefs RANK capabilities by signals-unlocked-per-capability (mechanically from T1's coverage.json) → the substrate brief specs which ~15-20 capabilities to wire as detectors + which generated-but-unexposed observables to surface in the detector input views (TxnView/AccountView), the casework brief specs which grounding_replay assertions to add; each carries code-verified current-HEAD facts (substrate@df23bba: 6 detectors, the rich observable schema, the label-blind views; casework@2381d71: 5 registered assertions, the per-capability pattern) + the SHARED acceptance (the coverage map's reachable-now count rises as each capability lands) + the DOCTRINE CONSTRAINT (no behavior/label stamping — author detectors + observable-exposure ONLY); authored here, executed in sibling-rooted sessions | size: M

> Phase 58 — T1–T3 all [x] (DELIVERED 2026-06-18, pending the user's delivery-gate acceptance). Single-beat signal-watch-local (the map + design + briefs); the corpus-driven 200+-signal BUILD is the sibling follow-on (gated on aml-substrate/aml-casework-rooted sessions). MEASURED (corpus@472b44e × substrate@df23bba × casework@2381d71, 523 buildable): reachable-now 93 (all C15 — 4 of 5 live detectors ground ZERO buildable gaps; the detection layer was NOT designed from the corpus) · needs-detector 62 (all C7) · needs-view-exposure 312 (the dominant gap — data the substrate GENERATES but never EXPOSES; the lever is exposure, not new data) · needs-behavior 54 · out-of-reach 2. signal_coverage_map.py --selftest + --check green; --check all 8/8 zero drift; no sibling import; build.py never imports it; ZERO ship artifacts in the change set.
> Exit (phase-58): (1) scripts/signal_coverage_map.py — tiered reachability over the 523 buildable indicators × the pinned snapshot × the vendored emission; observable MEASURED, behavioral flagged; --check byte-identical + --selftest green; no sibling import; build.py clean; (2) docs/corpus-substrate-coverage.md — the mapping contract + emergence boundary + measured numbers; integration contract extended; HEADs pinned; (3) both sibling briefs ranked by signals-unlocked-per-capability with the doctrine constraint; (4) --check all 8/8, ZERO ship artifacts in the change set.
> Abort: any of the 8 offline dists drift / a ship artifact touched → STOP and surface (never re-baseline). A brief that stamps behavior or labels → out of bounds (the emergence doctrine, A0's abort-line). The companion importing sibling code → out of bounds (subprocess/file-contract/vendored-pin only). A validator/selftest looks like it needs loosening → fix the data/design, never the check.

<!-- phase:phase-57-neural-drafter-backends -->
<!-- gate-log:phase-57 direction=approved delivery=accepted -->

## Phase 57 — Live neural SAR/STR draft + pluggable drafter backends (claude · OpenAI /v1 · opencode)

Make the Phase-56 workbench's "LIVE neural SAR/STR draft" actually true end-to-end from signal-watch
(today it only runs the stub) + open the drafter to multiple backends: claude (OAuth, exists) · an
OpenAI-standard /v1 endpoint (local models direct) · opencode (drive drafting THROUGH its agent loop).
The drafter is casework's pluggable Drafter Protocol; the gate stays the oracle. Two-beat, two-repo
(P55/P56 rhythm): beat 1 = the signal-watch spine + live-claude (drivable now); beat 2 = the
openai+opencode adapters (briefs here, live runs gated on casework sessions). Subprocess + file handoff
only — no sibling import. Plan: articles/phases/phase-57-neural-drafter-backends.md. Ledger: Phase-57
(all_accept:true, A2 reframed; A0 opencode-feasibility = T0 weakest). Grounded against aml-substrate@df23bba
+ aml-casework@2381d71. Gate 2026-06-18.

- [x] T1 serve_chain backend-name pass-through + server-side env mapping (the spine) | scope: scripts/serve_chain.py, tests | success: drafter selection expands from {key→claude/stub} to an N-backend name pass-through {stub, claude, openai, opencode}; each backend's creds/endpoint resolved from SERVER-SIDE env (anthropic OAuth / OPENAI_BASE_URL+key+model / opencode serve URL+model); the browser sends a backend NAME only; serve_chain passes `--drafter <name>` + the matched env to the casework subprocess; honest requested-vs-effective (fail-soft); `python3 scripts/serve_chain.py --selftest` green OFFLINE for ALL backends (name→env mapping; NO creds in the rendered config payload; unknown/unavailable backend → honest gated/stub); `! grep -nE "import aml_substrate|import aml_casework" scripts/serve_chain.py`; build.py never imports it; `python3 scripts/build.py --check all` 8/8 | size: M — DONE: BACKENDS=(stub,claude,openai,opencode) + backend_available/available_backends/default_backend/resolve_backend; _BACKEND_ENV (claude=AUTH_TOKEN|API_KEY, openai=OPENAI_BASE_URL, opencode=OPENCODE_SERVE_URL); resolve_backend = browser-name→effective, honest stub fallback on unknown/unavailable (named note, never a silent neural→neural switch); run_case gained `env`; _run reads the browser `backend`; live_config/_drafter_config expose NAMES+booleans only (selftest asserts NO secret/endpoint leaks into the served config/page). drafter_for_env kept as the auto-default (back-compat). --selftest PASS (N-backend availability + pass-through + no-leak + run-resolution); no sibling import; build.py serve_chain-free; --check all 8/8.
- [x] T2 chain.html backend picker + live-draft staged reveal + gate verdict | scope: chain.html, tests/chain.test.mjs | success: the workbench gains a backend selector (stub/claude/openai/opencode); renders the live neural SAR/STR draft as a STAGED reveal + the narrative_grounding + 6-verifier VERDICT on the GENERATED narrative + `drafter_effective` honesty (e.g. "claude → fell back to stub") + a stub-vs-neural comparison; always-on "Illustrative data & outputs" badge; esc() the sole escaper; line-buffered NDJSON (stage rendering, not a token stream); `node tests/chain.test.mjs` green (picker, live-draft stage, gate-verdict render, drafter_effective honesty, stub-vs-neural, XSS, NDJSON line-split); chain.html NOT a build target / NOT in dist/; `--check all` 8/8 | size: L — DONE: picker (pickerHTML/pickBackend — only available backends selectable, unavailable show disabled "n/a"; sends NAME to /run); backendLabel maps all 4; consume body generalized (effective label, requested→effective + the honest fallback note, the gate-is-oracle line); DRAFTS store + compareHTML (stub-vs-neural side by side, each gated); setDraftChip reflects the selected backend; dossier theme reused. tests/chain.test.mjs 31→46 (picker render/disabled, pickBackend availability guard, backend POSTed, live-claude label, requested→effective note, comparison, XSS on a compared narrative). chain.html not a build target; --check all 8/8.
- [x] T3 Re-ground the casework pin + run the live claude draft + walkthrough | scope: data/chain-cases/** (manifest pin), docs/chain-workbench.md, tests/smoke-checklist.md | success: the recorded grounding HEAD re-grounded to the current casework HEAD (@2381d71+); serve_chain drives `--drafter claude` via server-side OAuth (ANTHROPIC_AUTH_TOKEN) → the workbench renders a REAL neural SAR/STR draft gated by the verifiers, OR the documented honest fail-soft if OAuth isn't headless-available (selftest-proven either way); docs/chain-workbench.md updated (the N backends, server-side creds, the live-claude recipe + the gated openai/opencode beat, two-beat framing); smoke-checklist row; `python3 scripts/build.py --check all` 8/8 | size: M — DONE: pin re-grounded f0542b7→2381d71 (e2e_chain_check.GROUNDING_HEADS + data/pillar-status.json regenerated via --real; launcher rebuilt — only dist/index.html moved, 7 dists byte-identical). The REAL chain CONNECTED against casework@2381d71 via the stub drafter (casework ingest @2381d71 → signed SAR signed:true 0-blocking → e2e_chain_check --real CONNECTED). LIVE CLAUDE: no anthropic creds in this env → the documented honest fail-soft path (the live neural claude path is drivable when ANTHROPIC_AUTH_TOKEN/API_KEY is set; casework Phase 8 OAuth ClaudeDrafter; also covered by casework @integration) — recorded honestly in the doc. docs/chain-workbench.md rewritten (Phase-56→57: the backend table, server-side-creds recipe, the two-beat reframe — consume CLI+claude landed, openai+opencode gated); smoke-checklist Phase-57 section added. --check all 8/8; e2e --selftest preserves pillar-status byte-stable; launcher.test 23/23.
- [x] T4 Sibling brief: drafter_openai.py + --drafter openai (the OpenAI /v1 standard endpoint; local models direct) | scope: aml-casework/docs/openai-drafter-PLAN-BRIEF.md (NEW, sibling) | success: brief specs a thin Drafter-Protocol adapter (POST OPENAI_BASE_URL /chat/completions; model+key via env; fail-soft like claude) mirroring drafter_claude.py + an `openai` branch in ingest._generate_with_drafter; code-verified current-HEAD facts (aml-casework@2381d71); acceptance = serve_chain --drafter openai against a local llama-server → a verifier-gated narrative (authored here, executed in a casework session) | size: S — DONE: aml-casework/docs/openai-drafter-PLAN-BRIEF.md written (untracked in sibling tree; casework session commits). Code-verified @2381d71 facts (Drafter Protocol / GenerationContext / Draft / generate_narrative bounded loop / ClaudeDrafter template / _generate_with_drafter fail-soft / --drafter choices). Specs: (1) the one refactor — extract _system_prompt/build_user_prompt/_DraftSchema to a dep-light drafter_prompts.py (so the openai adapter reuses the exact prompt WITHOUT importing anthropic); (2) OpenAIDrafter (urllib/openai-SDK POST, response_format json_schema from _DraftSchema, None on malformed = fail-closed); (3) ingest `{name:factory}` dispatch + openai in --drafter choices, same fail-soft envelope. A0 thin-over-Protocol (defended), A1 structured-output-works (JSON-mode fallback), A2 local-hallucination-caught-by-gate (the designed demo). Acceptance + "already in place signal-watch-side" noted.
- [x] T5 Sibling brief: drafter_opencode.py + --drafter opencode (drive drafting THROUGH opencode — the load-bearing beat-2 leg) | scope: aml-casework/docs/opencode-drafter-PLAN-BRIEF.md (NEW, sibling) | success: brief specs a Drafter-Protocol adapter that drives `opencode serve` (OpenAPI 3.1 + SSE, async POST→poll; local model via opencode's @ai-sdk/openai-compatible provider, model key == llama-server --alias; AVOID the bare-`opencode run` headless-permission bug per working-knowledge) → output funnels into casework's grounding-aware generate loop (the 6 verifiers stay the oracle); fail-soft; the headless-feasibility risk NAMED as load-bearing (A0); acceptance = serve_chain --drafter opencode → a verifier-gated narrative from a local model via opencode | size: M — DONE: aml-casework/docs/opencode-drafter-PLAN-BRIEF.md written (untracked in sibling tree). Encodes the working-knowledge opencode pattern verbatim (use `opencode serve` OpenAPI+SSE NOT bare `opencode run`; async POST→consume SSE/poll; provider key==llama-server --alias; --jinja + ctx≥16384/64K+). Specs OpencodeDrafter (reuses drafter_prompts.py; create session → send the shaped prompt → consume SSE to the final message → tolerant-parse to Draft, None=fail-closed; BOUNDED turns/time) + the opencode dispatch entry with fail-soft. A0 (T0 weakest) = headless serve+SSE feasibility, with the /v1 adapter as the named fallback (build openai first); A1 structured-final-output, A2 opencode-available-in-env. The agent loop's output STILL funnels into generate_narrative + the 6 verifiers (the gate stays the oracle). Acceptance + "already in place signal-watch-side" noted; cross-refs the openai brief for the shared refactor.

> Phase 57 — **COMPLETE (T1–T5 all [x]); delivery ACCEPTED + user marked the phase completed 2026-06-18 (committed f49ec1b).** Beat 1 met; beat 2 (live openai/opencode) gated on the casework adapters. Beat 1 (the signal-watch spine + the re-grounded pin @2381d71): serve_chain N-backend name pass-through (creds server-side, no leak) + chain.html picker + live-draft staged reveal + the gate-verdict + stub-vs-neural comparison; the REAL chain CONNECTED against casework@2381d71 via the stub; the live `--drafter claude` (OAuth) path is drivable when creds are set (none in this env → documented honest fail-soft). Both sibling briefs (openai /v1 + opencode agent-loop) authored. Verified: --check all 8/8 (only dist/index.html moved with the pin) · serve_chain/chain(46)/validate/e2e/launcher(23) green · gate-console(68)/triage(93)/corpus/news(150) untouched. Beat 2 = the LIVE openai/opencode runs, GATED on the casework drafter_openai.py / drafter_opencode.py adapters landing (the two briefs, a casework session).
> Phase 57 BEATS: beat 1 = T1–T3 (signal-watch spine + live-claude, drivable now); beat 2 = T4–T5 (briefs now, the live openai/opencode runs gated on casework sessions). Delivery gate (beat 2) closes when the casework adapters land + serve_chain drives them.
> Exit (phase-57): (1) serve_chain --selftest green for ALL backends offline (stubbed), no creds leak, no sibling import; (2) node tests/chain.test.mjs green (picker + live-draft + gate-verdict + drafter_effective honesty); (3) the pin re-grounded + the live claude draft rendered/gated (or honest fail-soft documented); (4) both sibling briefs written; (5) docs + smoke-checklist + chain.html NOT a build target + --check all 8/8.
> Abort: chain.html becomes a build target OR any of the 8 offline dists drift → STOP and surface. The companion importing sibling code → out of bounds (subprocess + file-contract only). Any backend's key/token/base_url reaching the browser → out of bounds (non-negotiable §4.5). A validator/selftest looks like it needs loosening → fix the data/design, never the check.

<!-- phase:phase-56-chain-workbench -->
<!-- gate-log:phase-56 direction=approved delivery=accepted -->

## Phase 56 — Chain workbench: the HTML analyst UI (companion-served live consume + verify over a pre-baked case library)

An HTML frontend for the substrate→casework→verify chain, living in signal-watch. Reframed (the user's
"detection doesn't need runtime gen") to an ANALYST CASE-WORKBENCH: detection pre-baked/upstream (evidence
bundles vendored like the corpus pin); the casework consume (6 verifiers + a LIVE neural SAR draft) + the
cross-pillar verify run LIVE per case, stage-streamed. Dev-time companion (serve_chain.py + chain.html),
NEVER a ship artifact (offline dists byte-frozen; chain.html is not a build target). Subprocess + file
handoff only — no sibling import. Plan: articles/phases/phase-56-chain-workbench.md. Ledger: Phase-56
(all_accept:false; A4 casework-consume-CLI the T0 weakest; A3 defer-neural REJECTED→live-in-v1). Grounded
against aml-substrate@df23bba + aml-casework@85602c1. Gate 2026-06-17.

- [x] T1 Vendor the case library + build-time validator | scope: data/chain-cases/**, scripts/(validator) | success: ≥1 substrate evidence bundle committed under data/chain-cases/ (start CASE-P-0010361, synthetic, illustrative:true) with a provenance note (aml-substrate@df23bba); a validator confirms each vendored bundle passes e2e_chain_check's substrate-side checks (schema + §2 id-mint + corpus grounding); `python3 scripts/build.py --check all` 8/8 (data/chain-cases is NOT a build target) | size: S — DONE: data/chain-cases/CASE-P-0010361/bundle.json (byte-identical to aml-substrate@df23bba emission, sha1 bac574d…; 5 alerts C4/C3/C2/C5/C15, 71 txns, illustrative:true) + manifest.json (provenance + display metadata); scripts/validate_chain_cases.py reuses e2e_chain_check.check_substrate (NO sibling import) + manifest↔bundle referential integrity; --selftest PASS (catches id-mint tamper + capability drift), --check VALID; build.py chain-cases-free; --check all 8/8 zero drift.
- [x] T2 Sibling brief: the casework consume CLI | scope: aml-casework/docs/consume-cli-PLAN-BRIEF.md (NEW, sibling) | success: brief specs `python -m aml_casework.ingest <bundle> --out <signed.json> [--drafter stub|claude]` as a thin wrapper over load_real_bundle/generate_narrative/(ClaudeDrafter|FixedDraftStub)/record_signoff/emit_signed_sar; server-side ANTHROPIC_API_KEY for claude, stub default+fallback; code-verified current-HEAD facts (aml-casework@85602c1); acceptance = serve_chain.py drives it → e2e_chain_check --real CONNECTED (authored here, executed in a casework session) | size: S — DONE: aml-casework/docs/consume-cli-PLAN-BRIEF.md written (untracked in sibling tree; casework session commits). Code-verified @85602c1: reference flow = load_real_bundle→generate_narrative(drafter)→record_signoff→emit_signed_sar (test_real_ingest_chain.py); all 6 signatures confirmed; ClaudeDrafter() reads ANTHROPIC_API_KEY from env (anthropic>=0.109.2). SURFACED the load-bearing wrinkle: FixedDraftStub is test-only (tests/_strata.py) + fixed-content → brief recommends a src-resident bundle-derived deterministic stub (one inculpatory claim per alert citing its grounded signal_id; narrative = grounded atoms only) as A0; fail-soft claude→stub fallback; subprocess+file-handoff, no cross-import.
- [x] T3 serve_chain.py companion (stdlib, dev-time, no sibling import) | scope: scripts/serve_chain.py (NEW), tests/ | success: GET /cases lists the vendored library; POST /run {case} → subprocess the casework consume CLI (--drafter stub default; claude iff ANTHROPIC_API_KEY) → subprocess e2e_chain_check --real → stage-stream NDJSON (evidence → consume/verifiers+draft → cross-pillar verify → CONNECTED) → final payload (bundle + signed SAR + audit walk); `python3 scripts/serve_chain.py --selftest` green OFFLINE with the casework subprocess STUBBED (cases listed; a run streams stages → CONNECTED on a fixture; key→stub fallback; honest gated/error paths); `! grep -nE "import aml_substrate|import aml_casework" scripts/serve_chain.py`; build.py never imports it | size: M — DONE: scripts/serve_chain.py (stdlib http.server; GET /cases·/health, POST /run NDJSON stage-stream evidence→consume→verify→connected; single-flight 409). casework consume = subprocess of `python -m aml_casework.ingest` (injectable; honest "bridge gated" RunError until the CLI lands); e2e_chain_check verify = subprocess --real with pillar-status.json SNAPSHOT+RESTORE (no launcher drift). drafter_for_env: claude iff ANTHROPIC_API_KEY else stub. audit_walk grounds each alert flag→frozen corpus. --selftest PASS (stub consume → CONNECTED, 5/5 grounded, pillar-status byte-stable, gated+unknown-case named); no sibling import; build.py serve_chain-free; --check all 8/8.
- [x] T4 The HTML workbench (chain.html, dossier theme) | scope: chain.html (NEW), tests/chain.test.mjs (NEW) | success: chain.html renders the analyst workbench (case list → Run → staged reveal: evidence bundle → the 6 verifiers + the live SAR draft → cross-pillar verify → CONNECTED + signed SAR + flag→corpus walk); always-on "Illustrative data & outputs" badge; esc() the sole escaper; served by serve_chain.py (NOT a build target, NOT in dist/); `node tests/chain.test.mjs` green (stage-rendering contract + badge + XSS-escape + NDJSON stage consumption under the DOM shim); dossier aesthetic via frontend-design | size: L — DONE: chain.html (dossier theme reused from corpus.html :root; signature = a typed 4-node chain ledger that lights as the join connects — evidence→consume→verify→connected; case library rail; flag→corpus audit walk with verbatim corpus flags + red_flag translations; line-buffered NDJSON reader = stage rendering, not token stream; esc() sole escaper; `<!--__CHAIN_CONFIG__-->` marker → serve_chain injects window.__CHAIN_CONFIG__). tests/chain.test.mjs 31/31 (vm+DOM/fetch shim; progressive ledger reveal, 6 completeness chips, CONNECTED payoff, XSS on title/summary/narrative, NDJSON line split across chunks, gated-error banner, loadCases). serve_chain --selftest now renders the real page (config inlined, placeholder gone, badge present); build.py + dist/ chain-free; --check all 8/8.
- [x] T5 Walkthrough + isolation guard | scope: docs/chain-workbench.md (NEW), tests/smoke-checklist.md | success: docs/chain-workbench.md gives the run recipe (start serve_chain.py; the casework consume-CLI prerequisite; optional ANTHROPIC_API_KEY for the neural draft; the keyless stub-fallback path) + the two-beat framing (signal-watch spine selftest-proven now; the live run gated on the casework CLI); `! grep -nE "chain\.html|serve_chain" scripts/build.py` (chain.html NOT a build target); `python3 scripts/build.py --check all` 8/8 (offline dists byte-identical) | size: S — DONE: docs/chain-workbench.md (what-it-is + the subprocess/file-handoff boundary + pillar-status snapshot/restore + run recipe [serve_chain.py, AML_CASEWORK_DIR/PYTHON, key=neural else stub] + two-beat [spine selftest-proven now / live gated on the casework consume CLI] + isolation/abort rules); tests/smoke-checklist.md gained the Phase-56 section. grep guard clean (build.py has no chain.html/serve_chain ref); --check all 8/8.

> Phase 56 — **BOTH BEATS MET; delivery accepted 2026-06-17.** Beat 1 (SPINE T1–T5, all [x]) committed 16920d4. Beat 2: the casework consume CLI landed (aml-casework@f0542b7, Phase 7 — `python -m aml_casework.ingest` + `drafter_stub.py`, brief-A0 resolved) → `serve_chain` drives it → `e2e_chain_check --real` CONNECTED, verified from signal-watch (drafter stub here; neural `--drafter claude` fail-soft, key-gated, covered by casework @integration). Pin re-grounded 85602c1→f0542b7; pillar-status all-green; launcher rebuilt; --check all 8/8. Phase functionally COMPLETE (active→completed pending the user's explicit call).
> Exit (phase-56): (1) vendored library + validator; (2) serve_chain.py --selftest green (casework subprocess STUBBED, offline); (3) node tests/chain.test.mjs green; (4) the casework consume-CLI brief written; (5) docs/chain-workbench.md + chain.html NOT a build target + --check all 8/8.
> Abort: chain.html becomes a build target OR any of the 8 offline dists drift → STOP and surface. The companion importing sibling code → out of bounds (subprocess + file-contract only). The key reaching the browser → out of bounds (non-negotiable). A validator looks like it needs loosening → fix the data/design, never the validator.

<!-- phase:phase-55-cross-pillar-e2e-bridge -->
<!-- gate-log:phase-55 direction=approved delivery=accepted -->

## Phase 55 — Cross-pillar end-to-end bridge: aggregation + verification (the 3-pillar full demo connected)

Connect the 3-pillar full demo. The actual wiring executes in SIBLING sessions (their dev-* hooks bind
there); the AGGREGATION + FINAL DELIVERY sit here. signal-watch delivers the SPINE this session — the
ratified serialization contract + a re-runnable cross-repo verification harness (PROVE the join, don't
narrate it) + the two sibling briefs + a launcher front door + the walkthrough. The `--real` chain
verification (and the final delivery gate) is GATED on the sibling sessions landing the persist (#1) +
consume-real-bundle (#2) bridges. Shape = the user's "verifier harness + walkthrough" + the launcher
re-scope (A3). Grounded against aml-substrate@bafc67d + aml-casework@0316580 (code-verified). Plan:
articles/phases/phase-55-cross-pillar-e2e-bridge.md. Ledger: Phase-55 block (A0 = §5a-schema-ratifiable-now,
the T0 weakest; A2 C4-slice DEFENDED by code; A3 reject→launcher re-scope). Gate 2026-06-17 all_accept:false.

- [x] T1 Ratify the Pillar-1→Pillar-2 serialization format | scope: docs/pillar-integration-contract.md | success: §2 + §5a flipped PROPOSED→RATIFIED — the concrete evidence/ dir layout + per-record on-disk format (json) + the deterministic id-mint rule (alert_id from (detector, account_id, sorted txn_ids, signal_id); dossier_id from (account_id, sorted alert_ids)) spelled out; §7's serialization open-question struck; `! grep -nE "PROPOSED" docs/pillar-integration-contract.md` clean on the §2 record rows; `python3 scripts/build.py --check all` still 7/7 (no ship touch yet) | size: S
- [x] T2 The e2e acceptance contract | scope: docs/e2e-acceptance.md (NEW) | success: names the C4-structuring vertical slice + the DETERMINISTIC connect criteria (substrate-emitted bundle [ratified schema] → casework's 6 Class-G verifiers green → record_signoff signed=True, blocking_violations=[] → every cited flag grounds to fin-2026-alert001:IND-11 in the frozen corpus); enumerates EXACTLY the assertions e2e_chain_check makes (schema-match · alert_id/dossier_id referential integrity · SAR-citation→evidence→corpus resolution); cross-refs the T1 schema; Illustrative framing | size: S
- [x] T3 The cross-repo verification harness (NON-ship, stdlib, no sibling import) | scope: scripts/e2e_chain_check.py (NEW), data/e2e/** (synthetic C4 fixture), data/pillar-status.json (NEW) | success: RED a synthetic C4 evidence-bundle + signed-SAR fixture pair matching T1's schema (NOT from a sibling) → `python3 scripts/e2e_chain_check.py --selftest` exits 0 asserting the join (schema-match + id referential integrity + SAR citations resolve to evidence + corpus IND-11); `--real --substrate <p> --casework <p>` reads ONLY serialized json (`! grep -nE "import aml_substrate|aml_casework" scripts/e2e_chain_check.py`) + prints honest "GATED: sibling output absent" + nonzero exit when pins missing; every run regenerates data/pillar-status.json {bridge_1_persist, bridge_2_consume, e2e_real} with current state (all pending) + the grounding HEADs | size: M
- [x] T4 The launcher — the single front door (8th build target) | scope: launcher.html (NEW), scripts/build.py (launcher target), dist/index.html (NEW), tests/launcher.test.mjs (NEW) | success: `python3 scripts/build.py launcher` inlines data/pillar-status.json + emits a single self-contained offline dist/index.html (always-on "Illustrative data & outputs" badge; NO fetch/LLM; relative links to the 5 existing artifacts resolve under file://; a cross-pillar panel renders the 3 bridge states from the inlined status); `node tests/launcher.test.mjs` green (offline-no-fetch + 5 links + badge + status panel + XSS-escape); `python3 scripts/build.py --check all` → 8/8 with the 5 EXISTING dists byte-identical | size: L
- [x] T5 The two sibling PLAN-BRIEFs (cross-repo hand-offs) | scope: aml-substrate/docs/persist-evidence-seam-PLAN-BRIEF.md (NEW), aml-casework/docs/consume-real-bundle-PLAN-BRIEF.md (NEW) | success: each brief carries code-verified current-HEAD facts (substrate@bafc67d: compose()/io.serialize present, no id-mint, cli prints+discards; casework@0316580: 6 verifiers, C4 registered, the IND-11 pin), the §5a persist+mint-ids spec / consume-real-bundle steps, cost-sorted assumptions, and the SHARED external acceptance = `e2e_chain_check --real` green on that side; both name the T1 ratified schema as the contract (authored here, executed in sibling-rooted sessions) | size: M
- [x] T6 The presenter walkthrough + final-delivery framing | scope: docs/e2e-walkthrough.md (NEW), tests/smoke-checklist.md | success: gives the per-repo run order (substrate gen→emit; casework verify→sign; signal-watch e2e_chain_check + open dist/index.html), how the harness proves each join, the launcher as the single entry point, and the TWO-BEAT delivery framing (spine now + fixture-proven; --real verification gated on the sibling sessions); smoke-checklist points to it | size: S

> Phase 55 SPINE = T1–T6 (signal-watch-local) — **DELIVERED + committed 2026-06-17 (b37d7a0 spine + e95df82 review-gate hardening; --check all 8/8 zero drift, all 6 tasks [x]).** The end-to-end `--real` verification + pillar-status all-green is the DELIVERY GATE (beat 2), closed when the sibling sessions land bridges #1 (substrate persist) + #2 (casework consume-real-bundle). **→ BOTH BEATS MET 2026-06-17: bridge #1 (aml-substrate@df23bba) + bridge #2 (aml-casework@85602c1) landed → `e2e_chain_check --real` CONNECTED (10867ff, adversarially verified, all-green, --check all 8/8). Delivery gate accepted; phase COMPLETE (user-confirmed 2026-06-17, active→completed).**
> Exit (phase-55): (1) contract §2/§5a RATIFIED (no PROPOSED on schema), §7 serialization Q struck; (2) docs/e2e-acceptance.md names the C4 slice + the exact assertions; (3) scripts/e2e_chain_check.py --selftest green on the synthetic fixture, --real honestly gated, no sibling import (grep-clean), regenerates data/pillar-status.json; (4) dist/index.html builds (8th target), node tests/launcher.test.mjs green, --check all 8/8 with the 5 existing dists byte-identical; (5) both sibling PLAN-BRIEFs written with the shared --real acceptance; (6) docs/e2e-walkthrough.md + smoke-checklist pointer.
> Abort: blocked >3 attempts → mark [blocked: …] + ask skip/abort. Any of the 5 EXISTING dists drift, or console/triage touched → STOP and surface (never re-baseline). A validator looks like it needs loosening → fix the data/design, never the validator. The harness importing sibling code → out of bounds (file-contract only). Any synthetic fixture presented as real substrate output → out of bounds (Illustrative).

<!-- phase:phase-54-cd-tag-control -->
<!-- gate-log:phase-54 direction=approved delivery=accepted -->

## Phase 54 — C/D tag control: the measured-not-gated control made real (SR-11-7 ongoing monitoring)

Stop point-measuring; build the **control**. Phases 51/52/53 built the instrument + a standing baseline
(C/D self-consistency 0.677, κ≈0.65; disagreement sharp not scatter; committed-error bounded ≥37%/≤61%,
upper bound context-confounded). This phase makes the blueprint's §4–§5 **measured-not-gated control
class** CONCRETE + EXECUTABLE for the C/D tag (the canonical unguarded/judgmental dimension), closing the
named-OPEN **Phase-47 T2/T3 control-story blocker** — AND executes the Phase-52/53 deferred follow-up
once (the user's gate rigor escalation): a context-matched + genuinely-independent (local Qwen) rater →
the clean cross-family reliability number + Krippendorff's α. NON-ship, read-only, ship corpus
byte-frozen (the 51/52/53 envelope). Direction = the user's reframe ("prototype the tag control" over a
4th measurement / the heading→capability probe) + "execute the independent challenge once" over
scheduling it. Plan: articles/phases/phase-54-cd-tag-control.md. Ledger: Phase-54 block (A0 =
local-model availability+competence, probe-gated + named fallback). Gate 2026-06-16 all_accept:true.

- [x] T1 Local-model probe + context-matched independent rater companion (dev-time) | scope: scripts/cd_rate_independent.py (NEW, stdlib + urllib → 127.0.0.1; NEVER imported by build.py / cd_correctness.py replay path), tests | success: RED a selftest with a STUBBED model response (offline, no model) asserting (a) the context-matched prompt carries the source-doc region for the gid (gid=doc_id/ind_id → data/<source>/<doc_id>.md) + the 28+20 interview posture + the closed C/D vocab (NOT flag+red_flag-only), (b) the json-schema/grammar shape, (c) the parse of an in-vocab {C,D} from a model reply → GREEN `python3 scripts/cd_rate_independent.py --selftest` passes; CHECKPOINT (Phase-46 probe-gate): `python3 scripts/cd_rate_independent.py --probe 3` against 127.0.0.1:8080 returns parseable in-vocab C/D for 3 items — on failure surface + take the Opus-context-matched fallback (independence leg → scheduled), user adjudicates; `! grep -nE 'import build|from build' scripts/cd_rate_independent.py` clean | size: M
- [x] T2 Execute the independent rating → committed fixture | scope: data/cd-correctness/independent-sample.json (NEW) + scripts/cd_correctness.py (--verify-fixtures extended) | success: run the context-matched Qwen rating over the SAME committed n=96 enlarged-random sample (cross-comparable with random-sample-large.json) → independent-sample.json (one C + one D per gid, rater/model + context-matched flag tagged); `python3 scripts/cd_correctness.py --verify-fixtures` GREEN (independent gids == the seeded enlarged-random sample; committed C/D match current — the 51/52/53 integrity pattern). [Fallback: an Opus-context-matched fixture labeled accordingly, independence scheduled.] | size: M
- [x] T3 Cross-family reliability math + Krippendorff-α | scope: scripts/cd_correctness.py (--report, --selftest) | success: RED selftest asserting Krippendorff-α math (perfect→1, chance→~0, a hand fixture; degenerate single-category) + the independent-stratum agreement (Qwen-context-matched vs committed per axis: agreement + Wilson + Cohen's κ) + cross-rater (Opus-blind-flag-only vs Qwen-context-matched) → GREEN `--selftest` passes; `--report` emits the independent stratum, each number definition-carrying, framed CONSENSUS (≥2 genuinely independent raters) + context-matched, NEVER validated correctness; honesty boundary updated (the clean number reported as the Phase-52/53 follow-up delivered, human-rater still deferred) | size: M
- [x] T4 The control: design doc + executable harness | scope: docs/cd-tag-control.md (NEW), data/cd-correctness/cd-control-baseline.json (NEW), scripts/cd_correctness.py (--control-check, --control-freeze) | success: (a) docs/cd-tag-control.md renders the SR-11-7 measured-not-gated control for the C/D dimension — scope + risk-tiering (BUILD_NOW-driving codes tighter) + the committed BASELINE (Phase 51-53 + the new independent number) + sampling cadence/seed discipline + TRIP-WIRES (self-consistency below baseline Wilson lower bound; independent-challenge agreement below threshold; new-source regate failure) + the INDEPENDENT EFFECTIVENESS CHALLENGE (executed once, scheduled+owner-assigned thereafter) + ownership/escalation/re-baseline + the 5%-charter placement + the audit-walk; cites blueprint §4–§5 + SR-11-7 Pillar-2 + OSFI E-23 (aml-wiki, not guessed); always-on Illustrative badge; (b) `python3 scripts/cd_correctness.py --control-check` GREEN PASS on the current corpus vs cd-control-baseline.json; an injected-drift fixture → BREACH (selftest, the separability-gate pattern); `--control-freeze` re-baselines; `--selftest` GREEN; `! git diff --quiet docs/program-blueprint.md` (NO blueprint edit) | size: L
- [x] T5 Regate + docs + drift guard + close the Phase-47 blocker | scope: docs/cd-correctness-report.md, .dev-wiki/*, HANDOFF.md | success: `python3 scripts/build.py --check all` ZERO drift (7/7); `! grep -E 'cd_correctness|cd-correctness|cd_control|cd-tag-control|cd_rate_independent' scripts/build.py`; honesty grep clean (no "X% correct/error"-as-fact; consensus + context-matched framing; badges present); `python3 scripts/cd_correctness.py --selftest` green; cd-correctness-report.md "named follow-up" line updated → "delivered in Phase 54 (executed, context-matched + cross-family)"; the Phase-47 T2/T3 control-story blocker marked resolved in _CURRENT_STATE; journal + HANDOFF §8 (no CLAUDE.md edit — non-ship, matches 51-53) | size: S

> Phase 54 COMPLETE (T1–T5, all [x]) — the measured-not-gated control for the C/D tag, made real. MEASURED (NON-ship; --check all 7/7 zero drift; the independent rater ran on the LOCAL Qwen, probe-gated, competence adjudicated by the user): the deferred independent effectiveness challenge EXECUTED — a context-matched, genuinely CROSS-FAMILY (local Qwen3.6-35B) rater over the same committed n=96 sample → C **0.604** (58/96, κ 0.583) / D **0.646** (62/96, κ 0.599); cross-rater (Opus-blind vs Qwen-cm) C 0.677 / D 0.646; **Krippendorff's α** over {committed, Opus-blind, Qwen-cm} C **0.634** / D **0.618**. FINDING: the context-matched cross-family agreement is statistically indistinguishable from the same-family self-consistency (0.677; Wilson CIs overlap) → the dimension's reliability is GENUINE (survives a family change, chance-corrected, ~0.6/axis, never validated-correct), and context-matching RESOLVES the Phase-53 confound (the flag-only comparison overstated disagreement). The CONTROL: docs/cd-tag-control.md (SR-11-7 Pillar-2 + OSFI E-23 grounded; scope/risk-tier/baseline/cadence/trip-wires/independent-challenge/ownership/escalation/audit-walk; always-on badge; no blueprint edit) + cd_correctness.py --control-check (PASS 7/7 on the frozen corpus) / --control-freeze (cd-control-baseline.json) — BREACH demonstrated on injected drift (selftest + a live perturb-and-restore). Closes the Phase-47 T2/T3 control-story blocker. Artifacts: scripts/cd_rate_independent.py (NEW dev-time companion) · scripts/cd_correctness.py (extended) · data/cd-correctness/{independent-sample,cd-control-baseline}.json (NEW) · docs/cd-tag-control.md (NEW) · docs/cd-correctness-report.md (extended). Delivery gate pending user acceptance.

> Exit (phase-54): (1) scripts/cd_rate_independent.py (dev-time companion) selftest green + probe-gated, never imports build.py; (2) data/cd-correctness/independent-sample.json committed (or the labeled Opus fallback), --verify-fixtures green; (3) cd_correctness.py carries Krippendorff-α + the independent-stratum report + --control-check/--control-freeze, --selftest green, PASS on current corpus + BREACH on injected drift; (4) docs/cd-tag-control.md renders the SR-11-7 control (baseline + trip-wires + independent challenge + ownership + audit-walk), cites §4–§5/SR-11-7/E-23, badge on, no blueprint edit; (5) --check all 7/7 zero drift, honesty grep clean, the Phase-47 control-story blocker resolved.
> Abort: blocked >3 attempts on a task → mark [blocked: …] + ask the user skip or abort. Existing dists drift → STOP and surface (never re-baseline). Local model fails the T1 probe → take the named Opus-context-matched fallback + schedule independence, surface at the checkpoint (never silently degrade or force a low-confidence number). A validator looks like it needs loosening → fix the DATA/design, never the validator. Any C/D number presented as validated correctness (vs consensus + context-matched) → out of bounds, honesty constraints govern.

<!-- phase:phase-52-cd-correctness-measurement | CLOSED 2026-06-16 (delivered+accepted, score 10); block archived to articles/phases/phase-52-cd-correctness-measurement.md + journal + ledger -->

<!-- phase:phase-53-cd-disagreement-decomposition -->
<!-- gate-log:phase-53 direction=approved delivery=accepted -->

## Phase 53 — C/D disagreement decomposition (error vs defensible-neighbor, measured honestly)

The measurement workstream's THIRD deliverable — **finishes Phase 52, not a new dimension.** The Phase-52 report's own closing paragraph names the gap: the ~⅓ of random-stratum mismatches "whose error-vs-defensible composition was NOT adjudicated," and "a clean decomposition would require running the forced-pairwise adjudication on the SAME random-stratum mismatches." Phase 53 IS that decomposition — enlarge the random stratum (kappa added), then adjudicate its mismatches forced-pairwise (blind code vs committed, neutral order) into genuine-error / defensible-neighbour / committed-questionable / un-adjudicable. The adjudicator is same-Claude-family → the split is a BOUND (error = lower bound / scatter = upper bound; A0); genuine independence (different family / human) + an inter-rater α panel stay deferred-with-owner. NON-ship, read-only, ship corpus byte-frozen (the Phase-51/52 envelope). Plan: articles/phases/phase-53-cd-disagreement-decomposition.md. Assumption ledger: Phase-53 block (A0 = the same-family-adjudicator-is-a-bound honesty assumption). Gate 2026-06-16 all_accept:true.

- [x] T1 Decomposition machinery + larger random sampler + chance-correction | scope: scripts/cd_correctness.py (EXTEND, stdlib, read-only) | success: RED a selftest asserting (a) the decomposition sampler reads an enlarged random fixture and selects EXACTLY the mismatched items (blind != committed on C, D, or either), emitting each as a forced-pairwise via the existing divergence_options (blind code vs committed code, neutral seed-fixed order); (b) the decomposition scorer maps committed-better→error / both→defensible / blind-better→committed-questionable / neither→escalate on a hand fixture; (c) Cohen's kappa math (blind vs committed over the 28-C/20-D marginals) on a hand fixture → GREEN `python3 scripts/cd_correctness.py --selftest` passes; `--sample-random 96 --seed 0` and (post-T2) `--sample-decomposition --seed 0` dump blind items (flag, red_flag, two neutral codes — NO which-is-committed) byte-identical on re-run | size: M
- [x] T2 Blind raters → committed fixtures (enlarged re-rate + decomposition adjudication) | scope: data/cd-correctness/{random-sample-large.json, decomposition-sample.json} (NEW) + scripts/cd_correctness.py (--verify-fixtures extended) | success: a FIRST blind subagent re-rates the enlarged random sample (n≈72–96, one C + one D per item, never seeing the committed code) → random-sample-large.json (blind + committed C/D per gid); the script then dumps that fixture's MISMATCHES as forced-pairwise; a SECOND blind subagent adjudicates them (committed-better / both / blind-better / neither, never told which option is committed) → decomposition-sample.json (pick + the seed-fixed option→committed/blind mapping per id); both subagents tool_uses:0 (blindness by construction); `python3 scripts/cd_correctness.py --verify-fixtures` GREEN (judged ids == seeded sample ids + seed-fixed option order — the Phase-51/52 integrity pattern) | size: M
- [x] T3 Decomposition math + chance-corrected --report | scope: scripts/cd_correctness.py (--report) | success: `--report` emits the enlarged RANDOM stratum (raw agreement + Cohen's kappa + Wilson CI per axis) and the DECOMPOSITION (error / defensible-neighbour / committed-questionable / neither, counts + rates over the mismatch population, each WITH its measurement definition), framed explicitly as a BOUND (error = lower / scatter = upper, same-family adjudicator); honesty boundary updated (bound-not-clean-split, chance-corrected companion, independence + α-panel deferred, n/seed chosen); selftest on hand fixtures GREEN | size: S
- [x] T4 Non-ship report extension + drift guard | scope: docs/cd-correctness-report.md (EXTEND — Phase-52 sections intact; add the decomposition section that completes the report's own named follow-up) | success: the report renders the decomposition (definitions + the bounded error/defensible split + kappa + CIs), updates the "named follow-ups" line to "delivered in Phase 53," and keeps the always-on Illustrative badge + honesty boundary; `python3 scripts/build.py --check all` reports ZERO drift (7/7, ship corpus + all dists byte-identical); build.py has no cd_correctness/cd-correctness reference; honesty grep clean (no "X% correct/error"-as-fact — framed as a bound) | size: S

> Phase 53 COMPLETE (T1–T4, all [x]) — the measurement workstream's THIRD measured result; the Phase-52 random-stratum disagreement decomposed. MEASURED (NON-ship; --check all 7/7 zero drift; both blind raters tool_uses:0): enlarged random self-consistency C/D **0.677** (65/96), **Cohen's κ ≈ 0.65 ≈ raw** → the self-consistency is GENUINE, not chance (closes the "not chance-corrected" gap; reproduces the n=24 0.625, CIs overlap). Decomposition of the 62 mismatches: both-defensible **1/62 (1.6%)**, neither **0/62** → the Phase-52 "scatter not error" guess is **REFUTED** (disagreements are SHARP). Direction: committed-better **23/62 (37%)** = clean LOWER bound on committed-upheld; blind-better **38/62 (61%)** = UPPER bound on committed-questionable, **CONFOUNDED** by context asymmetry (flag-only re-rate vs full-document committed) — NOT a committed-error rate, the split is itself unmeasured. A0 mechanism correction: predicted both-defensible inflation did NOT fire; the real confound is context-asymmetry (recorded honestly). Follow-up reframed: an independent rater must be context-MATCHED. Artifacts: scripts/cd_correctness.py · data/cd-correctness/{random-sample-large,decomposition-sample}.json · docs/cd-correctness-report.md (extended). Delivery gate pending user acceptance.

<!-- phase:phase-51-corpus-redundancy-measurement -->
<!-- gate-log:phase-51 direction=approved delivery=accepted -->

## Phase 51 — Corpus redundancy measurement (the §13 fm-1 frontier, measured honestly) [COMPLETE]

The measurement workstream's first deliverable: a NON-SHIP analysis (blueprint-report.html / probe-history class) quantifying the committed corpus's cross-regulator redundancy EXPOSURE — composition-as-redundancy-management on real data — without touching the frozen ship corpus or the no-similarity/no-dedup non-negotiable. HYBRID approach (user gate 2026-06-16): deterministic label co-occurrence UPPER BOUND + a sampled consensus-class semantic-equivalence estimate. Plan: articles/phases/phase-51-corpus-redundancy-measurement.md. Assumption ledger: Phase-51 block.

- [x] T1 Deterministic co-occurrence engine | scope: scripts/corpus_redundancy.py (NEW, stdlib, read-only over data/*/derived + the overlay maps) | success: RED a selftest asserting a known (typology×capability)×regulator cross-tab cell count on a fixed slice → GREEN `python3 scripts/corpus_redundancy.py --selftest` passes + emits the cross-regulator co-occurrence UPPER-BOUND fraction (% indicators sharing a (typology,capability) cell with another regulator), reproducible byte-identical; typology resolved per-indicator-override-else-doc-inherit; framed as candidate co-occurrence, NEVER dedup | size: M — DONE: selftest GREEN (0.800 fixture + corpus loads 2251, 0 unmapped, deterministic); MEASURED cross-AGENCY upper bound **0.325** (731/2251 in 68/364 shared cells), cross-jurisdiction 0.288; A1 HELD (0 unmapped, 350 override + 1901 inherit); corpus byte-untouched (read-only). Hotspots: corruption/C17 118, corruption/C20 59, TF/C19 40.
- [x] T2 Sampled consensus-class semantic-equivalence harness | scope: scripts/corpus_redundancy.py (--sample / --equiv) + a committed blind-sample fixture | success: a deterministic blind sampler over cross-regulator co-occurring pairs + an equivalence-rate recorder that reports the rate WITH its measurement definition, LABELED consensus-not-ground-truth + illustrative; selftest on a hand-scored fixture; the redundancy ESTIMATE = upper-bound × equivalence-rate, both numbers definition-carrying | size: M — DONE: --sample/--equiv + the committed fixture data/corpus-redundancy/equiv-sample.json (24 single-rater blind judgments, seed 0; sample-integrity VERIFIED against the regenerated sample); equivalence rate 0.042 strict / 0.354 band (1 eq · 15 partial · 8 distinct); ESTIMATE 0.325×0.042 ≈ ~1.4% (band ~11.5%); selftest GREEN (rate math 0.500/0.625). FINDING: the corpus's refusal to dedup is empirically vindicated (shared label = distinct facets, not duplicates).
- [x] T3 Non-ship report artifact | scope: docs/corpus-redundancy-report.{md or html} (NON-ship, always-on illustrative badge) | success: renders the upper-bound + the sampled estimate with definitions, framed "candidate redundancy ≤ X%, est ~Z% (consensus)"; NO dedup, NO similarity number presented as validated; cites §13 fm-1; the §10 "every number carries its definition" honesty greps clean | size: S — DONE: docs/corpus-redundancy-report.md (always-on Illustrative badge; T1 ceiling + T2 estimate with definitions; the finding + the honesty boundary [single-rater, consensus deferred, not a dedup]; reproduce commands).
- [x] T4 Honesty regate + verify | scope: tests + the build drift guard | success: `python3 scripts/build.py --check all` byte-identical (ship corpus + all dists UNTOUCHED); the report carries the badge; no similarity/lift figure on any ship artifact; corpus_redundancy --selftest green; claim-shaped honesty greps (no "deduplicated"/"X% redundant"-as-fact) | size: S — DONE: --check all 7/7 ZERO drift; selftest GREEN; badge present; honesty grep clean (only disclaimer/negation matches); no ship-artifact drift; data/corpus-redundancy/ is non-corpus (build.py never reads it).

> Phase 51 COMPLETE (T1–T4, all [x]) — the measurement workstream's first MEASURED result. Headline: cross-regulator co-occurrence ≤ 32.5% (deterministic), genuine equivalence est ~1.4% (single-rater illustrative, consensus deferred) — the §13 fm-1 frontier measured honestly; the corpus's no-dedup stance empirically vindicated. Ship corpus byte-frozen throughout. Next (parallel handoffs): casework audit-walk-to-source; the structure-detector probe.

<!-- phase:phase-50-aml-program-build -->
<!-- gate-log:phase-50 direction=approved delivery=accepted -->

## Phase 50 — AML program build: the demo became the program (data-substrate pillar spun out)

Direction = a major user REFRAME at the dev-plan gate (2026-06-12): build the blueprint's §3 design-stage workloads as REAL systems, spun out **one real-system-class repo per pillar**. signal-watch is now the program-ARCHITECTURE home (blueprint + this lifecycle record); the build lives in sibling repos. Pillar 1 = the **data substrate** → `/Users/jwang/aml-substrate` (Python; its phases tracked in that repo's DESIGN.md + docs/phase-*.md — the dev-* skills don't operate cross-repo). Interview (institutional knowledge) + 3 parallel research agents (synthetic-AML generators · real field-level schemas · distributions/topology) grounded the design (AMLworld emergent method + transitive labeling; ISO 20022 / Interac EMT / Payments Canada Std 005–007 / FINTRAC schemas; log-normal+Pareto / Hawkes distributions; cited in aml-substrate/DESIGN.md). Assumption gate closed 2026-06-12, all_accept: false — A1 [HIGH, T0] foundation-first / emergence-ready ACCEPT · A2 [HIGH] build-on-research (user OVERRODE the offered review-first) · A3 [HIGH] 1M-scale REJECT → parameterized small-first · A4 [MED] hybrid eyes-open → DEFERRED to phase-2's flow engine; ledger Phase-50 block, revisit-status filled at Phase-1 delivery.

- [x] aml-substrate **Phase 1 (Foundation)** DELIVERED 2026-06-13 (commits 44ad9b1→9256dc5, 54 tests) — canonical data model w/ inert emergence hooks · deterministic population + KYC graph (15 Canadian archetypes; PEP 0.5% / HR 2% / active 60%) · 6-channel background transaction engine (Hawkes seasonality, Canadian channel medians, round-number snapping) · net-new EMT channel detail · parquet + realism report. Scale ~161s/1M (linear), byte-identical regen, Benford-clean 0.001.
- [x] aml-substrate **Phase 2 (Emergence)** DELIVERED 2026-06-13 (commits 42b0c17→5632816, 83 tests total) — criminal/mule/shell designation (hidden ground-truth only, NO KYC leak) · fresh-Python laundering behavior engine (structuring → funnel → layering → shell, margin skim) · transitive taint labeling (bounded; ground-truth cases) · coverage checklist (6/7; cycle honestly uncovered) · the A1 permutation-null SEPARABILITY GATE (passes clean, fires on injected artifact) · emergence realism. Class imbalance calibrated to 1:21,657 @100k; structuring breaks Benford 0.18>0.01. PROCESS: user flagged the first Phase-2 "assumptions" as mislabeled DIRECTION choices → re-ran a proper falsifiable-assumption gate (A1 don't-know → defended + down-scoped to the separability gate, which then caught its own naive fixed-threshold at scale).

- [x] aml-substrate **Phases 3–6** DELIVERED 2026-06-13→14 — P3 monitoring/grounding-chain (154 tests; label-blind detectors → Class-G alerts → dossiers → LCTR/EFTR + STR scaffold) · P4 scale-to-1M (peak 19.38GB <32GB, 18.79M txns) · P5 entity-resolution (resolver pw-F1 0.876 vs baselines) · P6 resolved-entity investigation — the measured NEGATIVE finding: entity resolution ⊥ laundering structure (cross-account unit = case/network), `counterparty_account_id` null so the network is ground-truth-only. 239 tests total; gen/ freeze byte-stable. Per-phase detail in aml-substrate's .dev-wiki + DESIGN §8.
- [x] aml-substrate **Phases 7–11** DELIVERED 2026-06-15→16 (HEAD 0daa3cc; 360 tests total; gen/ freeze byte-stable) — P7 observable network linkage (closed the P6 finding: counterparty_account_id populated + mirrored credit leg + case-id leak fixed + a positive freeze guard scripts/check-gen-freeze.sh; mule network observable, recall 1.0) · P8 reconstruction · P9 realism-validated + baseline-locked (measure-baseline.json, --validate --check/--freeze) · P10/P11 two subtlety phases. DOMINANT OPEN FINDING: laundering is single-feature-separable 3/3 (total_amount ROC ≈ 0.95; P10/P11 both failed to make it multivariate; memoryless-Poisson generator, §6 Hawkes/dormancy still SPEC-ONLY/DEFERRED) → blocks the §13 LFCM composition demonstration. Per-phase detail in aml-substrate's .dev-wiki + DESIGN §8.

> signal-watch SHIP ARTIFACTS FROZEN (program-architecture home; no further demo-track work unless re-opened). Next: aml-substrate **Phase 12 — Multivariate subtlety (the §6 realism lever)** — make label-blind detection require ≥2 composed grounded signals (no single feature ROC > ~0.8); measure-first sub-fork: legit-overlap cohort (Leg B fidelity 0/3 hints the cheaper root-cause fix) vs the Hawkes/dormancy temporal engine — direction chosen 2026-06-16 over open-Pillar-2 / thin-chain-slice / hardening-only; planning handed to an aml-substrate-rooted session (brief: aml-substrate/docs/phase-12-multivariate-subtlety-PLAN-BRIEF.md).

<!-- phase:phase-49-triage-loop-embryo -->
<!-- gate-log:phase-49 direction=approved delivery=accepted -->

## Phase 49 — Triage-loop embryo made demo-able: §14's continuous adjudication loop as the 5th ship artifact (triage console)

Direction = the OFFERED recommendation taken as offered at the dev-plan gate 2026-06-12 (first non-reframe in 3 gates; on the user's stated LFCM target path), STANDARD ceremony. Build blueprint §14's continuous adjudication loop as a demo-able FIFTH ship artifact: triage.html → dist/triage/index.html (single self-contained offline file; the gate console BYTE-FROZEN — sibling, not extension). Scenario source = NEW committed SYNTHETIC data/triage/scenarios.json, deterministically curated by scripts/curate_triage_scenarios.py from data/probe-history (rulebook + 44 alert stubs, AUTHORING time only — build.py NEVER reads probe-history; rule text EMBEDDED, dataset self-contained) + US-federal-allowlist-only committed corpus indicators (the synthetic-novel stratum). ~16 scenarios across the 4 §14 strata (history-signal-fired / history-below-the-line / synthetic-novel / random-population) + ~4 known-disposition controls; evidence panels template-derived from rule logic + stub fields w/ a thin authored layer, ONE panel per fact pattern shared BY REFERENCE across divergent-disposition pairs (process-inconsistency beat STRUCTURAL, build-validated); fired-rule state universal; seeded LABELED second-rater dispositions. Arc: Queue (stream w/ stratum chips) → Evidence → Disposition (confirm-risk / confirm-no-risk / both-defensible / escalate / need-more-info naming a C/D code via taxonomy picker + the policy-gap escape "no defensible option — flag for policy review"; rationale REQUIRED) → Reveal (historical disposition, decisions-not-correctness; second-rater replay; process-inconsistency surfacing) → Discovery ledger (signal gaps DERIVED from fired-rule state; data gaps per D-code; process inconsistencies; policy gaps; agreement arithmetic computed at render w/ visible measurement definitions; params "chosen, not measured"; JSON export; persists nothing). Badge always-on; keyboard nav; reduced-motion; NO LLM/fetch. NO edit to docs/program-blueprint.md (avoids the hand-synced blueprint-report drift). Assumption gate closed 2026-06-12, all_accept: false (A1 demo-first ACCEPT · A2 data-path DON'T-KNOW round 1 → defended [Phase-48 A4 verbatim + console-cases curation precedent + §14 purpose] → ACCEPT round 2 · A3 panels ACCEPT · A4 5th-ship-artifact ACCEPT; ledger block in assumption-ledger.md; decision article articles/decisions/phase-49-triage-loop-embryo.md D1–D8 confidence high; spec specs/phase-49-triage-loop-embryo.md). Reviews: approach 7/10→revised (D7 panel verifier, D8 no-fake-instrumentation); plan 7/10→revised→9/10 ACCEPT. FROZEN: all 4 existing ship artifacts + dists BYTE-IDENTICAL; derive_signals.py; news pipeline; derived data + the 3 overlays; docs/program-blueprint.md.

- [x] T1 Scenario dataset + curate script: test scripts/curate_triage_scenarios.py --selftest fixtures first — shared-panel-ref integrity, stratum closed-vocab, US-federal allowlist, determinism, second-rater-seed presence each FAIL on seeded-broken fixtures (RED), write the curate script (reads data/probe-history + allowlisted committed corpus indicators at AUTHORING time; template-derives panel skeletons from rule logic + stub fields, thin authored layer; EMBEDS rule text — dataset self-contained) → data/triage/scenarios.json (~16 scenarios, all 4 strata + ~4 known-disposition controls; seeded LABELED second-rater dispositions; fired-rule state rule_id|none universal; synthetic meta flag) (GREEN), template dedupe; if authoring runs hot, pre-drawn split = curate machinery+fixtures (stays T1) / authored-panel layer (new task) (REFACTOR) | scope: scripts/curate_triage_scenarios.py, data/triage/** | success: python3 scripts/curate_triage_scenarios.py --selftest && regen-twice byte-identical (concretize at VERIFY) && python3 -c sanity gate (all 4 strata populated; ≥3 controls; ≤20 scenarios; every divergent-disposition pair shares its panel BY REFERENCE; fired-rule field universal; novel-stratum doc-ids ∈ allowlist; ≥4 labeled second-rater seeds; synthetic meta flag present) | size: M
- [x] T2 Build boundary + target: test 4 tamper fixtures (broken stratum vocab / dangling panel ref / missing synthetic meta flag / C/D ref outside taxonomy) each make validate_triage_scenarios RAISE, invoked unit-level via python3 -c (template doesn't exist yet — build.py triage greenness deferred to T3) (RED), implement load_triage_scenarios + validate_triage_scenarios in build.py + the `triage` target wired into all/--check (GREEN), reuse existing validation/inline helpers (REFACTOR) | scope: scripts/build.py | success: python3 -c unit checks (valid passes; 4 tampers raise — VERIFIED, errors-list style per the console precedent, render dies on any; +2 bonus drift classes) && per-target --check green on the 6 existing targets (VERIFIED) && ! grep -E '^[^#]*"probe-history"|^[^#]*probe_history' scripts/build.py (concretized at VERIFY: the original word-shaped grep failed on its own documentation comments — the same claim-shape fix the plan reviewer prescribed for T5's greps; probe-history appears in comments/docstrings ONLY, no code reference) | size: S
- [x] T3 triage.html arc: test tests/triage-console.test.mjs skeleton failing, gate-console precedent EXACTLY (load TEMPLATE + inject stub dataset, never parse dist): parse, arc reachability, disposition-gate rules (rationale required; need-more-info requires C/D pick; reveal locked pre-disposition) (RED), implement the full arc → first passing python3 scripts/build.py triage (dist PROVISIONAL — final freeze at T4) (GREEN), dossier-theme copy hygiene, no dead console code (REFACTOR) | scope: triage.html, tests/triage-console.test.mjs, dist/triage/** | success: node tests/triage-console.test.mjs core set green && python3 scripts/build.py triage && python3 scripts/build.py --check triage && offline single-file grep on dist/triage/index.html (concretize at VERIFY) | size: L
- [x] T4 Harness full coverage: test RED→GREEN per feature — need-more-info lands a per-D-code data-gap ledger row; policy-gap escape requires rationale; second-rater replay LABELED; ledger agreement arithmetic equals a hand-computed fixture AND renders its measurement-definition string; signal-gap derivation from fired-rule state; XSS-escape; keyboard guards; both motion modes; badge; export JSON shape (RED), implement each feature to green (GREEN), consolidate shared harness helpers (REFACTOR) | scope: tests/triage-console.test.mjs, triage.html, dist/triage/** | success: node tests/triage-console.test.mjs fully green (~50+) && python3 scripts/build.py triage && python3 scripts/build.py --check triage (T4 owns the FINAL dist freeze) | size: M
- [x] T5 Integration + docs + full regate [deviation noted: the CLAUDE.md trim toward ~200 was consciously DEFERRED again (now ~330 lines after the artifact-#5 additions) — rewriting load-bearing live-mode paragraphs at session end failed the cost-of-error test; the trim debt stays carried, surfaced in the delivery report]: test the pre-edit bar — claim-shaped honesty greps green against T4's triage.html + git diff --quiet docs/program-blueprint.md (RED), write CLAUDE.md replace-in-place (5 ship artifacts; trim toward the ~200-line contract while in there) + HANDOFF.md §8 + tests/smoke-checklist.md row incl. "read 4 evidence panels, one per stratum" (believability adjudicated by the user at the delivery gate; delivery report presents one full scenario verbatim) (GREEN) | scope: CLAUDE.md, HANDOFF.md, tests/smoke-checklist.md | success: python3 scripts/build.py --check all zero drift && git diff --quiet scripts/derive_signals.py && git diff --quiet docs/program-blueprint.md && ! grep -niE "\b(accuracy|precision|recall)\b" triage.html && [ -z "$(grep -iE 'ground truth' triage.html | grep -ivE 'never|not ')" ] && grep -qi "chosen, not measured" triage.html && grep -qi "decisions, not correctness" triage.html && node tests/corpus-explorer.test.mjs && node tests/news-stream.test.mjs && node tests/gate-console.test.mjs && node tests/triage-console.test.mjs && python3 scripts/derive_signals.py --selftest && python3 tests/news_quality_harness.py --check | size: S

> Exit (phase-49): (1) data/triage/scenarios.json committed — ≤20 scenarios, all 4 strata populated, ≥3 controls, deterministic regen byte-identical, US-federal-only novel stratum, divergent pairs share panels by reference, fired-rule state universal, ≥4 labeled second-rater seeds, synthetic meta flag; (2) build.py `triage` target wired into all/--check, boundary validation fails loud on tamper (4 classes), no probe-history reference in build.py; (3) triage.html → dist/triage single-file offline with the full arc, badge always-on; (4) tests/triage-console.test.mjs fully green (~50+ assertions, gate-console precedent); (5) claim-shaped honesty greps green + FULL REGATE — --check all zero drift (7 targets incl. triage), git diff --quiet on derive_signals.py AND program-blueprint.md, all existing suites green.
> Abort: blocked >3 attempts on a task → mark [blocked: …] + ask the user skip or abort. Existing dists drift → STOP and surface (never re-baseline). Believable panels won't fit the ~16+4 ceiling → split per the T1 pre-drawn valve, never pad thin panels to a count. A validator looks like it needs loosening to pass the dataset → fix the DATA, never the validator. Any typed-in agreement/accuracy figure or a parameter presented as measured → out of bounds, honesty constraints govern.

<!-- phase:phase-48-history-utilization-lfcm -->
<!-- gate-log:phase-48 direction=approved delivery=accepted -->

<!-- phase:phase-47-agentic-aml-program-design -->
<!-- gate-log:phase-47 direction=approved delivery=accepted -->

<!-- phase:phase-46-corpus-live-derivation -->
<!-- gate-log:phase-46 direction=approved delivery=accepted -->

<!-- phase:phase-45-corpus-presentation-polish -->
<!-- gate-log:phase-45 direction=approved delivery=accepted -->

<!-- phase:phase-44-live-extraction-quality -->
<!-- gate-log:phase-44 direction=approved delivery=accepted -->

<!-- phase:phase-43-live-pipeline-robustness -->
<!-- gate-log:phase-43 direction=approved delivery=accepted -->

<!-- phase:phase-75-consume-substrate-v05-er-emission -->
<!-- gate-log:phase-75 direction=approved delivery=accepted -->

## Phase 75 — Consume the substrate v0.5 entity-resolution emission: the spine's real-data memory lever

Direction = the user's directive "check and consume sibling repo work per planned"; CHECK done (code-verified: substrate fc98b09 BUILT named-identity v0.4 + entity-resolution v0.5 additive; casework 4a858e6 built nothing consumable). Step-13 picks: FULL per-planned, STANDARD, measure-first ACCEPTED. Companion-only — dists byte-frozen, build.py imports nothing new; the A1 file-bar guard (no change to evidence_requirements.py evaluate_sufficiency). Ledger: assumption-ledger.md Phase 75.

- [x] T1 (M) MEASURE-FIRST GATE — cross-case overlap in substrate v0.5 | DONE 2026-06-25. `scripts/measure_xcase_overlap.py` (+ --selftest) re-emits substrate v0.5 (seed 0) and measures TWO signals: entity_ref re-surfacing (REAL co-reference) vs shared-strong-id across distinct entity_refs (the over-merge trap). CHECKPOINT FINDING (3k probe; full 40k for the record): **229 entity_refs re-surface cross-case** (real memory signal) but the shared strong identifiers + substrate's own resolution_edges OVER-MERGE distinct entities (99 traps; substrate's deliberate noise floor + controller-cluster SHARES; resolution_edges emit "resolved" for distinct-named people). The reliable same-entity key is `entity_ref` (100% name-consistent). → User picked "Entity_ref memory + SHARES adjudication" (ledger Phase-75 T1 resolution; A3 REVISED). The gate's zero-branch did NOT fire (signal non-zero); it caught a MECHANISM error and re-keyed it honestly.
- [x] T2 (M) Bump curate_workbench_cases to substrate v0.5 (additive; identifiers/resolution_edges = CANDIDATE SHARES links) | DONE 2026-06-25. SUBSTRATE_HEAD f15c241→fc98b09; re-curated the committed slice (376 cases incl. a +21 cross-case CO-REFERENCE pass so the memory beat is demonstrable on real data; coverage 128/376 MEASURED). _merge_bundles unions resolution_edges as candidate SHARES; validate_v05_bundle (referential integrity + strength vocab; additive on v0.3). TWO cross-pillar fixes surfaced: (a) casework rejects contract_version "0.5" but TOLERATES the additive fields → hand it the v0.3 VIEW (committed bundle stays v0.5); (b) a latent venv-path bug (relative --measure-casework + cwd-change) → resolve py to absolute. curate --selftest PASS. | RED: extend the curate --selftest to assert the merged case carries display_name/counterparty_name/entity_ref/identifiers AND that resolution_edges are carried as CANDIDATE SHARES links (NOT trusted as merges) AND that an all-empty-new-field bundle stays shape-compatible with the v0.3 path. GREEN: SUBSTRATE_HEAD f15c241→fc98b09; read the v0.4/v0.5 fields additively in _merge_bundles + the case serializer; validate the new shape at the curate boundary (closed strength vocab + referential integrity on resolution_edges.between → known entity_refs); re-curate the committed slice. REFACTOR: keep the v0.3 fallback explicit. VERIFY: python3 scripts/curate_workbench_cases.py --selftest && the --measure-casework v0.3 stub consume stays green. | scope: scripts/curate_workbench_cases.py, data/workbench/** | success: curate --selftest passes with the new fields; SUBSTRATE_HEAD pins fc98b09; identifiers/resolution_edges carried as candidate SHARES links; the existing related_parties[] + casework-stub behavior unchanged
- [x] T3 (L) Wire substrate into entity_spine — entity_ref-keyed cross-case memory + SHARES adjudication | DONE 2026-06-25. entity_spine: added `entity_ref` to STRONG_KINDS (the declared-identity strong key) + a pillar-neutral `entities_in_multiple_records` cross-case query + selftest (entity_ref merge / distinct-entity_ref over-merge REFUSAL / cross-case query). serve_workbench: `_observe_substrate_party` (entity_ref strong; email/phone DEMOTED to weak candidate-SHARES) + `substrate_memory` (the consumer adapter). Committed slice MEASURED: **36 cross-case co-references** + **66 over-merge-refused** (the spine kept distinct entity_refs apart). A1 GUARD HELD — evidence_requirements.py BYTE-UNCHANGED; the inject-prior-cleared→byte-identical-verdict guard green; spine/scorer/serve_workbench --selftest all PASS. | RED: add spine selftest cases asserting (a) the SAME entity_ref across 2 cases accumulates to ONE persistent_entity (real co-reference) and (b) DISTINCT entity_refs sharing a strong email/phone are NOT merged (the spine REFUSES the over-merge — distinct entity_refs stay distinct; the candidate SHARES link is recorded, not merged). GREEN: a substrate-observation adapter feeds each workbench case's parties through the spine keyed on `entity_ref` (the reliable identity — entity_ref drives merge; substrate email/phone are CANDIDATE SHARES links that do NOT drive merge); the spine adjudicates the resolution_edges/shared-id candidates (reject-merge on distinct entity_refs). The spine's MERGE LOGIC stays byte-unchanged (entity_ref is the strong key). The A1 GUARD: evidence_requirements.py evaluate_sufficiency BYTE-UNCHANGED; the inject-a-prior-cleared→byte-identical-verdict assertion stays green. REFACTOR: reuse the Phase-74 grade grammar (strong/weak/reject); no new grading logic. VERIFY: python3 scripts/entity_spine.py --selftest (under .venv) && python3 scripts/resolution_scorer.py --selftest && git diff --quiet scripts/evidence_requirements.py. | scope: scripts/entity_spine.py, scripts/serve_workbench.py, tests/*.py | success: spine --selftest extended + green; same-entity_ref accumulates cross-case, distinct-entity_ref shared-id REFUSED (the over-merge-trap count); the within-slice cross-case co-reference number recorded; evidence_requirements.py byte-identical; the byte-identical-verdict guard holds
- [x] T4 (M) Render real names + the SHARES adjudication beat on the workbench | DONE 2026-06-25. A `/memory` route (substrate_memory + casefile_memory; read-only, :memory:, persists nothing) + a "Persistent entity intelligence" panel (`memoryPanelHTML`/`loadMemory`): the cross-case co-reference count (36) + the SHARES over-merge-refused count (66) + the casefile short-circuit (Vesna Maric, targets shrink 1, predicate pre-named) + the honesty qualifier. Live end-to-end verified (GET /memory → the real numbers; the page wires the slot + boot fetch). workbench.test.mjs 151→159 (+8: header/counts/entity/short-circuit/qualifier/empty/XSS). | RED: extend tests/workbench.test.mjs to assert showcaseSurface renders display_name/counterparty_name (names-not-codes from substrate) + the candidate SHARES links the spine ADJUDICATED (the over-merge-trap-refused beat: distinct entities sharing contact details, NOT merged) + the cross-case memory (an entity_ref re-surfacing with its prior). GREEN: thread the v0.5 display_name/counterparty_name + the spine's adjudicated SHARES links into the workbench render; the memory short-circuit reads the entity_ref-keyed prior. REFACTOR: reuse the existing graph helpers; no new lib. VERIFY: node tests/workbench.test.mjs. | scope: workbench.html, scripts/serve_workbench.py, tests/workbench.test.mjs | success: workbench.test.mjs extended + green; real names + the SHARES-adjudication (over-merge-refused) beat render; the memory-lever framing matches T1/T3's measured outcome
- [x] T5 (S) Re-ground the 3 sibling briefs to live HEADs | DONE 2026-06-25 (out of order — T2/T3 hard-blocked on the slow substrate re-emit; T5 is independent docs). All 3 briefs carry a PHASE-75 RE-GROUNDING block: substrate-graded-counterparty-identifiers → fc98b09 LARGELY BUILT (party-level identifiers[] + RelationshipEdge.strength + resolution_edges; the over-merge gap → merge-adjudication console); substrate-exogenous-disposition-label → fc98b09 NOT BUILT (premise holds); casework-confidence-graded-resolution → 4a858e6 NOT BUILT (all 3 legs open; Phase 16 internal; re-vendor SKIPPED). | RED: n/a (docs). GREEN: re-pin substrate-graded-counterparty-identifiers + substrate-exogenous-disposition-label briefs to substrate@fc98b09 (BUILT-different-shape / NOT-BUILT) and casework-confidence-graded-resolution to casework@4a858e6 (NOT-BUILT, cleared + graded-resolution still outstanding); record the brief-shape divergence (party-level vs txn-row) + that casework has nothing to consume + the still-open substrate items (exogenous-disposition-label, C1/C7/TF). VERIFY: grep the new pins; the BUILT/PARTIAL/NOT-BUILT status present in each. | scope: docs/*PLAN-BRIEF.md | success: all 3 briefs re-grounded to the verified live HEADs with per-brief status + outstanding items
- [x] T6 (S) Full verification + true-up | DONE 2026-06-25. --check all 8/8 byte-frozen; build.py imports nothing new; news_store.py + evidence_requirements.py BYTE-UNCHANGED. All green: entity_spine/resolution_scorer/measure_xcase_overlap/curate/serve_workbench --selftest; workbench.test.mjs 159; news-stream 150 + news_live PASS (M8 not regressed); gather_quality_harness --check; gate-console 68 + triage-console 93 + corpus OK; uv run pytest 22. CLAUDE.md ## Current state trued up (v0.5 consume, 376-case slice, 36 co-ref/66 refused, re-grounded brief pins, the test list). HANDOFF.md §8 NOT updated (stops at Phase 54; the workbench phases 55-74 live in the journal + CLAUDE.md — consistent with recent practice). | RED: n/a. GREEN: CLAUDE.md ## Current state + HANDOFF.md §8 trued up (the v0.5 consume + the measured memory number; companion-only, dists byte-frozen). VERIFY (tiered): python3 scripts/build.py --check all (8/8 byte-frozen) && grep-guard build.py imports neither spine nor substrate && python3 scripts/entity_spine.py --selftest && node tests/workbench.test.mjs && node tests/news-stream.test.mjs && python3 tests/news_live_test.py && python3 tests/gather_quality_harness.py --check && uv run pytest && git diff --quiet scripts/evidence_requirements.py. | scope: CLAUDE.md, HANDOFF.md, scripts/**, tests/** | success: --check all 8/8 byte-frozen; no new build.py import; spine/workbench/news/news_live/gather/pytest green; evidence_requirements.py byte-unchanged; docs trued up

- [x] D1 (discovered, FIXED inline in T2) — a latent venv-path bug in curate `_measure_grounding`: a relative `--measure-casework` arg + a subprocess cwd-change → python not found. Resolved to an absolute path. | DONE 2026-06-25.

> Phase 75 — ALL T1-T6 [x] + D1 [x]. STANDARD ceremony (user override of the project LITE default). Delivery accepted by the user 2026-06-25. The T1 measure-first gate caught a MECHANISM error (the planned strong-merge would over-merge substrate's deliberate noise floor) → re-keyed on `entity_ref` + SHARES adjudication (ledger Phase-75 T1 resolution; A3 REVISED). Adversarial review: 0 must-fix, 2 should-fix + 1 nit ALL FIXED, 1 refuted; honesty/A1/boundary all praise. Companion-only — `--check all` 8/8 byte-frozen, build.py imports nothing new, `evidence_requirements.py` byte-unchanged, M8 not regressed.

<!-- gate-log:phase-76 direction=approved delivery=accepted -->
## Phase 76 — The merge-adjudication Class-J console: the human gate over candidate SHARES links

Direction = the user's "/dev-plan the merge-adjudication console" + Step-9 picks (FORM = a 6th SHIP console dist/merge; SCORED = show synthetic-scored, defer real). STANDARD. Consumes Phase 75's 66 candidate SHARES + the spine's ambiguous-refusal + the synthetic true_entities scorer. The novelty: the ONE gate with a measurable correctness oracle (synthetic-only). Ledger: assumption-ledger.md Phase 76. Existing 8 dists byte-frozen except the sanctioned launcher cascade; build.py validates merge cases at the boundary, imports no spine/scorer/sibling.

- [x] T1 (M) Expand the synthetic true_entities oracle (the A1 shaping; CHECKPOINT) | DONE 2026-06-25. Oracle 8→25 obs / 5→17 latent clusters spanning the FOUR merge-adjudication quadrants; 13 merge candidates across all 3 bases (strong/weak/name): **3 real-co-reference / 3 over-merge-trap / 6 fragmentation-gap / 1 correct-rejection → 9 GENUINELY AMBIGUOUS**. Ambiguity seeded HONESTLY (household landline / shared coworking inbox / joint account / fragmented same-person / 3-way REFUSE bridge / neighbours-at-an-address) — the load-bearing proof: the SAME weak-address basis carries OPPOSITE truths (Rahman same / Reyes-Haddad distinct), so a basis-only rule cannot decide. Deterministic resolver on the oracle: pairwise P=0.50/R=0.33 (fp=3 false merges, fn=6 missed) — the human gate's reason to exist. `resolution_scorer.py` gained `candidate_pairs()` (reuses the spine `edge_grade` grammar) + KLASS_* shared vocab + candidate enumeration in `score()`; the resolver-input firewall HELD (cluster + note eval-only, stripped before the resolver; a 1:1 surrogate still rejected). `resolution_scorer --selftest` PASS under .venv; entity_spine --selftest still PASS (import-source unchanged). | RED: extend resolution_scorer --selftest to assert the expanded oracle has ≥N genuinely-ambiguous merge cases (same-person-fragmented / household-share / coincidence) the scorer resolves same-entity vs distinct. GREEN: author ~20-30 synthetic observations in data/entity-spine/true_entities.json with the deliberate ambiguity classes, synthetic-only qualified (emails .test / phones 555-01XX); keep the resolver-input firewall (cluster field eval-only). REFACTOR: none. VERIFY/CHECKPOINT: python3 scripts/resolution_scorer.py --selftest (under .venv) + report the ambiguous-case count; if it can't yield genuine ambiguity without fabricating truth → STOP, fall back to consensus + defer scoring. | scope: data/entity-spine/true_entities.json, scripts/resolution_scorer.py | success: scorer --selftest green; the oracle has a meaningful ambiguous-case queue; firewall held
- [x] T2 (M) curate_merge_cases.py → committed data/merge/cases.json | DONE 2026-06-25. `scripts/curate_merge_cases.py` (companion-only authoring tool; reuses entity_spine + resolution_scorer, NOT imported by build.py) → committed `data/merge/cases.json`: **66 REAL consensus candidate SHARES** (the v0.5 over-merge-refused residual, reproduced = Phase-75's 66; basis=strong, spine_verdict=kept_distinct, substrate_claim=resolved, NO oracle — consensus-not-ground-truth) + **13 SYNTHETIC scored cases** (all 4 quadrants / 3 bases; each carries an `oracle` block with the latent truth + correct_adjudication + the synthetic-only qualifier). THE FIREWALL translated to the ship artifact: the pre-disposition evidence (a/b) carries NO truth field (`_TRUTH_LEAK_KEYS` rejected); the truth rides ONLY the separate `oracle` block (revealed post-disposition); real cases carry NO oracle (no fabricated ground truth on real data). Honesty: real substrate emails domain-MASKED to example.test (synthetic-by-construction; local-part kept proves the collision); closed adjudication vocab (uphold_merge/reject_as_shares/both_defensible/escalate); deterministic (byte-stable across runs); selftest 5 broken fixtures rejected. | RED: a curate --selftest that validates the committed cases (referential integrity to the v0.5 bundles' candidate SHARES + the oracle; closed adjudication vocab uphold-merge/reject-as-SHARES/both-defensible/escalate; the real-consensus vs synthetic-scored tag; the resolver-input firewall — no cluster leak into a resolver-input surface; broken fixtures rejected) + deterministic regen. GREEN: curate the adjudication cases from the committed v0.5 slice's 66 candidate SHARES (consensus, no oracle) + the expanded synthetic oracle's scored cases (the deterministic spine verdict + the latent truth on the eval-only channel) → data/merge/cases.json. REFACTOR: reuse the entity_spine grade grammar + resolution_scorer firewall; no new logic. VERIFY: python3 scripts/curate_merge_cases.py --selftest. | scope: scripts/curate_merge_cases.py, data/merge/** | success: curate --selftest passes; cases.json carries real-consensus + synthetic-scored cases with the firewall + closed vocab; deterministic
- [x] T3 (L) merge.html + the `merge` build target + validate_merge_cases at the boundary | DONE 2026-06-25. `merge.html` (6th ship artifact; clones the dossier theme + the 5-screen arc Queue→Evidence→Adjudication→Verdict→Ledger): queue grouped by basis (scored-synthetic surfaced first); Evidence shows both records + the shared signal + the deterministic spine baseline (+ substrate claim for real) NEUTRALLY; the rationale-REQUIRED non-binary gate (uphold_merge/reject_as_shares/both_defensible/escalate); the Verdict LOCKED pre-adjudication and SPLITTING real-consensus (no oracle) from synthetic-scored (latent truth + match indicator + synthetic-only qualifier); session ledger with the consensus/scored agreement arithmetic + JSON export (persists nothing). build.py `merge` target: MERGE_TEMPLATE/__MERGE__/MERGE_CASES + load_merge_cases + STANDALONE validate_merge_cases (closed vocab + referential + the resolver-input firewall translated to the ship shape + the consensus/scored honesty split) + render/build/check_merge; main() want_merge wired into all+--check. Launcher cascade: launcher.html gained the merge card (Five→Six) → dist/index.html regenerated (the ONE sanctioned existing-dist change). dist/merge built (115KB, self-contained, no fetch/module). `--check all` = 9 targets, ZERO drift; the other 7 dists byte-frozen (git: only dist/index.html [M] + dist/merge/ [new]); build.py imports no spine/scorer/sibling/curate (firewall grep clean). | RED: node tests/merge-console.test.mjs (RED — see T4) asserts the Class-J arc renders. GREEN: merge.html (the dossier theme; Queue [grouped by basis] → Evidence [identities + shared identifier + spine verdict + network] → Adjudication [uphold-merge/reject-as-SHARES/both-defensible/escalate, rationale REQUIRED] → Reveal [deterministic baseline + scored truth where oracle exists, the real-consensus/synthetic-scored SPLIT, synthetic-only qualifier] → Ledger [JSON export, persists nothing]); build.py `merge` target (CONSOLE_TEMPLATE pattern) + load_merge_cases/validate_merge_cases at the build boundary; the launcher lists the new artifact (sanctioned cascade). REFACTOR: reuse console.html/triage.html idioms + esc() the sole escaper. VERIFY: python3 scripts/build.py merge && python3 scripts/build.py --check merge (byte-frozen) && node tests/merge-console.test.mjs. | scope: merge.html, scripts/build.py, dist/merge/** | success: dist/merge built + --check merge byte-stable; the arc renders; build.py imports no spine/scorer/sibling
- [x] T4 (M) tests/merge-console.test.mjs | DONE 2026-06-25. `tests/merge-console.test.mjs` (zero-dep vm + the corpus-explorer DOM shim, clones gate-console.test.mjs): 73 assertions green over the full arc — queue grouped by basis + the consensus/scored split chips; Evidence NEUTRALITY (the FIREWALL: the oracle truth never appears pre-adjudication, on real AND synthetic cases); the rationale-REQUIRED graded gate; the Verdict LOCKED pre-adjudication; the SPLIT (real → consensus/no-oracle; synthetic → latent truth + match/mismatch indicator + the synthetic-only qualifier); ledger agreement arithmetic (matched/scored counts only synthetic; consensus counts only real) + valid JSON export + persists-nothing; the honesty-governor word-ban (no catch-rate/lift/precision — caught a disclaimer, reworded); XSS escape; keyboard guards; both motion modes. | RED: write the arc tests (queue grouping by basis; the graded adjudication gate [rationale required; escalate path]; reveal LOCKED pre-adjudication; the scored-vs-consensus SPLIT [synthetic-only label on scored, no oracle on real-consensus]; ledger + JSON export; badge always-on; XSS-escape; keyboard guards; both motion modes). GREEN: the render satisfies them. REFACTOR: mirror gate-console.test.mjs/triage-console.test.mjs structure. VERIFY: node tests/merge-console.test.mjs. | scope: tests/merge-console.test.mjs, merge.html | success: the .mjs arc green; the scored-vs-consensus split + synthetic-only qualifier asserted
- [x] T5 (S) The substrate-emit true_entities handoff brief | DONE 2026-06-25. `docs/substrate-true-entities-emission-PLAN-BRIEF.md` — substrate emits a slice-aligned `true_entities` artifact (entity_ref→latent cluster) on an EVAL-ONLY channel so the merge console scores the REAL 66 too (today consensus-only). Pinned to the VERIFIED live HEAD substrate@fc98b09 (re-checked live this session — "close Phase 28", no drift from the Phase-75 pin). EMIT (the clusters gen/identity.py already computes, exported slice-aligned) / GROUND (the resolver-input firewall: cluster NEVER in the bundle's resolver-input surface nor a 1:1 surrogate — mirrors assert_no_cluster_leak) / CONSUME (curate flips the real cases to scored, firewall held end-to-end). References the T1 standards (scorer-contract / grade-grammar / resolution-link-schema). Status: NOT BUILT (the named handoff). | RED: n/a (docs). GREEN: a brief (extend substrate-graded-counterparty-identifiers or a new docs/substrate-true-entities-emission-PLAN-BRIEF.md) — substrate emits true_entities for the slice on the eval-only channel (resolver-input firewall preserved) so the merge console scores REAL substrate cases; pinned to the verified live HEAD fc98b09; EMIT/GROUND/CONSUME + the firewall contract. VERIFY: grep the pin + the firewall contract present. | scope: docs/*PLAN-BRIEF.md | success: the brief exists, re-grounded to fc98b09, names the eval-only-channel firewall
- [x] T6 (S) Full verification + true-up | DONE 2026-06-25. `--check all` = 9 targets ZERO drift (the 8 existing byte-frozen; only dist/index.html [launcher cascade] + dist/merge [new] in git); build.py imports no entity_spine/resolution_scorer/serve_workbench/curate (firewall grep CLEAN). All arcs green: merge-console (73) / gate-console / triage-console / workbench / news-stream + news_live (M8 not regressed); curate_merge_cases + resolution_scorer + entity_spine selftests; `uv run pytest` 22→24 (added curate_merge_cases selftest + merge-console arc to the umbrella). CLAUDE.md trued up: "Six ship artifacts" + item 6 (merge console, durable snapshot) + the merge build target + the merge test entries (replace-in-place; +34 lines for the durable architecture, no per-phase narrative). | RED: n/a. GREEN: CLAUDE.md ## Current state — the 6th ship artifact (merge console) added to the five-ship list + the build target list + the test list; HANDOFF.md untouched (consistent). VERIFY (tiered): python3 scripts/build.py --check all (9 targets; the 8 existing byte-frozen except the sanctioned launcher cascade) && grep-guard build.py imports no entity_spine/serve_workbench/resolution_scorer/sibling && node tests/{merge-console,gate-console,triage-console,workbench,news-stream}.test.mjs && python3 scripts/resolution_scorer.py --selftest && python3 scripts/curate_merge_cases.py --selftest && uv run pytest. | scope: CLAUDE.md, scripts/**, tests/** | success: --check all green (only the launcher cascaded); no spine/scorer/sibling import in build.py; all arcs + selftests + pytest green; CLAUDE.md trued up

> Phase 76 — ALL T1-T6 [x]. STANDARD ceremony. Direction gate all-accept (2026-06-25; A1 accept-with-shaping). The T1 A1 CHECKPOINT PASSED: the synthetic oracle expanded to 25 obs / 17 latent clusters → 13 merge candidates (9 genuinely ambiguous) across all 4 quadrants / 3 bases, ambiguity seeded HONESTLY (same weak-address basis → opposite truths). The 6th ship artifact `dist/merge/` dramatizes the Class-J merge gate; 66 REAL candidate SHARES (consensus, no oracle) + 13 SYNTHETIC scored (latent-truth oracle) — the consensus/scored SPLIT visible in the Verdict. Adversarial review (6 dimensions → adversarial verify; 28 findings: 2 confirmed should-fix [build-boundary validator weaker than curate's firewall — the 'note' leak-key + the real basis/verdict shape; BOTH FIXED, validators now in exact parity], 2 refuted [oracle-in-view-source is the gate-accepted offline-single-file pattern — render never surfaces it pre-adjudication; noted], 24 praise). Companion-only — build.py imports no spine/scorer/sibling/curate; the existing 8 dists byte-frozen (only the sanctioned launcher cascade); the resolver-input firewall holds (no truth in pre-adjudication evidence); honesty governor holds (consensus-not-ground-truth for real, synthetic-only-qualified for scored, no catch-rate/lift/precision, always-on badge). Delivery gate: pending user acceptance.
