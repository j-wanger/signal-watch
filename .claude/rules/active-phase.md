# Active Phase Context

Phase: 27 - Make the corpus demo shippable: assess corpus output quality, then replan the fix (M7) — ASSESS-ONLY-FIRST. Direction approved 2026-06-07. The BUILD is deferred to a replan after the assessment.

Objective: Phase 26 marked the corpus demo "showcase quality" (reviewer 10/10) but on the BUILT artifact the user judges it NOT shippable. Named letdowns: (1) the Read-advisory extract/translate beat is "brutally bad"; (2) the build animation is "not in place" unlike the six-act showcase. User instinct: "almost forego the grounding system, rely on LLM extraction+translation fully." INLINE DIAGNOSIS (evidence-backed): the brutality is PRESENTATION, not grounding — the red_flag translations are already register-quality + the grounded flags are real sentences; what reads brutal is the left "Source document" panel dumping RAW markitdown (page headers / footnote-digit runs / hyphen-wraps / table soup) + best-effort highlighting that MISSES many flags, and renderSignal's build-log never adds the showcase's `.run` "working" pulse (just ticks ✓). PUSHBACK (held): forego-the-gate risks fabricated "verbatim" quotes (a compliance non-negotiable) AND wouldn't fix the raw-article render.

T1 ASSESSMENT DONE (read-only, `ph27-corpus-quality-assessment` 44-agent workflow + deterministic metrics): VERDICT not shippable but the fix is shallow + almost entirely PRESENTATION — tiers **PRESENTATION_ONLY 39 · RE_TRANSLATE 1 · RE_EXTRACT 2→3**. Register holds (36/42 @5); the brutality is raw-markitdown article render + a too-literal highlight matcher (95.3% overall but 8 docs sub-100, fin-2021-a001 @38%) + an unfinished build animation (no `.run`/gate-hold/proposal-grid/lift-panel; firestat correctly omitted). The grounding system WORKS. Findings → `.dev-wiki/articles/phase-27-assessment-findings.md`.

BUILD REPLAN (direction approved 2026-06-07 — user chose **broader re-extract**): presentation fixes for all 42 + re-extract the genuine span defects + tighten the >240-char long flags corpus-wide (180 flags / 39 docs) to crisp grounded spans, FAITHFULNESS-GUARDED. Tasks (tasks.md): T2 article render cleaner (corpus.html) · T3 highlight resilience (normalize both sides; gate untouched) · T4 finish the build animation (act4 rhythm: .run + gate hold + proposal grid + lift-side panel; firestat OUT) · [CHECKPOINT: presentation flips the verdict?] · T5 (L) re-extraction sweep via a dynamic workflow (span defects fully + >240 long flags tightened; each new flag re-grounds via check_record; FAITHFULNESS GUARD = keep whole where a sentence's meaning needs its full length, don't fabricate brevity) · T6 re-translate fin-2022-a001 (values only) · T7 regate + rebuild + harness + docs. ORDER: presentation FIRST (no data) → confirm → data sweep → regate.

Scope. UNFREEZE: corpus.html, the 4 span-defect + >240 derived/*.json (T5 flag VALUES; T6 fin-2022-a001 red_flag values), dist/corpus, tests/**, CLAUDE.md, README.md. FROZEN byte-untouched: the six-act showcase (index.html + config/typologies/** + the 3 typology dists, the ORACLE), every source MD, every corpus-status.json, data/typology-map.json, the grounding core derive_signals.py (`normalize`/`rf_region`/`check_record`) + the authoring scripts. The verbatim re-extraction tightens to a contiguous grounded SUB-span that STILL re-grounds via the unchanged gate — the gate logic never changes. NO non-negotiable change. NO fabricated numbers.

Abort: if T2+T3 don't flip the verdict, reassess before the data sweep. If a re-extracted flag can't ground faithfully, KEEP the original (never fabricate a tighter span that drops meaning). Blocked >3 attempts → ask the user: skip or abort.

STATUS: ALL 7 TASKS [x] — SHIPPABLE. T1 assessment (presentation not grounding) · T2 cleanArticle · T3 normalize-both-sides highlight (100%) · T4 build animation (proposal grid + lift-side, firestat omitted) · whole-article progressive read (user-requested) · T5 re-extraction (121 flags → grounded sub-spans, faithfulness-guarded, 36 long-but-faithful kept whole) · T6 fin-2022-a001 re-translate · T7 regate. Gates: `--check all` 4/4 zero drift · `--selftest` PASS · 42/42 `--check-derived` clean · harness 139→148 · frozen set byte-clean · non-negotiable wording unchanged · dist 2.17→2.15MB.

Gates:
- [x] Direction confirmed by user (assess-only first → then the build replan: broader re-extract, faithfulness-guarded; 2026-06-07)
- [ ] Delivery accepted (the shippable corpus demo — pending the user's browser eyeball of dist/corpus + the post-implementation report)
