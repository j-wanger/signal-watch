# Assumption Ledger

Append-only. One block per phase direction gate (dev-plan Step 13). `revisit-status:` filled at debrief.

## Phase 39 — Live news QOL: streamed extraction progress + one-shot URL acquisition (2026-06-09)

all_accept: false (A1 rejected in round 1 → revised → accepted with condition)

- A1 [HIGH] URL flow shape. Round 1 (preview-then-run): REJECT — user wants one-shot. Round 2 (one-shot /extract {url}, converted text fills textarea as passive recovery): ACCEPT WITH CONDITION — acquisition must go through a multi-method fetch ladder (bot guards expected) + a scripted format STANDARDIZER + VERIFIER before extraction. revisit-status: bit (the accept-with-condition standardizer/verifier turned out LOAD-BEARING — justice.gov answered urllib with a 2.5KB Akamai interstitial converting to 0 chars; a fetch-error-only ladder failed; D5 verifier-advances-the-ladder + D6 interstitial two-step were the fix — the condition validated the design)
- A2 [HIGH] Progress = real stage-level chunked-NDJSON events over POST /extract (fetch+ReadableStream client); token streaming + client-only spinner rejected; job-id+polling is the verified-at-impl fallback if stdlib chunked flush misbehaves. ACCEPT. revisit-status: held (HTTP/1.0 body-until-close per-line write+flush streamed cleanly; the polling fallback was never needed)
- A3 [MED] Verify-latency cause (per-entity second pass, N sequential model calls) stays OUT of scope — feedback this phase, batching stays deferred. ACCEPT. revisit-status: held (latency untouched by design; progress made the 42.7s wait legible, not shorter; batching stays the deferred candidate)
- A4 [MED] Invariants carry forward: all client code in /*LIVE_START*/…/*LIVE_END*/ → offline dist/news byte-identical; markitdown .venv-only lazy import + graceful degrade; build.py never imports the live layer; extract()/call_llm fixture seam preserved (on_progress defaults no-op, no fixture re-capture). ACCEPT. revisit-status: held (replay fixtures green WITHOUT re-capture; dist/news byte-identical; --check all 5/5 zero drift)

## Phase 40 — Live red-flag extraction quality (measure-first) (2026-06-09)

all_accept: false (A3 + A4 rejected in round 1 → revised → accepted)

- A1 [HIGH] The flag-quality gap is INSTRUCTION CLARITY, not Qwen capability — few-shot exemplars from committed gate-passing records + mechanism vocabulary generalize beyond calibration (holdout split guards the Phase-38 overfit trap). If false: fix shifts to the verify layer or an honest local-model-ceiling finding. ACCEPT. revisit-status: held (prompt-shaping closed the gap on HOLDOUT — covR 0.40→0.55, mechCovR 0.46→0.63, positional decay eliminated 0.64→0.43 second-half miss; though the recall/precision frontier took 3 calibration rounds, r3 rolled back on measured regression)
- A2 [HIGH] The negative-news wiki's 2,313 raw articles are usable stress material via deterministic frontmatter filter (extraction: full + source_score floor); stored bodies → text-mode runs independent of URL liveness; local-only/never committed; fixture promotion stays US-federal-only. ACCEPT. revisit-status: held (with deviation: the corpus WAS usable stress material, but `extraction: full` does NOT exist — 2,256/2,314 files carry no extraction field; the deterministic filter was rebuilt on the committed news_fetch standardize→verify_article gate + a measured `continue reading < 3` anti-listing rule; selection stayed deterministic, manifest frozen local-only)
- A3 [MED] Measurement design. Round 1 (deterministic proxies + qualitative cluster adjudication only): REJECT — user wants the quantitative second-rater design. Round 2 (proxies PLUS blind second-rater reference extraction → INTER-RATER AGREEMENT per dimension [completeness/span/register], divergence clusters human-adjudicated, agreement = consensus never ground truth, T4-verify trigger = measured agreement delta): ACCEPT. revisit-status: held (the blind second-rater design produced the decisive signal — the residue-is-RECALL call, the covQ-undercredits finding via blind validity judging of unmatched flags, and the user's registry-scoring reframe all came out of it)
- A4 [MED] Gate posture. Round 1 (news_ground.py FROZEN, checks stay measurement-only): REJECT — user expects measurement-earned deterministic flag-quality checks to SHIP into the gate. Round 2 (news_ground.py UNFREEZES: near-dup collapse / span-quality checks land as shared gate rules for companion + build.py; the 4 committed news records must pass the extended gate — failures are adjudicated findings, never silent loosening; replay goldens regenerate DETERMINISTICALLY from pinned qwen.json captures, no model re-capture; full regate incl. --check all; dists byte-identical): ACCEPT. revisit-status: held (the gate unfroze cleanly — flag_dup_key dup-collapse landed shared [live DROP / build CHECK]; the 4 committed records passed the extended gate with NO adjudication needed; replay goldens valid WITHOUT regeneration; --check all 5/5 zero drift)

## Phase 41 — Entity-resolution schema enrichment (live news) (2026-06-09)

all_accept: false (direction itself was a user REFRAME off the offered candidates; A1 rejected in round 1 → revised → accepted with conditions; A3 rejected in round 1 → split A3'a/A3'b → both accepted)

- A1 [HIGH] Attribute-slot basis. Round 1 (census-earned slots from the public federal corpus): REJECT — the real input domain includes PRIVATE investigation notes carrying address/phone/email + client_number/account_number; slots are designed for that domain, not census-limited. Round 2 (A1' two-layer model: nested per-scan extraction JSON [grounded-or-stripped] + DuckDB ANCHOR design — entity anchor table + property association table [entity×kind×value edge w/ evidence + provenance + grounded status] + relationship edge table; confidence column reserved, NOT model-populated until a measured basis): ACCEPT WITH CONDITIONS — (1) anchors need an entity SOURCE TYPE (document types differ in significance: gov-enforcement / commercial-news / investigation-note); (2) monolithic-vs-per-property-subtables is a real scaling decision — monolithic+kind chosen now, revisit on measured divergence. revisit-status: held (both conditions implemented — scans.source_type stamps anchors + watchlist provenance; ONE monolithic entity_properties table w/ reserved detail-JSON landed, no kind-schema divergence at this scale; confidence stayed NULL; the non-destructive both-kept conflict rule fixture-proven)
- A5 [HIGH] Privacy boundary (NEW, surfaced by the A1 reject): private/client data is first-class INPUT but confined to the local live layer — gitignored DuckDB store + live session only; never committed, never fixture-promoted (fixtures stay US-federal public-domain only), never in ship artifacts; local 127.0.0.1 model means notes never leave the machine; the "no real customer data ever" non-negotiable governs everything committed/shipped. ACCEPT. revisit-status: held + STRENGTHENED (the boundary became a CHECK, not a convention — fixture_replay_test asserts every fixture base id ∈ the US-federal FIXTURE_META allowlist; nothing private/commercial committed; pinned captures byte-clean)
- A2 [HIGH] Relationships are honestly gateable: each edge {from, to, label, evidence} = verbatim-grounded evidence quote + referential integrity to extracted entities + label from a small closed vocab (~8 relation types); the LABEL stays a neural judgment (vocab-checked, never correctness-checked — the C/D-code honest split); main subject = the entity the enforcement action targets, multi-subject honest. ACCEPT. revisit-status: held (13 gate-passed evidence edges across the 3 .ph41 federal captures; DRQ3 distribution owner-or-controller-of 9 / employee-or-agent-of 3 / co-conspirator 1 / other 0 — the vocab covers; honest multi-subject exercised, tgr 7 mains)
- A4 [MED] Alias inversion: the current alias-dedup DROP (token-subset names) + moniker DROP (@handles) become FOLDs — kept as aliases attached to the fuller entity; fixture-pinned drop behavior changes deterministically; screening matches name ∪ aliases. ACCEPT. revisit-status: held (folds land w/ folded_into audit + reconcile_refs remap; orphan handles still drop; UNEXPECTED upside — the model's own aliases[] extraction attached @monalisa7→Zhdanova where structural adjacency could not: the prompt does ER work the deterministic layer can't; class-aware screen rules [single-token/@-handle exact-only] kept the common-name trap dismissable)
- A3 [MED] Scope. Round 1 (bundled live-only/offline-frozen/merge-deferred): REJECT — ambiguous bundle. Round 2 split: A3'a offline demo artifacts byte-frozen this phase (dist/news + 4 committed derived records + book.json; enrichment renders in the live region only): ACCEPT. A3'b cross-scan ACCUMULATION in scope via the anchor table (exact-normalized name → same anchor, properties accumulate with provenance); fuzzy cross-scan MERGE adjudication DEFERRED: ACCEPT. revisit-status: held (offline dist/news + 4 committed records + book.json byte-frozen, --check all 5/5; exact-name accumulation proven 4-scans→1-anchor; merge stayed out; LATE BITE adjacent to A3's silent sibling: the enriched ONE-CALL prompt regressed the flag layer ~12.5% — caught by the SPEC's prompt-regression constraint, user-ruled, fixed by r2 flags-first schema ordering 24→25)

## Phase 42 — Anchor dossier view + per-scan network visualizer (live news) (2026-06-10)

all_accept: false (A2 don't-know in round 1 → evidence check REFUTED the cross-article-overlap seeding claim → revised A2' accepted; A4 accepted with condition)

- A1 [HIGH] LIVE-only rendering: the network visualizer + anchor dossier render in the companion LIVE region only; offline dist/news + the 4 committed records stay byte-frozen (committed records carry no relationships[] — an offline graph would force enrichment + regate + dist unfreeze, a different phase). ACCEPT. revisit-status: held — offline dist/news + the 4 committed records + book.json byte-frozen (`--check all` 5/5 zero drift); all new client code LIVE-region-only, the offline strip intact (harness-asserted).
- A2 [HIGH] Demo payoff seeding. Round 1 (documented re-scan of the 3 .ph41 articles, "which share entities"): DON'T-KNOW — evidence check REFUTED the claim (the 7 fixture articles are disjoint enforcement cases, zero cross-article entity overlap). Round 2 (A2': seeding is a small deliverable — same-article re-scan for accumulation/provenance PLUS a committed SYNTHETIC investigation note referencing a fixture entity [client_number + one deliberately conflicting property] pasted as source_type=investigation-note; note committed clearly-labeled synthetic per the book.json precedent, the resulting store stays local/gitignored): ACCEPT. revisit-status: bit (round 1) then held as A2' — the don't-know was the right position: the evidence check REFUTED the cross-article-overlap claim PRE-implementation; the revised synthetic-note seed CONFIRMED live at the T4 spec checkpoint (real Qwen: 2 scans w/ source-type provenance, client_number C-2024-0117, phone conflict flagged both-kept).
- A3 [MED] Integrated shape: the per-scan graph is the NAVIGATION surface (rendered at Disposition, replacing the Phase-41 text subject map); node click — or a watchlist row — opens that entity's anchor dossier (GET /anchor → anchor_summary) as the detail view; conflicts surfaced presentation-only (same kind, >1 distinct value → flagged, both kept, never auto-resolved — Phase-41 D2). ACCEPT. revisit-status: held — graph-as-navigation shipped at Disposition; node→dossier AND watchlist→dossier (wdoss) both wired; conflicts rendered "both kept", presentation-only.
- A4 [MED] Rendering tech: hand-rolled vanilla SVG, DETERMINISTIC layout (radial initial placement + fixed-iteration relaxation, no Math.random, no vendored lib; node counts ≤ ~35). ACCEPT WITH CONDITION — SVG is the INITIAL implementation for demo testing; revisit rendering tech (richer interaction / vendoring) if the live tool grows past demo scale. revisit-status: held (condition recorded) — SVG-initial shipped, deterministic layout harness-pinned; reviewer noted the min-gap pass can wrap a dense outer ring's last node past the first (harmless ≤35 nodes) — fold into the rendering-tech revisit if the tool grows.
- A5 [scope constraint, agent-verified] Pure consumption phase: no change to news_ground (shared gate), EXTRACT_SCHEMA/SYSTEM_PROMPT, store WRITES, or replay fixtures (no re-capture); investigation-note source_type already exists (Phase 41); anchor_summary already built. Recorded as a contract constraint, not a positioned assumption. revisit-status: held — pure consumption verified by the reviewer via git diff: news_ground.py / build.py / store writes / replay fixtures untouched; no re-capture.

## Phase 43 — Live pipeline robustness + progressive presentation (2026-06-10)

all_accept: false (direction itself was a user REFRAME off the offered candidates — a live test on a REAL investigation note timed out ~200s with no intermediate feedback; A2 rejected-by-reframe in round 1 → revised A2' accepted)

- A1 [HIGH] The live failure is LIMITS-class — the 180s call_llm read timeout, max_tokens=4096 truncation, or context overflow from the default llama-server launch (docs set no --ctx-size) — and a synthetic stress corpus at size/complexity tiers can reproduce it; NOT intrinsic model degradation on complex notes. If false: streaming/limit fixes yield a slow bad extraction and sectioned extraction becomes the main fix. ACCEPT (T1 measures + classifies before T2–T4 commit). revisit-status: held — T1 reproduced BOTH limits-class causes (OUTPUT-BUDGET primary at ~10+ entities / 4096 tokens, READ-TIMEOUT secondary at >180s once the budget was raised); intrinsic degradation REFUTED (planted recall perfect at every tier, head+tail); context overflow unreachable at the probed n_ctx=262144.
- A2 [HIGH] Progressive rendering shape. Round 1 (token-stream partial-JSON parse renders red flags mid-generation): REJECT-BY-REFRAME — user doesn't want visible token streaming/agent-thinking; the article content is static, a corpus-demo-style progressive reveal is the desired effect. Round 2 (A2': STAGE-COMPLETION-driven rendering — converted text → grounded red flags + provisional entities reveal as soon as grounding completes [early in wall-time; the verify loop is the measured majority] → entities refine live through verify i/N → final record; internal stream:true in call_llm serves ONLY the timeout fix + an elapsed/token progress counter, no partial-JSON parsing, no visible token stream): ACCEPT. revisit-status: held — stage-completion rendering shipped exactly as revised (grounded flags FINAL + provisional chips refined through verify + token counter; no token stream, no partial-JSON parse); verified live end-to-end (demo note: grounded at 18s, verdicts streaming).
- A3 [MED] Showing mid-pipeline content before the verify pass completes is acceptable when honestly staged — red flags render GROUNDED (the gate has already disposed by then); entities render provisional and refine as verify drops non-subjects; the final record remains the only persisted output. ACCEPT. revisit-status: held — red flags render post-gate (grounded) at the grounding-complete event; entities provisional until verified; the final record is the only persisted output; the 'persisting' probe event guarantees disconnect-before-done persists nothing.
- A4 [MED] Pure-transport scope: streaming stays INSIDE call_llm (same signature, full-text return → the 13 replay fixtures green, NO re-capture); news_ground / SYSTEM_PROMPT / EXTRACT_SCHEMA / store writes FROZEN; all client changes in the LIVE region; offline dist/news byte-identical (--check all 5/5). Sectioned extraction, IF earned at T4 by measurement, feeds section text without prompt/schema change — any prompt unfreeze is a surfaced finding, not silent drift. ACCEPT. revisit-status: held — pure transport verified by the reviewer via git diff (news_ground/EXTRACT_SCHEMA/SYSTEM_PROMPT/store writes/fixtures/news_fetch/build.py untouched); 13/13 replay green ZERO re-capture; offline dist/news byte-identical (--check all 5/5); sectioned extraction not earned, not built — no prompt/schema unfreeze needed.

## Phase 44 — Live extraction quality: targeted harness, classified fixes, processing page (2026-06-10)

all_accept: false (direction itself was a user REFRAME off the offered candidates — live extraction quality: missed high-risk-country wire flags + wrong alias assignments + a fresh processing page + "do we need a better specific harness?"; A1 accepted WITH conditions [sample sentences only + speed surfaced as a concern → quality-gated optimization task added]; A3 don't-know in round 1 → defended with the Phase-40/41 seam precedent → accepted)

- A1 [HIGH] The two reported failures REPRODUCE on constructible material — synthetic high-risk-country wire notes + local commercial articles, seeded with the user's failing examples. If false: we tune against a proxy and may fix the wrong thing (the Phase-40 r3 regression precedent). ACCEPT WITH CONDITIONS — (1) the user can provide SAMPLE SENTENCES only (not full notes): the harness embeds them into synthetic note scaffolds; kept local/gitignored, never committed; (2) processing SPEED surfaced as an additional concern ("don't know if we can optimize without losing quality") → resolved as a quality-gated optimization task: T1 measures per-stage wall-time, optimization lands ONLY where the harness proves quality holds. revisit-status: held — the failures REPRODUCED on constructible material (synthetic wire notes: 4/5 planted wire facts dropped on wrapped prose; the user's SEEDED STR sample sentences confirmed the gate-drop class on the real register, 3/5 dropped → 0 after the fix); the speed condition resolved as the T4 MEASURED skip (the hotspot IS model generation, 92–98% of wall on note-register input — no quality-preserving lever at a fixed model; slot-parallel verify + harness-gated smaller-model eval named as future levers).
- A2 [HIGH] The failures are CLASSIFIABLE by the harness into actionable causes — flags: missed-at-generation vs dropped-at-gate (grounding drop / dup-collapse) vs registry blind spot (the SYSTEM_PROMPT registry already names high-risk-jurisdiction as a family); aliases: model-generated bad alias vs deterministic fold misparent (token-subset or _adjacent_parent moniker). The fix follows the class; if ambiguous, eval rounds burn. ACCEPT. revisit-status: held — classification was DECISIVE: flag misses = GATE-DROP (raw-substring grounding broken by hard line-wraps + `*`-strip asymmetry + title-line quotes), NOT model recall (5/5 on single-line bullets); alias misassignment = predominantly DETERMINISTIC fold misparent (rule (a) proven order-dependent + type-blind), not model aliases; each fix followed its class exactly (T2 locate_span requote; T3 ambiguity-refusal + type-match); the constructed P1c wrong-owner model alias stayed a MEASURED residual (the harness alias_suspects dimension watches it; zero live evidence).
- A3 [MED] The fixes stay within KNOWN seams — prompt iteration through the existing prompt-regression gate (red_flags-FIRST schema order preserved, never-reduce guard); news_ground/fold changes through the known regate procedure (4 committed records + 13 replay goldens regenerate deterministically, NO re-capture); a structural EXTRACT_SCHEMA change surfaces as a finding first. Round 1: DON'T-KNOW → defended with evidence (Phase 40 rewrote SYSTEM_PROMPT, Phase 41 changed EXTRACT_SCHEMA + news_ground — both zero re-capture, regate triple green both times). Round 2: ACCEPT. revisit-status: held — the locate_span wrap-tolerant requote + the fold ambiguity/type rules landed through the KNOWN seams: 13/13 replay fixtures green ZERO re-capture (the goldens were not even regenerated — no pinned capture had wrap-broken quotes; the gate change is purely additive on the live path); the 4 committed records pass; no EXTRACT_SCHEMA change was needed (no finding to surface).
- A4 [MED] The processing-page UX + hygiene trim are mechanically bounded — the fresh page is a LIVE-region-only screen (run-extraction navigates there; the Phase-43 staged rendering moves onto it; offline dist/news byte-identical via the strip); the hygiene trim is lossless archival (closed tasks.md blocks + over-cap state files → articles/journals with pointers). ACCEPT. revisit-status: held — the processing page shipped LIVE-region-only (in-page #liveproc takeover; offline dist/news byte-identical, --check all 5/5 zero drift); the hygiene trim was lossless (reviewer-verified: 22+2=24 baseline blocker entries accounted for; 37 tasks.md phase blocks archived VERBATIM with pointer indexes).

## Phase 45 — Corpus demo presentation polish (pre-presentation day) (2026-06-10)

all_accept: false (A1 don't-know in round 1 — "give an example for both in text" → defended with worked examples carrying REAL corpus values (fin-2024-a002: 28 covered synthetic-opioids indicators × 2 regulators) → R2 accepted in round 2)

- A1 [HIGH] Lift-beat replacement: "concept-focused" still admits REAL numbers — R2 renders honest INVENTORY counts (covered indicators in the committed signal's typology × contributing regulators, computed client-side at render, same honesty class as every lens) as the composition search space; NO performance/lift claim of any kind; the "Illustrative · pending calibration" disclaimer is REMOVED because nothing on the screen is fabricated. Small/single-regulator typologies degrade honestly (e.g. elder: "5 covered indicators published by FinCEN"). Round 1: DON'T-KNOW → worked R1 and R2 examples written with real values. Round 2: ACCEPT (R2; R1 zero-numbers ladder remains the copy-only fallback if R2 reads wrong at the T5 walkthrough). revisit-status: held — R2 confirmed by the user at the T5 walkthrough (the worked-examples defense was the right call); honest small-N degradation copy carried the edge cases and the corpus-wide fallback was never needed on real docs; R1 stayed designed-but-unused; the harness pins the counts by INDEPENDENT RECOMPUTATION from the committed data files (which killed a real bare-vs-doc-qualified overlay-key bug on first contact).
- A2 [HIGH] Showcase divergence: index.html Act-5 keeps its illustrative lift figures (byte-frozen, protected by the non-negotiable wow-beat clause + corpus-is-the-primary-demo); the corpus-only removal creates a deliberate cross-artifact divergence. ACCEPT. revisit-status: held — showcase index.html + the 3 typology dists byte-identical in `--check all` 5/5; Act-5 untouched; the divergence stands as gate-accepted (a future true-up candidate only if the showcase is ever presented again).
- A3 [MED] Human-gate fix = copy + stagecraft, no interaction redesign tonight: pre-selection behavior stays; lead copy reframes to "the agent has PROPOSED all N — deselect to dispose"; the presenter visibly deselects 1–2 live (also tames the 30-card Signal screen). ACCEPT. revisit-status: held — copy-only landed (no interaction change); the deselect stagecraft is scripted in tests/smoke-checklist.md; the interactive second gate stays a named DEFERRED item.
- A4 [MED] FINTRAC attribution gap (the user's compliance call): verbatim FINTRAC excerpts on the Capabilities/Data-sources lens screens get the SAME relocated footer-attribution mechanism extended to those views (Phase-28 lineage) — verbatim-with-attribution held, not suppressed. ACCEPT. revisit-status: held with an UPSIDE — the spec's adversarial-constraints pass surfaced that the licence attributes PER REPRODUCED WORK, so the extension landed MULTI-DOC (the footer lists every contributing FINTRAC doc, re-derived per render; C1 lists 18 docs) — licence-complete where the planned single-doc footer would not have been; synthesis drill stays silent (titles/counts only, the Phase-28 precedent); harness asserts US-only-empty.
- A5 [MED] Breadth: GLOBAL polish (all HIGHs + ranked MEDIUMs artifact-wide) + curated demo-path notes in smoke-checklist — not a single hardened route; demo docs not yet named by the user → a route recommendation (one FinCEN advisory + one FINTRAC OA) lands at the T5 walkthrough. ACCEPT. revisit-status: held — global polish + the curated route notes landed in smoke-checklist (recommended route, deselect stagecraft, zero-build-now docs to avoid, fonts cache warm); the user's walkthrough feedback (2 items + 2 refinement rounds) was absorbed within the day and FROZEN with overflow explicitly deferred.

## Phase 46 — Corpus live derivation mode: local-model agentic derivation through the frozen gate (2026-06-10)

all_accept: true (warned + restated per gate protocol — the direction itself was a user REFRAME off all five carried DEFERRED candidates, the 5th reframe at 6 consecutive gates: "live mode with local model for the corpus demo as well, ideally with a better harness integration like opencode" + a pasted opencode/local-model practitioner report; the reframe carried the user's intent so positions landed accept)

- A1 [HIGH] The reading: Phase 46 = a live CORPUS DERIVATION mode (advisory md/URL → local-model agent derives indicators + C/D tags → the existing frozen derive_signals.py check_record gate disposes → only gate-green output renders into the corpus arc), companion-served dev/authoring-time like news live; opencode's role is the local-model agent RUNTIME for that derivation loop, NOT general dev tooling (maker/checker for building signal-watch itself). If false: the whole task list is wrong. ACCEPT. revisit-status: held — the phase shipped exactly as read: serve_corpus.py companion + corpus.html /*LIVE_*/ region, the FROZEN check_record gate disposing (live E2E 17 kept / 0 dropped, independent re-gate 0 violations); opencode was evaluated strictly as the derivation-loop runtime at T1, never as dev tooling (it stays installed for dev use, outside the phase).
- A2 [HIGH] Probe-gated harness: opencode adoption is decided by the T1 measure-first probe, not upfront — it must beat the direct serve_news-pattern pipeline on the real derivation task (gate pass rate, iteration count, wall time, tool-call reliability); if local-model tool-calling is unreliable, the phase still ships live corpus mode on the proven pattern. If adopted blind: the report's own caveats (version churn, headless permission bugs, quant-sensitive tool calls) become the schedule. ACCEPT. revisit-status: held — the probe gate WORKED as designed: opencode matched quality exactly (identical 17/17 indicators, both gate-green FIRST shot, 0 violations, 0 tool-call failures) at 3.1× wall (255.1s vs 82.6s); its differentiator (iterate-on-gate-failure) NEVER ENGAGED, so it did not earn its complexity → direct pipeline + ONE violation-guided retry chosen at the user checkpoint (the loop's one real idea folded in deterministically). Note: the retry path has no live exercise yet — both probe docs passed first-shot.
- A3 [MED] Model + material: probe with the EXISTING llama-server Qwen serving first (running at 127.0.0.1:8080, news-proven); held-out derivation material = a FINTRAC /intel/ frontier doc (composes with the deferred C1 candidate — its licence/heading-form sub-questions travel with it); model swap (Qwen3-Coder-Flash / GLM-4.7-Flash class per the user's report) only on measured tool-call-class failure. If false (model shopping first): scope explodes before anything is measured. ACCEPT. revisit-status: held — the swap clause never fired: the existing llama-server model measured as Qwen3.6-35B-A3B-UD-Q4_K_XL (agentic-tuned MoE class, --jinja confirmed via chat_template_caps, n_ctx 65536/slot) and carried both harnesses; held-out FINTRAC-2024-OA001 (tax-evasion real estate OA) parsed CLEAN under the EXISTING rf_region anchors (region (220,376)) — no anchor extension; the doc stayed LOCAL-ONLY, its committed-extension sub-questions still travel with the deferred candidate.
- A4 [MED] Boundaries: offline dist/corpus stays BYTE-IDENTICAL via the news-style /*LIVE_*/ build strip (live mode optional/isolated/off-by-default, scripted fallback — the non-negotiable); live-derived records are DISPLAY/PROPOSE-only — committing any new derived record to data/ remains a separate human-reviewed act under the existing licence rules; the 2026-06-11 presentation outranks the phase — nothing presentation-touching moves before it (--check all 5/5 enforced before any commit). ACCEPT. revisit-status: held — dist/corpus BYTE-IDENTICAL through the live-region addition (--check all 5/5 zero drift; the render_corpus strip works); live output DISPLAY/PROPOSE-only (session-only, labeled UNREVIEWED, nothing persisted — no store exists); nothing presentation-touching moved before 2026-06-11; no new record committed to data/.

## Phase 47 — Demo-to-program design: the regulatorily defensible agentic AML program (2026-06-12)

all_accept: false (the direction itself was a user REFRAME at the dev-plan gate — the 6th in 7 gates: demo presented 2026-06-11 was "a huge success" → design how the demo's pipeline patterns become a REAL efficient/effective/regulatorily-defensible program, ~95% agentic / 5% high-judgment human work; A1 REJECTED-BY-REFRAME in round 1 → revised A1′ accepted in round 2 — the user's reframe BECAME the design spine)

- A1 [HIGH] Round 1 (the demo's quote-grounding gate class does NOT transfer to no-source-text workloads; the spine = a gate taxonomy deriving a different gate class per workload): REJECT-BY-REFRAME — the user: "there's some level of source text to ground to anywhere… transaction monitoring will be built on those signals, the quotes then are the signals and the underlying data, transactional and non-transactional… SAR/STR narratives need to be grounded to the guidance, policies, signals, and data." Round 2 (A1′: grounding is UNIVERSAL as the architecture principle; what varies per workload is the grounding SUBSTRATE — advisory text [substring verifier] → signals + transactional/non-transactional data [referential/lineage verifiers] → guidance/policies/signals/data for SAR-STR narratives [citation verifiers]; grounding CHAINS, defensibility = the audit walk down the chain, nothing ungrounded survives a gate; each workload's substrate + verifier NAMED in the blueprint, never assumed): ACCEPT. revisit-status: held — the spine shipped AS REFRAMED: blueprint §2 states the universal grounding principle, the 5-workload × 4-cell substrate/verifier table is all-cells-filled (RED 5/0 → GREEN 5/5, deterministic table check), grounding-chains/audit-walk written as the defensibility argument; no workload turned up without a nameable substrate (the FINDING escape clause never fired).
- A2 [HIGH] Repo role: this repo remains the VISION LAB + design source-of-truth — Phase 47 produces the blueprint + the 4th demo-idiom artifact (gate console); the real program build is a future/elsewhere effort; ship artifacts + committed data/dist FROZEN. ACCEPT. revisit-status: held — design artifacts only (blueprint + spec + console vision artifact); no program build started; existing 3 ship artifacts + dists BYTE-IDENTICAL through the phase (--check all 6/6, reviewer re-verified).
- A3 [MED] Audience/register: no live bank engagement/procurement dictates the deliverable's register or deadline — the blueprint is engineering-first; a stakeholder digest derives in a later phase. ACCEPT. revisit-status: held — no external clock surfaced during the phase; the blueprint stayed engineering-register; no digest produced.
- A4 [MED] Honesty applied to the headline: "95/5" enters the blueprint as a design DIRECTION (automate-by-default, judgment-by-design), never a stated target ratio; any ratio carries its future measurement definition (decision-volume share per gate class) — the Phase-45 fake-lift deletion pattern applied forward. ACCEPT. revisit-status: held — blueprint §10 explicitly REFUSES a ratio target on design grounds; the T3 adversarial grep (no sentence states 95% as a target) ran green; the only numbers in the blueprint are real measured ones (3.1×, 5.5×).
- A5 [MED] Charter breadth: the 5% human charter includes a MANDATED-ACCOUNTABILITY class (SAR sign-off, board/model-governance attestations, three-lines-of-defense roles — compulsory under SR 11-7 Pillar 3 regardless of judgment-need) alongside the art-like judgment work; an interesting-only charter under-counts the human layer and leaves an examiner-visible defensibility hole. ACCEPT. revisit-status: held — the dual-stream charter (§5) names both judgment AND mandated-accountability work w/ 4 invariants; Class A sits in the 4-class gate taxonomy; the SAR/STR workload row carries it.

## Phase 48 — Brownfield history + LFCM: blueprint extension, triage-elicitation loop, synthetic-history probe (2026-06-12)

all_accept: false (A1 accepted WITH a condition; the direction itself is the user's own planning input — the blueprint's greenfield gap [TM/investigation/filing history unaddressed] + the LFCM idea [10^3–10^4 grounded signals assisting FC-risk judgment] + the mini-triage elicitation loop idea raised at the Step-9 questions)

- A1 [HIGH — the T0 weakest assumption] The triage-elicitation loop is designed so its value does NOT depend on analyst-judgment convergence: gap discovery (need-more-info wired to the C/D coverage model) is the value floor, calibration stats the upside, agreement itself a first-class measured dimension (divergence routes to adjudication — the console pattern). If false, the loop's design center moves. ACCEPT WITH CONDITION — the scenarios are sourced from REAL institutional history (alerts/cases/filings replayed as mini-triage scenarios): historical decisioning is ground truth about DECISIONS, never about correctness; and the loop's discovery outputs explicitly include PROCESS INCONSISTENCIES and POLICY GAPS alongside signal/data gaps. revisit-status: held — the loop designed gap-discovery-first in §14 (need-more-info wired to the C/D coverage model = the value floor); the CONDITION honored: history-sourced scenarios + process-inconsistency/policy-gap discovery written into the section; convergence stays an at-adoption measured open question (the revisit hook).
- A2 [HIGH] LFCM's regulatorily survivable architecture = a grounded signal LIBRARY + a small composition layer — the library IS the model inventory (per-signal lifecycle, tiered validation); "LFCM" stays the program-level name, never a single Tier-1 mega-model as the validation unit. If false, the LFCM section is wrong. ACCEPT. revisit-status: held — §13 shipped library-not-monolith: the grounded signal library IS the model inventory (per-signal lifecycle, tiered validation); LFCM kept as the program-level name; 5 named failure modes incl. the monolith trap.
- A3 [MED] History enters the blueprint as THREE distinct roles — derivation substrate / §6 probe baseline (legacy TM = the A/B comparator) / outcome-feedback embryo — under "history is evidence, never ground truth" (it encodes the legacy program's blind spots), with §8's deferred rows RE-DISPOSITIONED (vision-lab-deferred vs adopter-available-with-caveats), not duplicated. ACCEPT. revisit-status: held — §12 carries the three named roles each with substrate+verifier + the doctrine line; §8 rows re-dispositioned IN PLACE (vision-lab-deferred vs adopter-available-with-caveats), not duplicated; adversarial greps green.
- A4 [MED — silent infrastructure class] The probe executes as a fully SYNTHETIC, advisory-SHAPED legacy rulebook derived through the EXISTING FROZEN gate (zero gate changes; the shape caveat stated honestly in the writeup — real rulebooks may need the regression-gated anchor-extension path) + a stdlib stats script over synthetic alert/disposition history (incl. the disposition-inconsistency rate, every number with its measurement definition); outputs committed OUTSIDE every build.py-read path; all 4 dists byte-frozen (--check all in exit criteria); the two stale blueprint counts (§3 "five workloads", §12 "three ship artifacts") fixed in scope. ACCEPT. revisit-status: held — probe gate-green 12/12 FIRST shot through the byte-unchanged gate (git diff --quiet derive_signals.py held; zero check_record violations); outputs outside every build.py-read path; all 4 dists 6/6 zero drift; both stale counts fixed in scope + the reviewer's third stale count ("five implementations") caught and fixed post-review.

## Phase 49 — Triage-loop embryo made demo-able: §14 as the 5th ship artifact (2026-06-12)

all_accept: false (A2 took a don't-know round 1 → defended with the Phase-48 ledger verbatim + the console-cases curation precedent → accepted round 2; the direction itself = the user picked the offered §14-embryo candidate at the Step-9 direction question — the first non-reframe pick in 3 gates)

- A1 [HIGH] The triage-loop embryo is DEMO-FIRST — an artifact to present (bank-audience class), not an internal instrument; the loop's defining beat (analyst judgment vs the institution's historical decision, decisions-not-correctness) needs replayed history, which only the synthetic probe data carries. If false, the right build is instrument-first (extend the 213-case console over real divergences) and the synthetic scenario stream is the wrong deliverable. Instrument-first explicitly noted as the natural follow-on (sequencing, not dismissal). ACCEPT. revisit-status: held — the demo-first artifact was built and is presentable (full Queue→Evidence→Disposition→Reveal→Ledger arc, dist/triage single-file offline); instrument-first stays the noted natural follow-on.
- A2 [HIGH — silent infrastructure class] A NEW committed dataset data/triage/scenarios.json (deterministically curated from data/probe-history + US-federal-only corpus indicators) may be a build-read input for the NEW triage target — the console-cases layering: curate script reads probe-history at AUTHORING time only; build.py reads only the committed, build-boundary-validated scenarios.json; probe-history itself stays outside every build path; nothing enters __CORPUS__ (the Phase-48 A4 object untouched). DON'T-KNOW round 1 → defended (Phase-48 A4 verbatim scopes the probe's OWN files + __CORPUS__; data/console/cases.json precedent "validation never reads git"; §14 itself prescribes history replayed as scenarios) → ACCEPT round 2. revisit-status: held — the console-cases layering implemented exactly: curate script reads probe-history at AUTHORING time only; build.py reads only the committed build-boundary-validated scenarios.json (probe-history in build.py comments ONLY — claim-shaped code-grep verified); nothing entered __CORPUS__.
- A3 [HIGH — the T0 weakest assumption] Authored synthetic evidence panels can be made BELIEVABLE within the ~16-scenario + ~4-control ceiling: the 44 alert stubs carry no judgable fact pattern, so panel authoring IS the bulk of the phase; mitigated structurally (D7: template-derived skeletons from rule logic + stub fields; one panel per fact pattern shared BY REFERENCE across divergent-disposition pairs — the process-inconsistency beat build-validated, never an authored coincidence). If false, scenarios read thin, the disposition gate feels arbitrary, the beat dies. ACCEPT. revisit-status: held (structural) — the D7 mitigation built as designed (template-derived skeletons, ONE panel per fact pattern shared BY REFERENCE across the divergent pair, the 16+4 ceiling held without the split valve); believability itself is adjudicated by the user at the delivery gate (smoke-checklist "read 4 panels, one per stratum" row + a verbatim scenario in the delivery report).
- A4 [MED — either/or] Ship-class discipline is the right cost: a FIFTH ship artifact (triage.html → dist/triage, build target wired into all/--check, dedicated node harness, smoke-checklist row) on the console precedent — an interactive artifact presented without a harness is the riskier alternative; the non-ship docs/ shape (booth/blueprint-report precedent) was surfaced and declined. ACCEPT — 5th ship artifact. revisit-status: held — the 5th ship artifact landed with full discipline: `triage` build target wired into all/--check (7 targets, zero drift), 4-class tamper validation fail-loud, dedicated 93-assertion harness, smoke-checklist row.

## Phase 50 — AML program build kickoff: data-substrate pillar spun out (2026-06-12)

> Direction = a major user REFRAME at the dev-plan gate: from incremental signal-watch phases to BUILDING THE REAL AML PROGRAM (blueprint §3 design-stage workloads → real systems), **spun out one repo per pillar** (real-system-class). Pillar 1 = the data substrate → new repo `/Users/jwang/aml-substrate` (design in its DESIGN.md). signal-watch stays the program-architecture home. Pre-gate framing forks: first-build = data substrate (the keystone with no embryo); housing = real-system-class spin-out per pillar; engine = hybrid (own data model + Canadian/EMT/FINTRAC; reuse AMLSim plumbing; learn AMLgentex calibration); phase-1 shape = foundation-first. Interview (Jake's institutional knowledge) + 3 parallel research agents (generators · real schemas · distributions/topology/benchmarks) grounded the design.

all_accept: false (A3 rejected → revised to small-first; A2 = user override of the offered review-first mitigation)

- A1 [HIGH — T0 weakest] Foundation-first delivers an expert-credible phase-1 artifact even with no laundering in it yet, AND the faithful substrate won't need rework when emergence lands in phase 2. ACCEPT — mitigation: data model built EMERGENCE-READY (criminal-agent + transitive-label hooks in the schema from day 1). revisit-status: held — emergence-ready hooks (latent_role/illicit_income/laundering_label/illicit_flow) built INERT in P1; 54 tests confirm phase-2 emergence needs no schema migration. A1 mitigation proven.
- A2 [HIGH] Researched Canadian schemas/distributions are faithful enough to build phase 1 on directly (several flagged "representative, not canonical" — EMT field names, account-role legal rights, the σ values as design choices). Offered mitigation = a Jake-review checkpoint on the spec before generation; USER OVERRODE → build on research directly, corrections surface during build. ACCEPT (build directly). revisit-status: held (P1) — built directly on the researched schemas/distributions; channel medians landed on the Canadian anchors (EMT $400/card $46/cheque $400/retail-wire $8k), no fidelity blocker surfaced. Several fields remain "representative not canonical" (EMT names, account-role legal rights) — to be confirmed as the data is used.
- A3 [HIGH] 1M-client scale tractable scripted in phase 1. REJECT → parameterized SMALL-FIRST: build + validate at 10k–100k, scale the same engine to 1M later; scale is a parameter, never assumed. revisit-status: held — small-first VALIDATED: perf probe linear ~58us/client / ~5.5us/txn, stable 1k→200k, ~161s projection to 1M clients (1 month). Scale is a CLI parameter; byte-identical deterministic regen.
- A4 [MED] AMLSim flow/topology plumbing bends to the fresh Canadian/EMT model (hybrid saves real work vs collapsing to fresh). ACCEPT, eyes open — re-evaluate after a reuse spike. revisit-status: DEFERRED to phase 2 — the AMLSim flow/topology reuse is exercised by the EMERGENCE engine (phase 2), not the foundation. P1's data model + population + background engine are genuinely fresh (AMLSim has no foundation layer to reuse), so the hybrid-vs-fresh question is untouched and still open for the laundering flow layer.

## Phase 51 — Corpus redundancy measurement (the §13 fm-1 frontier, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's first deliverable (post-triple-null: composition's surviving value includes redundancy-management, real on the corpus). The OBVIOUS approach (cluster by semantics, report an overlap fraction) violates the LOAD-BEARING no-similarity/no-dedup non-negotiable. User gated the HYBRID (over deterministic-only [in-constraint but marginal vs the existing synthesis lens] and pivot-to-C/D-correctness): a NON-SHIP artifact = deterministic label co-occurrence UPPER BOUND + a sampled consensus-class semantic-equivalence estimate.

all_accept: false (the offered redundancy framing was rejected on honesty-constraint grounds → re-scoped to the hybrid non-ship measure-not-claim design before the gate)

- A0 [HIGH — the load-bearing honesty assumption] The hybrid stays IN-CONSTRAINT: the ship corpus (corpus.html/dist/corpus/all derived) stays byte-frozen + read-only; the deterministic part is honest union/co-occurrence arithmetic over EXISTING committed labels (the class CLAUDE.md:204 allows); the semantic-equivalence number is SAMPLED + blind + consensus-class + illustrative (the Phase-34 inter-rater doctrine) in a NON-ship artifact — NOT a re-open of "the ship artifact computes no similarity," NOT a validated dedup/lift. ACCEPT. If false (any similarity/dedup/lift figure lands on a ship artifact, or the semantic number is presented as ground truth) → STOP, it's the overclaim the whole thesis forbids. revisit-status: HELD — stayed in-constraint end to end: `--check all` 7/7 zero drift (ship corpus byte-frozen), the T2 number is single-rater/consensus-class/illustrative with consensus deferred, framed candidate/ceiling/estimate, the only dedup mentions are disclaimers; data/corpus-redundancy/ is non-corpus (build never reads it).
- A1 [HIGH] Typology resolution (per-indicator override in indicator-typology-map.json else doc-level inherit from typology-map.json) is deterministic and gives meaningful per-indicator coverage for the (typology×capability) cell. ACCEPT. If false (the overlay is too sparse — fintrac-guidance is 1,283 of 2,251 and the per-indicator map is sparse, so the axis collapses to doc-level inherit) → fall back to (capability × regulator) co-occurrence and report the coarser axis honestly (named in the abort rule). revisit-status: HELD — 0 unmapped (350 per-indicator override + 1901 doc-level inherit); the (typology × capability) axis was viable, the fallback was not needed.
- A2 [HIGH] A (typology × capability) cell is a meaningful candidate-redundancy proxy — co-occurrence in a cell across regulators ≈ an honest UPPER BOUND on redundancy. ACCEPT. If false (the cell is too coarse — many distinct indicators share a cell) → the upper bound is uninformatively loose and the sampled semantic estimate (A3) carries the real signal; report the bound as "loose ceiling," lean on the sample. revisit-status: BIT (as designed) — the cell IS a loose ceiling (0.325 co-occurrence vs ~1.4% strict genuine equivalence); the gap is exactly the finding (shared label = distinct facets), and T2 absorbed it. Reported as a ceiling refined by the sample, not as redundancy-as-fact.
- A3 [MED] A blind semantic-equivalence sample over cross-regulator co-occurring pairs yields a stable, defensible consensus equivalence rate. ACCEPT. If false (the sample is noisy / raters diverge widely) → report the deterministic upper bound ONLY and surface the divergence as the finding (consensus failure is itself honest). revisit-status: HELD (single-rater) — the sample produced a clear, integrity-verified distribution (1 equiv / 15 partial / 8 distinct), decisive enough to ground the finding; consensus (≥2 raters) + a larger sample stay deferred-with-owner as planned.
- A4 [LOW] The right home is a NON-ship analysis artifact in signal-watch (the blueprint-report.html / probe-history.md precedent — non-ship analysis already lives here), not a new sibling measurement repo. ACCEPT. If false (the measurement workstream wants its own repo/owner) → lift the script + report into a sibling, reading signal-watch's frozen corpus read-only. revisit-status: HELD — the blueprint-report.html / probe-history.md non-ship-analysis precedent fit cleanly; build.py never reads data/corpus-redundancy/; --check all unaffected.

## Phase 52 — C/D-tag reliability measurement (the unguarded dimension, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's SECOND deliverable (the sibling of Phase 51): the C/D dimension is the one the grounding gate explicitly never checks (CLAUDE.md: "a grounding gate ≠ a completeness gate ≠ a correctness gate"; working-knowledge: "that dimension is unguarded"). User gated the STRATIFIED frame (over 213-divergences-only and random-only): a NON-SHIP analysis = a blind same-model re-rate over a RANDOM corpus stratum + the 213 Phase-34 divergences as a labeled HARD stratum, reported as consensus / self-consistency, never validated correctness.

all_accept: true (the load-bearing set A0/A1/A2 accepted as stated; warn-tracked. A0 reshapes the random headline from "reliability/correctness" to "reproducibility/self-consistency"; A1 forces the blind-by-construction dump so the orchestrator never hand-rates; A2 fixes the illustrative-first n=24/seed-0 posture with consensus + larger-N deferred-with-owner; A3 is the standing non-ship byte-frozen abort rule.)

- A0 [HIGH — the load-bearing honesty assumption; T0 weakest] A same-model blind re-rate of C/D measures REPRODUCIBILITY / self-consistency, NOT validated correctness — the rater shares the original extractor's systematic biases, so agreement is necessary-not-sufficient for correctness. ACCEPT. If false (we headline the random number as "the C/D dimension is X% correct/reliable") → overclaim, the measure-not-claim violation Phase 51's A0 forbids. Mitigation: label the random number self-consistency; the divergence stratum (a real two-pass disagreement) is the closer-to-independent number; genuine independent-rater consensus (different model family / human) + larger N deferred-with-owner. revisit-status: HELD (with a post-delivery tightening) — the report labels the random number REPRODUCIBILITY / self-consistency throughout (never "correct"), the finding leans on the divergence stratum, independence is deferred-with-owner; honesty grep clean. NOTE: a post-delivery adversarial review (of the aml-wiki capture) found the report's FIRST finding sentence ("the gap = adjacent-code scatter, NOT error") OVERCLAIMED — the random + divergence strata are different samples/tasks/metrics, NOT subtractable, so the defensible-vs-error share is unmeasured; the level is also inflated by the forced-choice format and at n=24 the two rates are statistically indistinguishable. Tightened in docs/cd-correctness-report.md to "two separate non-subtractable measurements; defensible scatter plausibly one driver, share unmeasured" + a single-rater / no-chance-correction / overlapping-Wilson-CI caveat. A0 still HELD (no number was ever presented as correctness), but the gap-as-decomposition was the exact spot it nearly slipped — the catch validates the measure-not-claim discipline.
- A1 [HIGH] Blindness is preserved BY CONSTRUCTION — the rater (a subagent) never sees the committed code (random stratum) or which option is the Phase-34 correction (divergence stratum, order fixed by seed); the script dumps blind samples, the orchestrator does NOT hand-rate. ACCEPT. If false (blindness leaks — the rater sees the answer) → the agreement number is meaningless. Mitigation: the dump format excludes committed codes; divergence options presented neutrally with seed-fixed order; judgments committed as a replayed fixture (the Phase-51 / Phase-34 pattern). revisit-status: HELD — both blind subagents ran tool_uses: 0 (no repo file read at all); the dumps excluded committed codes (random) / which-is-correction (seed-fixed sha256 order, divergence); --verify-fixtures VERIFIED both against the seeded samples.
- A2 [MED] n=24 per stratum, seed 0 is an adequate ILLUSTRATIVE first instance (chosen-not-derived, the Phase-51 T2 posture) — the deliverable is the reproducible machinery + an honest first instance, not a final number. ACCEPT. If false (too small / too noisy to ground a finding) → report the distribution as illustrative + surface the noise; larger N + consensus already deferred-with-owner. revisit-status: HELD (noise noted) — n=24 produced a decisive-enough first instance (62.5%/axis · 70.8% uphold); the random both-axes 0.417 is the noisiest cell at this N and is labeled illustrative; larger N + consensus stay deferred-with-owner as planned.
- A3 [LOW] NON-ship guard + abort rule: the ship corpus (corpus.html / dist/corpus / all derived / the overlays) stays byte-frozen + read-only; build.py NEVER imports cd_correctness.py; data/cd-correctness/ is non-corpus; `--check all` 7/7 zero drift. ACCEPT. If false (any ship-artifact drift) → STOP and surface (the standing abort rule), never re-baseline. revisit-status: HELD — --check all 7/7 ZERO drift; build.py has no cd_correctness / cd-correctness reference; data/cd-correctness/ is non-corpus (build.py never reads it).

## Phase 53 — C/D disagreement decomposition (error vs defensible-neighbor, measured honestly) (2026-06-16)

> Direction = the measure-first workstream's THIRD deliverable (finishes Phase 52, not a new dimension): the Phase-52 report's own closing paragraph names the gap — the ~1/3 of random-stratum mismatches "whose error-vs-defensible composition was NOT adjudicated," and "a clean decomposition would require running the forced-pairwise adjudication on the SAME random-stratum mismatches." Phase 53 IS that decomposition: enlarge the random sample, adjudicate its mismatches forced-pairwise (blind code vs committed, neutral order), + add chance-corrected kappa (closes the report's named "not chance-corrected" gap). NON-ship, read-only, corpus byte-frozen (the Phase-51/52 envelope). Chosen at the gate over: an independent-rater panel + Krippendorff's alpha (needs a genuinely non-Claude rater — the scarce ingredient, subagents are all Claude-family; deferred-with-owner) · the heading->capability determinism probe (a new dimension, Phase-37 thread; deferred) · rooting into the aml-casework audit-walk (higher-value but un-drivable from a signal-watch-rooted session).

all_accept: true (direction confirmed + the load-bearing weakest assumption A0 ACCEPTED at the gate; warn-tracked. A0 reshapes the decomposition headline from a clean split to a BOUND [error = lower bound / scatter = upper bound] because the adjudicator is same-family; A1 fixes the decomposition as the report's named missing measurement, not a new claim; A2 sets the larger-N illustrative-first posture with even-larger-N + consensus deferred; A3 is the standing non-ship byte-frozen abort rule.)

- A0 [HIGH — the load-bearing honesty assumption; T0 weakest] A same-Claude-family fresh blind rater adjudicating the random-stratum mismatches (its-class blind code vs the committed code, neutral seed-fixed order) is independent-ENOUGH to license the decomposition ONLY as a BOUND — it shares the biases that generated the neighbouring code, so it tends to rate its own scatter "both-defensible" -> the measured error-share is a LOWER bound and the defensible-scatter-share an UPPER bound. ACCEPT. If false (the split is presented as a clean/exact decomposition, or as genuine independence) -> overclaim, the measure-not-claim violation Phase 51 A0 / Phase 52 A0 forbid. Mitigation: report error as lower-bound / scatter as upper-bound throughout; genuine independence (a different model family / human) stays deferred-with-owner (the panel+alpha framing was the gate alternative, not chosen). revisit-status: BIT (mechanism differed — the conclusion held, the prediction did not). The BOUND held (error = LOWER bound 23/62=37%; committed-questionable = UPPER bound 38/62=61%), but the PREDICTED mechanism — same-family adjudicator inflates "both-defensible" by rating its own scatter generously — did NOT fire (both-defensible 1/62=1.6%). The actual confound is CONTEXT ASYMMETRY: both blind raters saw flag+red_flag only while the committed code had full document context, so the two flag-only raters side with each other against committed. A0's measure-not-claim spirit HELD (no number presented as correctness; the 61% explicitly framed as an unmeasured error+confound mix, not a committed-error rate) — but the bound lives in a different place than written. Follow-up reframed: an independent rater must be CONTEXT-MATCHED.
- A1 [HIGH] Decomposition validity: adjudicating the random-stratum MISMATCHES as forced-pairwise (blind vs committed, neutral order) is a meaningful decomposition of the ~1/3 disagreement into genuine-error / defensible-neighbor / committed-questionable -- the EXACT measurement the Phase-52 report names as missing ("the forced-pairwise adjudication on the same random-stratum mismatches"). ACCEPT. If false (the forced-pairwise cannot isolate error from scatter -- the adjudicator can't tell the codes apart on the flag+red_flag alone) -> the neither/escalate rate spikes and THAT is the honest finding (the dimension is un-adjudicable, not soft-but-adjudicable). Mitigation: neither/escalate is a first-class outcome (it was 0 in the Phase-52 divergence stratum -- a spike here is itself a result). revisit-status: HELD — the decomposition was a meaningful, valid measurement: it REFUTED the Phase-52 "plausibly scatter" guess (both-defensible 1/62=1.6%) and was adjudicable (neither/escalate 0/62 — no spike, the dimension is not un-adjudicable). The forced-pairwise DID isolate a sharp preference; the surprise was the DIRECTION confound (A0), not an inability to adjudicate.
- A2 [MED] Larger-N feasibility: a modestly enlarged random sample (n~72-96, seed-fixed, corpus-proportional from the 2,251) yields enough mismatches (~25-40 at the Phase-52 ~58% any-axis disagreement) to decompose with reportable (if wide) CIs, and Cohen's kappa (blind vs committed over the 28-C / 20-D vocab) is computable. ACCEPT. If false (too few mismatches / sparse-marginal kappa undefined or uselessly noisy) -> report the distribution as illustrative + surface the noise; even-larger N deferred-with-owner (the deliverable stays "reproducible machinery + honest first instance," the Phase-51/52 posture). revisit-status: HELD — n=96 yielded 62 mismatches (31 C + 31 D, ample); Cohen's kappa computed cleanly (C 0.658, D 0.646, ≈ raw 0.677 → genuine self-consistency, not chance); Wilson CIs reported. Minor deviation (non-material): the first blind rater dropped 1 of 96 items (duplicate-flag fintrac-guid-casinos/IND-17); a fresh single-item blind rater filled it (SendMessage unavailable to continue the original) — rebuilt fixture gids == the seeded sample, integrity verified.
- A3 [LOW] NON-ship guard + abort rule: the ship corpus (corpus.html / dist/corpus / all derived / the overlays) stays byte-frozen + read-only; build.py NEVER imports cd_correctness.py; data/cd-correctness/ is non-corpus; `--check all` 7/7 zero drift; the report stays a NON-ship doc (the blueprint-report.html / probe-history.md / corpus-redundancy-report.md class). ACCEPT. If false (any ship-artifact drift) -> STOP and surface (the standing abort rule), never re-baseline. revisit-status: HELD — `--check all` 7/7 zero drift (ship corpus + all 5 dists byte-identical); build.py has no cd_correctness/cd-correctness reference; data/cd-correctness/ is non-corpus; the report stays a NON-ship doc. No abort triggered.

## Phase 54 — C/D tag control: the measured-not-gated control made real (SR-11-7 ongoing monitoring) (2026-06-16)

> Direction = the user's REFRAME at the dev-plan direction question (chose "prototype the tag control" OVER a 4th point-measurement [the context-matched-rater close-out] OR the heading→capability determinism probe) + a rigor ESCALATION at the gate (chose "execute the independent effectiveness challenge ONCE this phase" over merely scheduling it — a control whose key activity has never run is also prose). Phase 54 makes the blueprint's §4–§5 "measured-not-gated" control class CONCRETE + EXECUTABLE for the C/D tag (the canonical unguarded/judgmental dimension) — closing the named-OPEN Phase-47 T2/T3 blocker ("the production-grade SR-11-7-class control DESIGN at scale — cadence, sampling, ownership, where it sits in the 5% charter — is not yet written"). The three measurement phases (51/52/53) were the instrument + standing baseline; the control is what they were FOR. It also EXECUTES the Phase-52/53 deferred follow-up once: a context-matched + genuinely-independent (local Qwen, a different model family) rater → the clean cross-family reliability number + Krippendorff's α that 52/53 left deferred. NON-ship, read-only, ship corpus byte-frozen (the Phase-51/52/53 envelope). SR-11-7 Pillar-2 (ongoing monitoring) + OSFI E-23 vocab from the aml-wiki, not guessed.

all_accept: true (warned + restated per gate protocol — the direction is the user's chosen reframe + a deliberate rigor escalation; positions landed accept. A0 reshapes the "execute once" deliverable's RISK into a probe-gate + named fallback [availability confirmed up; competence is T1's probe]; A1 fixes "context-matched" as the apples-to-apples definition that makes the new number a reliability number not a context-gap re-measurement; A2 isolates the model dependency at the build boundary so the replay core stays pure + deterministic; A3 the standing non-ship / no-blueprint-edit constraint.)

- A0 [HIGH — the T0 weakest assumption] The local different-family model (Qwen3.6-35B on 127.0.0.1:8080, confirmed UP this session) is available AND competent enough at context-matched closed-vocab C/D tagging to serve as the genuine independent rater the "execute once" deliverable needs. ACCEPT (probe-gated + named fallback). If false (the model is unavailable mid-phase, or cannot reliably emit an in-vocab C/D from the context-matched prompt — Qwen≈Opus on EXTRACTION per working-knowledge, but closed-vocab classification is a DIFFERENT task, unverified) → the clean cross-family number blocks. Mitigation: T1 `--probe` (the Phase-46 probe-gate pattern) verifies competence on a few items BEFORE the full run; on failure → fall back to an Opus-context-matched rater (removes the Phase-53 CONTEXT-asymmetry confound but not the same-family one — still a strictly cleaner number than Phase 53) and the independence leg reverts to DESIGNED-IN / scheduled-with-owner, surfaced at the checkpoint (the user adjudicates). The phase cannot hard-block. revisit-status: HELD (positive surprise) — the local Qwen3.6-35B was available AND competent: the T1 8-item probe was user-adjudicated COMPETENT (8/8 in-vocab, sane/adjudicable picks, hit the named C8/C14 neighbour, flagged a plausible committed mis-tag) → ran the full n=96 AS-IS; the named Opus-context-matched fallback was NOT needed, and the independence leg ran for real (not just scheduled). Closed-vocab classification proved a solvable task for the cross-family model.
- A1 [HIGH] "Context-matched" = giving the rater the SAME inputs the committed assignment had — the source-doc region for the indicator (gid = doc_id/ind_id → data/<source>/<doc_id>.md) + the 28+20 capability/data-source interview posture + the closed vocab — NOT the Phase-53 flag+red_flag-only frame. So Qwen-context-matched-vs-committed is an apples-to-apples RELIABILITY comparison, not a re-measurement of the Phase-53 context gap. ACCEPT. If false (the doc region + posture do not reconstruct the context the inverted-extraction loop actually used → a residual context gap survives) → the "clean number" still mixes reliability with a smaller context gap. Mitigation: document precisely what "matched" feeds; the cross-rater (Opus-blind-flag-only vs Qwen-context-matched) + Krippendorff-α still BOUND the family/context effects; the number is reported as consensus + context-matched, never as validated correctness. revisit-status: HELD — context-matched = source-doc region + the 28+20 posture + the closed vocab, so Qwen-cm-vs-committed is an apples-to-apples reliability number (C 0.604 / D 0.646 ≈ the same-family self-consistency 0.677; Wilson CIs overlap) — NOT a re-measurement of the Phase-53 context gap. Reported as consensus + context-matched + κ + Krippendorff-α + Wilson, never as validated correctness. CAVEAT (documented, not a bite): "context-matching resolves the Phase-53 confound" is an INFERENCE from the aggregate ~0.6 ≈ self-consistency, NOT a re-run of the forced-pairwise decomposition with the Qwen rater — it bounds the confound's DIRECTION, not its exact share (a Qwen forced-pairwise decomposition would convert the bound to a measured share — deferred-with-owner).
- A2 [MED] The deterministic replay core (cd_correctness.py) stays PURE stdlib / no-network — a SEPARATE dev-time companion (scripts/cd_rate_independent.py) is the ONLY code that calls the local model + produces the independent fixture; cd_correctness.py reads fixtures only; build.py imports NEITHER. ACCEPT. If false (model-calling/network code leaks into the replay core or the build boundary) → the measurement stops being deterministically replayable AND the build→authoring-layer boundary is violated. Mitigation: the companion is the news/corpus live-mode pattern (companion calls 127.0.0.1, fixture captured-then-replayed); cd_correctness.py + build.py stay model-free; --check all proves the boundary. revisit-status: HELD — cd_rate_independent.py is the ONLY model/network code (urllib → 127.0.0.1, produces the fixture); cd_correctness.py stayed pure stdlib / deterministic replay-from-fixtures; build.py imports NEITHER (T5 grep clean); --check all 7/7 zero drift proves the build boundary held.
- A3 [MED] The control ships as a STANDALONE non-ship doc (docs/cd-tag-control.md) + the executable harness; NO edit to docs/program-blueprint.md (it CITES §4–§5, the cd-correctness-report-cites-§13 pattern) — dodging the known blueprint-report.html hand-sync drift (the Phase-49 precedent); ship corpus byte-frozen, --check all 7/7; the independent fixture stores JUDGMENTS over committed indicators (no new external content → no fixture-promotion-allowlist concern); 127.0.0.1 model only, nothing leaves the machine. ACCEPT (the standing non-ship abort rule + the Phase-49 no-blueprint-edit precedent). If false (blueprint edited, or any ship-artifact drift) → blueprint-report.html hand-sync drift / STOP-and-surface. revisit-status: HELD — docs/cd-tag-control.md ships as a standalone non-ship doc citing §4–§5; `git diff --quiet docs/program-blueprint.md` clean (NO blueprint edit); --check all 7/7 zero drift (ship corpus + all 5 dists byte-identical); the independent fixture stores JUDGMENTS over committed indicators (no new external content → no allowlist concern); 127.0.0.1 model only. No abort triggered; the change set contains ZERO ship artifacts.

## Phase 55 — Cross-pillar end-to-end bridge: aggregation + verification (the 3-pillar full demo connected) (2026-06-17)

> Direction = the user's ask at the dev-plan gate: plan the bridges that connect the 3-pillar full demo, where the actual wiring may execute in sibling sessions but the AGGREGATION + FINAL DELIVERY sit in signal-watch. Shape question answered "verifier harness + walkthrough" (Option A: PROVE the join with a re-runnable cross-repo check, not narrate it) + A3-reject re-scoped to "launcher + live pillar-status" (a new dist/index.html front door is the one ship-surface addition). signal-watch delivers the SPINE this session (ratified serialization contract + the e2e_chain_check harness fixture-proven + the two sibling briefs + the launcher + the walkthrough); the --real verification (and the final delivery gate) is GATED on the two sibling sessions executing the persist (#1) + consume-real-bundle (#2) bridges. Grounded against sibling HEADs aml-substrate@bafc67d + aml-casework@0316580 (code-verified this session).

all_accept: false (A2 don't-know round 1 → DEFENDED by code [three-way C4↔IND-11↔C4 alignment verified] → accept; A3 reject round 1 [pure-non-ship wrong, a ship change is intended] → RE-SCOPED to the launcher + pillar-status → accept on the revised set. A0/A1 accept.)

- A0 [HIGH — the T0 weakest assumption] The §5a evidence serialization schema can be RATIFIED NOW — from integration-contract §2 + casework's hand-mirrored contract.py at current HEADs — accurately enough that the fixture-driven e2e_chain_check written here matches what aml-substrate actually emits once the persist bridge lands. ACCEPT. If false (the real §5a emission diverges from the ratified schema) → e2e_chain_check --real fails at first contact and sibling brief #1 needs a revision round. Mitigation: the harness IS the divergence detector — brief #1's acceptance criterion is "e2e_chain_check --real passes its substrate-side checks," so any hand-mirror drift surfaces at the join at the cheapest point; ratification reconciles contract §2 ⟷ casework contract.py at current HEADs before brief #1 ships; the synthetic fixture is built FROM the ratified schema. revisit-status: HELD (spine) — ratified §2 from casework's actual validate_bundle + the briefs; the harness IS the divergence detector. The review gate confirmed the id-mint rule is BYTE-IDENTICAL across the contract, the harness, and both sibling briefs (ran all three as code → identical hashes). The REAL-emission match is the gated BEAT-2 test (the casework brief's A0 names it as load-bearing) — not yet bitten because beat 2 hasn't run. **UPDATE 2026-06-17 (beat 2 ran — A0 did NOT bite):** the real emission (aml-substrate@df23bba `CASE-P-0010361`) matched byte-for-byte (5/5 alert_ids + dossier_id; all 5 flags byte-grounded in the SOURCE regulator md), adversarially confirmed by an independent join re-derivation (no harness); the full `e2e_chain_check --real` chain is CONNECTED. The hand-mirrored schema held on first real contact — the load-bearing risk resolved clean.
- A1 [HIGH] The end-to-end join can be verified by READING committed/vendored sibling outputs (the serialized evidence json + casework's signed-SAR json) with NO import of sibling code — the file-contract / vendored-pin pattern casework already uses for the frozen corpus. ACCEPT. If false (verification needs to import aml_substrate / aml_casework or run a sibling engine) → signal-watch couples to sibling code, violating the one-repo-per-pillar doctrine. Mitigation: the harness reads serialized files only; the casework corpus vendored-pin precedent (reads signal-watch@a75a136 derived/*.json as data) proves the read-only file-contract seam works; --selftest pins it offline. revisit-status: HELD — e2e_chain_check.py imports NO sibling code (review-gate grep-confirmed; the only import is signal-watch's own derive_signals.normalize); reads committed json only; --selftest pins it offline; build.py never imports the harness.
- A2 [MED] The demonstrable vertical slice is a C4-STRUCTURING case — the substrate can emit one, casework registers C4, and the corpus pin (IND-11) grounds it — so the first real wired case lands in the EXISTING grounding path with no corpus-pin widening. ACCEPT (don't-know round 1 → DEFENDED by code). If false → sibling brief #2 needs a corpus-pin-widening / capability-registration prerequisite first. Evidence (verified this session): aml-substrate monitor/detectors/structuring.py StructuringDetector capability="C4" is docstring-grounded in fin-2026-alert001:IND-11; that same pinned record (casework fixtures/corpus/fincen-alerts/derived/fin-2026-alert001.json) has id=IND-11 → capability=C4; casework grounding_replay.py registers "C4":_assert_c4_structuring (+ fail-closed default). A clean three-way alignment on the existing pin. revisit-status: HELD — code-verified three-way C4↔fin-2026-alert001:IND-11↔C4 alignment (don't-know→DEFENDED-by-code at the gate); the review gate confirmed IND-11→C4 is unique in the corpus. No corpus-pin widening, no new capability registration needed for the slice.
- A3 [MED] The ONE ship-surface addition is the launcher — a new self-contained offline dist/index.html (8th build target) front door linking the 5 existing artifacts + a cross-pillar panel rendering data/pillar-status.json (each bridge's state, regenerated from the harness output, inlined at build like every other artifact's config); the 5 EXISTING artifacts + their dists stay byte-frozen; --check all extends to 8/8; the console/triage are NOT re-opened (Option C declined). ACCEPT (reject round 1 of pure-non-ship → re-scoped). If false (a 6th-artifact creep, or any of the 5 existing dists drift, or the console is touched) → STOP-and-surface per the standing abort rule. Mitigation: the launcher follows the ship doctrine (single offline file, always-on "Illustrative data & outputs" badge, no fetch/LLM, config inlined at build); build.py gains a launcher target; the freeze guard re-asserts the 5 existing dists byte-identical at 8/8. revisit-status: HELD — --check all 8/8 zero drift, the 5 existing dists byte-identical, only dist/index.html added; console/triage untouched; the obsolete M1 stale-dist/index.html deletion in build.py was removed (DISCOVERY — it would have deleted the launcher). LATE BITE (adjacent, caught+fixed by the review gate): --real on the synthetic data/e2e/ fixtures would have flipped the committed pillar-status to a FALSE green launcher (an honesty bite next to "the launcher reads as a claim") — fixed e95df82 (--real now refuses data/e2e/; signoff required; illustrative enforced on the signed SAR). SECOND late bite (beat 2, caught+fixed): a verification-workflow agent overstepped "do not modify" + committed e5a3138 — an incomplete all-green flip (stale launcher + stale grounding HEADs → --check all 1/8 drift); caught by the adversarial honesty/regression lens, undone (reset --soft, unpushed), redone clean in 10867ff (launcher rebuilt all-green, HEADs corrected df23bba/85602c1). Process learning: workflow agents keep git via Bash even under read-only prompts.

## Phase 56 — Chain workbench: the HTML analyst UI (companion-served live consume + verify over a pre-baked case library) (2026-06-17)

> Direction = the user wants an HTML frontend as the UI for the substrate→casework→verify chain, "living in signal-watch." Reframed (the user's "detection doesn't need runtime gen" + the scope/gate choices) from "orchestrator that runs the pipeline" to an ANALYST CASE-WORKBENCH: detection pre-baked/upstream (evidence bundles vendored like the corpus pin); the casework consume (6 verifiers + a LIVE neural SAR draft) + the cross-pillar verify run LIVE per case, stage-streamed. Dev-time companion (serve_chain.py + chain.html), NEVER a ship artifact (offline dists byte-frozen). A3 (defer live neural) REJECTED → live drafting pulled into v1 → the casework consume CLI is the one sibling prerequisite. Grounded against aml-substrate@df23bba + aml-casework@85602c1.

all_accept: false (A3 reject round 1 [defer-neural] → live drafting pulled into v1 → the revised set A1'/A4/A5 accepted; A0/A2 held round 1).

- A0 [HIGH] Detection is pre-baked/upstream — the substrate evidence BUNDLES are vendored (the case library); the cross-pillar grounding/verify is the LIVE, re-runnable proof ("the gate is the oracle"). ACCEPT. If false (the demo needs live detection/gen to be credible) → a live-gen hot path (slow, gen-frozen). Mitigation: frame as "detection runs upstream/batch as in a real system; this is the analyst's runtime loop"; the bundle is the analyst-facing input, exactly like a real alert. revisit-status: held (spine) — the vendored bundle IS the analyst input + the LIVE re-runnable verify is the proof: serve_chain --selftest streams a stubbed run → e2e_chain_check --real CONNECTED on the real vendored CASE-P-0010361; gate-is-oracle stands.
- A2 [MED] The case library = committed substrate evidence bundles (synthetic/illustrative), vendored into signal-watch like the corpus pin; CASE-P-0010361 + room for more. (The SAR is LIVE-drafted, not vendored — a cached SAR is the keyless fallback.) ACCEPT. If false (bundles not vendorable) → runtime gen. Mitigation: all synthetic, no real data; the committed CASE-P-0010361 proves vendorability. revisit-status: held — CASE-P-0010361 vendored byte-identical (sha1 bac574d…, illustrative:true) + a manifest; validate_chain_cases reuses check_substrate (schema + §2 id-mint + corpus grounding) + manifest↔bundle integrity; --check all 8/8 (not a build target). Vendorability proven; room for more cases by design.
- A4 [HIGH — the T0 weakest] casework can expose a thin per-case consume CLI cheaply — a ~15-line wrapper over its EXISTING functions (load_real_bundle / generate_narrative / ClaudeDrafter / record_signoff / emit_signed_sar), authored via a brief + run in a casework-rooted session. v1's live drafting is GATED on it. ACCEPT (defensible — the 5 functions exist; only the entrypoint is missing). If false (casework needs deeper changes) → v1 blocks on a bigger sibling change. Mitigation: the brief specs the thin wrapper; the companion's --selftest STUBS the subprocess so the signal-watch spine is provable offline before the CLI lands. revisit-status: pending beat 2 (gated, untested on the sibling) — the spine STUBS the subprocess (selftest green offline), brief written. EARLY WRINKLE recorded in the brief: casework's stub drafter (FixedDraftStub) is TEST-ONLY (tests/_strata.py) + fixed-content, so the CLI's --drafter stub needs a small SRC-resident bundle-derived stub — slightly more than the "~15-line wrapper"; the load-bearing open question moved to brief-A0 (can a generated narrative satisfy narrative_grounding). The neural-path wrapper stays thin; the casework session settles cheapness + brief-A0.
- A1' [HIGH] The companion (serve_chain.py) subprocesses casework's consume CLI + signal-watch's OWN e2e_chain_check — subprocess + file handoff, NO imports of either sibling; a DEV-TIME companion, never a ship artifact (offline dists byte-identical, --check all 8/8). ACCEPT. If false (import / dist drift) → coupling / ship regression. Mitigation: subprocess-only (the news/corpus live-mode pattern extended to drive a sibling CLI); chain.html is NOT a build target; the freeze guard holds 8/8. revisit-status: held — serve_chain.py subprocesses casework's CLI + signal-watch's OWN e2e_chain_check; grep clean (no import aml_substrate/aml_casework); chain.html not a build target, not in dist/; --check all 8/8 zero drift; pillar-status.json snapshot+restored so a workbench run never drifts the launcher.
- A5 [MED] The neural draft uses a SERVER-SIDE ANTHROPIC_API_KEY (companion env, never the browser), with the deterministic stub drafter as the no-key fallback — the demo runs keyless (deterministic SAR) AND with a key (live neural). ACCEPT. If false (key in frontend / can't run keyless) → non-negotiable violation. Mitigation: the key stays in the companion process; --drafter stub|claude; stub when no key (the "Copilot is NOT a web backend" + local-companion pattern). revisit-status: held (design+spine) — drafter_for_env() returns claude iff ANTHROPIC_API_KEY else stub (server-side only, never inlined into the page); selftest proves the key→stub selection both ways; the brief specs a fail-soft claude→stub fallback. The actual neural draft is beat 2, but the keyless-runs + key-server-side invariants are in place and tested.
