# Active Phase Context

**Phase 69 — *Evidence-sufficiency filing control: requirement profile → complete-or-honestly-gapped assembly → differentiated determination*** (signal-watch-local, LITE) — DELIVERED + accepted 2026-06-22. **No active signal-watch-local phase now — run `/dev-plan` for the next.**

## Status
**COMPLETE — T1–T6 ALL [x]; delivery accepted by the user 2026-06-22 (LITE).** The orchestrator flips the delivery gate after the commit verifies. Folded the user's two product-coherence defects into ONE companion-only control — evidence-sufficiency — over a shared per-typology evidence-requirement profile: completeness is the MEASUREMENT (honest-NULL gaps + a completeness panel); the decision is the SUFFICIENCY VERDICT (sufficient → a differentiated determination; insufficient → needs-more-info NAMING a §12 gap to build in aml-substrate). Supersedes the Phase-64 frequency-route (n_precedent → context). NEW `scripts/evidence_requirements.py` + `data/workbench/evidence-requirements.json` + `docs/evidence-driven-filing.md`. The ML bar = mechanism + ≥2 legs + named predicate risk + no unrebutted mitigation (A6/A7 mitigation atoms + A4 network); TF dropped. LIVE-once: ZERO fabrications, honest withhold. A4 partial (honest): population all-ML → kyc/TF profiles authored but unexercised. The no-ghost C5 fix (adversarial review). Companion-only; `--check all` 8/8 ZERO dist drift; build.py imports none of it; `uv run pytest` 19/19.

## Phase 70+ candidates
Roll the sufficiency model across the triage + gate consoles (still disposition-only) · substrate names/UBO-at-source + the SoF/anticipated-activity/income capabilities the §12 briefs name (sibling aml-substrate) · a kyc/TF case (needs a substrate slice emitting C14/TF detectors) · the live gather under-extracts corroboration findings (a Phase-65/66 gather-quality follow-on).

## Gates
- [x] spec — waived under LITE ceremony (the assumption-ledger gate IS the direction gate, Phase-67/68 precedent)
- [x] Direction confirmed by user (assumption positions taken 2026-06-22; A1/A2 accept, A3 resolved-local, A4 reconciled all-typology)
- [x] Delivery accepted (post-implementation report 2026-06-22; committed + pushed to main)

Plan [[phases/phase-69-evidence-sufficiency-filing-control]]; ledger Phase-69.
