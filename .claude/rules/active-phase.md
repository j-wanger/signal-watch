# Active Phase Context

Phase: 60 — **Consume the landed sibling halves (substrate P16-18 + casework P10-12): the C7 reachable-now rise + the real e2e chain**. signal-watch-local, LITE, NON-ship. A cross-pillar review (code-verified this session) found BOTH siblings moved 3 phases past the Phase-59 pins: **aml-substrate@9c75c03** [Phase 18 — §13 fourth-null closure (P16) · party-bearing bundle + `--screen` (P17) · emits the real C8 party-bearing **v0.2** evidence bundle, sample `CASE-P-0000251.json` (P18)]; **aml-casework@c6d8401** [Phase 12 — **C7** assertion (P10) · **C8** assertion (P11) · **C14** party-leaf assertion (P12); **C26** deliberately UNREGISTERED, the honest null]. Phase 59 A2 said reachable-now rises only when a party-bearing emission (substrate) + the paired grounding_replay assertions (casework) BOTH land — they now have. This phase consumes them.

Objective: re-ground the coverage map to the current sibling HEADs, **verify-first then re-freeze**, and run the **real** e2e chain on substrate's C8 bundle → a casework SAR. The VERIFIED consequence of the 3-way AND (has_detector ∧ has_casework_assertion ∧ behavior_emergence=="emerges"): **only C7 moves** (the one screening cap whose behavior emerges) → ALL 78 of its buildable signals become reachable-now — 62 direct + 16 proxy (`is_reachable` fires before the data_source_class fall-through, so the 16 C7 in needs-behavior(15)/needs-view-exposure(1) flip too, not just the 62 needs-detector) → **reachable-now 93→171 (+78)**, the first REAL rise — the C7 win banked for a combined consume. **C8/C14/C26 do NOT move** (behavior_emergence data-only for C8/C14, absent for C26 — the binding gap is emergent behavior, NOT the cross-pillar wiring that just completed). Honest finding: Phase 59's "both halves must land" sharpens to "only the cap whose behavior genuinely emerges reaches reachable-now."

Direction gate (2026-06-19, all_accept: true): the user up-scoped Q1 to the **COMBINED scope** (coverage-map consume + the real e2e chain) over the recommended coverage-only focus — a deliberate scale-up batching the full landed sibling work — and chose **"verify first, then freeze"** on the C7 +78. A0 (clean C7-only +78) is VERIFY-FIRST: re-ground → run map → diff tier counts → confirm BEFORE the freeze (the Phase-59 A0 pattern). A1 (C8/C14/C26 don't move — emergent behavior is the bottleneck) held by code-verified pin values. A2 (the e2e chain is honest-either-way: CONNECTED or a surfaced contract-drift GAP) / A3 (non-ship) accepted. Ledger: Phase-60 block. Grounded against aml-substrate@9c75c03 + aml-casework@c6d8401 + corpus@472b44e.

Scope: `data/coverage-map/**` (re-grounded pin + re-frozen coverage.json) · `scripts/signal_coverage_map.py` (read-only verify; extend `--selftest` goldens to the new reality as a regression anchor only) · `scripts/e2e_chain_check.py` + `scripts/e2e/**` (re-ground GROUNDING_HEADS + real run) · `docs/corpus-substrate-coverage.md` · `docs/pillar-integration-contract.md` (§8) · `aml-substrate/docs/corpus-coverage-build-PLAN-BRIEF.md` + `aml-casework/docs/capability-assertions-PLAN-BRIEF.md` (sibling, re-grounded). NOT touched: the 8 build targets / offline dists; the committed corpus records + overlays (read-only); `build.py` never imports `signal_coverage_map.py` or `e2e_chain_check.py`; no sibling import (file-contract / vendored-pin only).

Key constraints:
- **Verify-first is a hard gate** (T1): re-ground → run the map → diff → confirm EXACTLY a clean C7-only +78 (reachable-now 93→171, needs-detector 62→0, needs-view-exposure 70→69, needs-behavior 296→281, C8/C14/C26 unmoved) AND that casework's C7 assertion (`_screen_c7_peer_anomaly`, P10) is green at c6d8401, BEFORE T2 freezes coverage.json. Any other delta → STOP-and-surface, do NOT freeze.
- The honest finding (A1): C8/C14/C26 stay put despite the now-landed casework C8/C14 assertions + substrate's party-bearing emission — `behavior_emergence` (data-only / absent) is the 3rd leg of the AND, neither emission nor assertion work. Further reachable-now rises need EMERGENT BEHAVIOR (substrate emergence-engine work, bottom-up) or are a permanent null (C26). The prose makes this explicit so the +78 isn't misread as "the wiring was the bottleneck."
- The e2e chain (T3) runs casework's deterministic pipeline as a TOOL to produce a real signed SAR (file-contract, NO import — not driving casework's dev-lifecycle). The outcome is recorded honestly either way: CONNECTED (spine on a real C8 chain) or GAP-FOUND (a real cross-pillar finding, e.g. v0.2 not yet adopted casework-side — never papered over). Fallback: hand the `--real` run to a casework-rooted session if casework can't run from here.
- Re-ground before commit: both cross-pillar artifacts re-grounded against the sibling current HEAD, HEADs pinned inline (2026-06-16 process rule).

Exit criteria:
1. `signal_coverage_map.py --check` byte-identical (coverage.json re-frozen at the new HEADs, reachable-now 171) · `--selftest` green · `! grep import aml_substrate|aml_casework` · build.py never imports it.
2. `e2e_chain_check.py` GROUNDING_HEADS re-pinned (substrate@9c75c03, casework@c6d8401) · `--selftest` green · a `--real` run completed with an honest recorded outcome (CONNECTED or documented GAP) · no sibling import.
3. `docs/corpus-substrate-coverage.md` + contract §8: the measured C7 rise (93→171, +78, 62 direct + 16 proxy, first real rise) + the C8/C14/C26 emergence-gap finding + the e2e outcome; HEADs re-pinned. Both sibling briefs re-grounded: substrate detector/view + party-bearing-emission half DONE → next = emergence-engine work; casework C7/C8/C14 assertions DONE (C26 honest null) → next per its roadmap; shared acceptance = further rises need emergent behavior; doctrine constraint (no behavior/label stamping).
4. `python3 scripts/build.py --check all` → 8/8 (committed == fresh build). REVISED at impl (A3 bit, user Option-A 2026-06-19): the e2e re-ground cascades into the launcher ship dist via `data/pillar-status.json`, so the constraint is **7/8 ship dists byte-identical + the ONE sanctioned launcher re-ground** (`dist/index.html`, grounding_heads only — the Phase-55/57 pattern), NOT zero ship artifacts. Verified: `git diff --stat HEAD -- dist/` shows only `dist/index.html` moved (2 lines).

Abort: any of the 8 offline dists drift / a ship artifact touched → STOP and surface (never re-baseline). The C7 +78 verify (T1) comes back as anything but a clean C7-only +78 → STOP, do NOT freeze. A brief that stamps behavior or labels → out of bounds (emergence doctrine). The companion importing sibling code → out of bounds (vendored-pin / file-contract only). A validator/selftest looks like it needs loosening → fix the data/design, never the check.

Gates:
- [x] Direction confirmed by user (assumption positions taken 2026-06-19; combined scope + verify-first, both accepts; no unresolved reject/don't-know)
- [x] Delivery accepted (post-implementation report — user accepted via /dev-debrief, 2026-06-19; A0 held-with-correction [+62→+78], A1/A2 held, A3 bit [launcher cascade → Option-A])

Plan [[phases/phase-60-consume-landed-sibling-halves]]; ledger Phase-60.

---

## Standing program context (durable — not a changelog)

**Program structure (Phase 50, parallel-pillar):** signal-watch is the program-ARCHITECTURE home (the blueprint `docs/program-blueprint.md` + this lifecycle record only). The real build lives one repo per pillar:
- Pillar 1 = data substrate → **`/Users/jwang/aml-substrate`** (current HEAD **9c75c03**, Phase 18 — corpus detectors + PartyView (P15) · §13 fourth-null closure (P16) · party-bearing bundle + `--screen` (P17) · emits the real C8 party-bearing v0.2 evidence bundle (P18); the measured TRIPLE-NULL stands [composition never required to detect laundering]; composition detection-lift RETIRED).
- Pillar 2 = case-investigation → composition → SAR/STR narrative → **`/Users/jwang/aml-casework`** (current HEAD **c6d8401**, Phase 12 — 6 Class-G verifiers + the real neural narrator + pluggable drafter backends openai/opencode (P9); grounding_replay assertions C7 (P10) · C8 (P11) · C14 party-leaf reference-by-path (P12); C26 deliberately unregistered [honest null]; corpus_grounding ENFORCED-real against the vendored frozen-corpus pin).
- signal-watch owns the cross-pillar **integration contract** `docs/pillar-integration-contract.md` + the e2e harness `scripts/e2e_chain_check.py` + the coverage map `scripts/signal_coverage_map.py`.

**Doctrine (aml-substrate):** data-first/emergent (typologies EMERGE from modeled behavior, NEVER injected/stamped); deterministically SCRIPTED generation, no runtime LLM; grounded in researched REAL schemas + distributions; everything SYNTHETIC, no real customer data, ever. The corpus may drive DETECTOR + observable-exposure design TOP-DOWN; data generation + labels stay BOTTOM-UP. The coverage map MEASURES behavioral gaps, never stamps them (Phase 58 A0 / Phase 59 A1 / Phase 60 A1).

**FROZEN (signal-watch, no further demo work unless re-opened):** the 5 ship artifacts + dists byte-identical (index.html + config + 3 typology dists; corpus.html + dist/corpus; news.html + dist/news; console.html + dist/console; triage.html + dist/triage) + the launcher dist/index.html (8th target); derive_signals.py; the news pipeline; ALL committed derived data + the 3 overlays; docs/program-blueprint.md + blueprint-report.html. `--check all` → 8/8.

**Process rules:** (1) any cross-pillar artifact MUST be re-grounded against the sibling repo's CURRENT measured HEAD before commit/consume, HEADs pinned inline. (2) This file + _CURRENT_STATE drift between cross-pillar reviews — trust each sibling repo's CURRENT HEAD for its live state. (3) signal-watch-rooted sessions CANNOT drive the siblings' dev-* lifecycle (their hooks bind there) — but MAY run a sibling's tool to produce an output for the file-contract (the e2e `--real` chain), which is tool-use, not lifecycle.

**Prior signal-watch-local phases (detail in `_CURRENT_STATE.md` + the journal):** the measure-first workstream (51 corpus redundancy · 52/53/54 C/D-tag reliability→control) · the cross-pillar bridge (55 e2e · 56 chain workbench · 57 neural drafter backends) · 58 the corpus→substrate coverage map · 59 consume substrate Phase 15 (PartyView+detectors exposed, reachable-now held at 93 — the substrate half satisfied only exposure). Phase 60 consumes the now-landed BOTH halves → the first real reachable-now rise (C7 +78 → 171).
